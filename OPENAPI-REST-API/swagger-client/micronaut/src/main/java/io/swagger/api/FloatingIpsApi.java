/**
 * NOTE: This class is auto generated by the swagger code generator program (3.0.81).
 * https://github.com/swagger-api/swagger-codegen
 * Do not edit the class manually.
 */
package io.swagger.api;

import io.swagger.model.ChargeInvoiceRows;
import io.swagger.model.FloatingIpOrderRequest;
import io.swagger.model.InlineResponse2004;
import io.swagger.model.InlineResponse401;
import io.swagger.model.IpObject;
import io.swagger.model.ServiceOrderPostResponse;
import io.swagger.model.SuccessTextResponse;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.micronaut.http.*;
import io.micronaut.http.annotation.*;
import io.reactivex.rxjava3.core.Single;
import io.swagger.v3.oas.annotations.*;
import io.swagger.v3.oas.annotations.responses.*;

import javax.annotation.Nullable;
import javax.validation.Valid;
import javax.validation.constraints.*;
import java.io.IOException;
import java.util.List;
import java.util.Map;


public interface FloatingIpsApi {


    @Operation(summary = "Place a real Floating IP order, create billing records, and provision the service", operationId = "addFloatingIp", description = "Charges the customer and creates a new Floating IP service via `place_buy_floating_ip`. Validate first with `putFloating_ips` to avoid surprise failures. Body (form-encoded): `serviceType` (required, `services_id`), `coupon` (optional), `comment` (optional internal note). On success returns `{ continue:true, errors, total_cost, iid, iids, real_iids, serviceId, invoice_description, cj_params }` — `iid` is the master invoice ID, `serviceId` is the new `floating_ip_id`. On validation failure returns `{ continue:false, errors:[...] }` with no charge. Errors: 401 if unauthenticated; soft errors in `errors[]`. The newly-issued IP starts unassigned — point it at a target with `postFloatingIpsChangeIp` once the service is `active`.  Sibling ops: `getNewFloatingIp` (catalog), `putFloating_ips` (validate), `getFloatingIpInfo` (poll), `postFloatingIpsChangeIp` (route), `getBillingInvoice` + `initiatePayment` (settle invoice), `floating_ipsCancel`." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Order placed successfully. Use the invoice ID to proceed to payment via `/billing/pay/{method}/{invoices}` or view the invoice at `/billing/invoices/{id}`.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/floating_ips/order", produces = { "application/json" }, consumes = {"application/json"})
    default Single<HttpResponse<ServiceOrderPostResponse>> addFloatingIp(@NotNull @Valid @Parameter(description = "") @Body FloatingIpOrderRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Cancel a Floating IP service and release the IP — destructive, billing stops", operationId = "floatingIpsCancel", description = "Cancels the Floating IP via the shared `Api\\Billing\\CancelService` flow — flips status to canceled, halts recurring billing, and releases the IP back to the pool so it can no longer be re-routed. Not reversible: the customer cannot recover the same IP after release. Path param `id` (`floating_ip_id` from `getFloatingIpsList`). No body. Returns the `FloatingIpsCancelResponse` shape (success text / cancellation outcome). Errors: 401 if unauthenticated; 404 / cross-customer hidden when `id` is not owned by the caller; 409 if already canceled or otherwise non-cancelable. Confirm with the customer before calling — for routing changes use `postFloatingIpsChangeIp` instead of cancel-and-reorder.  Sibling ops: `getFloatingIpInfo` (status), `getFloatingIpInvoices` (outstanding charges), `postFloatingIpsChangeIp` (re-route instead of cancel), `addFloatingIp` (re-order)." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Floating Ip Cancel")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Delete(value = "/floating_ips/{id}", produces = { "application/json" })
    default Single<HttpResponse<InlineResponse2004>> floatingIpsCancel(@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Fetch full details for one Floating IP service, including current target IP", operationId = "getFloatingIpInfo", description = "Use for a Floating IP detail screen, or to read `floating_ip_ip` / `floating_ip_target_ip` before calling `postFloatingIpsChangeIp`. Read-only. Path param `id` (integer, `floating_ip_id` from `getFloatingIpsList`). No body. Returns the `ViewFloatingIp.getDetails()` payload — service info, billing/cost summary, status, target IP, and `client_links` (action URLs the UI can render). Internal-only fields (`admin_links`, `settings`, `csrf`) are stripped. Errors: 401 if unauthenticated; effectively 404 / cross-customer hidden when `id` is not owned by the caller (`get_service` filters by custid). Siblings: `postFloatingIpsChangeIp`, `updateFloatingIpInfo`, `getFloatingIpInvoices`, `getFloatingIpsWelcomeEmail`, `floating_ipsCancel`." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Detailed Floating IP service information.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/floating_ips/{id}", produces = { "application/json" })
    default Single<HttpResponse<Object>> getFloatingIpInfo(@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List all billing invoices charged against a specific Floating IP service", operationId = "getFloatingIpInvoices", description = "Use for a per-service billing history view — pulls the standard `Api\\Billing\\InvoicesList` rows scoped to this Floating IP. Read-only. Path param `id` (`floating_ip_id` from `getFloatingIpsList`). No body. Returns the `ChargeInvoiceRows` schema: array of invoice rows with id, date, amount, status, etc. Use the invoice IDs with the global billing endpoints (`getBillingInvoice`, `initiatePayment`) for line-item detail. Errors: 401 if unauthenticated; effectively 404 / cross-customer hidden when `id` is not owned by the caller. Siblings: `getFloatingIpInfo` (service details), `getFloatingIpsWelcomeEmail`." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Get Invoices response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/floating_ips/{id}/invoices", produces = { "application/json" })
    default Single<HttpResponse<ChargeInvoiceRows>> getFloatingIpInvoices(@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List all Floating IP services on the authenticated customer's account", operationId = "getFloatingIpsList", description = "Use to enumerate every Floating IP the caller owns before drilling into a specific one. Read-only; safe to call frequently. No params, no body. Returns an array of rows: `floating_ip_id`, `repeat_invoices_cost` (recurring price), `floating_ip_ip` (the portable IP), `floating_ip_target_ip` (the IP it currently routes to), `floating_ip_status` (active/pending/canceled/etc.), `services_name` (package label). Empty array if the account owns no Floating IPs. Errors: 401 if unauthenticated. Use returned IDs with `getFloatingIpInfo`, `postFloatingIpsChangeIp`, `getFloatingIpInvoices`, `getFloatingIpsWelcomeEmail`, or `floating_ipsCancel`. To order a new one see `getNewFloatingIp` / `addFloatingIp`.  Sibling ops: `getFloatingIpInfo`, `getNewFloatingIp` (catalog), `addFloatingIp` (order)." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "The listing of `Floating IPs` services on your account.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @ApiResponse(responseCode = "200", description = "Default response")
    @Get(value = "/floating_ips", produces = { "application/json" })
    default Single<HttpResponse<List<Object>>> getFloatingIpsList() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Resend the Floating IP welcome / setup email to the account contact", operationId = "getFloatingIpsWelcomeEmail", description = "Triggers `floating_ip_welcome_email($id)` to re-deliver the original setup email (the IP, routing instructions, etc.) to the customer's on-file address. Useful when the email was lost or the customer needs the IP/setup details again. No body, no params besides path `id` (`floating_ip_id`). Returns `{ text: 'Welcome Email has been resent.' }`. Errors: 401 if unauthenticated; 404 (`Invalid Service Passed`) if `id` is not owned by the caller; 409 (`Service is not active`) if status is not `active`. Side effect: sends an outbound email — avoid in tight loops. Read state first via `getFloatingIpInfo` if unsure of status.  Sibling ops: `getFloatingIpInfo` (status), `addFloatingIp` (new order), `floating_ipsCancel`." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/floating_ips/{id}/welcome_email", produces = { "application/json" })
    default Single<HttpResponse<SuccessTextResponse>> getFloatingIpsWelcomeEmail(@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get pricing and service-type options for ordering a new Floating IP", operationId = "getNewFloatingIp", description = "Use before showing a Floating IP order form, or before calling `addFloatingIp`, to discover which service types (`serviceTypes`) and prices (`packageCosts`, keyed by `services_id` in the customer's currency) are currently buyable. Read-only; no side effects. No params, no body. Returns `{ packageCosts: { <services_id>: <cost> }, serviceTypes: [ ... ] } `. Costs are `services.services_cost` filtered to `services_buyable=1` for module `floating_ips`. Errors: 401 if unauthenticated. Next steps: validate the chosen `serviceType` with `putFloating_ips`, then place the order with `addFloatingIp`. Floating IPs are portable IPv4 addresses that route to a target IP on one of the customer's active services.  Sibling ops: `putFloating_ips` (validate), `addFloatingIp` (commit), `getFloatingIpsList` (existing IPs)." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Available options and pricing for ordering a Floating IP.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/floating_ips/order", produces = { "application/json" })
    default Single<HttpResponse<Object>> getNewFloatingIp() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Re-point a Floating IP to a different target IP on one of the customer's services", operationId = "postFloatingIpsChangeIp", description = "Reattaches the Floating IP by removing the old static route on the source switch and adding a new one on the destination switch (via `Sshwitch`), then updates `floating_ip_target_ip`. Use to move a portable IP between the customer's VPS / Quickservers / websites / dedicated servers without renumbering apps. Path param `id` (`floating_ip_id`). Body: `{ ip: <new target IP> }` (also accepts multipart form). Returns `{ success:true, text:'IP Changed' }`. Errors (returned via `json_error`): invalid IP format; IP not in our datacenter; IP not in use by an active service of this customer; service not active; another Floating IP already points to that target; switch lookup failures; route still present after removal. 401 if unauthenticated.  Sibling ops: `getFloatingIpInfo` (read current target), `getFloatingIpsList`, `floating_ipsCancel`. Read current target with `getFloatingIpInfo` first." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/floating_ips/{id}/change_ip", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<SuccessTextResponse>> postFloatingIpsChangeIp(@NotNull @Parameter(description = "") @Body(value = "ip")  String ip,@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Re-point a Floating IP to a different target IP on one of the customer's services", operationId = "postFloatingIpsChangeIp", description = "Reattaches the Floating IP by removing the old static route on the source switch and adding a new one on the destination switch (via `Sshwitch`), then updates `floating_ip_target_ip`. Use to move a portable IP between the customer's VPS / Quickservers / websites / dedicated servers without renumbering apps. Path param `id` (`floating_ip_id`). Body: `{ ip: <new target IP> }` (also accepts multipart form). Returns `{ success:true, text:'IP Changed' }`. Errors (returned via `json_error`): invalid IP format; IP not in our datacenter; IP not in use by an active service of this customer; service not active; another Floating IP already points to that target; switch lookup failures; route still present after removal. 401 if unauthenticated.  Sibling ops: `getFloatingIpInfo` (read current target), `getFloatingIpsList`, `floating_ipsCancel`. Read current target with `getFloatingIpInfo` first." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/floating_ips/{id}/change_ip", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<SuccessTextResponse>> postFloatingIpsChangeIp(@NotNull @Valid @Parameter(description = "") @Body IpObject body,@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Validate a Floating IP order and price it without charging the customer", operationId = "putFloatingIps", description = "Dry-run for `addFloatingIp` — runs `validate_buy_floating_ip` to apply coupons, compute intro/repeat pricing, and surface errors before committing. No charge, no service created. Body fields (form-encoded): `serviceType` (required, `services_id` from `getNewFloatingIp.packageCosts`), `coupon` (optional code). Returns `{ continue, errors, serviceType, serviceCost, originalCost, repeatServiceCost, password, introFrequency, coupon, couponCode }`. `continue=true` means the order would succeed; `continue=false` plus populated `errors[]` means it would not. Errors: 401 if unauthenticated; 422-style soft errors arrive in the `errors` array. Use the returned `serviceType` and `couponCode` when calling `addFloatingIp`. Sibling ops: `getNewFloatingIp` (catalog), `addFloatingIp` (commit)." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "Validate Floating IPs order response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Put(value = "/floating_ips/order", produces = { "application/json" }, consumes = {"application/json"})
    default Single<HttpResponse<Void>> putFloatingIps(@NotNull @Valid @Parameter(description = "") @Body FloatingIpOrderRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Update a Floating IP service's editable settings (label / metadata)", operationId = "updateFloatingIpInfo", description = "Stub edit endpoint that delegates to the same handler as `getFloatingIpInfo` — currently used for label/metadata edits surfaced by `ViewFloatingIp`. To re-route the IP to a different target use the dedicated `postFloatingIpsChangeIp` instead; this op does not change routing. Path param `id` (`floating_ip_id`). Body: form-encoded fields exposed by the Floating IP edit form (label/comment style). Returns the standard success-text response. Errors: 401 if unauthenticated; effectively 404 if `id` not owned by the caller. Read state first with `getFloatingIpInfo`.  Sibling ops: `getFloatingIpInfo` (read), `postFloatingIpsChangeIp` (re-route), `floating_ipsCancel`." , tags = {"Floating_IPs"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/floating_ips/{id}", produces = { "application/json" })
    default Single<HttpResponse<SuccessTextResponse>> updateFloatingIpInfo(@Parameter(description = "The Floating IP service ID. Use the ID from `GET /floating_ips`.") @PathVariable("id") String id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }

}
