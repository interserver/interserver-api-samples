/**
 * NOTE: This class is auto generated by the swagger code generator program (3.0.81).
 * https://github.com/swagger-api/swagger-codegen
 * Do not edit the class manually.
 */
package io.swagger.api;

import io.swagger.model.ChargeInvoiceRows;
import io.swagger.model.IdBackupsBody;
import io.swagger.model.InlineResponse20011;
import io.swagger.model.InlineResponse20012;
import io.swagger.model.InlineResponse401;
import io.swagger.model.QsOrderRequest;
import io.swagger.model.QueueResponse;
import io.swagger.model.Quickserver;
import io.swagger.model.QuickserverOrder;
import io.swagger.model.QuickserverRow;
import io.swagger.model.RestoreRequest;
import io.swagger.model.ReverseDnsEntries;
import io.swagger.model.ServiceOrderPostResponse;
import io.swagger.model.SuccessTextResponse;
import io.swagger.model.TextResponse;
import io.swagger.model.TimezoneUpdate;
import io.swagger.model.VpsBackupRows;
import io.swagger.model.VpsTemplatesList;
import com.fasterxml.jackson.databind.ObjectMapper;
import io.micronaut.http.*;
import io.micronaut.http.annotation.*;
import io.reactivex.rxjava3.core.Single;
import io.swagger.v3.oas.annotations.*;
import io.swagger.v3.oas.annotations.responses.*;

import javax.annotation.Nullable;
import javax.validation.Valid;
import javax.validation.constraints.*;
import java.io.IOException;
import java.util.List;
import java.util.Map;


public interface QsApi {


    @Operation(summary = "Place a QuickServer order, generating a real invoice and queuing provisioning", operationId = "addQs", description = "Commits the validated order: creates the service row, generates a real invoice, and queues provisioning. Body fields match `putQs` (`server`, `password`, `os`, `comment`, `tos`) — call `putQs` first to catch errors. On `validation.continue=false`, returns the joined error string with no charge. Returns: `ServiceOrderPostResponse` with the new service ID and invoice info. Pay via `getBillingInvoice`/`initiatePayment`. Errors: 401 if unauthenticated, 4xx with message on validation failure. Siblings: `putQs` (validate first), `getNewQs`, `addVps` (VPS equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Order placed successfully. Use the invoice ID to proceed to payment via `/billing/pay/{method}/{invoices}` or view the invoice at `/billing/invoices/{id}`.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/order", produces = { "application/json" }, consumes = {"application/json"})
    default Single<HttpResponse<ServiceOrderPostResponse>> addQs(@NotNull @Valid @Parameter(description = "") @Body QsOrderRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Permanently delete a QuickServer backup file from object storage", operationId = "deleteQsBackup", description = "Removes the backup from its storage backend. Irreversible — the backup cannot be recovered. Path param: `id`. Required: `file` (the backup `name` from `getQsBackups`, in query or form body). Works for `swift` and `minio` backups; `zfs` snapshots cannot be deleted via this endpoint (returns an error pointing to support). Returns: `SuccessTextResponse` with the removed name. Errors: 401, 404 if not owned, error message if backup type is unsupported or the storage operation fails. Siblings: `getQsBackups` (list), `downloadQsBackup` (PATCH), `postQuickServerRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Delete(value = "/qs/{id}/backups", produces = { "application/json" })
    default Single<HttpResponse<SuccessTextResponse>> deleteQsBackup(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id,@NotNull @Parameter(description = "The backup filename to delete.") @QueryValue(value = "file") String file,@Nullable @Parameter(description = "Set to `1` to list all backups across all services, not just the ones for the given QuickServer.") @QueryValue(value = "all") String all) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Block outbound SMTP traffic on a QuickServer to halt mail abuse", operationId = "doQsBlockSmtp", description = "Queues a firewall rule that drops outbound port 25 traffic, used to halt spam/abuse without taking the server offline. Path param: `id` (integer). No body. Returns: `{ text, queueId }`. Async — applied within ~2 minutes via the queue worker, which also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Reversible only by support — there is no `unblock_smtp` endpoint. Siblings: `doVpsBlockSmtp`, `getQsInfo`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/block_smtp", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsBlockSmtp(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Disable the virtual CD/DVD drive device on a QuickServer", operationId = "doQsDisableCd", description = "Queues removal of the virtual CD/DVD device from the QuickServer (full disable, not just eject). Path param: `id` (integer). No body. Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Siblings: `doQsEjectCd` (eject the ISO but keep drive), `postQsInsertCd` (mount an ISO), `getQsInsertCd` (list available ISOs)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/disable_cd", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsDisableCd(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Disable disk-quota enforcement at OS level on a QuickServer", operationId = "doQsDisableQuota", description = "Queues a job to turn off disk-quota enforcement at the OS level. Use when quota errors block legitimate writes or before resizing disk space. Path param: `id` (integer). No body. Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Re-enable later with `doQsEnableQuota`. Siblings: `doQsEnableQuota` (re-enable), `doVpsDisableQuota` (VPS equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/disable_quota", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsDisableQuota(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Eject the currently mounted ISO from a QuickServer's virtual CD drive", operationId = "doQsEjectCd", description = "Queues an eject — drive remains attached but no media. Path param: `id` (integer). No body. Returns: `{ text, queueId }`. Async — applied within ~2 minutes. The queue worker also re-runs VNC setup so the console reflects the change. Errors: 401, 404 if `id` is not owned by caller. Note: this handler does not validate `active` status. Siblings: `postQsInsertCd` (mount an ISO), `getQsInsertCd` (list ISOs), `doQsDisableCd` (remove the drive itself)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/eject_cd", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsEjectCd(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Enable disk-quota enforcement at OS level on a QuickServer", operationId = "doQsEnableQuota", description = "Queues a job to turn on disk-quota enforcement at the OS level. Pair with `doQsDisableQuota` when re-enabling after maintenance, disk resizing, or restoring a backup. Path param: `id` (integer). No body. Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Siblings: `doQsDisableQuota` (turn off), `doVpsEnableQuota` (VPS equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/enable_quota", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsEnableQuota(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Reboot a QuickServer with a graceful OS-level restart", operationId = "doQsRestart", description = "Queues a graceful restart — equivalent to `reboot` inside the OS. Path param: `id` (integer). No body. Use to recover from a hung service or apply pending kernel/config changes. Returns: `{ text, queueId }`. Async — server is back online within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on `active` status — restarts work even on suspended services. Siblings: `doQsStart`, `doQsStop`, `doVpsRestart`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/restart", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsRestart(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Power on a QuickServer that is currently stopped or pending boot", operationId = "doQsStart", description = "Queues a `start` command to bring the QuickServer online. Path param: `id` (integer). No body. Idempotent in practice — re-running on an already-on server is a no-op at the worker. Returns: `{ text, queueId }`. Async — typically online within ~2 minutes; queue worker re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on status, so it can be issued even for non-active services. Siblings: `doQsStop`, `doQsRestart`, `getQsInfo`, `doVpsStart`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/start", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsStart(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Power off a QuickServer with a graceful shutdown command", operationId = "doQsStop", description = "Queues a `stop` command. Path param: `id` (integer). No body. Use before maintenance, snapshot, or to halt traffic — billing continues regardless of power state, so use `quickserversCancel` to also stop charges. Returns: `{ text, queueId }`. Async — typically off within ~2 minutes; queue worker re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on status. Siblings: `doQsStart`, `doQsRestart`, `doVpsStop`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/stop", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> doQsStop(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Generate a 24-hour pre-signed download URL for a QuickServer backup", operationId = "downloadQsBackup", description = "Returns a temporary signed URL to fetch the backup directly from object storage. Path param: `id`. Body (JSON or form): `file` (the backup `name` from `getQsBackups`). Only available for `minio`-type backups; `swift` and `zfs` backups return an error directing the caller to contact support. URL expires in 24 hours. Returns: `{ text, url }`. Errors: 401, 404 if not owned, error message for unsupported backup type or sharing failure. Siblings: `getQsBackups` (list, get `name`), `deleteQsBackup`, `postQuickServerRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Download URL for the backup file.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Patch(value = "/qs/{id}/backups", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse20012>> downloadQsBackup(@NotNull @Valid @Parameter(description = "") @Body IdBackupsBody body,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id,@Nullable @Parameter(description = "Set to `1` to list all backups across all services, not just the ones for the given QuickServer.") @QueryValue(value = "all") String all) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Generate a 24-hour pre-signed download URL for a QuickServer backup", operationId = "downloadQsBackup", description = "Returns a temporary signed URL to fetch the backup directly from object storage. Path param: `id`. Body (JSON or form): `file` (the backup `name` from `getQsBackups`). Only available for `minio`-type backups; `swift` and `zfs` backups return an error directing the caller to contact support. URL expires in 24 hours. Returns: `{ text, url }`. Errors: 401, 404 if not owned, error message for unsupported backup type or sharing failure. Siblings: `getQsBackups` (list, get `name`), `deleteQsBackup`, `postQuickServerRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Download URL for the backup file.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Patch(value = "/qs/{id}/backups", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<InlineResponse20012>> downloadQsBackup(@NotNull @Parameter(description = "") @Body(value = "file")  String file,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id,@Nullable @Parameter(description = "Set to `1` to list all backups across all services, not just the ones for the given QuickServer.") @QueryValue(value = "all") String all) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get QuickServer order form metadata and available plans/templates", operationId = "getNewQs", description = "Use before placing or validating a QuickServer order to retrieve pricing, available servers, OS templates, and form fields. Read-only — no params, no body, no charge. Returns: `QuickserverOrder` schema with plan/template/server options used to build the order payload for `putQs` (validate) or `addQs` (place). Errors: 401 if unauthenticated. Siblings: `putQs` (dry-run validation), `addQs` (commits and invoices), `getNewVps` (virtual VPS ordering surface)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Quickserver Ordering information.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/order", produces = { "application/json" })
    default Single<HttpResponse<QuickserverOrder>> getNewQs() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Queue creation of a new QuickServer backup snapshot (note: GET triggers job)", operationId = "getQsBackup", description = "Note: GET on `/qs/{id}/backup` triggers a backup job — despite the verb, this is a state-changing action. Queues a `backup` operation; backup name is auto-generated. Path param: `id` (integer). Returns: `{ text, queueId }`. Async — backup completes in minutes to hours depending on disk size. Poll `getQsBackups` to see when it appears. Errors: 401 if unauthenticated, 404 if not owned by caller, 409 if status != `active`. Siblings: `getQsBackups` (list), `postQuickServerRestore`, `downloadQsBackup`, `deleteQsBackup`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/backup", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> getQsBackup(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List available QuickServer backups across Swift, MinIO, and ZFS storage", operationId = "getQsBackups", description = "Returns all backups visible to the caller for this QuickServer across the three backup backends. Path param: `id` (integer). Optional query `all=1` lists every backup the customer owns, not just this server's. Returns: `VpsBackupRows` array — each row has `name`, `type` (swift/minio/zfs), `size`, `service`, `path`. Use `name` (not a numeric ID) with `downloadQsBackup` (PATCH), `deleteQsBackup` (DELETE), or `postQuickServerRestore`. Errors: 401, 404 if not owned by caller. Siblings: `getQsBackup` (create), `postQuickServerRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "The listing of available backups for the QuickServer.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/backups", produces = { "application/json" })
    default Single<HttpResponse<VpsBackupRows>> getQsBackups(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id,@Nullable @Parameter(description = "Set to `1` to list all backups across all services, not just the ones for the given QuickServer.") @QueryValue(value = "all") String all) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get current QuickServer hostname plus change rules and platform support", operationId = "getQsChangeHostname", description = "Read-only probe before calling `postQsChangeHostname`. Path param: `id` (integer). Returns the current hostname and the validation rules the new hostname must satisfy. Returns: object with hostname metadata. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Note: hostname changes are only supported on OpenVZ/Virtuozzo platforms — `postQsChangeHostname` rejects KVM/dedicated types with an explanatory error. Siblings: `postQsChangeHostname`, `getVpsChangeHostname`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Change Hostname info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/change_hostname", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsChangeHostname(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get metadata for QuickServer root/OS password change requirements", operationId = "getQsChangeRootPassword", description = "Read-only probe before calling `postQsChangeRootPassword`. Path param: `id` (integer). Use to surface password complexity rules and confirm the QuickServer accepts root password changes. Returns: object with reset metadata. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Note: this changes the OS root password (Linux) — for the Webuzo control panel password use `postQsChangeWebuzoPassword`. Siblings: `postQsChangeRootPassword`, `postQsResetPassword` (random password), `getVpsChangeRootPassword`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Change Root Password response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/change_root_password", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsChangeRootPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List timezones the QuickServer can be set to via change_timezone", operationId = "getQsChangeTimezone", description = "Returns the system timezone catalog (parsed from `/usr/share/zoneinfo/zone.tab`) for use with `postQsChangeTimezone`. Path param: `id` (integer). Read-only — no queue, no charge. Returns: array of timezone strings (e.g. `America/New_York`, `Europe/London`). Errors: 401, 404 if not owned by caller, 409 if status != `active` (handler labels these errors as `Invalid VPS Passed` / `VPS is not active` due to shared code). Siblings: `postQsChangeTimezone` (commit), `getVpsChangeTimezone`, `getQsChangeHostname` (also informational)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Change Timezone info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/change_timezone", produces = { "application/json" })
    default Single<HttpResponse<List<String>>> getQsChangeTimezone(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get metadata for changing the Webuzo control panel admin password", operationId = "getQsChangeWebuzoPassword", description = "Read-only probe before `postQsChangeWebuzoPassword`. Path param: `id` (integer). Webuzo is a control panel optionally installed on QuickServers — its admin password is separate from the OS root password. Returns: object with change instructions. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Siblings: `postQsChangeWebuzoPassword`, `postQsChangeRootPassword` (OS root password), `postQsResetPassword`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Change Webuzo Password info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/change_webuzo_password", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsChangeWebuzoPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get full details for one QuickServer including credentials and links", operationId = "getQsInfo", description = "Returns the QuickServer dashboard payload — service info, IPs, hostname, OS, status, billing, and the list of available `client_links` (action endpoints the caller is allowed to invoke). Path param: `id` (integer QuickServer ID). Returns: `Quickserver` schema. Use response links to drive `doQsStart`, `doQsStop`, `doQsRestart`, `getQsBackups`, `getQsReinstallOs`, `getQsReverseDns`, `getQsInvoices`. Errors: 401 if unauthenticated, 404 if `id` is not owned by caller. Siblings: `updateQsInfo` (mutate), `quickserversCancel` (delete), `getVpsInfo` (VPS equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Quickserver details")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}", produces = { "application/json" })
    default Single<HttpResponse<Quickserver>> getQsInfo(@Parameter(description = "QuickServer ID number.") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List ISO images available to mount on a QuickServer's virtual CD", operationId = "getQsInsertCd", description = "Returns the catalog of bootable ISOs the caller can mount via `postQsInsertCd`. Path param: `id` (integer). Read-only — no queue, no charge. Returns: object with available ISO entries (URLs/labels) keyed for the QuickServer's hardware type. Errors: 401 if unauthenticated. Note: this handler does not validate ownership or active status — pair with `getQsInfo` first if you need those checks before presenting options to a user. Siblings: `postQsInsertCd` (mount the chosen URL), `doQsEjectCd`, `doQsDisableCd`, `getVpsInsertCd`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Insert CD info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/insert_cd", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsInsertCd(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List billing invoices charged for one QuickServer service", operationId = "getQsInvoices", description = "Returns invoices charged for this QuickServer (initial setup + recurring). Path param: `id` (integer). Returns: `ChargeInvoiceRows` — each row has invoice ID, amount, status (paid/unpaid), date. Use the invoice ID with `getBillingInvoice` for full detail or `initiatePayment` to settle. Errors: 401 if unauthenticated, 404 if not owned by caller. Siblings: `getQsInfo`, `getVpsInvoices`, `getBillingInvoice`, `quickserversCancel` (check next-invoice date before canceling)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Get Invoices response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/invoices", produces = { "application/json" })
    default Single<HttpResponse<ChargeInvoiceRows>> getQsInvoices(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List QuickServer rapid-deploy dedicated servers on the account", operationId = "getQsList", description = "Use to enumerate the caller's QuickServers (quick-provision physical dedicated boxes that share the VPS billing model). No params, no body. Each row has `qs_id`, `qs_name`, `qs_hostname`, `qs_status`, `qs_comment`, and `cost`. Feed `qs_id` into `getQsInfo` for full details, or any per-server action (`doQsStart`, `doQsStop`, `doQsRestart`, `getQsBackups`, etc.). Returns: array of QuickServer rows. Errors: 401 if unauthenticated. Siblings: `getVpsList` (virtual VPS surface), `getQsInfo`, `getNewQs` for ordering metadata." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "The listing of `Quickservers` services on your account.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs", produces = { "application/json" })
    default Single<HttpResponse<List<QuickserverRow>>> getQsList() {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "List OS templates available for a QuickServer reinstall", operationId = "getQsReinstallOs", description = "Returns the OS template catalog filtered to the QuickServer's hardware/template type. Path param: `id` (integer). Read-only — no provisioning happens. Returns: `{ templates: [...] }` — each template has `template_file`, `template_name`, `template_version`. Use `template_file` with `postQsReinstallOs`. Non-admin callers only see templates with `template_available=1`. Errors: 401 if unauthenticated. Siblings: `postQsReinstallOs` (commit, destructive), `getVpsReinstallOs`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Reinstall OS info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/reinstall_os", produces = { "application/json" })
    default Single<HttpResponse<VpsTemplatesList>> getQsReinstallOs(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get options for QuickServer randomized root password reset", operationId = "getQsResetPassword", description = "Read-only probe before `postQsResetPassword`. Path param: `id` (integer). Use to confirm the QuickServer is in a state that allows password resets. Returns: object with reset configuration. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Note: `postQsResetPassword` generates a random password — for a chosen value use `postQsChangeRootPassword`. Siblings: `postQsResetPassword`, `postQsChangeRootPassword`, `getVpsResetPassword`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Reset password info")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/reset_password", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsResetPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get reverse DNS (PTR) records for all of a QuickServer's IPs", operationId = "getQsReverseDns", description = "Returns the current PTR record for the primary IP and any additional IPs assigned to the QuickServer. Path param: `id` (integer). Read-only — looks up live DNS, no queue. Returns: `{ ips: { \"<ip>\": \"<hostname>\", ... } }`. Use the keys with `postQsReverseDns` to update entries. Errors: 401 if unauthenticated. Note: handler does not gate on ownership/active status. Siblings: `postQsReverseDns`, `getVpsReverseDns`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "QuickServer Reverse DNS info response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/reverse_dns", produces = { "application/json" })
    default Single<HttpResponse<ReverseDnsEntries>> getQsReverseDns(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get current VNC console connection details for a QuickServer", operationId = "getQsSetupVnc", description = "Read-only probe for the VNC tunnel that exposes the server's console (host, port, credentials). Path param: `id` (integer). Returns: object with VNC connection info. Errors: 401 if unauthenticated, 404 if `id` is not owned by caller, 409 if service is not `active`. Note: this endpoint is currently a stub — the `// todo: return vnc info` line indicates the response body may be empty until completed. Siblings: `postQsSetupVnc` (configure access IP), `getVpsSetupVnc`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Get QuickServer Setup VNC Information")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/setup_vnc", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsSetupVnc(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get bandwidth usage for the QuickServer's current billing period", operationId = "getQsTrafficUsage", description = "Returns the inbound/outbound bandwidth totals and time-series points for the QuickServer's current cycle. Path param: `id` (integer). Read-only. Returns: bandwidth-data object from `qs_bandwidth_data` (totals, daily/hourly points, overage flag). Errors: 401 if unauthenticated. Note: handler does not gate on ownership or active status. Siblings: `postQsTrafficUsage` (same data, accessible via POST for filtered queries), `getVpsTrafficUsage`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Get QuickServer Traffic usage")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/traffic_usage", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsTrafficUsage(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Get the full QuickServer dashboard view payload (rich format)", operationId = "getQsViewDesktop", description = "Returns the same rich payload the AdminLTE UI uses — service info, billing, available client_links, resource graphs. Heavier than `getQsInfo` and intended for desktop dashboards. Path param: `id` (integer). Returns: object with `serviceInfo`, `client_links`, etc. (admin-only fields stripped). Errors: 401 if unauthenticated. Note: handler does not gate on ownership/active status. Siblings: `getQsInfo` (lighter), `postQsViewDesktop` (mutate variant), `getVpsViewDesktop`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Get QuickServer View Desktop Information")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/view_desktop", produces = { "application/json" })
    default Single<HttpResponse<Void>> getQsViewDesktop(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Resend the QuickServer welcome email with login credentials", operationId = "getQsWelcomeEmail", description = "Re-runs the `qs_welcome_email` function which composes and sends the welcome email containing connection details, root password, and management URLs to the account owner. Path param: `id` (integer). Returns: `{ text: \"Welcome Email has been resent.\" }`. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Use when the original welcome email was lost or the customer needs credentials again. Siblings: `getVpsWelcomeEmail`, `getQsInfo` (also exposes connection info)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response with a text message field.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Get(value = "/qs/{id}/welcome_email", produces = { "application/json" })
    default Single<HttpResponse<TextResponse>> getQsWelcomeEmail(@Parameter(description = "Quickserver ID") @PathVariable("id") String id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Change a QuickServer's system hostname (OpenVZ/Virtuozzo only)", operationId = "postQsChangeHostname", description = "Updates the hostname and the matching reverse DNS entry. Path param: `id`. Body (JSON or form): `hostname` (must pass `valid_hostname`, must differ from current). Only supported on OpenVZ/Virtuozzo platforms — KVM/dedicated returns a 4xx with a contact-support message. Pending services update the DB row directly (`{ text }`); active services queue the change (`{ text, queueId }`, ~2 min). Errors: 401, 404 if not owned, 409 if status != `active`, validation error for bad hostname or no change. Siblings: `getQsChangeHostname`, `postVpsChangeHostname`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/change_hostname", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsChangeHostname(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Change QuickServer root/administrator password to a chosen value", operationId = "postQsChangeRootPassword", description = "Queues a root password change. Path param: `id`. Body (JSON or form): `password` (the new password — required, no server-side complexity validation here). Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Both queue and history entries are written. Errors: 401, 404 if not owned, 409 if status != `active`, 400 if `password` is missing. For a randomly generated password use `postQsResetPassword` instead. For Webuzo panel password use `postQsChangeWebuzoPassword`. Siblings: `getQsChangeRootPassword`, `postVpsChangeRootPassword`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/change_root_password", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsChangeRootPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Change the system timezone on a QuickServer to a catalog entry", operationId = "postQsChangeTimezone", description = "Queues a timezone change. Path param: `id`. Body (JSON or form): `timezone` (must be one of the strings returned by `getQsChangeTimezone`). Returns: `{ text, queueId }`. Async — applied within ~2 minutes by the queue worker. Errors: 401, 404 if not owned, 409 if status != `active`, 422 if `timezone` is not in the catalog. Siblings: `getQsChangeTimezone` (call first to get valid options), `postVpsChangeTimezone`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/change_timezone", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<QueueResponse>> postQsChangeTimezone(@NotNull @Parameter(description = "") @Body(value = "timezone")  String timezone,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Change the system timezone on a QuickServer to a catalog entry", operationId = "postQsChangeTimezone", description = "Queues a timezone change. Path param: `id`. Body (JSON or form): `timezone` (must be one of the strings returned by `getQsChangeTimezone`). Returns: `{ text, queueId }`. Async — applied within ~2 minutes by the queue worker. Errors: 401, 404 if not owned, 409 if status != `active`, 422 if `timezone` is not in the catalog. Siblings: `getQsChangeTimezone` (call first to get valid options), `postVpsChangeTimezone`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/change_timezone", produces = { "application/json" }, consumes = {"multipart/form-data", "application/json"})
    default Single<HttpResponse<QueueResponse>> postQsChangeTimezone(@NotNull @Valid @Parameter(description = "") @Body TimezoneUpdate body,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Change Webuzo control panel admin password live (synchronous, not queued)", operationId = "postQsChangeWebuzoPassword", description = "Calls the Webuzo SDK directly on the server to change the panel `admin` password, then emails the new credentials. Path param: `id`. Body: `password` (new Webuzo password, must pass `valid_password`), `login_password` (caller's account login password — verified via md5 hash). Synchronous — no queue ID. Requires a prior Webuzo-Details history entry. Returns: success message string. Errors: 401, 404 if not owned, 409 if status != `active`, validation errors for missing fields, wrong login password, weak new password, or SDK failure. Siblings: `getQsChangeWebuzoPassword`, `postQsChangeRootPassword` (OS root)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/change_webuzo_password", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsChangeWebuzoPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Mount an ISO image as the QuickServer's virtual CD via URL", operationId = "postQsInsertCd", description = "Queues an `insert_cd` job that attaches the given ISO URL to the QuickServer's virtual CD drive (typically for OS reinstalls or rescue boots). Path param: `id`. Body (JSON or form): `url` (the ISO URL — pick one from `getQsInsertCd`). Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned by caller. The action is idempotent in effect (latest mount wins). Siblings: `getQsInsertCd` (list options), `doQsEjectCd` (unmount), `doQsDisableCd`, `postQsReinstallOs` (template-based)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/insert_cd", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsInsertCd(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Reinstall the operating system on a QuickServer (DESTRUCTIVE — wipes disk)", operationId = "postQsReinstallOs", description = "Wipes the disk and reinstalls the chosen OS template. All data, configs, and snapshots are erased. Path param: `id`. Body: `template` (a `template_file` from `getQsReinstallOs`), `password` (new root password — required for non-Windows templates). For active services, queues `reinstall_os` (~2 min). For inactive services, just stores the OS preference for next activation. Updates `qs_status` to `Reinstalling` and clears screenshots. Returns flash messages — typical envelope. Errors: 401, invalid template name returns error flash. Siblings: `getQsReinstallOs` (list options), `postVpsReinstallOs`, `postQuickServerRestore` (recover from backup instead)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/reinstall_os", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsReinstallOs(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Reset QuickServer root password to a server-generated random value", operationId = "postQsResetPassword", description = "Queues a `reset_password` job that generates a new root password and emails it to the account owner. Path param: `id` (integer). No body — password is generated server-side. Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned by caller, 409 if status != `active`. For a chosen password use `postQsChangeRootPassword` instead; for the Webuzo panel password use `postQsChangeWebuzoPassword`. Siblings: `getQsResetPassword`, `postVpsResetPassword`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/reset_password", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsResetPassword(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Update reverse DNS (PTR) records for a QuickServer's IPs", operationId = "postQsReverseDns", description = "Sets PTR records for one or more of the QuickServer's IPs. Path param: `id`. Body (form): `ips` — keyed by IP, value is the desired hostname (must be valid). Returns: `{ message: \"DNS Updated\", success: true }`. Caveat: in the current implementation the body is parsed but the per-IP update loop is a no-op shell — verify with `getQsReverseDns` after calling, and use the support channel if changes don't propagate. Errors: 401 if unauthenticated. Siblings: `getQsReverseDns`, `postVpsReverseDns`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Update QuickServer Reverse DNS response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/reverse_dns", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<TextResponse>> postQsReverseDns(@NotNull @Valid @Parameter(description = "") @Body ReverseDnsEntries body,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Update reverse DNS (PTR) records for a QuickServer's IPs", operationId = "postQsReverseDns", description = "Sets PTR records for one or more of the QuickServer's IPs. Path param: `id`. Body (form): `ips` — keyed by IP, value is the desired hostname (must be valid). Returns: `{ message: \"DNS Updated\", success: true }`. Caveat: in the current implementation the body is parsed but the per-IP update loop is a no-op shell — verify with `getQsReverseDns` after calling, and use the support channel if changes don't propagate. Errors: 401 if unauthenticated. Siblings: `getQsReverseDns`, `postVpsReverseDns`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Update QuickServer Reverse DNS response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/reverse_dns", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<TextResponse>> postQsReverseDns(@NotNull @Parameter(description = "") @Body(value = "ips")  Map<String, Object> ips,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Configure the source IP allowed to reach a QuickServer's VNC console", operationId = "postQsSetupVnc", description = "Sets the IP allowed to reach the VNC tunnel and queues a `setup_vnc` to apply it. Path param: `id`. Body (JSON or form): `vnc` (a valid IPv4 address — only this address can reach the console). Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned, 409 if status != `active`. Returns an inline `Invalid IP` message when `vnc` fails `validIp`. The VPS-style helper also runs after the DB update. Siblings: `getQsSetupVnc` (read), `postVpsSetupVnc`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/setup_vnc", produces = { "application/json" })
    default Single<HttpResponse<QueueResponse>> postQsSetupVnc(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Query QuickServer bandwidth usage via POST (filtered variant)", operationId = "postQsTrafficUsage", description = "Functional duplicate of `getQsTrafficUsage` exposed under POST so clients can pass a filter body. Path param: `id` (integer). Body fields are accepted but the current handler ignores them and returns the full current-cycle dataset. Returns: same bandwidth-data object as `getQsTrafficUsage`. Errors: 401 if unauthenticated. No active-status or ownership gate. Siblings: `getQsTrafficUsage`, `postVpsTrafficUsage`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Submit QuickServer Traffic Usage")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/traffic_usage", produces = { "application/json" })
    default Single<HttpResponse<Void>> postQsTrafficUsage(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Submit changes and re-fetch the QuickServer dashboard view payload", operationId = "postQsViewDesktop", description = "Same handler as `getQsViewDesktop` but accessible via POST so callers can pass body fields alongside re-fetching the view. Path param: `id`. Body fields are accepted by the underlying View handler. Returns: refreshed dashboard object — `serviceInfo`, `client_links`, etc. Errors: 401 if unauthenticated. For structured updates prefer the dedicated endpoints (`postQsChangeHostname`, `postQsReverseDns`, `postQsSetupVnc`, etc.) which return queue IDs. Siblings: `getQsViewDesktop`, `postVpsViewDesktop`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Submit QuickServer View Desktop Information")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/view_desktop", produces = { "application/json" })
    default Single<HttpResponse<Void>> postQsViewDesktop(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Restore a QuickServer from a backup (DESTRUCTIVE — overwrites disk)", operationId = "postQuickServerRestore", description = "Overwrites the live disk with a backup. Path param: `id`. Body (form): `backup` (composite key `<type>:<service>:<name>` from `getQsBackups`), `password` (caller's account login password — required for non-admin to confirm). Validates backup exists, caller's password (when applicable), and that the QuickServer disk is large enough (size check skipped for ZFS). Queues `snapshot_restore` for ZFS or `restore` for swift/minio; allow up to 10 minutes. Returns: `{ text, queueId }`. Errors: 401, 404 if not owned, 409 if status != `active`, errors for invalid password, missing backup, or insufficient disk space. Siblings: `getQsBackups`, `getQsBackup` (create), `postVpsRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "400", description = "Response with a text message field.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/restore", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<QueueResponse>> postQuickServerRestore(@NotNull @Valid @Parameter(description = "QuickServer Restore request") @Body RestoreRequest body,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Restore a QuickServer from a backup (DESTRUCTIVE — overwrites disk)", operationId = "postQuickServerRestore", description = "Overwrites the live disk with a backup. Path param: `id`. Body (form): `backup` (composite key `<type>:<service>:<name>` from `getQsBackups`), `password` (caller's account login password — required for non-admin to confirm). Validates backup exists, caller's password (when applicable), and that the QuickServer disk is large enough (size check skipped for ZFS). Queues `snapshot_restore` for ZFS or `restore` for swift/minio; allow up to 10 minutes. Returns: `{ text, queueId }`. Errors: 401, 404 if not owned, 409 if status != `active`, errors for invalid password, missing backup, or insufficient disk space. Siblings: `getQsBackups`, `getQsBackup` (create), `postVpsRestore`." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Response message from sending a service queue.")
    @ApiResponse(responseCode = "400", description = "Response with a text message field.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}/restore", produces = { "application/json" }, consumes = {"application/json", "multipart/form-data"})
    default Single<HttpResponse<QueueResponse>> postQuickServerRestore(@NotNull @Parameter(description = "") @Body(value = "backup")  String backup,@NotNull @Parameter(description = "") @Body(value = "password")  String password,@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Validate a QuickServer order without charging or provisioning", operationId = "putQs", description = "Dry-run the order payload before calling `addQs`. No invoice is created and no service is provisioned. Use to surface form errors, compute the price, and resolve the chosen `server`/`os`/`distro` against the master pool. Body (form): `server` (master ID), `password`, `os` (template), `comment`, `tos`. Returns the `validate_buy_qs` result with `continue` flag, normalized fields, `service_cost`, and `errors` array. Errors: 401 if unauthenticated; validation errors are returned in the body, not as 4xx. Siblings: `addQs` (commits the order), `getNewQs` (form metadata), `putVps` (VPS equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Validate QuickServer Order response")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Put(value = "/qs/order", produces = { "application/json" }, consumes = {"application/json"})
    default Single<HttpResponse<Void>> putQs(@NotNull @Valid @Parameter(description = "") @Body QsOrderRequest body) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Cancel a QuickServer service at the end of the current billing cycle", operationId = "quickserversCancel", description = "Schedules deprovisioning. The server keeps running until the current billing period ends, then is canceled and the recurring invoice stops. Path param: `id` (integer). Returns: `{ success: bool, text: string }`. Errors: 401 if unauthenticated, 404 if not owned by caller. Reversible only by support before the cycle closes — use `getQsInvoices` to check the next invoice date first. Siblings: `getQsInfo`, `VPSCancel` (VPS equivalent), `serversCancel` (dedicated equivalent)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "Rapid Deploy Servers Cancel")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Delete(value = "/qs/{id}", produces = { "application/json" })
    default Single<HttpResponse<InlineResponse20011>> quickserversCancel(@Parameter(description = "QuickServer ID number") @PathVariable("id") Integer id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }


    @Operation(summary = "Update QuickServer order metadata or stored settings without OS impact", operationId = "updateQsInfo", description = "Mutates QuickServer-level settings (comment, stored notes) without affecting the running OS. Path param: `id`. Body fields are module-specific and processed by the shared `View::go` handler. Returns: `SuccessTextResponse`. Errors: 401 if unauthenticated, 404 if not owned by caller. For server-side actions use the dedicated endpoints — hostname via `postQsChangeHostname`, password via `postQsChangeRootPassword`, OS via `postQsReinstallOs`. Siblings: `getQsInfo` (read), `quickserversCancel` (delete)." , tags = {"QuickServers"})
    @ApiResponse(responseCode = "200", description = "A response indicating the operation completed successfully with a text message.")
    @ApiResponse(responseCode = "401", description = "Unauthorized")
    @Post(value = "/qs/{id}", produces = { "application/json" })
    default Single<HttpResponse<SuccessTextResponse>> updateQsInfo(@Parameter(description = "QuickServer ID number.") @PathVariable("id") String id) {
        return Single.fromCallable(() -> {
            throw new UnsupportedOperationException();
        });
    }

}
