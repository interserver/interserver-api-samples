# Description: Set to true to include request body variables in the HTTP client.
bodyVariables: true

# Description: Set to true to include custom headers in the HTTP client.
customHeaders: true

# Sort method arguments to place required parameters before optional parameters.
sortParamsByRequiredFlag: true

# Sort model properties to place required parameters before optional parameters.
sortModelPropertiesByRequiredFlag: true

# Ensure parameter names are unique
ensureUniqueParams: true

# Handle unknown enum cases gracefully for forward compatibility
enumUnknownDefaultCase: true
