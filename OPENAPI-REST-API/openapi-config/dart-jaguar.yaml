# OpenAPI Generator Configuration for Dart Jaguar
# Optimized for Interserver API client with Jaguar HTTP client

# ====================
# Package Information
# ====================

# Library name in generated code (used in import statements)
pubLibrary: interserver_api

# Name in generated pubspec.yaml
pubName: interserver_api

# Version in generated pubspec.yaml
pubVersion: 1.0.0

# Description in generated pubspec.yaml
pubDescription: Interserver API client generated from OpenAPI spec

# Author name in generated pubspec.yaml
pubAuthor: Interserver

# Email address of the author in generated pubspec.yaml
pubAuthorEmail: support@interserver.net

# Homepage in generated pubspec.yaml
pubHomepage: https://interserver.net

# Repository in generated pubspec.yaml
pubRepository: https://github.com/interserver/interserver-api-samples

# ====================
# Generation Options
# ====================

# Hide the generation timestamp in generated files
hideGenerationTimestamp: true

# Source folder for generated code
sourceFolder: lib

# ====================
# Dart/Jaguar Options
# ====================

# Use Optional<T> to distinguish absent, null, and present for optional fields (Dart 3+ null safety)
useOptional: true

# Add 'final' to class properties, making them immutable
useFinalProperties: true

# ====================
# Serialization Options
# ====================

# Specify serialization library
# Available values: native_serialization
serializationLibrary: native_serialization

# ====================
# Date/Time Options
# ====================

# Specify Date library (core uses Dart's DateTime)
dateLibrary: core

# ====================
# Parameter/Model Options
# ====================

# Sort method arguments to place required parameters before optional parameters
sortParamsByRequiredFlag: true

# Sort model properties to place required parameters before optional parameters
sortModelPropertiesByRequiredFlag: true

# Ensure parameter names are unique in an operation
ensureUniqueParams: true

# Add form or body parameters to the beginning of the parameter list
prependFormOrBodyParameters: true

# ====================
# Enum Options
# ====================

# Allow the 'x-enum-values' extension for enums
useEnumExtension: true

# If the server adds new enum cases, handle them gracefully
enumUnknownDefaultCase: true

# ====================
# Discriminator Options
# ====================

# Set to false for better discriminator support
legacyDiscriminatorBehavior: false

# If false, the 'additionalProperties' implementation is compliant with OAS and JSON schema specifications
disallowAdditionalPropertiesIfNotPresent: false

# ====================
# Naming Options
# ====================

# Boolean, toggles whether unicode identifiers are allowed in names or not
allowUnicodeIdentifiers: false
