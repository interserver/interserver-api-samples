# OpenAPI Generator Configuration for Elixir
# Optimized for Tesla (HTTP) + Poison (JSON)

# ============================================================================
# Elixir-Specific Options
# ============================================================================

# Elixir package name (convention: lowercase, snake_case)
# Example: interserver_api
packageName: interserver_api

# Main module name (convention: CamelCase)
# Example: InterserverApi
moduleName: InterserverApi

# Main namespace for all classes
# Example: InterserverApi
invokerPackage: InterserverApi



# ============================================================================
# Code Generation Options
# ============================================================================

# Sort method arguments to place required parameters before optional parameters
sortParamsByRequiredFlag: true

# Sort model properties to place required parameters before optional parameters
sortModelPropertiesByRequiredFlag: true

# Whether to ensure parameter names are unique in an operation
ensureUniqueParams: true

# Add form or body parameters to the beginning of the parameter list
prependFormOrBodyParameters: false

# ============================================================================
# Enum and Discriminator Options
# ============================================================================

# Set to false for generators with better discriminator support
legacyDiscriminatorBehavior: true

# If the server adds new enum cases unknown by old spec/client,
# client will fail to parse. Enable to add 'unknown_default_open_api' case
enumUnknownDefaultCase: false

# ============================================================================
# Additional Properties
# ============================================================================

# If false, additionalProperties is compliant with OAS and JSON schema specs
# If true (default), keeps old behavior where additionalProperties is false by default
disallowAdditionalPropertiesIfNotPresent: false

# ============================================================================
# Naming Conventions
# ============================================================================

# Allow unicode identifiers in names
allowUnicodeIdentifiers: false

# ============================================================================
# Output Options
# ============================================================================

# Hide the generation timestamp in generated files
hideGenerationTimestamp: true

# License header to prepend to all source files (leave empty for none)
# licenseHeader: |
#   SPDX-License-Identifier: MIT
