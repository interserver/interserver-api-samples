# OpenAPI Generator configuration for Ada
# Optimized for Interserver API client generation

# Hide the generation timestamp in generated files
hideGenerationTimestamp: true

# Sort method arguments to place required parameters before optional parameters.
sortParamsByRequiredFlag: true

# Sort model properties to place required parameters before optional parameters.
sortModelPropertiesByRequiredFlag: true

# Whether to ensure parameter names are unique in an operation (rename parameters that are not).
ensureUniqueParams: true

# boolean, toggles whether unicode identifiers are allowed in names or not, default is false
allowUnicodeIdentifiers: false

# Add form or body parameters to the beginning of the parameter list.
prependFormOrBodyParameters: false

# GNAT project name - Proper Ada package naming
projectName: Interserver_Api

# Use AWS HTTP support library
httpSupport: aws

# The name of the Ada package which provides support for OpenAPI
openApiName: Swagger

# Set to false for generators with better support for discriminators.
legacyDiscriminatorBehavior: false

# If false, the 'additionalProperties' implementation is compliant with the OAS and JSON schema specifications.
disallowAdditionalPropertiesIfNotPresent: false

# If the server adds new enum cases, that are unknown by an old spec/client, the client will fail to parse the network response.
enumUnknownDefaultCase: false
