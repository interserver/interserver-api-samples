/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.model

import com.github.plokhotnyuk.jsoniter_scala.macros.named

  /**
   * Request body to validate (putSsl) or place (addSsl) an SSL certificate order. Contact fields default from the account if omitted. Package ids come from getNewSsl.serviceTypes.
   */
case class SslOrderRequest(
  /* SSL package service type id from getNewSsl.serviceTypes. */
  @named("ssl") ssl: Int,
  /* Domain the certificate is issued for. Wildcard certs require *.domain.com format. */
  @named("hostname") hostname: String,
  /* Domain-control approver email (required for all SSL orders). */
  @named("approver_email") approver_email: String,
  /* Billing frequency in months (12, 24, or 36). */
  @named("frequency") frequency: Option[Int] = scala.None,
  /* Coupon code. */
  @named("coupon") coupon: Option[String] = scala.None,
  /* Whether the CSR is server-generated or customer-provided. */
  @named("csr_type") csr_type: Option[SslOrderRequestEnums.CsrType] = scala.None,
  /* PEM-encoded CSR (>= 2048-bit) when csr_type=provided. */
  @named("csr") csr: Option[String] = scala.None,
  /* Contact first name (defaults from account). */
  @named("firstname") firstname: Option[String] = scala.None,
  /* Contact last name (defaults from account). */
  @named("lastname") lastname: Option[String] = scala.None,
  /* Contact email (defaults from account). */
  @named("email") email: Option[String] = scala.None,
  /* Contact address (defaults from account). */
  @named("address") address: Option[String] = scala.None,
  /* Contact city (defaults from account). */
  @named("city") city: Option[String] = scala.None,
  /* Contact state/region (defaults from account). */
  @named("state") state: Option[String] = scala.None,
  /* Contact postal code (defaults from account). */
  @named("zip") zip: Option[String] = scala.None,
  /* Contact country (defaults from account). */
  @named("country") country: Option[String] = scala.None,
  /* Contact phone (defaults from account). */
  @named("phone") phone: Option[String] = scala.None,
  /* Contact company/organization (defaults from account). */
  @named("company") company: Option[String] = scala.None,
  /* Contact department (defaults to Administration). */
  @named("department") department: Option[String] = scala.None,
  /* EV certificate incorporating agency (only for EV packages). */
  @named("agency") agency: Option[String] = scala.None,
  /* EV certificate business category (only for EV packages). */
  @named("business_category") business_category: Option[String] = scala.None
)

object SslOrderRequestEnums:
  enum CsrType:
    case `generated`
    case `provided`

  object CsrType:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given csrTypeCodec: JsonValueCodec[CsrType] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "generated" => "generated"
            case "provided" => "provided"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end SslOrderRequestEnums
