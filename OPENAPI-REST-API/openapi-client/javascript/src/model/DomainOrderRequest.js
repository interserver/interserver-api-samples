/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DomainOrderRequest model module.
 * @module model/DomainOrderRequest
 * @version 1.0.0
 */
class DomainOrderRequest {
    /**
     * Constructs a new <code>DomainOrderRequest</code>.
     * Request body for the domain order flow: putDomains (returns required fields), patchDomains (validates fields), addDomain (places the order). The TLD is resolved from hostname. Per-TLD registrant/contact fields returned by putDomains may be supplied as additional properties on patchDomains/addDomain.
     * @alias module:model/DomainOrderRequest
     * @extends Object
     * @param hostname {String} Fully-qualified domain to register or transfer (e.g. example.com).
     */
    constructor(hostname) { 
        
        DomainOrderRequest.initialize(this, hostname);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hostname) { 
        obj['hostname'] = hostname;
        obj['type'] = 'register';
        obj['coupon'] = '';
    }

    /**
     * Constructs a <code>DomainOrderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DomainOrderRequest} obj Optional instance to populate.
     * @return {module:model/DomainOrderRequest} The populated <code>DomainOrderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DomainOrderRequest();

            ApiClient.constructFromObject(data, obj, 'Object');
            

            if (data.hasOwnProperty('hostname')) {
                obj['hostname'] = ApiClient.convertToType(data['hostname'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('coupon')) {
                obj['coupon'] = ApiClient.convertToType(data['coupon'], 'String');
            }
            if (data.hasOwnProperty('whois_privacy')) {
                obj['whois_privacy'] = ApiClient.convertToType(data['whois_privacy'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DomainOrderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DomainOrderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DomainOrderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['hostname'] && !(typeof data['hostname'] === 'string' || data['hostname'] instanceof String)) {
            throw new Error("Expected the field `hostname` to be a primitive type in the JSON string but got " + data['hostname']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['coupon'] && !(typeof data['coupon'] === 'string' || data['coupon'] instanceof String)) {
            throw new Error("Expected the field `coupon` to be a primitive type in the JSON string but got " + data['coupon']);
        }
        // ensure the json data is a string
        if (data['whois_privacy'] && !(typeof data['whois_privacy'] === 'string' || data['whois_privacy'] instanceof String)) {
            throw new Error("Expected the field `whois_privacy` to be a primitive type in the JSON string but got " + data['whois_privacy']);
        }

        return true;
    }


}

DomainOrderRequest.RequiredProperties = ["hostname"];

/**
 * Fully-qualified domain to register or transfer (e.g. example.com).
 * @member {String} hostname
 */
DomainOrderRequest.prototype['hostname'] = undefined;

/**
 * Order type.
 * @member {module:model/DomainOrderRequest.TypeEnum} type
 * @default 'register'
 */
DomainOrderRequest.prototype['type'] = 'register';

/**
 * Coupon code (addDomain only).
 * @member {String} coupon
 * @default ''
 */
DomainOrderRequest.prototype['coupon'] = '';

/**
 * Set to \"enable\" to add Whois privacy (addDomain only).
 * @member {module:model/DomainOrderRequest.WhoisPrivacyEnum} whois_privacy
 */
DomainOrderRequest.prototype['whois_privacy'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
DomainOrderRequest['TypeEnum'] = {

    /**
     * value: "register"
     * @const
     */
    "register": "register",

    /**
     * value: "transfer"
     * @const
     */
    "transfer": "transfer",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


/**
 * Allowed values for the <code>whois_privacy</code> property.
 * @enum {String}
 * @readonly
 */
DomainOrderRequest['WhoisPrivacyEnum'] = {

    /**
     * value: "enable"
     * @const
     */
    "enable": "enable",

    /**
     * value: "disable"
     * @const
     */
    "disable": "disable",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};



export default DomainOrderRequest;

