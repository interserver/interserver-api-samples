openapi: 3.0.3
info:
  contact:
    email: support@interserver.net
    name: InterServer Support Staff
    url: https://www.interserver.net/contact-us.html
  description: |+
    # Overview

    The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.

    The API covers the following service categories:
    - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management
    - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting
    - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers
    - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html)
    - [Backups](https://www.interserver.net/storage/) — storage and backup services
    - Licenses — control panel and software licenses
    - [Mail](https://www.mail.baby/) — mail delivery services
    - SSL — certificate provisioning
    - Billing — invoices, payment methods, and account management

    For interactive testing, see the [API documentation](/api-docs/).

    # Authentication

    Most endpoints require authentication. Two methods are supported:

    ## API Key (Preferred)

    Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:

    ```
    X-API-KEY: your-api-key-here
    ```

    ## Session-Based Authentication

    Alternatively, authenticate by creating a session:

    1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier.
    2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:

    ```
    sessionid: your-session-id-here
    ```

    API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.

  license:
    name: MIT License
    url: https://opensource.org/licenses/MIT
  termsOfService: https://www.interserver.net/terms-of-service.html
  title: InterServer Management API
  version: 0.9.0
  x-service-info:
    category: web-infrastructure
    subcategories:
    - hosting
    - cloud-compute
    - domains-dns
    - email-delivery
    - tls-certificates
    - storage-backup
    - networking
    supported_currencies:
    - USD
    billing_models:
    - one-time
    - recurring
    marketplace:
      url: https://www.interserver.net/
      order_endpoint: /billing/cart
      checkout_endpoint: "/billing/pay/{method}/{invoices}"
externalDocs:
  description: Tips
  url: https://www.interserver.net/tips/
servers:
- description: Live API Endpoint
  url: https://my.interserver.net/apiv2
security:
- apiKeyAuth: []
- sessionIdHeaderAuth: []
- sessionIdCookieAuth: []
tags:
- description: "Endpoints that do not require authentication. Use these to check API\
    \ availability, look up reference data (countries, currencies, timezones), and\
    \ authenticate."
  name: Public
- description: "Manage your account profile, contact information, security settings,\
    \ API keys, SSH keys, two-factor authentication, and IP access restrictions."
  name: Account
- description: "Invoices, shopping cart, prepay balances, credit cards, and payment\
    \ processing. Use `/billing/pay/{method}/{invoices}` to complete payment for outstanding\
    \ invoices."
  name: Billing
- description: "Cloud backup storage services. Order, view, and manage backup storage\
    \ quotas and credentials."
  name: Backups
- description: "Manage DNS domains and records hosted on InterServer's nameservers.\
    \ Create domains, add/update/delete A, AAAA, CNAME, MX, TXT, and other record\
    \ types."
  name: DNS
- description: "Domain registration, transfers, renewals, WHOIS, nameserver management,\
    \ and DNSSEC configuration."
  name: Domains
- description: Floating IP services that provide portable IP addresses which can be
    reassigned between servers for high availability and failover.
  name: Floating_IPs
- description: "Software license provisioning for control panels (cPanel, Plesk, DirectAdmin)\
    \ and other licensed software. Order, manage, and change IP assignments."
  name: Licenses
- description: "Mail Baby email delivery services. Order, manage, view logs, configure\
    \ alerts and deny rules, and monitor deliverability."
  name: Mail
- description: "Rapid Deploy Servers (QuickServers) — pre-configured dedicated servers\
    \ available for fast provisioning with OS reinstall, backup, and VNC capabilities."
  name: QuickServers
- description: "Dedicated server ordering and management. Configure custom hardware,\
    \ view IPMI information, and manage server lifecycle."
  name: Servers
- description: "SSL/TLS certificate provisioning and management. Order, renew, and\
    \ manage certificates for securing your domains."
  name: SSL-Certificates
- description: "Virtual Private Server management. Order, configure, backup, restore,\
    \ reinstall, and control VPS instances with full root access."
  name: VPS
- description: "Shared and reseller web hosting packages. Order, manage, and access\
    \ hosting control panels, backups, and reverse DNS."
  name: Webhosting
- description: "Support ticket system. Create new tickets, reply to existing ones,\
    \ view ticket history, and close resolved issues."
  name: Tickets
- description: "DDoS protection and IP scrubbing services. Manage protected IPs, configure\
    \ firewall rules, geographic filters, and view traffic logs."
  name: Scrub Ips
paths:
  /account:
    description: |
      This endpoint allows you to manage various user details, including contact information and account security.

      - Use the `GET` method to retrieve your account information.
      - Use the `POST` method to update your stored contact and billing information.
    get:
      description: "Use to render the account-settings page or to verify current state\
        \ before mutating with `updateAccountInfo`. No body, no path params. Returns:\
        \ full profile (name, company, address1/2, city, state, zip, country, phone,\
        \ email_invoices, email_abuse, gstin, locale, timezone), masked credit-card\
        \ list (last-4 digits only — full PAN never returned), OAuth provider config\
        \ (with secret keys stripped), feature toggles (`disable_reset`, `disable_reinstall`,\
        \ `disable_*_notifications`), gravatar URL, language, country->currency map,\
        \ and `enableLocales`/`enableCurrencies` UI flags. Timezone defaults to IP-derived\
        \ value if unset, falling back to America/New_York. Errors: 401 if session\
        \ invalid or expired. Sibling ops: `updateAccountInfo`, `getAccountTfaSetup`,\
        \ `updateAccountFeatures`, `updateAccountIpLimits`."
      operationId: getAccountInfo
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountInfo"
          description: Your account information.
          links:
            UpdateAccountInfo:
              description: Use the returned account data to pre-fill an update form
                and submit changes via POST /account.
              operationId: updateAccountInfo
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read full account profile, billing address, and security settings"
      tags:
      - Account
    post:
      description: "Use to change the customer's name, company, mailing address, phone,\
        \ GSTIN, locale, timezone, or notification-email overrides (`email_invoices`,\
        \ `email_abuse`). Submit only fields you want to change — partial updates\
        \ supported. Required (must be non-empty if sent): `name`, `country`, `address`,\
        \ `city`, `state`, `zip`, `phone`. Phone is normalized: parens, dashes, underscores\
        \ stripped. Timezone must be a valid IANA identifier (e.g. `America/New_York`).\
        \ Side effects: triggers FraudRecord + MaxMind risk re-scoring on first save,\
        \ updates Kayako helpdesk username when `name` changes. Returns `{success:true}`.\
        \ Errors: 401 missing-required field; 422 invalid timezone or empty payload.\
        \ Sibling ops: `getAccountInfo`, `updateAccountFeatures`, `updateAccountPassword`."
      operationId: updateAccountInfo
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AccountInfoPost"
          application/json:
            schema:
              $ref: "#/components/schemas/AccountInfoPost"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "422":
          content:
            application/json:
              examples:
                InvalidTimeZone:
                  value:
                    message: Invalid Time Zone
                MissingRequiredFields:
                  value:
                    message: "fname, lname cannot be empty!"
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Validation error while updating account data.
      summary: Update contact and billing-address fields on the customer profile
      tags:
      - Account
    summary: User Account Information and Management
  /account/2fa:
    delete:
      description: "DESTRUCTIVE: removes the 2FA secret from `account_security` and\
        \ clears the in-session secret cache. After success, only password authentication\
        \ is required for future logins — security posture drops materially. No body,\
        \ no path params. Use when the customer has lost their authenticator device\
        \ or wants to re-enroll from scratch (call this, then `getAccountTfaSetup`\
        \ -> `updateAccountTfa`). Returns `{success:true, text:'Google Two Factor\
        \ Authentication is disabled successfully!'}`. Errors: 401 unauthenticated.\
        \ Caveat: existing sessions remain valid; rotate `updateAccountPassword` if\
        \ you suspect credential compromise. Sibling ops: `getAccountTfaSetup`, `updateAccountTfa`,\
        \ `updateAccountPassword`."
      operationId: deleteAccountTfa
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Disable two-factor authentication and remove the TOTP secret
      tags:
      - Account
    get:
      description: "Use as step 1 of 2FA enrollment. The 160-bit secret is generated\
        \ on first call and cached in the session until the user completes (or abandons)\
        \ setup. No body, no path params. Returns `{2fa_google_key, 2fa_google_split}`\
        \ — render `2fa_google_key` as a QR code (otpauth://totp/My.InterServer:LID?secret=KEY)\
        \ and display `2fa_google_split` (key chunked into 4-char groups, space-separated)\
        \ for manual entry. After the user types the 6-digit code from their app,\
        \ finalize enrollment with `updateAccountTfa`. Calling this multiple times\
        \ before enrolling reuses the same in-session secret. Errors: 401 if session\
        \ invalid. Sibling ops: `updateAccountTfa` (verify & enable), `deleteAccountTfa`\
        \ (disable)."
      operationId: getAccountTfaSetup
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountTfaSetup_200_response"
          description: Two-factor authentication setup data.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Fetch TOTP secret to enroll a 2FA authenticator app (Google Authenticator
        etc.)
      tags:
      - Account
    post:
      description: "Use as step 2 of 2FA enrollment, after `getAccountTfaSetup`. Body:\
        \ `{2fa_google_code:string}` — the 6-digit code currently displayed by the\
        \ user's authenticator app for the secret returned from `getAccountTfaSetup`.\
        \ On verify success, the secret is persisted to `account_security` (type `2fa_google_key`,\
        \ label `default`) and ALL OTHER active sessions for this account are invalidated\
        \ (server destroys appsessions and sessions rows where session_id != current).\
        \ The current session remains. Subsequent logins will require both password\
        \ and a fresh TOTP code. Returns `{success:true, text}`. Errors: 401 unauthenticated;\
        \ 422 `Invalid Code` if the TOTP doesn't match (clock skew, wrong app entry,\
        \ or expired). Sibling ops: `getAccountTfaSetup`, `deleteAccountTfa`."
      operationId: updateAccountTfa
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/updateAccountTfa_request"
          application/json:
            schema:
              $ref: "#/components/schemas/updateAccountTfa_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "422":
          content:
            application/json:
              examples:
                InvalidCode:
                  value:
                    message: Invalid Code
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Provided two-factor verification code was invalid.
      summary: Verify TOTP code and enable two-factor authentication on the account
      tags:
      - Account
  /account/apikey:
    post:
      description: "DESTRUCTIVE: generates a new 128-character random API key and\
        \ overwrites the existing entry in `account_security` (type `api_key`, label\
        \ `default`). The OLD key stops working the moment this returns — any scripts,\
        \ MCP clients, or CI jobs using the previous key will start receiving 401\
        \ until updated. No body, no path params. Returns `{success:true, text:NEW_KEY}`\
        \ — the plaintext key is returned ONCE in this response and is not retrievable\
        \ later (only stored hashed-equivalent server-side for verification). Store\
        \ immediately in a secret manager. Use after suspected credential leak, employee\
        \ offboarding, or routine rotation. Errors: 401 unauthenticated. Sibling ops:\
        \ `updateAccountPassword`, `updateAccountIpLimits`, `Logout`."
      operationId: updateAccountApiKey
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Rotate the account's REST/MCP API key — old key is invalidated immediately
      tags:
      - Account
  /account/features:
    post:
      description: |
        Updates account-level feature flags that gate destructive service operations across every VPS / dedicated / QuickServer the customer owns. Useful for production accounts that want belt-and-suspenders protection against accidental reinstalls or root-password resets via the panel/API. Changes take effect immediately for all subsequent service operations. Sibling ops: `getAccountInfo`, `updateAccountInfo`, `updateAccountIpLimits`.

        **Body fields:**
        - `disable_reset` (bool, optional) — when `true`, blocks server / VPS root-password resets account-wide.
        - `disable_reinstall` (bool, optional) — when `true`, blocks OS reinstalls account-wide.

        Submit either or both. Flags absent from the request default to `0` for the comparison and only persist if their value differs from the current stored value.

        **Returns:** `{ success: true, text }`.

        **Errors:**
        - `401` — unauthenticated.
        - `400` / `422` — `Nothing to update` when neither flag's value differs from current.
      operationId: updateAccountFeatures
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AccountFeatures"
          application/json:
            schema:
              $ref: "#/components/schemas/AccountFeatures"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "422":
          content:
            application/json:
              examples:
                NoChanges:
                  value:
                    message: Nothing to update
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Request was valid but did not contain updatable values.
      summary: Toggle account-wide safety locks for password reset and OS reinstall
      tags:
      - Account
  /account/iplimits:
    patch:
      description: "DESTRUCTIVE: deletes the matching `{start, end}` entry from `accounts.session_limit`.\
        \ Method is PATCH (not DELETE) because the path collides with `updateAccountIpLimits`.\
        \ Body: `{start, end}` — must exactly match an existing range (trim-equal\
        \ on both bounds). Behaviour: if removing this range would leave an empty\
        \ list, IP limiting is disabled and the account becomes accessible from any\
        \ IP. If ranges remain but none cover the caller's source IP, the server auto-injects\
        \ a /32 for the caller to prevent self-lockout (response text warns). MCP\
        \ callers bypass via `X-API-APP: 1` header. Returns `{success:true, text:'IP\
        \ Range deleted.'}`. Errors: 400/422 `Invalid IP Address` if `start`/`end`\
        \ aren't valid IPs; 401 unauthenticated. Sibling ops: `updateAccountIpLimits`,\
        \ `getAccountInfo`."
      operationId: deleteIpLimit
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IpLimitRange"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: IP Range removed.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Remove one IP range from the account allow-list (PATCH on /account/iplimits)
      tags:
      - Account
    post:
      description: "DESTRUCTIVE / LOCKOUT-RISK: appends an IP range to `accounts.session_limit`.\
        \ Once ANY range exists, all `/apiv2` and panel access is restricted to matching\
        \ source IPs. Body: `{start, end, restrict?}` — both IPv4 dotted-quad; `restrict`\
        \ is `Web & API` (default) or `Only API`. Safety net: server checks the caller's\
        \ IP against the resulting list and auto-appends a /32 for the caller if not\
        \ already covered (response text warns about this). The MCP server sets header\
        \ `X-API-APP: 1` which short-circuits the IP check entirely (see `api_check_auth_limits()`),\
        \ so MCP tools keep working. Caveats: `192.168.1.0`-`192.168.1.255` is rejected\
        \ as a placeholder. Returns `{success:true, text}`. Errors: 400/422 `Invalid\
        \ IP Address`; 401 unauthenticated. Sibling ops: `deleteIpLimit`, `getAccountInfo`."
      operationId: updateAccountIpLimits
      requestBody:
        content:
          multipart/form-data:
            examples:
              IpLimitRangeFormExample:
                value:
                  start: 1.2.3.0
                  end: 1.2.3.255
            schema:
              $ref: "#/components/schemas/IpLimitRange"
          application/json:
            examples:
              IpLimitRangeJsonExample:
                value:
                  start: 1.2.3.0
                  end: 1.2.3.255
            schema:
              $ref: "#/components/schemas/IpLimitRange"
        description: The lower and upper bounds of an ip range.
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "422":
          content:
            application/json:
              examples:
                InvalidIp:
                  value:
                    message: Invalid IP Address
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: IP limit payload contains an invalid address.
      summary: Add an IP CIDR/range to the account's API+web allow-list (lockout-safe)
      tags:
      - Account
  /account/oauth/{name}:
    delete:
      description: "DESTRUCTIVE: removes the linked provider's tokens from `accounts_ext`\
        \ (rows where `account_key` IN (`{name}_id`,`{name}_url`)). After unlinking,\
        \ that provider can no longer be used to log in or pre-fill profile data —\
        \ the user must log in via password (and 2FA if enabled). Path param: `name`\
        \ (case-insensitive provider key, e.g. `google`, `github`, `facebook`) — must\
        \ be present in `getOauthConfig().providers`. No request body. Use when the\
        \ customer wants to revoke a previously authorized social-login. Returns `{success:true,\
        \ text:'OAuth Provider Unlinked.'}`. Errors: 400 `Invalid Provider Name.`\
        \ if `name` not configured; 401 unauthenticated. Sibling ops: `logoutAccountOauth`,\
        \ `getAccountInfo`, `updateAccountPassword`."
      operationId: deleteAccountOauthName
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Unlink a third-party OAuth/social provider (Google, GitHub, etc.)\
        \ from the account"
      tags:
      - Account
  /account/oauth/{name}/logout:
    get:
      description: "Soft de-authorization for a linked OAuth provider — terminates\
        \ only the upstream provider session/cookie state. The account-level link\
        \ in `accounts_ext` is preserved, so the user can log back in with that provider\
        \ without re-linking. Path param: `name` (provider key, e.g. `google`, `github`).\
        \ No request body. Use when forcing a fresh consent screen on next OAuth login,\
        \ or after the user reports a stuck/stale provider session. NOT a substitute\
        \ for `Logout` (which kills the MyAdmin session) and NOT a substitute for\
        \ `deleteAccountOauthName` (which permanently severs the link). Returns `{success:true,\
        \ text:'OAuth Provider Logged Out.'}`. Errors: 401 unauthenticated. Sibling\
        \ ops: `deleteAccountOauthName`, `Logout`, `getAccountInfo`."
      operationId: logoutAccountOauth
      parameters:
      - explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Sign out of the upstream OAuth provider session (does not unlink the
        account)
      tags:
      - Account
  /account/password:
    post:
      description: |
        DESTRUCTIVE: changes the account login password and invalidates all OTHER active sessions for this account. The current caller's session is preserved; API keys generated via `updateAccountApiKey` remain valid. Sibling ops: `updateAccountApiKey`, `Logout`, `updateAccountTfa`.

        **Body fields:**
        - `currentpassword` (string, required) — verified via `auth::authenticate`.
        - `password` (string, required) — must pass `valid_password()` — 8–50 chars, at least one uppercase, one lowercase, one digit, and one of `_~-!@#$%^&*`.
        - `password2` (string, required) — must equal `password`.

        **Returns:** `{ success: bool }` — flash messages on the response capture per-field errors.

        **Side effects:**
        - Persists `md5(password)` to `accounts.account_passwd`.
        - Sends `password_change_notify.tpl` email to the account login id.
        - Destroys all other sessions for this account row-by-row.

        **Errors:**
        - `401` — unauthenticated.
        - Flash `Current login password is mismatching` — bad `currentpassword`.
        - Flash `Confirm Password is mismatching` — `password` ≠ `password2`.
        - Flash password-policy violation message.
      operationId: updateAccountPassword
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Change the account login password (verifies current, kills other sessions)"
      tags:
      - Account
  /account/sshkey:
    post:
      description: "Stores or replaces the SSH public key on `account_security` (type\
        \ `ssh_key`, label `default`). On future VPS, dedicated server, or quickserver\
        \ orders the activation flow can install this key into `~/.ssh/authorized_keys`\
        \ for the root/sudo user, eliminating password-based SSH for the initial provisioning.\
        \ Body: `{sshKey:string}` — full single-line OpenSSH public key (ssh-rsa/ssh-ed25519/ecdsa-sha2-*\
        \ + base64 + optional comment). Newlines are stripped on save. Existing servers\
        \ are NOT retroactively updated — only new orders pick this up. Use to set\
        \ up key-based access ahead of order activation, or to rotate the canonical\
        \ key. Returns `{success:true, text:'SSH Keys Updated.'}`. Errors: 401 unauthenticated.\
        \ Sibling ops: `getAccountInfo`, `updateAccountPassword`, `updateAccountApiKey`."
      operationId: updateAccountSshKey
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AccountSshKey"
          application/json:
            schema:
              $ref: "#/components/schemas/AccountSshKey"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Set the account-level SSH public key auto-installed on new VPS/dedicated
        orders
      tags:
      - Account
  /affiliate/banners:
    get:
      description: |
        Returns the catalog of pre-built banner images affiliates can embed on partner sites — same catalog for every account (not per-affiliate). Use to render a creative-asset picker in the affiliate dashboard. Each row carries the image filename and dimensions so the client can build correctly-sized `<img>` tags. Read-only. Sibling ops: `getAffiliateRichReport`, `getAffiliateSalesGraph`, `getAffiliateTrafficGraph`, `getAffiliateWebTraffic`, `getAffiliateSignups`, `updateAffiliateDockSetup`.

        **Path/Query/Body:** None.

        **Returns:** Array of `AffiliateBannerRow`:
        - `image` (string) — filename (e.g. `12946798.gif`); served from the affiliate asset bucket.
        - `width` (string) — pixels.
        - `height` (string) — pixels.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateBanners
      responses:
        "200":
          content:
            application/json:
              examples:
                AffiliateBannerExample:
                  value:
                  - image: 12946798.gif
                    width: "125"
                    height: "125"
                  - image: 12946800.gif
                    width: "160"
                    height: "90"
                  - image: 12946802.gif
                    width: "160"
                    height: "190"
                  - image: 12946806.gif
                    width: "200"
                    height: "200"
                  - image: 12946808.gif
                    width: "250"
                    height: "250"
                  - image: 12946811.gif
                    width: "300"
                    height: "250"
              schema:
                items:
                  $ref: "#/components/schemas/AffiliateBannerRow"
                type: array
          description: Affiliate Banners Array
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List affiliate banner image assets with filename and dimensions
      tags:
      - Billing
  /affiliate/dock_setup:
    post:
      description: |
        Customizes the branded landing-dock page shown to visitors arriving via the affiliate's referral link, and reserves a unique referrer coupon code that's automatically created across all affiliate-eligible modules. Title/description allow a limited HTML allowlist (`<b>`, `<br>`, `<strong>`, `<hr>`); everything else is entity-escaped. Coupon changes propagate to **all** affiliate modules atomically. Sibling ops: `updateAffiliatePaymentSetup`, `getAffiliateSignups`.

        **Body fields (multipart or JSON, schema `AffiliateDockSetup`):**
        - `affiliate_dock_title` (string, optional) — landing-page title. HTML allowlist: `<b>`, `<br>`, `<strong>`, `<hr>`.
        - `affiliate_dock_description` (string, optional) — landing-page body. Same allowlist.
        - `referrer_coupon` (string, optional) — coupon code reservation. Requirements:
          - ≥ 6 chars.
          - `^[a-zA-Z0-9]+$` (alphanumeric only).
          - Must NOT contain `facebook`, `test`, or `interserver` (substring check, case-insensitive).
          - Must NOT exactly match a reserved word.
          - Must NOT already exist as a coupon in any affiliate module (`webhosting`, `vps`, `quickservers`, `servers`, `backups`).

        **Returns:** `{text: "<status message>"}`.

        **Side effects:**
        - First time setting `referrer_coupon`: inserts a `coupons` row in each affiliate module (`type=3`, `amount=0.01`, `onetime=1`, `customer=-1`, `usable=1`, `applies=-1`).
        - Changing `referrer_coupon`: renames the coupon across all affiliate modules in one transaction.
        - Updates the account's `affiliate_dock_title`, `affiliate_dock_description`, `referrer_coupon` fields.

        **Auth:** Session/API key.

        **Errors:**
        - `422 The name must be at least 6 characters long`.
        - `422 Invalid Characters, use only standard english letters and numbers`.
        - `422 That is a reserved word that cannot be used here`.
        - `422 <position> is a reserved word that cannot be used here` (substring match against `facebook`/`test`/`interserver`).
        - `409 That name is already taken` — coupon exists in another account's module.
        - `401` — unauthenticated.
      operationId: updateAffiliateDockSetup
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AffiliateDockSetup"
          application/json:
            schema:
              $ref: "#/components/schemas/AffiliateDockSetup"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Configure the affiliate landing dock title, description, and referrer\
        \ coupon"
      tags:
      - Billing
  /affiliate/payment_setup:
    post:
      description: |
        Sets the disbursement preferences for affiliate commission payouts. Choose between PayPal payout (provide an email — validated) or internal prepay credit (auto-applied to future invoices via `method=prepay`). Selecting `not set` suspends payouts. Sibling ops: `updateAffiliateDockSetup`, `getAffiliateRichReport`, `getAffiliateDownload`.

        **Body fields (multipart or JSON, schema `AffiliatePaymentSetup`):**
        - `affiliate_payment_method` (string, optional) — one of `paypal` / `prepay` / `not set`.
        - `affiliate_paypal` (string, optional, required when method=`paypal`) — email validated by `valid_email()`.

        **Returns:** `{text: "Ok"}`.

        **Side effects:**
        - Updates the account's `affiliate_payment_method` and/or `affiliate_paypal` fields.

        **Auth:** Session/API key.

        **Errors:**
        - `422 Invalid Email` — `affiliate_paypal` fails `valid_email()`.
        - `422 Invalid Payment Method` — value not in `{paypal, prepay, not set}`.
        - `401` — unauthenticated.

        **Related calls:**
        - **Read current commissions:** `getAffiliateRichReport`, `getAffiliateSalesGraph`.
        - **Export commission report:** `getAffiliateDownload`.
      operationId: updateAffiliatePaymentSetup
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/AffiliatePaymentSetup"
          application/json:
            schema:
              $ref: "#/components/schemas/AffiliatePaymentSetup"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Configure how affiliate commissions get paid out (PayPal or internal
        prepay)
      tags:
      - Billing
  /affiliate/rich_report:
    get:
      description: |
        Returns a server-rendered HTML/text summary report combining commission totals, conversion rates, and traffic in one round-trip — useful for embedding in a dashboard panel. The payload is **not structured JSON** — for chart-friendly data use `getAffiliateSalesGraph` and `getAffiliateTrafficGraph` instead. Backed by `affiliate_summary_report()`. Sibling ops: `getAffiliateSalesGraph`, `getAffiliateTrafficGraph`, `getAffiliateSignups`, `getAffiliateDownload`, `getAffiliateWebTraffic`.

        **Path/Query/Body:** None.

        **Returns:** `{text: "<html-or-plain-text-report>"}`.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Structured time series:** `getAffiliateSalesGraph`, `getAffiliateTrafficGraph`.
        - **Per-signup detail:** `getAffiliateSignups`.
        - **CSV/XLSX export:** `getAffiliateDownload`.
      operationId: getAffiliateRichReport
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read a combined affiliate performance summary (HTML payload)
      tags:
      - Billing
  /affiliate/sales_graph:
    get:
      description: |
        Returns aggregated sales time-series data — monthly buckets with sale counts/totals — for the requested look-back window. Use to render a sales trend chart in the affiliate dashboard. Bucket granularity is fixed at monthly by `sales_graph_lte_data`; increasing `days` extends the window, it does not change bucket size. Sibling ops: `getAffiliateTrafficGraph` (clicks), `getAffiliateRichReport` (combined summary), `getAffiliateSignups`, `getAffiliateDownload`.

        **Query params:**
        - `days` (integer, optional, default `365`) — look-back window in days.

        **Returns:** `StatusMonthlyBreakdown` — buckets keyed by month with aggregated sale counts and amounts.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateSalesGraph
      parameters:
      - description: Number of days of sales history to include in the graph data.
          Determines the time window for the returned data points.
        explode: true
        in: query
        name: days
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusMonthlyBreakdown"
          description: Affiliate sales graph data broken down by time period.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read aggregated affiliate sales time-series (monthly buckets) for chart
        rendering
      tags:
      - Billing
  /affiliate/traffic_graph:
    get:
      description: |
        Returns aggregated click/visit time-series data from the `affiliate_traffic` table — monthly buckets with visit counts — for the requested look-back window. Pair with `getAffiliateSalesGraph` to compute click-to-sale conversion ratios client-side. Sibling ops: `getAffiliateSalesGraph` (sales), `getAffiliateWebTraffic` (raw per-visit log entries), `getAffiliateRichReport`.

        **Query params:**
        - `days` (integer, optional, default `180`) — look-back window in days.

        **Returns:** `MonthlyCounts` — buckets keyed by month with aggregated visit counts.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateTrafficGraph
      parameters:
      - description: Number of days of traffic history to include in the graph data.
          Determines the time window for the returned data points.
        explode: true
        in: query
        name: days
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MonthlyCounts"
          description: Affiliate traffic graph data broken down by time period.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read aggregated affiliate referral click/visit time-series for chart
        rendering
      tags:
      - Billing
  /affiliate/web_traffic:
    get:
      description: |
        Returns the 20 most recent raw referral visits from the `affiliate_traffic` table — visitor IP, full referral URL, and timestamp per row. Use to audit traffic sources, identify top referrers, or investigate suspicious click patterns. Hard-coded limit 20 (no pagination); for longer-term analysis use `getAffiliateTrafficGraph` or export via `getAffiliateDownload`. Sibling ops: `getAffiliateTrafficGraph`, `getAffiliateSignups`, `getAffiliateRichReport`, `getAffiliateDownload`.

        **Path/Query/Body:** None.

        **Returns:** Array of `AffiliateTrafficRow`:
        - `traffic_id` (string) — row id (most-recent-first).
        - `traffic_ip` (string) — visitor IP (IPv4 or IPv6).
        - `traffic_url` (string) — referral landing URL.
        - `traffic_affiliate` (string) — affiliate (= session `account_id`).
        - `traffic_referrer` (string) — HTTP Referer (may be empty).
        - `traffic_timestamp` (string) — `YYYY-MM-DD HH:MM:SS` in account timezone.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateWebTraffic
      responses:
        "200":
          content:
            application/json:
              examples:
                AffiliateWebTrafficResponseExample:
                  value:
                  - traffic_id: "91839913"
                    traffic_ip: "2a06:98c0:3600::"
                    traffic_url: https://www.interserver.net/webhosting/?id=8
                    traffic_affiliate: "8"
                    traffic_referrer: ""
                    traffic_timestamp: 2023-09-30 06:30:27
                  - traffic_id: "91831932"
                    traffic_ip: "2a06:98c0:3600::"
                    traffic_url: https://www.interserver.net/webhosting/?id=8
                    traffic_affiliate: "8"
                    traffic_referrer: ""
                    traffic_timestamp: 2023-09-30 03:15:13
              schema:
                items:
                  $ref: "#/components/schemas/AffiliateTrafficRow"
                type: array
          description: The recent affiliate web traffic
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "List the 20 most recent affiliate referral visits with IP, referrer,\
        \ timestamp"
      tags:
      - Billing
  /affiliate/signups:
    get:
      description: |
        Returns referred-customer signup statistics with optional status filtering — counts, conversion data, and per-customer detail produced by `affiliates_clientside()`. The inner `data` shape varies by status filter; pass `default` for the full dataset. Sibling ops: `getAffiliateRichReport`, `getAffiliateSalesGraph`, `getAffiliateTrafficGraph`, `getAffiliateDownload`.

        **Query params:**
        - `st` (string, optional, default `default`) — status filter. `default` returns all; other values narrow the results to that status.

        **Returns:** `{data: <object>}` — signup counts, conversions, per-customer detail (shape depends on `st`).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateSignups
      parameters:
      - description: Filter signups by status. Use `default` to show all or pass a
          specific status value to narrow results.
        explode: true
        in: query
        name: st
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAffiliateSignups_200_response"
          description: Affiliate signup statistics.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read affiliate signup stats and per-customer conversion data
      tags:
      - Billing
  /affiliate/download:
    get:
      description: |
        Exports the affiliate signup report as a downloadable file in the requested format. Use for accounting, tax filings, or sharing reports outside the dashboard. **Response is a binary stream, not JSON** — the handler emits the file body with matching `Content-Type` + `Content-Disposition: attachment` headers and `exit()`s the request immediately. Consumers must read the raw response body. Sibling ops: `getAffiliateRichReport`, `getAffiliateSignups`, `getAffiliateSalesGraph`.

        **Query params:**
        - `ex` (string, optional, enum `csv`/`xls`/`xlsx`/`pdf`, default `csv`) — export format.
        - `st` (string, optional, default `default`) — status filter (same as `getAffiliateSignups`).
        - `year` (integer, optional, default current year) — report scope.

        **Returns:** File download with format-appropriate Content-Type:
        - `csv` → `text/csv`, filename `Interserver_Affiliates.csv`.
        - `xls` / `xlsx` → `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, filename `Interserver_Affiliates.<ext>`.
        - `pdf` → `application/pdf`, filename `Interserver_Affiliates.pdf`.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
      operationId: getAffiliateDownload
      parameters:
      - description: Filter by status.
        explode: true
        in: query
        name: st
        required: false
        schema:
          type: string
        style: form
      - description: "Export format: csv, xls, xlsx, or pdf. Defaults to csv."
        explode: true
        in: query
        name: ex
        required: false
        schema:
          enum:
          - csv
          - xls
          - xlsx
          - pdf
          type: string
        style: form
      - description: Year to filter the report. Defaults to the current year.
        explode: true
        in: query
        name: year
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          description: "Affiliate report file download. The response Content-Type\
            \ matches the requested format (text/csv, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,\
            \ or application/pdf)."
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Export the affiliate signup report as CSV, XLS, XLSX, or PDF file\
        \ download"
      tags:
      - Billing
  /backups:
    get:
      description: |-
        Use when enumerating all off-site backup storage services (SFTP-style remote storage subscriptions) on the authenticated customer's account. NOT for VPS/QS/webhosting in-place snapshots — those live under their own tags (`getVpsBackups`, `getQsBackups`, `getWebsitesBackups`). No query params, no body.
        Returns an array of rows; each row carries `backup_id`, `backup_name`, `backup_username`, `backup_status`, `services_name` (plan), and `backup_cost` (recurring price from `repeat_invoices`). Use `backup_id` as the path `{id}` for `getBackupInfo`, `getBackupLogin`, `getBackupInvoices`, `getBackupsWelcomeEmail`, `cancelBackup`. Errors: HTTP 401 if unauthenticated. Empty array when the customer has no backup services.
        Siblings: `getBackupInfo`, `getNewBackup`, `addBackup`.
      operationId: getBackupsList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/BackupRow"
                type: array
          description: The listing of backup storage services on your account.
          links:
            GetBackupDetails:
              description: Use the backup_id from the list to retrieve full service
                details including connection info and status.
              operationId: getBackupInfo
              parameters:
                id: $response.body#/0/backup_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List off-site backup storage subscriptions on the authenticated account
      tags:
      - Backups
  /backups/order:
    get:
      description: |-
        Use before placing an off-site backup storage order to fetch the available plans, their service-type IDs, and per-tier pricing needed to render an order form. No params, no body.
        Returns `{ packageCosts, serviceTypes }` — `packageCosts` is a map of `services_id` → recurring cost (from `services` where `services_module='backups'` and `services_buyable=1`); `serviceTypes` is the dispatcher output of `run_event('get_service_types', true, 'backups')` describing each tier. Pass the chosen `services_id` as `serviceType` to `validateBackupOrder` (PUT) for a price preview, then to `addBackup` (POST) to commit. Errors: HTTP 401 if unauthenticated.
        Siblings: `validateBackupOrder`, `addBackup`, `getBackupsList`.
      operationId: getNewBackup
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupsOrder"
          description: Information needed to generate an order form.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get backup-storage order form metadata and pricing tiers
      tags:
      - Backups
    post:
      description: |
        Step 3 of the backup-storage order flow. Revalidates via `validate_buy_storage()`, then calls `place_buy_storage()` which creates a `backups` service row, a `repeat_invoices` recurring entry, and the first `invoices` row. **Real billable order — call `validateBackupOrder` first.** Service is provisioned only after the invoice is paid. Sibling ops: `getNewBackup` (catalog), `validateBackupOrder` (quote), `getBackupInvoices` (billing history), `initiatePayment` (settle).

        **Body fields** (JSON or multipart):
        - `serviceType` (integer, required) — `services_id` from `getNewBackup`.
        - `coupon` (string, optional) — coupon code.
        - `period` (integer, optional, default `1`) — billing months.
        - `comment` (string, optional) — saved on the order row.

        **Returns** (on success): `{ continue: true, total_cost, iid, iids, real_iids, serviceId, invoice_description, cj_params }` — feed `real_iids` into `initiatePayment`. On validation failure: `{ continue: false, errors: [...] }` with HTTP 200.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.
        - `422` inside `errors[]` — coupon/plan/duplicate-hostname validation.
        - Explicit error text when no backend storage server is available for assignment.

        **Side effects:** new rows in `backups`, `repeat_invoices`, `invoices`; queued provisioning kicks off only after payment.

        **Related calls:**
        - **Prerequisite:** `validateBackupOrder`.
        - **Pay:** `getBillingInvoice` → `initiatePayment`.
        - **Poll status:** `getBackupInfo` (until `backup_status='active'`).
      operationId: addBackup
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BackupOrderPutRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/BackupOrderPutRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupOrderPostResponse"
          description: Response from the backup order call including invoice IDs for
            payment.
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/invoices_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: Place a new off-site backup storage order and generate the invoice
      tags:
      - Backups
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: "Order a managed off-site backup storage service. Returns invoice\
          \ IDs for /billing/pay/{method}/{invoices}."
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |-
        Use to dry-run a backup order — runs `validate_buy_storage()` to compute final price, apply any coupon, and surface validation errors before the customer commits. No invoice is created and no service is provisioned.
        Body (JSON or multipart): `serviceType` (services_id from `getNewBackup`), optional `coupon`, `period` (months, default 1), `comment`. Returns `{ continue, errors, serviceType, serviceCost, originalCost, repeatServiceCost, hostname, password, coupon, couponCode }`. Use the response to render a confirmation screen, then call `addBackup` (POST same path) to place the order. Errors: HTTP 401 unauthenticated; HTTP 422 surfaced inside `errors[]` (invalid coupon, ineligible plan, duplicate hostname).
        Siblings: `addBackup`, `getNewBackup`.
      operationId: validateBackupOrder
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BackupOrderPutRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/BackupOrderPutRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupOrderPutResponse"
          description: Validate Backup Order Response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate a backup-storage order and preview pricing without charging
      tags:
      - Backups
  /backups/{id}:
    delete:
      description: |-
        DESTRUCTIVE. Use to terminate a backup-storage subscription. Delegates to `CancelService::go($id)` with module `backups`, which marks the service for cancellation and stops future recurring billing; data on the storage backend may become inaccessible at end of cycle. Path param: `id` from `getBackupsList`. No body.
        Returns `BackupsCancelResponse`. Caveats: irreversible — a new order via `addBackup` is required to restore service, with a new IP/username and no migration of prior data. Does NOT delete VPS/QS/webhosting in-place snapshots (those live under their own tags). Errors: HTTP 401 unauthenticated; HTTP 404 if `id` is not owned by the caller; HTTP 409 if the service is already cancelled or pending cancellation.
        Siblings: `addBackup`, `getBackupInfo`, `getBackupInvoices`.
      operationId: cancelBackup
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cancelBackup_200_response"
          description: Backups Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel an off-site backup storage subscription
      tags:
      - Backups
    get:
      description: |-
        Use to fetch the full management view for one backup-storage subscription. Path param: `id` (backup service ID from `getBackupsList`). No body.
        Returns `serviceInfo` (with `backup_username`, `backup_ip`, `backup_status`, `backup_quota`, `backup_type`, `backup_invoice`), plus `billingDetails`, `extraInfoTables`, `package`, `custCurrency`, and `client_links` (rewritten to surface the link target rather than the raw queue URL). `admin_links`, internal `settings`, and `csrf` are stripped. Errors: HTTP 401 unauthenticated; HTTP 404 if `id` does not belong to the caller (cross-account access blocked by `get_service`).
        Siblings: `getBackupLogin` (open storage panel session), `getBackupInvoices`, `getBackupsWelcomeEmail`, `cancelBackup`, `updateBackupInfo`.
      operationId: getBackupInfo
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Backup"
          description: "Full backup service details including serviceInfo, billingDetails,\
            \ and client_links."
          links:
            GetBackupInvoices:
              description: Retrieve invoices for this backup service.
              operationId: getBackupInvoices
              parameters:
                id: $request.path.id
            GetBackupLogin:
              description: Obtain a login session URL for the backup storage panel.
              operationId: getBackupLogin
              parameters:
                id: $request.path.id
            CancelBackup:
              description: Cancel this backup service.
              operationId: cancelBackup
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get details of a specific off-site backup storage service
      tags:
      - Backups
    post:
      description: |-
        Use to update non-billing metadata (e.g. stored credentials, comment, hostname) on an existing off-site backup storage service. Path param: `id` from `getBackupsList`. Body fields are forwarded to the same `View::go()` handler as the GET; consult the order form for accepted keys.
        Returns the standard `SuccessTextResponse`. Caveats: this endpoint does NOT change the plan, quota, or billing — those require cancel + reorder via `cancelBackup` and `addBackup`. It also does NOT trigger any backend SFTP credential rotation. Errors: HTTP 401 unauthenticated; HTTP 404 if `id` is not owned by the caller; HTTP 422 on invalid input.
        Siblings: `getBackupInfo`, `cancelBackup`, `getBackupLogin`.
      operationId: updateBackupInfo
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update stored metadata for a backup-storage subscription
      tags:
      - Backups
  /backups/{id}/invoices:
    get:
      description: |-
        Use to retrieve all invoices tied to one off-site backup storage service — useful for confirming billing status, locating an unpaid invoice to pay, or reconciling renewals. Path param: `id` from `getBackupsList`. Delegates to the shared `InvoicesList::go()` handler with module `backups`. No body.
        Returns `ChargeInvoiceRows` (array of invoice rows with `invoices_id`, status, amount, dates). Feed `invoices_id` into `getBillingInvoice` for full detail or `/billing/pay/{method}/{invoices}` to settle an unpaid invoice. For the account-wide invoice list use the Billing tag instead. Errors: HTTP 401 unauthenticated; HTTP 404 if `id` is not owned by the caller.
        Siblings: `getBackupInfo`, `addBackup`.
      operationId: getBackupInvoices
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List invoices for a single backup-storage subscription
      tags:
      - Backups
  /backups/{id}/login:
    get:
      description: |-
        Use to drop the customer straight into the off-site backup storage management panel without a separate login prompt. Calls `get_storage_session($id)` to mint a one-shot session URL; treat the URL as short-lived and credentials-equivalent — do not log or share.
        Path param: `id` from `getBackupsList`. No body. Returns `BackupLoginResponse` (`success`, session URL/token, optional connection hints). On `success=false` the handler returns `json_error(text)` (HTTP 400) with the upstream reason. Errors: HTTP 401 unauthenticated; HTTP 404 if `id` is not owned by the caller; backend errors when the storage server is unreachable.
        Siblings: `getBackupInfo` (SFTP `backup_username`/`backup_ip` for direct connections), `getBackupsWelcomeEmail` (resend setup credentials).
      operationId: getBackupLogin
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BackupLoginResponse"
          description: Login session details for the backup storage service.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Open a single sign-on session URL for the backup storage panel
      tags:
      - Backups
  /backups/{id}/welcome_email:
    get:
      description: |-
        Use when the original welcome email was lost or never arrived. Resends connection credentials (SFTP host, username, quota) and setup instructions to the account email by invoking the module's `backup_welcome_email($id)` helper.
        Path param: `id` from `getBackupsList`. No body. Returns `SuccessTextResponse` with `text='Welcome Email has been resent.'`. Caveats: only works while the service is `active`; cancelled/pending services will return 409. Email is sent to the customer-of-record on file — there is no override recipient parameter. Errors: HTTP 401 unauthenticated; HTTP 404 if `id` is not owned by the caller (`Invalid Service Passed`); HTTP 409 if `backup_status` is not `active` (`Service is not active`).
        Siblings: `getBackupLogin`, `getBackupInfo`.
      operationId: getBackupsWelcomeEmail
      parameters:
      - description: The backup service ID. Use the `backup_id` from `GET /backups`
          to identify the service.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the welcome email for an off-site backup storage service
      tags:
      - Backups
  /billing/cart:
    get:
      description: |
        Returns the customer's checkout state — every pending/unpaid invoice on the account aggregated as a cart, plus available payment methods, currency totals, and checkout metadata. Use to render a checkout page or, in agent flows, as a pre-payment confirmation step before calling `initiatePayment`. Backed by the `cart` helper module; `modules_json` and `csrf_token` are stripped from the response. Read-only. Sibling ops: `getBillingInvoices` (raw list), `getBillingInvoice` (one invoice in detail), `initiatePayment` (pay), `getBillingPrePays` (check prepay balance first).

        **Path/Query/Body:** None.

        **Returns:** A cart object with:
        - Line items aggregated from unpaid `invoices` rows for the session account.
        - Currency-normalized subtotal / total.
        - Available payment methods (filtered by feature flags, account country, and which gateways are enabled): `cc`, `paypal`, `btcpay`, `coinbase`, `payu`, `ccavenue`, `cashfree`, `payssion`, `prepay`.
        - Per-invoice description, module, service-id, amount.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **List unpaid invoices directly:** `getBillingInvoices`.
        - **Drill into one invoice:** `getBillingInvoice`.
        - **Pay:** `initiatePayment` (use the cart's invoice ids or the `SERVICEvpsN` / `INVvpsN` tag forms).
        - **Top up prepay first:** `getBillingPrePays`, `addBillingPrepay`.
      operationId: getBillingCart
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Cart contents and checkout metadata.
                type: object
          description: Current shopping cart contents and available payment methods.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read the current shopping cart contents, totals, and available payment\
        \ methods"
      tags:
      - Billing
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: View shopping cart contents and pick a payment method before
          checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
  /billing/prepays:
    get:
      description: |
        Returns every prepay deposit on the account — funded or pending — with remaining balances, modules they're scoped to, and the `automatic_use` flag controlling whether the balance auto-applies to future invoices. Use to gate `method=prepay` at checkout (a prepay must be funded to count toward payment) or to render a prepays management page. Read-only. `csrf_token` is stripped from the helper output. Sibling ops: `addBillingPrepay` (top up), `deleteBillingPrepay` (remove), `initiatePayment` (`method=prepay`), `getBillingCart`.

        **Path/Query/Body:** None.

        **Returns:** Object with per-prepay rows:
        - `prepay_id` (integer).
        - `prepay_module` (string) — service module the prepay is scoped to (or `default` for any).
        - `prepay_amount` (decimal) — original deposit amount.
        - `prepay_remaining` (decimal) — funds left.
        - `prepay_automatic_use` (bool) — auto-apply to invoices.
        - `prepay_paid` (bool) — whether the funding invoice has been paid (unpaid prepays are listed but unusable).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Top up:** `addBillingPrepay` (returns an invoice id you then pay via `initiatePayment`).
        - **Pay with prepay:** `initiatePayment` with `method=prepay`.
        - **Remove an unfunded prepay:** `deleteBillingPrepay`.
        - **Cart view:** `getBillingCart` (includes prepay summary).
      operationId: getBillingPrePays
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Prepay balance data.
                type: object
          description: Prepay balances and metadata.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List prepay deposits on the account — remaining balance and auto-use
        flags
      tags:
      - Billing
    post:
      description: |
        Creates a prepay row (`prepays` table) at the requested amount and inserts a matching `invoices` row (`Prepay ID {pid} Invoice`) that the customer must pay through `initiatePayment` before the balance becomes usable. The prepay is added with `PREPAY_TYPE_ANY` / `PREPAY_SERVICE_ANY` defaults via `add_prepay()`. Use to seed an account balance the customer can later spend via `method=prepay` at checkout. **Real money** — funding the returned invoice charges a real payment method. Sibling ops: `getBillingPrePays`, `deleteBillingPrepay`, `getBillingInvoice`, `initiatePayment`.

        **Body fields (JSON or multipart, schema `BillingPrepayRequest`):**
        - `amount` (number, required) — deposit size in account currency. **Minimum $10**; smaller values are rejected.
        - `module` (string, required) — service module scope (`default` for any service, or specific like `vps`, `webhosting`).
        - `automatic_use` (bool, required) — when `true`, the balance auto-applies to future invoices in the scoped module.

        **Returns:** `{text: "Thank you! Prepay created! Kindly pay the invoice to activate the prepay fund.", invoice: <integer>}` — pass `invoice` to `initiatePayment` (use a real `method` like `cc` / `paypal`, not `prepay` — you can't fund a prepay with a prepay).

        **Side effects:**
        - Inserts `prepays` row.
        - Inserts `invoices` row (`invoices_description = "Prepay ID {pid} Invoice"`, `invoices_paid=0`, `invoices_module='default'`).

        **Auth:** Session/API key.

        **Errors:**
        - `Sorry! Minimum prepay amount is $10.00` — amount below floor.
        - `Something went wrong! Try again or contact our support team!` — invoice insert failed.
        - `401` — unauthenticated.

        **Related calls:**
        - **Confirm invoice:** `getBillingInvoice` with the returned `invoice` id.
        - **Pay it:** `initiatePayment` (`method=cc|paypal|...`, not `prepay`).
        - **Verify it's now usable:** `getBillingPrePays` (look for `prepay_remaining > 0`).
        - **Cancel before paying:** `deleteBillingPrepay`.

        **Example happy path:**
        ```text
        POST /apiv2/billing/prepays { "amount": 100, "module": "default", "automatic_use": true }
        -> { "text": "...", "invoice": 25296701 }
        GET /apiv2/billing/pay/cc/25296701
        -> { "type": "single", "text": "Payment processed." }
        GET /apiv2/billing/prepays
        -> [{ "prepay_id": 99, "prepay_remaining": 100, ... }]
        ```
      operationId: addBillingPrepay
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingPrepayRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BillingPrepayRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Create a prepay deposit and return an invoice id to fund it
      tags:
      - Billing
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: "Top up prepay balance — creates an invoice you then pay via\
          \ /billing/pay/{method}/{invoices}."
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
  /billing/prepays/{id}:
    delete:
      description: |
        Removes a prepay from the account, with one safety rule: a prepay that still has usable credit (`prepay_remaining > $0.01`) cannot be deleted *unless* it also has unpaid funding invoices we can clean up — in which case those unpaid `invoices` rows are deleted and the prepay row stays. Use to back out a never-funded prepay, or to surface stuck unpaid funding invoices. **Irreversible** — funded credit is unrecoverable through this endpoint. Sibling ops: `getBillingPrePays`, `addBillingPrepay`, `deleteBillingInvoice`.

        **Path param:**
        - `id` (integer, required) — prepay id from `getBillingPrePays.prepay_id`.

        **Body:** None.

        **Returns:**
        - When unpaid funding invoices were stripped but prepay still has funds: `"PrePay {id} Unpaid Invoices Deleted"`.
        - When the prepay row was deleted: `"PrePay {id} deleted."`.

        **Side effects:**
        - Deletes any unpaid `invoices` rows matching `invoices_description = "Prepay ID {id} Invoice"` and `invoices_paid=0`.
        - Deletes the `prepays` row when remaining balance ≤ $0.01.

        **Auth:** Session/API key.

        **Errors:**
        - `Invalid Prepay` — `id` not found.
        - `That prepay still hands funds available on it` — funds remain AND no unpaid invoices to clean up.
        - `There was an error deleting the prepay, please contact support` — delete affected 0 rows.
        - `401` — unauthenticated.

        **Related calls:**
        - **List first:** `getBillingPrePays`.
        - **Re-add later:** `addBillingPrepay`.
        - **Cancel a specific funding invoice:** `deleteBillingInvoice` (routes prepay invoices here automatically).
      operationId: deleteBillingPrepay
      parameters:
      - description: The prepay balance ID to delete.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Delete an unfunded prepay or strip its unpaid funding invoices
      tags:
      - Billing
  /billing/invoices:
    get:
      description: |
        Returns the customer's complete invoice ledger — every charge, paid or unpaid, across every service module. Use to render a billing-history page, find an unpaid invoice id to pass to `initiatePayment`, or audit recent activity. Server-side strips the first synthetic header row from `get_view_invoices()` and reindexes the array. Read-only. The response includes a Link to `getBillingInvoice` for drilling into any row. Sibling ops: `getBillingInvoice`, `deleteBillingInvoice`, `initiatePayment`, `getBillingCart`, `getBillingPrePays`.

        **Path/Query/Body:** None.

        **Returns:** `BillingInvoiceList` — object containing:
        - `rows` (array) — per-invoice summaries: `id`, `amount`, `paid`, `description`, `date`, `due_date`, `module`, `service` (service-id within the module), `currency`.
        - Aggregate totals across the array (totals object: `total`, `paid_total`, `unpaid_total`).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Drill into one invoice:** `getBillingInvoice`.
        - **Pay an unpaid invoice:** `initiatePayment`.
        - **Cancel an unpaid pending-service invoice:** `deleteBillingInvoice` (only works on pending services / unpaid prepays).
        - **Per-service invoices instead:** `getVpsInvoices`, `getDomainInvoices`, `getMailInvoices`, `getBackupInvoices`, etc.
      operationId: getBillingInvoices
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingInvoiceList"
          description: Invoice listing and summary for the account.
          links:
            GetInvoiceDetails:
              description: Use an invoice ID from the list to retrieve the full invoice
                details for payment or review.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/invoices/0/id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List every invoice on the account with summary totals and paid/unpaid
        status
      tags:
      - Billing
  /billing/invoices/{id}:
    delete:
      description: |
        Cancels an unpaid invoice and cleans up the records it represents. Behavior depends on what the invoice funds: a **prepay** invoice is routed to `deleteBillingPrepay`; an **initial service charge** (where `repeat_invoices_id` matches the service's `_invoice` field) deletes the `repeat_invoices` row, all child `invoices`, AND the pending service row from the module's table; an **addon/recurring** invoice just deletes that one `invoices` row plus its `repeat_invoices` row. **Only invoices for services in `pending` status can be deleted** — once provisioned, the service must be cancelled via the per-service Cancel endpoint instead. **Irreversible**. Sibling ops: `getBillingInvoice`, `deleteBillingPrepay`, `VPSCancel` / `CancelDomain` / `mailCancel` / `webhostingCancel` / etc.

        **Path param:**
        - `id` (integer, required) — invoice id (`invoices_type=1`, ownership enforced via `invoices_custid`).

        **Body:** None.

        **Returns:** `Invoice Deleted` text.

        **Side effects:** (depends on invoice type)
        - **Prepay invoice** (description matches `Prepay ID N Invoice`) — delegates to `deleteBillingPrepay($pid)`.
        - **Initial service invoice** (`repeat_invoices_id == service._invoice`) — deletes:
          - the `repeat_invoices` row,
          - every `invoices` row for that service,
          - the service row in `{settings['TABLE']}`.
        - **Addon/recurring invoice** — deletes only the matching `repeat_invoices` row and the single `invoices` row.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `Invalid invoice` — `id` not found or wrong owner.
        - `Invalid service` — invoice references a service that no longer exists.
        - `Can only delete invoices for pending services or prepays` — service is `active`/`suspended`/`cancelled`.
        - `401` — unauthenticated.

        **Related calls:**
        - **List candidates:** `getBillingInvoices`.
        - **Detail first:** `getBillingInvoice`.
        - **For active services:** `VPSCancel`, `CancelDomain`, `mailCancel`, `webhostingCancel`, `licensesCancel`, `sslCancel`, `cancelScrubIp`, `floating_ipsCancel`, `cancelBackup`, `quickserversCancel`, `serversCancel` — these use `Billing\CancelService::go()`.
        - **For prepay invoices:** `deleteBillingPrepay` (delegated automatically).
      operationId: deleteBillingInvoice
      parameters:
      - description: The invoice ID to delete. Only unpaid invoices can be deleted.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a pending unpaid invoice — and its pending service or repeat
        invoice
      tags:
      - Billing
    get:
      description: |
        Returns the full rendered invoice payload for a single invoice — backed by `get_invoice_data()`, the same helper that builds the email-style invoice document. Use to confirm the exact balance due and the invoice description before calling `initiatePayment`, or to render an invoice viewer page. Read-only. The response is an email-style/HTML payload (not a structured line-item array) — for a structured cart-style summary use `getBillingCart`. The response includes a Link to `deleteBillingInvoice` for unpaid pending-service invoices. Sibling ops: `getBillingInvoices`, `deleteBillingInvoice`, `initiatePayment`, `getBillingCart`, per-service `getVpsInvoices` / `getMailInvoices` / etc.

        **Path param:**
        - `id` (integer, required) — invoice id from `getBillingInvoices.rows[].id`, from an order endpoint's response (e.g. `addVps.iid`), or from a per-service invoice list.

        **Body:** None.

        **Returns:** `BillingInvoiceDetail` — full rendered invoice payload (email body) with line items, totals, customer/billing info, and paid status. The exact shape mirrors what gets sent to the customer.

        **Auth:** Session/API key. Ownership enforced through the invoice's `invoices_custid`.

        **Errors:**
        - `Invalid Invoice` — `id` not found or owned by another account.
        - `401` — unauthenticated.

        **Related calls:**
        - **Pay it:** `initiatePayment` (`/billing/pay/{method}/{id}`).
        - **Delete if pending/unpaid:** `deleteBillingInvoice`.
        - **List all:** `getBillingInvoices`.
        - **Cart-style summary across all unpaid:** `getBillingCart`.
      operationId: getBillingInvoice
      parameters:
      - description: The invoice ID. Use IDs from `GET /billing/invoices` or from
          order responses.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingInvoiceDetail"
          description: Detailed invoice payload for the requested invoice.
          links:
            DeleteInvoice:
              description: Delete this invoice if it is still unpaid.
              operationId: deleteBillingInvoice
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read full invoice detail — line items, totals, paid status, customer\
        \ info"
      tags:
      - Billing
  /billing/creditcards:
    post:
      description: |
        Stores a new credit card on the account so it can later be selected via `updateBillingPaymentMethod` or used directly with `initiatePayment` (`method=cc`). The card number has dashes stripped and is sanitized through `FILTER_SANITIZE_NUMBER_INT`; billing address fields are HTML-entity-escaped server-side; the CC number is encrypted at rest via `App::encrypt()`. The flow may return `action='verify'` indicating a two-step micro-charge verification is required before the card is usable — complete it with `patchBillingCreditCardVerify` then `postBillingCreditCardVerify`. Sibling ops: `updateBillingCreditCard`, `deleteBillingCreditCard`, `patchBillingCreditCardVerify`, `postBillingCreditCardVerify`, `updateBillingPaymentMethod`.

        **Body fields (JSON or multipart, schema `BillingAddCcRequest`):**
        - `cc` (string, required) — card number; dashes stripped, non-digits filtered.
        - `name` (string, required) — cardholder name.
        - `cc_exp` (string, required) — `MM/YYYY`.
        - `address` (string, required), `city`, `state`, `country`, `zip` (strings) — billing address; HTML-entity-escaped.

        **Returns:**
        - **Added directly:** `{success: true, text: "Card Added Successfully!"}`.
        - **Verification needed:** `{success: false, text: "Kindly verify your card by updating the amounts in the fields", action: "verify"}` — proceed to `patchBillingCreditCardVerify`.

        **Side effects:**
        - Inserts the encrypted card into the account's `ccs` array (managed via `parse_ccs` / `add_cc`).
        - May trigger a small initial test charge (gateway-dependent).
        - First-card-on-account triggers MaxMind + FraudRecord risk-score recomputation.

        **Auth:** Session/API key.

        **Errors:**
        - `Card number, Full Name, Expiry date are required!` — required field missing/empty.
        - `401` — unauthenticated.
        - Gateway/AVS error text — declined, mismatch, etc.

        **Related calls:**
        - **Verify (if `action='verify'`):** `patchBillingCreditCardVerify` (CVV + initiate micro-charge) → `postBillingCreditCardVerify` (submit amounts).
        - **Make it the default:** `updateBillingPaymentMethod` with `payment_method=cc<idx>`.
        - **Pay an invoice with it:** `initiatePayment` (`method=cc`).
      operationId: addBillingCreditCard
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingAddCcRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BillingAddCcRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Store a credit card on the account — may return a verification flow
      tags:
      - Billing
  /billing/creditcards/{id}:
    delete:
      description: |
        Removes the indexed credit card from the account's `ccs` collection. If the deleted card was also the account's primary `cc`, the primary field is cleared — `initiatePayment` (`method=cc`) will then return an error until a new default is designated via `updateBillingPaymentMethod`. **Irreversible** — to re-store the same card, re-run `addBillingCreditCard`. Sibling ops: `addBillingCreditCard`, `updateBillingCreditCard`, `updateBillingPaymentMethod`, `getBillingCreditCardVerify`.

        **Path param:**
        - `id` (integer, required) — credit card index from `parse_ccs`.

        **Body:** None.

        **Returns:** `Card removed successfully.`.

        **Side effects:**
        - Removes the entry from the `ccs` array; re-serialized via `myadmin_stringify`.
        - When the deleted card was primary: clears account-level `cc`.

        **Auth:** Session/API key. Card ownership enforced.

        **Errors:**
        - `Invalid Credit Card Passed` — `id` not in `parse_ccs`.
        - `401` — unauthenticated.

        **Related calls:**
        - **Set a new default:** `updateBillingPaymentMethod`.
        - **Add a replacement:** `addBillingCreditCard`.
      operationId: deleteBillingCreditCard
      parameters:
      - description: The credit card ID to remove. Use IDs from `GET /billing/creditcards`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Remove a stored credit card from the account
      tags:
      - Billing
    post:
      description: |
        Updates the expiration date on a stored credit card and re-encrypts the card record. If the updated card matches the account's primary `cc`, the account-level `cc_exp` is also refreshed. If no MaxMind risk score exists yet for the card, `update_maxmind()` is called to compute one. Use to fix an upcoming expiration before recurring charges fail. Sibling ops: `addBillingCreditCard`, `deleteBillingCreditCard`, `getBillingCreditCardVerify`, `postBillingCreditCardVerify`, `updateBillingPaymentMethod`.

        **Path param:**
        - `id` (integer, required) — credit card index (the key in the account's `ccs` array, returned by `parse_ccs` and surfaced as `cc<idx>` in `updateBillingPaymentMethod`).

        **Body fields:**
        - `cc_exp` (string, required) — new expiration in `MM/YYYY` format.

        **Returns:** `Card updated successfully.`.

        **Side effects:**
        - Updates the `ccs` array (re-serialized via `myadmin_stringify`) on the account.
        - When the card == primary `cc`, the account-level `cc_exp` is also written.
        - Triggers `update_maxmind($custid, false, $cc_idx)` if no risk score exists.

        **Auth:** Session/API key. Card ownership enforced via `parse_ccs`.

        **Errors:**
        - `Invalid Credit Card Passed` — `id` not in `parse_ccs`.
        - `Please enter valid card expiry date` — `cc_exp` body field missing.
        - `Invalid expiration date. It must be in the form of MM/YYYY` — wrong format.
        - `401` — unauthenticated.

        **Related calls:**
        - **Verify a freshly added card:** `patchBillingCreditCardVerify` → `postBillingCreditCardVerify`.
        - **Remove the card:** `deleteBillingCreditCard`.
        - **Make it default:** `updateBillingPaymentMethod` with `payment_method=cc<idx>`.
      operationId: updateBillingCreditCard
      parameters:
      - description: The credit card ID. Use IDs from `GET /billing/creditcards` or
          the response from `POST /billing/creditcards`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Refresh stored card expiration and re-trigger MaxMind fraud scoring
      tags:
      - Billing
  /billing/creditcards/{id}/verify:
    get:
      description: |
        Status probe for the credit-card verification flow. Read-only — current implementation returns a placeholder string indicating verification is pending; the actual two-step verification happens via `patchBillingCreditCardVerify` (initiate dual micro-charge with CVV) followed by `postBillingCreditCardVerify` (submit the charged amounts). Use to drive the UI's "verify card" form rendering. Sibling ops: `patchBillingCreditCardVerify`, `postBillingCreditCardVerify`, `addBillingCreditCard`, `updateBillingPaymentMethod`.

        **Path param:**
        - `id` (integer, required) — credit card index from `parse_ccs`.

        **Body:** None.

        **Returns:** `Verification requirements` (placeholder text — reserved for future structured response with `requires_cvv` / `requires_amounts` flags).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Step 1 of verify flow:** `patchBillingCreditCardVerify`.
        - **Step 2 of verify flow:** `postBillingCreditCardVerify`.
        - **Add a new card:** `addBillingCreditCard`.
      operationId: getBillingCreditCardVerify
      parameters:
      - description: The credit card ID to verify. Use the ID returned from `POST
          /billing/creditcards`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Probe whether a stored card still needs micro-charge verification
      tags:
      - Billing
    patch:
      description: |
        Step 1 of the two-step card-verification flow. After `addBillingCreditCard` returns `action='verify'`, call this with the card's CVV to place two small charges (cents-scale) on the card. The customer must then look up the exact amounts in their bank statement and submit them via `postBillingCreditCardVerify` to finalize verification. **After 3 failed CVV attempts** (`cc_fails_<cc>` counter on the account) the card is locked from further verification attempts — contact support. Sibling ops: `getBillingCreditCardVerify`, `postBillingCreditCardVerify`, `addBillingCreditCard`, `updateBillingPaymentMethod`.

        **Path param:**
        - `id` (integer, required) — credit card index from `parse_ccs`.

        **Body fields:**
        - `cc_ccv2` (string, required) — the 3- or 4-digit CVV/CVC code from the back (or front, for Amex) of the card.

        **Returns:** `Your card is charged. Please enter the amounts charged up!` — surface to the UI to prompt for the two amounts.

        **Side effects:**
        - Places two test charges via `verify_cc_charge()` (gateway-side).
        - On failure: increments `cc_fails_<cc>` on the account.

        **Auth:** Session/API key. Card ownership enforced.

        **Errors:**
        - `Invalid Credit Card Passed` — `id` not in `parse_ccs`.
        - `Reached the max number of tries to authenticate this card` — `cc_fails_<cc> > 3`.
        - `Missing or blank CVV` — `cc_ccv2` absent or empty.
        - Gateway error text — charge attempt failed.
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite:** `addBillingCreditCard` (must have returned `action='verify'`).
        - **Next (step 2):** `postBillingCreditCardVerify` (submit `cc_amount1` + `cc_amount2`).
        - **After verification:** `updateBillingPaymentMethod` to make it the default.
      operationId: patchBillingCreditCardVerify
      parameters:
      - description: The credit card ID to verify. Use the ID returned from `POST
          /billing/creditcards`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/patchBillingCreditCardVerify_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/patchBillingCreditCardVerify_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Place two micro-charges on the card to start CVV verification (step
        1 of 2)
      tags:
      - Billing
    post:
      description: |
        Step 2 of the two-step card-verification flow. Submits the two exact micro-charge amounts the customer saw on their statement (placed by `patchBillingCreditCardVerify`) so the gateway can confirm the customer controls the card. On success, the card is marked verified and can be selected via `updateBillingPaymentMethod` (`payment_method=cc<idx>`) or used directly with `initiatePayment` (`method=cc`). After 3 failed attempts (`cc_fails_<cc> > 3`) the card is locked. Sibling ops: `getBillingCreditCardVerify`, `patchBillingCreditCardVerify`, `addBillingCreditCard`, `updateBillingPaymentMethod`.

        **Path param:**
        - `id` (integer, required) — credit card index from `parse_ccs`.

        **Body fields (schema `BillingVerifyCcRequest`):**
        - `cc_amount1` (number, required) — first micro-charge amount (in dollars, decimal).
        - `cc_amount2` (number, required) — second micro-charge amount.

        **Returns:** Verification success text (gateway-returned).

        **Side effects:**
        - Marks the card as verified when amounts match.
        - On failure: increments `cc_fails_<cc>` on the account.

        **Auth:** Session/API key. Card ownership enforced.

        **Errors:**
        - `Invalid Credit Card Passed` — `id` not in `parse_ccs`.
        - `Reached the max number of tries to authenticate this card` — `cc_fails_<cc> > 3`.
        - `Missing charge amounts` — `cc_amount1` or `cc_amount2` absent.
        - Verification failure text (status `failed` / `error` / `warning`) — amounts don't match.
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite (step 1):** `patchBillingCreditCardVerify`.
        - **Next:** `updateBillingPaymentMethod` to make the verified card default, or `initiatePayment` (`method=cc`) to pay immediately.
      operationId: postBillingCreditCardVerify
      parameters:
      - description: The credit card ID to verify. Use the ID returned from `POST
          /billing/creditcards`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingVerifyCcRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BillingVerifyCcRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Submit two micro-charge amounts to finalize card verification (step
        2 of 2)
      tags:
      - Billing
  /billing/payment_method:
    post:
      description: |
        Sets the account's preferred payment method for recurring/automatic charges and (when applicable) promotes a specific stored credit card to be the primary `cc` on the account. Use after `addBillingCreditCard` + verification to select the new card, or when switching between PayPal and credit-card billing. First-time payment-method assignment triggers `update_maxmind()` and `update_fraudrecord()` risk-score generation. Sibling ops: `addBillingCreditCard`, `postBillingCreditCardVerify`, `deleteBillingCreditCard`, `initiatePayment`.

        **Body fields (JSON or multipart, schema `BillingPaymentMethodRequest`):**
        - `payment_method` (string, required) — one of:
          - `cc` — use the existing primary credit card.
          - `cc<idx>` (e.g. `cc2`) — promote the card at index `idx` (from `parse_ccs`) to primary. Must be verified.
          - `paypal` — switch to PayPal.
        - `cc_auto` (string `0`/`1`, optional) — auto-charge flag. Implicitly set to `1` when selecting `cc`/`cc<idx>`, `0` for `paypal`.

        **Returns:** `{text: "Payment Method Updated"}`.

        **Side effects:**
        - When `payment_method=cc<idx>`: copies the indexed card's encrypted `cc` and `cc_exp` onto the account's primary fields.
        - First time a payment method is set: runs MaxMind risk score, then FraudRecord score.

        **Auth:** Session/API key.

        **Errors:**
        - `Invalid Credit Card Specified` — `cc<idx>` is malformed or `idx` not found in `parse_ccs`.
        - `This CC has not been verified.` — the chosen card hasn't completed `postBillingCreditCardVerify`.
        - `Invalid Payment Method Specified` — value not in `{cc, paypal, cc<idx>}`.
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite for `cc<idx>`:** `addBillingCreditCard` → `patchBillingCreditCardVerify` → `postBillingCreditCardVerify`.
        - **Now pay an invoice:** `initiatePayment` (`method=cc` will use the default; `method=paypal` if you switched).
        - **Audit current methods:** `getAccountInfo` (account profile shows cards as masked).
      operationId: updateBillingPaymentMethod
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BillingPaymentMethodRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/BillingPaymentMethodRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Set the account's default payment method for recurring/auto charges
      tags:
      - Billing
  /captcha:
    get:
      description: |
        Fetches a fresh captcha challenge image to display before submitting `submitSignup` (or any unauthenticated form that needs human verification). Public endpoint — no authentication required. Sibling ops: `getLoginInfo` (returns a captcha alongside other login-page data), `submitSignup` (consumes the answer), `submitLogin`.

        **Path/Query/Body:** None.

        **Returns:** `{ captcha: string }` — `captcha` is a `data:image/jpeg;base64,...` URL ready to drop into an `<img src>`.

        **Side effects:** the phrase is stored server-side in `$_SESSION['captcha']` (also aliased to the signup-flow key `$_SESSION['captchaSignup']` and forgot-password key `$_SESSION['captchaFP']`). The browser must send the same `PHPSESSID` cookie back when posting the answer.

        **Charset:** 8 chars from `3456789ABCDEFGHJKLMNPQRSTWXY` — no ambiguous `0`/`1`/`I`/`O`/`2`/`Z`.

        **Related calls:**
        - **Consumers:** `submitSignup`, `submitLogin`.
        - **One-shot login bootstrap:** `getLoginInfo`.
        answer in `captcha` field).
      operationId: getCaptcha
      responses:
        "200":
          content:
            application/json:
              examples:
                CaptchaResponseExample:
                  value:
                    captcha: "data:image/jpeg;base64,/9j/4AAQ"
              schema:
                $ref: "#/components/schemas/CaptchaResponse"
          description: An array containing a `captcha` field containing a string with
            a base64 encoded captcha image.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: Fetch a base64 JPEG captcha challenge for human verification
      tags:
      - Public
  /buy_now_servers_list:
    get:
      description: |-
        Use to browse pre-built dedicated servers ready for immediate provisioning (Rapid Deploy / marketplace). No params, no body. Pulls live inventory from `mynew.interserver.net/ajax/server_a.php`.
        Returns: array of `{ server_id, cpu: [model, {img,type,speed,num_cpus,num_cores}], memory, disk, bandwidth, ips, location, price }`. The `server_id` is the marketplace asset id — feed it into `buyItNowServerOrder` (GET options for asset `?a=<id>`) and `placeBuyNowServer` (POST to commit). Errors: 401 if session expired.
        Sibling ops: `buyItNowServerOrder` (configure asset), `placeBuyNowServer` (purchase), `getNewServer`/`addServer` (custom-spec build, not pre-built), `getServerList` (already-owned servers).
      operationId: getMPServers
      responses:
        "200":
          content:
            application/json:
              example:
              - server_id: "10467"
                cpu:
                - Xeon E3-1270v6
                - img: xeon-e3.png
                  type: XEON
                  speed: ""
                  num_cpus: "1"
                  num_cores: "4"
                memory: 64GB
                disk:
                  "4TB SSD": 4TB SSD
                bandwidth: 1Gbps Unmetered
                ips: 1 Vlan Ip (/30)
                location: NYC Region
                price: 64
              schema:
                $ref: "#/components/schemas/BuyItNowList"
          description: Marketplace Buy it now servers list
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List Rapid Deploy (Buy-It-Now) marketplace dedicated servers with live
        pricing
      tags:
      - Servers
      - Public
  /dns:
    description: View and manage your DNS domains and records
    get:
      description: |
        Returns every PowerDNS-hosted authoritative zone owned by the authenticated account, one row per zone, with the IP from the apex `A` record. Canonical entry point for discovering zone IDs before reading or editing records. The list is filtered server-side by session `account_id` — cross-account zones are never returned. Empty array means the account holds no zones (not an error). **Note:** this is the hosted DNS zone list, not registrar delegation — use the Domains tag's `updateDomainNameservers` to point a registered domain at `cdns1.interserver.net`/`cdns2.interserver.net`. Sibling ops: `getDnsDomain`, `addDnsDomain`, `addDnsRecord`, `deleteDnsDomain`.

        **Path/Query/Body:** None.

        **Returns:** Array of `DnsListItem`:
        - `id` (integer) — zone ID; pass to `getDnsDomain` / `addDnsRecord` / `deleteDnsDomain`.
        - `name` (string) — zone FQDN (e.g. `example.com`).
        - `content` (string) — IP from the apex `A` record matching the zone name (empty when no apex A exists yet).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Per-zone record list:** `getDnsDomain`.
        - **Add a zone:** `addDnsDomain`.
        - **Add a record to an existing zone:** `addDnsRecord`.
        - **Registrar delegation:** `getDomainNameservers` / `updateDomainNameservers` (Domains tag).
      operationId: getDnsList
      responses:
        "200":
          content:
            application/json:
              examples:
                DnsListExample:
                  value:
                  - id: 68
                    name: hussfamily.com
                    content: 64.20.35.186
                  - id: 1658
                    name: detain.interserver.net
                    content: 66.45.228.74
                  - id: 1659
                    name: creation.interserver.net
                    content: 66.23.239.99
              schema:
                items:
                  $ref: "#/components/schemas/DnsListItem"
                type: array
          description: Listing of DNS domains on the account with their primary A
            record.
          links:
            GetDnsDomain:
              description: Use the domain id from the list to retrieve all DNS records
                for that domain.
              operationId: getDnsDomain
              parameters:
                id: $response.body#/0/id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List DNS zones hosted on the account with each zone's apex A-record
        IP
      tags:
      - DNS
    post:
      description: |
        Creates a new authoritative zone in PowerDNS for this account and seeds it with a default record set: apex `A` record pointing at `ip`, `NS` records for InterServer's `cdns1.interserver.net` / `cdns2.interserver.net` anycast resolvers, and an `SOA`. Served immediately by InterServer's nameservers via supermaster propagation. **Important:** this only creates the hosted zone — the customer must still point their registrar's nameservers at `cdns1.interserver.net` / `cdns2.interserver.net` for queries to resolve through this zone (use `updateDomainNameservers` if the domain is registered through InterServer). Sibling ops: `getDnsList`, `getDnsDomain`, `addDnsRecord`, `updateDomainNameservers`.

        **Body fields (form or JSON, schema `DnsNewDomain`):**
        - `domain` (string, required) — FQDN of the zone (e.g. `example.com`).
        - `ip` (string, required) — IPv4 address for the apex A record.

        **Returns:** `SuccessTextResponse` — status text confirming the zone was created.

        **Side effects:**
        - Inserts `domains` row scoped to session `account_id`.
        - Inserts default `records` rows: apex `A`, two `NS`, one `SOA`.

        **Auth:** Session/API key.

        **Errors:**
        - `400` — missing `domain` or `ip`.
        - `401` — unauthenticated.
        - `409` — zone already exists.

        **Related calls:**
        - **Find new zone id:** `getDnsList`.
        - **Add more records:** `addDnsRecord`.
        - **Update registrar nameservers:** `updateDomainNameservers` (Domains tag).

        **Example request:**
        ```json
        { "domain": "mydomain.com", "ip": "203.0.113.42" }
        ```
      operationId: addDnsDomain
      requestBody:
        content:
          multipart/form-data:
            examples:
              DnsNewDomainExample:
                value:
                  domain: mydomain.com
                  ip: 1.2.3.4
            schema:
              $ref: "#/components/schemas/DnsNewDomain"
          application/json:
            examples:
              DnsNewDomainExampleJson:
                value:
                  domain: mydomain.com
                  ip: 1.2.3.4
            schema:
              $ref: "#/components/schemas/DnsNewDomain"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Create a new authoritative DNS zone seeded with apex A + NS + SOA records
      tags:
      - DNS
    summary: DNS Management
  /dns/{id}:
    delete:
      description: |
        Removes the zone identified by path `id` AND every record it contains from PowerDNS in a single transaction. **Permanent — no soft-delete, no undo.** Any service relying on these records (web, mail, SPF/DKIM, third-party domain verifications, ACME challenges) will start failing as resolver caches expire (per-record TTL, default 86400s). **Note:** this only deletes the hosted zone on InterServer's nameservers — it does not affect registrar delegation. If `cdns1`/`cdns2` are still delegated at the registrar, queries will return NXDOMAIN/SERVFAIL until delegation is changed or the zone is recreated. Sibling ops: `deleteDnsRecord` (delete one record only), `addDnsDomain` (recreate), `updateDomainNameservers` (change registrar delegation).

        **Path param:**
        - `id` (string, required) — zone ID from `getDnsList`.

        **Returns:** `{success: true, text: "Domain deleted"}`.

        **Side effects:**
        - Deletes every `records` row with `domain_id={id}`.
        - Deletes the `domains` row.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `invalid or missing domain or record id` — zone not found / not owned.

        **Related calls:**
        - **Delete one record only:** `deleteDnsRecord`.
        - **Recreate the zone:** `addDnsDomain`.
        - **Update registrar delegation:** `updateDomainNameservers` (Domains tag).
      operationId: deleteDnsDomain
      parameters:
      - description: The DNS domain ID to delete. Use the `id` from `GET /dns` to
          identify the domain.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Permanently delete a DNS zone and every record it contains
      tags:
      - DNS
    description: View and manage a domain and the records for it.
    get:
      description: |
        Returns the full record set for the specified PowerDNS zone (NS, A, AAAA, CNAME, MX, TXT, SRV, CAA, SOA, etc.) in a single response. Ownership is enforced via `get_dns_domain($id)` against the session account — cross-account access returns an error rather than 200. Use a returned record `id` together with the zone `id` to call `updateDnsRecord` or `deleteDnsRecord`. Sibling ops: `getDnsList`, `addDnsRecord`, `updateDnsRecord`, `deleteDnsRecord`, `deleteDnsDomain`.

        **Path param:**
        - `id` (integer, required) — zone ID from `getDnsList.id`.

        **Returns:** Array of `DnsRecord`:
        - `id` (string) — record ID; pass to `updateDnsRecord` / `deleteDnsRecord`.
        - `domain_id` (string) — parent zone ID.
        - `name` (string) — FQDN of the record (apex or subdomain).
        - `type` (string) — `A` / `AAAA` / `CNAME` / `MX` / `TXT` / `NS` / `SRV` / `CAA` / `SOA` / `PTR` / `SPF` / `TLSA`.
        - `content` (string) — record value (IP for A/AAAA, hostname for CNAME/NS/MX, free text for TXT, etc.).
        - `ttl` (string) — seconds; default 86400.
        - `prio` (string) — priority for MX/SRV (`0` for non-priority records).
        - `disabled` (string `0`/`1`), `ordername` (string), `auth` (string `0`/`1`).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `invalid or missing domain or record id` — zone not found or owned by another account.

        **Related calls:**
        - **Add a record:** `addDnsRecord` (POST same path).
        - **Update a record:** `updateDnsRecord` (`POST /dns/{domainId}/{recordId}`).
        - **Delete a record:** `deleteDnsRecord`.
        - **Delete the whole zone:** `deleteDnsDomain` (DELETE same path).
      operationId: getDnsDomain
      parameters:
      - description: The DNS domain ID. Use the `id` from `GET /dns` to identify the
          domain.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                DnsRecordExample:
                  value:
                  - id: "473"
                    domain_id: "68"
                    name: hussfamily.com
                    type: NS
                    content: cdns1.interserver.net
                    ttl: "86400"
                    prio: "0"
                    disabled: "0"
                    ordername: ""
                    auth: "1"
                  - id: "474"
                    domain_id: "68"
                    name: hussfamily.com
                    type: NS
                    content: cdns2.interserver.net
                    ttl: "86400"
                    prio: "0"
                    disabled: "0"
                    ordername: ""
                    auth: "1"
                  - id: "475"
                    domain_id: "68"
                    name: hussfamily.com
                    type: A
                    content: 64.20.35.186
                    ttl: "86400"
                    prio: "0"
                    disabled: "0"
                    ordername: ""
                    auth: "1"
              schema:
                items:
                  $ref: "#/components/schemas/DnsRecord"
                type: array
          description: The DNS records for the specified domain.
          links:
            UpdateDnsRecord:
              description: Use a record id from the list to update that DNS record.
              operationId: updateDnsRecord
              parameters:
                domainId: $request.path.id
                recordId: $response.body#/0/id
            DeleteDnsRecord:
              description: Use a record id from the list to delete that DNS record.
              operationId: deleteDnsRecord
              parameters:
                domainId: $request.path.id
                recordId: $response.body#/0/id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List every DNS record in one zone with the IDs needed to edit or delete
        them
      tags:
      - DNS
    post:
      description: |
        Adds a single record to the zone identified by path `id`. Type is validated against the global `$rtypes` allowlist (A, AAAA, CNAME, MX, TXT, NS, SRV, CAA, PTR, SPF, TLSA, etc.); content is validated against the record type by `validate_input()`. The record goes live on PowerDNS immediately; resolvers honor the existing TTL on any cached answer. Sibling ops: `getDnsDomain` (find record id afterward), `updateDnsRecord`, `deleteDnsRecord`.

        **Path param:**
        - `id` (integer, required) — zone ID from `getDnsList.id`.

        **Body fields (form or JSON, schema `DnsNewRecord`):**
        - `name` (string, required) — FQDN of the record (must be at or below the zone apex).
        - `type` (string, required) — `A` / `AAAA` / `CNAME` / `MX` / `TXT` / `NS` / `SRV` / `CAA` / `PTR` / `SPF` / `TLSA` (must be in `$rtypes`).
        - `content` (string, required) — value matching `type` syntax (IPv4 for A, IPv6 for AAAA, hostname for CNAME/NS/MX, free text for TXT).
        - `ttl` (integer, optional, default 86400) — seconds.
        - `prio` (integer, optional, default 0) — priority (MX/SRV only).

        **Returns:** `{success: true, text: "Record added"}`.

        **Auth:** Session/API key. Zone ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `Type must be one of: ...` — `type` not in allowlist.
        - `invalid or missing domain or record id` — zone not found / not owned.
        - Content-format validation failure (`text` describes the issue).

        **Related calls:**
        - **Find new record id:** `getDnsDomain`.
        - **Edit later:** `updateDnsRecord`.
        - **Delete:** `deleteDnsRecord`.
      operationId: addDnsRecord
      parameters:
      - description: The DNS Domain ID.
        examples:
          DnsDomainIdExample:
            value: "472"
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DnsNewRecord"
          application/json:
            schema:
              $ref: "#/components/schemas/DnsNewRecord"
        required: true
      responses:
        "200":
          description: Add DNS Domain Response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Add a DNS record (A, AAAA, MX, TXT, CNAME, NS, SRV, CAA, ...) to a\
        \ zone"
      tags:
      - DNS
    summary: DNS Domain Management
  /dns/{domainId}/{recordId}:
    delete:
      description: |
        Removes the record identified by `recordId` from zone `domainId`. The zone itself remains intact — only the one record is dropped. **Permanent** — applied to PowerDNS immediately, but resolvers continue to answer with cached values until the previous TTL expires. Use to surgically remove an A/AAAA/MX/TXT etc. record; to drop the entire zone and all its records, use `deleteDnsDomain`. Sibling ops: `getDnsDomain` (verify after deletion), `deleteDnsDomain`, `updateDnsRecord`.

        **Path params:**
        - `domainId` (integer, required) — zone ID from `getDnsList.id`.
        - `recordId` (integer, required) — record ID from `getDnsDomain.id`.

        **Returns:** `{success: true, text: "domain record deleted"}`.

        **Auth:** Session/API key. Zone ownership enforced via `get_dns_domain($domainId)`.

        **Errors:**
        - `401` — unauthenticated.
        - `invalid or missing domain or record id` — zone/record not found or not owned.
        - `error removing domain record` — underlying DB delete failed.

        **Related calls:**
        - **Verify after delete:** `getDnsDomain`.
        - **Recreate:** `addDnsRecord`.
        - **Delete entire zone instead:** `deleteDnsDomain`.
      operationId: deleteDnsRecord
      parameters:
      - description: The DNS domain ID. Use the `id` from `GET /dns` to identify the
          domain.
        explode: false
        in: path
        name: domainId
        required: true
        schema:
          type: integer
        style: simple
      - description: "The DNS record ID within the domain. Use the record `id` from\
          \ `GET /dns/{id}` to identify the record."
        explode: false
        in: path
        name: recordId
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Permanently delete one DNS record from a zone — zone itself is preserved
      tags:
      - DNS
    description: Manage a DNS Record for the given Domain.
    post:
      description: |
        Replaces the record identified by `recordId` within zone `domainId` with new values. **TTL caveat:** the change is written to PowerDNS immediately, but already-cached resolver answers persist until the previous record's TTL expires — plan TTL down ahead of a clean cutover. Type is validated against the global `$rtypes` allowlist; content is validated against the record type. Sibling ops: `getDnsDomain` (read), `addDnsRecord` (create), `deleteDnsRecord`.

        **Path params:**
        - `domainId` (integer, required) — zone ID from `getDnsList.id`.
        - `recordId` (integer, required) — record ID from `getDnsDomain.id`.

        **Body fields (form or JSON, schema `DnsUpdateRecord`):**
        - `name` (string, required) — FQDN at/below zone apex.
        - `type` (string, required) — one of the allowed PowerDNS types.
        - `content` (string, required) — value matching `type`.
        - `ttl` (integer, required) — seconds.
        - `prio` (integer, required) — MX/SRV priority (`0` otherwise).

        **Returns:** `{success: true, text: "domain record updated"}`.

        **Auth:** Session/API key. Zone ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `Type must be one of: ...` — `type` not in `$rtypes`.
        - `invalid or missing domain or record id` — zone/record not found / not owned.
        - Content-format validation text — `validate_input()` failure.

        **Related calls:**
        - **Read first:** `getDnsDomain`.
        - **Delete:** `deleteDnsRecord`.
        - **Create new:** `addDnsRecord`.
      operationId: updateDnsRecord
      parameters:
      - description: The DNS domain ID. Use the `id` from `GET /dns` to identify the
          domain.
        explode: false
        in: path
        name: domainId
        required: true
        schema:
          type: integer
        style: simple
      - description: "The DNS record ID within the domain. Use the record `id` from\
          \ `GET /dns/{id}` to identify the record."
        explode: false
        in: path
        name: recordId
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DnsUpdateRecord"
          application/json:
            schema:
              $ref: "#/components/schemas/DnsUpdateRecord"
        description: The request data to update a dns record.
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Replace values on an existing DNS record (name, type, content, ttl,\
        \ priority)"
      tags:
      - DNS
    summary: DNS Record Management
  /domains:
    get:
      description: |
        Enumerates every domain registration owned by the authenticated customer — hostname, expiry, recurring cost, status. The canonical entry point for finding a `domain_id` to pass into other Domains endpoints. Empty array means the account has no domains (not an error). Sibling ops: `getDomainInfo`, `getNewDomain`, `getDomainLookup`, `addDomain`, `CancelDomain`.

        **Path/Query/Body:** None.

        **Returns:** Array of `DomainRow`:
        - `domain_id` (string) — canonical id; pass to every `/domains/{id}/*` endpoint.
        - `domain_hostname` (string) — registered FQDN.
        - `domain_expire_date` (string ISO 8601 or empty) — registry expiry; empty when not yet activated or unknown.
        - `cost` (decimal string) — recurring renewal cost in the domain's billing currency.
        - `domain_status` (string enum) — `pending` / `active` / `expired` / `canceled` / `pending-transfer`.

        **Auth:** Session/API key. Filtered by `domain_custid`.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Per-domain detail:** `getDomainInfo`.
        - **Manage:** `getDomainContact` / `updateDomainContact`, `getDomainNameservers` / `updateDomainNameservers`, `getDomainDnssec` / `addDomainDnssec`, `getDomainWhoisPrivacy` / `updateDomainWhoisPrivacy`.
        - **Renew / transfer:** `getDomainRenewal` / `postDomainRenewal`, `getDomainTransfer`.
        - **Order a new domain:** `getDomainLookup` → `getNewDomain` → `addDomain`.
        - **Cancel:** `CancelDomain`.
      operationId: getDomainsList
      responses:
        "200":
          content:
            application/json:
              examples:
                DomainsExample:
                  value:
                  - domain_id: "418295"
                    domain_hostname: unixsrv10.com
                    domain_expire_date: ""
                    cost: "11.00"
                    domain_status: canceled
                  - domain_id: "592337"
                    domain_hostname: detain.dev
                    domain_expire_date: 2023-08-14T00:59:38.000Z
                    cost: "18.00"
                    domain_status: active
              schema:
                items:
                  $ref: "#/components/schemas/DomainRow"
                type: array
          description: The listing of `Domains` services on your account.
          links:
            getDomainInfo:
              description: "Use the `domain_id` from any item in the response array\
                \ to fetch full service details via `GET /domains/{id}`."
              operationId: getDomainInfo
              parameters:
                id: $response.body#/0/domain_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List every domain registration on the account with billing and registration
        metadata
      tags:
      - Domains
  /domains/lookup/{name}:
    get:
      description: |
        Looks up a single FQDN against OpenSRS: returns availability, premium-name flag, current new/renewal/transfer prices, per-TLD order field metadata, and multi-currency quotes. **Public** endpoint — no auth required (rate-limited via `domainlookup` cache). Repeated lookups within a short window may return cached results from the `domainlookup` table. Use as step 1 of an order: discover availability and pricing, then call `addDomain` to commit. Sibling ops: `getDomainSearch` (suggestions), `getNewDomain` (catalog), `addDomain`, `postDomainSearch`.

        **Path param:**
        - `name` (string, required) — full FQDN (e.g. `example.com`).

        **Returns** (schema `DomainLookupResponse`):
        - `available` (bool) — registerable now.
        - `premium` (bool) — premium-name pricing (often > $100).
        - `website` (bool) — same hostname is already a webhosting service on this account.
        - `domain_service` (bool) — same hostname is already a domain on this account.
        - `service` (object) — `services_id`, `services_name`, `services_cost`, `services_field1` (TLD), `services_module`.
        - `whois_privacy` (bool) — privacy add-on available for this TLD.
        - `new`, `renewal`, `transfer` (float) — base USD prices (with profit markup).
        - `fields` (object) — per-TLD order form schema (labels, options, current account values).
        - `currencies` (object) — `{<code>: {services_cost, new, renewal, transfer}}` converted to each enabled currency.

        **Auth:** Public (no auth required).

        **Errors:**
        - `422 Invalid Domain` — `valid_domain()` rejected input.

        **Related calls:**
        - **Brainstorm alternatives:** `getDomainSearch`.
        - **Place order:** `addDomain` with the resolved `service.services_id` and `fields`.
        - **TLD catalog:** `getNewDomain`.
      operationId: getDomainLookup
      parameters:
      - description: The full domain name to look up (for example `example.com`).
        explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainLookupResponse"
          description: "Availability, pricing, and field metadata for the requested\
            \ domain."
          links:
            addDomain:
              description: Use the pricing and field metadata from this lookup response
                to populate the order request for `POST /domains/order`.
              operationId: addDomain
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "422":
          content:
            application/json:
              examples:
                InvalidDomainName:
                  value:
                    message: Invalid Domain
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: The domain input was invalid.
      summary: "Check availability, premium status, and pricing for a specific domain"
      tags:
      - Domains
  /domains/search/{name}:
    get:
      description: |
        Returns registrar-suggested alternatives plus bulk availability data from OpenSRS for the supplied search term. Useful when a customer is brainstorming names. Pair with `getDomainLookup` to get full pricing and per-TLD order fields for any specific chosen result. Sibling ops: `postDomainSearch`, `getDomainLookup`, `getNewDomain`, `addDomain`.

        **Path param:**
        - `name` (string, required) — search term (e.g. `example` or `example.com`).

        **Returns** (schema `DomainSearchResponse`):
        - `success` (bool) — registrar call succeeded.
        - `response_text` (string) — registrar response message.
        - `response_time` (float) — registrar latency (seconds).
        - `lookup` (array) — exact-match availability across the searched TLD set.
        - `suggest` (array) — registrar's recommended alternative names with availability.
        - `tlds` (array) — TLDs queried.

        **Auth:** Public.

        **Errors:**
        - `422 Invalid Search Response!` — registrar returned no usable results.

        **Related calls:**
        - **Single-domain detail:** `getDomainLookup`.
        - **One-shot order preview from a search term:** `postDomainSearch`.
      operationId: getDomainSearch
      parameters:
      - description: The base domain name to search (for example `example` or `example.com`).
        explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainSearchResponse"
          description: Suggested and lookup results for the supplied search term.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidSearchResponse:
                  value:
                    message: Invalid Search Response!  Either there was an error communicating
                      with the registrars or We received no results.
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: No search suggestions or registrar response available.
      summary: Get registrar-suggested domain alternatives and bulk availability for
        a search term
      tags:
      - Domains
    post:
      description: |
        Returns the complete order-form payload — pricing, service catalog entry, per-TLD order fields — for the hostname in a single POST. Equivalent to calling `getDomainLookup` + `getNewDomain` + `putDomains` and merging the results, but with one round-trip. The path `name` is moved server-side into `$_POST['hostname']` and passed to `getOrderDomainData(true)`. Sibling ops: `getDomainSearch`, `getDomainLookup`, `getNewDomain`, `addDomain`.

        **Path param:**
        - `name` (string, required) — hostname (e.g. `example.com`).

        **Body:** None.

        **Returns:** Combined order-data response — pricing, service catalog entry, form fields ready to populate for `addDomain`.

        **Auth:** Session/API key (path is `client_api`, but called publicly).

        **Errors:**
        - `4xx` — hostname cannot be resolved to a TLD service.

        **Related calls:**
        - **Place order:** `addDomain` with the returned fields.
      operationId: postDomainSearch
      parameters:
      - description: The base domain name to search (for example `example` or `example.com`).
        explode: false
        in: path
        name: name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          description: Domain availability and pricing check results.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get the full order form data for a hostname in one round-trip (search
        → order preview)
      tags:
      - Domains
  /domains/order:
    get:
      description: |
        Returns the catalog of buyable TLD services and the base Whois-privacy add-on pricing. Use to resolve a hostname's TLD to a `service_id` for ordering, or to render a TLD picker. Pair with `getDomainLookup` for per-domain pricing and `addDomain` to commit. Sibling ops: `getDomainLookup`, `putDomains`, `patchDomains`, `addDomain`.

        **Path/Query/Body:** None.

        **Returns** (schema `DomainOrder`):
        - `whoisPrivacyCost` (float) — base per-year privacy cost (`OPENSRS_PRIVACY_COST` constant).
        - `whoisPrivacyCostTotal` (object) — privacy cost per TLD multiplied by that TLD's term length: `{<tld>: <total-cost>}`.
        - `services` (object) — `{<services_id>: {services_name, services_cost, services_field1 (tld), ...}}`. `services_ourcost` is stripped.
        - `tldServices` (object) — TLD → `services_id` lookup map (e.g. `{"com": 100, "net": 101, "io": 234}`).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Per-domain pricing:** `getDomainLookup`.
        - **Preview order fields:** `putDomains`.
        - **Validate fields:** `patchDomains`.
        - **Place order:** `addDomain`.
      operationId: getNewDomain
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainOrder"
          description: Domain registration order information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read the buyable domain TLD service catalog and Whois privacy pricing
      tags:
      - Domains
    patch:
      description: |
        Validates posted contact/registration field values via `validate_domain_fields()`. Enforces per-TLD requirements (nexus codes, postal formats, registrant org rules, EPP `auth_info` syntax for transfers, etc.). Use as the last step before `addDomain` to surface form errors cheaply. No commit — no invoice, no service record. Sibling ops: `putDomains`, `addDomain`, `getDomainLookup`, `getNewDomain`.

        **Body fields:**
        - `hostname` (string, required).
        - `type` (string, optional, default `register`) — `register` or `transfer`.
        - All per-TLD fields from `putDomains.domainFields`.

        **Returns:** `"success"` (string) when all fields validate; otherwise an `errors` object describing the failing fields and per-field validation messages.

        **Auth:** Session/API key.

        **Errors:**
        - `400 Missing hostname parameter`.
        - `400 Unable to determine service type for this domain TLD.`.
        - `401` — unauthenticated.
        - Validation error object — fields-level failures.

        **Related calls:**
        - **Schema:** `putDomains` (returns the field set to validate).
        - **Commit:** `addDomain`.
      operationId: patchDomains
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainOrderRequest"
        required: true
      responses:
        "200":
          description: Validate Domain order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate posted domain-order field values before committing — dry run
      tags:
      - Domains
    post:
      description: |
        Places a new domain registration or transfer order. Resolves the TLD to a `services_id`, runs `validate_buy_domain()` (hostname, TLD service, fields, coupon, whois-privacy add-on), then calls `place_buy_domain()` to create the `Repeat_Invoice` recurring billing row, generate the initial `invoices` row, and (when `whois_privacy=enable`) an additional add-on `Repeat_Invoice` for privacy. **Real money** — call `putDomains` then `patchDomains` first to preview and validate. Sibling ops: `getDomainLookup`, `getNewDomain`, `putDomains`, `patchDomains`, `initiatePayment`.

        **Body fields (JSON or form):**
        - `hostname` (string, required) — FQDN to register or transfer.
        - `type` (string, optional, default `register`) — `register` or `transfer`.
        - `whois_privacy` (string, optional) — `enable` to add the privacy add-on (separate recurring invoice).
        - `coupon` (string, optional) — coupon code.
        - All per-TLD contact/registration fields from `putDomains.domainFields` (registrant contact details, TLD-specific fields like nexus codes, EPP `auth_info` for transfers, etc.).

        **Returns** (schema `ServiceOrderPostResponse`): `{total_cost, iid, iids, real_iids, serviceid (new domain_id), invoice_description, cj_params, payUrl}` — pass `real_iids` to `initiatePayment` to fund the order.

        **Side effects:**
        - Inserts `domains` service row in `pending` status.
        - Inserts `repeat_invoices` row for recurring renewal.
        - Inserts `invoices` row for the first-period charge.
        - When `whois_privacy=enable`: inserts a separate add-on `repeat_invoices` row + its initial invoice.
        - For transfers: stores `auth_info` and marks `service_extra` as `transfer`.

        **Auth:** Session/API key.

        **Errors:**
        - `400 Missing hostname parameter`.
        - `400 Unable to determine service type for this domain TLD.`.
        - `401` — unauthenticated.
        - `422` — validation failure (e.g. coupon invalid, required TLD field missing, hostname not registerable). Response body is the combined `errors` array from `validate_buy_domain()`.

        **Related calls:**
        - **Prerequisites:** `getDomainLookup` → `getNewDomain` → `putDomains` → `patchDomains`.
        - **Pay:** `initiatePayment` with `real_iids`.
        - **Verify activation:** `getDomainInfo` (poll for `domain_status='active'`).
        - **Track in-progress transfer:** `getDomainTransfer`.
        - **For pending transfers needing EPP/auth_info:** `updateDomainContact` (set `auth_info`).

        **Example happy path (register):**
        ```text
        GET /apiv2/domains/lookup/example.com           -> available, pricing, fields
        PUT /apiv2/domains/order { hostname, type }     -> domainFields
        PATCH /apiv2/domains/order { hostname, ...fields } -> "success"
        POST /apiv2/domains/order { hostname, type, ...fields } -> { serviceid, real_iids }
        GET /apiv2/billing/pay/cc/{real_iids[0]}        -> pay
        GET /apiv2/domains/{serviceid}                  -> poll until domain_status=="active"
        ```
      operationId: addDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a new domain registration or transfer order, generate billing\
        \ invoice"
      tags:
      - Domains
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Register or transfer a domain. Returns invoice IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |
        Pre-flight that returns the per-TLD field schema required to register or transfer the supplied hostname. The schema varies significantly by TLD: `.us` requires nexus codes, `.ca` requires CIRA legal type, `.eu` has residency rules, `.fr` requires VAT for orgs, etc. Values pre-populate from the account profile when possible. No commit — use `patchDomains` to validate filled values, then `addDomain` to place the order. Sibling ops: `getNewDomain`, `getDomainLookup`, `patchDomains`, `addDomain`.

        **Body fields:**
        - `hostname` (string, required) — FQDN.
        - `type` (string, optional, default `register`) — `register` or `transfer`.
        - `coupon` (string, optional) — coupon code.

        **Returns:** `{domainFields: {<field_name>: {label, type, options, value, required, ...}}}` — schema for the dynamic order form.

        **Auth:** Session/API key.

        **Errors:**
        - `400 Missing hostname parameter`.
        - `400 Unable to determine service type for this domain TLD.` — unknown TLD.
        - `401` — unauthenticated.

        **Related calls:**
        - **Catalog first:** `getNewDomain`.
        - **Validate filled values:** `patchDomains`.
        - **Place order:** `addDomain`.
      operationId: putDomains
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainOrderRequest"
        required: true
      responses:
        "200":
          description: Validate Domain Order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Preview per-TLD field requirements for a domain order — no commit
      tags:
      - Domains
  /domains/{id}:
    delete:
      description: |
        Stops billing and auto-renewal for a domain in the customer account by setting the service to `canceled` via the shared `Billing\CancelService::go($id)` flow with `module='domains'`. **Important:** this only stops billing on InterServer's side — the domain registration at the registrar (OpenSRS) typically remains active until its current expiration date. To release the domain back to the public pool, let it expire OR submit a release request via support ticket. Sibling ops: `getDomainInfo` (verify status), `getDomainsList`, `postDomainRenewal` (re-activate before expiry).

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns:** `DomainsCancelResponse` — confirmation envelope.

        **Side effects:**
        - Sets `domain_status='canceled'`.
        - Marks the `repeat_invoices` row non-renewing — no future renewal invoices generated.
        - Does **not** call the registrar — the registration remains active at OpenSRS until natural expiry.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — already canceled.

        **Related calls:**
        - **Re-activate before expiry:** `postDomainRenewal`.
        - **Verify status:** `getDomainInfo`.
        - **Sibling cancels on other modules:** `VPSCancel`, `mailCancel`, `webhostingCancel`, etc. (same `CancelService` handler).
      operationId: CancelDomain
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelDomain_200_response"
          description: Domains Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a domain order in the billing system to stop auto-renewals
      tags:
      - Domains
    get:
      description: |
        Returns the full `ViewDomain` payload for one domain — billing summary, registration status, lock state, expiry date, contact summary, and `client_links` for related dashboard actions. Read-only. Internal `admin_links`, `settings`, `csrf` are stripped before return. Use to render a domain detail page, verify ownership before mutating, or poll `domain_status` after `addDomain`. Sibling ops: `getDomainContact`, `getDomainNameservers`, `getDomainDnssec`, `getDomainWhoisPrivacy`, `getDomainInvoices`, `updateDomainInfo`, `CancelDomain`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns** (schema `Domain`):
        - `serviceInfo` — `domain_id`, `domain_hostname`, `domain_status`, `domain_expire_date`, lock state, registrar metadata.
        - `serviceType` — TLD service row.
        - `client_links` (array) — `{name, link, icon}` for renew/transfer/contact/DNSSEC/whois-privacy actions. URLs pre-resolved.

        **Auth:** Session/API key. Ownership enforced via `domain_custid`.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Related calls:**
        - **Contact:** `getDomainContact` / `updateDomainContact`.
        - **Nameservers:** `getDomainNameservers` / `updateDomainNameservers`.
        - **DNSSEC:** `getDomainDnssec` / `addDomainDnssec` / `deleteDomainDnssec`.
        - **Whois privacy:** `getDomainWhoisPrivacy` / `updateDomainWhoisPrivacy`.
        - **Billing:** `getDomainInvoices`, `getDomainRenewal` / `postDomainRenewal`.
        - **Transfer status:** `getDomainTransfer`.
        - **Cancel:** `CancelDomain`.
      operationId: getDomainInfo
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Domain"
          description: Domain Information.
          links:
            getDomainContact:
              description: Retrieve the registrant and admin contact details for this
                domain.
              operationId: getDomainContact
              parameters:
                id: $response.body#/serviceInfo/domain_id
            getDomainDnssec:
              description: Retrieve the DNSSEC DS records for this domain.
              operationId: getDomainDnssec
              parameters:
                id: $response.body#/serviceInfo/domain_id
            getDomainInvoices:
              description: Retrieve the billing invoices for this domain order.
              operationId: getDomainInvoices
              parameters:
                id: $response.body#/serviceInfo/domain_id
            getDomainWhoisPrivacy:
              description: Retrieve the Whois privacy status for this domain.
              operationId: getDomainWhoisPrivacy
              parameters:
                id: $response.body#/serviceInfo/domain_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read full billing, registrar, and service detail for one domain"
      tags:
      - Domains
    post:
      description: |
        Account-level write-back hook for the domain service record. Runs the same `View::go()` handler as `getDomainInfo` — it does NOT push registrar-side changes by itself. **For specific changes use the dedicated endpoints** — they push to OpenSRS where appropriate. Sibling ops: `getDomainInfo`, `updateDomainContact`, `updateDomainNameservers`, `addDomainDnssec`, `updateDomainWhoisPrivacy`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body:** Form fields matching the domain service record.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Prefer these dedicated endpoints:**
        - **Registrant/admin contact:** `updateDomainContact` (pushes to OpenSRS).
        - **Nameservers:** `updateDomainNameservers`, `addDomainNameserver`, `deleteDomainNameserver`.
        - **DNSSEC:** `addDomainDnssec`, `deleteDomainDnssec`.
        - **Whois privacy:** `updateDomainWhoisPrivacy`.
        - **Renew:** `postDomainRenewal`.
        - **Cancel:** `CancelDomain`.
      operationId: updateDomainInfo
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: POST mutation hook for the domain detail page (use dedicated ops where
        possible)
      tags:
      - Domains
  /domains/{id}/contact:
    get:
      description: |
        Returns the current contact field set (registrant/admin/tech/billing) with current values for the domain — schema and values mirror what was set at registration. For pending transfer services, the response also includes a `transfer` selector and the EPP `auth_info` code so the client can resubmit. Read-only. Sibling ops: `updateDomainContact` (push changes to OpenSRS), `getDomainInfo`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns** (schema `DomainContactDetails`):
        - `firstname`, `lastname`, `email`.
        - `address`, `address2`, `address3`, `city`, `state`, `zip`, `country`.
        - `phone`, `fax` (E.164 format expected).
        - `company` (optional).
        - `auth_info` (string) — EPP/transfer code (present on transfer services).
        - `transfer` (string `yes`/`no`) — selector for pending transfer services.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Related calls:**
        - **Update:** `updateDomainContact`.
        - **Transfer status:** `getDomainTransfer`.
      operationId: getDomainContact
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainContactDetails"
          description: The registrant/admin contact details for the domain.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read the current registrant/admin/tech/billing contact field set for
        a domain
      tags:
      - Domains
    post:
      description: |
        Pushes updated contact data to the registrar via OpenSRS `provModify`, applied to admin/tech/billing/owner contact roles via `also_apply_to`. Active services apply changes immediately at the registrar; pending services may trigger `queue_process_payment` when the order is paid. **Domain must not be locked** — locked domains return an error directing the user to unlock first. **Note:** registrant-name changes on some TLDs (e.g. `.com`, `.net`) require a 60-day transfer lock per ICANN rules. Sibling ops: `getDomainContact`, `getDomainInfo`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields (JSON or multipart, schema `DomainContactDetails`):**
        Same fields returned by `getDomainContact`: `firstname`, `lastname`, `email`, `address`/`2`/`3`, `city`, `state`, `zip`, `country`, `phone`, `fax`, `company`, optional `auth_info`, optional `transfer`.

        **Returns:** `SuccessTextResponse`.

        **Side effects:**
        - Calls OpenSRS `provModify` with `also_apply_to=admin,tech,billing,owner`.
        - May trigger ICANN 60-day transfer lock for registrant-name changes on legacy TLDs.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — domain is locked (unlock first).
        - Registrar error (e.g. OpenSRS error code 465 / 466) surfaced as 4xx.

        **Related calls:**
        - **Read first:** `getDomainContact`.
        - **Transfer status:** `getDomainTransfer`.
      operationId: updateDomainContact
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainContactDetails"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DomainContactDetails"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update registrant/admin contact details and push them to OpenSRS
      tags:
      - Domains
  /domains/{id}/dnssec:
    delete:
      description: |
        Disables DNSSEC at the registrar by removing the entire DS record set in one call. **Propagation caveat:** DNSSEC removal can fail at the registry even after a 200 response — propagation is asynchronous; re-check with `getDomainDnssec` to confirm. To remove records selectively, replace the set via `addDomainDnssec` instead. Sibling ops: `getDomainDnssec`, `addDomainDnssec`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body:** None — removes the full DS record set.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - Registrar error surfaced as 4xx.

        **Related calls:**
        - **Verify propagation:** `getDomainDnssec`.
        - **Replace records selectively:** `addDomainDnssec`.
      operationId: deleteDomainDnssec
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Clear all DNSSEC DS records on the domain (disable DNSSEC at the registrar)
      tags:
      - Domains
    get:
      description: |
        Returns the DNSSEC DS record set currently registered for the domain at OpenSRS. Empty array means DNSSEC is not configured. Use to mirror existing settings or as a baseline before `addDomainDnssec` (which replaces the set). Sibling ops: `addDomainDnssec`, `deleteDomainDnssec`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns** (schema `DomainDnssecRecords`):
        - `records` (array) — DS entries:
          - `algorithm` (integer) — DNSKEY algorithm (e.g. 8 for RSASHA256, 13 for ECDSAP256SHA256).
          - `key_tag` (integer, < 65536).
          - `digest_type` (integer) — `1` (SHA-1), `2` (SHA-256), `3` (GOST), `4` (SHA-384).
          - `digest` (string, hex) — length depends on `digest_type`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.

        **Related calls:**
        - **Replace records:** `addDomainDnssec`.
        - **Clear all records:** `deleteDomainDnssec`.
      operationId: getDomainDnssec
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainDnssecRecords"
          description: DNSSEC records currently applied to the domain.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read the DNSSEC DS record set currently registered with the registrar
      tags:
      - Domains
    post:
      description: |
        Registers one or more DNSSEC DS records at the registrar. Body uses parallel arrays indexed per record. **Propagation caveat:** DNSSEC publication is asynchronous at the registry — a 200 here does not guarantee the records have propagated; re-call `getDomainDnssec` to verify. **Sets, not adds:** this replaces the full DS record set in one transaction; to remove all DS records use `deleteDomainDnssec`. Sibling ops: `getDomainDnssec`, `deleteDomainDnssec`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields (JSON or multipart, parallel arrays — `algorithm[0]` pairs with `key_tag[0]`, etc., schema `DomainDnssecRequest`):**
        - `algorithm[]` (integer) — DNSKEY algorithm (e.g. 8 = RSASHA256, 13 = ECDSAP256SHA256).
        - `key_tag[]` (integer) — must be < 65536.
        - `digest_type[]` (integer) — `1` (SHA-1, 40 hex chars), `2` (SHA-256, 64), `3` (GOST, 64), `4` (SHA-384, 96).
        - `digest[]` (string) — hex digest; length must match `digest_type[i]`.

        **Returns:** `SuccessTextResponse` on registrar confirmation.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"` or registrar refused.
        - `422` — `key_tag >= 65536` or digest length mismatch.

        **Related calls:**
        - **Verify propagation:** `getDomainDnssec`.
        - **Clear all records:** `deleteDomainDnssec`.
      operationId: addDomainDnssec
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainDnssecRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DomainDnssecRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Register DNSSEC DS records on the domain at OpenSRS
      tags:
      - Domains
  /domains/{id}/invoices:
    get:
      description: |
        Returns the billing history for one domain — initial registration/transfer invoice, recurring renewal invoices, Whois privacy add-on invoices. Extends `Billing\InvoicesList::go()` with `module='domains'`. Use to render a per-domain billing-history view or find an unpaid renewal/privacy invoice to pass to `initiatePayment`. Sibling ops: `getDomainInfo`, `postDomainRenewal`, `updateDomainWhoisPrivacy`, `initiatePayment`, `getBillingInvoice`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns:** `ChargeInvoiceRows` — array of `{id, amount, paid, description, date, due_date, currency, module: "domains", service: <id>}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service` — `id` not owned by caller.

        **Related calls:**
        - **Pay an unpaid invoice:** `initiatePayment`.
        - **Renew:** `postDomainRenewal`.
        - **Account-wide history:** `getBillingInvoices`.
      operationId: getDomainInvoices
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices scoped to one domain order
      tags:
      - Domains
  /domains/{id}/renew:
    get:
      description: |
        Returns renewal pricing, current expiry, Whois privacy availability, and whether an unpaid renewal invoice already exists for the domain. Use before triggering `postDomainRenewal` to render a renewal form and prevent duplicate invoices. Costs are converted to the customer's preferred currency. Sibling ops: `postDomainRenewal`, `getDomainInvoices`, `getDomainInfo`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns:**
        - `renewCost` (float) — renewal cost in `currency`.
        - `whoisCost` (float) — Whois privacy renewal cost.
        - `whoisAvailable` (bool) — privacy supported on this TLD.
        - `currency` (string), `currencySymbol` (string).
        - `expiryDate` (string).
        - `alreadyInvoiced` (bool) — a renewal `Repeat_Invoice` already produced an invoice.
        - `invoicePaid` (bool) — whether that invoice is paid.
        - `tld` (string).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - `4xx` — renewal not available for this TLD.

        **Related calls:**
        - **Submit renewal:** `postDomainRenewal`.
        - **Pay existing renewal invoice:** `getDomainInvoices` → `initiatePayment`.
      operationId: getDomainRenewal
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read renewal pricing, expiry, and whether a renewal invoice already\
        \ exists"
      tags:
      - Domains
    post:
      description: |
        Generates a renewal invoice for the domain (and optionally the Whois privacy add-on). Updates the domain's `Repeat_Invoice` cost/frequency/currency to the current price, then calls `Repeat_Invoice::invoice()` to produce a fresh invoice. **Real money.** If a prior unpaid renewal invoice already exists, returns an error directing the user to pay that one instead — prevents double-billing. Renewal is not supported for some TLDs. Sibling ops: `getDomainRenewal`, `getDomainInvoices`, `initiatePayment`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields:**
        - `whois_privacy` (string, optional) — `enable` to add or keep the privacy add-on; otherwise the existing privacy `Repeat_Invoice` is marked `deleted=1` on renewal.

        **Returns:** `{text, invoices, invoiceIds, payUrl}` — pass `invoiceIds` to `initiatePayment` to settle.

        **Side effects:**
        - Updates `repeat_invoices` cost/frequency/currency.
        - Inserts a new `invoices` row for the renewal period.
        - When `whois_privacy=enable`: extends the privacy add-on `repeat_invoices` and creates its renewal invoice.
        - When `whois_privacy` not enabled: marks the existing privacy `repeat_invoices` row `deleted=1`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`, or unpaid renewal invoice already exists, or already-paid renewal exists.
        - Registrar errors surfaced as 4xx.

        **Related calls:**
        - **Preview:** `getDomainRenewal`.
        - **Pay:** `initiatePayment` with the returned `invoiceIds`.
        - **Cancel auto-renew:** `CancelDomain`.
      operationId: postDomainRenewal
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Submit a domain renewal request and generate the renewal invoice
      tags:
      - Domains
  /domains/{id}/transfer:
    get:
      description: |
        Returns the OpenSRS transfer state for a domain order flagged as a transfer in `service_extra`. Use to poll an in-progress transfer; `pending_owner` means the customer must click the approval link in the email sent by OpenSRS to the registrant. Sibling ops: `postDomainTransfer` (re-poll), `getDomainContact` (set `auth_info`), `addDomain` (initiate new transfer).

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns:**
        - When not a transfer: `{transfer: false, message: "..."}`.
        - When a transfer: `{transfer: true, info: {status, statusText, type, transferrable, reason}}` where `status` is one of:
          - `pending` — submitted to OpenSRS, awaiting state change.
          - `pending_owner` — **customer action required** (approve the OpenSRS email).
          - `pending_admin` — InterServer staff review.
          - `pending_registry` — registry processing.
          - `declined` — transfer rejected (see `reason`).
          - `completed` — transfer landed.
          - `undef` — unknown.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - Registrar communication failures returned as errors.

        **Related calls:**
        - **Re-poll:** `postDomainTransfer`.
        - **Update auth_info:** `updateDomainContact`.
        - **Initiate new transfer:** `addDomain` with `type=transfer`.
      operationId: getDomainTransfer
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read OpenSRS transfer status for an in-progress domain transfer order
      tags:
      - Domains
    post:
      description: |
        Re-polls OpenSRS transfer state. Behaves identically to `getDomainTransfer` (same `go()` handler) — provided so dashboards can refresh via a form-action pattern. **This endpoint does not initiate transfers** — to start a transfer, use `addDomain` with `type=transfer`. Sibling ops: `getDomainTransfer`, `addDomain` (initiate), `getDomainContact` (set `auth_info`).

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body:** None.

        **Returns:** Same payload as `getDomainTransfer`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.

        **Related calls:**
        - **Read:** `getDomainTransfer`.
        - **Initiate new transfer:** `addDomain` with `type=transfer`.
      operationId: postDomainTransfer
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Re-poll OpenSRS transfer status for a domain order via POST
      tags:
      - Domains
  /domains/{id}/welcome_email:
    get:
      description: |
        Resends the domain welcome email (registration details, management instructions, EPP code where applicable) to the customer's address on file. Idempotent — safe to call multiple times. Sibling welcome-email endpoints: `getVpsWelcomeEmail`, `getWebsitesWelcomeEmail`, `getMailWelcomeEmail`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body:** None.

        **Returns:** `{text: "Welcome Email has been resent."}`.

        **Side effects:**
        - Sends an email to the account's billing email address.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service Passed` — `id` not owned by caller.
        - `409 Service is not active` — `domain_status != "active"`.

        **Related calls:**
        - **Domain detail:** `getDomainInfo`.
        - **Contact info:** `getDomainContact`.
      operationId: getDomainsWelcomeEmail
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the domain welcome email with registration details and management
        instructions
      tags:
      - Domains
  /domains/{id}/whois:
    get:
      description: |
        Returns Whois privacy state for the domain — whether the TLD supports privacy, whether it's currently enabled at OpenSRS, and the add-on cost. Some TLDs (e.g. `.us`, `.uk`, country-code variants) do not allow privacy regardless of pricing. Sibling op: `updateDomainWhoisPrivacy` (order/enable/disable).

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns:**
        - `available` (bool) — privacy supported for this TLD (via `get_domain_tld_whois_privacy()`).
        - `cost` (float) — annual privacy cost in `currency`.
        - `currency` (string), `currencySymbol` (string).
        - `whoisPrivacy` (string enum) — `enabled` / `disabled` (live OpenSRS state).
        - `repeatInvoice` (object|null) — current privacy add-on `Repeat_Invoice` row, if one exists.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Related calls:**
        - **Order/enable/disable:** `updateDomainWhoisPrivacy`.
        - **Billing:** `getDomainInvoices`.
      operationId: getDomainWhoisPrivacy
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read Whois privacy availability, current state, and add-on pricing\
        \ for a domain"
      tags:
      - Domains
    post:
      description: |
        Manages the Whois privacy add-on. Behavior branches on `action`:
        - **`order`**: creates an add-on `Repeat_Invoice` and emits the first invoice; pass the returned `payUrl` to the customer or use `initiatePayment` with `invoiceId`. **Real money.**
        - **`enable`**: activates Whois privacy at OpenSRS — call after the invoice is paid (calls `post_payment_processing_new`).
        - **`disableCancel`**: disables Whois privacy at OpenSRS and marks the add-on `Repeat_Invoice` `deleted=1`.
        - **(no action)**: returns current state — same shape as `getDomainWhoisPrivacy`.

        Sibling ops: `getDomainWhoisPrivacy`, `getDomainInvoices`, `initiatePayment`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields (JSON or multipart, schema `DomainWhoisPrivacyRequest`):**
        - `action` (string, optional) — one of `order` / `enable` / `disableCancel`. Omit for status.

        **Returns:** (varies by action)
        - `order`: `{text, invoiceId, repeatInvoiceId, payUrl}`.
        - `enable` / `disableCancel`: `{text}`.
        - No action: `{whoisPrivacy, cost, currency, currencySymbol}`.

        **Side effects:**
        - `order`: inserts add-on `repeat_invoices` + `invoices` rows.
        - `enable`: OpenSRS `provModify` with privacy=on; calls `post_payment_processing_new`.
        - `disableCancel`: OpenSRS `provModify` with privacy=off; marks add-on `repeat_invoices.deleted=1`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"` or TLD doesn't support privacy.
        - `4xx` — no add-on found for `enable` / `disableCancel`.

        **Related calls:**
        - **Read state:** `getDomainWhoisPrivacy`.
        - **Pay the order invoice:** `initiatePayment` (`method=cc|paypal|...`).
        - **Renew with privacy:** `postDomainRenewal` with `whois_privacy=enable`.
      operationId: updateDomainWhoisPrivacy
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainWhoisPrivacyRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DomainWhoisPrivacyRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Order, enable, or cancel the Whois privacy add-on for a domain"
      tags:
      - Domains
  /floating_ips:
    get:
      description: |-
        Use to enumerate every Floating IP the caller owns before drilling into a specific one. Read-only; safe to call frequently. No params, no body. Returns an array of rows: `floating_ip_id`, `repeat_invoices_cost` (recurring price), `floating_ip_ip` (the portable IP), `floating_ip_target_ip` (the IP it currently routes to), `floating_ip_status` (active/pending/canceled/etc.), `services_name` (package label). Empty array if the account owns no Floating IPs. Errors: 401 if unauthenticated. Use returned IDs with `getFloatingIpInfo`, `postFloatingIpsChangeIp`, `getFloatingIpInvoices`, `getFloatingIpsWelcomeEmail`, or `floating_ipsCancel`. To order a new one see `getNewFloatingIp` / `addFloatingIp`.

        Sibling ops: `getFloatingIpInfo`, `getNewFloatingIp` (catalog), `addFloatingIp` (order).
      operationId: getFloatingIpsList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  type: object
                type: array
          description: The listing of `Floating IPs` services on your account.
          links:
            getFloatingIpInfo:
              description: "Use an ID from the response array to fetch full details\
                \ via `GET /floating_ips/{id}`."
              operationId: getFloatingIpInfo
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: List all Floating IP services on the authenticated customer's account
      tags:
      - Floating_IPs
  /floating_ips/order:
    get:
      description: |-
        Use before showing a Floating IP order form, or before calling `addFloatingIp`, to discover which service types (`serviceTypes`) and prices (`packageCosts`, keyed by `services_id` in the customer's currency) are currently buyable. Read-only; no side effects. No params, no body. Returns `{ packageCosts: { <services_id>: <cost> }, serviceTypes: [ ... ] } `. Costs are `services.services_cost` filtered to `services_buyable=1` for module `floating_ips`. Errors: 401 if unauthenticated. Next steps: validate the chosen `serviceType` with `putFloating_ips`, then place the order with `addFloatingIp`. Floating IPs are portable IPv4 addresses that route to a target IP on one of the customer's active services.

        Sibling ops: `putFloating_ips` (validate), `addFloatingIp` (commit), `getFloatingIpsList` (existing IPs).
      operationId: getNewFloatingIp
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Floating IP ordering options and pricing.
                type: object
          description: Available options and pricing for ordering a Floating IP.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get pricing and service-type options for ordering a new Floating IP
      tags:
      - Floating_IPs
    post:
      description: |-
        Charges the customer and creates a new Floating IP service via `place_buy_floating_ip`. Validate first with `putFloating_ips` to avoid surprise failures. Body (form-encoded): `serviceType` (required, `services_id`), `coupon` (optional), `comment` (optional internal note). On success returns `{ continue:true, errors, total_cost, iid, iids, real_iids, serviceId, invoice_description, cj_params }` — `iid` is the master invoice ID, `serviceId` is the new `floating_ip_id`. On validation failure returns `{ continue:false, errors:[...] }` with no charge. Errors: 401 if unauthenticated; soft errors in `errors[]`. The newly-issued IP starts unassigned — point it at a target with `postFloatingIpsChangeIp` once the service is `active`.

        Sibling ops: `getNewFloatingIp` (catalog), `putFloating_ips` (validate), `getFloatingIpInfo` (poll), `postFloatingIpsChangeIp` (route), `getBillingInvoice` + `initiatePayment` (settle invoice), `floating_ipsCancel`.
      operationId: addFloatingIp
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FloatingIpOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a real Floating IP order, create billing records, and provision\
        \ the service"
      tags:
      - Floating_IPs
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Provision a floating IPv4 address. Returns invoice IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |-
        Dry-run for `addFloatingIp` — runs `validate_buy_floating_ip` to apply coupons, compute intro/repeat pricing, and surface errors before committing. No charge, no service created. Body fields (form-encoded): `serviceType` (required, `services_id` from `getNewFloatingIp.packageCosts`), `coupon` (optional code). Returns `{ continue, errors, serviceType, serviceCost, originalCost, repeatServiceCost, password, introFrequency, coupon, couponCode }`. `continue=true` means the order would succeed; `continue=false` plus populated `errors[]` means it would not. Errors: 401 if unauthenticated; 422-style soft errors arrive in the `errors` array. Use the returned `serviceType` and `couponCode` when calling `addFloatingIp`.
        Sibling ops: `getNewFloatingIp` (catalog), `addFloatingIp` (commit).
      operationId: putFloating_ips
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FloatingIpOrderRequest"
        required: true
      responses:
        "200":
          description: Validate Floating IPs order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate a Floating IP order and price it without charging the customer
      tags:
      - Floating_IPs
  /floating_ips/{id}:
    delete:
      description: |-
        Cancels the Floating IP via the shared `Api\Billing\CancelService` flow — flips status to canceled, halts recurring billing, and releases the IP back to the pool so it can no longer be re-routed. Not reversible: the customer cannot recover the same IP after release. Path param `id` (`floating_ip_id` from `getFloatingIpsList`). No body. Returns the `FloatingIpsCancelResponse` shape (success text / cancellation outcome). Errors: 401 if unauthenticated; 404 / cross-customer hidden when `id` is not owned by the caller; 409 if already canceled or otherwise non-cancelable. Confirm with the customer before calling — for routing changes use `postFloatingIpsChangeIp` instead of cancel-and-reorder.

        Sibling ops: `getFloatingIpInfo` (status), `getFloatingIpInvoices` (outstanding charges), `postFloatingIpsChangeIp` (re-route instead of cancel), `addFloatingIp` (re-order).
      operationId: floating_ipsCancel
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/floating_ipsCancel_200_response"
          description: Floating Ip Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Cancel a Floating IP service and release the IP — destructive, billing\
        \ stops"
      tags:
      - Floating_IPs
    get:
      description: "Use for a Floating IP detail screen, or to read `floating_ip_ip`\
        \ / `floating_ip_target_ip` before calling `postFloatingIpsChangeIp`. Read-only.\
        \ Path param `id` (integer, `floating_ip_id` from `getFloatingIpsList`). No\
        \ body. Returns the `ViewFloatingIp.getDetails()` payload — service info,\
        \ billing/cost summary, status, target IP, and `client_links` (action URLs\
        \ the UI can render). Internal-only fields (`admin_links`, `settings`, `csrf`)\
        \ are stripped. Errors: 401 if unauthenticated; effectively 404 / cross-customer\
        \ hidden when `id` is not owned by the caller (`get_service` filters by custid).\
        \ Siblings: `postFloatingIpsChangeIp`, `updateFloatingIpInfo`, `getFloatingIpInvoices`,\
        \ `getFloatingIpsWelcomeEmail`, `floating_ipsCancel`."
      operationId: getFloatingIpInfo
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                description: Floating IP service details.
                type: object
          description: Detailed Floating IP service information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Fetch full details for one Floating IP service, including current\
        \ target IP"
      tags:
      - Floating_IPs
    post:
      description: |-
        Stub edit endpoint that delegates to the same handler as `getFloatingIpInfo` — currently used for label/metadata edits surfaced by `ViewFloatingIp`. To re-route the IP to a different target use the dedicated `postFloatingIpsChangeIp` instead; this op does not change routing. Path param `id` (`floating_ip_id`). Body: form-encoded fields exposed by the Floating IP edit form (label/comment style). Returns the standard success-text response. Errors: 401 if unauthenticated; effectively 404 if `id` not owned by the caller. Read state first with `getFloatingIpInfo`.

        Sibling ops: `getFloatingIpInfo` (read), `postFloatingIpsChangeIp` (re-route), `floating_ipsCancel`.
      operationId: updateFloatingIpInfo
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update a Floating IP service's editable settings (label / metadata)
      tags:
      - Floating_IPs
  /floating_ips/{id}/change_ip:
    post:
      description: |-
        Reattaches the Floating IP by removing the old static route on the source switch and adding a new one on the destination switch (via `Sshwitch`), then updates `floating_ip_target_ip`. Use to move a portable IP between the customer's VPS / Quickservers / websites / dedicated servers without renumbering apps. Path param `id` (`floating_ip_id`). Body: `{ ip: <new target IP> }` (also accepts multipart form). Returns `{ success:true, text:'IP Changed' }`. Errors (returned via `json_error`): invalid IP format; IP not in our datacenter; IP not in use by an active service of this customer; service not active; another Floating IP already points to that target; switch lookup failures; route still present after removal. 401 if unauthenticated.

        Sibling ops: `getFloatingIpInfo` (read current target), `getFloatingIpsList`, `floating_ipsCancel`. Read current target with `getFloatingIpInfo` first.
      operationId: postFloatingIpsChangeIp
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/IpObject"
          application/json:
            schema:
              $ref: "#/components/schemas/IpObject"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Re-point a Floating IP to a different target IP on one of the customer's
        services
      tags:
      - Floating_IPs
  /floating_ips/{id}/invoices:
    get:
      description: "Use for a per-service billing history view — pulls the standard\
        \ `Api\\Billing\\InvoicesList` rows scoped to this Floating IP. Read-only.\
        \ Path param `id` (`floating_ip_id` from `getFloatingIpsList`). No body. Returns\
        \ the `ChargeInvoiceRows` schema: array of invoice rows with id, date, amount,\
        \ status, etc. Use the invoice IDs with the global billing endpoints (`getBillingInvoice`,\
        \ `initiatePayment`) for line-item detail. Errors: 401 if unauthenticated;\
        \ effectively 404 / cross-customer hidden when `id` is not owned by the caller.\
        \ Siblings: `getFloatingIpInfo` (service details), `getFloatingIpsWelcomeEmail`."
      operationId: getFloatingIpInvoices
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices charged against a specific Floating IP service
      tags:
      - Floating_IPs
  /floating_ips/{id}/welcome_email:
    get:
      description: |-
        Triggers `floating_ip_welcome_email($id)` to re-deliver the original setup email (the IP, routing instructions, etc.) to the customer's on-file address. Useful when the email was lost or the customer needs the IP/setup details again. No body, no params besides path `id` (`floating_ip_id`). Returns `{ text: 'Welcome Email has been resent.' }`. Errors: 401 if unauthenticated; 404 (`Invalid Service Passed`) if `id` is not owned by the caller; 409 (`Service is not active`) if status is not `active`. Side effect: sends an outbound email — avoid in tight loops. Read state first via `getFloatingIpInfo` if unsure of status.

        Sibling ops: `getFloatingIpInfo` (status), `addFloatingIp` (new order), `floating_ipsCancel`.
      operationId: getFloatingIpsWelcomeEmail
      parameters:
      - description: The Floating IP service ID. Use the ID from `GET /floating_ips`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the Floating IP welcome / setup email to the account contact
      tags:
      - Floating_IPs
  /home:
    get:
      description: "Use to render the post-login client portal home/dashboard. No\
        \ body, no params. Returns the structure produced by `getClientHomeData()`\
        \ — counts of active services per module (vps, webhosting, domains, mail,\
        \ ssl, licenses, backups, floating_ips, scrub_ips, quickservers, servers),\
        \ recent invoices, payment due alerts, ticket activity summaries, abuse/maintenance\
        \ announcements, and account-level banners. Designed for one-shot dashboard\
        \ hydration so individual modules don't each issue list calls. Cached implementation\
        \ lives in `function_requirements('client_home')` -> `getClientHomeData()`.\
        \ Errors: 401 if session is invalid or expired (unauthenticated). Sibling\
        \ ops: `getSearch` (autocomplete), `getAccountInfo`, plus per-module list\
        \ ops like `getVpsList`, `getDomainsList`, `getBillingInvoices`."
      operationId: getHome
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Home"
          description: General information for use on the home page.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Aggregate dashboard payload — service counts, recent activity, alerts"
      tags:
      - Account
  /search:
    get:
      description: "Use to power the global search box in the client portal — typeahead\
        \ across services, domains, hostnames, IPs, and ticket subjects scoped to\
        \ the current account (cross-account leakage is impossible). No body, no path\
        \ params. Query string is conventionally `q=` though the underlying `getSearchAutoComplete($custid)`\
        \ may match against multiple fields. Returns a `SearchAutocompleteResponse`\
        \ object grouping hits by category (vps, domains, websites, mail, tickets,\
        \ invoices, etc.) so the UI can render section headers. Optimized for low\
        \ latency — does NOT replace per-module list ops for paginated browsing. Errors:\
        \ 401 unauthenticated. Sibling ops: `getHome`, `getAccountInfo`, plus per-module\
        \ list ops (`getVpsList`, `getDomainsList`, `getMailList`, `getTicketsList`)."
      operationId: getSearch
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchAutocompleteResponse"
          description: Search autocomplete results for the account.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Global autocomplete across the caller's services, domains, and records"
      tags:
      - Account
  /info:
    get:
      description: |
        Enumerates what services this MyAdmin install sells before placing orders or building a signup UI. Public — no auth required. Sibling ops: `getNewVps`, `getNewWebsite`, `getNewMail`, `getNewSsl`, `getNewLicense`, `getNewBackup`, `getNewQs`, `getNewServer` — each module's catalog op for buyable-package details.

        **Path/Query/Body:** None.

        **Returns:** `{ modules, services, serviceTypes, serviceCategories }`.
        - `modules` (array) — enabled plugin modules (`vps`, `webhosting`, `domains`, `ssl`, etc.).
        - `services` (object) — map of `services_id` → row from the `services` table, filtered to `services_buyable=1 AND services_hidden=0`, with `services_ourcost` / `services_hidden` stripped, and `services_id` / `services_category` / `services_type` cast to int and `services_cost` cast to float.
        - `serviceTypes` (object) — joins service rows to human-readable type names.
        - `serviceCategories` (object) — joins service rows to category names.

        **Auth:** None.

        **Errors:** No documented error path; 401 only if a stricter auth layer is added upstream.

        **Related calls:**
        - **Module-specific order catalog:** `getNewVps`, `getNewWebsite`, `getNewMail`, `getNewSsl`, `getNewLicense`, `getNewBackup`, `getNewQs`, `getNewServer`.
        - **Deeper health probe:** `pingServer`.
      operationId: getInfo
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServicesInfo"
          description: The modules and services information from the server.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: "Discover available modules, service packages, categories, and types"
      tags:
      - Public
  /licenses:
    get:
      description: "Lists every software license service (cPanel, Plesk, LiteSpeed,\
        \ CloudLinux, etc.) on the authenticated customer's account. Use this as the\
        \ entry point for license management to discover the license_id needed by\
        \ every other Licenses endpoint. Returns an array of rows including license_id,\
        \ hostname, bound IP, services_name (license type), recurring cost, status\
        \ (pending/active/canceled), and last invoice date/paid state. No path or\
        \ query parameters; the customer scope is taken from the session. Errors:\
        \ 401 when the session is missing or expired. Caveats: list is unpaginated,\
        \ includes canceled rows so callers should filter by status. Sibling: getLicenseInfo\
        \ for full details on one license."
      operationId: getLicenseList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/LicenseRow"
                type: array
          description: The listing of `Licenses` services on your account.
          links:
            getLicenseInfo:
              description: "Use the `license_id` from any item in the response array\
                \ to fetch full service details via `GET /licenses/{id}`."
              operationId: getLicenseInfo
              parameters:
                id: $response.body#/0/license_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all software licenses owned by the authenticated customer
      tags:
      - Licenses
  /licenses/order:
    get:
      description: "Returns the catalog needed to build the license-order form: service\
        \ categories (category_id->name), buyable service types (services_id, name,\
        \ cost, billing frequency), package costs map keyed by services_id, the customer's\
        \ currency symbol, and per-package field metadata via get_license_fields.\
        \ Use this before addLicense to render type/package pickers and to validate\
        \ a chosen package_id exists and is buyable (services_hidden=0, services_buyable=1).\
        \ No path params or body. Returns LicensesOrder schema. Errors: 401 if unauthenticated.\
        \ Sibling endpoints: putLicenses (validate selection), addLicense (place order).\
        \ Note: pricing is converted to the session currency; coupon/IP/frequency\
        \ are evaluated in the validate step, not here."
      operationId: getNewLicense
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LicensesOrder"
          description: License ordering information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get available license types, packages, and pricing for ordering"
      tags:
      - Licenses
    post:
      description: |-
        Places an order for a new software license (cPanel, Plesk, LiteSpeed, etc.). Re-runs validate_buy_license then place_buy_license, which creates the repeat_invoices row, the first invoice, and queues payment processing. Always call putLicenses first to surface validation errors cheaply; addLicense re-validates and returns error JSON if continue=false. Body (form or JSON): package (services_id from getNewLicense), ip (target server IP the license binds to), frequency (billing months), coupon, comment, tos (truthy). No path params. Returns ServiceOrderPostResponse with the new service id and invoice info. Errors: 401 unauthenticated; validation or payment failures return json_error with the underlying message. Caveat: provisioning is asynchronous — poll getLicenseInfo for status.

        Sibling ops: `getNewLicense` (catalog), `putLicenses` (validate), `getLicenseInfo` (poll status), `getLicenseInvoices`, `getBillingInvoice` + `initiatePayment` (settle invoice), `licensesCancel`.
      operationId: addLicense
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LicenseOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Order a new software license and create the recurring invoice
      tags:
      - Licenses
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: "Order a software license (cPanel, Plesk, etc.). Returns invoice\
          \ IDs for checkout."
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: "Dry-runs validate_buy_license against the same payload addLicense\
        \ will accept, returning a structured result with continue=true/false plus\
        \ errors[], normalized package, ip, service_cost, original_cost, coupon_code,\
        \ custid, currency and service_extra. Always call this before addLicense to\
        \ surface package/IP/coupon/TOS issues without creating an invoice. Body fields\
        \ (form or JSON): package (services_id), ip, frequency (billing cycle months),\
        \ coupon, comment, tos. No path params. Returns the validation object. Errors:\
        \ 401 unauthenticated; 422-style errors are returned inside the body with\
        \ continue=false rather than as HTTP errors. Caveat: a valid PUT does not\
        \ reserve inventory; addLicense re-validates. Sibling: addLicense, getNewLicense."
      operationId: putLicenses
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LicenseOrderRequest"
        required: true
      responses:
        "200":
          description: Validate Licenses order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate a software license order before placing it (dry run preview)
      tags:
      - Licenses
  /licenses/{id}:
    delete:
      description: "Cancels a license service: invokes cancel_service which marks\
        \ the service canceled, deactivates the license key with the upstream vendor,\
        \ and stops the recurring invoice so no further charges occur. Use carefully\
        \ — once vendor-side deactivation propagates the key stops working on the\
        \ bound machine. Path: id (license_id from getLicenseList). No body. Returns\
        \ LicensesCancelResponse with success and a translated text message. Errors:\
        \ 401 unauthenticated; the underlying handler returns success=false JSON if\
        \ the service id is invalid or cancellation fails (contact support path).\
        \ Caveats: no prorated refund by default; pre-paid time is forfeited per TOS.\
        \ Sibling endpoints: getLicenseInfo, getLicenseInvoices for billing history\
        \ before cancelling."
      operationId: licensesCancel
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/licensesCancel_200_response"
          description: License Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a license service and stop future billing (irreversible)
      tags:
      - Licenses
    get:
      description: "Returns rich detail for a single license service: serviceInfo\
        \ row (license_id, hostname, license_ip, license_status, license_type), the\
        \ underlying services row (name, cost, frequency), client_links for self-service\
        \ actions (change IP, cancel, resend welcome email, view invoices), and provisioning\
        \ state. Use after getLicenseList to drill into a specific license, or as\
        \ the canonical lookup before postLicenseChangeIp / licensesCancel / getLicenseInvoices.\
        \ Path: id (license_id from list). No body. Errors: 401 unauthenticated; 404\
        \ if id is invalid or owned by a different customer. Caveat: admin_links/settings/csrf\
        \ are stripped — use admin endpoints for those. Sibling endpoints: updateLicenseInfo\
        \ (mutate fields), postLicenseChangeIp."
      operationId: getLicenseInfo
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/License"
          description: License information.
          links:
            postLicenseChangeIp:
              description: Change the IP address assigned to this license.
              operationId: postLicenseChangeIp
              parameters:
                id: $response.body#/serviceInfo/license_id
            getLicenseInvoices:
              description: Retrieve the billing invoices for this license service.
              operationId: getLicenseInvoices
              parameters:
                id: $response.body#/serviceInfo/license_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get full details for one license including status, IP, and links"
      tags:
      - Licenses
    post:
      description: "Updates settings on an existing license service. The primary mutable\
        \ field is the bound IP, but the endpoint shares routing with View::go so\
        \ other future fields flow through here. For IP changes prefer postLicenseChangeIp\
        \ which has explicit semantics and triggers vendor rebinding. Path: id (license_id).\
        \ Body: fields to update (form or JSON); shape varies by license type. Returns\
        \ SuccessTextResponse. Errors: 401 unauthenticated; 404 if id is invalid or\
        \ not owned; 409 if license is not active. Caveats: vendor-side propagation\
        \ (cPanel store, LiteSpeed key server, etc.) is asynchronous; some IP/hostname\
        \ changes incur a fee per vendor policy. Sibling: getLicenseInfo (read), postLicenseChangeIp\
        \ (dedicated)."
      operationId: updateLicenseInfo
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update mutable fields on a license service (e.g. assigned IP)
      tags:
      - Licenses
  /licenses/{id}/change_ip:
    post:
      description: "Changes the IP address that the license is bound to and triggers\
        \ re-issuance with the upstream vendor (cPanel store, LiteSpeed key server,\
        \ Plesk, etc.). The service must be active. Use getLicenseInfo first to read\
        \ the current license_ip, then submit the new IP. Path: id (license_id). Body\
        \ (JSON or multipart): IpObject with the new ip field. Returns SuccessTextResponse\
        \ on success. Errors: 401 unauthenticated; 404 invalid id or not owned; 409\
        \ if status != active; 422-style failures from the vendor are returned via\
        \ json_error with the upstream status_text. Caveats: many vendors charge a\
        \ per-change fee and rate-limit changes (e.g. cPanel allows limited free changes\
        \ per period); the new IP must be reachable for license verification. Sibling:\
        \ updateLicenseInfo."
      operationId: postLicenseChangeIp
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IpObject"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/IpObject"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Rebind a license to a new IP address (may incur a vendor fee)
      tags:
      - Licenses
  /licenses/{id}/invoices:
    get:
      description: "Returns the full invoice history for a single license service:\
        \ the original setup invoice plus every recurring renewal invoice generated\
        \ by the repeat_invoices entry. Use this for billing reconciliation, to display\
        \ past charges in the customer UI, or to confirm a renewal posted before contacting\
        \ support. Path: id (license_id from getLicenseList). No body. Returns ChargeInvoiceRows:\
        \ an array of invoice rows with id, date, amount, paid status, and payment\
        \ method. Errors: 401 unauthenticated; returns success=false with HTTP 400\
        \ if the service id is invalid or owned by a different customer. Caveat: only\
        \ invoices linked via repeat_invoices_id are included — manual one-off charges\
        \ from staff may not appear here. Sibling endpoints: getLicenseInfo, licensesCancel."
      operationId: getLicenseInvoices
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices tied to one software license service
      tags:
      - Licenses
  /licenses/{id}/welcome_email:
    get:
      description: "Resends the welcome email for an active license to the account\
        \ email on file. The email contains the license key, the bound IP, and vendor-specific\
        \ activation instructions (e.g. cPanel /usr/local/cpanel/cpkeyclt, LiteSpeed\
        \ lswsctrl). Use this when the customer lost the original email or rotated\
        \ mailboxes — the key itself is unchanged. Path: id (license_id). No body.\
        \ Returns SuccessTextResponse with a translated confirmation. Errors: 401\
        \ unauthenticated; 404 if the id is invalid or not owned by the session customer;\
        \ 409 if the license status is not active (cancelled licenses cannot resend).\
        \ Caveat: delivery is best-effort — check the email log if it does not arrive.\
        \ Sibling endpoints: getLicenseInfo, postLicenseChangeIp."
      operationId: getLicensesWelcomeEmail
      parameters:
      - description: The license service ID. Use `license_id` from `GET /licenses`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the license welcome email with the key and activation steps
      tags:
      - Licenses
  /oauth:
    description: "Authenticate using third-party OAuth providers such as Google. Use\
      \ GET to initiate the OAuth redirect, POST to handle the callback, and PATCH\
      \ to complete two-factor verification if required."
    get:
      description: |-
        Use as step 1 of social login. Navigate the browser (typically a popup) to `/apiv2/oauth?provider=X` so the provider authenticates the user, then handle the postMessage from the popup. Public — no auth required.
        Query params: `provider` (required, case-sensitive: `Google`/`GitHub`/`Facebook`/`Twitter`), `origin` (optional, opener window origin used to target postMessage instead of `*`).
        The endpoint redirects directly to the provider rather than returning JSON. After the provider callback, the popup posts one of: `oauth_success` (logged in), `oauth_2fa_required` (call `patchOauthTwoFactor` with the `oauth_token`), `oauth_link_required` (call `postOauthCallback` to link or create), or `oauth_error`.
        Siblings: `postOauthCallback`, `patchOauthTwoFactor`, `submitLogin` (password flow).
      operationId: getOauthRedirect
      parameters:
      - description: The OAuth provider name (e.g. `Google`).
        explode: true
        in: query
        name: provider
        required: true
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getOauthRedirect_200_response"
          description: OAuth redirect URL for the requested provider.
          links:
            postOauthCallback:
              description: "After the user completes OAuth authorization at the redirect\
                \ URL, call `POST /oauth` with the provider to handle the callback."
              operationId: postOauthCallback
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: Begin OAuth login flow — redirect user to provider for authentication
      tags:
      - Public
    patch:
      description: |
        Final step of the OAuth login flow when the account has 2FA enabled. Called after `postOauthCallback` (or the popup's `window.postMessage()` handshake) returned `2fa_required`. Verifies the TOTP against the account's stored Google Authenticator secret and creates the session. Public — no auth required. Sibling ops: `postOauthCallback` (prior step), `getOauthRedirect` (entry point), `getAccountTfaSetup` (enroll 2FA), `submitLogin`.

        **Body fields** (JSON or form):
        - `code` (string, required) — 6-digit TOTP from the authenticator app.
        - `account_id` (integer, required) — returned by the prior `postOauthCallback`.
        - `oauth_token` (string, optional) — signed token from the original `postMessage` payload, type `2fa`, 10-minute expiry. When present, its embedded OAuth profile data is merged into the account (name / picture / phone / address) for any fields still empty.

        **Returns:** `{ login: true, sessionId, account_id, account_lid, ima, name, gravatar }`.

        **Errors:**
        - `400` — invalid / expired `oauth_token` or no pending verification.
        - `401` — invalid 2FA code.
        - `409` — 2FA not enabled on the account.
        - `422` — missing `code`.

        **Related calls:**
        - **Prerequisite:** `postOauthCallback`.
        - **Enroll 2FA on the account first:** `getAccountTfaSetup` → `updateAccountTfa`.
        - **Alternate login:** `submitLogin`.
      operationId: patchOauthTwoFactor
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/patchOauthTwoFactor_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/patchOauthTwoFactor_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/patchOauthTwoFactor_200_response"
          description: OAuth 2FA verification result.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: Submit 2FA code to finish OAuth login when account has 2FA enabled
      tags:
      - Public
    post:
      description: |
        Step 3 of the OAuth login flow. Called after `getOauthRedirect` returned `oauth_link_required` via the popup's `window.postMessage()`. Either links the OAuth identity to an existing account (verifying password) or creates a new account. Public — no auth required. Sibling ops: `patchOauthTwoFactor` (2FA follow-up), `getOauthRedirect` (start the flow), `submitSignup`, `submitLogin`.

        **Body fields** (JSON or form):
        - `oauth_token` (string, required) — signed token from the popup's `window.postMessage()` payload; 10-minute expiry.
        - `login` (string, required) — email.
        - `password` (string, required).
        - `create` (boolean, optional) — set `true` to create a new account instead of linking.
        - `email_confirmation` (string, conditional) — 8-char code emailed on the first `create=true` attempt; server returns 422 `email_verification_required` until provided.
        - `tfa` (string, conditional) — 6-digit TOTP when the existing account has 2FA enabled (after the first attempt returns 422 `2fa_required`).

        **Returns:** `{ login|signup|linked: true, sessionId, account_id, account_lid, ima, name, gravatar }`.

        **Errors:**
        - `400` — invalid / expired `oauth_token`.
        - `401` — bad password or wrong 2FA code.
        - `409` — account already exists (when `create: true`).
        - `422` — missing field; `email_verification_required`; `2fa_required`.

        **Related calls:**
        - **Prerequisite:** `getOauthRedirect` to initiate the popup flow.
        - **Follow-up when 2FA required:** `patchOauthTwoFactor`.
        - **Alternate entry points:** `submitLogin`, `submitSignup`.
      operationId: postOauthCallback
      parameters:
      - description: The OAuth provider name (e.g. `Google`).
        explode: true
        in: query
        name: provider
        required: true
        schema:
          type: string
        style: form
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/postOauthCallback_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/postOauthCallback_request"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/postOauthCallback_200_response"
          description: OAuth callback result.
          links:
            patchOauthTwoFactor:
              description: "When the response contains `error_code: \"2fa_required\"\
                `, use the returned `account_id` with `PATCH /oauth` to complete two-factor\
                \ verification."
              operationId: patchOauthTwoFactor
              parameters:
                account_id: $response.body#/account_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: Complete OAuth login by linking provider to existing or new account
      tags:
      - Public
    summary: OAuth Authentication
  /login:
    description: Submit your login credentials to generate a session id which can
      be used for validating requests.
    get:
      description: |
        Bootstraps an unauthenticated login page in one round-trip — branding logo, fresh captcha challenge, auto-detected user language, and live counts of VPS / websites / servers managed by the system (often used as marketing stats). Public — no auth required. Sibling ops: `submitLogin` (consume the captcha), `getCaptcha` (refresh captcha only), `getAccountLocales`, `submitSignup`.

        **Path/Query/Body:** None.

        **Returns** `{ logo, captcha, language, counts }`:
        - `logo` (string) — URL; uses the `LOGO` constant or a default.
        - `captcha` (string) — `data:image/jpeg;base64,...` image; phrase is stored server-side under `$_SESSION['captcha']` (also aliased to `$_SESSION['captchaSignup']` and `$_SESSION['captchaFP']`) — the browser's `PHPSESSID` cookie carries the phrase to `submitLogin` / `submitSignup`.
        - `language` (string) — BCP-47 locale (e.g. `en-US`).
        - `counts` (object) — `{ vps: int, websites: int, servers: int }` from live `SELECT COUNT(*)` on the underlying tables.

        **Auth:** None.

        **Errors:** `403` per `LoginResponseError` if a stricter login gate is configured upstream.

        **Related calls:**
        - **Next:** `submitLogin` (login form post) or `submitSignup` (new account).
        - **Captcha refresh only:** `getCaptcha`.
        - **OAuth alternative:** `getOauthRedirect`.
      operationId: getLoginInfo
      responses:
        "200":
          content:
            application/json:
              examples:
                LoginInfoExample:
                  value:
                    logo: //my.interserver.net/images/logos/mystaging.png
                    captcha: "data:image/jpeg;base64,/9j/"
                    language: en-US
                    counts:
                      vps: 290201
                      websites: 205172
                      servers: 27940
              schema:
                $ref: "#/components/schemas/LoginInfo"
          description: Various pieces of information useful for rendering a login
            page.
        "403":
          content:
            application/json:
              examples:
                MissingTwoFactorCode:
                  value:
                    message: missing 2fa
                    field: tfa
                InvalidTwoFactorCode:
                  value:
                    message: invalid 2fa
                    field: tfa
                MissingCaptcha:
                  value:
                    message: missing captcha
                    field: captcha
                TooManyVerificationAttempts:
                  value:
                    message: "max tries treached on code, sending new one, submit\
                      \ again"
                    field: verify
              schema:
                $ref: "#/components/schemas/LoginErrorResponse"
          description: Error response to a login request.
      security:
      - {}
      summary: "Fetch logo, captcha, language, and stats for rendering a login page"
      tags:
      - Public
    post:
      description: |
        Primary password→session-token exchange. Pass the returned session id back as the `sessionid` HTTP header on subsequent calls. Public — no auth required. Sibling ops: `getLoginInfo` (captcha + branding), `getOauthRedirect` (social login), `submitSignup`, `updateAccountApiKey` (rotate API key once logged in).

        **Body fields** (JSON or form):
        - `login` (string, required) — email.
        - `passwd` (string, required) — password.
        - `tfa` (string, conditional) — 6-digit TOTP when the account has 2FA enabled.
        - `verify` (string, conditional) — 8-char email-confirmation code returned via email when logging in from a new IP. Triggered automatically when the IP has no `acquittal` trial record yet (see `Trial` ORM, type `verify_email`).
        - `remember` (boolean / `'true'` / `'yes'` / `'1'`, optional) — extends cookie lifetime.

        **Returns:** `{ sessionId, account_id, account_lid, ima, name, gravatar }`. The `sessionId` value is the credential to send on every subsequent authenticated request.

        **Errors:**
        - `401` — bad credentials or wrong 2FA / verify code.
        - `422` — missing `login` / `passwd` / `tfa` / `verify`; response body's `field` indicates which input is required next.
        - `429` — too many failed attempts (login-log rate-limit) or max code retries reached.

        **Related calls:**
        - **Prerequisite:** `getLoginInfo` to fetch the captcha challenge and counts.
        - **Alternate:** `getOauthRedirect` → `postOauthCallback` for social login.
        - **After login:** `updateAccountApiKey`.
      operationId: submitLogin
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/LoginSubmissionExample"
          application/json:
            examples:
              LoginSubmissionExampleExample:
                value:
                  login: user@domain.com
                  passwd: mypassword
                  remember: "true"
                  g-recaptcha-response:
                    __v_isShallow: false
                    dep:
                      w: 0
                      "n": 0
                    __v_isRef: true
                    _rawValue: zzzzz
                    _value: zzzzz
            schema:
              $ref: "#/components/schemas/LoginSubmissionExample"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginSuccessResponse"
          description: Successful login response containing the session token.
          links:
            getAccountInfo:
              description: "After a successful login, use the session token to fetch\
                \ account details via `GET /account`."
              operationId: getAccountInfo
        "402":
          content:
            application/json:
              examples:
                MissingTwoFactorCode:
                  value:
                    message: missing 2fa
                    field: tfa
                InvalidTwoFactorCode:
                  value:
                    message: invalid 2fa
                    field: tfa
                MissingCaptcha:
                  value:
                    message: missing captcha
                    field: captcha
                TooManyVerificationAttempts:
                  value:
                    message: "max tries treached on code, sending new one, submit\
                      \ again"
                    field: verify
              schema:
                $ref: "#/components/schemas/LoginErrorResponse"
          description: Error response to a login request.
        default:
          description: Default response
      security:
      - {}
      summary: Authenticate with email + password and return a session token
      tags:
      - Public
    summary: Log In to the system and create a session.
  /logout:
    get:
      description: |
        DESTRUCTIVE: invalidates the caller's session record and appsession bag. After this returns the session id can no longer authenticate requests; the client must discard it and prompt the user to log in again. Idempotent — calling with an already-invalid session returns `200` (no-op when `App::accounts()->data` is empty). API keys (`updateAccountApiKey`) and persistent OAuth links are NOT affected — only this session token. Sibling ops: `updateAccountPassword`, `updateAccountApiKey`, `logoutAccountOauth`, `deleteAccountOauthName`.

        **Path/Query/Body:** None.

        **Returns:** `{ success: true, text: 'Logged Out' }`.

        **Side effects:** calls `App::session()->destroy()` only when `api_check_auth_limits()` passes for the current account, so a locked account is short-circuited gracefully without further error.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — only on a completely malformed auth header.

        **Related calls:**
        - **Re-login:** `submitLogin` or `getOauthRedirect`.
        - **Per-provider OAuth sign-out (does NOT invalidate the session):** `logoutAccountOauth`.
      operationId: Logout
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Destroy the current API/web session — token becomes unusable
      tags:
      - Account
  /mail:
    get:
      description: |
        Enumerates every Mail Baby SMTP relay service owned by the authenticated customer. Canonical entry point for finding a `mail_id` to pass to other Mail endpoints. Filtered server-side by `mail_custid`. Sibling ops: `getMailInfo`, `getStats`, `viewMailLog`, `getMailDeliverability`, `getMailBlocks`, `getMailInvoices`, `addMail`.

        **Path/Query/Body:** None.

        **Returns:** Array of `MailRow`:
        - `mail_id` (integer) — canonical id.
        - `mail_username` (string) — SMTP username (e.g. `mb1234`).
        - `mail_status` (string enum) — `active` / `pending` / `canceled` / `suspended`.
        - `services_name` (string) — plan label.
        - `repeat_invoices_cost` (decimal string) — recurring cost.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Per-service detail:** `getMailInfo`.
        - **Send mail:** `sendMail` / `sendAdvMail`.
        - **Reputation:** `getMailDeliverability` / `getMailBlocks` / `getMailDelist`.
        - **Order a new service:** `getNewMail` → `putMail` → `addMail`.
      operationId: getMailList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/MailRow"
                type: array
          description: The listing of `Mail` services on your account.
          links:
            getMailInfo:
              description: "Use the `mail_id` from any item in the response array\
                \ to fetch full service details via `GET /mail/{id}`."
              operationId: getMailInfo
              parameters:
                id: $response.body#/0/mail_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List every Mail Baby SMTP relay service on the account
      tags:
      - Mail
  /mail/order:
    get:
      description: |
        Step 1 of the Mail Baby order flow. Returns the catalog used to bootstrap an order form: `packageCosts` keyed by `services_id` (only buyable services where `services_buyable=1`) and the full `serviceTypes` map. Read-only. Pricing is normalized to the customer's currency via `getCurrency()`. Sibling ops: `putMail`, `addMail`, `getMailList`.

        **Path/Query/Body:** None.

        **Returns** (schema `MailOrder`):
        - `packageCosts` (object) — `{<services_id>: <cost>}` per buyable plan.
        - `serviceTypes` (object) — full service-types registry (plan metadata).

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Next:** `putMail` (validate + quote — no charge), `addMail` (place order).
      operationId: getNewMail
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailOrder"
          description: Mail ordering information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read the Mail Baby order catalog — plans, package costs, service-type\
        \ metadata"
      tags:
      - Mail
    post:
      description: |
        Step 3 of the Mail Baby order flow. Revalidates via `validate_buy_mail()`, then calls `place_buy_mail()` to create a `Repeat_Invoice` recurring billing row, an initial `invoices` row, and a `mail` service record in pending status. SMTP credentials become active once the activation worker runs the welcome email (after the invoice is paid). **Real money** — call `putMail` first. Sibling ops: `getNewMail`, `putMail`, `getMailInfo`, `initiatePayment`.

        **Body fields:**
        - `serviceType` (integer, required) — plan id from `getNewMail`.
        - `coupon` (string, optional).
        - `comment` (string, optional) — saved on the order row.

        **Returns** (on success): `{continue: true, total_cost, iid, iids, real_iids, serviceId (new mail_id), invoice_description, cj_params}` — pass `real_iids` to `initiatePayment`. On validation failure: `{continue: false, errors: [...]}` with HTTP 200.

        **Side effects:**
        - Inserts `mail` service row in `pending` status.
        - Inserts `repeat_invoices` + `invoices` rows.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Pay:** `initiatePayment` with `real_iids`.
        - **Confirm activation:** `getMailInfo` (poll until `mail_status=='active'`).
        - **Resend credentials:** `getMailWelcomeEmail`.

        **Full ordering happy path:**
        ```text
        GET /mail/order                                    -> catalog (getNewMail)
        PUT /mail/order { serviceType, coupon? }           -> quote (putMail)
        POST /mail/order { serviceType, coupon?, comment? } -> { serviceId, real_iids }
        GET /billing/pay/cc/{real_iids[0]}                 -> pay (initiatePayment)
        GET /mail/{serviceId}                              -> poll until mail_status=='active'
        ```
      operationId: addMail
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MailOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a new Mail Baby order, generate invoice, and queue provisioning"
      tags:
      - Mail
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order a mail.baby relay/transactional mail service. Returns invoice
          IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |
        Step 2 of the Mail Baby order flow. Dry-runs the order through `validate_buy_mail()` without creating invoices. Returns the cost preview, coupon resolution, and validation errors. The endpoint also auto-generates an SMTP password preview the order will use. Use to surface live pricing in the UI before `addMail`. Sibling ops: `getNewMail`, `addMail`.

        **Body fields:**
        - `serviceType` (integer, required) — plan id from `getNewMail.packageCosts` keys.
        - `coupon` (string, optional) — coupon code.

        **Returns:**
        - `continue` (bool) — `true` if order can safely be POSTed.
        - `errors` (array) — validation messages.
        - `serviceType`, `serviceCost`, `originalCost`, `repeatServiceCost` (numeric).
        - `password` (string) — auto-generated SMTP password preview.
        - `introFrequency` (integer).
        - `coupon`, `couponCode` (string/integer) — resolved coupon.

        **Auth:** Session/API key.

        **Errors:**
        - `200` with `continue=false` and `errors[]` — validation problems.
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite:** `getNewMail` (catalog).
        - **Place order:** `addMail`.
      operationId: putMail
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MailOrderRequest"
        required: true
      responses:
        "200":
          description: Validate Mail order response.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Validate Mail Baby order, quote pricing, and verify coupon — no charge"
      tags:
      - Mail
  /mail/{id}:
    delete:
      description: |
        Cancels the Mail Baby service through the shared `Billing\CancelService::go($id)` flow with `module='mail'`. SMTP credentials are deactivated, the service transitions to canceled, the `repeat_invoice` is stopped, and queued submissions stop being accepted. **Irreversible via API** — re-activation requires placing a new order via `addMail`. Sibling ops: `getMailInfo`, `getMailInvoices`, `addMail`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns:** `MailCancelResponse`.

        **Side effects:**
        - Sets `mail_status='canceled'`.
        - Marks `repeat_invoices` non-renewing.
        - ZoneMTA-side: stops accepting new submissions for `mb{mail_id}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Related calls:**
        - **Sibling cancels:** `VPSCancel`, `CancelDomain`, `webhostingCancel`, etc.
        - **Re-provision:** `addMail`.
      operationId: mailCancel
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/mailCancel_200_response"
          description: Mail Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a Mail Baby service and stop the recurring invoice
      tags:
      - Mail
    get:
      description: |
        Returns the full `ViewMail` payload for one Mail Baby service — `serviceInfo`, `serviceType`, and `client_links` (URLs rewritten to API paths, e.g. `view_mail_log` → `log`). Admin fields (`admin_links`, `settings`, `csrf`) stripped. Use to render a service dashboard or retrieve SMTP host/username for MTA configuration. Sibling ops: `getMailList`, `updateMailInfo`, `mailCancel`, `resetMailPassword`, `getMailWelcomeEmail`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns** (schema `MailSchema`):
        - `serviceInfo` — `mail_id`, `mail_username` (e.g. `mb1234`), `mail_status`, `mail_invoice`, `mail_custid`, dates, currency.
        - `serviceType` — plan row (`services_ourcost` stripped).
        - `client_links` (array) — action URLs (log, alerts, blocks, etc.).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Related calls:**
        - **Send:** `sendMail` / `sendAdvMail`.
        - **Rotate password:** `resetMailPassword`.
        - **Reset credentials:** `getMailWelcomeEmail`.
        - **Cancel:** `mailCancel`.
      operationId: getMailInfo
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailSchema"
          description: Mail Information.
          links:
            getMailBlocks:
              description: Retrieve blocked email addresses for this mail service.
              operationId: getMailBlocks
              parameters:
                id: $response.body#/serviceInfo/mail_id
            getMailAlerts:
              description: Retrieve the alert configuration for this mail service.
              operationId: getMailAlerts
              parameters:
                id: $response.body#/serviceInfo/mail_id
            getMailInvoices:
              description: Retrieve the billing invoices for this mail service.
              operationId: getMailInvoices
              parameters:
                id: $response.body#/serviceInfo/mail_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read full detail for one Mail Baby service including SMTP credentials
      tags:
      - Mail
    post:
      description: |
        POST mutation hook for the Mail Baby service detail page. Currently delegates to the same `View::go()` handler as `getMailInfo` — placeholder for future field updates. Does NOT rotate credentials (use `resetMailPassword`) and does NOT change billing (use `/billing` endpoints). Sibling ops: `getMailInfo`, `mailCancel`, `resetMailPassword`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body:** Form fields.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `mail_status != "active"`.

        **Related calls:**
        - **Read:** `getMailInfo`.
        - **Rotate password:** `resetMailPassword`.
      operationId: updateMailInfo
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: POST mutation hook for the Mail Baby service detail page
      tags:
      - Mail
  /mail/{id}/blocks:
    get:
      description: |
        Returns relay-side block events for the SMTP user behind `mail_id` — the last 24 hours of `LOCAL_BL_RCPT` and `MBTRAP` rspamd hits, plus a 3-day window of suspicious-subject hits (credential-leak heuristic firing on subjects containing `@` / `smtp` / `socks5` / `socks4` more than 4 times). Use the `from` value with `delistBlock` or `postMailDelist` to clear a block. Sibling ops: `delistBlock`, `getMailDelist`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns** (schema `MailBlocks`):
        - `local` (array) — rspamd `LOCAL_BL_RCPT` hits: `{date, from, messageId, subject, to}`.
        - `mbtrap` (array) — spam-trap captures (`MBTRAP` symbol): same shape.
        - `subject` (array) — senders flagged by subject-line heuristic: `{from, subject}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `mail_status != "active"`.

        **Related calls:**
        - **Clear a block:** `delistBlock` (POST `/mail/{id}/blocks/delete`).
        - **Broader delist UI:** `getMailDelist`, `postMailDelist`.
      operationId: getMailBlocks
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                MailBlocksExample:
                  value:
                    local:
                    - date: 2023-08-07
                      from: user@domain.com
                      messageId: pFaRqFUEWkucjhTuIzYuoAgWU@domain.com
                      subject: Test Email
                      to: "['client@site.com']"
                    mbtrap:
                    - date: 2023-08-07
                      from: user@domain.com
                      messageId: pFaRqFUEWkucjhTuIzYuoAgWU@domain.com
                      subject: Test Email
                      to: "['client@site.com']"
                    subject:
                    - from: user@domain.com
                      subject: Test Email
              schema:
                $ref: "#/components/schemas/MailBlocks"
          description: OK
          links:
            delistBlockLink:
              description: "The `from` value returned in the response can be used\
                \ as the `email` parameter in `DELETE /mail/{id}/blocks`."
              operationId: delistBlock
              parameters:
                email: $response.body#/local/0/from
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List recent local-blocklist hits and spam-trap captures for the mail
        user
      tags:
      - Mail
  /mail/{id}/alerts:
    delete:
      description: |
        Hard-deletes a single alert row. Handler verifies the alert belongs to this service+module before deleting. **Irreversible** — no history is preserved; recreate via `createMailAlert` if needed. Sibling ops: `getMailAlerts`, `createMailAlert`, `updateMailAlert`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields:**
        - `alert_id` (integer, required) — from `getMailAlerts`.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `Invalid alert!` (alert not owned), `401`, `404`, `409 not active`.
      operationId: deleteMailAlert
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deleteMailAlert_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/deleteMailAlert_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Delete a Mail Baby alert by alert_id (hard delete — no recovery)
      tags:
      - Mail
    get:
      description: |
        Returns every alert row from `alerts` matching this service. Each row carries `alert_id` (use with PUT/DELETE), `alert_type`, `alert_value` (threshold), `alert_to` (notification email), `alert_enabled`, and timestamps. Sibling ops: `createMailAlert`, `updateMailAlert`, `deleteMailAlert`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns** (schema `MailAlertsResponse`): array of alert rows.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: getMailAlerts
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailAlertsResponse"
          description: Alert configuration for the mail service.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List configured delivery/bounce/quota alerts for one Mail Baby service
      tags:
      - Mail
    post:
      description: |
        Inserts a new alert row via the `Alert` ORM. The new `alert_id` is retrievable via `getMailAlerts`. Sibling ops: `getMailAlerts`, `updateMailAlert`, `deleteMailAlert`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (schema `MailAlertRequest`):**
        - `type` (string, required).
        - `value` (string/numeric, required) — threshold.
        - `to` (string, required) — notification email; validated via `FILTER_VALIDATE_EMAIL`.
        - `enabled` (bool, optional).

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** field-level errors for missing/invalid body, `401`, `404`, `409 not active`.
      operationId: createMailAlert
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MailAlertRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/MailAlertRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Create a new Mail Baby alert for delivery, bounce, or quota events"
      tags:
      - Mail
    put:
      description: |
        Updates a single alert row by `alert_id`. Handler verifies the alert belongs to this service+module before writing. Sibling ops: `getMailAlerts`, `createMailAlert`, `deleteMailAlert`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (schema `MailAlertUpdateRequest`):**
        - `alert_id` (integer, required) — from `getMailAlerts`.
        - `type` (string, required).
        - `value` (string/numeric, required) — threshold.
        - `to` (string, required) — notification email; validated via `FILTER_VALIDATE_EMAIL`.
        - `enabled` (bool, optional).

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `Invalid alert!` (alert not owned), field-level errors for missing/invalid body, `401`, `404`, `409 not active`.
      operationId: updateMailAlert
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MailAlertUpdateRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/MailAlertUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update an existing Mail Baby alert by alert_id
      tags:
      - Mail
  /mail/{id}/delist:
    get:
      description: |
        Returns a richer diagnostic snapshot than `getMailBlocks` — intended for the delist UI. Use any `SMTPFrom`/`from` value as the `unblock` field for `postMailDelist`. Sibling ops: `postMailDelist`, `getMailBlocks`, `delistBlock`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns** (schema `MailDelistResponse`):
        - `id` (integer) — `mail_id` echo.
        - `local`, `mbtrap` (array) — last 24h rspamd hits with capitalized keys (`Date`, `SMTPFrom`, `MessageId`, `Subject`, `MimeRecipients`).
        - `subject` (array) — credential-leak-heuristic firings (3-day window).
        - `manual` (array) — manually added blocks.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: getMailDelist
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailDelistResponse"
          description: Blocklist entries and delist details for the mail service.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read blocklist diagnostics and find senders eligible for delisting
      tags:
      - Mail
    post:
      description: |
        Removes all block rows for one sender email across three reputation stores: `rspamd` (by `fromemail`), `mailchannels` (by `email`), `mailbaby` (by `emailfrom`). Effect is global per-address across all three tables; takes effect immediately for new submissions. Sibling ops: `getMailDelist`, `delistBlock` (alias at `/mail/{id}/blocks/delete`), `getMailBlocks`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (schema `MailDelistRequest`):**
        - `unblock` (string, required) — sender email from `getMailDelist`/`getMailBlocks`.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `Missing parameter unblock`, `401`, `404`, `409 not active`.
      operationId: postMailDelist
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MailDelistRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/MailDelistRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Delist a sender from rspamd / mailchannels / mailbaby block lists
      tags:
      - Mail
  /mail/{id}/deliverability:
    get:
      description: |
        Returns deliverability analytics from `MailDeliveryStats` (Dragonfly cache) for the SMTP user behind `mail_id`. Default pivot is by sender; pass `?filter_domain=1` to pivot by recipient domain for the current year instead. Use to drive analytics dashboards. Sibling ops: `getStats`, `viewMailLog`, `getMailBlocks`, `getMailDelist`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Query params:**
        - `filter_domain` (string `1`, optional) — pivot by recipient domain instead of sender.

        **Returns** (schema `MailDeliverabilityResponse`):
        - `stat`: `{delivered, bounced, percent}` — totals and bounce ratio.
        - `header` (string), `col1` (string) — table headers.
        - `table_data` (array) — rows of `[<sender-or-domain>, bounced, delivered, bouncePercent]`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: getMailDeliverability
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailDeliverabilityResponse"
          description: Deliverability metrics for the mail service.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read delivered vs bounced totals broken down by sender (or by recipient
        domain)
      tags:
      - Mail
  /mail/{id}/invoices:
    get:
      description: |
        Returns every invoice associated with this `mail_id` via the shared `InvoicesList` workflow. Use to render per-service billing history or find unpaid invoices to pay via `initiatePayment`. Sibling ops: `getBillingInvoice`, `initiatePayment`, `addMail`, `mailCancel`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns:** `ChargeInvoiceRows` — array of `{id, amount, currency, paid, date, due_date, description, module: "mail", service}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404 Invalid Service`.
      operationId: getMailInvoices
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List billing invoices linked to this Mail Baby service
      tags:
      - Mail
  /mail/{id}/reset_password:
    get:
      description: |
        Generates a new 20-char SMTP password (lower/upper/digits via `generate_password`), writes it to the ZoneMTA Mongo `users` collection for username `mb{mail_id}`, logs the change to `App::history()`, and emails the result to the account-on-file via `client_email.tpl`. **Any MTA, app, or saved client still using the old password will start failing auth immediately.** The new password is **not** returned in the response — fetch via `getMailWelcomeEmail` or `getMailInfo`. Sibling ops: `getMailWelcomeEmail`, `getMailInfo`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns:** `SuccessTextResponse`.

        **Side effects:**
        - Mongo update on ZoneMTA `users` for `mb{mail_id}`.
        - `App::history()` audit entry.
        - Email sent to account owner.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** Mongo update modified 0 rows → error text; `401`, `404`, `409 not active`.
      operationId: resetMailPassword
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Rotate the SMTP password and email the new credential to the account
        owner
      tags:
      - Mail
  /mail/{id}/welcome_email:
    get:
      description: |
        Re-runs the `mail_welcome_email` plugin function — composes and sends the standard welcome email (SMTP host `relay.mailbaby.net`, port, username `mb{mail_id}`, current password, configuration tips) to the account-on-file. Use after `resetMailPassword` to redeliver the rotated credential, or when a customer reports losing the original setup email. Idempotent. Sibling ops: `resetMailPassword`, `getMailInfo`. Cross-module welcome-email endpoints: `getVpsWelcomeEmail`, `getWebsitesWelcomeEmail`, `getDomainsWelcomeEmail`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns:** `{text: "Welcome Email has been resent."}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: getMailWelcomeEmail
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the Mail Baby welcome email with SMTP credentials and setup
        info
      tags:
      - Mail
  /billing/pay/{method}/{invoices}:
    get:
      description: |
        Universal payment trigger — the final step in every order/checkout flow. Use after any order endpoint (`addVps`, `addQs`, `addBackup`, `addMail`, `addBillingPrepay`) returns an invoice id, or after `getBillingInvoices` surfaces unpaid invoices. Resolves the chosen gateway class under `include/Api/Billing/Pay/`, populates it with the invoices, and returns one of three response shapes the client must act on: `redirect` (send the user to the gateway URL), `submit` (POST a form with the supplied items), or `single` (processed synchronously). Sibling ops: `getBillingCart`, `getBillingInvoices`, `getBillingInvoice`, `addBillingPrepay`, `updateBillingPaymentMethod`, `addBillingCreditCard`.

        **Path params:**
        - `method` (string enum, required) — one of `cc`, `paypal`, `prepay`, `payssion`, `payu`, `ccavenue`, `cashfree`, `coinbase`, `btcpay`. Rejected with 400 otherwise.
        - `invoices` (string, required) — comma-separated identifiers. Each identifier may be:
          - a bare integer invoice id (e.g. `25296600`);
          - `INV<module><iid>` (e.g. `INVvps25296600`) — strict invoice lookup;
          - `SERVICE<module><id>` (e.g. `SERVICEvps12345`) — picks the most recent unpaid invoice for that service;
          - `RINV<module><rid>` (e.g. `RINVvps78901`) — picks the most recent unpaid invoice for that repeat-invoice row;
          - `PREPAYID<pid>INV<iid>` — explicit prepay-funding invoice.

        **Query params:**
        - `redirectUrl` (string, optional) — override the gateway return-URL. Defaults to `https://my.interserver.net/pay/`.

        **Returns** (one of three shapes — branch on `type`):
        - **type=`redirect`:** `{type: "redirect", redirect: "<gateway-url>", text: "..."}` — send the user to `redirect`.
        - **type=`submit`:** `{type: "submit", action: "<url>", method: "POST", items: {field: value, ...}}` — render a form with those fields, POST to `action`.
        - **type=`single`:** `{type: "single", text: "..."}` — payment already processed; surface `text` to the customer.

        **Side effects:**
        - Creates a `payment_requests` row tracking the attempt (via `addPaymentRequest`).
        - On `single`-mode success (`cc`, `prepay`): marks the underlying `invoices.invoices_paid=1`, triggers `queue_process_payment($iid)` → service activation.
        - On `redirect`/`submit`-mode: nothing is paid yet; the gateway IPN/callback handler in `confirm()` (in each `Pay/*.php` subclass) runs `queue_process_payment` after the gateway notifies us of success.

        **Auth:** Session/API key. Ownership of every referenced invoice is enforced through the `setInvoices()` lookup (filters by session `account_id`).

        **Errors:**
        - `400 Invalid payment method` — unrecognized `method`.
        - `402` / gateway-specific text — card declined, balance insufficient, etc. Returned as `{error: "<text>"}`.
        - `422 Invalid Invoice Tag` — identifier format not recognized.
        - `401` — unauthenticated.
        - Method-specific:
          - `cc`: card not verified (use `addBillingCreditCard` → `patchBillingCreditCardVerify` → `postBillingCreditCardVerify` first; verify via `updateBillingPaymentMethod`).
          - `prepay`: insufficient prepay balance (use `addBillingPrepay` to top up first).

        **Related calls:**
        - **Get an invoice id to pass:** `addVps` / `addQs` / `addBackup` / `addMail` / `addBillingPrepay` / `getBillingInvoices`.
        - **Confirm invoice detail first:** `getBillingInvoice`.
        - **Set up payment methods:** `addBillingCreditCard`, `patchBillingCreditCardVerify`, `postBillingCreditCardVerify`, `updateBillingPaymentMethod`.
        - **After payment:** poll the originating service endpoint (e.g. `getVpsInfo` for VPS) until status flips to `active`.

        **Example happy-path (VPS):**
        ```text
        # 1) Order created — POST /vps/order returned {serviceid: 12345, real_iids: ["25296600"]}
        # 2) Pay with stored credit card:
        GET /apiv2/billing/pay/cc/25296600
        # 3) Response:
        {"type": "single", "text": "Payment processed."}
        # 4) Poll service:
        GET /apiv2/vps/12345  -> {"vps_status": "active", ...}
        ```
        **Example PayPal flow:**
        ```text
        GET /apiv2/billing/pay/paypal/25296600
        {"type": "redirect", "redirect": "https://www.paypal.com/...", "text": "..."}
        # Client redirects user; PayPal IPN later marks invoice paid and activates service.
        ```
      operationId: initiatePayment
      parameters:
      - description: "The payment method to use. Valid values: `cc` (credit card),\
          \ `paypal`, `prepay`, `payssion`, `payu`, `ccavenue`, `cashfree`, `coinbase`,\
          \ `btcpay`."
        explode: false
        in: path
        name: method
        required: true
        schema:
          enum:
          - cc
          - paypal
          - prepay
          - payssion
          - payu
          - ccavenue
          - cashfree
          - coinbase
          - btcpay
          type: string
        style: simple
      - description: "A comma-separated list of invoice IDs or invoice Tags to pay.\
          \ These IDs are returned by order endpoints (e.g. `/backups/order`, `/vps/order`)\
          \ and by `/billing/invoices`. Invoice tags accepted are SERVICE<module><id>,\
          \ RINV<module><repeat invoice id>, INV<module><invoice id>, PREPAY<prepay\
          \ id><invoice id>, and <invoice id>."
        explode: false
        in: path
        name: invoices
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/initiatePayment_200_response"
          description: Payment initiation response with redirect or form data.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Pay invoices through the chosen gateway — returns the next-step action
      tags:
      - Billing
      x-payment-info:
        intent: charge
        method: card
        amount: 0
        currency: USD
        description: "Pay one or more invoices using the chosen method (cc, paypal,\
          \ btcpay, coinbase, payu, ccavenue, cashfree, payssion, prepay)."
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
  /ping:
    get:
      description: |
        Trivial GET that returns the JSON string `"pong"` so AI agents and monitors can verify the API endpoint is reachable. Public — no auth required, no params, no body. Does not exercise the database, queue, or any plugin modules. Sibling ops: `getInfo` (richer probe that touches MySQL).

        **Path/Query/Body:** None.

        **Returns:** JSON-encoded string `"pong"` with HTTP 200.

        **Auth:** None (public endpoint).

        **Errors:** No documented error path under normal operation — a non-200 or absent response indicates the API is down, the host is unreachable, or upstream routing is broken.

        **Use when:**
        - Bootstrapping a new client and want to confirm the API is reachable.
        - Smoke-testing in CI/health-check pipelines.
        - Diagnosing connectivity issues before higher-cost calls.

        **Related calls:**
        - **Deeper health probe:** `getInfo` (exercises the DB layer).
      operationId: pingServer
      responses:
        "200":
          content:
            application/json:
              examples:
                pingResponse:
                  value: '"pong"'
              schema:
                type: string
          description: 'A simple response of "pong" for use with simple tests to see
            if a service is up. '
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Something is wrong
      security:
      - {}
      summary: Liveness check — returns the JSON string "pong" to confirm API is up
      tags:
      - Public
  /qs:
    get:
      description: |-
        Use to enumerate the caller's QuickServers (quick-provision physical dedicated boxes that share the VPS billing model). No params, no body. Each row has `qs_id`, `qs_name`, `qs_hostname`, `qs_status`, `qs_comment`, and `cost`. Feed `qs_id` into `getQsInfo` for full details, or any per-server action (`doQsStart`, `doQsStop`, `doQsRestart`, `getQsBackups`, etc.).
        Returns: array of QuickServer rows. Errors: 401 if unauthenticated.
        Siblings: `getVpsList` (virtual VPS surface), `getQsInfo`, `getNewQs` for ordering metadata.
      operationId: getQsList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/QuickserverRow"
                type: array
          description: The listing of `Quickservers` services on your account.
          links:
            GetQuickServerDetails:
              description: Use the `qs_id` from any item in the response array to
                fetch full QuickServer details.
              operationId: getQsInfo
              parameters:
                id: $response.body#/0/qs_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List QuickServer rapid-deploy dedicated servers on the account
      tags:
      - QuickServers
  /qs/order:
    get:
      description: |-
        Use before placing or validating a QuickServer order to retrieve pricing, available servers, OS templates, and form fields. Read-only — no params, no body, no charge.
        Returns: `QuickserverOrder` schema with plan/template/server options used to build the order payload for `putQs` (validate) or `addQs` (place). Errors: 401 if unauthenticated.
        Siblings: `putQs` (dry-run validation), `addQs` (commits and invoices), `getNewVps` (virtual VPS ordering surface).
      operationId: getNewQs
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuickserverOrder"
          description: Quickserver Ordering information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get QuickServer order form metadata and available plans/templates
      tags:
      - QuickServers
    post:
      description: |-
        Commits the validated order: creates the service row, generates a real invoice, and queues provisioning. Body fields match `putQs` (`server`, `password`, `os`, `comment`, `tos`) — call `putQs` first to catch errors. On `validation.continue=false`, returns the joined error string with no charge.
        Returns: `ServiceOrderPostResponse` with the new service ID and invoice info. Pay via `getBillingInvoice`/`initiatePayment`. Errors: 401 if unauthenticated, 4xx with message on validation failure.
        Siblings: `putQs` (validate first), `getNewQs`, `addVps` (VPS equivalent).
      operationId: addQs
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QsOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a QuickServer order, generating a real invoice and queuing provisioning"
      tags:
      - QuickServers
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order a QuickServer instance. Returns invoice IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |-
        Dry-run the order payload before calling `addQs`. No invoice is created and no service is provisioned. Use to surface form errors, compute the price, and resolve the chosen `server`/`os`/`distro` against the master pool.
        Body (form): `server` (master ID), `password`, `os` (template), `comment`, `tos`. Returns the `validate_buy_qs` result with `continue` flag, normalized fields, `service_cost`, and `errors` array. Errors: 401 if unauthenticated; validation errors are returned in the body, not as 4xx.
        Siblings: `addQs` (commits the order), `getNewQs` (form metadata), `putVps` (VPS equivalent).
      operationId: putQs
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QsOrderRequest"
        required: true
      responses:
        "200":
          description: Validate QuickServer Order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate a QuickServer order without charging or provisioning
      tags:
      - QuickServers
  /qs/{id}:
    delete:
      description: |-
        Schedules deprovisioning. The server keeps running until the current billing period ends, then is canceled and the recurring invoice stops. Path param: `id` (integer).
        Returns: `{ success: bool, text: string }`. Errors: 401 if unauthenticated, 404 if not owned by caller. Reversible only by support before the cycle closes — use `getQsInvoices` to check the next invoice date first.
        Siblings: `getQsInfo`, `VPSCancel` (VPS equivalent), `serversCancel` (dedicated equivalent).
      operationId: quickserversCancel
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/quickserversCancel_200_response"
          description: Rapid Deploy Servers Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a QuickServer service at the end of the current billing cycle
      tags:
      - QuickServers
    get:
      description: |-
        Returns the QuickServer dashboard payload — service info, IPs, hostname, OS, status, billing, and the list of available `client_links` (action endpoints the caller is allowed to invoke). Path param: `id` (integer QuickServer ID).
        Returns: `Quickserver` schema. Use response links to drive `doQsStart`, `doQsStop`, `doQsRestart`, `getQsBackups`, `getQsReinstallOs`, `getQsReverseDns`, `getQsInvoices`. Errors: 401 if unauthenticated, 404 if `id` is not owned by caller.
        Siblings: `updateQsInfo` (mutate), `quickserversCancel` (delete), `getVpsInfo` (VPS equivalent).
      operationId: getQsInfo
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quickserver"
          description: Quickserver details
          links:
            GetQuickServerInvoices:
              description: Retrieve billing invoices for this QuickServer.
              operationId: getQsInvoices
              parameters:
                id: $request.path.id
            RestartQuickServer:
              description: Restart this QuickServer.
              operationId: doQsRestart
              parameters:
                id: $request.path.id
            StartQuickServer:
              description: Power on this QuickServer.
              operationId: doQsStart
              parameters:
                id: $request.path.id
            StopQuickServer:
              description: Power off this QuickServer.
              operationId: doQsStop
              parameters:
                id: $request.path.id
            BackupQuickServer:
              description: Create a backup of this QuickServer.
              operationId: postQsBackup
              parameters:
                id: $request.path.id
            ListQuickServerBackups:
              description: List available backups for this QuickServer.
              operationId: getQsBackups
              parameters:
                id: $request.path.id
            ReinstallQuickServerOS:
              description: View available OS templates for reinstalling this QuickServer.
              operationId: getQsReinstallOs
              parameters:
                id: $request.path.id
            GetReverseDns:
              description: View reverse DNS entries for this QuickServer.
              operationId: getQsReverseDns
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get full details for one QuickServer including credentials and links
      tags:
      - QuickServers
    post:
      description: |-
        Mutates QuickServer-level settings (comment, stored notes) without affecting the running OS. Path param: `id`. Body fields are module-specific and processed by the shared `View::go` handler.
        Returns: `SuccessTextResponse`. Errors: 401 if unauthenticated, 404 if not owned by caller.
        For server-side actions use the dedicated endpoints — hostname via `postQsChangeHostname`, password via `postQsChangeRootPassword`, OS via `postQsReinstallOs`. Siblings: `getQsInfo` (read), `quickserversCancel` (delete).
      operationId: updateQsInfo
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update QuickServer order metadata or stored settings without OS impact
      tags:
      - QuickServers
  /qs/{id}/backup:
    get:
      description: |-
        Note: GET on `/qs/{id}/backup` triggers a backup job — despite the verb, this is a state-changing action. Queues a `backup` operation; backup name is auto-generated. Path param: `id` (integer).
        Returns: `{ text, queueId }`. Async — backup completes in minutes to hours depending on disk size. Poll `getQsBackups` to see when it appears. Errors: 401 if unauthenticated, 404 if not owned by caller, 409 if status != `active`.
        Siblings: `getQsBackups` (list), `postQuickServerRestore`, `downloadQsBackup`, `deleteQsBackup`.
      operationId: getQsBackup
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Queue creation of a new QuickServer backup snapshot (note: GET triggers\
        \ job)"
      tags:
      - QuickServers
  /qs/{id}/backups:
    delete:
      description: |-
        Removes the backup from its storage backend. Irreversible — the backup cannot be recovered. Path param: `id`. Required: `file` (the backup `name` from `getQsBackups`, in query or form body).
        Works for `swift` and `minio` backups; `zfs` snapshots cannot be deleted via this endpoint (returns an error pointing to support). Returns: `SuccessTextResponse` with the removed name. Errors: 401, 404 if not owned, error message if backup type is unsupported or the storage operation fails.
        Siblings: `getQsBackups` (list), `downloadQsBackup` (PATCH), `postQuickServerRestore`.
      operationId: deleteQsBackup
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given QuickServer."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      - description: The backup filename to delete.
        explode: true
        in: query
        name: file
        required: true
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Permanently delete a QuickServer backup file from object storage
      tags:
      - QuickServers
    get:
      description: |-
        Returns all backups visible to the caller for this QuickServer across the three backup backends. Path param: `id` (integer). Optional query `all=1` lists every backup the customer owns, not just this server's.
        Returns: `VpsBackupRows` array — each row has `name`, `type` (swift/minio/zfs), `size`, `service`, `path`. Use `name` (not a numeric ID) with `downloadQsBackup` (PATCH), `deleteQsBackup` (DELETE), or `postQuickServerRestore`. Errors: 401, 404 if not owned by caller.
        Siblings: `getQsBackup` (create), `postQuickServerRestore`.
      operationId: getQsBackups
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given QuickServer."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsBackupRows"
          description: The listing of available backups for the QuickServer.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "List available QuickServer backups across Swift, MinIO, and ZFS storage"
      tags:
      - QuickServers
    patch:
      description: |-
        Returns a temporary signed URL to fetch the backup directly from object storage. Path param: `id`. Body (JSON or form): `file` (the backup `name` from `getQsBackups`).
        Only available for `minio`-type backups; `swift` and `zfs` backups return an error directing the caller to contact support. URL expires in 24 hours. Returns: `{ text, url }`. Errors: 401, 404 if not owned, error message for unsupported backup type or sharing failure.
        Siblings: `getQsBackups` (list, get `name`), `deleteQsBackup`, `postQuickServerRestore`.
      operationId: downloadQsBackup
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given QuickServer."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/downloadQsBackup_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/downloadQsBackup_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/downloadQsBackup_200_response"
          description: Download URL for the backup file.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Generate a 24-hour pre-signed download URL for a QuickServer backup
      tags:
      - QuickServers
  /qs/{id}/block_smtp:
    get:
      description: |-
        Queues a firewall rule that drops outbound port 25 traffic, used to halt spam/abuse without taking the server offline. Path param: `id` (integer). No body.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes via the queue worker, which also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Reversible only by support — there is no `unblock_smtp` endpoint.
        Siblings: `doVpsBlockSmtp`, `getQsInfo`.
      operationId: doQsBlockSmtp
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Block outbound SMTP traffic on a QuickServer to halt mail abuse
      tags:
      - QuickServers
  /qs/{id}/change_hostname:
    get:
      description: |-
        Read-only probe before calling `postQsChangeHostname`. Path param: `id` (integer). Returns the current hostname and the validation rules the new hostname must satisfy.
        Returns: object with hostname metadata. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Note: hostname changes are only supported on OpenVZ/Virtuozzo platforms — `postQsChangeHostname` rejects KVM/dedicated types with an explanatory error.
        Siblings: `postQsChangeHostname`, `getVpsChangeHostname`.
      operationId: getQsChangeHostname
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: QuickServer Change Hostname info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get current QuickServer hostname plus change rules and platform support
      tags:
      - QuickServers
    post:
      description: |-
        Updates the hostname and the matching reverse DNS entry. Path param: `id`. Body (JSON or form): `hostname` (must pass `valid_hostname`, must differ from current).
        Only supported on OpenVZ/Virtuozzo platforms — KVM/dedicated returns a 4xx with a contact-support message. Pending services update the DB row directly (`{ text }`); active services queue the change (`{ text, queueId }`, ~2 min). Errors: 401, 404 if not owned, 409 if status != `active`, validation error for bad hostname or no change.
        Siblings: `getQsChangeHostname`, `postVpsChangeHostname`.
      operationId: postQsChangeHostname
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Change a QuickServer's system hostname (OpenVZ/Virtuozzo only)
      tags:
      - QuickServers
  /qs/{id}/change_root_password:
    get:
      description: |-
        Read-only probe before calling `postQsChangeRootPassword`. Path param: `id` (integer). Use to surface password complexity rules and confirm the QuickServer accepts root password changes.
        Returns: object with reset metadata. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        Note: this changes the OS root password (Linux) — for the Webuzo control panel password use `postQsChangeWebuzoPassword`. Siblings: `postQsChangeRootPassword`, `postQsResetPassword` (random password), `getVpsChangeRootPassword`.
      operationId: getQsChangeRootPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: QuickServer Change Root Password response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get metadata for QuickServer root/OS password change requirements
      tags:
      - QuickServers
    post:
      description: |-
        Queues a root password change. Path param: `id`. Body (JSON or form): `password` (the new password — required, no server-side complexity validation here).
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Both queue and history entries are written. Errors: 401, 404 if not owned, 409 if status != `active`, 400 if `password` is missing.
        For a randomly generated password use `postQsResetPassword` instead. For Webuzo panel password use `postQsChangeWebuzoPassword`. Siblings: `getQsChangeRootPassword`, `postVpsChangeRootPassword`.
      operationId: postQsChangeRootPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Change QuickServer root/administrator password to a chosen value
      tags:
      - QuickServers
  /qs/{id}/change_timezone:
    get:
      description: |-
        Returns the system timezone catalog (parsed from `/usr/share/zoneinfo/zone.tab`) for use with `postQsChangeTimezone`. Path param: `id` (integer). Read-only — no queue, no charge.
        Returns: array of timezone strings (e.g. `America/New_York`, `Europe/London`). Errors: 401, 404 if not owned by caller, 409 if status != `active` (handler labels these errors as `Invalid VPS Passed` / `VPS is not active` due to shared code).
        Siblings: `postQsChangeTimezone` (commit), `getVpsChangeTimezone`, `getQsChangeHostname` (also informational).
      operationId: getQsChangeTimezone
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                TimeZones:
                  value:
                  - America/New_York
                  - America/Nome
                  - America/Noronha
                  - America/North_Dakota/Beulah
                  - America/North_Dakota/Center
              schema:
                items:
                  type: string
                type: array
          description: QuickServer Change Timezone info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List timezones the QuickServer can be set to via change_timezone
      tags:
      - QuickServers
    post:
      description: |-
        Queues a timezone change. Path param: `id`. Body (JSON or form): `timezone` (must be one of the strings returned by `getQsChangeTimezone`).
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes by the queue worker. Errors: 401, 404 if not owned, 409 if status != `active`, 422 if `timezone` is not in the catalog.
        Siblings: `getQsChangeTimezone` (call first to get valid options), `postVpsChangeTimezone`.
      operationId: postQsChangeTimezone
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/TimezoneUpdate"
          application/json:
            schema:
              $ref: "#/components/schemas/TimezoneUpdate"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Change the system timezone on a QuickServer to a catalog entry
      tags:
      - QuickServers
  /qs/{id}/change_webuzo_password:
    get:
      description: |-
        Read-only probe before `postQsChangeWebuzoPassword`. Path param: `id` (integer). Webuzo is a control panel optionally installed on QuickServers — its admin password is separate from the OS root password.
        Returns: object with change instructions. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        Siblings: `postQsChangeWebuzoPassword`, `postQsChangeRootPassword` (OS root password), `postQsResetPassword`.
      operationId: getQsChangeWebuzoPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: QuickServer Change Webuzo Password info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get metadata for changing the Webuzo control panel admin password
      tags:
      - QuickServers
    post:
      description: |-
        Calls the Webuzo SDK directly on the server to change the panel `admin` password, then emails the new credentials. Path param: `id`. Body: `password` (new Webuzo password, must pass `valid_password`), `login_password` (caller's account login password — verified via md5 hash).
        Synchronous — no queue ID. Requires a prior Webuzo-Details history entry. Returns: success message string. Errors: 401, 404 if not owned, 409 if status != `active`, validation errors for missing fields, wrong login password, weak new password, or SDK failure.
        Siblings: `getQsChangeWebuzoPassword`, `postQsChangeRootPassword` (OS root).
      operationId: postQsChangeWebuzoPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Change Webuzo control panel admin password live (synchronous, not\
        \ queued)"
      tags:
      - QuickServers
  /qs/{id}/disable_cd:
    get:
      description: |-
        Queues removal of the virtual CD/DVD device from the QuickServer (full disable, not just eject). Path param: `id` (integer). No body.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        Siblings: `doQsEjectCd` (eject the ISO but keep drive), `postQsInsertCd` (mount an ISO), `getQsInsertCd` (list available ISOs).
      operationId: doQsDisableCd
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Disable the virtual CD/DVD drive device on a QuickServer
      tags:
      - QuickServers
  /qs/{id}/disable_quota:
    get:
      description: |-
        Queues a job to turn off disk-quota enforcement at the OS level. Use when quota errors block legitimate writes or before resizing disk space. Path param: `id` (integer). No body.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Re-enable later with `doQsEnableQuota`.
        Siblings: `doQsEnableQuota` (re-enable), `doVpsDisableQuota` (VPS equivalent).
      operationId: doQsDisableQuota
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Disable disk-quota enforcement at OS level on a QuickServer
      tags:
      - QuickServers
  /qs/{id}/eject_cd:
    get:
      description: |-
        Queues an eject — drive remains attached but no media. Path param: `id` (integer). No body.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes. The queue worker also re-runs VNC setup so the console reflects the change. Errors: 401, 404 if `id` is not owned by caller. Note: this handler does not validate `active` status.
        Siblings: `postQsInsertCd` (mount an ISO), `getQsInsertCd` (list ISOs), `doQsDisableCd` (remove the drive itself).
      operationId: doQsEjectCd
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Eject the currently mounted ISO from a QuickServer's virtual CD drive
      tags:
      - QuickServers
  /qs/{id}/enable_quota:
    get:
      description: |-
        Queues a job to turn on disk-quota enforcement at the OS level. Pair with `doQsDisableQuota` when re-enabling after maintenance, disk resizing, or restoring a backup. Path param: `id` (integer). No body.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        Siblings: `doQsDisableQuota` (turn off), `doVpsEnableQuota` (VPS equivalent).
      operationId: doQsEnableQuota
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Enable disk-quota enforcement at OS level on a QuickServer
      tags:
      - QuickServers
  /qs/{id}/insert_cd:
    get:
      description: |-
        Returns the catalog of bootable ISOs the caller can mount via `postQsInsertCd`. Path param: `id` (integer). Read-only — no queue, no charge.
        Returns: object with available ISO entries (URLs/labels) keyed for the QuickServer's hardware type. Errors: 401 if unauthenticated. Note: this handler does not validate ownership or active status — pair with `getQsInfo` first if you need those checks before presenting options to a user.
        Siblings: `postQsInsertCd` (mount the chosen URL), `doQsEjectCd`, `doQsDisableCd`, `getVpsInsertCd`.
      operationId: getQsInsertCd
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: QuickServer Insert CD info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List ISO images available to mount on a QuickServer's virtual CD
      tags:
      - QuickServers
    post:
      description: |-
        Queues an `insert_cd` job that attaches the given ISO URL to the QuickServer's virtual CD drive (typically for OS reinstalls or rescue boots). Path param: `id`. Body (JSON or form): `url` (the ISO URL — pick one from `getQsInsertCd`).
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned by caller. The action is idempotent in effect (latest mount wins).
        Siblings: `getQsInsertCd` (list options), `doQsEjectCd` (unmount), `doQsDisableCd`, `postQsReinstallOs` (template-based).
      operationId: postQsInsertCd
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Mount an ISO image as the QuickServer's virtual CD via URL
      tags:
      - QuickServers
  /qs/{id}/invoices:
    get:
      description: |-
        Returns invoices charged for this QuickServer (initial setup + recurring). Path param: `id` (integer).
        Returns: `ChargeInvoiceRows` — each row has invoice ID, amount, status (paid/unpaid), date. Use the invoice ID with `getBillingInvoice` for full detail or `initiatePayment` to settle. Errors: 401 if unauthenticated, 404 if not owned by caller.
        Siblings: `getQsInfo`, `getVpsInvoices`, `getBillingInvoice`, `quickserversCancel` (check next-invoice date before canceling).
      operationId: getQsInvoices
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List billing invoices charged for one QuickServer service
      tags:
      - QuickServers
  /qs/{id}/reinstall_os:
    get:
      description: |-
        Returns the OS template catalog filtered to the QuickServer's hardware/template type. Path param: `id` (integer). Read-only — no provisioning happens.
        Returns: `{ templates: [...] }` — each template has `template_file`, `template_name`, `template_version`. Use `template_file` with `postQsReinstallOs`. Non-admin callers only see templates with `template_available=1`. Errors: 401 if unauthenticated.
        Siblings: `postQsReinstallOs` (commit, destructive), `getVpsReinstallOs`.
      operationId: getQsReinstallOs
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsTemplatesList"
          description: QuickServer Reinstall OS info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List OS templates available for a QuickServer reinstall
      tags:
      - QuickServers
    post:
      description: |-
        Wipes the disk and reinstalls the chosen OS template. All data, configs, and snapshots are erased. Path param: `id`. Body: `template` (a `template_file` from `getQsReinstallOs`), `password` (new root password — required for non-Windows templates).
        For active services, queues `reinstall_os` (~2 min). For inactive services, just stores the OS preference for next activation. Updates `qs_status` to `Reinstalling` and clears screenshots. Returns flash messages — typical envelope. Errors: 401, invalid template name returns error flash.
        Siblings: `getQsReinstallOs` (list options), `postVpsReinstallOs`, `postQuickServerRestore` (recover from backup instead).
      operationId: postQsReinstallOs
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reinstall the operating system on a QuickServer (DESTRUCTIVE — wipes
        disk)
      tags:
      - QuickServers
  /qs/{id}/reset_password:
    get:
      description: |-
        Read-only probe before `postQsResetPassword`. Path param: `id` (integer). Use to confirm the QuickServer is in a state that allows password resets.
        Returns: object with reset configuration. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        Note: `postQsResetPassword` generates a random password — for a chosen value use `postQsChangeRootPassword`. Siblings: `postQsResetPassword`, `postQsChangeRootPassword`, `getVpsResetPassword`.
      operationId: getQsResetPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: QuickServer Reset password info
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get options for QuickServer randomized root password reset
      tags:
      - QuickServers
    post:
      description: |-
        Queues a `reset_password` job that generates a new root password and emails it to the account owner. Path param: `id` (integer). No body — password is generated server-side.
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned by caller, 409 if status != `active`.
        For a chosen password use `postQsChangeRootPassword` instead; for the Webuzo panel password use `postQsChangeWebuzoPassword`. Siblings: `getQsResetPassword`, `postVpsResetPassword`.
      operationId: postQsResetPassword
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reset QuickServer root password to a server-generated random value
      tags:
      - QuickServers
  /qs/{id}/restart:
    get:
      description: |-
        Queues a graceful restart — equivalent to `reboot` inside the OS. Path param: `id` (integer). No body. Use to recover from a hung service or apply pending kernel/config changes.
        Returns: `{ text, queueId }`. Async — server is back online within ~2 minutes; queue worker also re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on `active` status — restarts work even on suspended services.
        Siblings: `doQsStart`, `doQsStop`, `doVpsRestart`.
      operationId: doQsRestart
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reboot a QuickServer with a graceful OS-level restart
      tags:
      - QuickServers
  /qs/{id}/reverse_dns:
    get:
      description: |-
        Returns the current PTR record for the primary IP and any additional IPs assigned to the QuickServer. Path param: `id` (integer). Read-only — looks up live DNS, no queue.
        Returns: `{ ips: { "<ip>": "<hostname>", ... } }`. Use the keys with `postQsReverseDns` to update entries. Errors: 401 if unauthenticated. Note: handler does not gate on ownership/active status.
        Siblings: `postQsReverseDns`, `getVpsReverseDns`.
      operationId: getQsReverseDns
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReverseDnsEntries"
          description: QuickServer Reverse DNS info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get reverse DNS (PTR) records for all of a QuickServer's IPs
      tags:
      - QuickServers
    post:
      description: |-
        Sets PTR records for one or more of the QuickServer's IPs. Path param: `id`. Body (form): `ips` — keyed by IP, value is the desired hostname (must be valid).
        Returns: `{ message: "DNS Updated", success: true }`. Caveat: in the current implementation the body is parsed but the per-IP update loop is a no-op shell — verify with `getQsReverseDns` after calling, and use the support channel if changes don't propagate. Errors: 401 if unauthenticated.
        Siblings: `getQsReverseDns`, `postVpsReverseDns`.
      operationId: postQsReverseDns
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Update QuickServer Reverse DNS response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update reverse DNS (PTR) records for a QuickServer's IPs
      tags:
      - QuickServers
  /qs/{id}/setup_vnc:
    get:
      description: |-
        Read-only probe for the VNC tunnel that exposes the server's console (host, port, credentials). Path param: `id` (integer).
        Returns: object with VNC connection info. Errors: 401 if unauthenticated, 404 if `id` is not owned by caller, 409 if service is not `active`. Note: this endpoint is currently a stub — the `// todo: return vnc info` line indicates the response body may be empty until completed.
        Siblings: `postQsSetupVnc` (configure access IP), `getVpsSetupVnc`.
      operationId: getQsSetupVnc
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get QuickServer Setup VNC Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get current VNC console connection details for a QuickServer
      tags:
      - QuickServers
    post:
      description: |-
        Sets the IP allowed to reach the VNC tunnel and queues a `setup_vnc` to apply it. Path param: `id`. Body (JSON or form): `vnc` (a valid IPv4 address — only this address can reach the console).
        Returns: `{ text, queueId }`. Async — applied within ~2 minutes. Errors: 401, 404 if not owned, 409 if status != `active`. Returns an inline `Invalid IP` message when `vnc` fails `validIp`. The VPS-style helper also runs after the DB update.
        Siblings: `getQsSetupVnc` (read), `postVpsSetupVnc`.
      operationId: postQsSetupVnc
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Configure the source IP allowed to reach a QuickServer's VNC console
      tags:
      - QuickServers
  /qs/{id}/start:
    get:
      description: |-
        Queues a `start` command to bring the QuickServer online. Path param: `id` (integer). No body. Idempotent in practice — re-running on an already-on server is a no-op at the worker.
        Returns: `{ text, queueId }`. Async — typically online within ~2 minutes; queue worker re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on status, so it can be issued even for non-active services.
        Siblings: `doQsStop`, `doQsRestart`, `getQsInfo`, `doVpsStart`.
      operationId: doQsStart
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Power on a QuickServer that is currently stopped or pending boot
      tags:
      - QuickServers
  /qs/{id}/stop:
    get:
      description: |-
        Queues a `stop` command. Path param: `id` (integer). No body. Use before maintenance, snapshot, or to halt traffic — billing continues regardless of power state, so use `quickserversCancel` to also stop charges.
        Returns: `{ text, queueId }`. Async — typically off within ~2 minutes; queue worker re-runs VNC setup. Errors: 401, 404 if not owned by caller. Note: handler does not gate on status.
        Siblings: `doQsStart`, `doQsRestart`, `doVpsStop`.
      operationId: doQsStop
      parameters:
      - description: QuickServer ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Power off a QuickServer with a graceful shutdown command
      tags:
      - QuickServers
  /qs/{id}/traffic_usage:
    get:
      description: |-
        Returns the inbound/outbound bandwidth totals and time-series points for the QuickServer's current cycle. Path param: `id` (integer). Read-only.
        Returns: bandwidth-data object from `qs_bandwidth_data` (totals, daily/hourly points, overage flag). Errors: 401 if unauthenticated. Note: handler does not gate on ownership or active status.
        Siblings: `postQsTrafficUsage` (same data, accessible via POST for filtered queries), `getVpsTrafficUsage`.
      operationId: getQsTrafficUsage
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get QuickServer Traffic usage
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get bandwidth usage for the QuickServer's current billing period
      tags:
      - QuickServers
    post:
      description: |-
        Functional duplicate of `getQsTrafficUsage` exposed under POST so clients can pass a filter body. Path param: `id` (integer). Body fields are accepted but the current handler ignores them and returns the full current-cycle dataset.
        Returns: same bandwidth-data object as `getQsTrafficUsage`. Errors: 401 if unauthenticated. No active-status or ownership gate.
        Siblings: `getQsTrafficUsage`, `postVpsTrafficUsage`.
      operationId: postQsTrafficUsage
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Submit QuickServer Traffic Usage
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Query QuickServer bandwidth usage via POST (filtered variant)
      tags:
      - QuickServers
  /qs/{id}/view_desktop:
    get:
      description: |-
        Returns the same rich payload the AdminLTE UI uses — service info, billing, available client_links, resource graphs. Heavier than `getQsInfo` and intended for desktop dashboards. Path param: `id` (integer).
        Returns: object with `serviceInfo`, `client_links`, etc. (admin-only fields stripped). Errors: 401 if unauthenticated. Note: handler does not gate on ownership/active status.
        Siblings: `getQsInfo` (lighter), `postQsViewDesktop` (mutate variant), `getVpsViewDesktop`.
      operationId: getQsViewDesktop
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get QuickServer View Desktop Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get the full QuickServer dashboard view payload (rich format)
      tags:
      - QuickServers
    post:
      description: |-
        Same handler as `getQsViewDesktop` but accessible via POST so callers can pass body fields alongside re-fetching the view. Path param: `id`. Body fields are accepted by the underlying View handler.
        Returns: refreshed dashboard object — `serviceInfo`, `client_links`, etc. Errors: 401 if unauthenticated.
        For structured updates prefer the dedicated endpoints (`postQsChangeHostname`, `postQsReverseDns`, `postQsSetupVnc`, etc.) which return queue IDs. Siblings: `getQsViewDesktop`, `postVpsViewDesktop`.
      operationId: postQsViewDesktop
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Submit QuickServer View Desktop Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Submit changes and re-fetch the QuickServer dashboard view payload
      tags:
      - QuickServers
  /scrub_ips:
    get:
      description: "Returns every Scrub IP service belonging to the authenticated\
        \ customer with status, protected IP, plan name, and recurring cost. Use this\
        \ for dashboards, picking a service ID for downstream calls (getScrubIpDetails,\
        \ enableScrub, createRule, getScrubIpLogs), or auditing which IPs are routed\
        \ through DDoS scrubbing. No path/query/body parameters; service ownership\
        \ is enforced via session account_id. Returns an array of {id, repeat_invoices_cost,\
        \ ip, status, services_name}; empty array if no scrub services. Errors: 401\
        \ unauthenticated. Caveat: only customer-owned services are visible. Siblings:\
        \ getScrubIpDetails, getOrderDetail, placeScrubOrder, cancelScrubIp."
      operationId: getScrubIpsList
      responses:
        "200":
          content:
            application/json:
              examples:
                ScrubIpsListExample:
                  value:
                  - scrub_ip_id: 123
                    repeat_invoices_cost: 5
                    scrub_ip_ip: 1.2.3.5
                    scrub_ip_status: active
                    services_name: Current IP + Scrub
                  - scrub_ip_id: 456
                    repeat_invoices_cost: 5
                    scrub_ip_ip: 4.3.2.1
                    scrub_ip_status: expired
                    services_name: Current IP + Scrub
              schema:
                items:
                  $ref: "#/components/schemas/ScrubIpsRowSchema"
                type: array
          description: Scrub Ips list
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: List all Scrub IP DDoS protection services on the authenticated account
      tags:
      - Scrub Ips
  /scrub_ips/{id}:
    delete:
      description: "Cancels the Scrub IP DDoS protection service. The protected IP\
        \ is removed from the scrubbing infrastructure and the recurring invoice is\
        \ closed; protection stops at end of the current billing cycle. Use only when\
        \ the customer no longer needs DDoS scrubbing for the IP. Path param: `id`\
        \ (integer, required) — service ID from getScrubIpsList. No request body.\
        \ Returns {success: true, text: 'Scrub Ips is canceled.'}. Errors: 401 unauthenticated;\
        \ 404/Invalid Service if id is not owned by the session account; 409 if the\
        \ service is not in a cancellable state. Caveat: leaves the underlying VPS/server\
        \ IP exposed to attacks once protection ends; contact billing for refund handling.\
        \ Siblings: getScrubIpDetails, disableScrub, getScrubIpInvoices."
      operationId: cancelScrubIp
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/cancelScrubIp_200_response"
          description: Request OK
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a Scrub IP service and stop its recurring DDoS billing
      tags:
      - Scrub Ips
    get:
      description: |
        Returns the full service-detail payload for one Scrub IP — used to render the dashboard or before mutating rules/filters. Includes `serviceInfo` (status, scrubbed IP, custid), `billingDetails` (cost, frequency), `client_links` (allowed self-service actions), and `filter_firewall` with the active firewall `rules`, geographic `geo_rules`, and traffic `filters`. Each rule/filter row carries its own `id` used by the delete endpoints. Sibling ops: `getScrubIpsList`, `enableScrub`, `disableScrub`, `createRule`, `scrubIpsDeleteRule`, `createGeoRule`, `scrubIpsDeleteGeoRule`, `createFilter`, `deleteFilter`, `getScrubIpInvoices`, `getScrubIpLogs`, `cancelScrubIp`.

        **Path:** `id` (integer, required) — service ID from `getScrubIpsList`.

        **Body / query:** None.

        **Returns:** object with `serviceInfo`, `billingDetails`, `client_links`, `filter_firewall` (`rules` / `geo_rules` / `filters`).

        **Auth:** Session/API key. Ownership enforced via `scrub_ips_custid`.

        **Errors:**
        - `401` — unauthenticated.
        - `Invalid Service` — `id` is not owned by the session account.

        **Caveat:** rule/filter IDs are regenerated after recreate — re-fetch before calling a delete endpoint.

        **Related calls:**
        - **Mutations:** `enableScrub`, `disableScrub`, `createRule`, `createGeoRule`, `createFilter`.
        - **Deletes:** `scrubIpsDeleteRule`, `scrubIpsDeleteGeoRule`, `deleteFilter`.
        - **Billing / activity:** `getScrubIpInvoices`, `getScrubIpLogs`.
        - **Cancel:** `cancelScrubIp`.
      operationId: getScrubIpDetails
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getScrubIpDetails_200_response"
          description: Scrub IP service details including firewall rules and filters.
          links:
            GetScrubIpInvoices:
              description: Retrieve billing invoices for this Scrub IP service.
              operationId: getScrubIpInvoices
              parameters:
                id: $request.path.id
            EnableScrub:
              description: Enable DDoS scrubbing protection on this IP.
              operationId: enableScrub
              parameters:
                id: $request.path.id
            DisableScrub:
              description: Disable DDoS scrubbing protection on this IP.
              operationId: disableScrub
              parameters:
                id: $request.path.id
            GetScrubIpLogs:
              description: View activity logs for this Scrub IP service.
              operationId: getScrubIpLogs
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get full Scrub IP service detail (rules + geo + filters)
      tags:
      - Scrub Ips
  /scrub_ips/{id}/invoices:
    get:
      description: "Returns the recurring and one-time invoices generated for the\
        \ Scrub IP service so the caller can verify billing status, present a payment\
        \ history, or initiate payment on an unpaid invoice. Use after placeScrubOrder\
        \ (to find the new invoice id) or before cancelScrubIp (to surface outstanding\
        \ balance). Path param: `id` (integer, required) — service ID from getScrubIpsList.\
        \ No body/query parameters. Returns ChargeInvoiceRows (array of invoice objects\
        \ with id, amount, status, due dates). Errors: 401 unauthenticated; empty\
        \ result if id is not owned by the session account. Caveat: paid invoices\
        \ remain in history; filter on status client-side. Siblings: getScrubIpDetails,\
        \ placeScrubOrder, cancelScrubIp."
      operationId: getScrubIpInvoices
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List recurring and one-time invoices billed for this Scrub IP service
      tags:
      - Scrub Ips
  /scrub_ips/filter_types:
    get:
      description: "Returns the catalog of scrub filter profiles (e.g. dns, http,\
        \ synproxy) currently enabled on the scrubbing platform, keyed by filter_name\
        \ with a humanized display `name` and `desc`. Call this to populate a dropdown\
        \ before invoking createFilter — the `filter_type` field on that endpoint\
        \ must be one of the keys returned here. Not service-scoped: no path/query/body\
        \ parameters and the same set applies to every Scrub IP. Returns {success:\
        \ true, filters: {<filter_name>: {name, desc}, ...}}. Errors: 401 unauthenticated.\
        \ Caveat: only filters with enabled=1 are returned; profile semantics are\
        \ platform-defined (synproxy uses different request shape internally). Siblings:\
        \ createFilter, deleteFilter, getScrubIpDetails."
      operationId: getScrubIpFilterTypes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScrubIpFilterTypes"
          description: Supported scrub filter types for building firewall rules.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List enabled traffic filter profiles available for createFilter
      tags:
      - Scrub Ips
  /scrub_ips/{id}/enable:
    get:
      description: "Routes the service's protected IP through the Wanguard scrubbing\
        \ infrastructure by creating a BGP announcement, so inbound traffic passes\
        \ through filtering before reaching the backend. Call after placeScrubOrder\
        \ activation, after disableScrub, or whenever the announcement was lost. Path\
        \ param: `id` (integer, required) — service ID from getScrubIpsList. No request\
        \ body (HTTP GET). Returns {success: true, text: 'Scrub is enabled on your\
        \ IP.'} on 201 from Wanguard, persisted into the service's `extra` column.\
        \ Errors: 400 Invalid Service if id is not owned by the session account; 401\
        \ unauthenticated; 500 if the upstream Wanguard call fails. Caveat: enabling\
        \ re-routes live traffic and can briefly disrupt active sessions. Siblings:\
        \ disableScrub, getScrubIpDetails, getScrubIpLogs."
      operationId: enableScrub
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/enableScrub_200_response"
          description: Request OK
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/enableScrub_500_response"
          description: Internal Server Error
      summary: Enable DDoS scrubbing (BGP announcement) on the service's protected
        IP
      tags:
      - Scrub Ips
  /scrub_ips/{id}/disable:
    get:
      description: "Withdraws the BGP announcement from Wanguard so the IP stops being\
        \ routed through scrubbing; traffic resumes flowing directly to the backend.\
        \ Use for maintenance windows or migration off scrub. Path param: `id` (integer,\
        \ required) — service ID from getScrubIpsList. No body (HTTP GET). The endpoint\
        \ reads the stored Wanguard `href` from the service's `extra` JSON to know\
        \ which announcement to delete; clears `extra` on success. Returns {success:\
        \ true, text: 'Scrub is disabled on your IP.'}. Errors: 400 Invalid Service\
        \ if id is not owned, or 'Scrub is not enabled in this service.' if there\
        \ is no active announcement; 401 unauthenticated; 500 if upstream delete fails.\
        \ Caveat: leaves the IP unprotected against DDoS until enableScrub is called.\
        \ Siblings: enableScrub, cancelScrubIp, getScrubIpDetails."
      operationId: disableScrub
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/disableScrub_200_response"
          description: Request OK
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/disableScrub_400_response"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/disableScrub_500_response"
          description: Internal Server Error
      summary: Disable DDoS scrubbing and remove the BGP announcement on the IP
      tags:
      - Scrub Ips
  /scrub_ips/{id}/create_rule:
    post:
      description: "Creates an XDP firewall rule on the scrubber for the service's\
        \ protected IP. Use to whitelist a known good source, block an abusive source,\
        \ or restrict a destination port. Path param: `id` (integer, required) — service\
        \ ID. Body (CreateFirewallRule): `source_ip` (IPv4, 0 = any), `source_port`\
        \ (int, 0 = any), `destination_port` (int, 0 = any), `protocol_id` (1 ICMP\
        \ or 2 TCP/UDP — must be 1 or 2), `xdp_action` (0 allow, 1 drop). Destination\
        \ IP is locked to the service IP server-side. Returns 201 {success: true}\
        \ when created. Errors: 400 with `errors[]` for invalid source_ip/protocol_id/xdp_action\
        \ or Invalid Service; 401 unauthenticated; 500 if upstream Scrub::firewallCreate\
        \ fails. Caveat: rules are stateless and may interact with active filters.\
        \ Siblings: scrubIpsDeleteRule, createGeoRule, createFilter."
      operationId: createRule
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFirewallRule"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createRule_201_response"
          description: Create firewall rule for scrub ip
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createRule_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createRule_500_response"
          description: Internal Server Error
      summary: "Add an L3/L4 firewall rule (allow/drop by IP, port, and protocol)"
      tags:
      - Scrub Ips
  /scrub_ips/{id}/delete_rule:
    post:
      description: "Removes a previously created L3/L4 firewall rule from the Scrub\
        \ IP service. The rule_id must come from the `filter_firewall.rules[].id`\
        \ array returned by getScrubIpDetails — the endpoint validates the id belongs\
        \ to this service before deleting. Path param: `id` (integer, required) —\
        \ Scrub IP service ID. Body (JSON): {`rule_id`: integer, required}. Returns\
        \ {success: true, text: 'Firewall Rule has been deleted.'}. Errors: 400 Invalid\
        \ Service, 'rule_id is required.' or 'Invalid rule id' (rule does not belong\
        \ to this service); 401 unauthenticated; 500 if upstream Scrub::firewallDelete\
        \ fails. Caveat: if the rule was the only protection against a specific source,\
        \ deleting it re-exposes the IP. Siblings: createRule, scrubIpsDeleteGeoRule,\
        \ deleteFilter, getScrubIpDetails."
      operationId: scrubIpsDeleteRule
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Delete_Firewall_Rule"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_200_response"
          description: Delete firewall rule for scrub ip
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_500_response"
          description: Internal Server Error
      summary: Delete an L3/L4 firewall rule by rule_id from getScrubIpDetails
      tags:
      - Scrub Ips
  /scrub_ips/{id}/create_geo_rule:
    post:
      description: "Creates a geo-based XDP rule on the scrubber for the service's\
        \ protected IP. Use to block traffic from specific countries or ASNs (botnet\
        \ source regions) or to allow only known regions. Path param: `id` (integer,\
        \ required) — service ID. Body (CreateGeoFirewallRule): `country_code` (int,\
        \ country numeric ID) OR `asn` (int) — at least one is required, `destination_port`\
        \ (int, defaults 80), `xdp_action` (0 allow, 1 drop, defaults 1). Destination\
        \ IP is locked to the service IP server-side. Returns 201 {success: true}\
        \ when created. Errors: 400 errors[] 'Country or Asn is required.' or Invalid\
        \ Service; 401 unauthenticated; 500 if upstream Scrub::geoFirewallCreate fails.\
        \ Caveat: country_code is an internal numeric ID, not ISO-3166. Siblings:\
        \ scrubIpsDeleteGeoRule, createRule, createFilter."
      operationId: createGeoRule
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGeoFirewallRule"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createRule_201_response"
          description: Create firewall rule for scrub ip
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createGeoRule_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createRule_500_response"
          description: Internal Server Error
      summary: Add a geographic firewall rule (block/allow by country code or ASN)
      tags:
      - Scrub Ips
  /scrub_ips/{id}/delete_geo_rule:
    post:
      description: "Removes a previously created geographic firewall rule from the\
        \ Scrub IP service. The rule_id must come from the `filter_firewall.geo_rules[].id`\
        \ array returned by getScrubIpDetails — the endpoint validates the id belongs\
        \ to this service before deleting. Path param: `id` (integer, required) —\
        \ Scrub IP service ID. Body (JSON): {`rule_id`: integer, required}. Returns\
        \ {success: true, text: 'Firewall Rule has been deleted.'}. Errors: 400 Invalid\
        \ Service, 'Rule Id is required.' or 'Invalid rule id' (rule does not belong\
        \ to this service); 401 unauthenticated; 500 if upstream Scrub::geoFirewallDelete\
        \ fails. Caveat: removing a country/ASN block re-admits that traffic. Siblings:\
        \ createGeoRule, scrubIpsDeleteRule, deleteFilter, getScrubIpDetails."
      operationId: scrubIpsDeleteGeoRule
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Delete_Geo_Firewall_Rule"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_200_response"
          description: Delete firewall rule for scrub ip
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/scrubIpsDeleteRule_500_response"
          description: Internal Server Error
      summary: Delete a geo firewall rule by rule_id from getScrubIpDetails
      tags:
      - Scrub Ips
  /scrub_ips/{id}/create_filter:
    post:
      description: "Attaches a named scrubbing profile to a destination port on the\
        \ protected IP, applying protocol-aware mitigation (DNS amplification protection,\
        \ HTTP rate limiting, synproxy SYN-cookies). Call getScrubIpFilterTypes first\
        \ to list valid `filter_type` values. Path param: `id` (integer, required)\
        \ — service ID. Body (CreateFilter): `filter_type` (string, required, one\
        \ of getScrubIpFilterTypes keys), `port` (int, required, >= 0). Destination\
        \ IP is locked to the service IP server-side; synproxy uses a different shape\
        \ internally. Returns 201 {success: true, text: 'New filter has been created.'}.\
        \ Errors: 400 'Filter type is empty/invalid', 'Port is invalid', or Invalid\
        \ Service; 401 unauthenticated; 500 if upstream Scrub::filterCreate fails.\
        \ Siblings: deleteFilter, getScrubIpFilterTypes, createRule."
      operationId: createFilter
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFilter"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createFilter_201_response"
          description: Request OK
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createFilter_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/createFilter_500_response"
          description: Internal Server Error
      summary: Apply a predefined scrubbing filter (DNS/HTTP/synproxy) to a port
      tags:
      - Scrub Ips
  /scrub_ips/{id}/delete_filter:
    post:
      description: |
        Removes a previously attached scrubbing profile from the protected IP. Identification is by composite key, not `rule_id` — pass the same `filter_type` and `port` that were used in `createFilter`. The endpoint splits `filter_type` on `_` to dispatch to the correct delete shape (synproxy vs generic). Sibling ops: `createFilter`, `getScrubIpFilterTypes`.

        **Path:** `id` (integer, required) — Scrub IP service ID.

        **Body fields:**
        - `filter_type` (string, required) — must match an enabled type from `getScrubIpFilterTypes`.
        - `port` (integer, required) — must be `> 0`.

        **Returns:** `{ success: true, text: 'Filter is deleted.' }`.

        **Errors:**
        - `400` — `'Filter is required.'` / `'Port is required.'` / `'Invalid filter'` / `Invalid Service`.
        - `401` — unauthenticated.
        - `500` — upstream `Scrub::filterDelete` failed.

        **Caveat:** the port loses its protocol-specific scrubbing protection until `createFilter` is called again with the same composite key.
      operationId: deleteFilter
      parameters:
      - description: ScrubIp ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFilter"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteFilter_200_response"
          description: Delete filter for scrub ip
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteFilter_400_response"
          description: Bad Request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/deleteFilter_500_response"
          description: Internal Server Error
      summary: Remove a scrubbing filter by matching filter_type and port
      tags:
      - Scrub Ips
  /scrub_ips/order:
    get:
      description: "Returns the data needed to render a new-order form: `packageCosts`\
        \ (default services_id and recurring price in customer currency with symbol),\
        \ `serviceTypes` (each buyable plan with services_id, services_name, services_cost,\
        \ services_module), and `ips` (the customer's existing VPS/server/floating\
        \ IPs eligible to be put behind a scrubber, each with service_id, service_module,\
        \ service_hostname). Use as a precursor to putScrubIps (validate) or placeScrubOrder\
        \ (commit). No path/query/body parameters. Returns object. Errors: 401 unauthenticated.\
        \ Caveat: ips list is filtered to the session account; pricing is converted\
        \ to the customer's currency. Siblings: putScrubIps, placeScrubOrder, getScrubIpsList."
      operationId: getOrderDetail
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getOrderDetail_200_response"
          description: Order details
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get plans, pricing, and eligible IPs for a new Scrub IP order"
      tags:
      - Scrub Ips
    post:
      description: "Commits the order: re-runs validate_buy_scrub_ip then place_buy_scrub_ip\
        \ which creates the service row, repeat_invoice, and a one-time invoice for\
        \ the prorated charge. Use putScrubIps first to surface errors without billing.\
        \ No path parameters. Body (ScrubIpPlaceOrder): `serviceType` (services_id),\
        \ `ip` (eligible IP from getOrderDetail). Returns 201 {success: true, text:\
        \ 'ScrubIp order is placed.', order_details: {total_cost, service_id, invoice_id,\
        \ invoice_description, cj_params}}. Errors: 400 {success: false, text: 'Unable\
        \ to place order.', errors: []} on validation; 401 unauthenticated; 422 on\
        \ invalid serviceType/ip; 409 if the IP is already protected. Caveat: invoice\
        \ is unpaid at creation — pay via Pay endpoints to activate. Siblings: putScrubIps,\
        \ getOrderDetail, enableScrub, getScrubIpInvoices."
      operationId: placeScrubOrder
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScrubIpPlaceOrder"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/placeScrubOrder_201_response"
          description: Request OK
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Place a new Scrub IP DDoS protection order and generate an invoice
      tags:
      - Scrub Ips
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order a DDoS-mitigated scrub IP. Returns invoice IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: "Dry-runs a Scrub IP purchase via validate_buy_scrub_ip and returns\
        \ whether the order would succeed plus the resolved pricing — without creating\
        \ an invoice. Use to render a real-time price/error panel as the user picks\
        \ options. No path parameters. Body (ScrubIpPlaceOrder): `serviceType` (services_id\
        \ from getOrderDetail.serviceTypes), `ip` (one of getOrderDetail.ips), optional\
        \ `coupon`. Returns {continue: bool, errors: [], serviceType, serviceCost,\
        \ originalCost, repeatServiceCost}. Errors: 401 unauthenticated; validation\
        \ failures appear in `errors`, not as HTTP 4xx. Caveat: idempotent — call\
        \ as often as needed; 422 on invalid coupon surfaces in the errors array.\
        \ Siblings: getOrderDetail, placeScrubOrder, getScrubIpsList."
      operationId: putScrubIps
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScrubIpPlaceOrder"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/putScrubIps_200_response"
          description: Scrub IP order validation result.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate a Scrub IP order and return effective pricing without billing
      tags:
      - Scrub Ips
  /scrub_ips/{id}/logs:
    get:
      description: "Pulls scrubbing telemetry directly from the SCRUBLOGS clickhouse-style\
        \ backend: timestamp, source IP, target IP, target port, protocol (ICMP/IGMP/TCP/UDP/etc.),\
        \ byte_count, action (Allow/Drop/Challenge), and the matching filter label.\
        \ Use for incident analysis, validating new firewall rules, or proving a DDoS\
        \ attack hit the scrubber. Path param: `id` (string, required) — service ID.\
        \ No body/query parameters. Timestamps are converted to the customer's timezone.\
        \ Returns array of log rows (ScrubIpsLogRowSchema), most recent first, capped\
        \ at 50000. Errors: 401 unauthenticated; returns false if id is not owned\
        \ or upstream returns no data — not a 404. Caveat: large response; logs are\
        \ not real-time and source IPs are reverse-byte-ordered. Siblings: getScrubIpDetails,\
        \ enableScrub, createRule."
      operationId: getScrubIpLogs
      parameters:
      - description: Scrub Order ID
        examples:
          ScrubOrderIdExample:
            value: '413232  '
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                ScrubIpsListExample:
                  value:
                  - date: "Feb 24, 2026 @ 05:39 pm"
                    filter: TCP FW Drop
                    blocked_ip: 49.36.168.21
                    target_ip: 173.214.163.18
                    target_port: 2083
                    protocol: TCP
                    byte_count: 1514
                    xdp_action: Allow
                  - date: "Feb 24, 2026 @ 05:39 pm"
                    filter: Generic UDP
                    blocked_ip: 67.215.86.57
                    target_ip: 173.214.163.18
                    target_port: 53
                    protocol: UDP
                    byte_count: 84
                    xdp_action: Allow
              schema:
                items:
                  $ref: "#/components/schemas/ScrubIpsLogRowSchema"
                type: array
          description: Scrub Ips logs
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get last 50000 packet/event log entries for the protected IP
      tags:
      - Scrub Ips
  /servers:
    get:
      description: |-
        Use to enumerate physical bare-metal dedicated servers on the calling account. No params, no body. Filters `servers` by session `account_id`.
        Returns: array of `{ server_id, account_lid, server_hostname, server_status }`. Use `server_id` with `getServerInfo` for full hardware/network/IPMI details, `getServerInvoices` for billing, or `serverIpmiPowerGet` for chassis power state. Errors: 401 if not authenticated; empty array if account owns no servers.
        Sibling ops: `getServerInfo` (details), `getVpsList` (virtual instead of physical hardware), `getMPServers` (purchasable inventory, not owned). For IPMI status across many servers in one call, prefer `serverBulkIpmiPowerGet`.
      operationId: getServerList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/ServerRow"
                type: array
          description: The listing of `Servers` services on your account.
          links:
            GetServerDetails:
              description: Use the `server_id` from any item in the response array
                to fetch full server details.
              operationId: getServerInfo
              parameters:
                id: $response.body#/0/server_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all dedicated servers owned by the authenticated customer
      tags:
      - Servers
  /servers/order:
    get:
      description: |-
        Use before placing a fully custom (non-Rapid-Deploy) dedicated server order to discover available CPUs, drives, memory tiers, OS images, control panels, RAID levels, bandwidth packages, IP blocks, and regions with monthly prices. No params, no body.
        Returns: object with `config_li` keyed by category (`cpu_li`, `hd_li`, `memory_li`, `bandwidth_li`, `ips_li`, `os_li`, `cp_li`, `raid_li`) plus `regions`. Use returned IDs as POST values for `addServer`. Note `hd_li` and `memory_li` are nested by `cpu` id — the chosen CPU constrains valid drive/memory options. Errors: 401 if not authenticated.
        Sibling ops: `addServer` (commits the order), `buyItNowServerOrder` (pre-built marketplace alternative), `getMPServers` (browse marketplace).
      operationId: getNewServer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerOrder"
          description: Server Ordering details
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get custom dedicated server ordering options, regions, and pricing"
      tags:
      - Servers
    post:
      description: |-
        Submits a fully custom dedicated server order. Creates a `pending` `servers` row, a `Repeat_Invoice`, and the first invoice, then emails customer + admin. Caveat: real billable order — confirm with the user first.
        Body (form fields): `cpu` (id from `cpu_li`), `hd[]` (array of drive ids), `memory`, `bandwidth`, `ips`, `os`, `cp`, `raid` (ids from `getNewServer`), `region` (region_id), `servername` (valid hostname), `rootpass`, `tos` (must be true), optional `comment`. `account.server_order_discount` (if set) applies.
        Returns: `{ text:'Order Completed', invoice, order }`. Errors: 422 'Missing/Invalid <field>'; 401 unauth.
        Sibling ops: `getNewServer` (options), `placeBuyNowServer` (pre-built path), `getServerInfo` (view new order), `getServerInvoices`.
      operationId: addServer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServerOrderPostRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/addServer_200_response"
          description: Server order placed successfully.
          links:
            GetServerInvoice:
              description: Use the invoice ID to view invoice details or proceed to
                payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/invoice
            ViewNewServer:
              description: Use the order ID to view the newly created server.
              operationId: getServerInfo
              parameters:
                id: $response.body#/order
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a custom dedicated server order, creating a real billable invoice"
      tags:
      - Servers
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order a dedicated or rapid-deploy server. Returns invoice IDs
          for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
  /servers/{id}:
    delete:
      description: |-
        Submits a cancellation request for a dedicated server. The server is deprovisioned and recurring billing stops at the end of the current billing cycle (not an immediate refund). Path param: `id` (integer server_id, from `getServerList`). No body.
        Caveat: billing-affecting action — always confirm with the user. Hardware-attached data may be wiped on deprovisioning. Returns: `{ success:bool, text:'Servers is canceled.' }`. Errors: 404 if `id` not owned by caller; 409 if already cancelled or non-active; 401 unauth.
        Sibling ops: `getServerInfo` (current status), `getServerInvoices` (outstanding charges), VPS counterpart `VPSCancel`. To re-order after cancel use `addServer` or `placeBuyNowServer`.
      operationId: serversCancel
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/serversCancel_200_response"
          description: Servers Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a dedicated server service at the end of the current billing
        cycle
      tags:
      - Servers
    get:
      description: |-
        Use to fetch complete configuration for one dedicated server — hardware, network/VLAN/IP layout, asset assignments, location, status, billing references, and client action links. Path param: `id` (integer server_id, from `getServerList`). No body.
        Returns: `ViewServer::getDetails()` shape: `serviceInfo`, `networkInfo` (vlans + assets, with `ipmi_admin_username`/`ipmi_admin_password` and admin lease creds REDACTED for client safety), normalized `client_links`, `serviceType`. `admin_links`/raw `settings`/`csrf` stripped. Errors: 404 not owned; 401 unauth.
        Sibling ops: `getServerInvoices`, `serverIpmiLiveGet`, `serverIpmiPowerGet` (single — prefer `serverBulkIpmiPowerGet` for many), `getServerReverseDns`, `getServersWelcomeEmail`, `serversCancel`.
      operationId: getServerInfo
      parameters:
      - description: Server ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Server"
          description: Server details
          links:
            GetServerInvoices:
              description: Retrieve billing invoices for this server.
              operationId: getServerInvoices
              parameters:
                id: $request.path.id
            ServerIpmiLive:
              description: View IPMI live connection information for this server.
              operationId: serverIpmiLiveGet
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get full hardware, network, and lifecycle details for a dedicated\
        \ server"
      tags:
      - Servers
    post:
      description: |-
        Use to modify metadata on an existing dedicated server order. Path param: `id` (integer server_id). Currently this method shares the same handler as `getServerInfo` (`View::go()`) — no dedicated update fields are processed; treat it as deprecated/no-op pending field-specific endpoints. For hostname, password, or rDNS changes use the dedicated ops below.
        Returns: same payload shape as `getServerInfo`. Errors: 404 if `id` not owned by caller; 401 unauth.
        Sibling ops: prefer `postServerReverseDns` (rDNS), `serverIpmiPowerPost` (power), `serverIpmiLivePost` (IPMI access), `serversCancel` (cancel). For new orders use `addServer` or `placeBuyNowServer`. View-only: `getServerInfo`.
      operationId: updateServerInfo
      parameters:
      - description: Server ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update settings on a dedicated server order (shares handler with view)
      tags:
      - Servers
  /servers/{id}/invoices:
    get:
      description: |-
        Use to retrieve the invoice history for a single dedicated server — e.g. before a cancel, refund, or to show outstanding balances. Path param: `id` (integer server_id from `getServerList`). No body. Inherits from `MyAdmin\Api\Billing\InvoicesList` with module=servers.
        Returns: `ChargeInvoiceRows` array — invoice rows with id, date, amount, status, currency, line items. Errors: 404 if `id` not owned by the caller; 401 unauth.
        Sibling ops: `getServerInfo` (current service state), `serversCancel` (cancel), `getBillingInvoice` (single invoice by invoice id), `getVpsInvoices`/`getDomainInvoices` for other modules, `getServersWelcomeEmail` to resend setup info.
      operationId: getServerInvoices
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List billing invoices (charges + payments) tied to one dedicated server
      tags:
      - Servers
  /servers/{id}/ipmi_live:
    get:
      description: |
        Reads the active IPMI Live session for a dedicated server — the temporary whitelisted public IP, the customer-side IPMI gateway URL, and the IPMI client (read-only) credentials so the customer can open the KVM/console. Looks up the asset's IPMI IP, the location's IPMI group, and any active `ipmi_ips` lease (3-hour TTL). Sibling ops: `serverIpmiLivePost` (allocate whitelist slot), `serverIpmiPowerGet` / `serverIpmiPowerPost` (chassis power).

        **Path:** `id` (integer, required) — server_id from `getServerList`.

        **Body / query:** None. Optionally pass `asset` (asset_id) to target a specific asset; default is first asset.

        **Returns:** when an active lease exists `{ text (html), public_ip, allowed_ip, client_username, client_password }`. When no lease yet: `{ text: 'Setup not yet completed' }` — then call `serverIpmiLivePost` to allocate a slot.

        **Auth:** Session/API key. Ownership enforced via `server_custid`.

        **Errors:**
        - `404` — `id` not owned, or `asset` not on this server.
        - `409` — service not `active`.
        - `200` with error text `'No IPMI IP Set'` / `'Invalid IPMI IP'` / `'Live IPMI not Available for this location.'` when the asset/location is not configured for IPMI Live.

        **Caveat:** returns `client_password` — never log/echo verbatim.

        **Related calls:**
        - **Allocate:** `serverIpmiLivePost`.
        - **Chassis power:** `serverIpmiPowerGet`, `serverIpmiPowerPost`.
      operationId: serverIpmiLiveGet
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerIpmiLiveInfo"
          description: Response from the Servers IPMI Live information request.
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Read current IPMI Live whitelist + KVM gateway URL for a dedicated
        server
      tags:
      - Servers
    post:
      description: |
        Allocates / refreshes an IPMI Live whitelist slot so the customer's specified IP can reach the BMC's KVM/console for 3 hours. Picks a free `ipmi_ips` row for the location's `ipmi_group`, refreshes the lease if the same IP is already allocated, otherwise pushes the new whitelist via `ipmi_live_setup()`. Sibling ops: `serverIpmiLiveGet` (read current lease), `serverIpmiPowerPost` (DESTRUCTIVE — chassis power).

        **Path:** `id` (integer, required) — server_id.

        **Body fields:**
        - `ip` (string, required) — public IPv4 to whitelist.
        - `asset` (integer, optional) — asset_id; defaults to first asset on the server.

        **Returns:** `{ text (html), public_ip, allowed_ip, client_username, client_password }` for KVM login.

        **Auth:** Session/API key. Ownership enforced via `server_custid`.

        **Errors:**
        - `404` — `id` not owned, or `asset` not on this server.
        - `409` — service not `active`.
        - `200` with error text — `'An Invalid IP was passed.'`, `'No Live IPs are currently free for use with the IPMI Gateway. Please wait <duration> for the next IP to free up.'`, `'There was an error communicating with the IPMI Management server'`, `'No IPMI IP Set'` / `'Invalid IPMI IP'` / `'Live IPMI not Available for this location.'`.

        **Caveat:** returns IPMI client password — handle securely; whitelist exposes the BMC briefly.

        **Related calls:**
        - **Read current lease:** `serverIpmiLiveGet`.
        - **Power control:** `serverIpmiPowerPost`.
      operationId: serverIpmiLivePost
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ServerIpmiLiveRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/ServerIpmiLiveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerIpmiLiveInfo"
          description: Response from the Servers IPMI Live information request.
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Whitelist an IP for IPMI Live KVM gateway access (3-hour lease)
      tags:
      - Servers
  /servers/{id}/welcome_email:
    get:
      description: |-
        Use when the customer asks for the original setup/login info to be re-sent (root password, IPs, control-panel URL). Path param: `id` (integer server_id, must be `active`). No body. Invokes `server_welcome_email($id)` which re-sends the welcome message to the account's email.
        Returns: `{ text:'Welcome Email has been resent.' }`. Errors: 404 if `id` not owned by caller; 409 if service not active (cancelled/pending/suspended); 401 unauth. Caveat: re-sending is rate-sensitive; do not call repeatedly in a loop. The email may contain root credentials — confirm intent before triggering.
        Sibling ops: `getServerInfo` (status check), `getServerInvoices`, `getVpsWelcomeEmail` for VPS, `getDomainsWelcomeEmail` for domains.
      operationId: getServersWelcomeEmail
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the dedicated server welcome email with setup credentials
      tags:
      - Servers
  /signup:
    post:
      description: |
        First step of the signup flow before adding payment or services. Public — no auth required. The account is created in `pending` state and moved to `active` once the email-confirmation code is verified; an `account.activated` event then fires (welcome email + admin notification). MaxMind GeoIP populates `country` from the client IP. Sibling ops: `submitLogin`, `getCaptcha`, `getLoginInfo`, `addBillingPrepay`, plus the `add*` service ops to follow up after signup.

        **Body fields** (JSON or form):
        - `login` (string, required) — email; must be valid and not an alias like `+tag` or dotted gmail.
        - `passwd` (string, required) — 4–64 chars.
        - `tos` (truthy, required) — `yes` / `true` / `1`.
        - `captcha` (string, required) — answer to the phrase from `getCaptcha` or `getLoginInfo` (server reads the phrase from `$_SESSION['captchaSignup']`).
        - `email_confirmation` (string, conditional) — 8-char code emailed on the first attempt; server returns `400 { field: 'email_confirmation' }` until provided.
        - `remember` (boolean / `'true'` / `'yes'` / `'1'`, optional) — 256-day cookie.

        **Returns:** `{ sessionId, account_id, account_lid, ima }`.

        **Errors:**
        - `400` — missing or invalid `login` / `passwd` / `tos` / `captcha` / `email_confirmation`; blocked-domain or aliased-email; account already exists.
        - `402` per `LoginResponseError` — signup gate misconfigured upstream.

        **Related calls:**
        - **Prerequisite:** `getCaptcha` or `getLoginInfo`.
        - **After signup:** `submitLogin`, `addBillingPrepay`, plus any `add*` order op.
      operationId: submitSignup
      requestBody:
        content:
          application/json:
            examples:
              LoginSubmissionExampleExample:
                value:
                  login: user@domain.com
                  passwd: mypassword
                  remember: "true"
                  g-recaptcha-response:
                    __v_isShallow: false
                    dep:
                      w: 0
                      "n": 0
                    __v_isRef: true
                    _rawValue: zzzzz
                    _value: zzzzz
            schema:
              $ref: "#/components/schemas/LoginSubmissionExample"
      responses:
        "200":
          description: Account created successfully.
        "402":
          content:
            application/json:
              examples:
                MissingTwoFactorCode:
                  value:
                    message: missing 2fa
                    field: tfa
                InvalidTwoFactorCode:
                  value:
                    message: invalid 2fa
                    field: tfa
                MissingCaptcha:
                  value:
                    message: missing captcha
                    field: captcha
                TooManyVerificationAttempts:
                  value:
                    message: "max tries treached on code, sending new one, submit\
                      \ again"
                    field: verify
              schema:
                $ref: "#/components/schemas/LoginErrorResponse"
          description: Error response to a login request.
      security:
      - {}
      summary: Create a new customer account (email + password + captcha + ToS)
      tags:
      - Public
  /ssl:
    get:
      description: |-
        Use to enumerate every SSL certificate (DV/OV/EV) the current customer owns before drilling into a specific cert. Returns an array of SslRow objects with id, hostname, services_name (package), status (pending/active/expired/canceled), and company. No query parameters - results are auto-scoped to the session account_id. Empty array if customer has no certs. Returns 401 if unauthenticated. Pair the returned id with getSslInfo for full details, getSslInvoices for billing, getSslWelcomeEmail to resend credentials, sslCancel to terminate, or addSsl to order a new cert. Status values may be stale relative to CA - issuance/validation can take minutes to hours after order.

        Sibling ops: `getSslInfo`, `getNewSsl` (catalog), `addSsl` (order new cert).
      operationId: getSslList
      responses:
        "200":
          description: The listing of `SSL` services on your account.
          links:
            GetSslDetails:
              description: Use the SSL certificate ID from any item in the response
                array to fetch full certificate details.
              operationId: getSslInfo
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: List all SSL certificates on the authenticated customer account with
        status and hostname
      tags:
      - SSL-Certificates
  /ssl/order:
    get:
      description: |-
        Use before addSsl to discover which DV/OV/EV certificate types and validation tiers are buyable, plus their costs. Returns object with packageCosts (services_id keyed map of float costs) and serviceTypes (full list of SSL product offerings from the get_service_types event). No parameters required - prices are in the customer's currency. Returns 401 if unauthenticated. Show these to the customer to pick a service_type, then call putSsl to dry-run validation (hostname, CSR, coupon) without charging, then addSsl to commit. Costs do not include taxes or applied coupons — putSsl returns the actual computed price with discounts.

        Sibling ops: `putSsl` (validate), `addSsl` (commit), `getSslList` (existing certs), `getSslInfo` (per-cert).
      operationId: getNewSsl
      responses:
        "200":
          content:
            application/json:
              schema:
                description: SSL certificate types and pricing.
                type: object
          description: Available SSL certificate types and pricing for ordering.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get available SSL certificate packages and pricing for placing a new
        order
      tags:
      - SSL-Certificates
    post:
      description: |-
        [DESTRUCTIVE] Use after putSsl returns continue=true to commit the SSL order. Body (form): frequency (default 12 months), service_type, hostname, csr, coupon_code, plus per-type vars/extra. Re-runs validate_buy_ssl then calls place_buy_ssl which creates the service row, generates invoice (iid/iids/real_iids), and returns serviceId, serviceCost, invoice_description. CA validation is async - issuance takes minutes to hours and may require DNS or email validation post-order. If validation fails, returns continue=false with errors and no charge. Returns 401 unauthenticated, 422 invalid input. Caveat: cert is not active until invoice paid AND CA validation completes. Poll status via getSslInfo; resend instructions via getSslWelcomeEmail.

        Sibling ops: `getNewSsl` (catalog), `putSsl` (validate), `getSslInfo` (poll), `getSslInvoices`, `initiatePayment` (settle invoice), `getSslWelcomeEmail`, `sslCancel`.
      operationId: addSsl
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SslOrderRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Place a new SSL certificate order - creates invoice and queues issuance
      tags:
      - SSL-Certificates
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order an SSL certificate (DV/OV/EV). Returns invoice IDs for
          checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |-
        Use after getNewSsl and before addSsl to verify hostname, CSR, service_type, frequency, and coupon_code are acceptable without creating an invoice or charging the customer. Body params (form): frequency (months, default 12), service_type, hostname, csr, coupon_code, plus extra/vars per cert type. Returns continue (bool), errors (array), serviceType, serviceCost (after coupon), originalCost, hostname, couponCode. If continue=false the errors array explains what to fix - typical issues are invalid hostname/CSR mismatch, expired coupon, or unsupported service_type. Returns 401 if unauthenticated, 422 on validation failure semantics. No state is mutated. Always run this before addSsl to prevent failed charges.
        Sibling ops: `getNewSsl` (catalog), `addSsl` (commit).
      operationId: putSsl
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SslOrderRequest"
        required: true
      responses:
        "200":
          description: Validate SSL Order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Validate an SSL certificate order without charging - dry-run before
        addSsl
      tags:
      - SSL-Certificates
  /ssl/{id}:
    delete:
      description: |-
        [DESTRUCTIVE] Use to cancel a customer-owned SSL cert. Path param id (integer, required) is the ssl_id. Cancellation marks the service for non-renewal - the cert stays valid until its current paid period ends, after which auto-billing stops. The CA-issued certificate itself is NOT revoked by this call (file a separate revocation request if needed). Returns SSLCancelResponse with success bool and text. Returns 401 unauthenticated, 404 if id not owned by session customer, error if the cancel_service hook fails. Caveat: irreversible at the billing level - re-enabling requires a new addSsl order. Verify the right cert with getSslInfo and confirm no unpaid charges via getSslInvoices first.

        Sibling ops: `getSslInfo` (verify cert), `getSslInvoices` (check unpaid), `addSsl` (re-order).
      operationId: sslCancel
      parameters:
      - description: SSL Cert ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/sslCancel_200_response"
          description: SSL Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel an SSL certificate service - stops renewals at end of billing
        cycle
      tags:
      - SSL-Certificates
    get:
      description: |-
        Use to inspect a single SSL cert after locating its id via getSslList. Path param id (integer, required) is the ssl_id; cross-account ids return 404 (get_service enforces ownership). Returns the ViewSSL detail payload: hostname, service_type, status, expiration, company, plus client_links (rewrite/reissue/install actions available to the customer). admin_links, settings, csrf are stripped from client responses. Returns 401 unauthenticated, 404 if id not owned by the session customer. Reissue/rekey/install actions surfaced in client_links are time-sensitive and may require fresh DNS validation. Pair with getSslInvoices for billing history, getSslWelcomeEmail to resend, sslCancel to terminate, updateSslInfo to modify settings.

        Sibling ops: `updateSslInfo`, `getSslInvoices`, `getSslWelcomeEmail`, `sslCancel`, `getSslList`.
      operationId: getSslInfo
      parameters:
      - description: SSL certificate ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                description: SSL certificate service details.
                type: object
          description: Detailed SSL certificate information.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get full details for one SSL certificate by id - status, expiration,\
        \ links"
      tags:
      - SSL-Certificates
    post:
      description: |-
        Use to modify mutable fields on a customer-owned SSL cert (e.g. contact info, renewal preferences, hostname or CSR data depending on cert state and CA rules). Path param id (string/int, required) is the ssl_id. Body params depend on the cert package and which fields the underlying service supports - inspect getSslInfo client_links first to see which actions are exposed. Returns SuccessTextResponse on success. Returns 401 unauthenticated, 404 if id not owned, 409 if cert state forbids the change (e.g. canceled or pending CA validation), 422 on invalid field values. Caveat: changes that affect the certificate identity (hostname, CSR) typically trigger a reissue with the CA which is time-sensitive and may require new DNS or email validation.

        Sibling ops: `getSslInfo` (read), `sslCancel` (terminate), `getSslWelcomeEmail`.
      operationId: updateSslInfo
      parameters:
      - description: SSL certificate ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update mutable settings on an existing SSL certificate order by id
      tags:
      - SSL-Certificates
  /ssl/{id}/invoices:
    get:
      description: |-
        Use to retrieve the full invoice history for a single SSL cert - initial order, renewals, and any addon charges. Path param id (integer, required) is the ssl_id; ownership is enforced via get_service so cross-account ids return an Invalid Service error. Returns ChargeInvoiceRows: success bool plus invoices array of charge/invoice rows with iid, date, cost, status (paid/unpaid/refunded), and description. Returns 401 unauthenticated, 400 if the id resolves to no service. Useful for auditing renewals before sslCancel, reconciling payment failures, or showing the customer their billing history.

        Sibling ops: `getSslInfo`, `sslCancel`, `getSslWelcomeEmail`, `getBillingInvoice` (per-invoice detail), `initiatePayment` (settle unpaid).
      operationId: getSslInvoices
      parameters:
      - description: SSL Cert ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices and charges tied to one SSL certificate by
        id
      tags:
      - SSL-Certificates
  /ssl/{id}/welcome_email:
    get:
      description: |-
        Use when a customer lost the original welcome email containing CSR submission steps, validation links, or installation guidance for an active SSL cert. Path param id (integer, required) is the ssl_id. Triggers the module's ssl_welcome_email function to re-send to the account's email on file. Returns SuccessTextResponse: text='Welcome Email has been resent.' Returns 401 unauthenticated, 404 if id not found or not owned by session customer ('Invalid Service Passed'), 409 if cert status is not 'active' (pending/canceled/expired certs do not have a welcome email to resend). Caveat: cannot change the destination email - update the account profile first if the customer's address has changed.

        Sibling ops: `getSslInfo` (verify status), `sslCancel` (terminate), `updateAccountInfo` (change email first).
      operationId: getSslWelcomeEmail
      parameters:
      - description: SSL Cert ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the SSL welcome email with cert credentials and install instructions
      tags:
      - SSL-Certificates
  /tickets:
    get:
      description: |-
        Use to browse the customer's helpdesk tickets, paginated, with optional status and recency filters. Returns tickets where email matches the session account_lid. Query params: page (int, default 1, 50 per page), period (string: '30', '90', '365', '1825', or 'all' days back; default '30'), view (string: 'Open', 'Closed', 'On Hold', 'In Progress'; omit for all). Body: none. Returns: object with tickets[], total, pages, currentPage, st_count[] (counts grouped by status: Open/On Hold/Closed), selected_period, view. Errors: 401 unauthorized session. Note ticketstatusid mapping (Open=4, On Hold=5, Closed=6, In Progress=7). To search by subject/email/mask use postTicketsList. To open a ticket detail use getTicketInfo with the returned id. To create a new ticket see addNewTicket.

        Sibling ops: `getTicketInfo` (detail), `postTicketsList` (search), `addNewTicket` (open new).
      operationId: getTicketsList
      parameters:
      - description: Page number for paginated results.
        explode: true
        in: query
        name: page
        required: false
        schema:
          default: 1
          type: integer
        style: form
      - description: How far back to show tickets from. Value is in days.
        explode: true
        in: query
        name: period
        required: false
        schema:
          default: "30"
          enum:
          - "30"
          - "90"
          - "365"
          - "1825"
          - all
          type: string
        style: form
      - description: "The status of tickets to view. Possible values are Open, Closed,\
          \ On Hold, and In Progress.  If not specified it will show all types."
        explode: true
        in: query
        name: view
        required: false
        schema:
          enum:
          - Open
          - Closed
          - On Hold
          - In Progress
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              examples:
                TicketsExample:
                  value:
                    ima: client
                    custid: "223513"
                    view: Open
                    currentPage: 1
                    limit: 50
                    sortcol: 6
                    sortdir: 1
                    rowsOffset: 0
                    tickets: []
                    pages: 7
                    rowsTotal: 311
                    inboxCount: 311
                    countArray:
                      Open: 3
                      On Hold: 3
                      Closed: 305
                    viewText: Inbox
              schema:
                $ref: "#/components/schemas/Tickets"
          description: The listing of support tickets.
          links:
            GetTicketDetails:
              description: Use a ticket ID from the response to fetch full ticket
                details and reply history.
              operationId: getTicketInfo
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List the authenticated account's support tickets with status and date
        filters
      tags:
      - Tickets
    post:
      description: |-
        Use when the user supplies a search term (subject keyword, email substring, or full ticket mask ID like 'ABC-123-456'). Scoped to tickets owned by the session account_lid. Body (form): search (string, required). If the term contains exactly two hyphens it is treated as an exact ticketmaskid match; otherwise a LIKE search runs across subject, email, and ticketmaskid. Returns: array of up to 25 matching ticket rows ordered by lastactivity DESC, each enriched with lastactivity_time (human-relative). Errors: 400 if search is empty or missing; 401 unauthorized. Caveat: this is a POST that reads, not a creator. To create see addNewTicket. To paginate full inbox use getTicketsList. To open one use getTicketInfo.

        Sibling ops: `getTicketsList` (full inbox), `getTicketInfo` (detail), `addNewTicket` (open new).
      operationId: postTicketsList
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tickets"
          description: "Search through the ticket system for a given email, subject,\
            \ or ticket mask id."
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Search the authenticated account's tickets by subject, email, or mask\
        \ ID"
      tags:
      - Tickets
  /tickets/new:
    get:
      description: |-
        Use to populate dropdowns before calling addNewTicket. Returns the customer's services grouped by product type so the user can attach a ticket to a specific resource. Iterates all enabled modules (vps, webhosting, domains, mail, etc.; mailbaby instances see only mail) and filters out services with status canceled, deleted, or fraud. Params: none. Body: none. Returns: object keyed by product TITLE (e.g. 'Vps', 'Webhosting'), each value a map of '{module}-{service_id}' to a description string including title, type/plan, VPS hypervisor name where applicable, and uppercase status tag. Errors: 401 unauthorized. Use the returned product key as the 'product' field on addNewTicket.

        Sibling ops: `addNewTicket` (consumes the product key), `getTicketsList`.
      operationId: getNewTicket
      responses:
        "200":
          description: New ticket form data including departments and service categories.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Fetch services and product options to populate the new-ticket form
      tags:
      - Tickets
    post:
      description: "Use when the customer wants to contact support. Creates the Kayako\
        \ ticket in the 'New Unassigned' department (id 18). Body (form): subject\
        \ (string, required), body (string, required), product (string, optional,\
        \ format '{module}-{service_id}' from getNewTicket), service_id+service_module\
        \ (alternative to product), attachments[] (optional, each {name, type, content\
        \ base64}), and optional server-access custom fields ip, root_pass, sudo_user,\
        \ sudo_pass, port_no, server_access (passwords are AES-encrypted with a generated\
        \ auth_key). Returns: {success: true, text, ticket: ticketmaskid}. Errors:\
        \ 400 missing subject or body; 401 unauthorized; 422 ticket creation failure.\
        \ Sibling: getNewTicket for product list, getTicketInfo to view, ReplyTicket\
        \ to add replies."
      operationId: addNewTicket
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TicketNew"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/TicketNew"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketNewResponse"
          description: A successful response after creating a ticket.
          links:
            ViewCreatedTicket:
              description: Use the ticket ID from the response to view the newly created
                ticket details.
              operationId: getTicketInfo
              parameters:
                id: $response.body#/ticket_id
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketNewResponse"
          description: A successful response after creating a ticket
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Open a new helpdesk ticket, optionally linked to a service and attachments"
      tags:
      - Tickets
  /tickets/{id}:
    delete:
      description: "Use when the customer marks a ticket resolved. IMPORTANT: despite\
        \ the DELETE verb this only CLOSES the ticket via Ticket::closeTicket — no\
        \ data is destroyed. Closed tickets remain readable through getTicketInfo\
        \ and appear in getTicketsList when view=Closed. Path: id (int ticket ID).\
        \ Body: none. Returns: 'Ticket is closed!' string on success. Errors: 401\
        \ unauthorized; 404/422 'Invalid ticket!' when id is unknown or owned by another\
        \ account. Idempotent on already-closed tickets. Siblings: CloseTicket (GET\
        \ /tickets/{id}/close — same effect, simpler URL), ReplyTicket to add a final\
        \ reply before closing, getTicketInfo to verify state."
      operationId: deleteTicketInfo
      parameters:
      - description: Ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ViewTicketResponse"
          description: Ticket Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: "Close a customer ticket via DELETE verb (closes only, never destroys\
        \ data)"
      tags:
      - Tickets
    get:
      description: "Use to render a ticket page or feed full context to an LLM. Path:\
        \ id (int, ticket ID, e.g. 1511222). Returns ticket header (subject, status,\
        \ department, dates), the ordered post/reply history, attachments, and any\
        \ custom-field values. Resolved via Ticket::getTicket(id, account_lid) so\
        \ cross-account access returns Invalid ticket. Body: none. Errors: 401 unauthorized;\
        \ 404/422 'Invalid ticket!' when the id is unknown or owned by another account.\
        \ Caveats: the same path with POST appends a reply (postTicketInfo) and DELETE\
        \ closes the ticket (deleteTicketInfo) — it does not destroy data. Siblings:\
        \ ReplyTicket, updateTicketInfo, CloseTicket, getTicketsList."
      operationId: getTicketInfo
      parameters:
      - description: Ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ViewTicketResponse"
          description: Ticket Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: "Get full ticket details including subject, status, and the reply thread"
      tags:
      - Tickets
    post:
      description: "Use to post a customer reply on an existing ticket. Path: id (int\
        \ ticket ID). Body: body (string reply text; trimmed to first 500 words),\
        \ file_attachment (multipart upload, optional), and the server-access custom\
        \ fields ip/root_pass/sudo_user/sudo_pass/port_no/server_access (passwords\
        \ AES-encrypted with auth_key=7). Either body OR an attachment is required.\
        \ Notifies any swticketwatchers staff via templated email after posting. Returns:\
        \ {status: 'success', message: 'Reply posted successfully'}. Errors: 400 'Please\
        \ enter a message or attach a file'; 401 unauthorized; 404/422 'Invalid ticket!'\
        \ when id missing or cross-account. Sibling: ReplyTicket (cleaner JSON-only\
        \ reply at /tickets/{id}/reply), updateTicketInfo, getTicketInfo, deleteTicketInfo."
      operationId: postTicketInfo
      parameters:
      - description: Ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ViewTicketResponse"
          description: Ticket Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: "Append a reply (and optional attachment, server-access fields) to\
        \ a ticket"
      tags:
      - Tickets
    put:
      description: "Reserved for future use to update ticket subject/status. The PHP\
        \ handler is currently an empty stub that returns no body, so callers should\
        \ not rely on it in production. Path: id (int). Body: would carry subject/status\
        \ fields when implemented. Returns: undefined behavior today. Errors: 401\
        \ unauthorized; expect 404/422 when implemented if id is invalid or not owned.\
        \ Caveats: prefer updateTicketInfo (POST /tickets/{id}/update) for editing\
        \ custom-field values today, postTicketInfo to add a reply, CloseTicket or\
        \ deleteTicketInfo to close. Avoid scripting against this endpoint until the\
        \ handler ships. Siblings: getTicketInfo, ReplyTicket."
      operationId: putTicketInfo
      parameters:
      - description: Ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ViewTicketResponse"
          description: Ticket Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: "Update a ticket's properties such as subject or status (stub, not\
        \ implemented)"
      tags:
      - Tickets
  /tickets/{id}/update:
    post:
      description: |-
        Use to save or change the structured custom-field values attached to a ticket — typically server-access details supplied by the customer. Path: id (int ticket ID). Body (form): one field per custom-field title, lowercased with spaces replaced by underscores (e.g. ip, root_pass, sudo_user, sudo_pass, port_no, server_access). Field id 7 (auth_key) is skipped — never set it directly. Returns: {success: true, text: 'Ticket is updated!'} or {success: false, text: 'Unable to update ticket'}. Errors: 401 unauthorized; 404 invalid or non-owned ticket. Caveats: this updates metadata only — to add a reply use ReplyTicket, to close use CloseTicket, to read current state use getTicketInfo.

        Sibling ops: `getTicketInfo` (read), `ReplyTicket` (reply), `CloseTicket` (close).
      operationId: updateTicketInfo
      parameters:
      - description: The ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicket"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/UpdateTicket"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateTicketResponseSchema"
          description: Response from updating a support ticket.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: "Update a ticket's custom field values (server-access details, etc.)"
      tags:
      - Tickets
  /tickets/{id}/reply:
    post:
      description: "Use this lightweight endpoint to add a reply to an existing ticket\
        \ without attachments or server-access fields. Cleaner alternative to postTicketInfo\
        \ when only text is being submitted. Path: id (int ticket ID). Body (form):\
        \ content (string, required reply body). Returns: {success: true, post_id:\
        \ int} on success or {success: false, text: 'Reply content cannot be empty!'\
        \ | 'Unable to reply ticket'}. Errors: 401 unauthorized; 404 implied via 'Unable\
        \ to reply ticket' when id is invalid or owned by another account. Siblings:\
        \ postTicketInfo (POST /tickets/{id}, supports attachments + custom fields),\
        \ updateTicketInfo (custom fields only), CloseTicket, getTicketInfo to verify\
        \ the new post_id appears in the thread."
      operationId: ReplyTicket
      parameters:
      - description: The ticket ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: number
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplyTicketRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReplyTicketRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplyTicketResponseSchema"
          description: Post Reply to the ticket.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: Post a simple text reply to an existing ticket thread (no attachments)
      tags:
      - Tickets
  /tickets/{id}/close:
    get:
      description: "Use to close a ticket from a link or one-click action — closure-only\
        \ equivalent of deleteTicketInfo with friendlier semantics. Calls Ticket::closeTicket\
        \ on the resolved ticket and leaves the record fully readable; closed tickets\
        \ disappear from the active inbox but remain in getTicketsList when view=Closed.\
        \ Path: id (int ticket ID, e.g. 1511222). Body: none. Returns: {success: true,\
        \ text: 'Ticket is closed!'} or {success: false, text: 'Unable to close ticket'}.\
        \ Errors: 401 unauthorized; 404 implied via 'Unable to close ticket' when\
        \ id is unknown or cross-account. Idempotent on already-closed tickets. Siblings:\
        \ deleteTicketInfo (DELETE /tickets/{id} — same effect), getTicketInfo to\
        \ confirm new status."
      operationId: CloseTicket
      parameters:
      - description: Ticket ID
        examples:
          TicketIdExample:
            value: "1511222"
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CloseTicketResponseSchema"
          description: Close ticket.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Close an open support ticket via simple GET request (no body required)
      tags:
      - Tickets
  /vps:
    get:
      description: |
        Enumerates every VPS owned by the authenticated customer — status, hostname, primary IP, plan name, and monthly cost. The canonical entry point for finding a VPS `id` to pass into other VPS operations (`getVpsInfo`, lifecycle, billing, backups, etc.). No path params, no query params, no body. Server-side filtered by session account; rows come from the `vps` table joined to `repeat_invoices` (for cost) and `services` (for plan name). Returns an array of `VpsRow` (empty array if the account has no VPS). Sibling ops: `getVpsInfo` (full detail), `getNewVps`/`putVps`/`addVps` (order a new one).

        **Returned fields (per row):**
        - `vps_id` (integer) — canonical VPS id, used in every `/vps/{id}/*` path.
        - `vps_name` (string) — display name shown in the dashboard.
        - `vps_hostname` (string) — FQDN currently assigned to the VPS.
        - `vps_ip` (string) — primary IPv4 address.
        - `vps_status` (string enum) — `active`, `pending` (awaiting payment/provisioning), `suspended` (non-payment), or `cancelled`.
        - `services_name` (string) — service-type name (e.g. `KVM`, `KVM Storage`, `HyperV`).
        - `repeat_invoices_cost` (decimal) — current monthly cost in the VPS's billing currency.
        - `vps_comment` (string|null) — customer-provided note.

        **Auth:** Session (`sessionid` header) or API key (`X-API-KEY` header). API key preferred for integrations.

        **Errors:**
        - `401 Unauthorized` — missing/invalid session or API key.

        **Related calls:**
        - **Next (per-VPS):** `getVpsInfo` (full detail incl. extra IPs, slices, addons), `getVpsInvoices` (billing per VPS), `doVpsRestart`/`doVpsStart`/`doVpsStop` (lifecycle).
        - **Order a new VPS:** `getNewVps` (catalog) → `putVps` (validate + quote) → `addVps` (place + invoice).
        - **Cancel:** `VPSCancel` (end of cycle, customer-initiated).
      operationId: getVpsList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/VpsRow"
                type: array
          description: The listing of `Vps` services on your account.
          links:
            GetVpsDetails:
              description: Use the `vps_id` from any item in the response array to
                fetch full VPS details.
              operationId: getVpsInfo
              parameters:
                id: $response.body#/0/vps_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all VPS services on the customer's account
      tags:
      - VPS
  /vps/order:
    get:
      description: |
        Step 1 of the VPS order flow. Returns the full ordering catalog the customer needs to build a valid VPS configuration: virtualization platforms (`kvm`, `kvmstorage`, `hyperv`), OS templates grouped by platform+distro, datacenter locations with current stock flags, per-slice resource costs converted to the customer's billing currency, control-panel prices, and slice resource defaults (RAM/HD/BW per slice, max slices per VPS). No path or query params, no body. The response drives the order form; once the user picks a config, call `putVps` for a dry-run price quote, then `addVps` to actually place the order. Sibling ops: `putVps`, `addVps`, `getVpsList` (existing VPS).

        **Returned top-level fields** (schema `VpsOrder`):
        - `platformNames` (object) — display names keyed by platform tag (`{kvm: "KVM", kvmstorage: "KVM Storage", hyperv: "HyperV"}`).
        - `platformPackages` (object) — service-type ids keyed by platform tag (`{kvm: 32, kvmstorage: 57, hyperv: 54}`).
        - `packageCosts` (object) — base list cost keyed by service-type id.
        - `templates` (object) — nested `{platform: {os: {template_file: template_version}}}` template tree.
        - `osNames` (object) — display name per `template_os` key.
        - `locationNames` (object) — `{1: "New Jersey", 2: "Los Angeles", 3: "Dallas, TX"}`.
        - `locationStock` (object) — `{location_id: {platform_tag: bool}}` — `true` = in stock.
        - `vpsSlice<Platform>Cost` (float) — per-slice cost per platform in customer currency (e.g. `vpsSliceKvmLCost`, `vpsSliceKvmStorageCost`, `vpsSliceHypervCost`, `vpsSliceOvzCost`).
        - `cpanelCost`, `daCost` (float) — control-panel addon costs.
        - `ramSlice`, `hdSlice`, `bwSlice` (int) — RAM (MB), HD (GB), BW (GB) per slice.
        - `maxSlices` (int) — `VPS_SLICE_MAX` cap for non-admin callers.
        - `currency`, `currencySymbol` (string) — derived from the account profile.

        **Auth:** Session or API key.

        **Errors:**
        - `401 Unauthorized` — missing session/API key.

        **Related calls:**
        - **Next:** `putVps` (validate + quote a chosen config — no charge), `addVps` (place the order).
        - **After ordering:** pay via `initiatePayment` with the returned `real_iids`, then poll `getVpsInfo` until `vps_status == "active"`.

        **Example happy-path response (abridged):**
        ```json
        {
          "platformNames": {"kvm": "KVM", "kvmstorage": "KVM Storage", "hyperv": "HyperV"},
          "platformPackages": {"kvm": 32, "kvmstorage": 57, "hyperv": 54},
          "locationNames": {"1": "New Jersey", "2": "Los Angeles", "3": "Dallas, TX"},
          "locationStock": {"1": {"kvm": true, "kvmstorage": true, "hyperv": false}},
          "osNames": {"centos-7-x86_64": "CentOS 7", "ubuntu-22.04-x86_64": "Ubuntu 22.04"},
          "templates": {"kvm": {"centos-7-x86_64": {"centos-7-x86_64.qcow2": "7"}}},
          "vpsSliceKvmLCost": 6.00,
          "cpanelCost": 18.00,
          "ramSlice": 2048, "hdSlice": 25, "bwSlice": 2000, "maxSlices": 8,
          "currency": "USD", "currencySymbol": "$"
        }
        ```
      operationId: getNewVps
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsOrder"
          description: VPS Order information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get the VPS order catalog — platforms, OS templates, locations, pricing"
      tags:
      - VPS
    post:
      description: |
        Step 3 of the VPS order flow — actually places the order. Revalidates the same configuration that `putVps` accepts (so the request is safe even if `putVps` was skipped), then calls `place_buy_vps`: allocates a backing hypervisor server via `get_vps_next_server`, creates a `Repeat_Invoice` ORM row for the recurring charge, generates the initial `invoices` row via `$repeat_invoice->invoice()`, inserts a `vps` service record with `vps_status='pending'`, and creates any control-panel addon invoices (CPanel/DirectAdmin). Returns the new service id plus invoice ids the caller must pay before provisioning runs. **Real money** — call `putVps` first to preview cost. Sibling ops: `getNewVps`, `putVps`, `getVpsInfo`, `VPSCancel`.

        **Body fields:** Identical to `putVps`. Required: `osDistro`, `osVersion`, `vpsPlatform`, `slices`. Optional: `location` (default 1), `period` (default 1), `coupon`, `hostname`, `rootpass`, `controlpanel` (default `none`), `comment`. Same validation rules apply (slice range, rootpass regex for Linux, hostname FQDN format, platform↔OS↔controlpanel compatibility).

        **Returned fields** (schema `ServiceOrderPostResponse`):
        - `success` (bool) — `true` on successful placement.
        - `serviceid` (integer) — new VPS id; use this with `getVpsInfo` to poll status.
        - `iid` (string) — primary invoice id (numeric).
        - `real_iids` (array of strings) — numeric invoice ids to pass to `initiatePayment` (`invoices` path param).
        - `iids` (array of strings) — tagged invoice ids (e.g. `SERVICEvps12345`) — alternative payment identifier.
        - `total_cost` (decimal string) — total to pay across all generated invoices.
        - `invoice_description` (string) — human-readable summary (e.g. `KVM 2 Slices`).
        - `cj_params` (object) — Commission Junction tracking parameters (affiliate flows).

        **Side effects:**
        - Inserts row into `vps` table (`vps_status='pending'`).
        - Inserts `repeat_invoices` row for the recurring charge.
        - Inserts `invoices` row for the first period charge.
        - Inserts additional `invoices` rows for CPanel/DirectAdmin addons if `controlpanel != 'none'`.
        - Logs a `vps` signup event in `history_log`.
        - Saves root password to `history_log` (encrypted at rest).

        **Errors:**
        - `400 Bad Request` — validation failed; response body is the `errors` array from validation.
        - `401 Unauthorized` — missing session/API key.

        **Related calls:**
        - **Prerequisite:** `getNewVps` (catalog), `putVps` (preview cost — strongly recommended).
        - **Next:** `getBillingInvoice` (review invoice line items), `initiatePayment` (`GET /billing/pay/{method}/{invoices}` — pay with `real_iids`), then `getVpsInfo` (poll for `vps_status == "active"`), `getVpsWelcomeEmail` (resend credentials).
        - **Cancel before paying:** `VPSCancel`.

        **Example request body:** Same as `putVps`.

        **Example response:**
        ```json
        {
          "success": true,
          "serviceid": 12345,
          "iid": "25296600",
          "real_iids": ["25296600"],
          "iids": ["SERVICEvps12345"],
          "total_cost": "12.00",
          "invoice_description": "KVM 2 Slices",
          "cj_params": {}
        }
        ```
        **Full ordering happy path:**
        ```text
        GET /vps/order                                  -> catalog (getNewVps)
        PUT /vps/order { ...config }                    -> price quote (putVps)
        POST /vps/order { ...config }                   -> { serviceid, real_iids } (addVps)
        GET /billing/invoices/{iid}                     -> confirm invoice (getBillingInvoice)
        GET /billing/pay/cc/{real_iids[0]}              -> pay (payInvoice; type=submit|redirect|single)
        GET /vps/{serviceid}                            -> poll until vps_status=="active" (getVpsInfo)
        ```
      operationId: addVps
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VpsOrderPostRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/VpsOrderPostRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a new VPS order, create the invoice, and queue provisioning"
      tags:
      - VPS
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: "Order a VPS instance. Returns invoice IDs for /billing/pay/{method}/{invoices}."
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |
        Step 2 of the VPS order flow. Validates a chosen VPS configuration (platform, OS, slices, location, control panel, coupon, etc.) against stock and policy, applies any coupon discount and frequency discount, and returns a cost breakdown — without creating any invoice or service record. Use this to preview the first-month and recurring cost before the customer commits via `addVps`. The body shape is identical between `putVps` and `addVps`; the only difference is the HTTP verb — PUT validates, POST commits. Sibling ops: `getNewVps` (catalog), `addVps` (place order).

        **Required body fields:**
        - `osDistro` (string) — OS template tag from `getNewVps.osNames` (e.g. `centos-7-x86_64`).
        - `osVersion` (string) — OS version from `getNewVps.templates[platform][os][template_file]`.
        - `vpsPlatform` (string) — one of the keys in `getNewVps.platformNames`: `kvm`, `kvmstorage`, `hyperv`, `openvz`, `ssdopenvz`, `virtuozzo`, `ssdvirtuozzo`, `lxc`, `cloudkvm`, `docker`. HTML stripped server-side.
        - `slices` (integer) — `1 ≤ slices ≤ getNewVps.maxSlices`. Windows (`kvm` with `osDistro` starting `windows` or `os==5`, plus `hyperv`/`cloudkvm` Windows) requires `slices ≥ 2`.

        **Optional body fields:**
        - `location` (integer, default 1) — `1`=NJ, `2`=LA, `3`=TX. Out-of-stock platforms in a location auto-fail with an error.
        - `period` (integer, default 1) — billing cycle in months: `1` / `6` / `12` / `24` / `36`. Discounts: 6mo=5%, 12mo=10%, 24mo=15%, 36mo=20%.
        - `controlpanel` (string, default `none`) — `none` / `cpanel` (forces CentOS) / `da` (DirectAdmin). Incompatible with Windows.
        - `coupon` (string) — coupon code; validated against `coupons` table (custid match, module=`vps`, applies-to-service-type, usable count). Returns "Invalid Coupon" if not found/usable.
        - `hostname` (string) — FQDN matching `/^.*\..*\..*$/` (e.g. `server.example.com`). Skipped for Windows KVM (auto-set to `vps{id}` server-side).
        - `rootpass` (string) — required for all Linux platforms. Must match `/(?=.{8,})(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*\W)/`. Not required for Windows.
        - `comment` (string) — free-form note saved on the `vps` row.

        **Returned fields** (schema `VpsOrderPutResponse`):
        - `continue` (bool) — `true` if validation passed and the order can be POSTed. If `false`, render `errors` and do not call `addVps`.
        - `errors` (array of strings) — human-readable validation messages.
        - `coupon_code` (integer) — id of the matched coupon row, or `0` if no coupon applied.
        - `service_cost` (float) — first-period cost in customer currency (includes coupon + period discount).
        - `slice_cost` (float) — per-slice cost after coupon.
        - `repeat_service_cost` (float) — recurring monthly/period cost after coupon and period discount.
        - `original_slice_cost`, `original_cost` (float) — undiscounted reference values.
        - `service_type` (integer) — resolved service-type id (e.g. KVM Linux=33, KVM Win=32, KVM Storage=57, HyperV=54, OpenVZ=31, Virtuozzo=55).
        - `monthly_service_cost` (float) — recurring cost normalized to monthly.

        **Side effects:** None — `PUT /vps/order` is a pure read.

        **Errors:**
        - `400` — missing required field (`osDistro` / `vpsPlatform` / `slices` / `osVersion`) or invalid `location`. Body shape: `{error: "Missing field <name>"}`.
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite:** `getNewVps` (provides every option value referenced in the body).
        - **Next:** `addVps` (place the order with the same body once `continue == true`).

        **Example request body:**
        ```json
        {
          "vpsPlatform": "kvm",
          "osDistro": "centos-7-x86_64",
          "osVersion": "centos-7-x86_64.qcow2",
          "slices": 2,
          "location": 1,
          "period": 1,
          "controlpanel": "none",
          "hostname": "web1.example.com",
          "rootpass": "Sup3rS3cret!",
          "coupon": ""
        }
        ```
        **Example response (validation passed):**
        ```json
        {
          "continue": true,
          "errors": [],
          "coupon_code": 0,
          "service_cost": 12.00,
          "slice_cost": 6.00,
          "repeat_service_cost": 12.00,
          "service_type": 33,
          "monthly_service_cost": 12.00,
          "platform": "kvm", "os": "centos-7-x86_64",
          "slices": 2, "location": 1, "period": 1,
          "hostname": "web1.example.com"
        }
        ```
      operationId: putVps
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VpsOrderPutRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/VpsOrderPutRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsOrderPutResponse"
          description: Validate VPS order response.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Validate a VPS order configuration and quote the cost — dry run, no\
        \ charge"
      tags:
      - VPS
  /vps/{id}:
    delete:
      description: |
        Customer-facing cancel — schedules termination, stops future renewals, and queues deprovisioning. Billing continues until the end of the already-paid period, and the customer keeps access until then; this endpoint does **not** issue refunds. Delegates to `Billing\CancelService::go()` (shared cancellation handler used across all service modules). The repeat-invoice is marked for cancellation and the VPS row's status will eventually flip to `cancelled`. Reversible: a customer can typically un-cancel before the cycle ends by opening a support ticket. Sibling ops: `getVpsInfo` (verify status), `getVpsInvoices` (review final invoices), `getVpsBackup` (snapshot before cancellation).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `VPSCancelResponse` — confirmation text plus the scheduled cancellation date.

        **Side effects:**
        - Sets the `repeat_invoices` row for the VPS to non-renewing.
        - Logs the cancellation event in `history_log`.
        - Queues `vpsqueue` deprovisioning action to run at end-of-cycle.
        - Does NOT immediately stop the VPS — power state is unchanged until the cycle ends.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401 Unauthorized` — missing session/API key.
        - `404 Invalid VPS Passed` — `id` not owned by caller.

        **Related calls:**
        - **Before cancelling:** `getVpsBackup` (create a final snapshot), `downloadVpsBackup` (export it).
        - **After cancelling:** `getVpsInfo` (confirm `vps_status` flipped), `getVpsInvoices` (final invoices).
        - **Sibling cancels on other modules:** `CancelDomain`, `mailCancel`, `webhostingCancel`, etc. all use the same `CancelService` handler.
      operationId: VPSCancel
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VPSCancel_200_response"
          description: VPS Cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Cancel a VPS service at the end of the current billing cycle
      tags:
      - VPS
    get:
      description: |
        Returns everything the customer dashboard shows for a single VPS — hostname, primary IP plus any extra IPs, OS, allocated slices (CPU/RAM/disk), current `vps_status`, plan/service-type, datacenter location, billing currency, and `serviceAddons` (extra IPs and additional GB disk). Read-only. Backed by `ViewVPS::getDetails()`; ownership is enforced via `get_service($id, 'vps')` — cross-customer requests return 404. Use to render a VPS detail page, to verify ownership before mutating, or to poll `vps_status` after `addVps` (status flips `pending` → `active` once provisioning completes). Sibling ops: `getVpsList`, `doVpsRestart`/`doVpsStart`/`doVpsStop` (lifecycle), `getVpsTrafficUsage` (bandwidth), `getVpsBackups`, `getVpsInvoices`, `updateVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Returned fields** (schema `Vps` plus extras):
        - Core: `vps_id`, `vps_hostname`, `vps_ip`, `vps_status`, `vps_slices`, `vps_os`, `vps_type` (service-type id), `vps_server` (backing hypervisor id), `vps_custid`, `vps_comment`, `vps_coupon`.
        - `services_name` (string) — plan name (e.g. `KVM`, `HyperV`).
        - `client_links` (array) — UI action links (`{name, link, icon}`) for restart, snapshot, console, etc. Internal `?link=queue&action=...` URLs are pre-resolved.
        - `serviceAddons` (object) — `{extra_ips: [...], additional_gb: <int>}` populated from `repeat_invoices` rows that match `Additional IP*` / `Additional N GB Space*` patterns.
        - **Stripped fields:** `admin_links`, `settings`, `csrf` are removed before response.

        **Auth:** Session or API key. Customer must own the VPS (enforced via `vps_custid` match).

        **Errors:**
        - `401 Unauthorized` — missing session/API key.
        - `404 Invalid VPS Passed` — `id` does not exist or is owned by a different account.

        **Related calls:**
        - **Lifecycle:** `doVpsStart`, `doVpsStop`, `doVpsRestart`.
        - **Maintenance:** `postVpsChangeHostname`, `postVpsChangeRootPassword`, `postVpsReverseDns`.
        - **Upgrade:** `getVpsSlices`/`postVpsSlices`, `getVpsBuyHdSpace`/`putVpsBuyHdSpace`/`postVpsBuyHdSpace`, `getVpsBuyIp`/`postVpsBuyIp`.
        - **Backups:** `getVpsBackup` (create), `getVpsBackups` (list), `downloadVpsBackup`, `postVpsRestore`.
        - **Billing:** `getVpsInvoices`, `VPSCancel`.
        - **Metrics:** `getVpsTrafficUsage`.
      operationId: getVpsInfo
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vps"
          description: The VPS Information
          links:
            GetVpsInvoices:
              description: Retrieve billing invoices for this VPS.
              operationId: getVpsInvoices
              parameters:
                id: $request.path.id
            RestartVps:
              description: Restart this VPS.
              operationId: doVpsRestart
              parameters:
                id: $request.path.id
            StartVps:
              description: Power on this VPS.
              operationId: doVpsStart
              parameters:
                id: $request.path.id
            StopVps:
              description: Power off this VPS.
              operationId: doVpsStop
              parameters:
                id: $request.path.id
            BackupVps:
              description: Create a backup of this VPS.
              operationId: postVpsBackup
              parameters:
                id: $request.path.id
            ListVpsBackups:
              description: List available backups for this VPS.
              operationId: getVpsBackups
              parameters:
                id: $request.path.id
            ReinstallVpsOS:
              description: View available OS templates for reinstalling this VPS.
              operationId: getVpsReinstallOs
              parameters:
                id: $request.path.id
            GetVpsReverseDns:
              description: View reverse DNS entries for this VPS.
              operationId: getVpsReverseDns
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Get full details for one VPS — IPs, hostname, OS, slices, status,\
        \ addons"
      tags:
      - VPS
    post:
      description: |
        Write-back endpoint for the VPS service record — typically the dashboard's "save changes" action. The body is processed by `ViewVPS::getDetails()`, the same handler as the GET, so the accepted fields mirror what the VPS detail view edits in place (e.g. customer comment/label, display preferences). For lifecycle or provisioning changes use the dedicated endpoints — they enforce platform-specific validation and queue hypervisor actions correctly. Sibling ops: `getVpsInfo`, `VPSCancel`, and all the dedicated mutation endpoints listed below.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** Form-encoded or JSON with editable fields (handler decides which are persisted). Most callers should use the dedicated endpoints instead.

        **Returns:** `SuccessTextResponse` — `{text: "..."}` on success.

        **Auth:** Session/API key. Ownership enforced via `vps_custid` match.

        **Errors:**
        - `401 Unauthorized` — missing session/API key.
        - `404 Invalid VPS Passed` — `id` not owned by caller.

        **Prefer these dedicated endpoints when applicable:**
        - Hostname → `postVpsChangeHostname` (OpenVZ/Virtuozzo only).
        - Root password → `postVpsChangeRootPassword` (specific value) or `postVpsResetPassword` (random).
        - Reverse DNS → `postVpsReverseDns`.
        - Slice upgrade/downgrade → `getVpsSlices` → `postVpsSlices` (creates prorated invoice).
        - Additional disk → `getVpsBuyHdSpace` → `putVpsBuyHdSpace` → `postVpsBuyHdSpace`.
        - Additional IPs → `getVpsBuyIp` → `postVpsBuyIp`.
        - Timezone → `getVpsChangeTimezone` → `postVpsChangeTimezone`.
        - Cancel service entirely → `VPSCancel`.
      operationId: updateVpsInfo
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update editable settings on a VPS service record
      tags:
      - VPS
  /vps/{id}/backup:
    get:
      description: |
        Creates an on-demand backup of the VPS — typically called before a risky change (OS reinstall, slice upgrade, restore from older backup). Enqueues a `backup` action on the hypervisor (`history_log` `vpsqueue` entry) and returns immediately; the actual snapshot runs asynchronously and may take a few minutes. Despite being GET, this is a side-effecting action and the MCP parser flags it accordingly. The new backup, once complete, appears in `getVpsBackups` keyed by `name`. Sibling ops: `getVpsBackups` (list), `downloadVpsBackup` (download via pre-signed URL), `deleteVpsBackup`, `postVpsRestore`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text: "Action has been sent to the server. Please allow up to 2 minutes for action to be completed.", queueId: <integer> }` — `queueId` is the row id in `queue_log`/`history_log` and can be used to track action status.

        **Backup limits (per platform):**
        - KVM / KVM Storage: backups **enabled**.
        - HyperV, OpenVZ, SSD-OpenVZ, Virtuozzo, SSD-Virtuozzo: **disabled** server-side (returns 400 "Backups are disabled for this type").
        - Max 4 backups per VPS for non-admin callers. If at the cap, returns "Currently 4 backups per VPS max" — delete an old one first via `deleteVpsBackup`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `409 VPS is not active` — `vps_status != "active"`.
        - `400 Backups are disabled for this type` — incompatible platform.
        - `400 Currently 4 backups per VPS max` — at cap.

        **Related calls:**
        - **List existing:** `getVpsBackups`.
        - **Download:** `downloadVpsBackup` (PATCH; returns 24-hr pre-signed URL for MinIO backups; Swift/ZFS disabled).
        - **Delete:** `deleteVpsBackup` (DELETE; Swift/MinIO only).
        - **Restore from a backup:** `postVpsRestore`.
      operationId: getVpsBackup
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Trigger a manual on-demand snapshot/backup of the VPS
      tags:
      - VPS
  /vps/{id}/backups:
    delete:
      description: |
        Removes a backup file from storage to free space. For `minio`-typed backups runs `mc rm --force --recursive` on the path; for `swift`-typed backups removes the storage object via the Swift API. ZFS-typed backups **cannot** be deleted through this endpoint — they return an error directing the caller to open a support ticket. **Irreversible** — once deleted the backup cannot be used with `postVpsRestore` or `downloadVpsBackup`. Sibling ops: `getVpsBackups` (list), `downloadVpsBackup` (download first), `getVpsBackup` (create new).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Query params:**
        - `file` (string, required) — exact `name` from `getVpsBackups` (the canonical backup identifier).

        **Returns:** `SuccessTextResponse` — `Backup <name> removed.` on success.

        **Side effects:**
        - **minio**: `mc rm --force --recursive` removes the entire backup directory.
        - **swift**: deletes the listed object(s) plus any multi-part `extra` segments.

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `No file specified` — `file` query param missing.
        - **ZFS backup:** `This type of backup if not removable. Please contact support if you need this removed.`
        - **MinIO rm failure:** `Error removing file <name>`.

        **Related calls:**
        - **List first to get `name`:** `getVpsBackups`.
        - **Download before deleting:** `downloadVpsBackup` (MinIO only; Swift/ZFS disabled).
        - **Restore (don't delete):** `postVpsRestore`.
      operationId: deleteVpsBackup
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given VPS."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      - description: The backup filename to delete.
        explode: true
        in: query
        name: file
        required: true
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Permanently delete a VPS backup file by name (irreversible)
      tags:
      - VPS
    get:
      description: |
        Enumerates the backup files available for the VPS across all backend storage systems (OpenStack Swift, MinIO/S3, and ZFS snapshots). Each entry's `name` is the canonical identifier the caller must pass to sibling endpoints (`downloadVpsBackup`, `deleteVpsBackup`, `postVpsRestore`) — there is no separate integer id. The list is filtered to the VPS's owner by default; admins can list all backups on the account by passing `all=1`. Sibling ops: `getVpsBackup` (create new), `downloadVpsBackup`, `deleteVpsBackup`, `postVpsRestore`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Query params:**
        - `all` (string, optional, enum `0`/`1`, default `0`) — set to `1` to list every backup across all services on the account, not just the ones for `{id}`.

        **Returns:** `VpsBackupRows` — array of objects:
        - `name` (string) — canonical identifier, e.g. `vps-12345-2026-05-12.tar.gz`.
        - `type` (string enum) — `swift` / `minio` / `zfs`. Determines which operations are available (see Sibling notes).
        - `service` (integer) — VPS id the backup belongs to.
        - `path` (string) — storage path/URL.
        - `size` (integer) — bytes.
        - `repoIdx` (integer) — repository index (0 or 1 for Swift; selects which credentials/bucket).
        - `extra` (array, optional) — multi-part backup pieces.

        **Auth:** Session/API key. Ownership enforced via `vps_custid` on the parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.

        **Capability matrix by backup type:**
        - `swift`: list ✓, download ✗ (disabled — contact support), delete ✓.
        - `minio`: list ✓, download ✓ (24-hr pre-signed URL via `downloadVpsBackup`), delete ✓.
        - `zfs`: list ✓, download ✗ (disabled), delete ✗ (open a ticket).

        **Related calls:**
        - **Create new:** `getVpsBackup`.
        - **Download:** `downloadVpsBackup` (PATCH).
        - **Delete:** `deleteVpsBackup` (DELETE).
        - **Restore:** `postVpsRestore` — pass `backup` as `<type>:<service>:<name>`.
      operationId: getVpsBackups
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given VPS."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsBackupRows"
          description: The listing of the available backups
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "List existing backups for the VPS across Swift, MinIO, and ZFS"
      tags:
      - VPS
    patch:
      description: |
        Generates a time-limited download link for a MinIO/S3-backed VPS backup so the customer can fetch it off-platform. Runs `mc share download --expire=24h` against the resolved backup path and returns the resulting public URL — valid for 24 hours from issue. Only `minio`-typed backups are downloadable; `swift` and `zfs` backups have direct download disabled (returns an error directing the customer to support). Sibling ops: `getVpsBackups` (list to find `name`), `postVpsRestore` (restore in place — no download needed), `deleteVpsBackup`, `getVpsBackup` (create new).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body (JSON or multipart, required):**
        - `file` (string, required) — exact `name` from `getVpsBackups`.

        **Returns:**
        - `text` (string) — `URL available for the next 24 hours`.
        - `url` (string) — pre-signed download URL (HTTPS).

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `No file specified` — `file` body field missing.
        - **Swift backup:** `Downloads for this type have been disabled. Please contact support if you need this backup.`
        - **ZFS backup:** same disabled message.
        - **MinIO share failure:** `Error sharing file <name>`.

        **Related calls:**
        - **Prerequisite:** `getVpsBackups` (find a backup with `type == "minio"`).
        - **Alternative:** `postVpsRestore` (restore in place — no download).
        - **Cleanup after download:** `deleteVpsBackup`.
      operationId: downloadVpsBackup
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "Set to `1` to list all backups across all services, not just\
          \ the ones for the given VPS."
        explode: true
        in: query
        name: all
        required: false
        schema:
          enum:
          - "0"
          - "1"
          type: string
        style: form
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/downloadQsBackup_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/downloadQsBackup_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/downloadQsBackup_200_response"
          description: Download URL for the backup file.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Issue a 24-hour pre-signed URL to download a MinIO-backed VPS backup
      tags:
      - VPS
  /vps/{id}/block_smtp:
    get:
      description: |
        Blocks outbound SMTP (port 25) traffic on the VPS at the hypervisor level — typical for cPanel/WHM customers who route through a smart relay, or for VPS that should never emit mail directly. Queues a `block_smtp` action on the `vpsqueue` and triggers a VNC re-setup. Despite being GET, this is a side-effecting action and the MCP parser flags it accordingly. **One-way from the client side:** there is no public unblock endpoint — re-enabling outbound SMTP requires a support ticket so abuse history can be reviewed. Sibling ops: `getVpsInfo` (verify state), `doVpsRestart`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — `queueId` references the `queue_log` row; allow up to 2 minutes for the iptables/firewall rule to take effect.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `409 VPS is not active` — `vps_status != "active"`.

        **Reversibility:** Not client-reversible — open a ticket.

        **Related calls:**
        - **Verify state:** `getVpsInfo`.
        - **General lifecycle:** `doVpsStart`, `doVpsStop`, `doVpsRestart`.
      operationId: doVpsBlockSmtp
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Block outbound SMTP (port 25) on the VPS to prevent spam/abuse
      tags:
      - VPS
  /vps/{id}/buy_hd_space:
    get:
      description: |
        Step 1 of the disk-space addon flow. Returns the current "Additional N GB Space" already purchased for the VPS (0 if none) and the per-GB monthly cost in USD — both adjusted for the customer's reseller pricing tier via `get_reseller_price`. Read-only. Use this to populate a disk-upgrade form before calling `putVpsBuyHdSpace` (preview) and `postVpsBuyHdSpace` (commit). For whole-plan upgrades (CPU+RAM+disk together) use the slices flow instead (`getVpsSlices` / `postVpsSlices`). Sibling ops: `putVpsBuyHdSpace`, `postVpsBuyHdSpace`, `postVpsSlices`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:**
        - `gbCost` (float) — per-GB monthly cost in USD (after reseller discount).
        - `size` (integer) — current additional GB already purchased (0 if none).

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - **Pre-condition:** an existing `Additional N GB Space for VPS {id}` repeat-invoice is required for the lookup to find a baseline; if it isn't parseable, the endpoint returns an error asking the customer to contact support. New installs always start at `size: 0`.

        **Related calls:**
        - **Next (preview):** `putVpsBuyHdSpace` — returns prorated `diffCost` for a target size.
        - **Next (commit):** `postVpsBuyHdSpace` — creates/updates the addon repeat invoice.
        - **Alternative path (whole plan):** `getVpsSlices` → `postVpsSlices`.
      operationId: getVpsBuyHdSpace
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get VPS Buy HD Space information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get current additional disk size and per-GB monthly cost for the VPS
      tags:
      - VPS
    post:
      description: |
        Step 3 of the disk-space addon flow — commit. Creates or updates the `Additional N GB Space for VPS {id}` `repeat_invoices` row with the new size and recurring cost, then generates a one-off prorated `invoices` row for the immediate difference. The hypervisor disk-grow action is queued either immediately (`update_hdsize` in `vpsqueue`) when no charge is owed, or after the invoice is paid. When increasing from an existing size, any unpaid prior addon invoice is deleted and any already-paid one is credited against `diffCost`. **Real money** — call `putVpsBuyHdSpace` first to preview. Sibling ops: `getVpsBuyHdSpace`, `putVpsBuyHdSpace`, `postVpsSlices`, `initiatePayment`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `size` (integer, required) — target additional GB. Range `1..100`. Must differ from current.

        **Returns:**
        - When immediate charge owed: `{ text: "Invoice Created, Please Pay This To Activate Extra Space", invoice: <integer> }` — pass `invoice` to `initiatePayment`.
        - When no charge owed (downgrade/credit): `{ text: "Repeat Invoice Updated, Server Size Update Queued" }` — disk grow already queued.

        **Side effects:**
        - Inserts or updates `repeat_invoices` row for the addon.
        - Inserts `invoices` row for `diffCost` when > 0.
        - Deletes any unpaid prior addon invoices for the same VPS within the last month.
        - Credits any paid prior invoice against `diffCost`.
        - Queues `update_hdsize` in `vpsqueue` when no payment is owed.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `Invalid Size Specified` — `size` out of `1..100`.
        - `No Change Made, Size The Same`.
        - **Pre-condition:** an existing addon row is required. If not found, the request short-circuits (`go()` returns without acting); use the order flow for the very first addon, or `postVpsSlices` for whole-plan upgrades.

        **Related calls:**
        - **Preview first:** `putVpsBuyHdSpace`.
        - **Pay the invoice:** `initiatePayment` (`GET /billing/pay/{method}/{invoices}`).
        - **Whole-plan upgrades:** `postVpsSlices` (bundles disk + RAM + CPU).
      operationId: postVpsBuyHdSpace
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Post Buy HD Space for VPS response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Buy or resize the VPS additional-disk addon and create a prorated invoice
      tags:
      - VPS
    put:
      description: |
        Step 2 of the disk-space addon flow. Dry-run that quotes a new "Additional N GB Space" addon at the target size, prorated to the VPS's existing billing cycle. **No invoice is created and no charge happens.** Use to show the customer the immediate prorated `diffCost` and the new recurring `cost` before they commit via `postVpsBuyHdSpace`. Sibling ops: `getVpsBuyHdSpace`, `postVpsBuyHdSpace`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `size` (integer, required) — target additional GB. Range `1 ≤ size ≤ 100`; rejected with "Invalid Size Specified" otherwise. Rejected with "No Change Made, Size The Same" if it equals current.

        **Returns:**
        - `gbCost` (float) — per-GB monthly cost in USD.
        - `curSize` (integer) — currently purchased additional GB.
        - `newSize` (integer) — requested target size.
        - `cost` (float) — new recurring cost (size × gbCost × frequency).
        - `diffCost` (float) — prorated immediate charge for the partial cycle plus the remainder of the period (when frequency > 1).
        - `frequency` (integer) — billing cycle in months (e.g. 1, 6, 12).

        **Side effects:** None — pure read.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `Invalid Size Specified` — `size` out of `1..100`.
        - `No Change Made, Size The Same` — `size == curSize`.

        **Related calls:**
        - **Prerequisite:** `getVpsBuyHdSpace` (read current state).
        - **Next:** `postVpsBuyHdSpace` (commit; creates invoice when diffCost > 0, queues immediate update otherwise).
      operationId: putVpsBuyHdSpace
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Pricing preview for the requested HD space size.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Preview cost to set additional VPS disk to a target GB size — dry run
      tags:
      - VPS
  /vps/{id}/buy_ip:
    get:
      description: |
        Step 1 of the additional-IP addon flow. Returns the list of extra IPs already on the VPS (each with a `cancel_link` for removing that specific addon), how many more are allowed (`maxIps` = `VPS_MAX_IPS` constant), and the per-IP monthly cost converted to the VPS's billing currency. Read-only. Use to render the "buy another IP" form and to enforce the cap before calling `postVpsBuyIp`. Sibling ops: `postVpsBuyIp` (purchase one more), `postVpsReverseDns` (set PTR on the new IP), `getVpsReverseDns`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:**
        - `ipsDetails` (array) — one entry per existing extra IP. Each entry includes:
          - `ip` (string) — the IPv4 address (parsed from the repeat-invoice description).
          - `cancel_link` (string) — relative URL `cancel_addon?module=vps&r=<repeat_invoice_id>` to cancel that specific IP addon.
          - The underlying `repeat_invoices` / `invoices` columns (description, amount, dates, etc.).
        - `ipCount` (integer) — current count of extra IPs already purchased.
        - `maxIps` (integer) — hard cap (`VPS_MAX_IPS`).
        - `ipCost` (float) — per-IP monthly cost, converted from `VPS_IP_COST` to the VPS's billing currency.
        - `currency` (string) — VPS billing currency code (ISO 4217, e.g. `USD`).
        - `currencySymbol` (string).

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.

        **Related calls:**
        - **Next (buy one more):** `postVpsBuyIp` — auto-allocates the next free IP on the same hypervisor.
        - **After activation:** `postVpsReverseDns` (set PTR for the new IP), `getVpsInfo` (verify allocation).
        - **Cancel an existing extra IP:** follow the `cancel_link` URL (renders the cancel-addon page).
      operationId: getVpsBuyIp
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: VPS Buy IP information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read current additional IPs, cap, and per-IP monthly cost for the\
        \ VPS"
      tags:
      - VPS
    post:
      description: |
        Step 2 of the additional-IP addon flow — commit. Auto-selects the next free IP on the same hypervisor via `vps_get_next_ip`, creates a `Additional IP for VPS {id}` recurring invoice (`repeat_invoices`), and generates an immediate one-off `invoices` row at the current IP cost. **Real money.** The network-side IP allocation happens once the invoice is paid; the IP is bound to the VPS during the next provisioning sweep. Sibling ops: `getVpsBuyIp` (preview), `initiatePayment`, `getVpsInfo` (verify), `postVpsReverseDns` (set PTR after activation).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None — the next free IP is auto-selected.

        **Returns:**
        - `text` (string) — `Ordered Additional IP successfully.`
        - `invoice` (integer) — new invoice id to pay via `initiatePayment`.

        **Side effects:**
        - Reserves the next free IP on the VPS's `vps_server` (parked until payment).
        - Inserts `repeat_invoices` row (`Additional IP for VPS {id}`, recurring at `ipCost`, frequency from parent service).
        - Inserts `invoices` row for the immediate one-period charge.
        - Logs the addon creation in `myadmin_log`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `VPS already has the maximum number of IPs allowed. If you require additional IPs please contact support.` — `ipCount >= maxIps` (`VPS_MAX_IPS`).
        - `No available free ips on this server. Please contact support to order additional ips.` — `vps_get_next_ip` returned false.

        **Related calls:**
        - **Prerequisite:** `getVpsBuyIp` (capacity check + preview).
        - **Next:** `initiatePayment` with the returned `invoice` id, then `getVpsInfo` to confirm allocation.
        - **Post-activation:** `postVpsReverseDns` to set the PTR for the new IP.
      operationId: postVpsBuyIp
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Submit VPS Buy IP form response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Purchase one additional IP for the VPS and create the invoice
      tags:
      - VPS
  /vps/{id}/change_hostname:
    get:
      description: |
        Step 1 of the hostname-change flow. Returns the hostname currently stored on the `vps` row so the customer can confirm the existing value before submitting a new one. Read-only. **Platform restriction:** hostname changes through `postVpsChangeHostname` are only supported on OpenVZ/SSD-OpenVZ/Virtuozzo/SSD-Virtuozzo; KVM and HyperV require a support ticket — so for those platforms this endpoint is informational only. Sibling ops: `postVpsChangeHostname` (apply new value), `postVpsReverseDns` (PTR for primary IP — auto-updated by `postVpsChangeHostname`).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Current hostname (object form: `{ hostname: "<fqdn>" }`).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `409 VPS is not active` — `vps_status != "active"`.

        **Related calls:**
        - **Next:** `postVpsChangeHostname` (OpenVZ/Virtuozzo only; auto-updates PTR for the primary IP).
        - **PTR for extra IPs:** `postVpsReverseDns`.
        - **Verify after change:** `getVpsInfo`.
      operationId: getVpsChangeHostname
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Current hostname information for the VPS.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read the VPS's current hostname before changing it
      tags:
      - VPS
    post:
      description: |
        Renames the VPS — validates the FQDN with `valid_hostname()`, sets the reverse-DNS PTR record for the primary IP (`reverse_dns($vps_ip, $hostname)`), and either updates the `vps_hostname` column directly (if the VPS is still `pending`) or queues a `change_hostname` action on the hypervisor (`vpsqueue`) for active services. **Platform restriction:** rejected unless the VPS runs on OpenVZ, SSD-OpenVZ, Virtuozzo, or SSD-Virtuozzo — KVM/HyperV must open a support ticket. Sibling ops: `getVpsChangeHostname`, `postVpsReverseDns`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `hostname` (string, required) — new FQDN (e.g. `web1.example.com`). Validated by `valid_hostname()`.

        **Returns:**
        - For active services: `{ text, queueId }` — `queueId` references the `queue_log` row. Allow ~2 minutes.
        - For pending services: `{ text: "Hostname Updated" }` — applied in place.

        **Side effects:**
        - Sets PTR record for `vps_ip` via `reverse_dns()`.
        - Either updates `vps_hostname` directly (pending) or queues `change_hostname` (active) plus logs `change_hostname` history entry with the `old to new` transition.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `Invalid Hostname` — fails `valid_hostname()`.
        - `No change in hostname` — value matches current.
        - `Hostname changing is only enabled on OpenVZ/Virtuozzo Platforms currently. Contact support and we can change it for you.` — wrong platform.

        **Related calls:**
        - **Prerequisite:** `getVpsChangeHostname` (read current).
        - **Extra IPs need separate PTR updates:** `postVpsReverseDns`.
        - **Verify:** `getVpsInfo` (look for updated `vps_hostname`).
      operationId: postVpsChangeHostname
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/HostnameObject"
          application/json:
            schema:
              $ref: "#/components/schemas/HostnameObject"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Rename the VPS hostname (OpenVZ/Virtuozzo only) and auto-set PTR for
        the primary IP
      tags:
      - VPS
  /vps/{id}/change_root_password:
    get:
      description: |
        Step 1 of the root-password change flow. Validates ownership and active status; the response is a placeholder/policy object the dashboard uses to render the form (current implementation does not yet return a populated policy — it short-circuits after the ownership/status checks). Read-only. Use to confirm the VPS exists and is active before calling `postVpsChangeRootPassword`. For a server-generated random password instead, use `postVpsResetPassword`. Sibling ops: `postVpsChangeRootPassword`, `postVpsResetPassword`, `postVpsChangeWebuzoPassword` (Webuzo control panel).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Object with password requirements/state. Currently a no-op pre-check; reserved for future policy fields (min length, complexity rules, last-change timestamp).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` — `id` not owned by caller.
        - `409 VPS is not active` — `vps_status != "active"`.

        **Related calls:**
        - **Next (specific password):** `postVpsChangeRootPassword`.
        - **Random password:** `postVpsResetPassword`.
        - **Webuzo control panel password:** `postVpsChangeWebuzoPassword` (separate credential).
      operationId: getVpsChangeRootPassword
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Password requirements and current state.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Pre-flight check before changing the VPS root password
      tags:
      - VPS
    post:
      description: |
        Sets a specific root password (Administrator on Windows) chosen by the customer. Queues a `change_root` action on the hypervisor (`vpsqueue`) and records the new password in `history_log` as a `change_root_password` entry for operator reference. The password takes effect within ~2 minutes. **Caveat:** there is no rollback — to "undo", set another new password. For a server-generated random password instead, use `postVpsResetPassword`. Sibling ops: `getVpsChangeRootPassword`, `postVpsResetPassword`, `postVpsChangeWebuzoPassword`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `password` (string, required) — new root/Administrator password. The endpoint does not enforce a regex here (the hypervisor agent applies platform policy), but `validate_buy_vps`-style strength is strongly recommended: 8+ chars, upper, lower, digit, special.

        **Returns:** `{ text, queueId }` — `queueId` tracks the action in `queue_log`.

        **Side effects:**
        - Inserts `vpsqueue` `change_root` row.
        - Inserts `history_log` `change_root_password` audit entry storing the new password (operator-readable for support).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `400 Missing field "password"`.

        **Related calls:**
        - **Pre-flight:** `getVpsChangeRootPassword`.
        - **Random instead:** `postVpsResetPassword`.
        - **Control panel password:** `postVpsChangeWebuzoPassword`.
        - **Verify:** `getVpsInfo` (no field change — verification is operational, not via API).
      operationId: postVpsChangeRootPassword
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Set a specific new root/Administrator password on the VPS
      tags:
      - VPS
  /vps/{id}/change_timezone:
    get:
      description: |
        Step 1 of the timezone-change flow. Returns the list of IANA timezone identifiers the VPS accepts (e.g. `America/New_York`, `Europe/London`, `Asia/Tokyo`) — sourced from `/usr/share/zoneinfo/zone.tab` on the MyAdmin host. Use to populate a timezone picker before calling `postVpsChangeTimezone`. Read-only. Sibling op: `postVpsChangeTimezone`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Array of strings — IANA timezone identifiers, sorted alphabetically.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Next:** `postVpsChangeTimezone` (must pass a value present in this array).
        - **Account-level timezone:** `updateAccountInfo` (sets the default for new VPS).
      operationId: getVpsChangeTimezone
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                TimeZones:
                  value:
                  - America/New_York
                  - America/Nome
                  - America/Noronha
                  - America/North_Dakota/Beulah
                  - America/North_Dakota/Center
              schema:
                items:
                  type: string
                type: array
          description: VPS Change Timezone info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List IANA timezones supported by the VPS guest OS
      tags:
      - VPS
    post:
      description: |
        Step 2 of the timezone-change flow — commit. Validates `timezone` against the list from `getVpsChangeTimezone`, then queues a `change_timezone` action on the hypervisor (`vpsqueue`). Action takes effect within ~2 minutes. Sibling op: `getVpsChangeTimezone`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `timezone` (string, required) — IANA identifier; **must** be one of the values returned by `getVpsChangeTimezone` (in-array check enforced server-side).

        **Returns:** `{ text, queueId }`.

        **Side effects:**
        - Inserts `vpsqueue` `change_timezone` row with the validated value.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `422 Invalid timezone` — value not in the supported list.

        **Related calls:**
        - **Prerequisite:** `getVpsChangeTimezone` (the only valid source for `timezone` values).
      operationId: postVpsChangeTimezone
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/TimezoneUpdate"
          application/json:
            schema:
              $ref: "#/components/schemas/TimezoneUpdate"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Set the system timezone on the VPS guest OS
      tags:
      - VPS
  /vps/{id}/change_webuzo_password:
    post:
      description: |
        Rotates the admin password on the Webuzo control panel that ships pre-installed on certain VPS templates. Re-authenticates the caller via their MyAdmin account password (`account_passwd` md5 check), then calls the Webuzo SDK (`Webuzo_API::change_password`) to apply the new password, updates the stored credential in `history_log`, and emails the customer a confirmation via the `client/client_email.tpl` template. Used for the control panel only — for the underlying OS root/Administrator password use `postVpsChangeRootPassword`/`postVpsResetPassword`. Sibling ops: `postVpsChangeRootPassword`, `postVpsResetPassword`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields (both required):**
        - `password` (string, required) — new Webuzo admin password. Validated by `valid_password()`.
        - `login_password` (string, required) — the customer's current MyAdmin account password (re-auth check; md5-compared to `accounts.account_passwd`).

        **Returns:** `{ text }` — `Password updated successfully!`

        **Side effects:**
        - Calls Webuzo API to apply new password.
        - Updates the `Webuzo Details` row in `history_log` with the new value.
        - Sends a confirmation email to the account's billing email.
        - Logs the rotation in `myadmin_log`.

        **Auth:** Session/API key plus re-auth via `login_password`.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `Missing Password` / `Missing Login Password` — body field absent.
        - `Login Password is incorrect!` — `login_password` doesn't match stored hash.
        - `New Password is not valid` — fails `valid_password()`.
        - `Missing Existing Webuzo Password Details.` — no Webuzo credential in `history_log` (contact support).
        - `Unable to update password. Please contact support team for further assistance.` — Webuzo API call failed.

        **Related calls:**
        - **OS root password instead:** `postVpsChangeRootPassword`, `postVpsResetPassword`.
        - **Account password rotation:** `updateAccountPassword`.
      operationId: postVpsChangeWebuzoPassword
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Rotate the Webuzo control panel admin password (re-auth required)
      tags:
      - VPS
  /vps/{id}/disable_cd:
    get:
      description: |
        Removes the virtual CD/DVD device from the VPS hardware configuration entirely — distinct from `doVpsEjectCd`, which only unmounts the ISO but leaves the drive attached. Queues a `disable_cd` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET. Reversible by attaching a new CD via `postVpsInsertCd`. Sibling ops: `doVpsEjectCd` (eject ISO but keep drive), `getVpsInsertCd` (list ISOs), `postVpsInsertCd` (mount ISO).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — allow ~2 minutes for hypervisor action.

        **Side effects:**
        - Inserts `vpsqueue` `disable_cd` row.
        - Calls `vps_resetup_vnc()` to refresh the VNC config.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Re-attach drive:** `postVpsInsertCd` (provide ISO URL).
        - **Just unmount the ISO:** `doVpsEjectCd`.
      operationId: doVpsDisableCd
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Remove the virtual CD/DVD device entirely from the VPS configuration
      tags:
      - VPS
  /vps/{id}/disable_quota:
    get:
      description: |
        Stops enforcing per-user disk quotas inside the VPS guest OS — useful when an application or user workflow conflicts with quota limits. Queues a `disable_quota` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET. Reversible via `doVpsEnableQuota`. Sibling op: `doVpsEnableQuota`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — allow ~2 minutes.

        **Side effects:**
        - Inserts `vpsqueue` `disable_quota` row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Re-enable:** `doVpsEnableQuota`.
      operationId: doVpsDisableQuota
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Disable per-user disk quota enforcement inside the VPS guest OS
      tags:
      - VPS
  /vps/{id}/eject_cd:
    get:
      description: |
        Unmounts whatever ISO is currently in the VPS virtual CD drive, leaving the drive attached so another ISO can be mounted. Distinct from `doVpsDisableCd` (which removes the drive itself). Queues an `eject_cd` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET. Sibling ops: `getVpsInsertCd` (list available ISOs), `postVpsInsertCd` (mount a different one), `doVpsDisableCd` (remove drive entirely).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — allow ~2 minutes.

        **Side effects:**
        - Inserts `vpsqueue` `eject_cd` row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Mount a different ISO:** `postVpsInsertCd`.
        - **Remove the drive entirely:** `doVpsDisableCd`.
      operationId: doVpsEjectCd
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Eject the mounted ISO from the VPS virtual CD drive (keep the drive)
      tags:
      - VPS
  /vps/{id}/enable_quota:
    get:
      description: |
        Turns on per-user disk-quota enforcement inside the VPS guest OS. Queues an `enable_quota` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET. Reversible via `doVpsDisableQuota`. Sibling op: `doVpsDisableQuota`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — allow ~2 minutes.

        **Side effects:**
        - Inserts `vpsqueue` `enable_quota` row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Disable later:** `doVpsDisableQuota`.
      operationId: doVpsEnableQuota
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Enable per-user disk quota enforcement inside the VPS guest OS
      tags:
      - VPS
  /vps/{id}/insert_cd:
    get:
      description: |
        Step 1 of the CD-mount flow. Returns the catalog of ISO images the customer can mount in the VPS virtual CD drive — typically rescue ISOs, OS installers, or recovery media. Read-only. Use to populate a CD/ISO picker before calling `postVpsInsertCd` with a chosen URL. Sibling ops: `postVpsInsertCd` (mount), `doVpsEjectCd` (unmount), `doVpsDisableCd` (remove drive), `doVpsRestart` (boot from mounted CD).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Object with available ISO templates (current implementation returns the platform's CD options).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Next:** `postVpsInsertCd` (provide `url` for the ISO to mount).
        - **Boot from CD:** `doVpsRestart` after mounting.
      operationId: getVpsInsertCd
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Available CD/ISO images for the VPS.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List ISO templates that can be mounted in the VPS virtual CD drive
      tags:
      - VPS
    post:
      description: |
        Mounts an ISO image in the VPS virtual CD drive from the supplied URL — used to boot into rescue media, run an OS installer, or temporarily attach removable media. Queues an `insert_cd` action on the hypervisor (`vpsqueue`) with the URL. After mounting, restart the VPS with `doVpsRestart` to boot from the CD. Sibling ops: `getVpsInsertCd` (list), `doVpsEjectCd` (unmount), `doVpsDisableCd` (remove drive), `doVpsRestart` (boot from CD).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `url` (string, required) — http(s):// URL to a `.iso` file accessible from the hypervisor.

        **Returns:** `{ text, queueId }` — allow ~2 minutes.

        **Side effects:**
        - Inserts `vpsqueue` `insert_cd` row with the URL.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **List options first:** `getVpsInsertCd`.
        - **Boot the ISO:** `doVpsRestart`.
        - **Unmount when done:** `doVpsEjectCd`.
      operationId: postVpsInsertCd
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/UrlRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/UrlRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Mount an ISO image in the VPS virtual CD drive from a URL
      tags:
      - VPS
  /vps/{id}/invoices:
    get:
      description: |
        Returns the billing history for one VPS — initial purchase invoice, monthly/period renewal invoices, addon invoices (extra IPs, additional disk space), and any prorated upgrade invoices for slice changes. Read-only. Backed by `Billing\InvoicesList::go()`. Use to render a per-VPS billing-history view, to find an unpaid invoice id to pass to `initiatePayment`, or to confirm a recent charge. Sibling ops: `getVpsInfo`, `getBillingInvoice` (single invoice detail), `initiatePayment`, `addVps` (creates the first invoice).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `ChargeInvoiceRows` — array of invoice rows with `id`, `amount`, `paid`, `description`, `date`, `due_date`, `currency`, `module=vps`, `service={id}`, and any addon-specific fields. Order is most-recent-first.

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service Passed` — `id` not owned by caller.

        **Related calls:**
        - **Single invoice detail:** `getBillingInvoice`.
        - **Pay an unpaid invoice:** `initiatePayment` (`GET /billing/pay/{method}/{invoices}`).
        - **All invoices across account:** `getBillingInvoices`.
      operationId: getVpsInvoices
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get VPS Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices associated with this specific VPS
      tags:
      - VPS
  /vps/{id}/reinstall_os:
    get:
      description: |
        Step 1 of the OS-reinstall flow. Returns the list of OS templates that can be installed on this specific VPS — filtered server-side by the VPS's backing hypervisor type (KVM, HyperV, OpenVZ, Virtuozzo) and by `template_available=1` (non-admin callers only see published templates). Use to populate the reinstall picker; the `template_file` from a chosen row is what `postVpsReinstallOs` accepts. **Reinstall destroys all data** — recommend a backup via `getVpsBackup` first. Sibling ops: `postVpsReinstallOs` (commit reinstall), `getVpsBackup` (snapshot first), `postVpsRestore` (restore from backup instead).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `VpsTemplatesList`:
        - `templates` (array) — one entry per available template:
          - `template_id` (integer)
          - `template_name` (string) — display name (e.g. `CentOS 7`).
          - `template_version` (string) — version (e.g. `7`).
          - `template_file` (string) — **canonical identifier** to pass to `postVpsReinstallOs` (e.g. `centos-7-x86_64.qcow2`).
          - `template_os` (string) — OS family tag.
          - `template_type` (integer) — internal hypervisor type id.
          - `template_available` (integer) — `1` for non-admin visible templates.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Snapshot before reinstall:** `getVpsBackup`.
        - **Commit reinstall:** `postVpsReinstallOs` (pass `template_file` + MyAdmin login password).
        - **Alternative — restore old backup:** `postVpsRestore`.
      operationId: getVpsReinstallOs
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsTemplatesList"
          description: VPS Reinstall OS info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List OS templates compatible with this VPS's hypervisor for reinstall
      tags:
      - VPS
    post:
      description: |
        **DESTRUCTIVE.** Wipes the VPS disk and reinstalls the chosen OS template. Re-authenticates via the customer's MyAdmin account password (`auth->authenticate` against `account_lid`+`localPassword`), updates the `vps` row (`vps_server_status='Reinstalling'`, `vps_os=<template>`), saves any new root password to `history_log`, and queues a `reinstall_os` action on the hypervisor (`vpsqueue`). **No rollback** — recover by restoring a backup via `postVpsRestore` (must have been created beforehand). Allow ~2 minutes for reinstall to start. Sibling ops: `getVpsReinstallOs` (list templates), `getVpsBackup` (snapshot before reinstalling), `postVpsRestore` (alternative — restore from backup instead).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields (required):**
        - `template` (string, required) — `template_file` from `getVpsReinstallOs.templates[].template_file`. Rejected if not found in `vps_templates` for the VPS's `template_type`.
        - `localPassword` (string, required) — the customer's current MyAdmin account password (re-auth check).

        **Body fields (optional):**
        - `password` (string, optional) — new root password to set during reinstall. If absent, the template default is used.

        **Returns:** `{ text: "Reinstall to has been sent to the server. Please allow up to 2 minutes for action to be completed." }`. If the VPS is `pending` rather than `active`, the OS selection is saved for activation and `{ text: "OS selection has been updated in our system for when the service is activated." }` is returned.

        **Side effects:**
        - Updates `vps_server_status` and `vps_os` columns.
        - Inserts new password into `history_log` when provided.
        - Inserts `vpsqueue` `reinstall_os` row (active services only).

        **Auth:** Session/API key plus re-auth via `localPassword`.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `Missing Account Password` — `localPassword` body field absent/empty.
        - `Invalid Account Password` — `localPassword` re-auth failed.
        - `This Template <name> does not exist` — `template` not found for this VPS's platform.

        **Related calls:**
        - **Prerequisite:** `getVpsReinstallOs` (find valid `template_file`).
        - **Recommended pre-step:** `getVpsBackup` (snapshot).
        - **Alternative (preserve state):** `postVpsRestore`.
        - **Verify after reinstall:** `getVpsInfo` (look for updated `vps_os`).
      operationId: postVpsReinstallOs
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/TemplateRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/TemplateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reinstall the VPS OS (DESTRUCTIVE — wipes disk; requires re-auth)
      tags:
      - VPS
  /vps/{id}/reset_password:
    get:
      description: |
        Step 1 of the random-root-password reset flow. Validates ownership and active status; the response describes the reset behavior (currently a passthrough — full implementation reserved for future policy fields). Read-only. Use to confirm the VPS exists and is active before triggering the reset via `postVpsResetPassword`. For a customer-chosen password use `postVpsChangeRootPassword` instead. Sibling ops: `postVpsResetPassword`, `postVpsChangeRootPassword`, `postVpsChangeWebuzoPassword`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Object with reset options (reserved for policy info).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Next (random):** `postVpsResetPassword`.
        - **Specific password instead:** `postVpsChangeRootPassword`.
      operationId: getVpsResetPassword
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Password reset requirements and current state.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Pre-flight check before resetting the VPS root password to a random
        value
      tags:
      - VPS
    post:
      description: |
        Resets the VPS root/Administrator password to a fresh random value generated on the hypervisor — typical when the customer has lost the existing password and cannot recover it. Queues a `reset_password` action on the hypervisor (`vpsqueue`). The new password is delivered via the standard hypervisor password-change channel (logged to `history_log` and surfaced through `getVpsWelcomeEmail` / dashboard credential views). **No rollback** — to undo, set a new password via `postVpsChangeRootPassword` or call `postVpsResetPassword` again. Sibling ops: `getVpsResetPassword`, `postVpsChangeRootPassword`, `postVpsChangeWebuzoPassword`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None — password is generated server-side.

        **Returns:** `{ text, queueId }` — allow ~2 minutes.

        **Side effects:**
        - Inserts `vpsqueue` `reset_password` row; hypervisor agent generates and applies the new password.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Specific password instead:** `postVpsChangeRootPassword`.
        - **Retrieve the new credentials:** `getVpsWelcomeEmail` (resends with current credentials).
      operationId: postVpsResetPassword
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reset the VPS root password to a server-generated random value
      tags:
      - VPS
  /vps/{id}/restart:
    get:
      description: |
        Reboots the VPS — typically after a kernel update, configuration change, or to recover from an unresponsive state. **Preferred over `doVpsStop` followed by `doVpsStart`** because it preserves the boot context and lets the hypervisor handle the sequence atomically. Queues a `restart` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET. Idempotent in effect — restarting a running VPS reboots it, restarting a stopped VPS starts it. Sibling ops: `doVpsStart`, `doVpsStop`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — allow ~2 minutes; poll `getVpsInfo` to confirm state.

        **Side effects:**
        - Inserts `vpsqueue` `restart` row.
        - Calls `vps_resetup_vnc()` to refresh the VNC config.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active` — also returned for `cancelled`/`suspended` services.

        **Related calls:**
        - **Verify state:** `getVpsInfo`.
        - **Power off only:** `doVpsStop`.
        - **Power on:** `doVpsStart`.
        - **Boot from CD:** `postVpsInsertCd` then `doVpsRestart`.
      operationId: doVpsRestart
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Reboot the VPS — preferred over stop+start for software changes
      tags:
      - VPS
  /vps/{id}/reverse_dns:
    get:
      description: |
        Returns the PTR/reverse-DNS hostname currently resolving for every IP attached to the VPS — primary `vps_ip` plus any extras purchased via `postVpsBuyIp`. PTRs are read live via `get_hostname()` (DNS lookup), not cached. Read-only. Sibling ops: `postVpsReverseDns` (update entries), `getVpsBuyIp` (add more IPs first), `postVpsChangeHostname` (auto-updates PTR for the primary IP).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `ReverseDnsEntries` — `{ ips: { "<ip>": "<ptr-hostname>", ... } }`. Empty string for IPs with no PTR set.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Update PTRs:** `postVpsReverseDns`.
        - **Add IPs first:** `getVpsBuyIp` → `postVpsBuyIp`.
        - **Hostname change (auto-PTR for primary):** `postVpsChangeHostname`.
      operationId: getVpsReverseDns
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReverseDnsEntries"
          description: VPS Reverse DNS info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read the current PTR (reverse-DNS) records for every IP on the VPS
      tags:
      - VPS
    post:
      description: |
        Sets the PTR hostname for each VPS IP — bulk update via the `ips` map. Calls `reverse_dns($ip, $newHostname)` for every IP in the body whose value differs from the current PTR; IPs not currently attached to the VPS are silently ignored. Propagation depends on the reverse-zone TTL but is typically minutes, not instant. Sibling ops: `getVpsReverseDns`, `postVpsChangeHostname` (auto-sets PTR for primary IP), `getVpsBuyIp`/`postVpsBuyIp` (add more IPs first).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `ips` (object, required) — `{ "<ip>": "<new-hostname>", ... }`. Only IPs that already belong to the VPS will be updated; others are ignored. Empty-string values are skipped.

        **Returns:** `{ message: "DNS Updated", success: true }`.

        **Side effects:**
        - One `reverse_dns()` call per IP whose value changed.
        - PTR records are written to the in-addr.arpa zone; propagation depends on TTL.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Read current PTRs first:** `getVpsReverseDns`.
        - **For the primary IP:** `postVpsChangeHostname` (renames the VPS hostname and auto-PTRs in one go).
        - **Adding IPs:** `getVpsBuyIp` → `postVpsBuyIp` → `postVpsReverseDns`.
      operationId: postVpsReverseDns
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Update VPS Reverse DNS response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Bulk-update PTR (reverse-DNS) records for one or more VPS IPs
      tags:
      - VPS
  /vps/{id}/setup_vnc:
    get:
      description: |
        Returns the VNC IP/port the customer can connect to for an out-of-band console (KVM-style remote console — useful when SSH/RDP is unavailable, during boot, or for rescue work). Read-only. **Note:** the current implementation is a stub for some platforms — if you get an empty response, call `postVpsSetupVnc` to (re)provision the VNC endpoint, then call this again. Sibling ops: `postVpsSetupVnc`, `getVpsViewDesktop` (Windows GUI access via RDP/HTML5).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Object with VNC connection info (IP, port, credentials when provisioned).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Provision/refresh VNC:** `postVpsSetupVnc`.
        - **Windows remote desktop:** `getVpsViewDesktop`.
      operationId: getVpsSetupVnc
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get VPS Setup VNC Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read current VNC console connection info for the VPS
      tags:
      - VPS
    post:
      description: |
        (Re)establishes out-of-band VNC console access on the VPS — typically after a network change, hypervisor migration, or when troubleshooting boot issues. Validates the supplied VNC IP via `validIp()`, persists it to the `vps` row (`vps_vnc` column), and queues a `setup_vnc` action on the hypervisor (`vpsqueue`). Sibling ops: `getVpsSetupVnc` (read current), `getVpsViewDesktop` (Windows GUI/RDP path).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `vnc` (string, required) — IPv4 address for the VNC endpoint. Validated by `validIp()`.

        **Returns:** `{ text, queueId }` — allow ~2 minutes for the hypervisor to bring up the listener.

        **Side effects:**
        - Updates `vps.vps_vnc` to the new IP.
        - Inserts `vpsqueue` `setup_vnc` row.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `Invalid IP "<value>" or is blank` — `vnc` failed `validIp()`.

        **Related calls:**
        - **Read after provisioning:** `getVpsSetupVnc`.
        - **Windows GUI access:** `getVpsViewDesktop`.
      operationId: postVpsSetupVnc
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Provision or refresh the VNC console endpoint for the VPS
      tags:
      - VPS
  /vps/{id}/slices:
    get:
      description: |
        Step 1 of the slice upgrade/downgrade flow. A "slice" bundles RAM, disk, and CPU on the VPS — the smallest unit of vertical scaling. Returns the current slice count (`vps_slices`), the range available (`min_slices = current`, `max_slices = host capacity`), the per-slice recurring cost (after coupon and frequency discount), the prorated cost for the remainder of the current cycle, and the underlying resource units (`slice_ram` in MB, `slice_hd` in GB). Read-only. Use to render an upgrade picker before calling `postVpsSlices`. Sibling ops: `postVpsSlices` (commit), `postVpsBuyHdSpace` (disk-only addon).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:**
        - `min_slices` (integer) — current allocation; floor for downgrades is also this value (downgrade decreases from here).
        - `max_slices` (integer) — host-capacity-limited upper bound (`get_vps_max_slices()`).
        - `slice_cost` (float) — per-slice recurring cost in customer currency.
        - `prorated_slice_cost` (float) — prorated cost for the remainder of the current billing cycle.
        - `frequency` (integer) — billing cycle in months (1/6/12/24/36).
        - `slice_ram` (integer) — RAM (GB) per slice.
        - `slice_hd` (integer) — disk (GB) per slice. For KVM Storage (service-type 57) this comes from `services_field2.slice_hd`.
        - `vps_slices` (integer) — current count.
        - `vps_cost` (float) — current monthly cost.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Next:** `postVpsSlices` (commit; creates prorated invoice for upgrades, opens ticket for active services).
        - **Disk-only:** `getVpsBuyHdSpace`/`putVpsBuyHdSpace`/`postVpsBuyHdSpace`.
      operationId: getVpsSlices
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: VPS Slices information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read current slice count, min/max range, and prorated per-slice upgrade\
        \ cost"
      tags:
      - VPS
    post:
      description: |
        Changes the VPS's slice count — bumps or shrinks CPU/RAM/disk allocation. Deletes any unpaid prior slice-upgrade invoices for this VPS, updates the recurring `repeat_invoices` row to the new slice count's cost, creates a one-off `invoices` row prorated for the rest of the cycle, and either auto-opens a support ticket (active services — slice upgrades typically need a manual hypervisor action) or queues `set_slices` directly. Downgrades complete free and immediately (`paid=1` zero-amount invoice). **Real money** on upgrades. Sibling ops: `getVpsSlices`, `postVpsBuyHdSpace`, `initiatePayment`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `slices` (integer, required) — target slice count. `min_slices ≤ slices ≤ max_slices` per `getVpsSlices`. Must differ from current.

        **Returns:**
        - **Upgrade with payment owed:** `{ text: "Thank you for your upgrade request. Kindly Pay the invoice to activate the upgrade.", invoice: <integer> }` — pay via `initiatePayment`.
        - **Active VPS, upgrade queued via ticket:** `{ text: "Thank you for your upgrade request. A ticket has been automatically opened for you. Please allow us 24 hours to complete your upgrade. ..." }`.
        - **Downgrade:** `{ text: "You have downgraded N slices from your VPS, the changes will be apply shortly." }`.

        **Side effects:**
        - Deletes unpaid `N Slice Upgrade for VPS {id}` invoices.
        - Updates `repeat_invoices` recurring cost and description (`<plan_name> N Slices`).
        - Creates a new `invoices` row for the prorated upgrade amount (or `paid=1` zero-amount for downgrades).
        - Updates `vps.vps_slices` and `vps.vps_currency`.
        - **Active VPS:** opens a support ticket via `create_ticket()` so an operator can resize manually, OR (when `$deferUpgradeViaTicket` is false) inserts `vpsqueue` `set_slices` row.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `Must pass the slices field.` — body field missing.
        - `Invalid Slices. You requested N but that amount is not available currently.` — out of range.
        - `No Changes were made, Please Try Again!` — equals current.
        - `Please activate the service first.` — VPS not in `pending`/`active`.

        **Related calls:**
        - **Prerequisite:** `getVpsSlices` (read range + prorated cost).
        - **Pay the upgrade invoice:** `initiatePayment` with the returned `invoice` id.
        - **Disk-only addon:** `postVpsBuyHdSpace`.
        - **Verify:** `getVpsInfo`.
      operationId: postVpsSlices
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Update VPS slices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Upgrade or downgrade the VPS slice count (creates prorated invoice
        on upgrade)
      tags:
      - VPS
  /vps/{id}/start:
    get:
      description: |
        Powers on a stopped VPS. Queues a `start` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET, idempotent in practice — calling on an already-running VPS is a no-op at the hypervisor. The `vps_status` field at the service level remains `active` (status reflects billing/lifecycle, not running power state); use `getVpsTrafficUsage` or external monitoring to confirm the VPS is actually up. Sibling ops: `doVpsStop`, `doVpsRestart`, `getVpsInfo`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }` — `queueId` references the `queue_log` entry. Allow ~10–30 s for the hypervisor to act.

        **Side effects:**
        - Inserts `vpsqueue` `start` row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active` — service is `cancelled`, `suspended` (non-payment), or `pending`. Resolve via payment (`initiatePayment`) for `suspended`, or contact support for `cancelled`/`pending` issues.

        **Related calls:**
        - **Reboot instead:** `doVpsRestart` (preferred over stop+start).
        - **Power off:** `doVpsStop`.
        - **Current state:** `getVpsInfo`.
      operationId: doVpsStart
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Power on a stopped VPS instance
      tags:
      - VPS
  /vps/{id}/stop:
    get:
      description: |
        Halts the VPS without rebooting — typical before manually triggering a snapshot, freeing hypervisor resources, or temporarily taking a workload offline. Queues a `stop` action on the hypervisor (`vpsqueue`) and triggers a VNC re-setup. Side-effecting GET, idempotent — stopping an already-stopped VPS is a no-op. **Billing continues while the VPS is stopped** — to stop both the workload and billing, use `VPSCancel`. Sibling ops: `doVpsStart`, `doVpsRestart`, `VPSCancel`, `getVpsBackup` (snapshot first).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `{ text, queueId }`. Allow ~10–30 s.

        **Side effects:**
        - Inserts `vpsqueue` `stop` row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Reboot instead:** `doVpsRestart` (preferred for software changes).
        - **Power on later:** `doVpsStart`.
        - **Snapshot first:** `getVpsBackup`.
        - **Stop billing too:** `VPSCancel`.
      operationId: doVpsStop
      parameters:
      - description: VPS ID number.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Power off a running VPS — billing continues until cancellation
      tags:
      - VPS
  /vps/{id}/traffic_usage:
    get:
      description: |
        Returns bandwidth-consumption data for the VPS — inbound/outbound bytes per day and aggregated totals against the plan's `bwSlice` × `vps_slices` allowance. Pulled via `vps_bandwidth_data($vps_id)` from the bandwidth-tracking subsystem. Read-only. For custom date-range or granularity filters use `postVpsTrafficUsage` (currently mirrors GET behavior but reserved for filter parameters). Sibling ops: `postVpsTrafficUsage`, `getVpsInfo` (BW allowance fields are shown there).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `VpsTrafficResponse` — array/object with bandwidth usage points (timestamp, inbound bytes, outbound bytes, totals).

        **Auth:** Session/API key. Ownership enforced via parent VPS.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.

        **Related calls:**
        - **Filtered query:** `postVpsTrafficUsage` (reserved for date-range filters).
        - **Plan allowance:** `getVpsInfo` returns `bw_total` / `slices` you can compute against.
      operationId: getVpsTrafficUsage
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VpsTrafficResponse"
          description: Get VPS Traffic usage
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read bandwidth traffic usage data for the VPS
      tags:
      - VPS
    post:
      description: |
        Filtered variant of the bandwidth-usage endpoint — reserved for date-range and granularity filters. The current implementation mirrors `getVpsTrafficUsage` behavior and returns the full dataset; the body shape is reserved for future filter parameters (start/end date, day/hour granularity). Sibling op: `getVpsTrafficUsage`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** Filter fields (reserved — date range, granularity).

        **Returns:** Same `VpsTrafficResponse` shape as `getVpsTrafficUsage`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.

        **Related calls:**
        - **Unfiltered alternative:** `getVpsTrafficUsage`.
      operationId: postVpsTrafficUsage
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Filtered traffic usage search results.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Search/filter VPS bandwidth usage with custom criteria (reserved)
      tags:
      - VPS
  /vps/{id}/view_desktop:
    get:
      description: |
        Returns remote-desktop connection details for a Windows VPS or any VPS with a GUI session — IP, port, recommended client, and the `client_links` the dashboard surfaces for launching the session. Backed by `ViewVPS::getDetails()`; response shape mirrors `getVpsInfo` minus the `admin_links` block. Sibling ops: `postVpsViewDesktop` (refresh), `getVpsSetupVnc`/`postVpsSetupVnc` (low-level VNC console).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** Same shape as `getVpsInfo` (sans `admin_links`) — includes `client_links` with RDP/HTML5 launch URLs.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Refresh session:** `postVpsViewDesktop`.
        - **Low-level console:** `getVpsSetupVnc` / `postVpsSetupVnc`.
      operationId: getVpsViewDesktop
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Get VPS View Desktop Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read remote-desktop (RDP/HTML5) connection info for a Windows/GUI VPS
      tags:
      - VPS
    post:
      description: |
        Force-refreshes the remote-desktop connection metadata — typically called after a hostname change (`postVpsChangeHostname`), IP addition (`postVpsBuyIp`), or reverse-DNS update (`postVpsReverseDns`) so the dashboard can re-fetch via its form-action pattern. Returns the same payload as `getVpsViewDesktop`. Sibling op: `getVpsViewDesktop`.

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None (form-action pattern).

        **Returns:** Same shape as `getVpsViewDesktop`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.

        **Related calls:**
        - **Read without refresh:** `getVpsViewDesktop`.
        - **Common triggers:** `postVpsChangeHostname`, `postVpsBuyIp`, `postVpsReverseDns`.

        Path param: `id` (integer). No body.

        Returns the VPS view-desktop payload. Errors: HTTP 404 wrong owner, HTTP 409 not `active`. Sibling:
        `getVpsViewDesktop`, `postVpsSetupVnc`.
      operationId: postVpsViewDesktop
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          description: Submit VPS View Desktop Information
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Refresh the remote-desktop session connection info after IP/hostname
        changes
      tags:
      - VPS
  /websites:
    get:
      description: |
        Enumerates every shared/reseller hosting account ("website") owned by the authenticated customer. The canonical entry point for discovering a `website_id` to pass into other webhosting endpoints. Filtered server-side by `website_custid = session account_id` — cross-customer leaks are not possible. Empty array means the account has no websites (not an error). Sibling ops: `getWebsiteInfo`, `getWebsitesLogin`, `getWebsitesBackups`, `getWebsiteInvoices`, `webhostingCancel`, `getNewWebsite` (order a new one).

        **Path/Query/Body:** None.

        **Returns:** Array of `WebsiteRow` — per-website summary:
        - `website_id` (integer) — canonical id used in `/websites/{id}/*` paths.
        - `website_hostname` (string) — primary FQDN.
        - `website_status` (string enum) — `pending` / `active` / `pending-cancel` / `canceled`.
        - `services_name` (string) — plan/package label (e.g. `Standard`, `Reseller`).
        - `repeat_invoices_cost` (decimal) — current recurring cost in the website's billing currency.
        - `website_comment` (string|null) — customer-provided note.

        **Auth:** Session/API key. Ownership filter enforced via `website_custid`.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Per-website detail:** `getWebsiteInfo` (full).
        - **Control panel:** `getWebsitesLogin` (auto-login URL).
        - **Backups + restore points:** `getWebsitesBackups`.
        - **Billing:** `getWebsiteInvoices`.
        - **Order a new site:** `getNewWebsite` → `putWebsites` → `addWebsite`.
        - **Cancel:** `webhostingCancel`.
      operationId: getWebsiteList
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/WebsiteRow"
                type: array
            multipart/form-data:
              schema:
                items:
                  $ref: "#/components/schemas/WebsiteRow"
                type: array
          description: The listing of `Websites` services on your account.
          links:
            getWebsiteById:
              description: "The `website_id` returned in each row can be used as the\
                \ `id` parameter in `GET /websites/{id}` to retrieve full service\
                \ details."
              operationId: getWebsiteInfo
              parameters:
                id: $response.body#/0/website_id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List the caller's webhosting (cPanel/DirectAdmin/Plesk/Webuzo) services
      tags:
      - Webhosting
  /websites/order:
    get:
      description: |
        Step 1 of the webhosting order flow. Returns the full ordering catalog needed to build a valid order: available `serviceTypes` (plans), `serviceOffers` (promotional bundles), `packages`, billing `period` options, the customer's currency symbol, default `serviceOfferId`, and `enableDomainRegistering` (whether free/paid domain registration is bundled). Read-only — no service or invoice created. Sibling discovery endpoints in other modules: `getNewVps`, `getNewMail`, `getNewDomain`. Sibling order-flow ops: `putWebsites`, `addWebsite`.

        **Path/Query/Body:** None.

        **Returns** (schema `WebsitesOrder`):
        - `currencySymbol` (string) — locale currency symbol for display.
        - `step` (integer) — current step in the multi-step order wizard.
        - `website` (integer) — pre-selected default plan id.
        - `period` (integer) — pre-selected default billing frequency.
        - `serviceOfferId` (integer) — pre-selected promo offer.
        - `serviceTypes` (array) — every plan; `services_ourcost` stripped server-side. Keys per row: `services_id`, `services_name`, `services_cost`, `services_type` (`WEB_CPANEL` / `WEB_DIRECTADMIN` / `WEB_PLESK` / `WEB_VESTA` / `WEB_PPA` / `WEB_WORDPRESS` / `WEB_STORAGE`), etc.
        - `serviceOffers` (array) — current promotional bundles.
        - `packages`, `packges` (array — legacy field name preserved alongside `packages`).
        - `enableDomainRegistering` (bool) — when `true`, the order can also register/transfer a domain.
        - `jsonServices`, `jsonServiceOffers` (string) — JSON-encoded copies for inline use in HTML.

        **Auth:** Session/API key.

        **Errors:**
        - `401` — unauthenticated.

        **Related calls:**
        - **Next:** `putWebsites` (validate + quote — no charge), `addWebsite` (place order).

        **Example abridged response:**
        ```json
        {
          "currencySymbol": "$",
          "step": 1,
          "website": 23,
          "period": 1,
          "enableDomainRegistering": true,
          "serviceTypes": [
            {"services_id": 23, "services_name": "Standard", "services_cost": 8.00, "services_type": 1},
            {"services_id": 25, "services_name": "Reseller", "services_cost": 24.95, "services_type": 1}
          ]
        }
        ```
      operationId: getNewWebsite
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebsitesOrder"
          description: Website ordering details including available plans and pricing.
          links:
            validateWebsiteOrder:
              description: "After reviewing available plans, use `PUT /websites/order`\
                \ to validate your order before placing it."
              operationId: putWebsites
            placeWebsiteOrder:
              description: Use `POST /websites/order` to place an order for a new
                webhosting package.
              operationId: addWebsite
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read the webhosting order catalog — plans, packages, promo offers,\
        \ pricing"
      tags:
      - Webhosting
    post:
      description: |
        Step 3 of the webhosting order flow — actually places the order. Revalidates via `validate_buy_website()` (same checks as `putWebsites`), then calls `place_buy_website()` to allocate a backing webhosting server, create the `webhosting` service row in `pending` status, generate a `Repeat_Invoice` recurring billing row, produce an initial `invoices` row, and (when `registerDomain=true`) also kick off a domain order with its own invoice. The activator runs once the invoice is paid; `getWebsitesWelcomeEmail` then fires automatically with control-panel credentials. **Real money** — call `putWebsites` first to preview cost. Sibling ops: `getNewWebsite`, `putWebsites`, `getWebsiteInfo`, `webhostingCancel`.

        **Body fields:** Identical to `putWebsites`. Required: `hostname`, `packageId`. Optional: `rootpass` (auto-generated if blank), `period`, `coupon`, `serviceOfferId`, `script`, `comment`, `registerDomain`.

        **Returns** (schema `ServiceOrderPostResponse`):
        - `total_cost` (string/decimal) — total to pay across all generated invoices.
        - `iid` (string) — primary invoice id (numeric).
        - `iids` (array) — tagged invoice ids (e.g. `SERVICEwebhosting12345`).
        - `real_iids` (array) — numeric invoice ids to pass to `initiatePayment`.
        - `serviceId` (integer) — new `website_id`; use with `getWebsiteInfo` to poll status.
        - `invoice_description` (string) — human-readable summary.
        - `cj_params` (object) — Commission Junction tracking parameters.

        **Side effects:**
        - Inserts `webhosting` service row (`website_status='pending'`).
        - Inserts `repeat_invoices` row for recurring charge.
        - Inserts `invoices` row for the first period.
        - When `registerDomain=true`: also creates a domain service row and its own invoice (`domain_serviceid`, `diid` returned alongside).
        - Hashes/encrypts `rootpass` to `history_log`.

        **Auth:** Session/API key.

        **Errors:**
        - When validation fails: response is the same `errors` array from `putWebsites` (HTTP 200 with `continue=false` shape).
        - `401` — unauthenticated.

        **Related calls:**
        - **Prerequisite:** `getNewWebsite`, `putWebsites`.
        - **Next:** `getBillingInvoice` (confirm), `initiatePayment` (pay with `real_iids`), then poll `getWebsiteInfo` until `website_status=='active'`.
        - **Resend credentials after activation:** `getWebsitesWelcomeEmail`.
        - **Cancel before paying:** `webhostingCancel` (or `deleteBillingInvoice` for the pending invoice).

        **Full ordering happy path:**
        ```text
        GET /websites/order                              -> catalog (getNewWebsite)
        PUT /websites/order { ...config }                -> price quote (putWebsites)
        POST /websites/order { ...config }               -> { serviceId, real_iids } (addWebsite)
        GET /billing/invoices/{iid}                      -> confirm invoice (getBillingInvoice)
        GET /billing/pay/cc/{real_iids[0]}               -> pay (initiatePayment)
        GET /websites/{serviceId}                        -> poll until website_status=="active"
        GET /websites/{serviceId}/welcome_email          -> resend credentials if needed
        ```
      operationId: addWebsite
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebsiteOrderPostRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceOrderPostResponse"
          description: "Order placed successfully. Use the invoice ID to proceed to\
            \ payment via `/billing/pay/{method}/{invoices}` or view the invoice at\
            \ `/billing/invoices/{id}`."
          links:
            InitiatePayment:
              description: Use the invoice ID from the order response to retrieve
                invoice details or proceed to payment.
              operationId: getBillingInvoice
              parameters:
                id: $response.body#/iid
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Place a new webhosting order, create the invoice, and queue provisioning"
      tags:
      - Webhosting
      x-payment-info:
        intent: session
        method: card
        amount: 0
        currency: USD
        description: Order a shared or reseller web hosting account. Returns invoice
          IDs for checkout.
        method_options:
        - cc
        - paypal
        - btcpay
        - coinbase
        - payu
        - ccavenue
        - cashfree
        - payssion
        - prepay
    put:
      description: |
        Step 2 of the webhosting order flow. Dry-runs the order through `validate_buy_website()`: checks `hostname` against `valid_hostname()` and the keyword blocklist, validates `packageId` against the customer's plan eligibility, confirms the chosen plan's hypervisor pool is in stock (`OUTOFSTOCK_WEBHOSTING_*` constants), applies any coupon and frequency discount, and returns a cost preview plus any validation errors. No invoice or service record is created. **Always call before `addWebsite`** to surface coupon/pricing/hostname problems cheaply. Sibling ops: `getNewWebsite` (catalog), `addWebsite` (place order).

        **Body fields (form or JSON):**
        - `hostname` (string, required) — primary FQDN for the website. Must pass `valid_hostname()`, must not contain `interserver.net` (non-admin), must not be on the blocked-keyword list, must match the plan's TOS rules.
        - `rootpass` (string, optional) — control-panel admin password; if blank, a random 8-char password is generated server-side via `generateRandomString(8,1,1,1,1)`.
        - `packageId` (integer, required) — plan id from `getNewWebsite.serviceTypes[].services_id`. Must have `services_module='webhosting'` and `services_buyable=1` (non-admin).
        - `period` (integer, optional, default 1) — billing cycle in months: 1 / 6 / 12 / 24 / 36. Same frequency discounts as VPS apply.
        - `coupon` (string, optional) — coupon code.
        - `serviceOfferId` (integer, optional) — promo bundle from `getNewWebsite.serviceOffers`.
        - `script` (integer, optional, default 0) — auto-installer id (Softaculous/WordPress/etc., 0 = none).
        - `comment` (string, optional) — free-form note saved on the service row.
        - `registerDomain` (bool, optional) — when `true` and `enableDomainRegistering=true` from the catalog, also registers/transfers the domain through the order.
        - Implicit: TOS acceptance (validated via `tos='yes'` in source — required for non-admin).

        **Returns** (validation envelope):
        - `continue` (bool) — `true` if the order can safely be POSTed.
        - `errors` (array of strings) — human-readable validation messages.
        - `frequency` (integer) — resolved billing frequency.
        - `coupon` (string) — the applied coupon name (echoed).
        - `couponCode` (integer) — the matched coupon row id, or `0` if none.
        - `serviceType` (integer) — resolved plan id.
        - `serviceCost` (float) — first-period total cost (includes coupon + period discount).
        - `originalCost` (float) — undiscounted reference.
        - `repeatServiceCost` (float) — recurring cost after discounts.
        - `hostname`, `password` (string) — final sanitized values (may differ from input — e.g. random password generated).
        - `introFrequency` (integer) — first-period bonus length (intro pricing).

        **Side effects:** None — pure read.

        **Auth:** Session/API key.

        **Errors (within `errors` array, `continue=false`):**
        - `Invalid Billing Interval` — `period` not numeric.
        - `All webhosting servers are currently full.` — `OUTOFSTOCK_WEBHOSTING`.
        - `Invalid Package Specified.` — plan id not in the webhosting module or not buyable.
        - `Our <Plan> Webhosting Servers are currently full.` — plan-specific stock check.
        - `The hostname cannot contain interserver.net`.
        - `Hostname "<x>" Contains Invalid Characters Or Is Blank`.
        - `Hostname contains a blocked keyword.`.
        - `You must agree to the terms of service and click the checkbox saying so.`.
        - `Invalid Coupon Specified` — coupon not usable for this plan/customer.

        Top-level HTTP errors: `401` unauthenticated.

        **Related calls:**
        - **Prerequisite:** `getNewWebsite` (catalog).
        - **Next:** `addWebsite` (same body — actually places the order).

        **Example request body:**
        ```json
        {
          "hostname": "mystore.example.com",
          "rootpass": "Sup3rS3cret!",
          "packageId": 23,
          "period": 12,
          "coupon": "",
          "registerDomain": false,
          "script": 0
        }
        ```
      operationId: putWebsites
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebsiteOrderPutRequest"
        required: true
      responses:
        "200":
          description: Validate Website order response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Validate a webhosting order and preview cost — dry run, no charge"
      tags:
      - Webhosting
  /websites/{id}:
    delete:
      description: |
        **DESTRUCTIVE.** Schedules the website for cancellation via the shared `Billing\CancelService::go($id)` flow with `module='webhosting'`. Marks the service `pending-cancel`, halts the recurring invoice, and queues deprovisioning so cPanel/DirectAdmin/Plesk/Webuzo removes the account and **all hosted files, databases, mailboxes, and DNS** at end-of-cycle. **There is no client-side restore** — take a cpmove backup via `getWebsitesBackups` first (with `download=<name>`) if data must be preserved. Sibling ops: `getWebsitesBackups`, `getWebsiteInfo` (verify status flipped), `getWebsiteInvoices`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:** `WebsiteCancelResponse` — cancel-service confirmation payload.

        **Side effects:**
        - Sets `website_status='pending-cancel'`.
        - Marks the `repeat_invoices` row as non-renewing.
        - Logs the cancellation in `history_log`.
        - Queues deprovisioning to run at end-of-cycle (the cPanel/DA/Plesk account, all hosted files, databases, email accounts, and DNS will be removed).
        - Customer retains panel access until the cycle ends.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — service in a state that cannot be cancelled (already `canceled`, etc.).

        **Related calls:**
        - **Before cancelling:** `getWebsitesBackups` (download a cpmove archive — irretrievable after deprovisioning).
        - **After cancelling:** `getWebsiteInfo` (confirm `pending-cancel`), `getWebsiteInvoices` (final invoices).
        - **Sibling cancels on other modules:** `VPSCancel`, `CancelDomain`, `mailCancel`, etc. all use the same `CancelService` handler.
      operationId: webhostingCancel
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        examples:
          WebsiteExample:
            value: "123"
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/webhostingCancel_200_response"
          description: Website cancel
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Schedule termination of a webhosting service — wipes panel account
        at cycle end
      tags:
      - Webhosting
    get:
      description: |
        Returns everything the customer dashboard shows for one website — status, hostname, control-panel username, primary IP, host server, plan, billing summary, action `client_links`, and supported addons. Read-only. Backed by `ViewWebsite::getDetails()`. Internal `admin_links`, `settings`, `csrf`, and `serviceMaster.website_key` (the API key) are stripped before return. Use to render a website detail page, verify ownership before mutating, or poll `website_status` after `addWebsite`. Sibling ops: `getWebsiteList`, `getWebsitesLogin`, `getWebsitesBackups`, `getWebsiteInvoices`, `webhostingCancel`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns** (schema `Website`):
        - `serviceInfo` — `website_id`, `website_hostname`, `website_username`, `website_ip`, `website_server`, `website_type` (plan id), `website_status`, `website_comment`.
        - `serviceMaster` — host-server row (cPanel/DA/Plesk hostname, panel URL). `website_key` is stripped.
        - `serviceType` — plan row (`services_ourcost` stripped).
        - `client_links` (array) — `{name, link, icon}` for restart, login, backup, etc. Internal `?link=queue&action=...` URLs are pre-resolved to plain action names.
        - `serviceAddons` — extra IPs, additional resources.

        **Auth:** Session/API key. Ownership enforced via `website_custid`.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` (legacy text — webhosting reuses the helper) — `id` not owned by caller.

        **Related calls:**
        - **Login to panel:** `getWebsitesLogin` (auto-login URL).
        - **Backups + restore:** `getWebsitesBackups`.
        - **Billing:** `getWebsiteInvoices`.
        - **Reverse DNS:** `gettWebsiteReverseDns`, `postWebsitesReverseDns`.
        - **Buy extra IP:** `getWebsiteBuyIp`, `postWebsiteBuyIp`.
        - **Migration:** `postWebsiteMigration`.
        - **Resend welcome email:** `getWebsitesWelcomeEmail`.
        - **Cancel:** `webhostingCancel`.
      operationId: getWebsiteInfo
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Website"
          description: Website details
          links:
            getWebsiteInvoices:
              description: Retrieve invoices for this website service.
              operationId: getWebsiteInvoices
              parameters:
                id: $request.path.id
            getWebsiteLogin:
              description: Get an auto-login URL for the webhosting control panel.
              operationId: getWebsitesLogin
              parameters:
                id: $request.path.id
            getWebsiteBackups:
              description: List available backups for this website.
              operationId: getWebsitesBackups
              parameters:
                id: $request.path.id
            cancelWebsite:
              description: Cancel this website service.
              operationId: webhostingCancel
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read full configuration and status detail for one webhosting service
      tags:
      - Webhosting
    post:
      description: |
        POST mutation hook for the website detail page. The implementation currently routes through the same `View::go()` handler as `getWebsiteInfo`; concrete update behavior depends on which `client_links` action the form is driving. **For specific changes, prefer the dedicated endpoints** — they enforce field-level validation and queue the correct hypervisor/panel actions. Sibling ops: `getWebsiteInfo`, all dedicated mutation endpoints below.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** Form-encoded fields appropriate to the `client_links` action being driven.

        **Returns:** `SuccessTextResponse` — `{text: "..."}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.

        **Prefer these dedicated endpoints:**
        - **Buy a paid IP or update reverse DNS:** `postWebsiteBuyIp` (the latter via `action=reverse_dns`).
        - **PTR-only changes:** `postWebsitesReverseDns`.
        - **Migrate site from another host:** `postWebsiteMigration`.
        - **Resend control-panel credentials:** `getWebsitesWelcomeEmail`.
        - **Auto-login to cPanel/DA/Plesk:** `getWebsitesLogin`.
        - **Cancel:** `webhostingCancel`.
      operationId: updateWebsiteInfo
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: POST mutation hook for the website detail page (use dedicated ops where
        possible)
      tags:
      - Webhosting
  /websites/{id}/buy_ip:
    get:
      description: |
        Combined IP/billing view for a website: returns the primary `website_ip` plus any addon extras, each mapped to its current PTR hostname (via `get_hostname()`), the list of existing additional-IP repeat invoices (with `cancel_link` URLs), the count of paid extras, and the per-IP cost (in the website's billing currency, falling back to USD/`WEBSITE_IP_COST`). Read-only. Use to populate a "buy another IP" form or to audit current IP allocations. Sibling ops: `postWebsiteBuyIp` (buy or update PTR), `gettWebsiteReverseDns` (PTR-only view), `postWebsitesReverseDns` (PTR-only update).

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:**
        - `ips` (object) — `{"<ipv4>": "<ptr-hostname>", ...}` for every IP attached.
        - `ipsDetails` (array) — existing addon invoices with each row's `ip`, `cancel_link` (`cancel_addon?module=webhosting&r=<rid>`), invoice metadata.
        - `ipCount` (integer) — count of paid addon IPs.
        - `ipCost` (float) — per-IP recurring cost in `currency`.
        - `currency` (string), `currencySymbol` (string).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Website Passed` — `id` not owned by caller.
        - `409 Website is not active` — `website_status != "active"`.

        **Related calls:**
        - **Buy another IP:** `postWebsiteBuyIp`.
        - **Update PTRs only:** `postWebsitesReverseDns` (or `postWebsiteBuyIp` with `action=reverse_dns`).
        - **Cancel an addon IP:** follow the `cancel_link` URL.
      operationId: getWebsiteBuyIp
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getWebsiteBuyIp_200_response"
          description: Current IP addresses and their reverse DNS hostnames for the
            website.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read website IPs, current reverse DNS, and additional-IP pricing"
      tags:
      - Webhosting
    post:
      description: |
        Dual-purpose mutation that branches on the `action` body field. **`action=buy_ip`** (default): allocates a new addon IP via `website_addon_get_free_ips`, creates an addon `repeat_invoices` row at `WEBSITE_IP_COST` (currency-converted to the parent invoice's currency), and emits a one-period `invoices` row to fund the first month — provisioning waits on payment and free-IP availability on the host server. **Real money**. **`action=reverse_dns`**: skips billing entirely and updates PTR records via `reverse_dns()` for any IP in the `ips` map whose new hostname differs from the current `get_hostname()` value. Sibling ops: `getWebsiteBuyIp` (preview), `gettWebsiteReverseDns` / `postWebsitesReverseDns` (PTR-only).

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body fields:**
        - `action` (string, optional, default `buy_ip`) — `buy_ip` or `reverse_dns`.
        - For `action=reverse_dns`: `ips` (object, required) — `{"<ip>": "<new-hostname>", ...}`. Only IPs already on the website are updated; others ignored. Empty-string values skipped.

        **Returns:**
        - For `buy_ip`: `{text: "Ordered Additional IP successfully.", invoice: <integer>, repeatInvoice: <integer>}`.
        - For `reverse_dns`: `{message: "DNS Updated", success: true}`.

        **Side effects:**
        - `buy_ip`: inserts `repeat_invoices` row (`Additional IP for Webhosting <id>`) and an `invoices` row for the first period.
        - `reverse_dns`: writes PTR records to the in-addr.arpa zone for changed IPs.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Website Passed`.
        - `409 Website is not active`.
        - `No available free IPs on this server. Please contact support to order additional IPs.` — host has no free IPs.

        **Related calls:**
        - **Preview first:** `getWebsiteBuyIp`.
        - **Pay the new addon invoice:** `initiatePayment` with the returned `invoice`.
      operationId: postWebsiteBuyIp
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/postWebsiteBuyIp_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/postWebsiteBuyIp_request"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/postWebsiteBuyIp_200_response"
          description: DNS update result.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Buy an additional IP for the website OR update reverse DNS records
      tags:
      - Webhosting
  /websites/{id}/invoices:
    get:
      description: |
        Returns the billing history for one webhosting service — initial purchase invoice, recurring monthly/period invoices, and any IP-addon invoices created via `postWebsiteBuyIp`. Backed by `Billing\InvoicesList::go()` with `module='webhosting'` (same handler pattern as VPS/Mail/etc. per-service invoice endpoints). Use to render a per-website billing-history view or find an unpaid invoice id to pass to `initiatePayment`. Sibling ops: `getBillingInvoice`, `initiatePayment`, sibling cross-module: `getVpsInvoices`, `getDomainInvoices`, `getMailInvoices`. For account-wide history use top-level `getBillingInvoices`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:** `ChargeInvoiceRows` — array of invoice rows: `id`, `amount`, `paid`, `description`, `date`, `due_date`, `currency`, `module=webhosting`, `service={id}`.

        **Auth:** Session/API key. Ownership enforced via parent website.

        **Errors:**
        - `401` — unauthenticated.
        - `400 Invalid Service` — `id` not owned by caller.

        **Related calls:**
        - **Single invoice detail:** `getBillingInvoice`.
        - **Pay an unpaid invoice:** `initiatePayment`.
        - **Account-wide history:** `getBillingInvoices`.
      operationId: getWebsiteInvoices
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChargeInvoiceRows"
          description: Get Invoices response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List all billing invoices and recurring charges scoped to one website
      tags:
      - Webhosting
  /websites/{id}/login:
    get:
      description: |
        Returns a single-use auto-login URL so the customer can jump into their control panel without entering credentials. Branches on `serviceMaster.website_type`:
        - **WEB_CPANEL** (default): calls WHM `create_user_session` for the `cpaneld` service, returns a session-bound cPanel URL.
        - **WEB_DIRECTADMIN**: calls DA `CMD_API_LOGIN_KEYS` (`max_uses=2`, IP-locked to `127.0.0.1` plus the caller's `client_ip`); returns one-time URL.
        - **WEB_PLESK**: calls Plesk SDK `createSession`, returns `https://<host>:8443/enterprise/rsession_init.php?PLESKSESSID=...`.
        - **WEB_PPA**, **WEB_VESTA**: placeholders (return `Unhandled Server Type`).

        Sibling ops: `getWebsiteInfo`, `getWebsitesWelcomeEmail` (re-send credentials instead).

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:** `{type: "location", location: "<one-time-url>"}`.

        **Side effects:**
        - WHM/DA/Plesk-side session creation; sessions usually expire after first use (DirectAdmin: `max_uses=2`, IP-locked).

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `Invalid Website Passed` — `id` not owned by caller.
        - `Website is <status>, only websites that are "active" can do this.` — `website_status != "active"`.
        - `No Host server or username` — service has no `website_username` or `website_server` resolved.
        - `Sorry! something went wrong, couldn't connect to <panel>!` — panel-side failure.
        - `Unhandled Server Type` — `website_type` is WEB_PPA / WEB_VESTA (or unrecognized).

        **Related calls:**
        - **If you need the credentials themselves:** `getWebsitesWelcomeEmail` (re-sends the welcome email with username/password).
        - **List sites first:** `getWebsiteList`.
      operationId: getWebsitesLogin
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebsiteLoginResponse"
          description: Get Website Login response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get a one-time auto-login URL for the website's control panel
      tags:
      - Webhosting
  /websites/{id}/backups:
    get:
      description: |
        Returns the list of off-site cpmove backups stored for the webhosting account, or — with the `download=<name>` query param — inline-streams the chosen archive as base64. Backups are read from the OpenStack Swift container `serviceMaster.website_name` (authenticated with `SWIFT_WEBHOSTING_USER`/`SWIFT_WEBHOSTING_PASS`) and filtered to objects matching `cpmove-{website_username}-*`. Use to find restore points before a risky change or before `webhostingCancel`. Empty array means no off-site cpmoves have been pushed for this account. Sibling ops: `webhostingCancel` (snapshot before terminating), `getWebsiteInfo`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Query params:**
        - `download` (string, optional) — when set to a backup `name` from the list, switches to inline download mode (returns the file base64-encoded). **Large payload** — only fetch when actually restoring.

        **Returns:**
        - **List mode** (no `download`): array of `{name: "<cpmove-...>", size: "<human-scaled>"}` (size from `Content-Length` via `Scale($len, 'bytes', 1)`).
        - **Download mode** (`?download=<name>`): single object `{name, size, file: "<base64-encoded-archive>"}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` (legacy text) — `id` not owned by caller.
        - `409 Website is not active` — `website_status != "active"`.

        **Related calls:**
        - **Take a backup before cancelling:** `getWebsitesBackups` (with `download=`) → `webhostingCancel`.
        - **Migrate to/from another host:** `postWebsiteMigration`.
      operationId: getWebsitesBackups
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebsiteBackups"
          description: Get Website Backups response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List off-site cpmove backups stored in Swift — list or inline-download
        archive
      tags:
      - Webhosting
  /websites/{id}/migration:
    post:
      description: |
        Submits a migration request: opens a support ticket containing the customer's credentials for their current host (cPanel/FTP/domain registrar) so InterServer staff can copy the site, databases, and email into this webhosting account. **Sensitive** — the body contains plaintext credentials for the source host. Do not log responses. The created ticket's id is returned; track progress with the helpdesk/tickets API. Sibling ops: `getWebsiteInfo`, `getWebsitesBackups`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body fields** (JSON or multipart):
        - `custPortal` (string) — URL of the current hosting provider's customer portal (e.g. `sso.godaddy.com`).
        - `regEmail` (string) — email/username at the current host.
        - `password` (string) — password at the current host.
        - `ctrlPanel` (string) — current control-panel URL (e.g. `yourdomain.com/cpanel/`).
        - `ftpUsername` (string), `ftpPassword` (string) — FTP credentials.
        - `siteBusyMig` (string) — info on site traffic / whether a holding page can be shown during migration.
        - `splReqMig` (string) — special requirements (PHP version, modules, etc.).
        - `domainReg` (string) — whether domain-registration transfer is also needed (`yes`/`no` or freeform).
        - `dataMig` (string) — nameserver switch timing preference.
        - `domainRegPortal`, `domainRegEmail`, `domainRegPassword` (strings) — domain-registrar credentials.

        **Returns:** `{text: "Your migration request has been sucessfully submitted...", ticket: <integer>}` — pass `ticket` to the tickets API to monitor.

        **Side effects:**
        - Creates a support ticket via `create_ticket()` with the credentials in the ticket body.
        - Inserts a `history_log` row of type `Webhost Migration`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service Passed` — `id` not owned by caller.

        **Related calls:**
        - **Track migration progress:** Tickets API (use the returned `ticket` id).
        - **Verify after migration:** `getWebsitesLogin`, `getWebsiteInfo`.
      operationId: postWebsiteMigration
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/postWebsiteMigration_request"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/postWebsiteMigration_request_1"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/postWebsiteMigration_200_response"
          description: Migration request submitted.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Submit a request for InterServer staff to migrate a website from another
        host
      tags:
      - Webhosting
  /websites/{id}/reverse_dns:
    get:
      description: |
        Returns the current PTR/reverse-DNS hostname for every IP attached to the website — primary `website_ip` plus any addons (from `get_service_addons().extra_ips`). PTRs are read live via `get_hostname()`, not cached. Use to render a PTR editor before calling `postWebsitesReverseDns`. **Note:** the operationId has a typo (`gettWebsiteReverseDns` with double-t) preserved for back-compat — do not rename. Sibling ops: `postWebsitesReverseDns` (update), `getWebsiteBuyIp` (broader IP+billing view), `postWebsiteBuyIp` (also supports `action=reverse_dns`).

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:** `ReverseDnsEntries` — `{"ips": {"<ip>": "<ptr-hostname>", ...}}`. Empty string for IPs with no PTR set.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` (legacy text) — `id` not owned by caller.
        - `409 Website is not active` — `website_status != "active"`.

        **Related calls:**
        - **Update PTRs:** `postWebsitesReverseDns`.
        - **Add IPs first:** `getWebsiteBuyIp` → `postWebsiteBuyIp`.
      operationId: gettWebsiteReverseDns
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReverseDnsEntries"
          description: List of reverse dns entries
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read current reverse-DNS (PTR) records for the website's IPs
      tags:
      - Webhosting
    post:
      description: |
        Sets the PTR hostname for each IP in the website's IP set. Calls `reverse_dns($ip, $newHostname)` for every IP in the body whose value differs from the current PTR and is non-empty; IPs not in the body are left alone. Always returns `{message: "DNS Updated", success: true}` even if no entries actually changed. PTR propagation is asynchronous — re-call `gettWebsiteReverseDns` after a few minutes to confirm. Equivalent to calling `postWebsiteBuyIp` with `action=reverse_dns`. Sibling ops: `gettWebsiteReverseDns`, `getWebsiteBuyIp`, `postWebsiteBuyIp`.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body fields** (schema `ReverseDnsEntries`):
        - `ips` (object, required) — `{"<ip>": "<new-hostname>", ...}`. Only IPs that already belong to the website are updated; others ignored. Empty-string values skipped.

        **Returns:** `{message: "DNS Updated", success: true}`.

        **Side effects:**
        - One `reverse_dns()` call per IP whose value changed. Records are written to the in-addr.arpa zone; TTL-dependent propagation.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed` (legacy text) — `id` not owned by caller.
        - `409 Website is not active` — `website_status != "active"`.

        **Related calls:**
        - **Read current PTRs first:** `gettWebsiteReverseDns`.
        - **Equivalent endpoint:** `postWebsiteBuyIp` (`action=reverse_dns`).
      operationId: postWebsitesReverseDns
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response from the update reverse DNS call.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Bulk-update reverse-DNS (PTR) records for one or more website IPs
      tags:
      - Webhosting
  /websites/{id}/welcome_email:
    get:
      description: |
        Resends the webhosting welcome email — the new-account email containing control-panel hostname, username, password, and getting-started instructions. Calls the dynamically-resolved `website_welcome_email($id)` helper which composes and dispatches the message to the account's `account_lid`. Idempotent — safe to call multiple times. Use after `addWebsite` finishes provisioning, or whenever a customer reports losing the original. Sibling welcome-email endpoints in other modules: `getVpsWelcomeEmail`, `getDomainsWelcomeEmail`, `getMailWelcomeEmail`. For an auto-login URL (no password reveal), use `getWebsitesLogin` instead.

        **Path param:**
        - `id` (integer, required) — `website_id` from `getWebsiteList`.

        **Body:** None.

        **Returns:** `SuccessTextResponse` — `{text: "Welcome Email has been resent."}`.

        **Side effects:**
        - Sends an email to the account's billing email address with the control-panel credentials currently stored in `history_log` for this website.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service Passed` — `id` not owned by caller.
        - `409 Service is not active` — `website_status != "active"`.

        **Related calls:**
        - **Auto-login instead:** `getWebsitesLogin` (one-time URL, no password disclosure).
        - **List sites first:** `getWebsiteList`.
      operationId: getWebsitesWelcomeEmail
      parameters:
      - description: The website service ID. Use `website_id` from `GET /websites`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the webhosting welcome email with control-panel credentials
        and URL
      tags:
      - Webhosting
  /mail/{id}/advsend:
    description: "Sends An email through one of your mail orders allowing additional\
      \ options such as file attachments, cc, bcc, etc."
    post:
      description: |
        Submits an outbound message through `relay.mailbaby.net:25` using the service's SMTP credentials (fetched via `mail_get_password`). Use for multi-recipient sends, named addresses, CC/BCC, ReplyTo, or attachments. For single-recipient plain sends, `sendMail` is the lighter option. Sibling ops: `sendMail`, `viewMailLog` (find queued message), `getMailDeliverability` (analyze bounces).

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (JSON or form-urlencoded, schema `SendMailAdv`):**
        - `from` (string or `{email, name}`, required).
        - `to` (array of strings or `{email, name}` objects, required).
        - `subject` (string, required).
        - `body` (string, required) — HTML auto-detected when tags are present.
        - `replyto` (array, optional) — same shape as `to`.
        - `cc`, `bcc` (array, optional) — same shape as `to`.
        - `attachments` (array, optional) — each `{filename, data}` where `data` is base64-encoded; added via `addStringAttachment`.

        **Returns:** `{status: "ok", text: "Email queued successfully"}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `400` with PHPMailer `ErrorInfo` on send failure or missing required field.
        - `401` — unauthenticated.
        - `404 Invalid Service Passed`.
        - `409 Service is not active`.
      operationId: sendAdvMail
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            examples:
              AdvSendExample:
                value:
                  subject: Welcome
                  body: Hello
                  from:
                    email: user@domain.com
                  to:
                  - email: someone@client.com
                    name: Mr Client
                  attachments:
                  - filename: message.txt
                    data: base64_encoded_contents
                  id: 66
            schema:
              $ref: "#/components/schemas/SendMailAdv"
          application/x-www-form-urlencoded:
            examples:
              AdvSendExample:
                value:
                  subject: Welcome
                  body: Hello
                  from:
                    email: user@domain.com
                  to:
                  - email: someone@client.com
                    name: Mr Client
                  attachments:
                  - filename: message.txt
                    data: base64_encoded_contents
                  id: 66
            schema:
              $ref: "#/components/schemas/SendMailAdv"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Email queued successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: "Send email via Mail Baby SMTP relay with attachments, CC/BCC, and\
        \ multi-recipient"
      tags:
      - Mail
    summary: Sends an Email with Advanced Options
  /mail/{id}/blocks/delete:
    post:
      description: |
        Removes block rows for the supplied email across the three reputation stores: `rspamd` (by `fromemail`), `mailchannels` (by `email`), `mailbaby` (by `emailfrom`). Functionally equivalent to `postMailDelist` but uses `email` parameter naming and returns 400 (not error JSON) for an invalid address. Sibling ops: `getMailBlocks`, `getMailDelist`, `postMailDelist`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (schema `EmailAddress`):**
        - `email` (string, required) — sender address; validated via `FILTER_VALIDATE_EMAIL`.

        **Returns:** `{status: "ok", text: "Email '...' removed from block list"}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `400` invalid email, `401`, `404`, `409 not active`.
      operationId: delistBlock
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            examples:
              BlocksDeleteExampleForm:
                value:
                  email: client@domain.com
            schema:
              $ref: "#/components/schemas/EmailAddress"
          application/json:
            examples:
              BlocksDeleteExample:
                value:
                  email: client@domain.com
            schema:
              $ref: "#/components/schemas/EmailAddress"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Email address removed from block list successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Delist a sender email from rspamd / mailchannels / mailbaby block lists
      tags:
      - Mail
  /mail/{id}/rules:
    description: The deny rules allow you to setup assorted types of rules which will
      automatically deny the email from being sent through our system when  the citeria
      match.
    get:
      description: |
        Returns every `mail_spam` row scoped to this service's `mail_username` — local sender/recipient block rules the customer has configured. Sibling ops: `addRule`, `updateRule`, `deleteRule`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Returns:** Array of `DenyRuleRecord` — `{id, user, type, data, created}`. `type` values:
        - `domain` — block by sender domain.
        - `email` — block by exact sender email.
        - `startswith` — block when sender local-part starts with a string.
        - `destination` — block by recipient email.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: getRules
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              examples:
                ExampleMailRulesList:
                  value:
                  - id: 14
                    user: mb20682
                    type: email
                    data: domeinwo@server.guesshost.net
                    created: 2022-03-22 19:16:35
                  - id: 15
                    user: mb20682
                    type: email
                    data: mamediieva@thedigital.gov.ua
                    created: 2022-03-22 19:18:01
              schema:
                items:
                  $ref: "#/components/schemas/DenyRuleRecord"
                type: array
          description: List of configured deny rules.
          links:
            deleteRuleLink:
              description: "The `id` value returned in each rule can be used as the\
                \ `rule` parameter in `DELETE /mail/{id}/rules/{rule}` to remove that\
                \ rule."
              operationId: deleteRule
              parameters:
                id: $request.path.id
                rule: $response.body#/0/id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List configured deny rules (sender/recipient blocks) for a Mail Baby
        service
      tags:
      - Mail
    post:
      description: |
        Inserts a new `mail_spam` row scoped to this service's `mail_username` so the relay drops matching submissions. Sibling ops: `getRules`, `updateRule`, `deleteRule`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (schema `DenyRuleNew`):**
        - `type` (string, required) — `domain` / `email` / `startswith` / `destination`.
        - `data` (string, required) — literal value matched; validation: no quotes, valid domain for `type=domain`, valid email for `type=email`, `[A-Z0-9+_.-]+` for `startswith`.

        **Returns:** `"Spam Block Added"`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** field-level errors on validation failure, `401`, `404`, `409 not active`.
      operationId: addRule
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            examples:
              DenyRuleNewJsonExample:
                value:
                  user: mb20682
                  type: email
                  data: domeinwo@server.guesshost.net
            schema:
              $ref: "#/components/schemas/DenyRuleNew"
          application/x-www-form-urlencoded:
            examples:
              DenyRuleNewFormExample:
                value:
                  user: mb20682
                  type: email
                  data: domeinwo@server.guesshost.net
            schema:
              $ref: "#/components/schemas/DenyRuleNew"
        description: These are the fields needed to create a new email deny rule.
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Deny rule created successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Create a new deny rule to auto-block matching submissions
      tags:
      - Mail
    summary: Deny Rules
  /mail/{id}/rules/{rule}:
    delete:
      description: |
        Hard-deletes a single `mail_spam` row scoped to this service's `mail_username`. **Irreversible** — no audit copy preserved. Query filter `id={rule} AND user='{mail_username}'` prevents cross-tenant deletes; passing a `rule` belonging to a different mail order is silently a no-op (still returns success). Sibling ops: `getRules`, `addRule`, `updateRule`.

        **Path params:**
        - `id` (integer, required) — `mail_id` from `getMailList`.
        - `rule` (string, required) — rule id from `getRules`.

        **Returns:** `"Block deleted successfully."`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `401`, `404`, `409 not active`.
      operationId: deleteRule
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: The ID of the Rules entry.
        examples:
          RuleIdExample:
            value: "34"
        explode: false
        in: path
        name: rule
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Deny rule deleted successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Delete a Mail Baby deny rule by rule ID (hard delete — no recovery)
      tags:
      - Mail
    put:
      description: |
        Updates `type` and `data` on a single `mail_spam` row. Query is bounded by `id={rule} AND user='{mail_username}'` so cross-tenant updates are impossible. Same validation rules as `addRule`. Sibling ops: `getRules`, `addRule`, `deleteRule`.

        **Path params:**
        - `id` (integer, required) — `mail_id` from `getMailList`.
        - `rule` (string, required) — rule id from `getRules`.

        **Body fields (schema `DenyRuleNew`):**
        - `type` (string, required) — `domain` / `email` / `startswith` / `destination`.
        - `data` (string, required) — see `addRule` for type-specific validation.

        **Returns:** `"Record updated successfully."`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** field-level errors on validation failure, `401`, `404`, `409 not active`.
      operationId: updateRule
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: The ID of the deny rule to update.
        explode: false
        in: path
        name: rule
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DenyRuleNew"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DenyRuleNew"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Deny rule updated successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Update an existing Mail Baby deny rule's type and match data
      tags:
      - Mail
  /mail/{id}/send:
    post:
      description: |
        Sends a single-recipient transactional email through `relay.mailbaby.net:25` authenticated as this `mail_id`. Body fields are the minimum needed for a plain send; Reply-To is auto-set to `from`. For multi-recipient sends, CC/BCC, named addresses, or attachments use `sendAdvMail` instead. Sibling ops: `sendAdvMail`, `viewMailLog`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Body fields (JSON or form-urlencoded, schema `SendMail`):**
        - `to` (string, required) — recipient email.
        - `from` (string, required) — sender email.
        - `subject` (string, required).
        - `body` (string, required) — HTML auto-detected when tags are present.

        **Returns:** `{status: "ok", text: "Email queued successfully"}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `400` with PHPMailer `ErrorInfo` on send failure or missing required field, `401`, `404`, `409 not active`.
      operationId: sendMail
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMail"
          application/x-www-form-urlencoded:
            schema:
              $ref: "#/components/schemas/SendMail"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenericResponse"
          description: Email queued successfully.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              examples:
                InvalidVps:
                  value:
                    code: 404
                    message: Invalid VPS Passed
                InvalidService:
                  value:
                    code: 404
                    message: Invalid Service Passed
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: Send a simple single-recipient email through the Mail Baby SMTP relay
      tags:
      - Mail
  /mail/{id}/stats:
    get:
      description: |
        Returns aggregate usage and cost metrics for the SMTP user behind `mail_id` from the ZoneMTA `mail_messagestore` / `mail_senderdelivered` tables. Use to drive an analytics dashboard or to project end-of-cycle cost. Sibling ops: `viewMailLog`, `getMailDeliverability`.

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList`.

        **Query params:**
        - `time` (string enum, optional, default `1h`) — window: `all` / `billing` (current invoice cycle) / `month` / `7d` / `24h` / `1d` / `1h`.

        **Returns** (schema `MailStatsType`):
        - `time` (string) — echo of selected window.
        - `usage` (integer) — full-billing-cycle send count.
        - `currency`, `currencySymbol` (string).
        - `cost` (decimal) — projected = base + `$0.20 / 1000 emails`.
        - `received`, `sent` (integer).
        - `volume.to`, `volume.from`, `volume.ip` (object) — top-500 destinations / senders / origin IPs by count.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `Invalid or missing mail order id`, `401`.
      operationId: getStats
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: The timeframe for the statistics.
        explode: true
        in: query
        name: time
        required: false
        schema:
          enum:
          - all
          - billing
          - month
          - 7d
          - 24h
          - 1d
          - 1h
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MailStatsType"
          description: Mail service usage statistics.
          links:
            getMailLog:
              description: "Use `GET /mail/{id}/log` to view detailed message-level\
                \ logs for this mail service."
              operationId: viewMailLog
              parameters:
                id: $request.path.id
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        "404":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Read Mail Baby usage counts, send volume totals, top destinations,\
        \ and projected cost"
      tags:
      - Mail
  /account/timezones:
    get:
      description: |
        Populates a timezone picker for account preferences or for VPS / QuickServer timezone changes. Public — no auth required. Backed by PHP's `DateTimeZone::listIdentifiers()` so the catalog is large (~400+ zones, including deprecated aliases like `US/Eastern`). Result is fixed for a given PHP build — cache aggressively client-side. Sibling ops: `postVpsChangeTimezone`, `postQsChangeTimezone`, `getCountries`, `getAccountLocales`, `getAccountCurrencies`.

        **Path/Query/Body:** None.

        **Returns:** flat JSON array of stable IANA tz strings, e.g. `["Africa/Abidjan", "America/New_York", "Asia/Tokyo", "Europe/London", "UTC"]`. Values are usable verbatim on the timezone-change endpoints; no translation or country-grouping is performed here.

        **Auth:** None.

        **Errors:** No documented error path under normal operation.

        **Related calls:**
        - **Apply selection to a service:** `postVpsChangeTimezone` (`/vps/{id}/change_timezone`), `postQsChangeTimezone` (`/qs/{id}/change_timezone`).
        - **Apply to account profile:** `updateAccountInfo` (sets `timezone`).
        - **Other preference catalogs:** `getCountries`, `getAccountLocales`, `getAccountCurrencies`.
      operationId: getTimezones
      responses:
        "200":
          content:
            application/json:
              examples:
                TimezonesExample:
                  value:
                  - Africa/Abidjan
                  - America/New_York
                  - Asia/Tokyo
                  - Europe/London
                  - UTC
              schema:
                items:
                  type: string
                type: array
          description: An array of all available timezone identifiers.
      security:
      - {}
      summary: List all PHP timezone identifiers usable on accounts and services
      tags:
      - Public
  /account/countries:
    get:
      description: |
        Populates country dropdowns in account registration, billing-address forms, and domain/whois contact forms. Public — no auth required. Disabled countries (e.g. embargoed jurisdictions) are excluded — admins toggle this in `country_t.enabled`. Sibling ops: `getTimezones`, `getAccountLocales`, `getAccountCurrencies`, `updateAccountInfo` (consumes the chosen country).

        **Query parameters:**
        - `fetch_by` (string, optional) — one of `iso2` (default; two-letter codes like `US`, `GB`), `iso3` (three-letter like `USA`, `GBR`), or `numcode` (UN M49 numeric like `840`). Any other value silently falls back to `iso2`.

        **Body:** None.

        **Returns:** JSON object mapping the chosen key format to the country's short name — e.g. `{ "AF": "Afghanistan", "US": "United States", "ZW": "Zimbabwe" }`. Sourced from the `country_t` table, filtered to `enabled=1`, ordered alphabetically by `short_name`.

        **Auth:** None.

        **Errors:** No documented error path.

        **Related calls:**
        - **Apply to account profile:** `updateAccountInfo`.
        - **Other preference catalogs:** `getTimezones`, `getAccountLocales`, `getAccountCurrencies`.
      operationId: getCountries
      parameters:
      - description: Get countries by iso2 or iso3 or numcode
        explode: true
        in: query
        name: fetch_by
        required: false
        schema:
          enum:
          - iso2
          - iso3
          - numcode
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              examples:
                AccountCountriesExample:
                  value:
                    AF: Afghanistan
                    US: United States
                    ZW: Zimbabwe
              schema:
                type: object
          description: The list of countries with both 2 letter abbreviations and
            the country names.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: List enabled countries keyed by ISO-2/ISO-3/numeric code
      tags:
      - Public
  /account/locales:
    get:
      description: |
        Renders the language-picker for account preferences or login pages. Public — no auth required. Cross-references PHP's Punic locale data with `locale/google_langs.php` so only locales with Google Translate support are returned. Result is essentially static — cache client-side. Sibling ops: `getCountries`, `getTimezones`, `getAccountCurrencies`, `updateAccountInfo` (consumes the chosen locale).

        **Path/Query/Body:** None.

        **Returns:** JSON object keyed by BCP-47-style locale code, with `{ name, local_name }` per entry — e.g.

            {
              "en": { "name": "English", "local_name": "English" },
              "es": { "name": "Spanish", "local_name": "español" },
              "fr": { "name": "French", "local_name": "français" }
            }

        `name` is the English label; `local_name` is the locale's name in its own language (good for accessibility and avoiding the wrong-script problem).

        **Auth:** None.

        **Errors:** No documented error path.

        **Related calls:**
        - **Apply to account profile:** `updateAccountInfo` (sets `locale`).
        - **Other preference catalogs:** `getCountries`, `getTimezones`, `getAccountCurrencies`.
      operationId: getAccountLocales
      responses:
        "200":
          content:
            application/json:
              examples:
                AccountLocalesExample:
                  value:
                    en:
                      name: English
                      local_name: English
                    es:
                      name: Spanish
                      local_name: español
                    fr:
                      name: French
                      local_name: français
              schema:
                additionalProperties:
                  $ref: "#/components/schemas/getAccountLocales_200_response_value"
                type: object
          description: Map of locale identifiers to display names.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: List supported UI locales with English and native display names
      tags:
      - Public
  /account/currencies:
    get:
      description: |
        Populates a currency selector on signup, billing-preferences, or invoice-display forms. Public — no auth required. The list changes only when an admin enables/disables a currency — cache client-side. Sibling ops: `getCountries`, `getTimezones`, `getAccountLocales`, plus the billing-preference endpoints under `/account/*` and `/billing/*`.

        **Path/Query/Body:** None.

        **Returns:** flat JSON array of ISO-4217 currency codes — e.g. `["USD", "EUR", "GBP", "INR"]`. Sourced from rows in the `currencies` table where `currency_enabled=1`, in the natural row order. The endpoint returns codes only — for symbols, decimals, or exchange rates use a separate currency-detail endpoint or a static client-side map.

        **Auth:** None.

        **Errors:** No documented error path.

        **Related calls:**
        - **Apply to account profile:** `updateAccountInfo`.
        - **Other preference catalogs:** `getCountries`, `getTimezones`, `getAccountLocales`.
      operationId: getAccountCurrencies
      responses:
        "200":
          content:
            application/json:
              examples:
                AccountCurrenciesExample:
                  value:
                  - USD
                  - EUR
                  - GBP
                  - INR
              schema:
                items:
                  type: string
                type: array
          description: An array of enabled currency codes.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      security:
      - {}
      summary: List enabled currency codes accepted for billing and preferences
      tags:
      - Public
  /servers/{id}/ipmi_power:
    get:
      description: |-
        Use to check whether a server's chassis is currently `on`/`off` via IPMI before issuing a power action. Path param: `id` (integer server_id). Optional body `asset` (asset_id — defaults to first asset). Issues `ipmitool power status` against the asset's `ipmi_ip` using its location IPMI group/credentials.
        Returns: `{ text:'Chassis Power is on' }` (or 'off'). Errors: 404 if `id` not owned by caller; 409 if service not active; 'There was an error sending the IPMI command' if BMC unreachable. Caveat: BMCs occasionally rate-limit — back off on repeated errors.
        Sibling ops: `serverBulkIpmiPowerGet` (preferred when polling many servers — single round-trip), `serverIpmiPowerPost` (DESTRUCTIVE — change power), `getServerInfo` (full state), `serverIpmiLiveGet` (IPMI Live KVM).
      operationId: serverIpmiPowerGet
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read IPMI chassis power status for a dedicated server (single)
      tags:
      - Servers
    post:
      description: |
        Sends an IPMI chassis power command (`on`, `off`, `cycle`, `reset`, `soft`) to a customer's physical dedicated server. **DESTRUCTIVE on running hardware:** `off` / `cycle` / `reset` are forced power events that can corrupt filesystems, lose un-flushed data, or break in-flight workloads. `soft` requests an ACPI shutdown (safer when the guest OS is responsive). Always confirm intent with the operator. Sibling ops: `serverIpmiPowerGet` (read first), `serverBulkIpmiPowerGet` (status only), `serverIpmiLivePost` (KVM access).

        **Path:** `id` (integer, required) — server_id.

        **Body fields:**
        - `action` (string, required) — one of `on` / `off` / `cycle` / `reset` / `soft`.
        - `asset` (integer, optional) — asset_id; defaults to first asset on the server.

        **Returns:** `{ text: 'Power command sent. Response: <ipmi output>' }`.

        **Auth:** Session/API key. Ownership enforced via `server_custid`.

        **Errors:**
        - `422` / inline error text — `Invalid Action` when `action` is not in the allowed set.
        - `404` — `id` not owned, or `asset` not on this server.
        - `409` — service not `active`.
        - `200` with error text — `'There was an error sending the IPMI command.'` when BMC is unreachable or rate-limiting.

        **Related calls:**
        - **Status (single / bulk):** `serverIpmiPowerGet`, `serverBulkIpmiPowerGet`.
        - **KVM console:** `serverIpmiLivePost`.
      operationId: serverIpmiPowerPost
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ServerIpmiPowerRequest"
          application/json:
            schema:
              $ref: "#/components/schemas/ServerIpmiPowerRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
      summary: DESTRUCTIVE — change chassis power state on a bare-metal server
      tags:
      - Servers
  /servers/bulk/ipmi_power:
    get:
      description: |-
        Use when you need power status for several owned servers at once (dashboards, mass health checks). Each server is queried independently; per-server failures (invalid id, inactive service, no asset, BMC error) are reported in the same response without aborting the batch. Read-only — does NOT change power state.
        Query: `ids` (required) — comma-separated string `?ids=2313,2314,2315` OR repeated `ids[]` array. Duplicates de-duped; non-positive ints become per-row errors. Returns: `{ results: [ { id, asset?, text|error } ] }`. Errors: 400 'No server IDs provided.' if `ids` empty/missing; 401 unauth.
        Sibling ops: `serverIpmiPowerGet` (single-server equivalent), `serverIpmiPowerPost` (DESTRUCTIVE — change power; no bulk equivalent — call per server), `getServerList` (discover ids).
      operationId: serverBulkIpmiPowerGet
      parameters:
      - description: "Comma-separated list of Server IDs to query (e.g. `2313,2314,2315`).\
          \ May also be passed as repeated `ids[]` query parameters."
        example: "2313,2314,2315"
        explode: true
        in: query
        name: ids
        required: true
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerBulkIpmiPowerResponse"
          description: "Bulk IPMI power status response, one entry per requested server."
        "400":
          content:
            application/json:
              examples:
                InvalidTimezone:
                  value:
                    code: 422
                    message: Invalid timezone
                MissingCaptcha:
                  value:
                    code: 422
                    message: missing captcha
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: The specified resource was not found
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Read IPMI chassis power status for many dedicated servers in one call
      tags:
      - Servers
  /qs/{id}/welcome_email:
    get:
      description: |-
        Re-runs the `qs_welcome_email` function which composes and sends the welcome email containing connection details, root password, and management URLs to the account owner. Path param: `id` (integer).
        Returns: `{ text: "Welcome Email has been resent." }`. Errors: 401, 404 if not owned by caller, 409 if status != `active`. Use when the original welcome email was lost or the customer needs credentials again.
        Siblings: `getVpsWelcomeEmail`, `getQsInfo` (also exposes connection info).
      operationId: getQsWelcomeEmail
      parameters:
      - description: Quickserver ID
        examples:
          websiteIdExample:
            value: "133123"
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Resend the QuickServer welcome email with login credentials
      tags:
      - QuickServers
  /vps/{id}/welcome_email:
    get:
      description: |
        Resends the VPS welcome email — the original new-service email containing IP, hostname, root/Administrator credentials, and connection instructions. Calls `vps_welcome_email($id)` to regenerate and dispatch via the standard mail pipeline. Use when the customer didn't receive (or lost) the original right after provisioning. The dashboard's "show credentials" view is the alternative for in-app retrieval. Sibling ops: `getVpsInfo` (shows connection details in the response), `postVpsChangeRootPassword` / `postVpsResetPassword` (rotate before resending if security is a concern).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body:** None.

        **Returns:** `SuccessTextResponse` — `Welcome Email has been resent.`

        **Side effects:**
        - Sends an email to the account's billing email address with the credentials currently stored in `history_log` for this VPS.

        **Auth:** Session/API key. Ownership enforced via `vps_custid`.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid Service Passed` — `id` not owned by caller.
        - `409 Service is not active` — `vps_status != "active"`.

        **Related calls:**
        - **In-app credential view:** `getVpsInfo`.
        - **Before resending, rotate password:** `postVpsResetPassword` or `postVpsChangeRootPassword`.
      operationId: getVpsWelcomeEmail
      parameters:
      - description: VPS ID
        examples:
          vpsIdExample:
            value: "4813"
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessTextResponse"
          description: A response indicating the operation completed successfully
            with a text message.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: "Resend the welcome email containing VPS IP, hostname, and root credentials"
      tags:
      - VPS
  /servers/{id}/reverse_dns:
    get:
      description: |-
        Use to read the existing PTR/rDNS hostnames assigned to each public IP in the server's VLANs — typically before calling `postServerReverseDns` to update them. Path param: `id` (integer server_id). No body. Walks `networkInfo.vlans`, expands each network to usable host IPs (handles /31 and /32 edge cases), and resolves each via `get_hostname()`.
        Returns: `{ ips: { '<ipv4>': '<ptr_or_empty_string>', ... } }`. Empty string indicates no PTR set. Errors: 404 if `id` not owned by caller; 401 unauth.
        Sibling ops: `postServerReverseDns` (update PTRs), `getServerInfo` (full network), `getVpsReverseDns` for VPS, `getDomainNameservers` / DNS endpoints for forward records. Note rDNS propagation is delegated to the in-addr.arpa zone — changes are not always instant.
      operationId: getServerReverseDns
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReverseDnsEntries"
          description: Servers Reverse DNS info response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List current reverse-DNS (PTR) records for a dedicated server's IPs
      tags:
      - Servers
    post:
      description: |-
        Use to set or remove PTR records for the server's public IPs. Path param: `id` (server_id). Body: `ips` (object mapping `'<ipv4>'` to desired hostname; empty string removes the PTR). Only IPs that already exist on the server's VLANs and whose hostname differs from current are updated; each diff calls `reverse_dns($ip, $host, 'set_reverse'|'remove_reverse')`.
        Returns: `{ message, success:bool }`. `success:false` with 'No valid IPs were passed or there were no changes' when nothing to update; otherwise reports update count. Errors: 404 invalid id; 401 unauth. Caveats: caller can only set PTRs for IPs they actually own; rDNS propagation is async — do not assume immediate visibility downstream.
        Sibling ops: `getServerReverseDns` (read first), `getServerInfo`, VPS counterpart `postVpsReverseDns`.
      operationId: postServerReverseDns
      parameters:
      - description: Server ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ReverseDnsEntries"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Update Server Reverse DNS response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Update reverse-DNS (PTR) hostnames on a dedicated server's IPs
      tags:
      - Servers
  /mail/{id}/log:
    get:
      description: |
        Paginated search over ZoneMTA's `mail_messagestore` joined with `mail_senderdelivered` and `mail_queuerelease`. Supports envelope, header, and metadata filters; sortable; choose recipient-level or message-level grouping. Use to investigate delivery issues, find specific messages by Message-ID, audit bounce rates, or feed an analytics dashboard. Sibling ops: `getStats`, `getMailDeliverability`, `delistBlock` (clear a block surfaced by a bounce).

        **Path param:**
        - `id` (integer, required) — `mail_id` from `getMailList` (omit to span all owned mail users — admin-only).

        **Query params:**
        - `from`, `to` (string) — envelope address, exact match.
        - `headerfrom`, `replyto` (string) — header address, exact match; validated as email.
        - `subject` (string) — LIKE match on subject.
        - `mailid` (string, 18–19 chars) — relay id, exact.
        - `messageId` (string) — Message-ID header, substring match.
        - `origin` (string) — submitter IP, exact.
        - `mx` (string) — destination MX hostname, LIKE.
        - `delivered` (integer 0/1).
        - `startDate`, `endDate` (Unix timestamp or `strtotime`-parseable string).
        - `skip` (integer, default 0), `limit` (integer 1–10000, default 100).
        - `sort` (`time`), `dir` (`asc`/`desc`, default `desc`).
        - `groupby` (`recipient` default — one row per delivery attempt; `message` — one row per `_id`).

        **Returns** (schema `MailLog`):
        `{total, skip, limit, emails: [{id, _id, from, to, subject, messageId, time, mxHostname, delivered, code, response, recipient, ...}]}`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:** `400` bad input, `401`.
      operationId: viewMailLog
      parameters:
      - description: The mail service ID. Use `mail_id` from `GET /mail`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "The numeric ID of the mail order to filter by.  When omitted,\
          \ logs from the first active mail order are returned.  Obtain valid IDs\
          \ from `GET /mail` or `GET /mail/{id}`."
        example: 2604
        explode: true
        in: query
        name: id
        required: false
        schema:
          format: int64
          type: integer
        style: form
      - description: Filter by the originating IP address from which the message was
          submitted to the relay.  Must be a valid IPv4 or IPv6 address.
        example: 1.2.3.4
        explode: true
        in: query
        name: origin
        required: false
        schema:
          format: ipv4
          type: string
        style: form
      - description: Filter by the MX hostname the relay attempted delivery to.  For
          example `mx.google.com` would return messages destined for Gmail recipients.
          Maps to `mxHostname` in the `MailLogEntry` response.
        example: mx.google.com
        explode: true
        in: query
        name: mx
        required: false
        schema:
          type: string
        style: form
      - description: Filter by SMTP envelope `MAIL FROM` address (exact match).  This
          is the address the relay used for bounce handling and may differ from the
          `From:` message header.  For header-level filtering use `headerfrom`.
        example: me@sender.com
        explode: true
        in: query
        name: from
        required: false
        schema:
          format: email
          type: string
        style: form
      - description: Filter by SMTP envelope `RCPT TO` address (exact match).  This
          is the delivery address used by the relay and may differ from the `To:`
          header when BCC recipients are involved.
        example: you@receiver.com
        explode: true
        in: query
        name: to
        required: false
        schema:
          format: email
          type: string
        style: form
      - description: Filter by email `Subject` header (exact match).  MIME-encoded
          subjects are decoded automatically in the response.
        example: Your order has shipped
        explode: true
        in: query
        name: subject
        required: false
        schema:
          type: string
        style: form
      - description: Filter by the relay-assigned mail ID string (exact match).  This
          corresponds to the `id` field in `MailLogEntry` and to the `text` value
          returned by the sending endpoints on success.  Format is an 18-19 character
          hexadecimal string such as `185997065c60008840`.
        example: 185997065c60008840
        explode: true
        in: query
        name: mailid
        required: false
        schema:
          maxLength: 19
          minLength: 18
          type: string
        style: form
      - description: Filter by the `Message-ID` email header using a substring (case-insensitive)
          match. The `Message-ID` is assigned by the sending mail client and is visible
          in the `messageId` field of `MailLogEntry`.
        example: <abc123@yourdomain.com>
        explode: true
        in: query
        name: messageId
        required: false
        schema:
          type: string
        style: form
      - description: Filter by the `Reply-To` message header address (exact match).  Only
          returns messages where this header was explicitly set.
        example: replies@sender.com
        explode: true
        in: query
        name: replyto
        required: false
        schema:
          format: email
          type: string
        style: form
      - description: Filter by the `From` message header address (exact match).  This
          is the human-visible sender address and may differ from the SMTP envelope
          `from` parameter when sending on behalf of another address.
        example: newsletter@sender.com
        explode: true
        in: query
        name: headerfrom
        required: false
        schema:
          format: email
          type: string
        style: form
      - description: "Filter by delivery status.  `1` returns only messages that were\
          \ successfully delivered to the destination MX.  `0` returns messages that\
          \ are still queued, deferred, or failed.  Omit to return all messages regardless\
          \ of delivery status."
        example: 1
        explode: true
        in: query
        name: delivered
        required: false
        schema:
          enum:
          - 0
          - 1
          title: DeliveredStatus
          type: integer
        style: form
      - description: Number of records to skip for pagination.  Use in combination
          with `limit` to page through large result sets.  Defaults to `0` (no skip).
        example: 0
        explode: true
        in: query
        name: skip
        required: false
        schema:
          default: 0
          format: int32
          minimum: 0
          type: integer
        style: form
      - description: Maximum number of records to return per page.  Defaults to `100`.
          Maximum allowed value is `10000`.  The response also includes a `total`
          field with the full matched count so you can calculate the number of pages.
        example: 100
        explode: true
        in: query
        name: limit
        required: false
        schema:
          default: 100
          format: int32
          maximum: 10000
          minimum: 1
          type: integer
        style: form
      - description: Earliest date to include.  Accepts either a Unix timestamp (integer
          seconds since epoch) or a date string parseable by `strtotime()` such as
          `2024-01-15` or `last monday`.  Messages with a `time` value **greater than
          or equal to** this value will be included.
        example: 1641781008
        explode: true
        in: query
        name: startDate
        required: false
        schema:
          $ref: "#/components/schemas/viewMailLog_startDate_parameter"
        style: form
      - description: Latest date to include.  Accepts either a Unix timestamp (integer
          seconds since epoch) or a date string parseable by `strtotime()` such as
          `2024-01-31` or `yesterday`. Messages with a `time` value **less than or
          equal to** this value will be included.
        example: 1673317008
        explode: true
        in: query
        name: endDate
        required: false
        schema:
          $ref: "#/components/schemas/viewMailLog_startDate_parameter"
        style: form
      - description: Field to sort results by.  Currently only `time` is supported
          (sorts by internal row ID which corresponds to chronological order).
        example: time
        explode: true
        in: query
        name: sort
        required: false
        schema:
          default: time
          enum:
          - time
          type: string
        style: form
      - description: "Sort direction.  `desc` returns newest first (default), `asc`\
          \ returns oldest first."
        example: desc
        explode: true
        in: query
        name: dir
        required: false
        schema:
          default: desc
          enum:
          - asc
          - desc
          type: string
        style: form
      - description: "Controls how results are grouped.  `recipient` (default) returns\
          \ one row per delivery attempt — a message sent to 4 recipients produces\
          \ 4 rows, each with its own `recipient`, `delivered`, `response`, and delivery\
          \ metadata.  `message` collapses to one row per unique message ID; delivery-level\
          \ fields will reflect one arbitrary recipient per message.  The `total`\
          \ count in the response matches the grouping mode."
        example: recipient
        explode: true
        in: query
        name: groupby
        required: false
        schema:
          default: recipient
          enum:
          - message
          - recipient
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              examples:
                MailLogRecipientExample:
                  summary: groupby=recipient (default) - one row per delivery attempt
                  value:
                    total: 4
                    skip: 0
                    limit: 100
                    emails:
                    - _id: 103172
                      id: 17c7eda538e0005d03
                      from: person@mysite.com
                      to: client@isp.com
                      subject: sell 0.005 shares
                      messageId: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
                      created: 2021-10-14 08:50:10
                      time: 1634215809
                      user: mb5658
                      transtype: ESMTPSA
                      origin: 199.231.189.154
                      interface: feeder
                      sendingZone: interserver
                      bodySize: 63
                      seq: 1
                      delivered: 1
                      code: 250
                      recipient: client@isp.com
                      domain: interserver.net
                      locked: 1
                      lockTime: "1634215818533"
                      assigned: relay1
                      queued: 2021-10-14T12:50:15.487Z
                      mxHostname: mx.j.is.cc
                      response: 250 2.0.0 Ok queued as C91D83E128C
                MailLogMessageExample:
                  summary: groupby=message - one row per message
                  value:
                    total: 1
                    skip: 0
                    limit: 100
                    emails:
                    - _id: 103172
                      id: 17c7eda538e0005d03
                      from: person@mysite.com
                      to: client@isp.com
                      subject: sell 0.005 shares
                      messageId: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
                      created: 2021-10-14 08:50:10
                      time: 1634215809
                      user: mb5658
                      transtype: ESMTPSA
                      origin: 199.231.189.154
                      interface: feeder
                      sendingZone: interserver
                      bodySize: 63
                      seq: 1
                      delivered: 1
                      code: 250
                      recipient: client@isp.com
                      domain: interserver.net
                      locked: 1
                      lockTime: "1634215818533"
                      assigned: relay1
                      queued: 2021-10-14T12:50:15.487Z
                      mxHostname: mx.j.is.cc
                      response: 250 2.0.0 Ok queued as C91D83E128C
              schema:
                $ref: "#/components/schemas/MailLog"
          description: Paginated list of mail log entries matching the specified filters.
          links:
            delistSenderBlock:
              description: A `from` address returned in the log can be passed as `email`
                to `POST /mail/blocks/delete` to remove a block on that sender.
              operationId: delistBlock
              parameters:
                email: $response.body#/emails/0/from
        "400":
          description: bad input parameter
      summary: Search and paginate per-message Mail Baby delivery log entries
      tags:
      - Mail
  /vps/{id}/restore:
    post:
      description: |
        **DESTRUCTIVE.** Overwrites the VPS disk with a previously created backup. Re-authenticates via the customer's MyAdmin password (when set), validates the backup row from `getVpsBackups`, checks disk capacity (skipped for ZFS), then queues `snapshot_restore` (ZFS — instant) or `restore` (Swift/MinIO — copy) on the hypervisor. Allow ~10 minutes. **Recommended pre-step:** `getVpsBackup` to snapshot current state first. Sibling ops: `getVpsBackups`, `getVpsBackup`, `postVpsReinstallOs` (wipe to fresh OS instead).

        **Path param:**
        - `id` (integer, required) — VPS id from `getVpsList.vps_id`.

        **Body fields:**
        - `backup` (string, required) — composite key `<type>:<service>:<name>` matching one of the entries from `getVpsBackups`. `type` is `swift` / `minio` / `zfs`; `service` is the originating VPS id; `name` is the backup filename.
        - `password` (string, required for non-admin callers when the account has a password set) — the customer's MyAdmin account password (re-auth check via `auth->authenticate`).

        **Returns:** `{ text: "Action has been sent to the server. Please allow up to 10 minutes for action to be completed.", queueId: <integer> }`.

        **Side effects:**
        - Inserts `vpsqueue` `snapshot_restore` (ZFS) or `restore` (Swift/MinIO) row.
        - Calls `vps_resetup_vnc()`.

        **Auth:** Session/API key plus re-auth via `password` for non-admin callers.

        **Errors:**
        - `401` — unauthenticated.
        - `404 Invalid VPS Passed`.
        - `409 VPS is not active`.
        - `You do not seem to currently have a backup that you are allowed to restore yourself.` — no eligible backups found (admin-only Swift backups are excluded for non-admin callers).
        - `Invalid Password` — `password` re-auth failed.
        - `Invalid Backup Image` — `backup` doesn't match any row in `getVpsBackups`.
        - `Not Enough Space To Restore Backup. (Backup Takes up X bytes, The VPS Has Y)` — disk-size check failed (skipped for ZFS).

        **Related calls:**
        - **Find the backup:** `getVpsBackups` (capture `type`, `service`, `name`).
        - **Snapshot before restoring:** `getVpsBackup`.
        - **Alternative (fresh OS, no data preserved):** `postVpsReinstallOs`.
        - **Verify after restore:** `getVpsInfo`.
      operationId: postVpsRestore
      parameters:
      - description: VPS ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/RestoreRequest"
        description: VPS Restore request
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Restore the VPS from a backup (DESTRUCTIVE — overwrites disk)
      tags:
      - VPS
  /qs/{id}/restore:
    post:
      description: |-
        Overwrites the live disk with a backup. Path param: `id`. Body (form): `backup` (composite key `<type>:<service>:<name>` from `getQsBackups`), `password` (caller's account login password — required for non-admin to confirm).
        Validates backup exists, caller's password (when applicable), and that the QuickServer disk is large enough (size check skipped for ZFS). Queues `snapshot_restore` for ZFS or `restore` for swift/minio; allow up to 10 minutes. Returns: `{ text, queueId }`. Errors: 401, 404 if not owned, 409 if status != `active`, errors for invalid password, missing backup, or insufficient disk space.
        Siblings: `getQsBackups`, `getQsBackup` (create), `postVpsRestore`.
      operationId: postQuickServerRestore
      parameters:
      - description: QuickServer ID number
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RestoreRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/RestoreRequest"
        description: QuickServer Restore request
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
          description: Response message from sending a service queue.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Restore a QuickServer from a backup (DESTRUCTIVE — overwrites disk)
      tags:
      - QuickServers
  /domains/{id}/nameservers:
    delete:
      description: |
        Removes a single registered nameserver entry from the domain at the registrar. Handler first calls OpenSRS `nsGet` to resolve the entry by index, then `nsDelete`. Per-entry — to replace the full delegation set use `updateDomainNameservers` instead. Sibling ops: `getDomainNameservers`, `addDomainNameserver`, `updateDomainNameservers`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Query params:**
        - `index` (integer, required) — zero-based index from `getDomainNameservers` array — or alternatively pass `name` + `ip` to target by value.

        **Returns:** `SuccessTextResponse`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - `422` — `index` out of range or `name`/`ip` not provided.
        - Registrar errors surfaced as 4xx.

        **Related calls:**
        - **List candidates:** `getDomainNameservers`.
        - **Replace delegation entirely:** `updateDomainNameservers`.
      operationId: deleteDomainNameserver
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      - description: "The index of the registered nameserver from the registered nameservers\
          \ list to delete.\r\n"
        explode: true
        in: query
        name: index
        required: true
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Remove one registered nameserver glue record from the domain
      tags:
      - Domains
    get:
      description: |
        Returns the registered-nameserver / glue-record entries currently set for the domain at OpenSRS. **Important distinction:** these are *registered nameservers* (`host.example.com` + glue IP records the registrant has created at the registry), NOT the domain's delegation NS records. To replace the delegation set, use `updateDomainNameservers`. Empty array means no glue records are registered. Sibling ops: `addDomainNameserver`, `updateDomainNameservers`, `deleteDomainNameserver`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Returns** (schema `DomainNameserverGetResponse`):
        - Array of `{name, ipaddress, sortorder}` — use the zero-based array index when calling `deleteDomainNameserver` with `index`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - `5xx` — registrar communication failure.

        **Related calls:**
        - **Add glue record:** `addDomainNameserver`.
        - **Replace delegation set:** `updateDomainNameservers`.
        - **Remove glue record:** `deleteDomainNameserver`.
      operationId: getDomainNameservers
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainNameserverGetResponse"
          description: Domain registered nameservers list response
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: List registered nameserver hosts and glue IP addresses for a domain
      tags:
      - Domains
    post:
      description: |
        Adds a registered nameserver (host + glue IP) for the domain at the registrar via `opensrs_create_nameserver`. Use when running your own authoritative nameservers under the domain itself (e.g. `ns1.example.com`, `ns2.example.com`). **Glue records, not delegation:** this registers the host's IP at the registry; the domain's delegation must separately point at these names via `updateDomainNameservers`. Sibling ops: `getDomainNameservers`, `updateDomainNameservers`, `deleteDomainNameserver`.

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields (schema `DomainNameserverPostRequest`):**
        - `name` (string, required) — FQDN of the nameserver (e.g. `ns1.example.com`). Validated by `valid_hostname()`.
        - `ipAddress` (string, required) — IPv4 address. Validated by `validIp()`.

        **Returns:** `SuccessTextResponse` on success.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"`.
        - `422` — invalid hostname or IP, or registrar refused.

        **Related calls:**
        - **Verify glue record:** `getDomainNameservers`.
        - **Use this nameserver in delegation:** `updateDomainNameservers`.
      operationId: addDomainNameserver
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainNameserverPostRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DomainNameserverPostRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Register a new nameserver host with glue IP at the registry (registered
        nameserver)
      tags:
      - Domains
    put:
      description: |
        Atomically replaces the domain's authoritative-nameserver delegation list via OpenSRS `nsAdvancedUpdt` with `op_type=assign`. **Domain must be unlocked.** Use this to switch DNS providers (e.g. point at InterServer `cdns1`/`cdns2`, Cloudflare, AWS Route53, etc.). **Replaces the full set in one call** — partial updates are not supported through this endpoint. To register a new glue record (host + IP) at the registry, use `addDomainNameserver`. Sibling ops: `getDomainNameservers`, `addDomainNameserver`, `deleteDomainNameserver`, `addDnsDomain` (host a zone at InterServer).

        **Path param:**
        - `id` (integer, required) — `domain_id` from `getDomainsList`.

        **Body fields (JSON or multipart, schema `DomainNameserverPutRequest`):**
        - `nameserver[]` (array of strings, required) — FQDN strings (e.g. `["ns1.example.com", "ns2.example.com"]`). Trimmed; empty entries dropped server-side.

        **Returns:** `SuccessTextResponse` on registrar `is_success=1`.

        **Auth:** Session/API key. Ownership enforced.

        **Errors:**
        - `401` — unauthenticated.
        - `404` — `id` not owned by caller.
        - `409` — `domain_status != "active"` or domain locked.
        - Registrar errors surfaced as 4xx.

        **Related calls:**
        - **Read current set:** `getDomainNameservers`.
        - **Register a glue record:** `addDomainNameserver`.
        - **Host the DNS zone at InterServer:** `addDnsDomain` (DNS tag).
      operationId: updateDomainNameservers
      parameters:
      - description: The domain service ID. Use `domain_id` from `GET /domains`.
        explode: false
        in: path
        name: id
        required: true
        schema:
          type: integer
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DomainNameserverPutRequest"
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/DomainNameserverPutRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TextResponse"
          description: Response with a text message field.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Replace the full authoritative-nameserver delegation list at the registrar
      tags:
      - Domains
  /servers/order/buy_now_server:
    get:
      description: |
        Step 1 of the Rapid Deploy / coupon dedicated server order flow. Returns options + pricing for either a marketplace asset (`a=<asset_id>`) or a coupon (`c=<coupon_name>`) so the order form can be rendered before `placeBuyNowServer`. Read-only; no charge. Sibling ops: `placeBuyNowServer` (commit), `getMPServers` (browse marketplace), `addServer` (custom build flow).

        **Query (one required):**
        - `a` (integer) — asset_id from `getMPServers`.
        - `c` (string) — `server_coupons.name`.

        **Returns:** `{ bandwidth[], ips[], os[], cp[], raid[], regions[], a?: {asset + items}, c?: {coupon + region} }`. Each option row is `{ id, short_desc, long_desc, monthly_price }` — feed those ids into `placeBuyNowServer`.

        **Auth:** Session/API key.

        **Errors:**
        - `400` — `'No Server Coupon or Market-Place Asset Specified'` when neither `a` nor `c` is passed.
        - `400` — `'Invalid Asset ID'` / `'No Server Coupon with that name'`.
        - `409` — `'Server already sold!'` (asset already in-cart) or `'Server Out of stock'` (coupon).
        - `401` — unauthenticated.

        **Related calls:**
        - **Next:** `placeBuyNowServer` (commit the order).
        - **Browse:** `getMPServers`.
        - **Custom build alternative:** `addServer`.
      operationId: buyItNowServerOrder
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/buyItNowServerOrder_200_response"
          description: Available server configurations with pricing and hardware options.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
      summary: Get configurable options for a Rapid Deploy / coupon dedicated server
      tags:
      - Servers
    post:
      description: |
        Step 2 of the Rapid Deploy / coupon order flow. Commits a marketplace asset OR coupon-based dedicated server order. Inserts the `servers` row, creates a `Repeat_Invoice` plus the first `invoices` row, marks the asset `MarketPlace-Incart` (or decrements `server_coupons.in_stock`), then emails customer + admin. **Real billable order — confirm intent first.** Sibling ops: `buyItNowServerOrder` (catalog), `getServerInfo` (poll provisioning), `getServerInvoices` (billing), `addServer` (custom build alternative).

        **Query (one required, same as `buyItNowServerOrder`):**
        - `a` (integer) — asset_id.
        - `c` (string) — `server_coupons.name`.

        **Body fields:**
        - `hostname` (string, required) — valid FQDN; validated by `valid_hostname`.
        - `enablepassword` (boolean, optional, default `false`) — when true the client must supply `rootPassword`; otherwise a secure password is generated server-side via `generate_password()`.
        - `rootPassword` (string, required when `enablepassword=true`) — must be ≥8 chars with at least one uppercase, lowercase, digit, and special character (`valid_password`).
        - `os`, `bandwidth`, `ips`, `cp`, `raid` (integer, optional) — option ids from `buyItNowServerOrder`; defaults `30` / `10` / `9` / `1` / `0` applied when missing.
        - `comments` (string, optional) — appended to the order comment.

        **Returns:** `201 { success: true, text: 'Server order is placed.', service_id, invoice_id }`.

        **Auth:** Session/API key.

        **Errors:**
        - `400` — `'Server Hostname is missing.'` / `'Invalid Hostname!'` / `'Server Password is missing.'` / password complexity message.
        - `409` — `'Server already sold!'` / `'Server Out of stock.'`
        - `401` — unauthenticated.

        **Side effects:** inserts `servers` row, creates `repeat_invoices` + `invoices` rows, updates `assets.status` or `server_coupons.in_stock`, queues admin + customer welcome emails.

        **Related calls:**
        - **Prerequisite:** `buyItNowServerOrder`.
        - **Next:** `getBillingInvoice` + `initiatePayment` to pay, then poll `getServerInfo` for provisioning state.
        - **Custom build alternative:** `addServer`.
      operationId: placeBuyNowServer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/placeBuyNowServer_request"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServersBuyNowResponse"
          description: Order placed successfully.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServersBuyNowError"
          description: Order validation failed.
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/getAccountInfo_401_response"
          description: Unauthorized
        default:
          description: Default response
      summary: Place a Rapid Deploy / coupon dedicated server order; creates real
        invoice
      tags:
      - Servers
components:
  responses:
    BadInput:
      content:
        application/json:
          examples:
            InvalidTimezone:
              value:
                code: 422
                message: Invalid timezone
            MissingCaptcha:
              value:
                code: 422
                message: missing captcha
          schema:
            $ref: "#/components/schemas/getAccountInfo_401_response"
      description: The specified resource was not found
    LoginResponseError:
      content:
        application/json:
          examples:
            MissingTwoFactorCode:
              value:
                message: missing 2fa
                field: tfa
            InvalidTwoFactorCode:
              value:
                message: invalid 2fa
                field: tfa
            MissingCaptcha:
              value:
                message: missing captcha
                field: captcha
            TooManyVerificationAttempts:
              value:
                message: "max tries treached on code, sending new one, submit again"
                field: verify
          schema:
            $ref: "#/components/schemas/LoginErrorResponse"
      description: Error response to a login request.
    NotFound:
      content:
        application/json:
          examples:
            InvalidVps:
              value:
                code: 404
                message: Invalid VPS Passed
            InvalidService:
              value:
                code: 404
                message: Invalid Service Passed
          schema:
            $ref: "#/components/schemas/getAccountInfo_401_response"
      description: The specified resource was not found
    ServiceOrderPostResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ServiceOrderPostResponse"
      description: "Order placed successfully. Use the invoice ID to proceed to payment\
        \ via `/billing/pay/{method}/{invoices}` or view the invoice at `/billing/invoices/{id}`."
      links:
        InitiatePayment:
          description: Use the invoice ID from the order response to retrieve invoice
            details or proceed to payment.
          operationId: getBillingInvoice
          parameters:
            id: $response.body#/iid
    QueueResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/QueueResponse"
      description: Response message from sending a service queue.
    SuccessTextResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SuccessTextResponse"
      description: A response indicating the operation completed successfully with
        a text message.
    TextResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: Response with a text message field.
    AccountValidationError:
      content:
        application/json:
          examples:
            InvalidTimeZone:
              value:
                message: Invalid Time Zone
            MissingRequiredFields:
              value:
                message: "fname, lname cannot be empty!"
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: Validation error while updating account data.
    DomainSearchNoResults:
      content:
        application/json:
          examples:
            InvalidSearchResponse:
              value:
                message: Invalid Search Response!  Either there was an error communicating
                  with the registrars or We received no results.
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: No search suggestions or registrar response available.
    InvalidDomain:
      content:
        application/json:
          examples:
            InvalidDomainName:
              value:
                message: Invalid Domain
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: The domain input was invalid.
    InvalidIpAddress:
      content:
        application/json:
          examples:
            InvalidIp:
              value:
                message: Invalid IP Address
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: IP limit payload contains an invalid address.
    InvalidTwoFactorCode:
      content:
        application/json:
          examples:
            InvalidCode:
              value:
                message: Invalid Code
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: Provided two-factor verification code was invalid.
    NothingToUpdate:
      content:
        application/json:
          examples:
            NoChanges:
              value:
                message: Nothing to update
          schema:
            $ref: "#/components/schemas/TextResponse"
      description: Request was valid but did not contain updatable values.
    Unauthorized:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/getAccountInfo_401_response"
      description: Unauthorized
    WebsiteCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/webhostingCancel_200_response"
      description: Website cancel
    VPSCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/VPSCancel_200_response"
      description: VPS Cancel
    DomainsCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CancelDomain_200_response"
      description: Domains Cancel
    BackupsCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/cancelBackup_200_response"
      description: Backups Cancel
    MailCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/mailCancel_200_response"
      description: Mail Cancel
    LicensesCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/licensesCancel_200_response"
      description: License Cancel
    SSLCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/sslCancel_200_response"
      description: SSL Cancel
    FloatingIpsCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/floating_ipsCancel_200_response"
      description: Floating Ip Cancel
    QuickserversCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/quickserversCancel_200_response"
      description: Rapid Deploy Servers Cancel
    ServerCancelResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/serversCancel_200_response"
      description: Servers Cancel
    LoginSuccessResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/LoginSuccessResponse"
      description: Successful login response containing the session token.
      links:
        getAccountInfo:
          description: "After a successful login, use the session token to fetch account\
            \ details via `GET /account`."
          operationId: getAccountInfo
    ServerIpmiGetResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ServerIpmiLiveInfo"
      description: Response from the Servers IPMI Live information request.
    TicketNewResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TicketNewResponse"
      description: A successful response after creating a ticket
    ViewTicketResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ViewTicketResponse"
      description: Ticket Information
    UpdateTicketResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UpdateTicketResponseSchema"
      description: Response from updating a support ticket.
    ReplyTicketResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ReplyTicketResponseSchema"
      description: Post Reply to the ticket.
    CloseTicketResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CloseTicketResponseSchema"
      description: Close ticket.
    scrubIpsLists:
      content:
        application/json:
          examples:
            ScrubIpsListExample:
              value:
              - scrub_ip_id: 123
                repeat_invoices_cost: 5
                scrub_ip_ip: 1.2.3.5
                scrub_ip_status: active
                services_name: Current IP + Scrub
              - scrub_ip_id: 456
                repeat_invoices_cost: 5
                scrub_ip_ip: 4.3.2.1
                scrub_ip_status: expired
                services_name: Current IP + Scrub
          schema:
            items:
              $ref: "#/components/schemas/ScrubIpsRowSchema"
            type: array
      description: Scrub Ips list
    CreateFirewallResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createRule_201_response"
      description: Create firewall rule for scrub ip
    DeleteFirewallResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/scrubIpsDeleteRule_200_response"
      description: Delete firewall rule for scrub ip
    CreateFirewall400Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createRule_400_response"
      description: Bad Request
    CreateGeoFirewall400Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createGeoRule_400_response"
      description: Bad Request
    DeleteFirewall400Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/scrubIpsDeleteRule_400_response"
      description: Bad Request
    CreateFirewall500Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createRule_500_response"
      description: Internal Server Error
    DeleteFirewall500Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/scrubIpsDeleteRule_500_response"
      description: Internal Server Error
    CreateFilterResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createFilter_201_response"
      description: Request OK
    DeleteFilterResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/deleteFilter_200_response"
      description: Delete filter for scrub ip
    CreateFilter400Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createFilter_400_response"
      description: Bad Request
    DeleteFilter400Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/deleteFilter_400_response"
      description: Bad Request
    CreateFilter500Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/createFilter_500_response"
      description: Internal Server Error
    DeleteFilter500Err:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/deleteFilter_500_response"
      description: Internal Server Error
    OrderScrubGetResponse:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/getOrderDetail_200_response"
      description: Order details
    ScrubIpPlaceOrder:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/placeScrubOrder_201_response"
      description: Request OK
    ScrubIpLogs:
      content:
        application/json:
          examples:
            ScrubIpsListExample:
              value:
              - date: "Feb 24, 2026 @ 05:39 pm"
                filter: TCP FW Drop
                blocked_ip: 49.36.168.21
                target_ip: 173.214.163.18
                target_port: 2083
                protocol: TCP
                byte_count: 1514
                xdp_action: Allow
              - date: "Feb 24, 2026 @ 05:39 pm"
                filter: Generic UDP
                blocked_ip: 67.215.86.57
                target_ip: 173.214.163.18
                target_port: 53
                protocol: UDP
                byte_count: 84
                xdp_action: Allow
          schema:
            items:
              $ref: "#/components/schemas/ScrubIpsLogRowSchema"
            type: array
      description: Scrub Ips logs
  schemas:
    AccountFeatures:
      description: Account Features data.
      example:
        disable_reset: 1
        disable_reinstall: 1
      properties:
        disable_reset:
          format: int32
          type: integer
        disable_reinstall:
          format: int32
          type: integer
      title: AccountFeatures
      type: object
    AccountInfo:
      description: "Contains the full account profile including personal info, billing\
        \ details, OAuth connections, and security settings."
      example:
        custid: "98651"
        ima: client
        data:
          group: "0"
          address: 112 Uber St.
          city: Townsville
          country: US
          disable_cc: "0"
          fraudrecord_score: "0"
          ima: client
          name: John Doe
          payment_method: paypal
          phone: "8675309"
          pin: "000000"
          state: PA
          status: active
          zip: "11111"
          account_id: "98651"
          account_lid: user@domain.com
          address2: ""
          affiliate_dock_description: Use this coupon when placing an order to get
            the first month of hosting for only 1 penny.
          affiliate_dock_title: Exclusive offer to viewers
          affiliate_payment_method: paypal
          affiliate_paypal: user@domain.com
          cc: '************1111'
          cc_auto: "0"
          cc_exp: 05/2019
          cc_type: visa
          cc_whitelist: "1"
          ccs:
            "1":
              cc: '************1111'
              cc_exp: 05/2019
              name: John Doe
              country: US
              verified: true
            "5":
              cc: '************2222'
              cc_exp: 05/2019
              name: John Doe
              address: 123 Uber St
              city: Townsville
              state: PA
              zip: "11122"
              country: US
              maxmind_riskscore: "0.25"
              maxmind:
                distance: "0"
                countryMatch: "Yes"
                countryCode: US
                freeMail: "No"
                anonymousProxy: "No"
                binMatch: "No"
                binCountry: IN
                err: ""
                proxyScore: "0.00"
                ip_region: PA
                ip_city: Townsville
                ip_latitude: "40.175"
                ip_longitude: "-76.1817"
                binName: state bank of india
                ip_isp: PenTeleData
                ip_org: PenTeleData
                binNameMatch: NA
                binPhoneMatch: NA
                binPhone: ""
                custPhoneInBillingLoc: NotFound
                highRiskCountry: "No"
                queriesRemaining: "2764"
                cityPostalMatch: "Yes"
                shipCityPostalMatch: ""
                maxmindID: 7IVXKOVV
                ip_asnum: AS3737 AS-PTD
                ip_userType: residential
                ip_countryConf: "99"
                ip_regionConf: "99"
                ip_cityConf: "30"
                ip_postalCode: "11122"
                ip_postalConf: "30"
                ip_accuracyRadius: "20"
                ip_netSpeedCell: Cable/DSL
                ip_metroCode: "566"
                ip_areaCode: ""
                ip_timeZone: America/New_York
                ip_regionName: Pennsylvania
                ip_domain: ptd.net
                ip_countryName: United States
                ip_continentCode: NA
                ip_corporateProxy: "No"
                isTransProxy: "No"
                carderEmail: "No"
                highRiskUsername: "No"
                highRiskPassword: "No"
                riskScore: 0.25
                prepaid: "No"
                minfraud_version: "1.3"
                service_level: premium
                female_name: "no"
              verified: true
            "6":
              cc: '************1111'
              cc_exp: 05/2021
              name: John Doe
              city: Townsville
              state: PA
              zip: "11122"
              country: US
              maxmind_riskscore: "0.48"
              maxmind:
                distance: "0"
                countryMatch: "Yes"
                countryCode: US
                freeMail: "No"
                anonymousProxy: "No"
                binMatch: "Yes"
                binCountry: US
                err: ""
                proxyScore: "0.00"
                ip_region: PA
                ip_city: Townsville
                ip_latitude: "40.175"
                ip_longitude: "-76.1817"
                binName: Bank of No Hope
                ip_isp: PenTeleData
                ip_org: PenTeleData
                binNameMatch: NA
                binPhoneMatch: NA
                binPhone: "1234561234"
                custPhoneInBillingLoc: NotFound
                highRiskCountry: "No"
                queriesRemaining: "2765"
                cityPostalMatch: "Yes"
                shipCityPostalMatch: ""
                maxmindID: AAAAAZZZZZ
                ip_asnum: AS3737 AS-PTD
                ip_userType: residential
                ip_countryConf: "99"
                ip_regionConf: "99"
                ip_cityConf: "30"
                ip_postalCode: "11122"
                ip_postalConf: "30"
                ip_accuracyRadius: "20"
                ip_netSpeedCell: Cable/DSL
                ip_metroCode: "566"
                ip_areaCode: ""
                ip_timeZone: America/New_York
                ip_regionName: Pennsylvania
                ip_domain: ptd.net
                ip_countryName: United States
                ip_continentCode: NA
                ip_corporateProxy: "No"
                isTransProxy: "No"
                carderEmail: "No"
                highRiskUsername: "No"
                highRiskPassword: "No"
                riskScore: 0.48
                prepaid: "No"
                minfraud_version: "1.3"
                service_level: premium
                female_name: "no"
              verified: true
          ccs_added: "14"
          company: "InterServer, Inc."
          currency: USD
          disable_reinstall: "0"
          disable_reset: "0"
          email: user@domain.com
          email_abuse: ""
          email_settings:
            admin/cc_bad_response: "1"
            admin/mass_communications.tpl: "1"
          extra:
            private_whois: "0"
          facebook_id: "111111111111111"
          facebook_url: https://www.facebook.com/111111111111111
          firstname: John
          fraudrecord:
            score: "0"
            count: "0"
            reliability: "0.0"
            code: zxcvzxcvzxcv
          github_id: "2222222"
          github_url: https://github.com/user
          google_id: "355431342512341234"
          google_url: https://plus.google.com/+JohnDoe
          innertell_id: "2222222"
          lastname: Doe
          locale: auto
          maxmind:
            distance: "6146"
            countryMatch: "No"
            countryCode: US
            freeMail: "No"
            anonymousProxy: "No"
            score: "5.00"
            binMatch: NotFound
            binCountry: ""
            err: CITY_NOT_FOUND
            proxyScore: "0.00"
            ip_region: PA
            ip_city: Townsville
            ip_latitude: "40.1767"
            ip_longitude: "-76.4297"
            binName: ""
            ip_isp: PenTeleData
            ip_org: PenTeleData
            binNameMatch: NA
            binPhoneMatch: NA
            binPhone: ""
            custPhoneInBillingLoc: ""
            highRiskCountry: "No"
            queriesRemaining: "171"
            cityPostalMatch: ""
            shipCityPostalMatch: ""
            maxmindID: HMOCUJP7
            ip_asnum: AS3737 PenTeleData Inc.
            ip_userType: residential
            ip_countryConf: "99"
            ip_regionConf: "97"
            ip_cityConf: "30"
            ip_postalCode: "11122"
            ip_postalConf: "30"
            ip_accuracyRadius: "10"
            ip_netSpeedCell: Dialup
            ip_metroCode: "566"
            ip_areaCode: "333"
            ip_timeZone: America/New_York
            ip_regionName: Pennsylvania
            ip_domain: ptd.net
            ip_countryName: United States
            ip_continentCode: NA
            ip_corporateProxy: "No"
            carderEmail: "No"
            highRiskUsername: "No"
            riskScore: "4.82"
            explanation: "You should review this order carefully, as it is considered\
              \ high risk. We suggest you be very cautious about accepting this order.\
              \ This order is higher risk because the distance between the billing\
              \ address and the user's actual location is so great. The order is higher\
              \ risk because the billing country and the country in which the IP address\
              \ is located don't match"
            female_name: "no"
          maxmind_score: "0"
          mb_id: "4769"
          modernbill_id: "1234"
          picture: https://avatars3.githubusercontent.com/u/1364504
          referrer_coupon: detainaffiliatecoupon
          reseller_markup: "10"
          username: user@domain.com
          ssh_key: zzzz
          ssh_key_wrapped: zzzz
          api_key: aaaaaaaaaaaaaaaaaaaaaaabbbbbbbbbbbbbccccccccccccccccccc
          api_key_wrapped: aaaaaaaaaaaaaaaaaaaaaaa<br>bbbbbbbbbbbbb<br>ccccccccccccccccccc
          "2fa_google_key": ssssssssssssssssssssssssss
          "2fa_google_enabled": true
          "2fa_google": 1
          "2fa_google_split": 'zzzz zzzz zzzz zzzz '
          "2fa_google_qr": "data:image/png;base64,zzz"
        ip: 1.2.3.4
        oauthproviders: []
        oauthconfig:
          callback: https://my.interserver.net/oauth/callback.php
          providers:
            Twitter:
              enabled: false
              linked: false
            Facebook:
              enabled: true
              account: "111111111111111111111"
              url: https://www.facebook.com/111111111111111111111
              linked: true
            Google:
              enabled: true
              account: "111111111111111111111"
              url: https://plus.google.com/+UserName
              linked: true
            GitHub:
              enabled: true
              account: "111111111111111111111"
              url: https://github.com/detain
              linked: true
        oauthadapters: []
        limits:
        - start: 1.1.1.1
          end: 1.1.1.254
        - start: 2600:387:0:809::1b
          end: 2600:387:0:809::1b
        language: en-US
        countryCurrencies:
          CN:
          - USD
          BR:
          - USD
          - BRL
          RU:
          - USD
        enableLocales: true
        enableCurrencies: false
        gravatar: https://avatars3.githubusercontent.com/u/1364504
      properties:
        custid:
          type: string
        ima:
          type: string
        data:
          $ref: "#/components/schemas/AccountInfoData"
        ip:
          type: string
        oauthproviders:
          $ref: "#/components/schemas/AccountInfo_oauthproviders"
        oauthconfig:
          $ref: "#/components/schemas/AccountInfoOauthConfig"
        oauthadapters:
          items:
            type: string
          type: array
        limits:
          $ref: "#/components/schemas/AccountInfoLimits"
        language:
          type: string
        countryCurrencies:
          $ref: "#/components/schemas/AccountInfoCountryCurrencies"
        enableLocales:
          type: boolean
        enableCurrencies:
          type: boolean
        gravatar:
          type: string
      title: AccountInfo
      type: object
    AccountInfoCountryCurrencies:
      additionalProperties:
        items:
          type: string
        type: array
      description: Maps country codes to their available currency options.
      example:
        CN:
        - USD
        BR:
        - USD
        - BRL
        RU:
        - USD
      title: AccountInfoCountryCurrencies
      type: object
    AccountInfoData:
      description: "Detailed account profile data including contact info, billing,\
        \ API keys, and security settings."
      example:
        group: "0"
        address: 112 Uber St.
        city: Townsville
        country: US
        disable_cc: "0"
        fraudrecord_score: "0"
        ima: client
        name: John Doe
        payment_method: paypal
        phone: "8675309"
        pin: "000000"
        state: PA
        status: active
        zip: "11111"
        account_id: "98651"
        account_lid: user@domain.com
        address2: ""
        affiliate_dock_description: Use this coupon when placing an order to get the
          first month of hosting for only 1 penny.
        affiliate_dock_title: Exclusive offer to viewers
        affiliate_payment_method: paypal
        affiliate_paypal: user@domain.com
        cc: '************1111'
        cc_auto: "0"
        cc_exp: 05/2019
        cc_type: visa
        cc_whitelist: "1"
        ccs:
          "1":
            cc: '************1111'
            cc_exp: 05/2019
            name: John Doe
            country: US
            verified: true
          "5":
            cc: '************2222'
            cc_exp: 05/2019
            name: John Doe
            address: 123 Uber St
            city: Townsville
            state: PA
            zip: "11122"
            country: US
            maxmind_riskscore: "0.25"
            maxmind:
              distance: "0"
              countryMatch: "Yes"
              countryCode: US
              freeMail: "No"
              anonymousProxy: "No"
              binMatch: "No"
              binCountry: IN
              err: ""
              proxyScore: "0.00"
              ip_region: PA
              ip_city: Townsville
              ip_latitude: "40.175"
              ip_longitude: "-76.1817"
              binName: state bank of india
              ip_isp: PenTeleData
              ip_org: PenTeleData
              binNameMatch: NA
              binPhoneMatch: NA
              binPhone: ""
              custPhoneInBillingLoc: NotFound
              highRiskCountry: "No"
              queriesRemaining: "2764"
              cityPostalMatch: "Yes"
              shipCityPostalMatch: ""
              maxmindID: 7IVXKOVV
              ip_asnum: AS3737 AS-PTD
              ip_userType: residential
              ip_countryConf: "99"
              ip_regionConf: "99"
              ip_cityConf: "30"
              ip_postalCode: "11122"
              ip_postalConf: "30"
              ip_accuracyRadius: "20"
              ip_netSpeedCell: Cable/DSL
              ip_metroCode: "566"
              ip_areaCode: ""
              ip_timeZone: America/New_York
              ip_regionName: Pennsylvania
              ip_domain: ptd.net
              ip_countryName: United States
              ip_continentCode: NA
              ip_corporateProxy: "No"
              isTransProxy: "No"
              carderEmail: "No"
              highRiskUsername: "No"
              highRiskPassword: "No"
              riskScore: 0.25
              prepaid: "No"
              minfraud_version: "1.3"
              service_level: premium
              female_name: "no"
            verified: true
          "6":
            cc: '************1111'
            cc_exp: 05/2021
            name: John Doe
            city: Townsville
            state: PA
            zip: "11122"
            country: US
            maxmind_riskscore: "0.48"
            maxmind:
              distance: "0"
              countryMatch: "Yes"
              countryCode: US
              freeMail: "No"
              anonymousProxy: "No"
              binMatch: "Yes"
              binCountry: US
              err: ""
              proxyScore: "0.00"
              ip_region: PA
              ip_city: Townsville
              ip_latitude: "40.175"
              ip_longitude: "-76.1817"
              binName: Bank of No Hope
              ip_isp: PenTeleData
              ip_org: PenTeleData
              binNameMatch: NA
              binPhoneMatch: NA
              binPhone: "1234561234"
              custPhoneInBillingLoc: NotFound
              highRiskCountry: "No"
              queriesRemaining: "2765"
              cityPostalMatch: "Yes"
              shipCityPostalMatch: ""
              maxmindID: AAAAAZZZZZ
              ip_asnum: AS3737 AS-PTD
              ip_userType: residential
              ip_countryConf: "99"
              ip_regionConf: "99"
              ip_cityConf: "30"
              ip_postalCode: "11122"
              ip_postalConf: "30"
              ip_accuracyRadius: "20"
              ip_netSpeedCell: Cable/DSL
              ip_metroCode: "566"
              ip_areaCode: ""
              ip_timeZone: America/New_York
              ip_regionName: Pennsylvania
              ip_domain: ptd.net
              ip_countryName: United States
              ip_continentCode: NA
              ip_corporateProxy: "No"
              isTransProxy: "No"
              carderEmail: "No"
              highRiskUsername: "No"
              highRiskPassword: "No"
              riskScore: 0.48
              prepaid: "No"
              minfraud_version: "1.3"
              service_level: premium
              female_name: "no"
            verified: true
        ccs_added: "14"
        company: "InterServer, Inc."
        currency: USD
        disable_reinstall: "0"
        disable_reset: "0"
        email: user@domain.com
        email_abuse: ""
        email_settings:
          admin/cc_bad_response: "1"
          admin/mass_communications.tpl: "1"
        extra:
          private_whois: "0"
        facebook_id: "111111111111111"
        facebook_url: https://www.facebook.com/111111111111111
        firstname: John
        fraudrecord:
          score: "0"
          count: "0"
          reliability: "0.0"
          code: zxcvzxcvzxcv
        github_id: "2222222"
        github_url: https://github.com/user
        google_id: "355431342512341234"
        google_url: https://plus.google.com/+JohnDoe
        innertell_id: "2222222"
        lastname: Doe
        locale: auto
        maxmind:
          distance: "6146"
          countryMatch: "No"
          countryCode: US
          freeMail: "No"
          anonymousProxy: "No"
          score: "5.00"
          binMatch: NotFound
          binCountry: ""
          err: CITY_NOT_FOUND
          proxyScore: "0.00"
          ip_region: PA
          ip_city: Townsville
          ip_latitude: "40.1767"
          ip_longitude: "-76.4297"
          binName: ""
          ip_isp: PenTeleData
          ip_org: PenTeleData
          binNameMatch: NA
          binPhoneMatch: NA
          binPhone: ""
          custPhoneInBillingLoc: ""
          highRiskCountry: "No"
          queriesRemaining: "171"
          cityPostalMatch: ""
          shipCityPostalMatch: ""
          maxmindID: HMOCUJP7
          ip_asnum: AS3737 PenTeleData Inc.
          ip_userType: residential
          ip_countryConf: "99"
          ip_regionConf: "97"
          ip_cityConf: "30"
          ip_postalCode: "11122"
          ip_postalConf: "30"
          ip_accuracyRadius: "10"
          ip_netSpeedCell: Dialup
          ip_metroCode: "566"
          ip_areaCode: "333"
          ip_timeZone: America/New_York
          ip_regionName: Pennsylvania
          ip_domain: ptd.net
          ip_countryName: United States
          ip_continentCode: NA
          ip_corporateProxy: "No"
          carderEmail: "No"
          highRiskUsername: "No"
          riskScore: "4.82"
          explanation: "You should review this order carefully, as it is considered\
            \ high risk. We suggest you be very cautious about accepting this order.\
            \ This order is higher risk because the distance between the billing address\
            \ and the user's actual location is so great. The order is higher risk\
            \ because the billing country and the country in which the IP address\
            \ is located don't match"
          female_name: "no"
        maxmind_score: "0"
        mb_id: "4769"
        modernbill_id: "1234"
        picture: https://avatars3.githubusercontent.com/u/1364504
        referrer_coupon: detainaffiliatecoupon
        reseller_markup: "10"
        username: user@domain.com
        ssh_key: zzzz
        ssh_key_wrapped: zzzz
        api_key: aaaaaaaaaaaaaaaaaaaaaaabbbbbbbbbbbbbccccccccccccccccccc
        api_key_wrapped: aaaaaaaaaaaaaaaaaaaaaaa<br>bbbbbbbbbbbbb<br>ccccccccccccccccccc
        "2fa_google_key": ssssssssssssssssssssssssss
        "2fa_google_enabled": true
        "2fa_google": 1
        "2fa_google_split": 'zzzz zzzz zzzz zzzz '
        "2fa_google_qr": "data:image/png;base64,zzz"
      properties:
        group:
          type: string
        address:
          type: string
        city:
          type: string
        country:
          type: string
        disable_cc:
          type: string
        fraudrecord_score:
          type: string
        ima:
          type: string
        name:
          type: string
        payment_method:
          type: string
        phone:
          type: string
        pin:
          type: string
        state:
          type: string
        status:
          type: string
        zip:
          type: string
        account_id:
          type: string
        account_lid:
          type: string
        address2:
          type: string
        affiliate_dock_description:
          type: string
        affiliate_dock_title:
          type: string
        affiliate_payment_method:
          type: string
        affiliate_paypal:
          type: string
        cc:
          type: string
        cc_auto:
          type: string
        cc_exp:
          type: string
        cc_type:
          type: string
        cc_whitelist:
          type: string
        ccs:
          $ref: "#/components/schemas/AccountInfoDataCcs"
        ccs_added:
          type: string
        company:
          type: string
        currency:
          type: string
        disable_reinstall:
          type: string
        disable_reset:
          type: string
        email:
          type: string
        email_abuse:
          type: string
        email_settings:
          $ref: "#/components/schemas/AccountInfoData_email_settings"
        extra:
          $ref: "#/components/schemas/AccountInfoData_extra"
        facebook_id:
          type: string
        facebook_url:
          type: string
        firstname:
          type: string
        fraudrecord:
          $ref: "#/components/schemas/AccountInfoData_fraudrecord"
        github_id:
          type: string
        github_url:
          type: string
        google_id:
          type: string
        google_url:
          type: string
        innertell_id:
          type: string
        lastname:
          type: string
        locale:
          type: string
        maxmind:
          $ref: "#/components/schemas/AccountInfoMaxMindResponse"
        maxmind_score:
          type: string
        mb_id:
          type: string
        modernbill_id:
          type: string
        picture:
          type: string
        referrer_coupon:
          type: string
        reseller_markup:
          type: string
        username:
          type: string
        ssh_key:
          type: string
        ssh_key_wrapped:
          type: string
        api_key:
          type: string
        api_key_wrapped:
          type: string
        "2fa_google_key":
          type: string
        "2fa_google_enabled":
          type: boolean
        "2fa_google":
          format: int32
          type: integer
        "2fa_google_split":
          type: string
        "2fa_google_qr":
          type: string
      title: AccountInfoData
      type: object
    AccountInfoDataCc:
      description: Credit card information stored on the account.
      example:
        cc: '************2222'
        cc_exp: 05/2019
        name: John Doe
        address: 123 Uber St
        city: Townsville
        state: PA
        zip: "11122"
        country: US
        maxmind_riskscore: "0.25"
        maxmind:
          distance: "0"
          countryMatch: "Yes"
          countryCode: US
          freeMail: "No"
          anonymousProxy: "No"
          binMatch: "No"
          binCountry: IN
          err: ""
          proxyScore: "0.00"
          ip_region: PA
          ip_city: Townsville
          ip_latitude: "40.175"
          ip_longitude: "-76.1817"
          binName: state bank of india
          ip_isp: PenTeleData
          ip_org: PenTeleData
          binNameMatch: NA
          binPhoneMatch: NA
          binPhone: ""
          custPhoneInBillingLoc: NotFound
          highRiskCountry: "No"
          queriesRemaining: "2764"
          cityPostalMatch: "Yes"
          shipCityPostalMatch: ""
          maxmindID: 7IVXKOVV
          ip_asnum: AS3737 AS-PTD
          ip_userType: residential
          ip_countryConf: "99"
          ip_regionConf: "99"
          ip_cityConf: "30"
          ip_postalCode: "11122"
          ip_postalConf: "30"
          ip_accuracyRadius: "20"
          ip_netSpeedCell: Cable/DSL
          ip_metroCode: "566"
          ip_areaCode: ""
          ip_timeZone: America/New_York
          ip_regionName: Pennsylvania
          ip_domain: ptd.net
          ip_countryName: United States
          ip_continentCode: NA
          ip_corporateProxy: "No"
          isTransProxy: "No"
          carderEmail: "No"
          highRiskUsername: "No"
          highRiskPassword: "No"
          riskScore: 0.25
          prepaid: "No"
          minfraud_version: "1.3"
          service_level: premium
          female_name: "no"
        verified: true
      properties:
        cc:
          type: string
        cc_exp:
          type: string
        name:
          type: string
        address:
          type: string
        city:
          type: string
        state:
          type: string
        zip:
          type: string
        country:
          type: string
        maxmind_riskscore:
          type: string
        maxmind:
          $ref: "#/components/schemas/AccountInfoMaxMindResponse"
        verified:
          type: boolean
      title: AccountInfoDataCc
      type: object
    AccountInfoDataCcs:
      additionalProperties:
        $ref: "#/components/schemas/AccountInfoDataCc"
      description: "Collection of credit cards on the account, keyed by card ID."
      example:
        "1":
          cc: '************1111'
          cc_exp: 05/2019
          name: John Doe
          country: US
          verified: true
        "5":
          cc: '************2222'
          cc_exp: 05/2019
          name: John Doe
          address: 123 Uber St
          city: Townsville
          state: PA
          zip: "11122"
          country: US
          maxmind_riskscore: "0.25"
          maxmind:
            distance: "0"
            countryMatch: "Yes"
            countryCode: US
            freeMail: "No"
            anonymousProxy: "No"
            binMatch: "No"
            binCountry: IN
            err: ""
            proxyScore: "0.00"
            ip_region: PA
            ip_city: Townsville
            ip_latitude: "40.175"
            ip_longitude: "-76.1817"
            binName: state bank of india
            ip_isp: PenTeleData
            ip_org: PenTeleData
            binNameMatch: NA
            binPhoneMatch: NA
            binPhone: ""
            custPhoneInBillingLoc: NotFound
            highRiskCountry: "No"
            queriesRemaining: "2764"
            cityPostalMatch: "Yes"
            shipCityPostalMatch: ""
            maxmindID: 7IVXKOVV
            ip_asnum: AS3737 AS-PTD
            ip_userType: residential
            ip_countryConf: "99"
            ip_regionConf: "99"
            ip_cityConf: "30"
            ip_postalCode: "11122"
            ip_postalConf: "30"
            ip_accuracyRadius: "20"
            ip_netSpeedCell: Cable/DSL
            ip_metroCode: "566"
            ip_areaCode: ""
            ip_timeZone: America/New_York
            ip_regionName: Pennsylvania
            ip_domain: ptd.net
            ip_countryName: United States
            ip_continentCode: NA
            ip_corporateProxy: "No"
            isTransProxy: "No"
            carderEmail: "No"
            highRiskUsername: "No"
            highRiskPassword: "No"
            riskScore: 0.25
            prepaid: "No"
            minfraud_version: "1.3"
            service_level: premium
            female_name: "no"
          verified: true
        "6":
          cc: '************1111'
          cc_exp: 05/2021
          name: John Doe
          city: Townsville
          state: PA
          zip: "11122"
          country: US
          maxmind_riskscore: "0.48"
          maxmind:
            distance: "0"
            countryMatch: "Yes"
            countryCode: US
            freeMail: "No"
            anonymousProxy: "No"
            binMatch: "Yes"
            binCountry: US
            err: ""
            proxyScore: "0.00"
            ip_region: PA
            ip_city: Townsville
            ip_latitude: "40.175"
            ip_longitude: "-76.1817"
            binName: Bank of No Hope
            ip_isp: PenTeleData
            ip_org: PenTeleData
            binNameMatch: NA
            binPhoneMatch: NA
            binPhone: "1234561234"
            custPhoneInBillingLoc: NotFound
            highRiskCountry: "No"
            queriesRemaining: "2765"
            cityPostalMatch: "Yes"
            shipCityPostalMatch: ""
            maxmindID: AAAAAZZZZZ
            ip_asnum: AS3737 AS-PTD
            ip_userType: residential
            ip_countryConf: "99"
            ip_regionConf: "99"
            ip_cityConf: "30"
            ip_postalCode: "11122"
            ip_postalConf: "30"
            ip_accuracyRadius: "20"
            ip_netSpeedCell: Cable/DSL
            ip_metroCode: "566"
            ip_areaCode: ""
            ip_timeZone: America/New_York
            ip_regionName: Pennsylvania
            ip_domain: ptd.net
            ip_countryName: United States
            ip_continentCode: NA
            ip_corporateProxy: "No"
            isTransProxy: "No"
            carderEmail: "No"
            highRiskUsername: "No"
            highRiskPassword: "No"
            riskScore: 0.48
            prepaid: "No"
            minfraud_version: "1.3"
            service_level: premium
            female_name: "no"
          verified: true
      title: AccountInfoDataCcs
      type: object
    AccountInfoLimits:
      description: IP address ranges used to restrict account access to specific IPs.
      example:
      - start: 8.7.6.5
        end: 8.7.6.5
      - start: 107.77.194.166
        end: 107.77.194.166
      - start: 2600:387:0:809::1b
        end: 2600:387:0:809::1b
      - start: 192.64.80.218
        end: 192.64.80.218
      - start: 66.45.235.102
        end: 66.45.235.102
      items:
        $ref: "#/components/schemas/AccountInfoLimits_inner"
      title: AccountInfoLimits
      type: array
    AccountInfoMaxMindResponse:
      description: MaxMind fraud detection scoring data for a credit card transaction.
      example:
        distance: "6146"
        countryMatch: "No"
        countryCode: US
        freeMail: "No"
        anonymousProxy: "No"
        score: "5.00"
        binMatch: NotFound
        binCountry: ""
        err: CITY_NOT_FOUND
        proxyScore: "0.00"
        ip_region: PA
        ip_city: Townsville
        ip_latitude: "40.1767"
        ip_longitude: "-76.4297"
        binName: ""
        ip_isp: PenTeleData
        ip_org: PenTeleData
        binNameMatch: NA
        binPhoneMatch: NA
        binPhone: ""
        custPhoneInBillingLoc: ""
        highRiskCountry: "No"
        queriesRemaining: "171"
        cityPostalMatch: ""
        shipCityPostalMatch: ""
        maxmindID: HMOCUJP7
        ip_asnum: AS3737 PenTeleData Inc.
        ip_userType: residential
        ip_countryConf: "99"
        ip_regionConf: "97"
        ip_cityConf: "30"
        ip_postalCode: "11122"
        ip_postalConf: "30"
        ip_accuracyRadius: "10"
        ip_netSpeedCell: Dialup
        ip_metroCode: "566"
        ip_areaCode: "333"
        ip_timeZone: America/New_York
        ip_regionName: Pennsylvania
        ip_domain: ptd.net
        ip_countryName: United States
        ip_continentCode: NA
        ip_corporateProxy: "No"
        carderEmail: "No"
        highRiskUsername: "No"
        riskScore: "4.82"
        explanation: "You should review this order carefully, as it is considered\
          \ high risk. We suggest you be very cautious about accepting this order.\
          \ This order is higher risk because the distance between the billing address\
          \ and the user's actual location is so great. The order is higher risk because\
          \ the billing country and the country in which the IP address is located\
          \ don't match"
        female_name: "no"
      properties:
        distance:
          type: string
        countryMatch:
          type: string
        countryCode:
          type: string
        freeMail:
          type: string
        anonymousProxy:
          type: string
        score:
          type: string
        binMatch:
          type: string
        binCountry:
          type: string
        err:
          type: string
        proxyScore:
          type: string
        ip_region:
          type: string
        ip_city:
          type: string
        ip_latitude:
          type: string
        ip_longitude:
          type: string
        binName:
          type: string
        ip_isp:
          type: string
        ip_org:
          type: string
        binNameMatch:
          type: string
        binPhoneMatch:
          type: string
        binPhone:
          type: string
        custPhoneInBillingLoc:
          type: string
        highRiskCountry:
          type: string
        queriesRemaining:
          type: string
        cityPostalMatch:
          type: string
        shipCityPostalMatch:
          type: string
        maxmindID:
          type: string
        ip_asnum:
          type: string
        ip_userType:
          type: string
        ip_countryConf:
          type: string
        ip_regionConf:
          type: string
        ip_cityConf:
          type: string
        ip_postalCode:
          type: string
        ip_postalConf:
          type: string
        ip_accuracyRadius:
          type: string
        ip_netSpeedCell:
          type: string
        ip_metroCode:
          type: string
        ip_areaCode:
          type: string
        ip_timeZone:
          type: string
        ip_regionName:
          type: string
        ip_domain:
          type: string
        ip_countryName:
          type: string
        ip_continentCode:
          type: string
        ip_corporateProxy:
          type: string
        carderEmail:
          type: string
        highRiskUsername:
          type: string
        highRiskPassword:
          type: string
        riskScore:
          $ref: "#/components/schemas/AccountInfoMaxMindResponse_riskScore"
        isTransProxy:
          type: string
        prepaid:
          type: string
        minfraud_version:
          type: string
        service_level:
          type: string
        explanation:
          type: string
        female_name:
          type: string
      title: AccountInfoMaxMindResponse
      type: object
    AccountInfoOauthConfig:
      description: OAuth integration configuration including callback URL and available
        providers.
      example:
        callback: https://my.interserver.net/oauth/callback.php
        providers:
          Twitter:
            enabled: false
            linked: false
          Facebook:
            enabled: true
            account: "10204015683980049"
            url: https://www.facebook.com/10204015683980049
            linked: true
          Google:
            enabled: true
            account: "103826860529802474211"
            url: https://plus.google.com/+JoeHuss
            linked: true
          GitHub:
            enabled: true
            account: "1364504"
            url: https://github.com/detain
            linked: true
      properties:
        callback:
          type: string
        providers:
          $ref: "#/components/schemas/AccountInfoOauthConfigProviders"
      required:
      - callback
      - providers
      title: AccountInfoOauthConfig
      type: object
    AccountInfoOauthConfigProviders:
      additionalProperties:
        $ref: "#/components/schemas/AccountInfoOauthConfigProviders_value"
      description: Map of OAuth providers and their linked status on the account.
      example:
        Twitter:
          enabled: false
          linked: false
        Facebook:
          enabled: true
          account: "10204015683980049"
          url: https://www.facebook.com/10204015683980049
          linked: true
        Google:
          enabled: true
          account: "103826860529802474211"
          url: https://plus.google.com/+JoeHuss
          linked: true
        GitHub:
          enabled: true
          account: "1364504"
          url: https://github.com/detain
          linked: true
      title: AccountInfoOauthConfigProviders
      type: object
    AccountInfoPost:
      description: Request to update account information.
      example:
        name: John Doe
        company: My Company
        address: 124 My St
        address2: ""
        city: My Town
        state: PA
        zip: "17522"
        country: US
        phone: "8675309"
        locale: ""
        email_invoices: ""
        email_abuse: ""
        disable_reset: false
        disable_reinstall: false
        disable_server_notifications: false
        disable_email_notifications: false
        gstin: ""
      properties:
        name:
          description: Your name.
          example: John Doe
          type: string
        company:
          description: Your company name.
          example: My Company
          type: string
        address:
          description: Your address.
          example: 124 My St
          type: string
        address2:
          description: Additional address information.
          type: string
        city:
          description: Your city.
          example: My Town
          type: string
        state:
          description: Your state.
          example: PA
          type: string
        zip:
          description: Your ZIP code.
          example: "17522"
          type: string
        country:
          description: Your country.
          example: US
          type: string
        phone:
          description: Your phone number.
          example: "8675309"
          type: string
        locale:
          description: Your preferred locale.
          type: string
        email_invoices:
          description: Your email for invoice notifications.
          type: string
        email_abuse:
          description: Your email for abuse notifications.
          type: string
        disable_reset:
          description: "Set to `true` to disable account resets, or `false` to enable\
            \ them."
          type: boolean
        disable_reinstall:
          description: "Set to `true` to disable server reinstalls, or `false` to\
            \ enable them."
          type: boolean
        disable_server_notifications:
          description: "Set to `true` to disable server notifications, or `false`\
            \ to enable them."
          type: boolean
        disable_email_notifications:
          description: "Set to `true` to disable email notifications, or `false` to\
            \ enable them."
          type: boolean
        gstin:
          description: Your GST identification number (if applicable).
          type: string
      required:
      - address
      - city
      - country
      - name
      - phone
      - state
      - zip
      title: Account Information Update Request
      type: object
    AccountSshKey:
      description: SSH Keys
      example:
        ssh_key: ""
      properties:
        ssh_key:
          type: string
      title: AccountSshKey
      type: object
    AffiliateBannerRow:
      description: An affiliate banner image details.
      example:
        image: 12946798.gif
        width: "125"
        height: "125"
      properties:
        image:
          type: string
        width:
          type: string
        height:
          type: string
      title: AffiliateBannerRow
      type: object
    AffiliateTrafficRow:
      description: Affiliate Web Traffic Entry
      example:
        traffic_id: "91839913"
        traffic_ip: "2a06:98c0:3600::"
        traffic_url: https://www.interserver.net/webhosting/?id=8
        traffic_affiliate: "8"
        traffic_referrer: ""
        traffic_timestamp: 2023-09-30 06:30:27
      properties:
        traffic_id:
          type: string
        traffic_ip:
          type: string
        traffic_url:
          type: string
        traffic_affiliate:
          type: string
        traffic_referrer:
          type: string
        traffic_timestamp:
          type: string
      title: AffiliateTrafficRow
      type: object
    Backup:
      description: "Full detail view of a backup service including billing, service\
        \ info, and configuration."
      example:
        serviceInfo:
          backup_id: "21163"
          backup_server: "38"
          backup_username: st21163
          backup_type: "10831"
          backup_currency: USD
          backup_order_date: 2021-12-29T14:09:57.000Z
          backup_custid: "2773"
          backup_quota: "0"
          backup_ip: 64.20.55.234
          backup_status: canceled
          backup_invoice: "19591007"
          backup_coupon: "0"
          backup_extra: "[]"
          backup_server_status: deleted
          backup_comment: backup_comment
        client_links:
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
          other_attr: other_attr
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
          other_attr: other_attr
        billingDetails:
          service_last_invoice_date: "December 29, 2021"
          service_payment_status: Paid
          service_frequency: Monthly
          next_date: 2022-01-29T14:09:57.000Z
          service_next_invoice_date: "January 29, 2022"
          service_currency: USD
          service_currency_symbol: $
          service_cost_info: "3"
          service_extra: "[]"
          service_extra_json: "[]"
        custCurrency: USD
        custCurrencySymbol: $
        serviceMaster:
          backup_id: 38
          backup_name: storage1400.is.cc
          backup_ip: 44.22.11.88
          backup_type: 703
          backup_hdsize: 156448
          backup_hdfree: 61374
          backup_last_update: 2023-08-17T23:20:02.000Z
          backup_available: 0
          backup_iowait: 0
          backup_order: 21359
        package: package
        serviceExtra: serviceExtra
        extraInfoTables:
          ip_info:
            title: IP Information
            rows:
            - desc: Netmask
              value: 255.255.255.248
            - desc: Netmask
              value: 255.255.255.248
      properties:
        serviceInfo:
          $ref: "#/components/schemas/BackupServiceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/BackupClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/BackupBillingDetails"
        custCurrency:
          description: Customer's currency.
          example: USD
          type: string
        custCurrencySymbol:
          description: Customer's currency symbol.
          example: $
          type: string
        serviceMaster:
          $ref: "#/components/schemas/BackupServiceMaster"
        package:
          description: Package information.
          type: string
        serviceExtra:
          type: string
        extraInfoTables:
          $ref: "#/components/schemas/BackupExtraInfoTables"
      type: object
    BackupBillingDetails:
      example:
        service_last_invoice_date: "December 29, 2021"
        service_payment_status: Paid
        service_frequency: Monthly
        next_date: 2022-01-29T14:09:57.000Z
        service_next_invoice_date: "January 29, 2022"
        service_currency: USD
        service_currency_symbol: $
        service_cost_info: "3"
        service_extra: "[]"
        service_extra_json: "[]"
      properties:
        service_last_invoice_date:
          description: Last invoice date of the service.
          example: "December 29, 2021"
          type: string
        service_payment_status:
          description: Payment status of the service.
          example: Paid
          type: string
        service_frequency:
          description: Billing frequency of the service.
          example: Monthly
          type: string
        next_date:
          description: Next billing date of the service.
          example: 2022-01-29T14:09:57.000Z
          type: string
        service_next_invoice_date:
          description: Next invoice date of the service.
          example: "January 29, 2022"
          type: string
        service_currency:
          description: Currency of the service.
          example: USD
          type: string
        service_currency_symbol:
          description: Currency symbol of the service.
          example: $
          type: string
        service_cost_info:
          description: Cost information of the service.
          example: "3"
          type: string
        service_extra:
          description: Service Extra Info
          example: "[]"
          type: string
        service_extra_json:
          description: JSON representation of extra service information.
          example: "[]"
          type: string
      type: object
    BackupClientLink:
      description: A navigation link for backup service actions in the client portal.
      example:
        label: label
        link: link
        icon: icon
        icon_text: icon_text
        help_text: help_text
        other_attr: other_attr
      properties:
        label:
          description: Label of the client link.
          type: string
        link:
          description: Link URL of the client link.
          type: string
        icon:
          description: Icon of the client link.
          type: string
        icon_text:
          description: Icon text of the client link.
          type: string
        help_text:
          description: Help text of the client link.
          type: string
        other_attr:
          description: Other attributes of the client link.
          type: string
      type: object
    BackupExtraInfoTables:
      description: "Supplementary information tables for a backup service (IP info,\
        \ etc.)."
      example:
        ip_info:
          title: IP Information
          rows:
          - desc: Netmask
            value: 255.255.255.248
          - desc: Netmask
            value: 255.255.255.248
      properties:
        ip_info:
          $ref: "#/components/schemas/BackupIPInfo"
      type: object
    BackupIPInfo:
      description: IP address information table for a backup service.
      example:
        title: IP Information
        rows:
        - desc: Netmask
          value: 255.255.255.248
        - desc: Netmask
          value: 255.255.255.248
      properties:
        title:
          description: Title of the IP information table.
          example: IP Information
          type: string
        rows:
          items:
            $ref: "#/components/schemas/BackupIPInfoRow"
          type: array
      type: object
    BackupIPInfoRow:
      description: A single row in the backup IP information table.
      example:
        desc: Netmask
        value: 255.255.255.248
      properties:
        desc:
          description: Description of the IP information.
          example: Netmask
          type: string
        value:
          description: Value of the IP information.
          example: 255.255.255.248
          type: string
      type: object
    BackupRow:
      description: A result row from the `Backups` `GET` request.
      example:
        backup_id: "2414"
        backup_name: storage-nj.interserver.net
        backup_cost: "5.99"
        backup_username: detainin2414
        backup_status: pending-setup
        services_name: Swift Storage
      properties:
        backup_id:
          description: The id of the backup.
          example: "2414"
          type: string
        backup_name:
          description: The name of the backup.
          example: storage-nj.interserver.net
          type: string
        backup_cost:
          description: The cost of the backup.
          example: "5.99"
          type: string
        backup_username:
          description: The username of the backup.
          example: detainin2414
          type: string
        backup_status:
          description: The status of the backup.
          example: pending-setup
          type: string
        services_name:
          description: The services name of the backup.
          example: Swift Storage
          type: string
      title: BackupRow
      type: object
    BackupLoginResponse:
      description: Login session response for backup storage.
      example:
        success: true
        text: text
      properties:
        success:
          description: Indicates whether a login session was created.
          type: boolean
        text:
          description: Login URL or error text returned by the storage provider.
          type: string
      title: BackupLoginResponse
      type: object
    BackupServiceExtra:
      type: string
    BackupServiceInfo:
      description: "Core service record for a backup storage service including ID,\
        \ status, quota, and billing details."
      example:
        backup_id: "21163"
        backup_server: "38"
        backup_username: st21163
        backup_type: "10831"
        backup_currency: USD
        backup_order_date: 2021-12-29T14:09:57.000Z
        backup_custid: "2773"
        backup_quota: "0"
        backup_ip: 64.20.55.234
        backup_status: canceled
        backup_invoice: "19591007"
        backup_coupon: "0"
        backup_extra: "[]"
        backup_server_status: deleted
        backup_comment: backup_comment
      properties:
        backup_id:
          description: Backup ID.
          example: "21163"
          type: string
        backup_server:
          description: Backup server ID.
          example: "38"
          type: string
        backup_username:
          description: Backup username.
          example: st21163
          type: string
        backup_type:
          description: Backup type.
          example: "10831"
          type: string
        backup_currency:
          description: Backup currency.
          example: USD
          type: string
        backup_order_date:
          description: Backup order date.
          example: 2021-12-29T14:09:57.000Z
          type: string
        backup_custid:
          description: Backup customer ID.
          example: "2773"
          type: string
        backup_quota:
          description: Backup quota.
          example: "0"
          type: string
        backup_ip:
          description: Backup IP address.
          example: 64.20.55.234
          type: string
        backup_status:
          description: Backup status.
          example: canceled
          type: string
        backup_invoice:
          description: Backup invoice.
          example: "19591007"
          type: string
        backup_coupon:
          description: Backup coupon.
          example: "0"
          type: string
        backup_extra:
          description: Backup extra information.
          example: "[]"
          type: string
        backup_server_status:
          description: Backup server status.
          example: deleted
          type: string
        backup_comment:
          description: Backup comment.
          type: string
      type: object
    BackupServiceMaster:
      example:
        backup_id: 38
        backup_name: storage1400.is.cc
        backup_ip: 44.22.11.88
        backup_type: 703
        backup_hdsize: 156448
        backup_hdfree: 61374
        backup_last_update: 2023-08-17T23:20:02.000Z
        backup_available: 0
        backup_iowait: 0
        backup_order: 21359
      properties:
        backup_id:
          description: Backup ID of the service master.
          example: 38
          type: integer
        backup_name:
          description: Name of the backup service.
          example: storage1400.is.cc
          type: string
        backup_ip:
          description: IP address of the backup service.
          example: 44.22.11.88
          type: string
        backup_type:
          description: Type of the backup service.
          example: 703
          type: integer
        backup_hdsize:
          description: Size of the backup service's hard drive.
          example: 156448
          type: integer
        backup_hdfree:
          description: Amount of free space on the backup service's hard drive.
          example: 61374
          type: integer
        backup_last_update:
          description: Last update timestamp of the backup service.
          example: 2023-08-17T23:20:02.000Z
          type: string
        backup_available:
          description: Availability status of the backup service.
          example: 0
          type: integer
        backup_iowait:
          description: I/O wait status of the backup service.
          example: 0
          type: integer
        backup_order:
          description: Order associated with the backup service.
          example: 21359
          type: integer
      type: object
    BackupsOrder:
      description: Available backup storage packages and pricing for ordering a new
        backup service.
      example:
        packageCosts:
          "11006": 84
        serviceTypes:
          "11006":
            services_id: "11006"
            services_name: Storage ST 700
            services_cost: "84.00"
            services_category: "702"
            services_buyable: "1"
            services_type: "703"
            services_field1: ""
            services_field2: "bandwidth=40000000,quota=40000000,inode=100,uinode=ON,vdomains=2,nsubdomains=5,nemails=0,nemailf=0,nemailml=0,nemailr=0,mysql=5,domainptr=5,ftp=100,aftp=OFF,cgi=ON,php=ON,spam=OFF,cron=OFF,catchall=OFF,ssl=ON,ssh=ON,sysinfo=ON,login_keys=ON,dnscontrol=OFF,suspend_at_limit=ON"
            services_module: backups
      properties:
        packageCosts:
          $ref: "#/components/schemas/BackupsOrderPackageCosts"
        serviceTypes:
          $ref: "#/components/schemas/BackupsOrder_serviceTypes"
      required:
      - packageCosts
      - serviceTypes
      type: object
    BackupsOrderPackageCosts:
      example:
        "11006": 84
      properties:
        "11006":
          description: The cost associated with package 11006.
          example: 84
          type: integer
      required:
      - "11006"
      type: object
    BackupsOrderServiceTypes:
      example:
        services_id: "11006"
        services_name: Storage ST 700
        services_cost: "84.00"
        services_category: "702"
        services_buyable: "1"
        services_type: "703"
        services_field1: ""
        services_field2: "bandwidth=40000000,quota=40000000,inode=100,uinode=ON,vdomains=2,nsubdomains=5,nemails=0,nemailf=0,nemailml=0,nemailr=0,mysql=5,domainptr=5,ftp=100,aftp=OFF,cgi=ON,php=ON,spam=OFF,cron=OFF,catchall=OFF,ssl=ON,ssh=ON,sysinfo=ON,login_keys=ON,dnscontrol=OFF,suspend_at_limit=ON"
        services_module: backups
      properties:
        services_id:
          description: The ID of the service.
          example: "11006"
          type: string
        services_name:
          description: The name of the service.
          example: Storage ST 700
          type: string
        services_cost:
          description: The cost of the service.
          example: "84.00"
          type: string
        services_category:
          description: The category of the service.
          example: "702"
          type: string
        services_buyable:
          description: Indicates if the service is buyable.
          example: "1"
          type: string
        services_type:
          description: The type of the service.
          example: "703"
          type: string
        services_field1:
          description: Field 1 of the service.
          example: ""
          type: string
        services_field2:
          description: Field 2 of the service.
          example: "bandwidth=40000000,quota=40000000,inode=100,uinode=ON,vdomains=2,nsubdomains=5,nemails=0,nemailf=0,nemailml=0,nemailr=0,mysql=5,domainptr=5,ftp=100,aftp=OFF,cgi=ON,php=ON,spam=OFF,cron=OFF,catchall=OFF,ssl=ON,ssh=ON,sysinfo=ON,login_keys=ON,dnscontrol=OFF,suspend_at_limit=ON"
          type: string
        services_module:
          description: The module of the service.
          example: backups
          type: string
      required:
      - services_buyable
      - services_category
      - services_cost
      - services_field1
      - services_field2
      - services_id
      - services_module
      - services_name
      - services_type
      type: object
    BillingAddCcRequest:
      description: Request to add a new creditcard into the system.
      example:
        name: John Doe
        address: 1 Somewhere St.
        city: Secaucus
        state: NJ
        country: US
        zip: "07013"
        cc: "1234556781923121"
        cc_exp: 12/2023
        cc_ccv2: "123"
      properties:
        name:
          type: string
        address:
          type: string
        city:
          type: string
        state:
          type: string
        country:
          type: string
        zip:
          type: string
        cc:
          type: string
        cc_exp:
          type: string
        cc_ccv2:
          type: string
      title: BillingAddCcRequest
      type: object
    BillingPrepayRequest:
      description: Request payload for creating a new prepay balance.
      example:
        module: module
        amount: 0.8008281904610115
        automatic_use: "1"
      properties:
        module:
          description: Module the prepay should be applied to (for example `default`).
          type: string
        amount:
          description: Amount to add to prepay balance. Minimum is $10.
          type: number
        automatic_use:
          description: Whether the prepay balance should be used automatically.
          example: "1"
          type: string
      title: BillingPrepayRequest
      type: object
    BillingInvoiceList:
      description: Summary list of invoices for the account.
      example:
        rows:
        - "{}"
        - "{}"
        summary: "{}"
      properties:
        rows:
          description: Invoice rows returned for the account.
          items:
            type: object
          type: array
        summary:
          description: Totals and summary data for the invoices list.
          type: object
      title: BillingInvoiceList
      type: object
    BillingInvoiceDetail:
      additionalProperties:
        description: The invoice payload varies by invoice type and includes line
          item details.
        type: string
      description: Detailed invoice data payload returned for a single invoice.
      title: BillingInvoiceDetail
      type: object
    BillingVerifyCcRequest:
      description: Payload for verifying a credit card through the verification flow.
      example:
        idx: 0
        cc_ccv2: cc_ccv2
        cc_amount1: cc_amount1
        cc_amount2: cc_amount2
        terms: true
      properties:
        idx:
          description: Card index to verify.
          type: integer
        cc_ccv2:
          description: CVV code for verification.
          type: string
        cc_amount1:
          description: First micro-charge amount for verification.
          type: string
        cc_amount2:
          description: Second micro-charge amount for verification.
          type: string
        terms:
          description: Whether terms were accepted for verification.
          type: boolean
      title: BillingVerifyCcRequest
      type: object
    BillingPaymentMethodRequest:
      description: Payload for updating the default account payment method.
      example:
        payment_method: payment_method
        cc_auto: cc_auto
      properties:
        payment_method:
          description: "Payment method identifier (cc, paypal, or cc{index})."
          type: string
        cc_auto:
          description: Whether automatic credit card payments are enabled.
          type: string
      title: BillingPaymentMethodRequest
      type: object
    CaptchaResponse:
      description: A base-64 encoded captcha image.
      example:
        captcha: "data:image/jpeg;base64,/9j/4AAQ"
      properties:
        captcha:
          description: The base64 encoded captcha image.
          example: "data:image/jpeg;base64,/9j/4AAQ"
          type: string
      required:
      - captcha
      title: CaptchaResponse
      type: object
    ChargeInvoiceRows:
      description: "Billing invoices associated with a service, including invoice\
        \ IDs, descriptions, amounts, and payment status."
      example:
        success: true
        invoices:
          "1234565":
            invoices_id: 1234565
            invoices_description: Current IP + Scrub
            invoices_amount: 5
            invoices_date: 2025-12-05T19:38:24.000Z
            invoices_paid: 1
            invoices_due_date: 2025-12-19T19:38:24.000Z
            invoices_currency: USD
            currency_symbol: $
            invoices_date_formatted: 5th Dec 2025
            paid_invoices:
              "654321":
                invoices_id: "654321,"
                invoices_description: Credit Card Payment 2189347824
                invoices_amount: 5
                invoices_date: 2025-12-05T19:38:38.000Z
                invoices_currency: USD
                currency_symbol: $
                invoices_date_formatted: 5th Dec 2025
                payment_type: Credit Card Payment
                refund_invoices:
                  "98765":
                    invoices_id: 98765
                    invoices_description: "REFUND: Credit Card Payment 2189347824"
                    invoices_amount: 5
                    invoices_date: 2025-12-16T13:32:22.000Z
                    invoices_currency: USD
                    currency_symbol: $
                    invoices_date_formatted: 16th Dec 2025
      properties:
        success:
          description: Whether the invoice retrieval was successful.
          type: boolean
        invoices:
          additionalProperties:
            $ref: "#/components/schemas/ChargeInvoiceRows_invoices_value"
          description: List of invoices for the service.
          type: object
      type: object
    DnsListItem:
      additionalProperties: false
      description: "A DNS zone entry with its ID, domain name, and record content."
      example:
        id: 68
        name: hussfamily.com
        content: 64.20.35.186
      properties:
        id:
          type: integer
        name:
          type: string
        content:
          type: string
      title: DnsListItem
      type: object
    DnsNewDomain:
      description: The request for a new domain to be managed by the dns servers.
      example:
        domain: mydomain.com
        ip: 1.2.3.4
      properties:
        domain:
          description: The domain name.
          example: mydomain.com
          type: string
        ip:
          description: IP Address to point the domain to.
          example: 1.2.3.4
          type: string
      required:
      - domain
      - ip
      title: DnsNewDomain
      type: object
    DnsRecord:
      description: A single DNS Record row for a Domain
      example:
        id: "473"
        domain_id: "68"
        name: hussfamily.com
        type: NS
        content: cdns1.interserver.net
        ttl: "86400"
        prio: "0"
        disabled: "0"
        ordername: ""
        auth: "1"
      properties:
        id:
          description: The ID of the DNS Record.
          example: "472"
          type: string
        domain_id:
          description: The ID of the Domain this is a record of.
          example: "68"
          type: string
        name:
          example: hussfamily.com
          type: string
        type:
          $ref: "#/components/schemas/DnsRecordType"
        content:
          description: "The content of the record, such as the IP address or hsotname."
          example: cdns1.interserver.net
          type: string
        ttl:
          description: Time To Live (seconds)
          example: "86400"
          type: string
        prio:
          description: Priority
          example: "0"
          type: string
        disabled:
          example: "0"
          type: string
        ordername:
          description: Alternate name to use for sorting
          type: string
        auth:
          example: "1"
          type: string
      required:
      - auth
      - content
      - disabled
      - domain_id
      - id
      - name
      - ordername
      - prio
      - ttl
      - type
      title: DnsRecord
      type: object
    DnsUpdateRecord:
      description: The request data to update a dns record.
      example:
        name: mydomain.com
        type: NS
        content: cdns1.interserver.net
        ttl: "86400"
        prio: "0"
        disabled: "0"
        ordername: ""
        auth: "1"
      properties:
        name:
          type: string
        type:
          $ref: "#/components/schemas/DnsRecordType"
        content:
          type: string
        ttl:
          type: string
        prio:
          type: string
        disabled:
          type: string
        ordername:
          type: string
        auth:
          type: string
      title: DnsUpdateRecord
      type: object
    Domain:
      description: "Full detail view of a domain service including billing, contacts,\
        \ DNS, and configuration."
      example:
        serviceInfo:
          domain_id: domain_id
          domain_hostname: domain_hostname
          domain_username: domain_username
          domain_password: domain_password
          domain_type: domain_type
          domain_currency: domain_currency
          domain_expire_date: domain_expire_date
          domain_order_date: domain_order_date
          domain_custid: domain_custid
          domain_status: domain_status
          domain_invoice: domain_invoice
          domain_coupon: domain_coupon
        serviceTypes:
          key:
            services_id: services_id
            services_name: services_name
            services_cost: services_cost
            services_category: services_category
            services_buyable: services_buyable
            services_type: services_type
            services_field1: services_field1
            services_field2: services_field2
            services_module: services_module
        client_links:
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        billingDetails:
          service_last_invoice_date: service_last_invoice_date
          service_payment_status: service_payment_status
          service_frequency: service_frequency
          next_date: next_date
          service_next_invoice_date: service_next_invoice_date
          service_currency: service_currency
          service_currency_symbol: service_currency_symbol
          service_cost_info: service_cost_info
          service_extra:
            order:
              _OPS_version: _OPS_version
              protocol: protocol
              is_success: is_success
              action: action
              attributes:
                id: id
                admin_email: admin_email
              response_text: response_text
              object: object
              response_code: response_code
            order_id: order_id
            domain_id: domain_id
            provProcessPending:
              _OPS_version: _OPS_version
              response_text: response_text
              protocol: protocol
              response_code: response_code
              action: action
              object: object
              is_success: is_success
              attributes:
                id: id
                order_id: order_id
                registration expiration date: registration expiration date
                f_auto_renew: f_auto_renew
            email: email
            firstname: firstname
            lastname: lastname
            company: company
            address: address
            address2: address2
            address3: address3
            city: city
            state: state
            zip: zip
            country: country
            phone: phone
            fax: fax
          service_extra_json: service_extra_json
        custCurrency: custCurrency
        custCurrencySymbol: custCurrencySymbol
        serviceExtra:
          order:
            _OPS_version: _OPS_version
            protocol: protocol
            is_success: is_success
            action: action
            attributes:
              id: id
              admin_email: admin_email
            response_text: response_text
            object: object
            response_code: response_code
          order_id: order_id
          domain_id: domain_id
          provProcessPending:
            _OPS_version: _OPS_version
            response_text: response_text
            protocol: protocol
            response_code: response_code
            action: action
            object: object
            is_success: is_success
            attributes:
              id: id
              order_id: order_id
              registration expiration date: registration expiration date
              f_auto_renew: f_auto_renew
          email: email
          firstname: firstname
          lastname: lastname
          company: company
          address: address
          address2: address2
          address3: address3
          city: city
          state: state
          zip: zip
          country: country
          phone: phone
          fax: fax
        extraInfoTables:
          ip_info:
            title: IP Information
            rows:
            - desc: Netmask
              value: 255.255.255.248
            - desc: Netmask
              value: 255.255.255.248
        serviceType:
          services_id: services_id
          services_name: services_name
          services_cost: services_cost
          services_category: services_category
          services_buyable: services_buyable
          services_type: services_type
          services_field1: services_field1
          services_field2: services_field2
          services_module: services_module
        contact_details:
          status: status
          state: state
          org_name: org_name
          country: country
          postal_code: postal_code
          email: email
          fax: fax
          address2: address2
          address3: address3
          address1: address1
          city: city
          phone: phone
          first_name: first_name
          last_name: last_name
        pwarning: pwarning
        transfer_info: transfer_info
        errors: true
        domain_logs:
        - domain_logs
        - domain_logs
        allInfo:
          _OPS_version: _OPS_version
          attributes:
            contact_set:
              owner:
                postal_code: postal_code
                org_name: org_name
                country: country
                status: status
                state: state
                first_name: first_name
                last_name: last_name
                address3: address3
                fax: fax
                address2: address2
                email: email
                city: city
                phone: phone
                address1: address1
              admin:
                status: status
                state: state
                org_name: org_name
                country: country
                postal_code: postal_code
                email: email
                fax: fax
                address2: address2
                address3: address3
                address1: address1
                city: city
                phone: phone
                first_name: first_name
                last_name: last_name
              tech:
                state: state
                status: status
                org_name: org_name
                country: country
                postal_code: postal_code
                fax: fax
                email: email
                address2: address2
                address3: address3
                address1: address1
                city: city
                phone: phone
                first_name: first_name
                last_name: last_name
            registry_createdate: registry_createdate
            registry_expiredate: registry_expiredate
            tld_data: tld_data
            let_expire: let_expire
            auto_renew: auto_renew
            sponsoring_rsp: sponsoring_rsp
            gdpr_consent_status: gdpr_consent_status
            nameserver_list:
            - sortorder: sortorder
              name: name
              ipaddress: ipaddress
            - sortorder: sortorder
              name: name
              ipaddress: ipaddress
            registry_updatedate: registry_updatedate
            affiliate_id: affiliate_id
            expiredate: expiredate
          object: object
          protocol: protocol
          response_text: response_text
          response_code: response_code
          action: action
          is_success: is_success
        registrarStatus: registrarStatus
        locked: locked
        whoisPrivacy: whoisPrivacy
        autoRenew: autoRenew
      properties:
        serviceInfo:
          $ref: "#/components/schemas/DomainServiceInfo"
        serviceTypes:
          additionalProperties:
            $ref: "#/components/schemas/DomainServiceType"
          type: object
        client_links:
          items:
            $ref: "#/components/schemas/DomainClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/DomainBillingDetails"
        custCurrency:
          type: string
        custCurrencySymbol:
          type: string
        serviceExtra:
          $ref: "#/components/schemas/DomainBillingExtra"
        extraInfoTables:
          $ref: "#/components/schemas/BackupExtraInfoTables"
        serviceType:
          $ref: "#/components/schemas/DomainServiceType"
        contact_details:
          $ref: "#/components/schemas/DomainContactDetails"
        pwarning:
          type: string
        transfer_info:
          type: string
        errors:
          type: boolean
        domain_logs:
          items:
            type: string
          type: array
        allInfo:
          $ref: "#/components/schemas/DomainAllInfo"
        registrarStatus:
          type: string
        locked:
          type: string
        whoisPrivacy:
          type: string
        autoRenew:
          type: string
      type: object
    DomainAdminContact:
      description: Administrative contact information for a domain registration.
      example:
        status: status
        state: state
        org_name: org_name
        country: country
        postal_code: postal_code
        email: email
        fax: fax
        address2: address2
        address3: address3
        address1: address1
        city: city
        phone: phone
        first_name: first_name
        last_name: last_name
      properties:
        status:
          type: string
        state:
          type: string
        org_name:
          type: string
        country:
          type: string
        postal_code:
          type: string
        email:
          type: string
        fax:
          type: string
        address2:
          type: string
        address3:
          type: string
        address1:
          type: string
        city:
          type: string
        phone:
          type: string
        first_name:
          type: string
        last_name:
          type: string
      type: object
    DomainAllInfo:
      description: "Complete domain registration information returned from the registrar,\
        \ including contacts, nameservers, and expiry dates."
      example:
        _OPS_version: _OPS_version
        attributes:
          contact_set:
            owner:
              postal_code: postal_code
              org_name: org_name
              country: country
              status: status
              state: state
              first_name: first_name
              last_name: last_name
              address3: address3
              fax: fax
              address2: address2
              email: email
              city: city
              phone: phone
              address1: address1
            admin:
              status: status
              state: state
              org_name: org_name
              country: country
              postal_code: postal_code
              email: email
              fax: fax
              address2: address2
              address3: address3
              address1: address1
              city: city
              phone: phone
              first_name: first_name
              last_name: last_name
            tech:
              state: state
              status: status
              org_name: org_name
              country: country
              postal_code: postal_code
              fax: fax
              email: email
              address2: address2
              address3: address3
              address1: address1
              city: city
              phone: phone
              first_name: first_name
              last_name: last_name
          registry_createdate: registry_createdate
          registry_expiredate: registry_expiredate
          tld_data: tld_data
          let_expire: let_expire
          auto_renew: auto_renew
          sponsoring_rsp: sponsoring_rsp
          gdpr_consent_status: gdpr_consent_status
          nameserver_list:
          - sortorder: sortorder
            name: name
            ipaddress: ipaddress
          - sortorder: sortorder
            name: name
            ipaddress: ipaddress
          registry_updatedate: registry_updatedate
          affiliate_id: affiliate_id
          expiredate: expiredate
        object: object
        protocol: protocol
        response_text: response_text
        response_code: response_code
        action: action
        is_success: is_success
      properties:
        _OPS_version:
          type: string
        attributes:
          $ref: "#/components/schemas/DomainAllInfo_attributes"
        object:
          type: string
        protocol:
          type: string
        response_text:
          type: string
        response_code:
          type: string
        action:
          type: string
        is_success:
          type: string
      type: object
    DomainBillingDetails:
      description: "Billing information for a domain service including payment status,\
        \ dates, and cost."
      example:
        service_last_invoice_date: service_last_invoice_date
        service_payment_status: service_payment_status
        service_frequency: service_frequency
        next_date: next_date
        service_next_invoice_date: service_next_invoice_date
        service_currency: service_currency
        service_currency_symbol: service_currency_symbol
        service_cost_info: service_cost_info
        service_extra:
          order:
            _OPS_version: _OPS_version
            protocol: protocol
            is_success: is_success
            action: action
            attributes:
              id: id
              admin_email: admin_email
            response_text: response_text
            object: object
            response_code: response_code
          order_id: order_id
          domain_id: domain_id
          provProcessPending:
            _OPS_version: _OPS_version
            response_text: response_text
            protocol: protocol
            response_code: response_code
            action: action
            object: object
            is_success: is_success
            attributes:
              id: id
              order_id: order_id
              registration expiration date: registration expiration date
              f_auto_renew: f_auto_renew
          email: email
          firstname: firstname
          lastname: lastname
          company: company
          address: address
          address2: address2
          address3: address3
          city: city
          state: state
          zip: zip
          country: country
          phone: phone
          fax: fax
        service_extra_json: service_extra_json
      properties:
        service_last_invoice_date:
          description: Date of the last invoice for the domain.
          type: string
        service_payment_status:
          description: "Current payment status (e.g., Paid, Unpaid)."
          type: string
        service_frequency:
          description: "Billing frequency (e.g., Yearly, Monthly)."
          type: string
        next_date:
          description: Next billing date (ISO 8601).
          type: string
        service_next_invoice_date:
          description: Human-readable next invoice date.
          type: string
        service_currency:
          description: Billing currency code.
          type: string
        service_currency_symbol:
          description: Billing currency symbol.
          type: string
        service_cost_info:
          description: Cost breakdown information.
          type: string
        service_extra:
          $ref: "#/components/schemas/DomainBillingExtra"
        service_extra_json:
          description: Raw JSON string of extra billing data.
          type: string
      type: object
    DomainBillingExtra:
      description: Extended billing context for a domain including registrar order
        details and contact info.
      example:
        order:
          _OPS_version: _OPS_version
          protocol: protocol
          is_success: is_success
          action: action
          attributes:
            id: id
            admin_email: admin_email
          response_text: response_text
          object: object
          response_code: response_code
        order_id: order_id
        domain_id: domain_id
        provProcessPending:
          _OPS_version: _OPS_version
          response_text: response_text
          protocol: protocol
          response_code: response_code
          action: action
          object: object
          is_success: is_success
          attributes:
            id: id
            order_id: order_id
            registration expiration date: registration expiration date
            f_auto_renew: f_auto_renew
        email: email
        firstname: firstname
        lastname: lastname
        company: company
        address: address
        address2: address2
        address3: address3
        city: city
        state: state
        zip: zip
        country: country
        phone: phone
        fax: fax
      properties:
        order:
          $ref: "#/components/schemas/DomainOrderResponse"
        order_id:
          type: string
        domain_id:
          type: string
        provProcessPending:
          $ref: "#/components/schemas/DomainProvProcessPending"
        email:
          type: string
        firstname:
          type: string
        lastname:
          type: string
        company:
          type: string
        address:
          type: string
        address2:
          type: string
        address3:
          type: string
        city:
          type: string
        state:
          type: string
        zip:
          type: string
        country:
          type: string
        phone:
          type: string
        fax:
          type: string
      type: object
    DomainClientLink:
      description: Links and labels for domain-related UI actions.
      example:
        label: label
        link: link
        icon: icon
        icon_text: icon_text
        help_text: help_text
      properties:
        label:
          type: string
        link:
          type: string
        icon:
          type: string
        icon_text:
          type: string
        help_text:
          type: string
      type: object
    DomainContactDetails:
      description: Contact details used for domain registrant/admin/technical/billing
        records.
      example:
        status: status
        state: state
        org_name: org_name
        country: country
        postal_code: postal_code
        email: email
        fax: fax
        address2: address2
        address3: address3
        address1: address1
        city: city
        phone: phone
        first_name: first_name
        last_name: last_name
      properties:
        status:
          description: Contact status reported by the registrar.
          type: string
        state:
          description: State or region for the contact address.
          type: string
        org_name:
          description: Organization name for the contact.
          type: string
        country:
          description: Two-letter country code for the contact.
          type: string
        postal_code:
          description: Postal or ZIP code for the contact address.
          type: string
        email:
          description: Email address for the contact.
          type: string
        fax:
          description: "Fax number for the contact, if available."
          type: string
        address2:
          description: Secondary street address line.
          type: string
        address3:
          description: Tertiary street address line.
          type: string
        address1:
          description: Primary street address line.
          type: string
        city:
          description: City for the contact address.
          type: string
        phone:
          description: Phone number for the contact.
          type: string
        first_name:
          description: First name for the contact.
          type: string
        last_name:
          description: Last name for the contact.
          type: string
      type: object
    DomainNameServer:
      description: A single registered nameserver entry with glue IPs.
      example:
        sortorder: sortorder
        name: name
        ipaddress: ipaddress
      properties:
        sortorder:
          type: string
        name:
          type: string
        ipaddress:
          type: string
      type: object
    DomainDnssecRequest:
      description: Request payload for adding DNSSEC DS records to a domain.
      example:
        algorithm:
        - 0
        - 0
        digest_type:
        - 6
        - 6
        digest:
        - digest
        - digest
        key_tag:
        - 1
        - 1
      properties:
        algorithm:
          description: List of DNSSEC algorithm IDs for each record.
          items:
            type: integer
          type: array
        digest_type:
          description: List of digest type IDs for each record.
          items:
            type: integer
          type: array
        digest:
          description: List of hex digests for each record.
          items:
            type: string
          type: array
        key_tag:
          description: List of key tag values corresponding to each record.
          items:
            type: integer
          type: array
      type: object
    DomainDnssecRecords:
      description: DNSSEC DS records currently configured for a domain.
      items:
        $ref: "#/components/schemas/DomainDnssecRecords_inner"
      type: array
    DomainLookupResponse:
      description: "Availability, pricing, and order-field metadata for a domain lookup."
      example:
        available: true
        premium: true
        website: true
        domain_service: true
        service: "{}"
        whois_privacy: true
        new: new
        renewal: renewal
        transfer: transfer
        fields: "{}"
        currencies: "{}"
      properties:
        available:
          description: Whether the domain is available to register.
          type: boolean
        premium:
          description: Whether the domain is marked as premium by the registrar.
          type: boolean
        website:
          description: Indicates if the domain is already used by a website service.
          type: boolean
        domain_service:
          description: Indicates if the domain already exists as a domain service
            on the account.
          type: boolean
        service:
          description: Service catalog details for the domain's TLD.
          type: object
        whois_privacy:
          description: Whether Whois privacy is available for the TLD.
          type: boolean
        new:
          description: "Calculated registration price, when available."
          type: string
        renewal:
          description: "Calculated renewal price, when available."
          type: string
        transfer:
          description: "Calculated transfer price, when available."
          type: string
        fields:
          description: Registrar field requirements for this domain/TLD.
          type: object
        currencies:
          description: Pricing information normalized to supported currencies.
          type: object
      type: object
    DomainSearchResponse:
      description: Lookup and suggestion results returned for a domain search.
      example:
        success: true
        response_text: response_text
        response_time: response_time
        lookup:
        - "{}"
        - "{}"
        suggest:
        - "{}"
        - "{}"
        tlds:
        - tlds
        - tlds
      properties:
        success:
          description: Indicates whether the registrar search succeeded.
          type: boolean
        response_text:
          description: Human-readable status text from the registrar.
          type: string
        response_time:
          description: Response time as reported by the registrar.
          type: string
        lookup:
          description: Availability lookup results for queried domains.
          items:
            type: object
          type: array
        suggest:
          description: Suggested alternative domains and availability data.
          items:
            type: object
          type: array
        tlds:
          description: TLDs evaluated during the search.
          items:
            type: string
          type: array
      type: object
    DomainOrder:
      description: Ordering metadata for domain registration and transfers.
      example:
        whoisPrivacyCost: "5"
        services:
          DomainOrderServices10001:
            services_id: "10001"
            services_name: .asia Domain Name Registration
            services_cost: "19.00"
            services_currency: USD
            services_category: "100"
            services_buyable: "1"
            services_type: "100"
            services_field1: .asia
            services_field2: ""
            services_module: domains
            services_hidden: "0"
        tldServices: "{}"
      properties:
        whoisPrivacyCost:
          description: Cost of Whois Privacy
          example: "5"
          type: string
        services:
          $ref: "#/components/schemas/DomainOrder_services"
        tldServices:
          description: Map of TLDs to domain service IDs.
          type: object
      type: object
    DomainOrderResponse:
      description: Registrar response metadata returned after a domain order.
      example:
        _OPS_version: _OPS_version
        protocol: protocol
        is_success: is_success
        action: action
        attributes:
          id: id
          admin_email: admin_email
        response_text: response_text
        object: object
        response_code: response_code
      properties:
        _OPS_version:
          type: string
        protocol:
          type: string
        is_success:
          type: string
        action:
          type: string
        attributes:
          $ref: "#/components/schemas/DomainOrderResponse_attributes"
        response_text:
          type: string
        object:
          type: string
        response_code:
          type: string
      type: object
    DomainOrderServices10001:
      description: Example schema for a domain service catalog entry.
      example:
        services_id: "10001"
        services_name: .asia Domain Name Registration
        services_cost: "19.00"
        services_currency: USD
        services_category: "100"
        services_buyable: "1"
        services_type: "100"
        services_field1: .asia
        services_field2: ""
        services_module: domains
        services_hidden: "0"
      properties:
        services_id:
          description: Service ID
          example: "10001"
          type: string
        services_name:
          description: Service Name
          example: .asia Domain Name Registration
          type: string
        services_cost:
          description: Service Cost
          example: "19.00"
          type: string
        services_currency:
          description: Currency of Service Cost
          example: USD
          type: string
        services_category:
          description: Service Category
          example: "100"
          type: string
        services_buyable:
          description: Buyable flag for Service
          example: "1"
          type: string
        services_type:
          description: Service Type
          example: "100"
          type: string
        services_field1:
          description: Field 1 of Service
          example: .asia
          type: string
        services_field2:
          description: Field 2 of Service
          example: ""
          type: string
        services_module:
          description: Module of Service
          example: domains
          type: string
        services_hidden:
          description: Hidden flag for Service
          example: "0"
          type: string
      type: object
    DomainOwnerContact:
      description: Contact details for the registered domain owner.
      example:
        postal_code: postal_code
        org_name: org_name
        country: country
        status: status
        state: state
        first_name: first_name
        last_name: last_name
        address3: address3
        fax: fax
        address2: address2
        email: email
        city: city
        phone: phone
        address1: address1
      properties:
        postal_code:
          type: string
        org_name:
          type: string
        country:
          type: string
        status:
          type: string
        state:
          type: string
        first_name:
          type: string
        last_name:
          type: string
        address3:
          type: string
        fax:
          type: string
        address2:
          type: string
        email:
          type: string
        city:
          type: string
        phone:
          type: string
        address1:
          type: string
      type: object
    DomainProvProcessPending:
      description: Provisioning status data for a pending domain order.
      example:
        _OPS_version: _OPS_version
        response_text: response_text
        protocol: protocol
        response_code: response_code
        action: action
        object: object
        is_success: is_success
        attributes:
          id: id
          order_id: order_id
          registration expiration date: registration expiration date
          f_auto_renew: f_auto_renew
      properties:
        _OPS_version:
          type: string
        response_text:
          type: string
        protocol:
          type: string
        response_code:
          type: string
        action:
          type: string
        object:
          type: string
        is_success:
          type: string
        attributes:
          $ref: "#/components/schemas/DomainProvProcessPending_attributes"
      type: object
    DomainRow:
      description: A result row from the `Domains` `GET` request.
      example:
        domain_id: "592337"
        domain_hostname: mydomain.com
        domain_expire_date: 2023-08-14T00:59:38.000Z
        cost: "18.00"
        domain_status: active
      properties:
        domain_id:
          description: The ID number of the domain in our billing system.
          example: "59237"
          type: string
        domain_hostname:
          description: The hostname of the domain.
          example: mydomain.com
          type: string
        domain_expire_date:
          description: The expiration date of the domain.
          example: 2023-08-14T00:59:38.000Z
          type: string
        cost:
          description: The cost of the domain.
          example: "11.00"
          type: string
        domain_status:
          description: The billing / registration status of the domain.
          example: active
          type: string
      title: DomainRow
      type: object
    DomainServiceInfo:
      description: Detailed domain service record for a domain order.
      example:
        domain_id: domain_id
        domain_hostname: domain_hostname
        domain_username: domain_username
        domain_password: domain_password
        domain_type: domain_type
        domain_currency: domain_currency
        domain_expire_date: domain_expire_date
        domain_order_date: domain_order_date
        domain_custid: domain_custid
        domain_status: domain_status
        domain_invoice: domain_invoice
        domain_coupon: domain_coupon
      properties:
        domain_id:
          type: string
        domain_hostname:
          type: string
        domain_username:
          type: string
        domain_password:
          type: string
        domain_type:
          type: string
        domain_currency:
          type: string
        domain_expire_date:
          type: string
        domain_order_date:
          type: string
        domain_custid:
          type: string
        domain_status:
          type: string
        domain_invoice:
          type: string
        domain_coupon:
          type: string
      type: object
    DomainWhoisPrivacyRequest:
      description: Request payload for enabling or disabling Whois privacy.
      example:
        func: enable
        csrf_token: csrf_token
        domain_firstname: domain_firstname
        domain_lastname: domain_lastname
        domain_email: domain_email
        domain_address: domain_address
        domain_address2: domain_address2
        domain_address3: domain_address3
        domain_city: domain_city
        domain_state: domain_state
        domain_zip: domain_zip
        domain_country: domain_country
        domain_phone: domain_phone
        domain_fax: domain_fax
        domain_company: domain_company
        domain_extra: domain_extra
      properties:
        func:
          description: Action to perform (enable or disableCancel).
          example: enable
          type: string
        csrf_token:
          description: CSRF token if the API requires it for the account.
          type: string
        domain_firstname:
          type: string
        domain_lastname:
          type: string
        domain_email:
          type: string
        domain_address:
          type: string
        domain_address2:
          type: string
        domain_address3:
          type: string
        domain_city:
          type: string
        domain_state:
          type: string
        domain_zip:
          type: string
        domain_country:
          type: string
        domain_phone:
          type: string
        domain_fax:
          type: string
        domain_company:
          type: string
        domain_extra:
          type: string
      type: object
    DomainServiceType:
      description: "Service type definition for a domain registration, including name,\
        \ pricing, and category."
      example:
        services_id: services_id
        services_name: services_name
        services_cost: services_cost
        services_category: services_category
        services_buyable: services_buyable
        services_type: services_type
        services_field1: services_field1
        services_field2: services_field2
        services_module: services_module
      properties:
        services_id:
          description: Service type ID.
          type: string
        services_name:
          description: "Service type name (e.g., .com Registration)."
          type: string
        services_cost:
          description: Service cost per billing period.
          type: string
        services_category:
          description: Service category ID.
          type: string
        services_buyable:
          description: Whether this service type is available for purchase.
          type: string
        services_type:
          description: Internal service type identifier.
          type: string
        services_field1:
          description: TLD or first configurable field for the service type.
          type: string
        services_field2:
          description: Second configurable field for the service type.
          type: string
        services_module:
          description: Backend module handling this service type.
          type: string
      type: object
    DomainTechContact:
      description: Technical contact information for a domain registration.
      example:
        state: state
        status: status
        org_name: org_name
        country: country
        postal_code: postal_code
        fax: fax
        email: email
        address2: address2
        address3: address3
        address1: address1
        city: city
        phone: phone
        first_name: first_name
        last_name: last_name
      properties:
        state:
          type: string
        status:
          type: string
        org_name:
          type: string
        country:
          type: string
        postal_code:
          type: string
        fax:
          type: string
        email:
          type: string
        address2:
          type: string
        address3:
          type: string
        address1:
          type: string
        city:
          type: string
        phone:
          type: string
        first_name:
          type: string
        last_name:
          type: string
      type: object
    GenericResponse:
      description: Generic Response
      example:
        status: ok
        text: The command completed successfully.
      properties:
        status:
          type: string
        text:
          type: string
      required:
      - status
      - text
      title: GenericResponse
      type: object
    Home:
      example:
        last_login_ip: 99.88.77.66
        last_login: "14:58:pm - 17 Aug, 2023"
        currency: $
        amount: $23.50
        invoice_list: 5
        balance: $0.60
        full_name: John
        email: user@domain.com
        tickets:
        - tickets
        - tickets
        ticketStatus:
          Open: 4
          On Hold: 5
        ticketStatusView:
          "4": Open
          "5": On Hold
          "6": Closed
        details:
          modules:
            domains:
              icon: globe
              view_link: view_domain
              heading: Domains
              buy_link: domain_order
              list_link: view_domains_list
            webhosting:
              icon: window-maximize
              view_link: view_website
              heading: Web Hosting
              buy_link: order_website
              list_link: view_websites_list
            vps:
              icon: cloud-meatball
              view_link: view_vps
              heading: VPS
              buy_link: order_vps
              list_link: view_vps_list
            licenses:
              icon: id-card
              view_link: view_license
              heading: Licenses
              buy_link: order_license
              list_link: view_licenses_list
            backups:
              icon: warehouse
              view_link: view_backup
              heading: Storages
              buy_link: order_storage
              list_link: view_backups_list
            servers:
              icon: server
              view_link: view_server
              heading: Dedicated Servers
              buy_link: order_server
              list_link: view_servers_list
            quickservers:
              icon: database
              view_link: view_qs
              heading: Quick Servers
              buy_link: order_quickserver
              list_link: view_quickservers_list
        services:
          domains:
            links:
              "376114": pimpmy.website
              "376503": hostingenuity.com
              "592337": detain.dev
            count: 5
          webhosting:
            links:
              "376359": Doefamily.com
              "376473": fancytush.com
              "386218": admincoded.net
            count: 8
          vps:
            links:
              "465503": vps465503
              "2500081": vps2500081
              "2578866": vps2578866
            count: 20
          licenses:
            links:
              "386522": 1.2.3.4
            count: 1
          servers:
            links:
              "16058": anotherserver.com
            count: 1
          backups:
            links:
            - links
            - links
            count: 0
        AFFILIATE_AMOUNT: "100"
      properties:
        last_login_ip:
          description: Last login IP.
          example: 99.88.77.66
          type: string
        last_login:
          description: Last login time.
          example: "14:58:pm - 17 Aug, 2023"
          type: string
        currency:
          description: Currency symbol.
          example: $
          type: string
        amount:
          description: Amount with currency.
          example: $23.50
          type: string
        invoice_list:
          description: Number of invoices.
          example: 5
          type: integer
        balance:
          description: Balance with currency.
          example: $0.60
          type: string
        full_name:
          description: Users full name.
          example: John
          type: string
        email:
          description: User email address.
          example: user@domain.com
          type: string
        tickets:
          description: List of tickets.
          items:
            type: string
          type: array
        ticketStatus:
          $ref: "#/components/schemas/Home_ticketStatus"
        ticketStatusView:
          $ref: "#/components/schemas/Home_ticketStatusView"
        details:
          $ref: "#/components/schemas/Home_details"
        services:
          $ref: "#/components/schemas/Home_services"
        AFFILIATE_AMOUNT:
          description: Affiliate amount with currency.
          example: "100"
          type: string
      required:
      - AFFILIATE_AMOUNT
      - amount
      - balance
      - currency
      - details
      - email
      - full_name
      - invoice_list
      - last_login
      - last_login_ip
      - services
      - ticketStatus
      - ticketStatusView
      - tickets
      type: object
    SearchAutocompleteResponse:
      additionalProperties:
        description: Search result fields vary based on service type.
        type: object
      description: Autocomplete search results for the authenticated account.
      type: object
    HomeDetailsModulesBackups:
      description: Dashboard module configuration for backup storage services.
      example:
        icon: warehouse
        view_link: view_backup
        heading: Storages
        buy_link: order_storage
        list_link: view_backups_list
      properties:
        icon:
          description: The icon for storages.
          example: warehouse
          type: string
        view_link:
          description: Link to view backup.
          example: view_backup
          type: string
        heading:
          description: Heading for storages.
          example: Storages
          type: string
        buy_link:
          description: Link to order storage.
          example: order_storage
          type: string
        list_link:
          description: Link to view backups list.
          example: view_backups_list
          type: string
      type: object
    HomeDetailsModulesDomains:
      description: Dashboard module configuration for domain registration services.
      example:
        icon: globe
        view_link: view_domain
        heading: Domains
        buy_link: domain_order
        list_link: view_domains_list
      properties:
        icon:
          description: The icon for domains.
          example: globe
          type: string
        view_link:
          description: Link to view domain.
          example: view_domain
          type: string
        heading:
          description: Heading for domains.
          example: Domains
          type: string
        buy_link:
          description: Link to order domain.
          example: domain_order
          type: string
        list_link:
          description: Link to view domains list.
          example: view_domains_list
          type: string
      type: object
    HomeDetailsModulesLicenses:
      description: Dashboard module configuration for software license services.
      example:
        icon: id-card
        view_link: view_license
        heading: Licenses
        buy_link: order_license
        list_link: view_licenses_list
      properties:
        icon:
          description: The icon for licenses.
          example: id-card
          type: string
        view_link:
          description: Link to view license.
          example: view_license
          type: string
        heading:
          description: Heading for licenses.
          example: Licenses
          type: string
        buy_link:
          description: Link to order license.
          example: order_license
          type: string
        list_link:
          description: Link to view licenses list.
          example: view_licenses_list
          type: string
      type: object
    HomeDetailsModulesQuickservers:
      description: Dashboard module configuration for QuickServer services.
      example:
        icon: database
        view_link: view_qs
        heading: Quick Servers
        buy_link: order_quickserver
        list_link: view_quickservers_list
      properties:
        icon:
          description: The icon for quick servers.
          example: database
          type: string
        view_link:
          description: Link to view quick servers.
          example: view_qs
          type: string
        heading:
          description: Heading for quick servers.
          example: Quick Servers
          type: string
        buy_link:
          description: Link to order quick server.
          example: order_quickserver
          type: string
        list_link:
          description: Link to view quick servers list.
          example: view_quickservers_list
          type: string
      type: object
    HomeDetailsModulesServers:
      description: Dashboard module configuration for dedicated server services.
      example:
        icon: server
        view_link: view_server
        heading: Dedicated Servers
        buy_link: order_server
        list_link: view_servers_list
      properties:
        icon:
          description: The icon for dedicated servers.
          example: server
          type: string
        view_link:
          description: Link to view server.
          example: view_server
          type: string
        heading:
          description: Heading for dedicated servers.
          example: Dedicated Servers
          type: string
        buy_link:
          description: Link to order server.
          example: order_server
          type: string
        list_link:
          description: Link to view servers list.
          example: view_servers_list
          type: string
      type: object
    HomeDetailsModulesVps:
      description: Dashboard module configuration for VPS services.
      example:
        icon: cloud-meatball
        view_link: view_vps
        heading: VPS
        buy_link: order_vps
        list_link: view_vps_list
      properties:
        icon:
          description: The icon for VPS.
          example: cloud-meatball
          type: string
        view_link:
          description: Link to view VPS.
          example: view_vps
          type: string
        heading:
          description: Heading for VPS.
          example: VPS
          type: string
        buy_link:
          description: Link to order VPS.
          example: order_vps
          type: string
        list_link:
          description: Link to view VPS list.
          example: view_vps_list
          type: string
      type: object
    HomeDetailsModulesWebhosting:
      description: Dashboard module configuration for webhosting services.
      example:
        icon: window-maximize
        view_link: view_website
        heading: Web Hosting
        buy_link: order_website
        list_link: view_websites_list
      properties:
        icon:
          description: The icon for web hosting.
          example: window-maximize
          type: string
        view_link:
          description: Link to view website.
          example: view_website
          type: string
        heading:
          description: Heading for web hosting.
          example: Web Hosting
          type: string
        buy_link:
          description: Link to order website.
          example: order_website
          type: string
        list_link:
          description: Link to view websites list.
          example: view_websites_list
          type: string
      type: object
    HomeServicesDomainsLinks:
      description: Map of domain service IDs to their hostnames for the account dashboard.
      example:
        "376114": pimpmy.website
        "376503": hostingenuity.com
        "592337": detain.dev
      properties:
        "376114":
          description: Link to a domain.
          example: pimpmy.website
          type: string
        "376503":
          description: Link to a domain.
          example: hostingenuity.com
          type: string
        "592337":
          description: Link to a domain.
          example: detain.dev
          type: string
      type: object
    HomeServicesLicensesLinks:
      description: Map of license service IDs to their IP addresses for the account
        dashboard.
      example:
        "386522": 1.2.3.4
      properties:
        "386522":
          description: Link to a license.
          example: 1.2.3.4
          type: string
      type: object
    HomeServicesServersLinks:
      description: Map of dedicated server service IDs to their hostnames for the
        account dashboard.
      example:
        "16058": anotherserver.com
      properties:
        "16058":
          description: Link to a server.
          example: anotherserver.com
          type: string
      type: object
    HomeServicesVpsLinks:
      description: Map of VPS service IDs to their hostnames for the account dashboard.
      example:
        "465503": vps465503
        "2500081": vps2500081
        "2578866": vps2578866
      properties:
        "465503":
          description: Link to a VPS.
          example: vps465503
          type: string
        "2500081":
          description: Link to a VPS.
          example: vps2500081
          type: string
        "2578866":
          description: Link to a VPS.
          example: vps2578866
          type: string
      type: object
    HomeServicesWebhostingLinks:
      description: Map of webhosting service IDs to their hostnames for the account
        dashboard.
      example:
        "376359": Doefamily.com
        "376473": fancytush.com
        "386218": admincoded.net
      properties:
        "376359":
          description: Link to a website.
          example: Doefamily.com
          type: string
        "376473":
          description: Link to a website.
          example: fancytush.com
          type: string
        "386218":
          description: Link to a website.
          example: admincoded.net
          type: string
      type: object
    HostnameObject:
      description: Request specifying the hostname.
      example:
        hostname: new.host.com
      properties:
        hostname:
          type: string
      title: HostnameObject
      type: object
    IpLimitRange:
      description: The lower and upper bounds of an ip range.
      example:
        start: 1.2.3.0
        end: 1.2.3.255
      properties:
        start:
          description: The begining (or first) IP address in the range.
          example: 1.2.3.0
          type: string
        end:
          description: The ending (or last) IP address in the range.
          example: 1.2.3.255
          type: string
      required:
      - end
      - start
      title: IpLimitRange
      type: object
    License:
      example:
        serviceInfo:
          license_id: "386522"
          license_type: "5034"
          license_currency: USD
          license_order_date: 2020-01-14T10:48:14Z
          license_custid: "771282"
          license_ip: 1.2.3.4
          license_status: active
          license_hostname: ""
          license_key: ""
          license_invoice: "18704419"
          license_coupon: "1836"
          license_extra: ""
        client_links:
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: ""
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: ""
        billingDetails:
          service_last_invoice_date: "August 14, 2023"
          service_payment_status: Paid
          service_frequency: Monthly
          next_date: 2023-09-14T09:39:46Z
          service_next_invoice_date: "September 14, 2023"
          service_currency: USD
          service_currency_symbol: $
          service_coupon: ACOUPONFORLICENSES
          service_cost_info: "0.00"
          service_extra:
          - service_extra
          - service_extra
          service_extra_json: "[\"\"]"
        custCurrency: USD
        custCurrencySymbol: $
        package: KernelCare License
        serviceExtra:
        - serviceExtra
        - serviceExtra
        extraInfoTables:
          ip_info:
            title: IP Information
            rows:
            - desc: Netmask
              value: 255.255.255.248
            - desc: Netmask
              value: 255.255.255.248
        service_overview_extra: service_overview_extra
        serviceType:
          services_id: "5034"
          services_name: KernelCare License
          services_cost: "2.95"
          services_category: "508"
          services_buyable: "1"
          services_type: "508"
          services_field1: "16"
          services_field2: ""
          services_module: licenses
        license_key: ""
      properties:
        serviceInfo:
          $ref: "#/components/schemas/LicenseServiceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/LicenseClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/LicenseBillingDetails"
        custCurrency:
          description: Customer's currency
          example: USD
          type: string
        custCurrencySymbol:
          description: Currency symbol for customer
          example: $
          type: string
        package:
          description: Package name
          example: KernelCare License
          type: string
        serviceExtra:
          description: Extra service information
          items:
            type: string
          type: array
        extraInfoTables:
          $ref: "#/components/schemas/License_extraInfoTables"
        service_overview_extra:
          description: Extra service overview information
          type: string
        serviceType:
          $ref: "#/components/schemas/LicenseServiceType"
        license_key:
          description: License key
          example: ""
          type: string
      required:
      - billingDetails
      - client_links
      - custCurrency
      - custCurrencySymbol
      - extraInfoTables
      - license_key
      - package
      - serviceExtra
      - serviceInfo
      - serviceType
      - service_overview_extra
      type: object
    LicenseBillingDetails:
      description: "Billing information for a software license including payment status,\
        \ billing cycle, and cost."
      example:
        service_last_invoice_date: "August 14, 2023"
        service_payment_status: Paid
        service_frequency: Monthly
        next_date: 2023-09-14T09:39:46Z
        service_next_invoice_date: "September 14, 2023"
        service_currency: USD
        service_currency_symbol: $
        service_coupon: ACOUPONFORLICENSES
        service_cost_info: "0.00"
        service_extra:
        - service_extra
        - service_extra
        service_extra_json: "[\"\"]"
      properties:
        service_last_invoice_date:
          description: Last invoice date
          example: "August 14, 2023"
          type: string
        service_payment_status:
          description: Payment status
          example: Paid
          type: string
        service_frequency:
          description: Service frequency
          example: Monthly
          type: string
        next_date:
          description: Next date
          example: 2023-09-14T09:39:46Z
          format: date-time
          type: string
        service_next_invoice_date:
          description: Next invoice date
          example: "September 14, 2023"
          type: string
        service_currency:
          description: Service currency
          example: USD
          type: string
        service_currency_symbol:
          description: Service currency symbol
          example: $
          type: string
        service_coupon:
          description: Service coupon
          example: ACOUPONFORLICENSES
          type: string
        service_cost_info:
          description: Service cost information
          example: "0.00"
          type: string
        service_extra:
          description: Additional service information
          items:
            type: string
          type: array
        service_extra_json:
          description: Additional service information in JSON format
          example: "[\"\"]"
          type: string
      type: object
    LicenseClientLink:
      example:
        label: Invoices
        link: invoices
        icon: fas fa-file-invoice-dollar fa-w-12
        icon_text: ""
        help_text: Invoice History
        other_attr: ""
      properties:
        label:
          description: Link label
          example: Invoices
          type: string
        link:
          description: Link URL
          example: invoices
          type: string
        icon:
          description: Link icon
          example: fas fa-file-invoice-dollar fa-w-12
          type: string
        icon_text:
          description: Icon text
          example: ""
          type: string
        help_text:
          description: Help text
          example: Invoice History
          type: string
        other_attr:
          description: Other attributes
          example: ""
          type: string
      required:
      - help_text
      - icon
      - label
      - link
      type: object
    LicenseIpInfo:
      description: IP address information table for a software license service.
      example:
        title: IP Information
        rows:
        - desc: Netmask
          value: 255.255.255.248
        - desc: Netmask
          value: 255.255.255.248
      properties:
        title:
          description: Table title
          example: IP Information
          type: string
        rows:
          items:
            $ref: "#/components/schemas/LicenseIpInfoRow"
          type: array
      type: object
    LicenseIpInfoRow:
      description: A single row in the license IP information table.
      example:
        desc: Netmask
        value: 255.255.255.248
      properties:
        desc:
          description: Row description
          example: Netmask
          type: string
        value:
          description: Row value
          example: 255.255.255.248
          type: string
      type: object
    LicenseRow:
      description: A result row from the `Licenses` `GET` request.
      example:
        license_id: "386111"
        license_hostname: ""
        license_ip: 66.45.228.100
        services_name: Imunify360 up to 30 users
        cost: "25.00"
        license_status: canceled
        invoices_paid: "1"
        invoices_date: 2019-08-28T14:27:22.000Z
      properties:
        license_id:
          description: The id of the license.
          example: "386111"
          type: string
        license_hostname:
          description: The hostname of the license.
          example: ""
          type: string
        license_ip:
          description: The ip of the license.
          example: 66.45.228.100
          type: string
        services_name:
          description: The services name of the license.
          example: Imunify360 up to 30 users
          type: string
        cost:
          description: The cost of the license.
          example: "25.00"
          type: string
        license_status:
          description: The status of the license.
          example: canceled
          type: string
        invoices_paid:
          description: The invoices paid of the license.
          example: "1"
          type: string
        invoices_date:
          description: The invoices date of the license.
          example: 2019-08-28T14:27:22Z
          format: date-time
          type: string
      title: LicenseRow
      type: object
    LicenseServiceInfo:
      example:
        license_id: "386522"
        license_type: "5034"
        license_currency: USD
        license_order_date: 2020-01-14T10:48:14Z
        license_custid: "771282"
        license_ip: 1.2.3.4
        license_status: active
        license_hostname: ""
        license_key: ""
        license_invoice: "18704419"
        license_coupon: "1836"
        license_extra: ""
      properties:
        license_id:
          description: License ID
          example: "386522"
          type: string
        license_type:
          description: License type
          example: "5034"
          type: string
        license_currency:
          description: License currency
          example: USD
          type: string
        license_order_date:
          description: License order date
          example: 2020-01-14T10:48:14Z
          format: date-time
          type: string
        license_custid:
          description: Customer ID
          example: "771282"
          type: string
        license_ip:
          description: License IP
          example: 1.2.3.4
          type: string
        license_status:
          description: License status
          example: active
          type: string
        license_hostname:
          description: License hostname
          example: ""
          type: string
        license_key:
          description: License key
          example: ""
          type: string
        license_invoice:
          description: License invoice
          example: "18704419"
          type: string
        license_coupon:
          description: License coupon
          example: "1836"
          type: string
        license_extra:
          description: Additional license information
          example: ""
          type: string
      required:
      - license_coupon
      - license_currency
      - license_custid
      - license_id
      - license_invoice
      - license_ip
      - license_order_date
      - license_status
      - license_type
      type: object
    LicenseServiceType:
      description: "Service type definition for a software license, including name,\
        \ pricing, and category."
      example:
        services_id: "5034"
        services_name: KernelCare License
        services_cost: "2.95"
        services_category: "508"
        services_buyable: "1"
        services_type: "508"
        services_field1: "16"
        services_field2: ""
        services_module: licenses
      properties:
        services_id:
          description: Service ID
          example: "5034"
          type: string
        services_name:
          description: Service name
          example: KernelCare License
          type: string
        services_cost:
          description: Service cost
          example: "2.95"
          type: string
        services_category:
          description: Service category
          example: "508"
          type: string
        services_buyable:
          description: Buyable status
          example: "1"
          type: string
        services_type:
          description: Service type
          example: "508"
          type: string
        services_field1:
          description: Service field 1
          example: "16"
          type: string
        services_field2:
          description: Service field 2
          example: ""
          type: string
        services_module:
          description: Service module
          example: licenses
          type: string
      type: object
    LicensesOrder:
      description: Available license packages and pricing for ordering a new software
        license.
      example:
        serviceCategories:
          LicensesOrderServiceCategories509:
            category_id: "509"
            category_name: Webuzo
            category_tag: webuzo
            category_module: licenses
        packageCosts:
          LicensesOrderPackageCosts11468: 3.75
        serviceTypes:
          LicensesOrderServiceTypes11482:
            services_id: "11482"
            services_name: DirectAdmin Standard
            services_cost: "21.75"
            services_category: "506"
            services_buyable: "1"
            services_type: "506"
            services_field1: "2704,Standard"
            services_field2: Unlimited Accounts & Domains<br><br>The DirectAdmin standard
              license is best for medium to large shared-hosting and/or reseller-hosting
              companies. No limit on accounts or domains.
            services_module: licenses
      properties:
        serviceCategories:
          $ref: "#/components/schemas/LicensesOrder_serviceCategories"
        packageCosts:
          $ref: "#/components/schemas/LicensesOrder_packageCosts"
        serviceTypes:
          $ref: "#/components/schemas/LicensesOrder_serviceTypes"
      type: object
    LicensesOrderServiceCategories509:
      example:
        category_id: "509"
        category_name: Webuzo
        category_tag: webuzo
        category_module: licenses
      properties:
        category_id:
          description: Category ID
          example: "509"
          type: string
        category_name:
          description: Category Name
          example: Webuzo
          type: string
        category_tag:
          description: Category Tag
          example: webuzo
          type: string
        category_module:
          description: Category Module
          example: licenses
          type: string
      type: object
    LicensesOrderServiceTypes11482:
      example:
        services_id: "11482"
        services_name: DirectAdmin Standard
        services_cost: "21.75"
        services_category: "506"
        services_buyable: "1"
        services_type: "506"
        services_field1: "2704,Standard"
        services_field2: Unlimited Accounts & Domains<br><br>The DirectAdmin standard
          license is best for medium to large shared-hosting and/or reseller-hosting
          companies. No limit on accounts or domains.
        services_module: licenses
      properties:
        services_id:
          description: Service ID
          example: "11482"
          type: string
        services_name:
          description: Service Name
          example: DirectAdmin Standard
          type: string
        services_cost:
          description: Service Cost
          example: "21.75"
          type: string
        services_category:
          description: Service Category
          example: "506"
          type: string
        services_buyable:
          description: Buyable flag for Service
          example: "1"
          type: string
        services_type:
          description: Service Type
          example: "506"
          type: string
        services_field1:
          description: Field 1 of Service
          example: "2704,Standard"
          type: string
        services_field2:
          description: Field 2 of Service
          example: Unlimited Accounts & Domains<br><br>The DirectAdmin standard license
            is best for medium to large shared-hosting and/or reseller-hosting companies.
            No limit on accounts or domains.
          type: string
        services_module:
          description: Module of Service
          example: licenses
          type: string
      type: object
    LoginErrorResponse:
      description: Error resposne during login indicating further action.
      example:
        message: missing 2fa
        field: 2fa
      properties:
        message:
          type: string
        field:
          type: string
      title: LoginErrorResponse
      type: object
    LoginInfo:
      description: Basic information useful for rendering a login page.
      example:
        logo: //my.interserver.net/images/logos/mystaging.png
        captcha: "data:image/jpeg;base64,/9j/"
        language: en-US
        counts:
          vps: 290201
          websites: 205172
          servers: 27940
      properties:
        logo:
          description: A logo image url.
          example: //my.interserver.net/images/logos/mystaging.png
          type: string
        captcha:
          description: A base64 encoded image to use for rendering the alternateive
            captcha.
          example: "data:image/jpeg;base64,/9j/"
          type: string
        language:
          description: The desired langauge to render the site with.
          example: en-US
          type: string
        counts:
          $ref: "#/components/schemas/LoginServiceCounts"
      required:
      - captcha
      - counts
      title: LoginInfo
      type: object
    LoginServiceCounts:
      description: Order counts per module.
      example:
        vps: 290201
        websites: 205172
        servers: 27940
      properties:
        vps:
          description: The number of total VPS orders that have been placed in our
            billing system.
          example: 205172
          format: int32
          type: integer
        websites:
          description: The number of total website orders that have been placed in
            our billing system.
          example: 205172
          format: int32
          type: integer
        servers:
          description: The number of total server orders that have been placed in
            our billing system.
          example: 205172
          format: int32
          type: integer
      required:
      - servers
      - vps
      - websites
      title: LoginServiceCounts
      type: object
    LoginSubmissionExample:
      description: The data to submit in the login request.
      example:
        login: user@domain.com
        passwd: mypassword
        remember: "true"
        g-recaptcha-response:
          __v_isShallow: false
          dep:
            w: 0
            "n": 0
          __v_isRef: true
          _rawValue: zzzzz
          _value: zzzzz
      properties:
        login:
          type: string
        passwd:
          type: string
        remember:
          type: string
        g-recaptcha-response:
          $ref: "#/components/schemas/LoginSubmissionExample_g_recaptcha_response"
        tfa:
          description: Two Factor Authentication Response.
          type: string
      required:
      - login
      - passwd
      title: LoginSubmissionExample
      type: object
    MailBillingDetails:
      description: "Billing information for a mail service including payment status,\
        \ billing cycle, and cost."
      example:
        service_last_invoice_date: "July 16, 2023"
        service_payment_status: Unpaid
        service_frequency: Monthly
        next_date: 2023-08-16T00:55:05.000Z
        service_next_invoice_date: "August 16, 2023"
        service_currency: USD
        service_currency_symbol: $
        service_cost_info: "1.00"
        service_extra: []
        service_extra_json: "[]"
      properties:
        service_last_invoice_date:
          description: The last invoice date of the service.
          example: "July 16, 2023"
          type: string
        service_payment_status:
          description: The payment status of the service.
          example: Unpaid
          type: string
        service_frequency:
          description: The frequency of the service payment.
          example: Monthly
          type: string
        next_date:
          description: The next payment date of the service.
          example: 2023-08-16T00:55:05.000Z
          type: string
        service_next_invoice_date:
          description: The next invoice date of the service.
          example: "August 16, 2023"
          type: string
        service_currency:
          description: The currency of the service.
          example: USD
          type: string
        service_currency_symbol:
          description: The currency symbol of the service.
          example: $
          type: string
        service_cost_info:
          description: The cost information of the service.
          example: "1.00"
          type: string
        service_extra:
          description: Extra information for the service.
          example: []
          items:
            type: string
          type: array
        service_extra_json:
          description: Extra JSON information for the service.
          example: "[]"
          type: string
      type: object
    MailClientLink:
      description: A navigation link for mail service actions in the client portal.
      example:
        label: Invoices
        link: invoices
        icon: fas fa-file-invoice-dollar fa-w-12
        icon_text: ""
        help_text: Invoice History
        other_attr: ""
      properties:
        label:
          description: The label of the client link.
          example: Invoices
          type: string
        link:
          description: The link URL of the client link.
          example: invoices
          type: string
        icon:
          description: The icon class of the client link.
          example: fas fa-file-invoice-dollar fa-w-12
          type: string
        icon_text:
          description: The text for the icon of the client link.
          example: ""
          type: string
        help_text:
          description: Help text for the client link.
          example: Invoice History
          type: string
        other_attr:
          description: Additional attributes for the client link.
          example: ""
          type: string
      type: object
    MailExtraInfoTable:
      description: "A supplementary information table for a mail service (e.g., connection\
        \ details)."
      example:
        title: Connection Information
        rows:
        - desc: SMTP Server
          value: relay.mailbaby.net
        - desc: SMTP Server
          value: relay.mailbaby.net
      properties:
        title:
          description: The title of the extra info table.
          example: Connection Information
          type: string
        rows:
          description: The rows of the extra info table.
          items:
            $ref: "#/components/schemas/MailExtraInfoTableRow"
          type: array
      type: object
    MailExtraInfoTableRow:
      description: A single row in the mail service supplementary information table.
      example:
        desc: SMTP Server
        value: relay.mailbaby.net
      properties:
        desc:
          description: The description of the extra info table row.
          example: SMTP Server
          type: string
        value:
          description: The value of the extra info table row.
          example: relay.mailbaby.net
          type: string
      type: object
    MailOrder:
      description: A mail order record
      example:
        id: 21472
        status: active
        username: mb21472
      properties:
        id:
          description: The ID of the order.
          example: 21472
          format: int32
          type: integer
        status:
          description: The order status.
          example: active
          type: string
        username:
          description: The username to use for this order.
          example: mb21472
          type: string
        comment:
          description: Optional order comment.
          type: string
      required:
      - id
      - status
      - username
      title: MailOrder
      type: object
    MailRow:
      description: A result row from the `Mail` `GET` request.
      example:
        mail_id: "5652"
        repeat_invoices_cost: "5.99"
        mail_username: mb5652
        mail_status: expired
        services_name: MailBaby Mail
      properties:
        mail_id:
          description: The id of the mail.
          example: "5652"
          type: string
        repeat_invoices_cost:
          description: The repeat invoices cost of the mail.
          example: "5.99"
          type: string
        mail_username:
          description: The username of the mail.
          example: mb5652
          type: string
        mail_status:
          description: The status of the mail.
          example: expired
          type: string
        services_name:
          description: The services name of the mail.
          example: MailBaby Mail
          type: string
      title: MailRow
      type: object
    MailSchema:
      example:
        serviceInfo:
          mail_id: "43171"
          mail_username: ""
          mail_type: "10880"
          mail_currency: USD
          mail_order_date: 2023-07-16T00:55:05.000Z
          mail_custid: "771282"
          mail_quota: "0"
          mail_ip: ""
          mail_status: expired
          mail_invoice: "20410322"
          mail_coupon: "0"
          mail_extra: "[]"
          mail_server_status: ""
          mail_comment: ""
        client_links:
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: ""
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: ""
        billingDetails:
          service_last_invoice_date: "July 16, 2023"
          service_payment_status: Unpaid
          service_frequency: Monthly
          next_date: 2023-08-16T00:55:05.000Z
          service_next_invoice_date: "August 16, 2023"
          service_currency: USD
          service_currency_symbol: $
          service_cost_info: "1.00"
          service_extra: []
          service_extra_json: "[]"
        custCurrency: USD
        custCurrencySymbol: $
        package: MailBaby Mail
        serviceExtra: []
        extraInfoTables:
          mail:
            title: Connection Information
            rows:
            - desc: SMTP Server
              value: relay.mailbaby.net
            - desc: SMTP Server
              value: relay.mailbaby.net
          tutorials:
            title: Tutorials
            rows:
            - desc: cPanel Tutorial
              value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
                Here</a>
            - desc: cPanel Tutorial
              value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
                Here</a>
        serviceType:
          services_id: "10880"
          services_name: MailBaby Mail
          services_cost: "1.00"
          services_category: "800"
          services_buyable: "1"
          services_type: "800"
          services_field1: ""
          services_field2: ""
          services_module: mail
        usage_count: "0"
      properties:
        serviceInfo:
          $ref: "#/components/schemas/MailServiceInfo"
        client_links:
          description: Links related to the mail service for clients.
          items:
            $ref: "#/components/schemas/MailClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/MailBillingDetails"
        custCurrency:
          description: The customer's currency.
          example: USD
          type: string
        custCurrencySymbol:
          description: The currency symbol for the customer.
          example: $
          type: string
        package:
          description: The package of the mail service.
          example: MailBaby Mail
          type: string
        serviceExtra:
          description: Extra information for the mail service.
          example: []
          items:
            type: string
          type: array
        extraInfoTables:
          $ref: "#/components/schemas/MailSchema_extraInfoTables"
        serviceType:
          $ref: "#/components/schemas/MailServiceType"
        usage_count:
          description: The usage count of the mail service.
          example: "0"
          type: string
      required:
      - billingDetails
      - client_links
      - custCurrency
      - custCurrencySymbol
      - extraInfoTables
      - package
      - serviceInfo
      - serviceType
      - usage_count
      type: object
    MailAlertsResponse:
      description: Alert configuration entries for a mail service.
      items:
        $ref: "#/components/schemas/MailAlertsResponse_inner"
      type: array
    MailAlertRequest:
      description: Payload for creating a mail alert.
      example:
        type: type
        value: value
        to: to
        enabled: enabled
      properties:
        type:
          description: Alert type identifier.
          type: string
        value:
          description: Alert value or threshold.
          type: string
        to:
          description: Email address to notify.
          type: string
        enabled:
          description: Whether the alert is enabled.
          type: string
      type: object
    MailAlertUpdateRequest:
      description: Payload for updating an existing mail alert.
      example:
        alert_id: 0
        type: type
        value: value
        to: to
        enabled: enabled
      properties:
        alert_id:
          description: Alert ID to update.
          type: integer
        type:
          description: Alert type identifier.
          type: string
        value:
          description: Alert value or threshold.
          type: string
        to:
          description: Email address to notify.
          type: string
        enabled:
          description: Whether the alert is enabled.
          type: string
      type: object
    MailDelistRequest:
      description: Payload for removing a sender from mail blocklists.
      example:
        unblock: unblock
      properties:
        unblock:
          description: Email address to delist.
          type: string
      type: object
    MailDelistResponse:
      description: Blocklist status information for a mail service.
      example:
        id: 0
        local:
        - "{}"
        - "{}"
        mbtrap:
        - "{}"
        - "{}"
        subject:
        - "{}"
        - "{}"
        manual:
        - "{}"
        - "{}"
      properties:
        id:
          description: Mail service ID.
          type: integer
        local:
          description: Local blocklist entries.
          items:
            type: object
          type: array
        mbtrap:
          description: MailBaby trap block entries.
          items:
            type: object
          type: array
        subject:
          description: Subject-based block entries.
          items:
            type: object
          type: array
        manual:
          description: Manually blocked entries.
          items:
            type: object
          type: array
      type: object
    MailDeliverabilityResponse:
      description: Deliverability statistics for a mail service.
      example:
        stat: "{}"
        percent: 0.8008281904610115
        table_data:
        - - table_data
          - table_data
        - - table_data
          - table_data
      properties:
        stat:
          description: Delivered and bounced counts.
          type: object
        percent:
          description: Bounce percentage.
          type: number
        table_data:
          description: Detailed deliverability breakdown by sender or domain.
          items:
            items:
              type: string
            type: array
          type: array
      type: object
    MailServiceInfo:
      example:
        mail_id: "43171"
        mail_username: ""
        mail_type: "10880"
        mail_currency: USD
        mail_order_date: 2023-07-16T00:55:05.000Z
        mail_custid: "771282"
        mail_quota: "0"
        mail_ip: ""
        mail_status: expired
        mail_invoice: "20410322"
        mail_coupon: "0"
        mail_extra: "[]"
        mail_server_status: ""
        mail_comment: ""
      properties:
        mail_id:
          description: The ID of the mail service.
          example: "43171"
          type: string
        mail_username:
          description: The username associated with the mail service.
          example: ""
          type: string
        mail_type:
          description: The type of mail service.
          example: "10880"
          type: string
        mail_currency:
          description: The currency of the mail service.
          example: USD
          type: string
        mail_order_date:
          description: The order date of the mail service.
          example: 2023-07-16T00:55:05.000Z
          type: string
        mail_custid:
          description: The customer ID associated with the mail service.
          example: "771282"
          type: string
        mail_quota:
          description: The mail quota for the service.
          example: "0"
          type: string
        mail_ip:
          description: The IP address associated with the mail service.
          example: ""
          type: string
        mail_status:
          description: The status of the mail service.
          example: expired
          type: string
        mail_invoice:
          description: The invoice ID of the mail service.
          example: "20410322"
          type: string
        mail_coupon:
          description: The coupon associated with the mail service.
          example: "0"
          type: string
        mail_extra:
          description: Additional information for the mail service.
          example: "[]"
          type: string
        mail_server_status:
          description: The server status of the mail service.
          example: ""
          type: string
        mail_comment:
          description: Additional comments for the mail service.
          example: ""
          type: string
      required:
      - mail_currency
      - mail_custid
      - mail_id
      - mail_invoice
      - mail_order_date
      - mail_quota
      - mail_status
      - mail_type
      type: object
    MailServiceType:
      description: "Service type definition for a mail service, including name, pricing,\
        \ and category."
      example:
        services_id: "10880"
        services_name: MailBaby Mail
        services_cost: "1.00"
        services_category: "800"
        services_buyable: "1"
        services_type: "800"
        services_field1: ""
        services_field2: ""
        services_module: mail
      properties:
        services_id:
          description: The ID of the service type.
          example: "10880"
          type: string
        services_name:
          description: The name of the service type.
          example: MailBaby Mail
          type: string
        services_cost:
          description: The cost of the service type.
          example: "1.00"
          type: string
        services_category:
          description: The category of the service type.
          example: "800"
          type: string
        services_buyable:
          description: Whether the service type is buyable.
          example: "1"
          type: string
        services_type:
          description: The type of the service type.
          example: "800"
          type: string
        services_field1:
          description: Additional field for the service type.
          example: ""
          type: string
        services_field2:
          description: Additional field for the service type.
          example: ""
          type: string
        services_module:
          description: The module of the service type.
          example: mail
          type: string
      type: object
    MailTutorialsTable:
      description: A table of tutorial links displayed for a mail service.
      example:
        title: Tutorials
        rows:
        - desc: cPanel Tutorial
          value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
            Here</a>
        - desc: cPanel Tutorial
          value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
            Here</a>
      properties:
        title:
          description: The title of the tutorials table.
          example: Tutorials
          type: string
        rows:
          description: The rows of the tutorials table.
          items:
            $ref: "#/components/schemas/MailTutorialsTableRow"
          type: array
      type: object
    MailTutorialsTableRow:
      description: A single tutorial entry with a label and URL.
      example:
        desc: cPanel Tutorial
        value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
          Here</a>
      properties:
        desc:
          description: The description of the tutorials table row.
          example: cPanel Tutorial
          type: string
        value:
          description: The value of the tutorials table row.
          example: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
            Here</a>
          type: string
      type: object
    ModuleSettings:
      description: The settings for a module.
      example:
        SERVICE_ID_OFFSET: 0
        USE_REPEAT_INVOICE: true
        USE_PACKAGES: true
        BILLING_DAYS_OFFSET: 0
        IMGNAME: root-server.png
        REPEAT_BILLING_METHOD: 2
        DELETE_PENDING_DAYS: 45
        SUSPEND_DAYS: 14
        SUSPEND_WARNING_DAYS: 7
        TITLE: VPS
        MENUNAME: VPS
        EMAIL_FROM: support@interserver.net
        TBLNAME: VPS
        TABLE: vps
        TITLE_FIELD: vps_hostname
        TITLE_FIELD2: vps_ip
        TITLE_FIELD3: vps_vzid
        PREFIX: vps
      properties:
        SERVICE_ID_OFFSET:
          example: 0
          format: int32
          type: integer
        USE_REPEAT_INVOICE:
          example: true
          type: boolean
        USE_PACKAGES:
          example: true
          type: boolean
        BILLING_DAYS_OFFSET:
          example: 0
          format: int32
          type: integer
        IMGNAME:
          example: root-server.png
          type: string
        REPEAT_BILLING_METHOD:
          example: 2
          format: int32
          type: integer
        DELETE_PENDING_DAYS:
          example: 45
          format: int32
          type: integer
        SUSPEND_DAYS:
          example: 14
          format: int32
          type: integer
        SUSPEND_WARNING_DAYS:
          example: 7
          format: int32
          type: integer
        TITLE:
          example: VPS
          type: string
        MENUNAME:
          example: VPS
          type: string
        EMAIL_FROM:
          example: support@interserver.net"
          type: string
        TBLNAME:
          example: VPS
          type: string
        TABLE:
          example: vps
          type: string
        TITLE_FIELD:
          example: vps_hostname
          type: string
        TITLE_FIELD2:
          example: vps_ip
          type: string
        TITLE_FIELD3:
          example: vps_vzid
          type: string
        PREFIX:
          example: vps
          type: string
      required:
      - BILLING_DAYS_OFFSET
      - DELETE_PENDING_DAYS
      - EMAIL_FROM
      - IMGNAME
      - MENUNAME
      - PREFIX
      - REPEAT_BILLING_METHOD
      - SERVICE_ID_OFFSET
      - SUSPEND_DAYS
      - SUSPEND_WARNING_DAYS
      - TABLE
      - TBLNAME
      - TITLE
      - TITLE_FIELD
      - USE_PACKAGES
      - USE_REPEAT_INVOICE
      title: ModuleSettings
      type: object
    Modules:
      additionalProperties:
        $ref: "#/components/schemas/ModuleSettings"
      description: The modules and settings.
      example:
        domains:
          SERVICE_ID_OFFSET: 10000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 45
          IMGNAME: domain.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Domain Registrations
          MENUNAME: Domains
          EMAIL_FROM: support@interserver.net
          TBLNAME: Domains
          TABLE: domains
          TITLE_FIELD: domain_hostname
          PREFIX: domain
        vps:
          SERVICE_ID_OFFSET: 0
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: root-server.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: VPS
          MENUNAME: VPS
          EMAIL_FROM: support@interserver.net
          TBLNAME: VPS
          TABLE: vps
          TITLE_FIELD: vps_hostname
          TITLE_FIELD2: vps_ip
          TITLE_FIELD3: vps_vzid
          PREFIX: vps
        backups:
          SERVICE_ID_OFFSET: 2000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: network-drive.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Backup Services
          MENUNAME: Storage
          EMAIL_FROM: support@interserver.net
          TBLNAME: Storage
          TABLE: backups
          TITLE_FIELD: backup_username
          TITLE_FIELD2: backup_ip
          PREFIX: backup
        mail:
          SERVICE_ID_OFFSET: 11000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: e-mail.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Mail Services
          MENUNAME: Mail
          EMAIL_FROM: support@interserver.net
          TBLNAME: Mail
          TABLE: mail
          TITLE_FIELD: mail_username
          TITLE_FIELD2: mail_ip
          PREFIX: mail
        licenses:
          SERVICE_ID_OFFSET: 5000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: certificate.png
          REPEAT_BILLING_METHOD: 1
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 9
          SUSPEND_WARNING_DAYS: 7
          TITLE: Licensing
          EMAIL_FROM: invoice@cpaneldirect.net
          TBLNAME: Licenses
          TABLE: licenses
          PREFIX: license
          TITLE_FIELD: license_ip
          TITLE_FIELD2: license_key
          TITLE_FIELD3: license_hostname
          MENUNAME: Licensing
        ssl:
          SERVICE_ID_OFFSET: 3000
          USE_REPEAT_INVOICE: false
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: security-ssl.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: SSL Certificates
          MENUNAME: SSL
          EMAIL_FROM: support@inssl.net
          TBLNAME: SSL
          TABLE: ssl_certs
          TITLE_FIELD: ssl_order_id
          PREFIX: ssl
        floating_ips:
          SERVICE_ID_OFFSET: 12000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: e-mail.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Floating IPs
          MENUNAME: Floating IPs
          EMAIL_FROM: support@interserver.net
          TBLNAME: Floating IPs
          TABLE: floating_ips
          TITLE_FIELD: floating_ip_ip
          TITLE_FIELD2: floating_ip_target_ip
          PREFIX: floating_ip
        webhosting:
          SERVICE_ID_OFFSET: 1000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: true
          BILLING_DAYS_OFFSET: 0
          IMGNAME: website.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Web Hosting
          EMAIL_FROM: invoice@interserver.net
          TBLNAME: Websites
          TABLE: websites
          PREFIX: website
          TITLE_FIELD: website_hostname
          TITLE_FIELD2: website_username
          TITLE_FIELD3: website_ip
          MENUNAME: Web Hosting
        quickservers:
          SERVICE_ID_OFFSET: 0
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: false
          BILLING_DAYS_OFFSET: 0
          IMGNAME: server.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 30
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Rapid Deploy Servers
          MENUNAME: Rapid Deploy Servers
          EMAIL_FROM: support@interserver.net
          TBLNAME: Rapid Deploy Servers
          TABLE: quickservers
          TITLE_FIELD: qs_hostname
          TITLE_FIELD2: qs_ip
          TITLE_FIELD3: qs_vzid
          PREFIX: qs
        servers:
          SERVICE_ID_OFFSET: 4000
          USE_REPEAT_INVOICE: true
          USE_PACKAGES: false
          BILLING_DAYS_OFFSET: 0
          IMGNAME: stack.png
          REPEAT_BILLING_METHOD: 2
          DELETE_PENDING_DAYS: 45
          SUSPEND_DAYS: 14
          SUSPEND_WARNING_DAYS: 7
          TITLE: Dedicated Servers
          MENUNAME: Servers
          EMAIL_FROM: support@interserver.net
          TBLNAME: Servers
          TABLE: servers
          TITLE_FIELD: server_hostname
          PREFIX: server
      type: object
    PasswordRequest:
      description: Request containing a password
      example:
        password: abcdefg123456
      properties:
        password:
          type: string
      required:
      - password
      title: PasswordRequest
      type: object
    QueueResponse:
      description: Response after sending an action queue to a service.
      example:
        text: Action has been sent to the server. Please allow up to 2 minutes for
          action to be completed.
        queueId: 14670065
      properties:
        text:
          description: Response text
          example: Action has been sent to the server. Please allow up to 2 minutes
            for action to be completed.
          type: string
        queueId:
          description: The Queue ID attached to the action.
          example: 14670065
          format: int32
          type: integer
      required:
      - queueId
      - text
      title: QueueResponse
      type: object
    Quickserver:
      example:
        serviceInfo:
          qs_id: "24355"
          qs_custid: "771282"
          qs_server: "365"
          qs_ip: ""
          qs_ipv6: qs_ipv6
          qs_vzid: qs24355
          qs_currency: USD
          qs_type: "700"
          qs_order_date: 2023-04-11T20:00:06.000Z
          qs_status: canceled
          qs_invoice: "20297531"
          qs_coupon: "0"
          qs_extra: "{\"platform\":\"kvm\"}"
          qs_hostname: qs24355
          qs_server_status: deleted
          qs_comment: ""
          qs_slices: "0"
          qs_vnc: 99.88.77.66
          qs_vnc_port: 0
          qs_rootpass: ""
          qs_mac: ""
          qs_os: ubuntu24
          qs_version: Ubuntu
          qs_location: "1"
          qs_platform: qs_platform
        client_links:
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: other_attr
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
          other_attr: other_attr
        billingDetails:
          service_last_invoice_date: "April 11, 2023"
          service_payment_status: Paid
          service_frequency: Monthly
          next_date: 2023-05-11T20:00:06.000Z
          service_next_invoice_date: "May 11, 2023"
          service_currency: USD
          service_currency_symbol: $
          service_cost_info: "49.00"
          service_extra:
            platform: kvm
          service_extra_json: "{\"platform\":\"kvm\"}"
        custCurrency: USD
        custCurrencySymbol: $
        serviceMaster:
          qs_id: "365"
          qs_name: Qs365
          qs_ip: ""
          qs_type: "14"
          qs_hdsize: "1760"
          qs_hdfree: "1192"
          qs_bits: "64"
          qs_load: "3.45"
          qs_ram: "29550679"
          qs_cpu_model: Intel(R) Xeon(R) CPU E3-1271 v3 @ 3.60GHz
          qs_cpu_mhz: "2900"
          qs_location: "1"
          qs_available: "0"
          qs_cost: "49"
          qs_last_update: 2023-08-17T23:52:02.000Z
          qs_cores: "8"
          qs_iowait: "6.89"
          qs_raid_status: "OK: zfs:all pools are healthy"
          qs_drive_type: SSD
          qs_order: "92263"
          qs_raid_building: "0"
          qs_kernel: 5.15.0-69-generic
          qs_ioping: "330707348"
          qs_speed: "1000"
          qs_distro: Ubuntu
          qs_distro_version: "22.04"
          qs_bytes_sec_in: "0"
          qs_bytes_sec_out: "0"
          qs_packets_sec_in: "0"
          qs_packets_sec_out: "0"
          qs_last_install_time: qs_last_install_time
          qs_partitions: qs_partitions
          qs_cpu_flags: ""
        package: Rapid Deploy Server
        os_template: Ubuntu 22.04
        serviceExtra:
          platform: kvm
        extraInfoTables:
          ip_info:
            title: IP Information
            rows:
            - desc: Netmask
              value: ""
            - desc: Netmask
              value: ""
          addons:
            title: Addons
            rows:
            - desc: Additional IP Address
              value: ""
            - desc: Additional IP Address
              value: ""
        cpu_graph_data: "{\"labels\":[],\"value\":[]}"
        bandwidth_xaxis: "[]"
        bandwidth_yaxis: "[]"
        module: quickservers
        token: '%3Ftoken%3DAKLFIJOAQIRFOPIEWQRURQOIURWQOI'
        service_disk_used: 0.00 GB
        service_disk_total: 0.00 GB
        disk_percentage: 32.27
        memory: 0GB
        hdd: 0GB
        service_overview_extra:
        - service_overview_extra
        - service_overview_extra
      properties:
        serviceInfo:
          $ref: "#/components/schemas/QuickserverServiceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/QuickserverClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/QuickserverBillingDetails"
        custCurrency:
          description: Currency of the customer
          example: USD
          type: string
        custCurrencySymbol:
          description: Currency symbol of the customer
          example: $
          type: string
        serviceMaster:
          $ref: "#/components/schemas/QuickserverServiceMaster"
        package:
          description: Package name
          example: Rapid Deploy Server
          type: string
        os_template:
          description: Operating system template
          example: Ubuntu 22.04
          type: string
        serviceExtra:
          $ref: "#/components/schemas/Quickserver_serviceExtra"
        extraInfoTables:
          $ref: "#/components/schemas/Quickserver_extraInfoTables"
        cpu_graph_data:
          description: CPU graph data
          example: "{\"labels\":[],\"value\":[]}"
          type: string
        bandwidth_xaxis:
          description: Bandwidth x-axis data
          example: "[]"
          type: string
        bandwidth_yaxis:
          description: Bandwidth y-axis data
          example: "[]"
          type: string
        module:
          description: Module information
          example: quickservers
          type: string
        token:
          description: Authentication token
          example: '%3Ftoken%3DAKLFIJOAQIRFOPIEWQRURQOIURWQOI'
          type: string
        service_disk_used:
          description: Used disk space
          example: 0.00 GB
          type: string
        service_disk_total:
          description: Total disk space
          example: 0.00 GB
          type: string
        disk_percentage:
          description: Disk usage percentage
          example: 32.27
          type: number
        memory:
          description: Memory information
          example: 0GB
          type: string
        hdd:
          description: HDD information
          example: 0GB
          type: string
        service_overview_extra:
          items:
            type: string
          type: array
      required:
      - bandwidth_xaxis
      - bandwidth_yaxis
      - billingDetails
      - client_links
      - cpu_graph_data
      - custCurrency
      - custCurrencySymbol
      - disk_percentage
      - extraInfoTables
      - hdd
      - memory
      - module
      - os_template
      - package
      - serviceExtra
      - serviceInfo
      - serviceMaster
      - service_disk_total
      - service_disk_used
      - service_overview_extra
      - token
      type: object
    QuickserverAddons:
      example:
        title: Addons
        rows:
        - desc: Additional IP Address
          value: ""
        - desc: Additional IP Address
          value: ""
      properties:
        title:
          description: Table title
          example: Addons
          type: string
        rows:
          items:
            $ref: "#/components/schemas/QuickserverAddonsRow"
          type: array
      type: object
    QuickserverAddonsRow:
      description: A single add-on item associated with a QuickServer service.
      example:
        desc: Additional IP Address
        value: ""
      properties:
        desc:
          description: Description
          example: Additional IP Address
          type: string
        value:
          description: Value
          example: ""
          type: string
      type: object
    QuickserverBillingDetails:
      description: "Billing information for a QuickServer service including payment\
        \ status, billing cycle, and cost."
      example:
        service_last_invoice_date: "April 11, 2023"
        service_payment_status: Paid
        service_frequency: Monthly
        next_date: 2023-05-11T20:00:06.000Z
        service_next_invoice_date: "May 11, 2023"
        service_currency: USD
        service_currency_symbol: $
        service_cost_info: "49.00"
        service_extra:
          platform: kvm
        service_extra_json: "{\"platform\":\"kvm\"}"
      properties:
        service_last_invoice_date:
          description: Last invoice date
          example: "April 11, 2023"
          type: string
        service_payment_status:
          description: Payment status
          example: Paid
          type: string
        service_frequency:
          description: Service frequency
          example: Monthly
          type: string
        next_date:
          description: Next date
          example: 2023-05-11T20:00:06.000Z
          type: string
        service_next_invoice_date:
          description: Next invoice date
          example: "May 11, 2023"
          type: string
        service_currency:
          description: Currency
          example: USD
          type: string
        service_currency_symbol:
          description: Currency symbol
          example: $
          type: string
        service_cost_info:
          description: Cost information
          example: "49.00"
          type: string
        service_extra:
          $ref: "#/components/schemas/QuickserverServiceExtra"
        service_extra_json:
          description: Extra information (JSON format)
          example: "{\"platform\":\"kvm\"}"
          type: string
      type: object
    QuickserverClientLink:
      description: A navigation link for QuickServer-related actions in the client
        portal.
      example:
        label: Invoices
        link: invoices
        icon: fas fa-file-invoice-dollar fa-w-12
        icon_text: ""
        help_text: Invoice History
        other_attr: other_attr
      properties:
        label:
          description: Link label
          example: Invoices
          type: string
        link:
          description: Link
          example: invoices
          type: string
        icon:
          description: Icon class
          example: fas fa-file-invoice-dollar fa-w-12
          type: string
        icon_text:
          description: Icon text
          example: ""
          type: string
        help_text:
          description: Help text
          example: Invoice History
          type: string
        other_attr:
          description: Other attribute
          type: string
      type: object
    QuickserverIpInfo:
      description: IP address information table for a QuickServer service.
      example:
        title: IP Information
        rows:
        - desc: Netmask
          value: ""
        - desc: Netmask
          value: ""
      properties:
        title:
          description: Table title
          example: IP Information
          type: string
        rows:
          items:
            $ref: "#/components/schemas/QuickserverIpTableRow"
          type: array
      type: object
    QuickserverIpTableRow:
      description: A single row in the QuickServer IP information table.
      example:
        desc: Netmask
        value: ""
      properties:
        desc:
          description: Description
          example: Netmask
          type: string
        value:
          description: Value
          example: ""
          type: string
      type: object
    QuickserverOrder:
      description: Available QuickServer options and OS templates for ordering a new
        QuickServer.
      example:
        qs_id: "221"
        server_details:
          "381":
            cpu: AMD Ryzen 9 5900X 12-Core Processor
            ram: 119GB
            hd: 1760GB
            cores: 24
            cost: $140.00
        templates:
          Ubuntu:
          - - ubuntu24
            - ubuntu24
          - - ubuntu24
            - ubuntu24
        version:
          centosstream-8:
            centosstream-8: 8 Stream (64 bits)
        distro_sel:
          Ubuntu:
            Ubuntu: Ubuntu
      properties:
        qs_id:
          description: Quickserver ID.
          example: "221"
          type: string
        server_details:
          $ref: "#/components/schemas/QuickserverOrder_server_details"
        templates:
          $ref: "#/components/schemas/QuickserverOrder_templates"
        version:
          $ref: "#/components/schemas/QuickserverOrder_version"
        distro_sel:
          $ref: "#/components/schemas/QuickserverOrder_distro_sel"
      type: object
    QuickserverOrderDistroSelUbuntu:
      example:
        Ubuntu: Ubuntu
      properties:
        Ubuntu:
          description: Selected distribution for Ubuntu.
          example: Ubuntu
          type: string
      type: object
    QuickserverOrderServerDetails381:
      example:
        cpu: AMD Ryzen 9 5900X 12-Core Processor
        ram: 119GB
        hd: 1760GB
        cores: 24
        cost: $140.00
      properties:
        cpu:
          description: CPU details of the server.
          example: AMD Ryzen 9 5900X 12-Core Processor
          type: string
        ram:
          description: RAM capacity of the server.
          example: 119GB
          type: string
        hd:
          description: Hard disk capacity of the server.
          example: 1760GB
          type: string
        cores:
          description: Number of CPU cores.
          example: 24
          type: integer
        cost:
          description: Cost of the server.
          example: $140.00
          type: string
      type: object
    QuickserverOrderTemplatesUbuntu64:
      description: Ubuntu template details.
      items:
        items:
          description: Name of the Ubuntu template.
          example: ubuntu24
          type: string
        type: array
      type: array
    QuickserverOrderVersionCentosstream8:
      example:
        centosstream-8: 8 Stream (64 bits)
      properties:
        centosstream-8:
          description: Version details of CentOS Stream 8.
          example: 8 Stream (64 bits)
          type: string
      type: object
    QuickserverRow:
      description: A result row from the `Quickservers` `GET` request.
      example:
        qs_id: "19504"
        qs_name: Quickserver199
        cost: "65.00"
        qs_hostname: qs19504
        qs_status: canceled
        qs_comment: ""
      properties:
        qs_id:
          description: The id of the qs.
          example: "19504"
          type: string
        qs_name:
          description: The name of the qs.
          example: Quickserver199
          type: string
        cost:
          description: The cost of the qs.
          example: "65.00"
          type: string
        qs_hostname:
          description: The hostname of the qs.
          example: qs19504
          type: string
        qs_status:
          description: The status of the qs.
          example: canceled
          type: string
        qs_comment:
          description: The comment of the qs.
          example: ""
          type: string
      required:
      - cost
      - qs_comment
      - qs_hostname
      - qs_id
      - qs_name
      - qs_status
      title: QuickserverRow
      type: object
    QuickserverServiceExtra:
      example:
        platform: kvm
      properties:
        platform:
          description: Platform information
          example: kvm
          type: string
      type: object
    QuickserverServiceInfo:
      description: "Core service record for a QuickServer including ID, status, IP,\
        \ OS, and billing details."
      example:
        qs_id: "24355"
        qs_custid: "771282"
        qs_server: "365"
        qs_ip: ""
        qs_ipv6: qs_ipv6
        qs_vzid: qs24355
        qs_currency: USD
        qs_type: "700"
        qs_order_date: 2023-04-11T20:00:06.000Z
        qs_status: canceled
        qs_invoice: "20297531"
        qs_coupon: "0"
        qs_extra: "{\"platform\":\"kvm\"}"
        qs_hostname: qs24355
        qs_server_status: deleted
        qs_comment: ""
        qs_slices: "0"
        qs_vnc: 99.88.77.66
        qs_vnc_port: 0
        qs_rootpass: ""
        qs_mac: ""
        qs_os: ubuntu24
        qs_version: Ubuntu
        qs_location: "1"
        qs_platform: qs_platform
      properties:
        qs_id:
          description: Quickserver ID
          example: "24355"
          type: string
        qs_custid:
          description: Customer ID
          example: "771282"
          type: string
        qs_server:
          description: Server information
          example: "365"
          type: string
        qs_ip:
          description: IP address
          example: ""
          type: string
        qs_ipv6:
          description: IPv6 address (null)
          nullable: true
          type: string
        qs_vzid:
          description: VZ ID
          example: qs24355
          type: string
        qs_currency:
          description: Currency
          example: USD
          type: string
        qs_type:
          description: Type
          example: "700"
          type: string
        qs_order_date:
          description: Order date
          example: 2023-04-11T20:00:06.000Z
          type: string
        qs_status:
          description: Status
          example: canceled
          type: string
        qs_invoice:
          description: Invoice number
          example: "20297531"
          type: string
        qs_coupon:
          description: Coupon information
          example: "0"
          type: string
        qs_extra:
          description: Extra information
          example: "{\"platform\":\"kvm\"}"
          type: string
        qs_hostname:
          description: Hostname
          example: qs24355
          type: string
        qs_server_status:
          description: Server status
          example: deleted
          type: string
        qs_comment:
          description: Comment
          example: ""
          type: string
        qs_slices:
          description: Slices information
          example: "0"
          type: string
        qs_vnc:
          description: VNC information
          example: 99.88.77.66
          type: string
        qs_vnc_port:
          description: VNC port (null)
          nullable: true
          type: integer
        qs_rootpass:
          description: Root password
          example: ""
          type: string
        qs_mac:
          description: MAC address
          example: ""
          type: string
        qs_os:
          description: Operating system
          example: ubuntu24
          type: string
        qs_version:
          description: OS version
          example: Ubuntu
          type: string
        qs_location:
          description: Location
          example: "1"
          type: string
        qs_platform:
          description: Platform (null)
          nullable: true
          type: string
      type: object
    QuickserverServiceMaster:
      description: "Information about the host node running this QuickServer, including\
        \ hardware specs and resource utilization."
      example:
        qs_id: "365"
        qs_name: Qs365
        qs_ip: ""
        qs_type: "14"
        qs_hdsize: "1760"
        qs_hdfree: "1192"
        qs_bits: "64"
        qs_load: "3.45"
        qs_ram: "29550679"
        qs_cpu_model: Intel(R) Xeon(R) CPU E3-1271 v3 @ 3.60GHz
        qs_cpu_mhz: "2900"
        qs_location: "1"
        qs_available: "0"
        qs_cost: "49"
        qs_last_update: 2023-08-17T23:52:02.000Z
        qs_cores: "8"
        qs_iowait: "6.89"
        qs_raid_status: "OK: zfs:all pools are healthy"
        qs_drive_type: SSD
        qs_order: "92263"
        qs_raid_building: "0"
        qs_kernel: 5.15.0-69-generic
        qs_ioping: "330707348"
        qs_speed: "1000"
        qs_distro: Ubuntu
        qs_distro_version: "22.04"
        qs_bytes_sec_in: "0"
        qs_bytes_sec_out: "0"
        qs_packets_sec_in: "0"
        qs_packets_sec_out: "0"
        qs_last_install_time: qs_last_install_time
        qs_partitions: qs_partitions
        qs_cpu_flags: ""
      properties:
        qs_id:
          description: Quickserver ID
          example: "365"
          type: string
        qs_name:
          description: Quickserver name
          example: Qs365
          type: string
        qs_ip:
          description: IP address
          example: ""
          type: string
        qs_type:
          description: Type
          example: "14"
          type: string
        qs_hdsize:
          description: HDD size
          example: "1760"
          type: string
        qs_hdfree:
          description: Free HDD space
          example: "1192"
          type: string
        qs_bits:
          description: Bits
          example: "64"
          type: string
        qs_load:
          description: Load
          example: "3.45"
          type: string
        qs_ram:
          description: RAM information
          example: "29550679"
          type: string
        qs_cpu_model:
          description: CPU model
          example: Intel(R) Xeon(R) CPU E3-1271 v3 @ 3.60GHz
          type: string
        qs_cpu_mhz:
          description: CPU frequency
          example: "2900"
          type: string
        qs_location:
          description: Location
          example: "1"
          type: string
        qs_available:
          description: Available information
          example: "0"
          type: string
        qs_cost:
          description: Cost
          example: "49"
          type: string
        qs_last_update:
          description: Last update date
          example: 2023-08-17T23:52:02.000Z
          type: string
        qs_cores:
          description: Number of cores
          example: "8"
          type: string
        qs_iowait:
          description: I/O wait
          example: "6.89"
          type: string
        qs_raid_status:
          description: RAID status
          example: "OK: zfs:all pools are healthy"
          type: string
        qs_drive_type:
          description: Drive type
          example: SSD
          type: string
        qs_order:
          description: Order number
          example: "92263"
          type: string
        qs_raid_building:
          description: RAID building information
          example: "0"
          type: string
        qs_kernel:
          description: Kernel version
          example: 5.15.0-69-generic
          type: string
        qs_ioping:
          description: IOPing information
          example: "330707348"
          type: string
        qs_speed:
          description: Speed information
          example: "1000"
          type: string
        qs_distro:
          description: Distribution name
          example: Ubuntu
          type: string
        qs_distro_version:
          description: Distribution version
          example: "22.04"
          type: string
        qs_bytes_sec_in:
          description: Bytes/sec in
          example: "0"
          type: string
        qs_bytes_sec_out:
          description: Bytes/sec out
          example: "0"
          type: string
        qs_packets_sec_in:
          description: Packets/sec in
          example: "0"
          type: string
        qs_packets_sec_out:
          description: Packets/sec out
          example: "0"
          type: string
        qs_last_install_time:
          description: Last install time (null)
          nullable: true
          type: string
        qs_partitions:
          description: Partitions information (null)
          nullable: true
          type: string
        qs_cpu_flags:
          description: CPU flags
          example: ""
          type: string
      type: object
    ReverseDnsEntries:
      description: The Reverse DNS entries.
      example:
        ips:
          "1.2.3.4": yourhost.com
          "1.2.3.5": anotherhost.com
      properties:
        ips:
          additionalProperties: true
          description: The IPs you have access to and their current reverse dns mapping.
          type: object
      title: ReverseDnsEntries
      type: object
    ScrubIpsRowSchema:
      example:
        scrub_ip_id: 0
        repeat_invoices_cost: 6.027456183070403
        scrub_ip_ip: scrub_ip_ip
        scrub_ip_status: scrub_ip_status
        services_name: services_name
      properties:
        scrub_ip_id:
          type: integer
        repeat_invoices_cost:
          type: number
        scrub_ip_ip:
          type: string
        scrub_ip_status:
          type: string
        services_name:
          type: string
      title: Scrub Ips list
      type: object
    ScrubIpsLogRowSchema:
      example:
        date: date
        filter: filter
        blocked_ip: blocked_ip
        target_ip: target_ip
        target_port: 0.8008281904610115
        protocol: protocol
        byte_count: 6.027456183070403
        xdp_action: xdp_action
      properties:
        date:
          type: string
        filter:
          type: string
        blocked_ip:
          type: string
        target_ip:
          type: string
        target_port:
          type: number
        protocol:
          type: string
        byte_count:
          type: number
        xdp_action:
          type: string
      title: Scrub Ips logs
      type: object
    ScrubIpFilterTypes:
      description: Available scrub filter types for building firewall rules.
      example:
        success: true
        filters:
          key:
            name: name
            desc: desc
      properties:
        success:
          type: boolean
        filters:
          additionalProperties:
            $ref: "#/components/schemas/ScrubIpFilterTypes_filters_value"
          type: object
      type: object
    Server:
      example:
        ipmiAuth: true
        client_links:
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        billingDetails:
          service_last_invoice_date: service_last_invoice_date
          service_payment_status: service_payment_status
          service_frequency: service_frequency
          next_date: next_date
          service_next_invoice_date: service_next_invoice_date
          service_currency: service_currency
          service_currency_symbol: service_currency_symbol
          service_cost_info: service_cost_info
          service_extra:
          - service_extra
          - service_extra
          service_extra_json: service_extra_json
        custCurrency: custCurrency
        custCurrencySymbol: custCurrencySymbol
        package: package
        serviceExtra:
        - serviceExtra
        - serviceExtra
        locations:
          "1":
            location_id: 0
            location_name: location_name
            location_description: location_description
            location_lat: location_lat
            location_long: location_long
            location_ipmi_group: 6
        networkInfo:
          vlans: []
          vlans6: []
          assets:
            "3497":
              id: 3497
              order_id: "16058"
              hostname: myserver.host.com
              status: active
              primary_ipv4: 1.2.3.250
              primary_ipv6: ""
              mac: mac
              datacenter: "2"
              type_id: "1"
              asset_tag: ""
              rack: "68"
              row: "017"
              col: "06"
              unit_start: "37"
              unit_end: "37"
              unit_sub: "0"
              ipmi_mac: 0c:c4:7a:af:35:00
              ipmi_ip: 10.8.69.7
              ipmi_admin_username: ipmi_admin_username
              ipmi_admin_password: ipmi_admin_password
              ipmi_client_username: ipmi_client_username
              ipmi_client_password: ipmi_client_password
              ipmi_updated: ipmi_updated
              ipmi_working: "0"
              company: int
              comments: ""
              make: Supermicro
              model: SYS-6018R-TDW
              description: ""
              customer_id: int5377
              external_id: ""
              billing_status: active
              overdue: "0"
              create_timestamp: create_timestamp
              update_timestamp: update_timestamp
              asset_id: "1"
              asset_name: server
              rack_id: "68"
              rack_name: "112.16"
              rack_location: "2"
              rack_size: "44"
              rack_x: "25"
              rack_y: "5"
              comment: comment
              switchports:
              - 10414
              vlans: []
              vlans6: []
              lease:
                mac: 0c:c4:7a:af:35:00
                authenticated: false
                group: "1"
          switchports:
            "10414":
              switchport_id: 10414
              switch_id: "118"
              switch: edge1
              port: Ethernet1/33
              blade: Ethernet1
              justport: "33"
              graph_id: "12622"
              vlans: []
              vlans6: []
              asset_id: 3497
        extraInfoTables:
          assets:
            title: Assets
            size: 2
            type: table
            header:
            - Id
            - Hostname
            - Description
            - Location Name
            - Rack Name
            - Status
            - Primary Ipv4
            - Comments
            rows:
            - - "3497"
              - myserver.host.com
              - "CPU:..............2x Intel Xeon E5-2620v4 @2.10GHz\r\nMemory:...........128GB\
                \ DDR3 1600MHz\r\nHard Drive 1:.....Crucial 500 GB SSD\r\nHard Drive\
                \ 2:.....Crucial 500 GB SSD\r\nRAID:.............none\r\nOS:...............Ubuntu\
                \ 20\r\nControl Panel:....none\r\nNetwork...........10G Card\r\nIP(s):............Vlan\
                \ 3497\r\nBandwidth:........10Tb @ 10gb port"
              - TEB2
              - "112.16"
              - active
              - 1.2.3.250
              - ""
        serviceInfo:
          server_id: "16058"
          server_hostname: myserver.host.com
          server_custid: "771282"
          server_type: "600"
          server_currency: USD
          server_order_date: 2020-05-08T17:22:36.000Z
          server_invoice: "18738142"
          server_coupon: "0"
          server_status: active
          server_root: ""
          server_dedicated_tag: "0"
          server_custom_tag: ""
          server_comment: ""
          server_initial_bill: "0"
          server_hardware: "0"
          server_ips: "0"
          server_monthly_bill: "0"
          server_setup: "0"
          server_discount: server_discount
          server_rep: "0"
          server_date: "1588972956"
          server_total_cost: "230"
          server_location: server_location
          server_hardware_ordered: "0"
          server_billed: "0"
          server_welcome_email: "1"
          server_dedicated_cpu: "38"
          server_dedicated_memory: "22"
          server_dedicated_hd1: "20"
          server_dedicated_hd2: server_dedicated_hd2
          server_dedicated_bandwidth: "3"
          server_dedicated_ips: "5"
          server_dedicated_os: "30"
          server_dedicated_cp: server_dedicated_cp
          server_dedicated_raid: "0"
          server_extra: "[]"
      properties:
        ipmiAuth:
          type: boolean
        client_links:
          items:
            $ref: "#/components/schemas/ServerClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/ServerBillingDetails"
        custCurrency:
          type: string
        custCurrencySymbol:
          type: string
        package:
          type: string
        serviceExtra:
          items:
            type: string
          type: array
        locations:
          $ref: "#/components/schemas/Server_locations"
        networkInfo:
          $ref: "#/components/schemas/ServerNetworkInfo"
        extraInfoTables:
          $ref: "#/components/schemas/ServerExtraInfoTables"
        serviceInfo:
          $ref: "#/components/schemas/ServerServiceInfo"
      required:
      - billingDetails
      - client_links
      - custCurrency
      - custCurrencySymbol
      - extraInfoTables
      - ipmiAuth
      - locations
      - networkInfo
      - package
      - serviceExtra
      - serviceInfo
      type: object
    ServerAsset:
      example:
        id: 3497
        order_id: "16058"
        hostname: myserver.host.com
        status: active
        primary_ipv4: 1.2.3.250
        primary_ipv6: ""
        mac: mac
        datacenter: "2"
        type_id: "1"
        asset_tag: ""
        rack: "68"
        row: "017"
        col: "06"
        unit_start: "37"
        unit_end: "37"
        unit_sub: "0"
        ipmi_mac: 0c:c4:7a:af:35:00
        ipmi_ip: 10.8.69.7
        ipmi_admin_username: ipmi_admin_username
        ipmi_admin_password: ipmi_admin_password
        ipmi_client_username: ipmi_client_username
        ipmi_client_password: ipmi_client_password
        ipmi_updated: ipmi_updated
        ipmi_working: "0"
        company: int
        comments: ""
        make: Supermicro
        model: SYS-6018R-TDW
        description: ""
        customer_id: int5377
        external_id: ""
        billing_status: active
        overdue: "0"
        create_timestamp: create_timestamp
        update_timestamp: update_timestamp
        asset_id: "1"
        asset_name: server
        rack_id: "68"
        rack_name: "112.16"
        rack_location: "2"
        rack_size: "44"
        rack_x: "25"
        rack_y: "5"
        comment: comment
        switchports:
        - 10414
        vlans: []
        vlans6: []
        lease:
          mac: 0c:c4:7a:af:35:00
          authenticated: false
          group: "1"
      properties:
        id:
          description: Unique identifier for the asset.
          example: 3497
          type: integer
        order_id:
          description: Order identifier for the asset.
          example: "16058"
          type: string
        hostname:
          description: Hostname associated with the asset.
          example: myserver.host.com
          type: string
        status:
          description: Status of the asset.
          example: active
          type: string
        primary_ipv4:
          description: Primary IPv4 address of the asset.
          example: 1.2.3.250
          type: string
        primary_ipv6:
          description: Primary IPv6 address of the asset.
          example: ""
          type: string
        mac:
          description: MAC address associated with the asset.
          type: string
        datacenter:
          description: Datacenter identifier for the asset.
          example: "2"
          type: string
        type_id:
          description: Type identifier for the asset.
          example: "1"
          type: string
        asset_tag:
          description: Asset tag associated with the asset.
          example: ""
          type: string
        rack:
          description: Rack identifier for the asset.
          example: "68"
          type: string
        row:
          description: Row identifier for the asset.
          example: "017"
          type: string
        col:
          description: Column identifier for the asset.
          example: "06"
          type: string
        unit_start:
          description: Starting unit identifier for the asset.
          example: "37"
          type: string
        unit_end:
          description: Ending unit identifier for the asset.
          example: "37"
          type: string
        unit_sub:
          description: Subunit identifier for the asset.
          example: "0"
          type: string
        ipmi_mac:
          description: IPMI MAC address associated with the asset.
          example: 0c:c4:7a:af:35:00
          type: string
        ipmi_ip:
          description: IPMI IP address associated with the asset.
          example: 10.8.69.7
          type: string
        ipmi_admin_username:
          description: IPMI admin username associated with the asset.
          type: string
        ipmi_admin_password:
          description: IPMI admin password associated with the asset.
          type: string
        ipmi_client_username:
          description: IPMI client username associated with the asset.
          type: string
        ipmi_client_password:
          description: IPMI client password associated with the asset.
          type: string
        ipmi_updated:
          description: IPMI update status associated with the asset.
          type: string
        ipmi_working:
          description: IPMI working status associated with the asset.
          example: "0"
          type: string
        company:
          description: Company associated with the asset.
          example: int
          type: string
        comments:
          description: Comments associated with the asset.
          example: ""
          type: string
        make:
          description: Make of the asset.
          example: Supermicro
          type: string
        model:
          description: Model of the asset.
          example: SYS-6018R-TDW
          type: string
        description:
          description: Description of the asset.
          example: ""
          type: string
        customer_id:
          description: Customer identifier for the asset.
          example: int5377
          type: string
        external_id:
          description: External identifier for the asset.
          example: ""
          type: string
        billing_status:
          description: Billing status of the asset.
          example: active
          type: string
        overdue:
          description: Overdue status of the asset.
          example: "0"
          type: string
        create_timestamp:
          description: Timestamp of asset creation.
          type: string
        update_timestamp:
          description: Timestamp of asset update.
          type: string
        asset_id:
          description: Asset identifier for the asset.
          example: "1"
          type: string
        asset_name:
          description: Name of the asset.
          example: server
          type: string
        rack_id:
          description: Rack identifier for the asset.
          example: "68"
          type: string
        rack_name:
          description: Rack name associated with the asset.
          example: "112.16"
          type: string
        rack_location:
          description: Location of the rack associated with the asset.
          example: "2"
          type: string
        rack_size:
          description: Size of the rack associated with the asset.
          example: "44"
          type: string
        rack_x:
          description: X-coordinate of the asset within the rack.
          example: "25"
          type: string
        rack_y:
          description: Y-coordinate of the asset within the rack.
          example: "5"
          type: string
        comment:
          description: Comment associated with the asset.
          type: string
        switchports:
          description: List of switchports associated with the asset.
          example:
          - 10414
          items:
            type: integer
          type: array
        vlans:
          description: List of VLANs associated with the asset.
          example: []
          items:
            type: string
          type: array
        vlans6:
          description: List of IPv6 VLANs associated with the asset.
          example: []
          items:
            type: string
          type: array
        lease:
          $ref: "#/components/schemas/ServerLease"
      required:
      - asset_id
      - asset_name
      - asset_tag
      - billing_status
      - col
      - comments
      - company
      - customer_id
      - datacenter
      - description
      - external_id
      - hostname
      - id
      - ipmi_ip
      - ipmi_mac
      - ipmi_working
      - lease
      - make
      - model
      - order_id
      - overdue
      - primary_ipv4
      - primary_ipv6
      - rack
      - rack_id
      - rack_location
      - rack_name
      - rack_size
      - rack_x
      - rack_y
      - row
      - status
      - switchports
      - type_id
      - unit_end
      - unit_start
      - unit_sub
      - vlans
      - vlans6
      type: object
    ServerAssets:
      example:
        title: Assets
        size: 2
        type: table
        header:
        - Id
        - Hostname
        - Description
        - Location Name
        - Rack Name
        - Status
        - Primary Ipv4
        - Comments
        rows:
        - - "3497"
          - myserver.host.com
          - "CPU:..............2x Intel Xeon E5-2620v4 @2.10GHz\r\nMemory:...........128GB\
            \ DDR3 1600MHz\r\nHard Drive 1:.....Crucial 500 GB SSD\r\nHard Drive 2:.....Crucial\
            \ 500 GB SSD\r\nRAID:.............none\r\nOS:...............Ubuntu 20\r\
            \nControl Panel:....none\r\nNetwork...........10G Card\r\nIP(s):............Vlan\
            \ 3497\r\nBandwidth:........10Tb @ 10gb port"
          - TEB2
          - "112.16"
          - active
          - 1.2.3.250
          - ""
      properties:
        title:
          description: The title of the assets.
          example: Assets
          type: string
        size:
          description: The size of the assets.
          example: 2
          type: integer
        type:
          description: The type of the assets.
          example: table
          type: string
        header:
          description: The header of the assets table.
          items:
            type: string
          type: array
        rows:
          description: The rows of the assets table.
          items:
            items:
              type: string
            type: array
          type: array
      type: object
    ServerBillingDetails:
      description: "Billing information for a dedicated server service including payment\
        \ status, billing cycle, and cost."
      example:
        service_last_invoice_date: service_last_invoice_date
        service_payment_status: service_payment_status
        service_frequency: service_frequency
        next_date: next_date
        service_next_invoice_date: service_next_invoice_date
        service_currency: service_currency
        service_currency_symbol: service_currency_symbol
        service_cost_info: service_cost_info
        service_extra:
        - service_extra
        - service_extra
        service_extra_json: service_extra_json
      properties:
        service_last_invoice_date:
          type: string
        service_payment_status:
          type: string
        service_frequency:
          type: string
        next_date:
          type: string
        service_next_invoice_date:
          type: string
        service_currency:
          type: string
        service_currency_symbol:
          type: string
        service_cost_info:
          type: string
        service_extra:
          items:
            type: string
          type: array
        service_extra_json:
          type: string
      type: object
    ServerClientLink:
      example:
        label: label
        link: link
        icon: icon
        icon_text: icon_text
        help_text: help_text
      properties:
        label:
          type: string
        link:
          type: string
        icon:
          type: string
        icon_text:
          type: string
        help_text:
          type: string
      required:
      - label
      - link
      type: object
    ServerExtraInfoTables:
      example:
        assets:
          title: Assets
          size: 2
          type: table
          header:
          - Id
          - Hostname
          - Description
          - Location Name
          - Rack Name
          - Status
          - Primary Ipv4
          - Comments
          rows:
          - - "3497"
            - myserver.host.com
            - "CPU:..............2x Intel Xeon E5-2620v4 @2.10GHz\r\nMemory:...........128GB\
              \ DDR3 1600MHz\r\nHard Drive 1:.....Crucial 500 GB SSD\r\nHard Drive\
              \ 2:.....Crucial 500 GB SSD\r\nRAID:.............none\r\nOS:...............Ubuntu\
              \ 20\r\nControl Panel:....none\r\nNetwork...........10G Card\r\nIP(s):............Vlan\
              \ 3497\r\nBandwidth:........10Tb @ 10gb port"
            - TEB2
            - "112.16"
            - active
            - 1.2.3.250
            - ""
      properties:
        assets:
          $ref: "#/components/schemas/ServerAssets"
      required:
      - assets
      type: object
    ServerLease:
      example:
        mac: 0c:c4:7a:af:35:00
        authenticated: false
        group: "1"
      properties:
        mac:
          description: MAC address associated with the lease.
          example: 0c:c4:7a:af:35:00
          type: string
        authenticated:
          description: Indicates if the lease is authenticated.
          example: false
          type: boolean
        group:
          description: Group identifier for the lease.
          example: "1"
          type: string
      required:
      - authenticated
      - group
      - mac
      type: object
    ServerLocation1:
      example:
        location_id: 0
        location_name: location_name
        location_description: location_description
        location_lat: location_lat
        location_long: location_long
        location_ipmi_group: 6
      properties:
        location_id:
          type: integer
        location_name:
          type: string
        location_description:
          type: string
        location_lat:
          type: string
        location_long:
          type: string
        location_ipmi_group:
          type: integer
      required:
      - location_id
      - location_lat
      - location_long
      - location_name
      type: object
    ServerNetworkInfo:
      example:
        vlans: []
        vlans6: []
        assets:
          "3497":
            id: 3497
            order_id: "16058"
            hostname: myserver.host.com
            status: active
            primary_ipv4: 1.2.3.250
            primary_ipv6: ""
            mac: mac
            datacenter: "2"
            type_id: "1"
            asset_tag: ""
            rack: "68"
            row: "017"
            col: "06"
            unit_start: "37"
            unit_end: "37"
            unit_sub: "0"
            ipmi_mac: 0c:c4:7a:af:35:00
            ipmi_ip: 10.8.69.7
            ipmi_admin_username: ipmi_admin_username
            ipmi_admin_password: ipmi_admin_password
            ipmi_client_username: ipmi_client_username
            ipmi_client_password: ipmi_client_password
            ipmi_updated: ipmi_updated
            ipmi_working: "0"
            company: int
            comments: ""
            make: Supermicro
            model: SYS-6018R-TDW
            description: ""
            customer_id: int5377
            external_id: ""
            billing_status: active
            overdue: "0"
            create_timestamp: create_timestamp
            update_timestamp: update_timestamp
            asset_id: "1"
            asset_name: server
            rack_id: "68"
            rack_name: "112.16"
            rack_location: "2"
            rack_size: "44"
            rack_x: "25"
            rack_y: "5"
            comment: comment
            switchports:
            - 10414
            vlans: []
            vlans6: []
            lease:
              mac: 0c:c4:7a:af:35:00
              authenticated: false
              group: "1"
        switchports:
          "10414":
            switchport_id: 10414
            switch_id: "118"
            switch: edge1
            port: Ethernet1/33
            blade: Ethernet1
            justport: "33"
            graph_id: "12622"
            vlans: []
            vlans6: []
            asset_id: 3497
      properties:
        vlans:
          description: List of VLANs.
          example: []
          items:
            type: string
          type: array
        vlans6:
          description: List of IPv6 VLANs.
          example: []
          items:
            type: string
          type: array
        assets:
          $ref: "#/components/schemas/ServerNetworkInfo_assets"
        switchports:
          $ref: "#/components/schemas/ServerNetworkInfo_switchports"
      required:
      - assets
      - switchports
      - vlans
      - vlans6
      type: object
    ServerOrder:
      description: Object representing a server order.
      example:
        form_values:
          memory: 22
          bandwidth: "3"
          ips: "9"
          os: "5"
          cp: 5
          raid: "0"
          hd: "16"
        config_ids:
          memory: 22
          bandwidth: "3"
          ips: "9"
          os: "5"
          cp: 5
          raid: "0"
          hd: "16"
        cpu: 184
        field_label:
          bandwidth: ""
          ips: ""
          os: ""
          cp: ""
          raid: ""
          memory: ""
          hd: ""
        cpu_li:
          "254":
            id: "254"
            price: 0
            img: ryzen.png
            short_desc: AMD RYZEN 7900X
            long_desc: High core and thread count...
            location: New York
            fsb: fsb
            manu: manu
            type: AMD
            speed: "4.7"
            cache: cache
            active: "1"
            num_cores: "12"
            num_cpus: "1"
            benchmark: "121148"
            monthly_price: 75
            max_ram: "128"
            min_ram: "128"
            max_lff: "4"
            max_sff: "4"
            max_nve: "2"
            visible: "yes"
            hd_ids: hd_ids
            price_display: $0.00
            monthly_price_display: $75.00
        memory_li:
          "254":
            "65":
              id: "65"
              price: "0"
              img: hd.jpg
              short_desc: 20TB SATA
              long_desc: long_desc
              manu: manu
              size: "3000"
              type: type
              hidden: "0"
              monthly_price: 50
              drive_type: lff
              monthly_price_display: $50.00
        bandwidth_li:
          "15":
            id: "15"
            price: 0
            img: bandwidth.jpg
            short_desc: 150TB (10Gb Port)
            long_desc: ' - 2000GB Bandwidth...'
            type: Standard
            qty: "4000"
            active: "1"
            monthly_price: 15
            price_display: $0.00
            monthly_price_display: $15.00
        ips_li:
          "9":
            id: "9"
            price: 0
            img: ips.jpg
            short_desc: 1 Vlan Ip (/30)
            long_desc: 1 IP In personal Vlan
            qty: "1"
            monthly_price: 0
            price_display: $0.00
            monthly_price_display: $0.00
        os_li:
          "51":
            id: "51"
            price: 0
            img: img
            short_desc: AlmaLinux
            long_desc: long_desc
            monthly_price: 0
            active: "1"
            price_display: $0.00
            monthly_price_display: $0.00
        cp_li:
          "9":
            id: "9"
            price: 80
            img: cpanel.gif
            short_desc: cPanel ($45+)
            long_desc: long_desc
            os_type: os_type
            monthly_price: 0
            types:
            - "1"
            - "2"
            - "4"
            - "5"
            - "8"
            - "17"
            - "23"
            - "30"
            - "51"
            price_display: $80.00
            monthly_price_display: $0.00
        raid_li:
        - id: "7"
          price: 50
          img: raid.png
          short_desc: Hardware Raid 5
          long_desc: Hardware Raid 5
          monthly_price: 50
          active: "1"
          price_display: $50.00
          monthly_price_display: $50.00
        - id: "7"
          price: 50
          img: raid.png
          short_desc: Hardware Raid 5
          long_desc: Hardware Raid 5
          monthly_price: 50
          active: "1"
          price_display: $50.00
          monthly_price_display: $50.00
      properties:
        form_values:
          $ref: "#/components/schemas/ServerOrderFormValues"
        config_ids:
          $ref: "#/components/schemas/ServerOrderConfigIds"
        cpu:
          description: Number of CPUs for the server order.
          example: 184
          type: integer
        field_label:
          $ref: "#/components/schemas/ServerOrderFieldLabels"
        cpu_li:
          $ref: "#/components/schemas/ServerOrder_cpu_li"
        memory_li:
          $ref: "#/components/schemas/ServerOrder_memory_li"
        bandwidth_li:
          $ref: "#/components/schemas/ServerOrder_bandwidth_li"
        ips_li:
          $ref: "#/components/schemas/ServerOrder_ips_li"
        os_li:
          $ref: "#/components/schemas/ServerOrder_os_li"
        cp_li:
          $ref: "#/components/schemas/ServerOrder_cp_li"
        raid_li:
          description: RAID options for the server order.
          items:
            $ref: "#/components/schemas/ServerOrderRAID"
          type: array
      type: object
    ServerOrderBandwidth:
      description: A bandwidth option available when ordering a dedicated server.
      example:
        id: "15"
        price: 0
        img: bandwidth.jpg
        short_desc: 150TB (10Gb Port)
        long_desc: ' - 2000GB Bandwidth...'
        type: Standard
        qty: "4000"
        active: "1"
        monthly_price: 15
        price_display: $0.00
        monthly_price_display: $15.00
      properties:
        id:
          description: Bandwidth ID.
          example: "15"
          type: string
        price:
          description: Bandwidth price.
          example: 0
          type: integer
        img:
          description: Bandwidth image.
          example: bandwidth.jpg
          type: string
        short_desc:
          description: Short description of the bandwidth.
          example: 150TB (10Gb Port)
          type: string
        long_desc:
          description: Long description of the bandwidth.
          example: ' - 2000GB Bandwidth...'
          type: string
        type:
          description: Bandwidth type.
          example: Standard
          type: string
        qty:
          description: Quantity of bandwidth.
          example: "4000"
          type: string
        active:
          description: Active status.
          example: "1"
          type: string
        monthly_price:
          description: Monthly price.
          example: 15
          type: integer
        price_display:
          description: Display of bandwidth price.
          example: $0.00
          type: string
        monthly_price_display:
          description: Display of monthly bandwidth price.
          example: $15.00
          type: string
      type: object
    ServerOrderCPU:
      description: A CPU option available when ordering a dedicated server.
      example:
        id: "254"
        price: 0
        img: ryzen.png
        short_desc: AMD RYZEN 7900X
        long_desc: High core and thread count...
        location: New York
        fsb: fsb
        manu: manu
        type: AMD
        speed: "4.7"
        cache: cache
        active: "1"
        num_cores: "12"
        num_cpus: "1"
        benchmark: "121148"
        monthly_price: 75
        max_ram: "128"
        min_ram: "128"
        max_lff: "4"
        max_sff: "4"
        max_nve: "2"
        visible: "yes"
        hd_ids: hd_ids
        price_display: $0.00
        monthly_price_display: $75.00
      properties:
        id:
          description: CPU ID.
          example: "254"
          type: string
        price:
          description: CPU price.
          example: 0
          type: integer
        img:
          description: CPU image.
          example: ryzen.png
          type: string
        short_desc:
          description: Short description of the CPU.
          example: AMD RYZEN 7900X
          type: string
        long_desc:
          description: Long description of the CPU.
          example: High core and thread count...
          type: string
        location:
          description: Location of the CPU.
          example: New York
          type: string
        fsb:
          description: Front Side Bus information.
          type: string
        manu:
          description: Manufacturer information.
          type: string
        type:
          description: CPU type.
          example: AMD
          type: string
        speed:
          description: CPU speed.
          example: "4.7"
          type: string
        cache:
          description: Cache information.
          type: string
        active:
          description: Active status.
          example: "1"
          type: string
        num_cores:
          description: Number of cores.
          example: "12"
          type: string
        num_cpus:
          description: Number of CPUs.
          example: "1"
          type: string
        benchmark:
          description: CPU benchmark.
          example: "121148"
          type: string
        monthly_price:
          description: Monthly price.
          example: 75
          type: integer
        max_ram:
          description: Maximum RAM supported.
          example: "128"
          type: string
        min_ram:
          description: Minimum RAM required.
          example: "128"
          type: string
        max_lff:
          description: Maximum LFF (Large Form Factor) supported.
          example: "4"
          type: string
        max_sff:
          description: Maximum SFF (Small Form Factor) supported.
          example: "4"
          type: string
        max_nve:
          description: Maximum NVMe drives supported.
          example: "2"
          type: string
        visible:
          description: Visibility status.
          example: "yes"
          type: string
        hd_ids:
          description: Hard drive IDs.
          type: string
        price_display:
          description: Display of CPU price.
          example: $0.00
          type: string
        monthly_price_display:
          description: Display of monthly CPU price.
          example: $75.00
          type: string
      type: object
    ServerOrderPostRequest:
      description: Request body to place a custom dedicated server order. All ids
        come from getNewServer's config_li / regions.
      example:
        cpu: 58
        hd:
        - 16
        memory: 22
        bandwidth: 3
        ips: 9
        os: 5
        cp: 5
        raid: 0
        region: 1
        servername: server.example.com
        rootpass: ""
        tos: true
        comment: ""
      properties:
        cpu:
          description: CPU configuration id from config_li.cpu_li. Constrains valid
            hd/memory options.
          type: integer
        hd:
          $ref: "#/components/schemas/ServerOrderPostRequest_hd"
        memory:
          description: "Memory configuration id from config_li.memory_li[cpu]."
          type: integer
        bandwidth:
          description: Bandwidth configuration id from config_li.bandwidth_li.
          type: integer
        ips:
          description: IP block configuration id from config_li.ips_li.
          type: integer
        os:
          description: Operating System configuration id from config_li.os_li.
          type: integer
        cp:
          description: Control Panel configuration id from config_li.cp_li.
          type: integer
        raid:
          description: RAID configuration id from config_li.raid_li.
          type: integer
        region:
          description: Region id from the regions list.
          type: integer
        servername:
          description: Server hostname. Must pass valid_hostname().
          type: string
        rootpass:
          description: Root password for the server.
          type: string
        tos:
          description: Terms-of-service acceptance. Must be true to place the order.
          type: boolean
        comment:
          default: ""
          description: Optional free-form order comment.
          type: string
      required:
      - bandwidth
      - cp
      - cpu
      - hd
      - ips
      - memory
      - os
      - raid
      - region
      - rootpass
      - servername
      - tos
      type: object
    MailOrderRequest:
      description: Request body to validate (putMail) or place (addMail) a mail order.
        Service type ids come from getNewMail.serviceTypes.
      example:
        serviceType: 0
        coupon: ""
        comment: ""
      properties:
        serviceType:
          description: Mail plan service type id from getNewMail.serviceTypes (must
            be buyable).
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        comment:
          default: ""
          description: Free-form note saved on the service row (used on addMail).
          type: string
      required:
      - serviceType
      type: object
    FloatingIpOrderRequest:
      description: Request body to validate (putFloating_ips) or place (addFloatingIp)
        a floating IP order. Service type ids come from getNewFloatingIp.serviceTypes.
      example:
        serviceType: 0
        coupon: ""
        comment: ""
      properties:
        serviceType:
          description: Floating IP plan service type id from getNewFloatingIp.serviceTypes
            (must be buyable).
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        comment:
          default: ""
          description: Free-form note saved on the service row (used on addFloatingIp).
          type: string
      required:
      - serviceType
      type: object
    LicenseOrderRequest:
      description: Request body to validate (putLicenses) or place (addLicense) a
        license order. Package ids come from getNewLicense.serviceTypes.
      example:
        package: 0
        ip: ""
        tos: true
        frequency: 1
        coupon: ""
        comment: ""
      properties:
        package:
          description: License service type id from getNewLicense.serviceTypes (must
            be buyable).
          type: integer
        ip:
          description: IP address the license is bound to.
          type: string
        tos:
          description: Terms-of-service acceptance. Must be true to place the order.
          type: boolean
        frequency:
          default: 1
          description: Billing frequency in months.
          format: int32
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        comment:
          default: ""
          description: Free-form note saved on the service row.
          type: string
      required:
      - ip
      - package
      - tos
      type: object
    QsOrderRequest:
      description: Request body to validate (putQs) or place (addQs) a QuickServer
        order. Server and OS template come from getNewQs.
      example:
        server: 0
        password: ""
        os: ""
        tos: true
        comment: ""
      properties:
        server:
          description: QuickServer plan/server id from getNewQs.
          type: integer
        password:
          description: Root password for the QuickServer.
          type: string
        os:
          default: ""
          description: OS template file name from getNewQs (falls back to a default
            if unrecognized).
          type: string
        tos:
          description: Terms-of-service acceptance. Must be true to place the order.
          type: boolean
        comment:
          default: ""
          description: Free-form note saved on the service row.
          type: string
      required:
      - password
      - server
      - tos
      type: object
    SslOrderRequest:
      description: Request body to validate (putSsl) or place (addSsl) an SSL certificate
        order. Contact fields default from the account if omitted. Package ids come
        from getNewSsl.serviceTypes.
      example:
        ssl: 0
        hostname: example.com
        approver_email: admin@example.com
        frequency: 12
        csr_type: generated
        coupon: ""
      properties:
        ssl:
          description: SSL package service type id from getNewSsl.serviceTypes.
          type: integer
        hostname:
          description: Domain the certificate is issued for. Wildcard certs require
            *.domain.com format.
          type: string
        approver_email:
          description: Domain-control approver email (required for all SSL orders).
          type: string
        frequency:
          default: 12
          description: "Billing frequency in months (12, 24, or 36)."
          format: int32
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        csr_type:
          default: generated
          description: Whether the CSR is server-generated or customer-provided.
          enum:
          - generated
          - provided
          type: string
        csr:
          default: ""
          description: PEM-encoded CSR (>= 2048-bit) when csr_type=provided.
          type: string
        firstname:
          description: Contact first name (defaults from account).
          type: string
        lastname:
          description: Contact last name (defaults from account).
          type: string
        email:
          description: Contact email (defaults from account).
          type: string
        address:
          description: Contact address (defaults from account).
          type: string
        city:
          description: Contact city (defaults from account).
          type: string
        state:
          description: Contact state/region (defaults from account).
          type: string
        zip:
          description: Contact postal code (defaults from account).
          type: string
        country:
          description: Contact country (defaults from account).
          type: string
        phone:
          description: Contact phone (defaults from account).
          type: string
        company:
          description: Contact company/organization (defaults from account).
          type: string
        department:
          default: Administration
          description: Contact department (defaults to Administration).
          type: string
        agency:
          description: EV certificate incorporating agency (only for EV packages).
          type: string
        business_category:
          description: EV certificate business category (only for EV packages).
          type: string
      required:
      - approver_email
      - hostname
      - ssl
      type: object
    DomainOrderRequest:
      additionalProperties: true
      description: "Request body for the domain order flow: putDomains (returns required\
        \ fields), patchDomains (validates fields), addDomain (places the order).\
        \ The TLD is resolved from hostname. Per-TLD registrant/contact fields returned\
        \ by putDomains may be supplied as additional properties on patchDomains/addDomain."
      example:
        hostname: example.com
        type: register
        coupon: ""
        whois_privacy: enable
      properties:
        hostname:
          description: Fully-qualified domain to register or transfer (e.g. example.com).
          type: string
        type:
          default: register
          description: Order type.
          enum:
          - register
          - transfer
          type: string
        coupon:
          default: ""
          description: Coupon code (addDomain only).
          type: string
        whois_privacy:
          description: Set to "enable" to add Whois privacy (addDomain only).
          enum:
          - enable
          - disable
          type: string
      required:
      - hostname
      type: object
    ServerOrderConfigIds:
      description: Configuration IDs for the server order.
      example:
        memory: 22
        bandwidth: "3"
        ips: "9"
        os: "5"
        cp: 5
        raid: "0"
        hd: "16"
      properties:
        memory:
          description: Memory configuration ID for the server order.
          example: 22
          type: integer
        bandwidth:
          description: Bandwidth configuration ID for the server order.
          example: "3"
          type: string
        ips:
          description: IPs configuration ID for the server order.
          example: "9"
          type: string
        os:
          description: Operating System configuration ID for the server order.
          example: "5"
          type: string
        cp:
          description: Control Panel configuration ID for the server order.
          example: 5
          type: integer
        raid:
          description: RAID configuration ID for the server order.
          example: "0"
          type: string
        hd:
          description: Hard Drives configuration ID for the server order.
          example: "16"
          type: string
      type: object
    ServerOrderControlPanel:
      description: A control panel option available when ordering a dedicated server.
      example:
        id: "9"
        price: 80
        img: cpanel.gif
        short_desc: cPanel ($45+)
        long_desc: long_desc
        os_type: os_type
        monthly_price: 0
        types:
        - "1"
        - "2"
        - "4"
        - "5"
        - "8"
        - "17"
        - "23"
        - "30"
        - "51"
        price_display: $80.00
        monthly_price_display: $0.00
      properties:
        id:
          description: Control Panel ID.
          example: "9"
          type: string
        price:
          description: Control Panel price.
          example: 80
          type: integer
        img:
          description: Control Panel image.
          example: cpanel.gif
          type: string
        short_desc:
          description: Short description of the control panel.
          example: cPanel ($45+)
          type: string
        long_desc:
          description: Long description of the control panel.
          type: string
        os_type:
          description: OS types compatible with the control panel.
          type: string
        monthly_price:
          description: Monthly price.
          example: 0
          type: integer
        types:
          description: List of types.
          example:
          - "1"
          - "2"
          - "4"
          - "5"
          - "8"
          - "17"
          - "23"
          - "30"
          - "51"
          items:
            type: string
          type: array
        price_display:
          description: Display of control panel price.
          example: $80.00
          type: string
        monthly_price_display:
          description: Display of monthly control panel price.
          example: $0.00
          type: string
      type: object
    ServerOrderFieldLabel:
      description: A display label for a field in the server order form.
      properties:
        name:
          description: Name of the field label.
          example: Bandwidth
          type: string
        active:
          description: Active status of the field label.
          example: 1
          type: integer
      type: object
    ServerOrderFieldLabels:
      description: Field labels for the server order.
      example:
        bandwidth: ""
        ips: ""
        os: ""
        cp: ""
        raid: ""
        memory: ""
        hd: ""
      properties:
        bandwidth:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: Bandwidth field label.
        ips:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: IPs field label.
        os:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: Operating System field label.
        cp:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: Control Panel field label.
        raid:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: RAID field label.
        memory:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: Memory field label.
        hd:
          allOf:
          - $ref: "#/components/schemas/ServerOrderFieldLabel"
          description: Hard Drives field label
      type: object
    ServerOrderFormValues:
      description: Form values for the server order.
      example:
        memory: 22
        bandwidth: "3"
        ips: "9"
        os: "5"
        cp: 5
        raid: "0"
        hd: "16"
      properties:
        memory:
          description: Memory value for the server order.
          example: 22
          type: integer
        bandwidth:
          description: Bandwidth value for the server order.
          example: "3"
          type: string
        ips:
          description: IPs value for the server order.
          example: "9"
          type: string
        os:
          description: Operating System value for the server order.
          example: "5"
          type: string
        cp:
          description: Control Panel value for the server order.
          example: 5
          type: integer
        raid:
          description: RAID value for the server order.
          example: "0"
          type: string
        hd:
          description: Hard Drives value for the server order.
          example: "16"
          type: string
      type: object
    ServerOrderIP:
      description: An IP block option available when ordering a dedicated server.
      example:
        id: "9"
        price: 0
        img: ips.jpg
        short_desc: 1 Vlan Ip (/30)
        long_desc: 1 IP In personal Vlan
        qty: "1"
        monthly_price: 0
        price_display: $0.00
        monthly_price_display: $0.00
      properties:
        id:
          description: IP ID.
          example: "9"
          type: string
        price:
          description: IP price.
          example: 0
          type: integer
        img:
          description: IP image.
          example: ips.jpg
          type: string
        short_desc:
          description: Short description of the IP.
          example: 1 Vlan Ip (/30)
          type: string
        long_desc:
          description: Long description of the IP.
          example: 1 IP In personal Vlan
          type: string
        qty:
          description: Quantity of IPs.
          example: "1"
          type: string
        monthly_price:
          description: Monthly price.
          example: 0
          type: integer
        price_display:
          description: Display of IP price.
          example: $0.00
          type: string
        monthly_price_display:
          description: Display of monthly IP price.
          example: $0.00
          type: string
      type: object
    ServerOrderMemory:
      description: A memory (RAM) option available when ordering a dedicated server.
      example:
        id: "65"
        price: "0"
        img: hd.jpg
        short_desc: 20TB SATA
        long_desc: long_desc
        manu: manu
        size: "3000"
        type: type
        hidden: "0"
        monthly_price: 50
        drive_type: lff
        monthly_price_display: $50.00
      properties:
        id:
          description: Memory ID.
          example: "65"
          type: string
        price:
          description: Memory price.
          example: "0"
          type: string
        img:
          description: Memory image.
          example: hd.jpg
          type: string
        short_desc:
          description: Short description of the memory.
          example: 20TB SATA
          type: string
        long_desc:
          description: Long description of the memory.
          type: string
        manu:
          description: Manufacturer information.
          type: string
        size:
          description: Memory size.
          example: "3000"
          type: string
        type:
          description: Memory type.
          type: string
        hidden:
          description: Hidden status.
          example: "0"
          type: string
        monthly_price:
          description: Monthly price.
          example: 50
          type: integer
        drive_type:
          description: Drive type.
          example: lff
          type: string
        monthly_price_display:
          description: Display of monthly memory price.
          example: $50.00
          type: string
      type: object
    ServerOrderOS:
      description: An operating system option available when ordering a dedicated
        server.
      example:
        id: "51"
        price: 0
        img: img
        short_desc: AlmaLinux
        long_desc: long_desc
        monthly_price: 0
        active: "1"
        price_display: $0.00
        monthly_price_display: $0.00
      properties:
        id:
          description: Operating System ID.
          example: "51"
          type: string
        price:
          description: Operating System price.
          example: 0
          type: integer
        img:
          description: Operating System image.
          type: string
        short_desc:
          description: Short description of the OS.
          example: AlmaLinux
          type: string
        long_desc:
          description: Long description of the OS.
          type: string
        monthly_price:
          description: Monthly price.
          example: 0
          type: integer
        active:
          description: Active status.
          example: "1"
          type: string
        price_display:
          description: Display of OS price.
          example: $0.00
          type: string
        monthly_price_display:
          description: Display of monthly OS price.
          example: $0.00
          type: string
      type: object
    ServerOrderRAID:
      description: A RAID configuration option available when ordering a dedicated
        server.
      example:
        id: "7"
        price: 50
        img: raid.png
        short_desc: Hardware Raid 5
        long_desc: Hardware Raid 5
        monthly_price: 50
        active: "1"
        price_display: $50.00
        monthly_price_display: $50.00
      properties:
        id:
          description: RAID ID.
          example: "7"
          type: string
        price:
          description: RAID price.
          example: 50
          type: integer
        img:
          description: RAID image.
          example: raid.png
          type: string
        short_desc:
          description: Short description of the RAID.
          example: Hardware Raid 5
          type: string
        long_desc:
          description: Long description of the RAID.
          example: Hardware Raid 5
          type: string
        monthly_price:
          description: Monthly price.
          example: 50
          type: integer
        active:
          description: Active status.
          example: "1"
          type: string
        price_display:
          description: Display of RAID price.
          example: $50.00
          type: string
        monthly_price_display:
          description: Display of monthly RAID price.
          example: $50.00
          type: string
      type: object
    ServerRow:
      description: A result row from the `Servers` `GET` request.
      example:
        server_id: "8404"
        account_lid: detain@interserver.net
        server_hostname: testsignup.is.net
        server_status: deleted
      properties:
        server_id:
          description: The id of the server.
          example: "8404"
          type: string
        account_lid:
          description: The account lid of the server.
          example: detain@interserver.net
          type: string
        server_hostname:
          description: The hostname of the server.
          example: testsignup.is.net
          type: string
        server_status:
          description: The status of the server.
          example: deleted
          type: string
      required:
      - account_lid
      - server_hostname
      - server_id
      - server_status
      title: ServerRow
      type: object
    ServerServiceInfo:
      example:
        server_id: "16058"
        server_hostname: myserver.host.com
        server_custid: "771282"
        server_type: "600"
        server_currency: USD
        server_order_date: 2020-05-08T17:22:36.000Z
        server_invoice: "18738142"
        server_coupon: "0"
        server_status: active
        server_root: ""
        server_dedicated_tag: "0"
        server_custom_tag: ""
        server_comment: ""
        server_initial_bill: "0"
        server_hardware: "0"
        server_ips: "0"
        server_monthly_bill: "0"
        server_setup: "0"
        server_discount: server_discount
        server_rep: "0"
        server_date: "1588972956"
        server_total_cost: "230"
        server_location: server_location
        server_hardware_ordered: "0"
        server_billed: "0"
        server_welcome_email: "1"
        server_dedicated_cpu: "38"
        server_dedicated_memory: "22"
        server_dedicated_hd1: "20"
        server_dedicated_hd2: server_dedicated_hd2
        server_dedicated_bandwidth: "3"
        server_dedicated_ips: "5"
        server_dedicated_os: "30"
        server_dedicated_cp: server_dedicated_cp
        server_dedicated_raid: "0"
        server_extra: "[]"
      properties:
        server_id:
          description: The ID of the server.
          example: "16058"
          type: string
        server_hostname:
          description: The hostname of the server.
          example: myserver.host.com
          type: string
        server_custid:
          description: The customer ID associated with the server.
          example: "771282"
          type: string
        server_type:
          description: The type of the server.
          example: "600"
          type: string
        server_currency:
          description: The currency used for billing.
          example: USD
          type: string
        server_order_date:
          description: The date when the server was ordered.
          example: 2020-05-08T17:22:36.000Z
          type: string
        server_invoice:
          description: The invoice number for the server.
          example: "18738142"
          type: string
        server_coupon:
          description: The coupon associated with the server.
          example: "0"
          type: string
        server_status:
          description: The status of the server.
          example: active
          type: string
        server_root:
          description: The root of the server.
          example: ""
          type: string
        server_dedicated_tag:
          description: The dedicated tag of the server.
          example: "0"
          type: string
        server_custom_tag:
          description: The custom tag of the server.
          example: ""
          type: string
        server_comment:
          description: Comments related to the server.
          example: ""
          type: string
        server_initial_bill:
          description: The initial billing amount for the server.
          example: "0"
          type: string
        server_hardware:
          description: The hardware information of the server.
          example: "0"
          type: string
        server_ips:
          description: The number of IPs associated with the server.
          example: "0"
          type: string
        server_monthly_bill:
          description: The monthly billing amount for the server.
          example: "0"
          type: string
        server_setup:
          description: The setup status of the server.
          example: "0"
          type: string
        server_discount:
          description: Discount information for the server.
          type: string
        server_rep:
          description: The reputation of the server.
          example: "0"
          type: string
        server_date:
          description: The date related to the server.
          example: "1588972956"
          type: string
        server_total_cost:
          description: The total cost of the server.
          example: "230"
          type: string
        server_location:
          description: The location of the server.
          type: string
        server_hardware_ordered:
          description: The ordered hardware for the server.
          example: "0"
          type: string
        server_billed:
          description: The billed amount for the server.
          example: "0"
          type: string
        server_welcome_email:
          description: Indicates whether a welcome email was sent.
          example: "1"
          type: string
        server_dedicated_cpu:
          description: The number of dedicated CPUs for the server.
          example: "38"
          type: string
        server_dedicated_memory:
          description: The amount of dedicated memory for the server.
          example: "22"
          type: string
        server_dedicated_hd1:
          description: The size of the first dedicated hard drive.
          example: "20"
          type: string
        server_dedicated_hd2:
          description: The size of the second dedicated hard drive.
          type: string
        server_dedicated_bandwidth:
          description: The bandwidth of the server.
          example: "3"
          type: string
        server_dedicated_ips:
          description: The number of dedicated IPs for the server.
          example: "5"
          type: string
        server_dedicated_os:
          description: The operating system of the server.
          example: "30"
          type: string
        server_dedicated_cp:
          description: The control panel of the server.
          type: string
        server_dedicated_raid:
          description: The RAID configuration of the server.
          example: "0"
          type: string
        server_extra:
          description: Additional information about the server.
          example: "[]"
          type: string
      type: object
    ServerSwitchport:
      example:
        switchport_id: 10414
        switch_id: "118"
        switch: edge1
        port: Ethernet1/33
        blade: Ethernet1
        justport: "33"
        graph_id: "12622"
        vlans: []
        vlans6: []
        asset_id: 3497
      properties:
        switchport_id:
          description: Unique identifier for the switchport.
          example: 10414
          type: integer
        switch_id:
          description: Unique identifier for the switch associated with the switchport.
          example: "118"
          type: string
        switch:
          description: Name of the switch associated with the switchport.
          example: edge1
          type: string
        port:
          description: Port name on the switch.
          example: Ethernet1/33
          type: string
        blade:
          description: Blade name associated with the port.
          example: Ethernet1
          type: string
        justport:
          description: Port identifier.
          example: "33"
          type: string
        graph_id:
          description: Identifier for the graph associated with the switchport.
          example: "12622"
          type: string
        vlans:
          description: List of VLANs associated with the switchport.
          example: []
          items:
            type: string
          type: array
        vlans6:
          description: List of IPv6 VLANs associated with the switchport.
          example: []
          items:
            type: string
          type: array
        asset_id:
          description: Unique identifier of the asset associated with the switchport.
          example: 3497
          type: integer
      required:
      - asset_id
      - blade
      - graph_id
      - justport
      - port
      - switch
      - switch_id
      - switchport_id
      type: object
    Service:
      description: An individual package tied to one of our services.
      example:
        services_id: 31
        services_name: OpenVZ VPS Slice
        services_cost: 6
        services_currency: USD
        services_category: 1
        services_buyable: true
        services_type: 6
        services_field1: slice
        services_field2: ""
        services_module: vps
      properties:
        services_id:
          example: 31
          format: int32
          type: integer
        services_name:
          example: OpenVZ VPS Slice
          type: string
        services_cost:
          example: 6
          type: number
        services_currency:
          example: USD
          type: string
        services_category:
          example: 1
          format: int32
          type: integer
        services_buyable:
          example: true
          type: boolean
        services_type:
          example: 6
          format: int32
          type: integer
        services_field1:
          example: slice
          type: string
        services_field2:
          type: string
        services_module:
          example: vps
          type: string
      required:
      - services_buyable
      - services_category
      - services_cost
      - services_currency
      - services_field1
      - services_field2
      - services_id
      - services_module
      - services_name
      - services_type
      title: Service
      type: object
    ServiceCategories:
      additionalProperties:
        $ref: "#/components/schemas/ServiceCategory"
      description: The listing of the categories for the services.
      example:
        "1":
          category_id: 1
          category_name: OpenVZ Virtual Servers
          category_tag: openvz
          category_module: vps
        "2":
          category_id: 2
          category_name: KVM Virtual Servers
          category_tag: kvm
          category_module: vps
        "3":
          category_id: 3
          category_name: Xen Virtual Servers
          category_tag: xen
          category_module: vps
        "4":
          category_id: 4
          category_name: LXC Virtual Servers
          category_tag: lxc
          category_module: vps
      type: object
    ServiceCategory:
      description: A Category of Services.  This is the broadest grouping within a
        module.
      example:
        category_id: 505
        category_name: KSplice
        category_tag: ksplice
        category_module: licenses
      properties:
        category_id:
          example: 1
          type: integer
        category_name:
          example: OpenVZ Virtual Servers
          type: string
        category_tag:
          example: openvz
          type: string
        category_module:
          example: vps
          type: string
      required:
      - category_id
      - category_module
      - category_name
      - category_tag
      title: ServiceCategory
      type: object
    ServiceType:
      description: A general grouping of services within a category.
      example:
        st_id: 600
        st_name: Dedicated Server
        st_category: 600
        st_module: servers
      properties:
        st_id:
          example: 600
          type: integer
        st_name:
          example: Dedicated Server
          type: string
        st_category:
          example: 600
          type: integer
        st_module:
          example: servers
          type: string
      required:
      - st_category
      - st_id
      - st_module
      - st_name
      title: ServiceType
      type: object
    ServiceTypes:
      additionalProperties:
        $ref: "#/components/schemas/ServiceType"
      description: The listing of service types
      example:
        "1":
          st_id: 1
          st_name: KVM Windows
          st_category: 2
          st_module: vps
        "2":
          st_id: 2
          st_name: KVM Linux
          st_category: 2
          st_module: vps
        "3":
          st_id: 3
          st_name: Cloud KVM Windows
          st_category: 3
          st_module: vps
        "4":
          st_id: 4
          st_name: Cloud KVM Linux
          st_category: 3
          st_module: vps
      type: object
    Services:
      additionalProperties:
        $ref: "#/components/schemas/Service"
      description: The list of service packages.
      example:
        "31":
          services_id: 31
          services_name: OpenVZ VPS Slice
          services_cost: 6
          services_currency: USD
          services_category: 1
          services_buyable: true
          services_type: 6
          services_field1: slice
          services_field2: ""
          services_module: vps
        "32":
          services_id: 32
          services_name: KVM Windows VPS Slice
          services_cost: 10
          services_currency: USD
          services_category: 2
          services_buyable: true
          services_type: 1
          services_field1: slice
          services_field2: ""
          services_module: vps
        "33":
          services_id: 33
          services_name: KVM Linux VPS Slice
          services_cost: 6
          services_currency: USD
          services_category: 2
          services_buyable: true
          services_type: 2
          services_field1: slice
          services_field2: ""
          services_module: vps
      type: object
    ServicesInfo:
      description: "Contains the complete catalog of available modules, services,\
        \ service types, and service categories."
      example:
        modules:
          domains:
            SERVICE_ID_OFFSET: 10000
            USE_REPEAT_INVOICE: true
            USE_PACKAGES: true
            BILLING_DAYS_OFFSET: 45
            IMGNAME: domain.png
            REPEAT_BILLING_METHOD: 2
            DELETE_PENDING_DAYS: 45
            SUSPEND_DAYS: 14
            SUSPEND_WARNING_DAYS: 7
            TITLE: Domain Registrations
            MENUNAME: Domains
            EMAIL_FROM: support@interserver.net
            TBLNAME: Domains
            TABLE: domains
            TITLE_FIELD: domain_hostname
            PREFIX: domain
          vps:
            SERVICE_ID_OFFSET: 0
            USE_REPEAT_INVOICE: true
            USE_PACKAGES: true
            BILLING_DAYS_OFFSET: 0
            IMGNAME: root-server.png
            REPEAT_BILLING_METHOD: 2
            DELETE_PENDING_DAYS: 45
            SUSPEND_DAYS: 14
            SUSPEND_WARNING_DAYS: 7
            TITLE: VPS
            MENUNAME: VPS
            EMAIL_FROM: support@interserver.net
            TBLNAME: VPS
            TABLE: vps
            TITLE_FIELD: vps_hostname
            TITLE_FIELD2: vps_ip
            TITLE_FIELD3: vps_vzid
            PREFIX: vps
        services:
          "31":
            services_id: 31
            services_name: OpenVZ VPS Slice
            services_cost: 6
            services_currency: USD
            services_category: 1
            services_buyable: true
            services_type: 6
            services_field1: slice
            services_field2: ""
            services_module: vps
          "32":
            services_id: 32
            services_name: KVM Windows VPS Slice
            services_cost: 10
            services_currency: USD
            services_category: 2
            services_buyable: true
            services_type: 1
            services_field1: slice
            services_field2: ""
            services_module: vps
        serviceTypes:
          "1":
            st_id: 1
            st_name: KVM Windows
            st_category: 2
            st_module: vps
          "2":
            st_id: 2
            st_name: KVM Linux
            st_category: 2
            st_module: vps
          "3":
            st_id: 3
            st_name: Cloud KVM Windows
            st_category: 3
            st_module: vps
        serviceCategories:
          "1":
            category_id: 1
            category_name: OpenVZ Virtual Servers
            category_tag: openvz
            category_module: vps
          "2":
            category_id: 2
            category_name: KVM Virtual Servers
            category_tag: kvm
            category_module: vps
          "3":
            category_id: 3
            category_name: Xen Virtual Servers
            category_tag: xen
            category_module: vps
      properties:
        modules:
          $ref: "#/components/schemas/Modules"
        services:
          $ref: "#/components/schemas/Services"
        serviceTypes:
          $ref: "#/components/schemas/ServiceTypes"
        serviceCategories:
          $ref: "#/components/schemas/ServiceCategories"
      required:
      - modules
      - serviceCategories
      - serviceTypes
      - services
      type: object
    SuccessTextResponse:
      description: Response with success flag and text description.
      example:
        success: true
        text: Ok
      properties:
        success:
          description: Indicates whether or not the command was successful or not.
          type: boolean
        text:
          description: Text associated with the response.
          type: string
        action:
          description: Optional Action relating to the response.
          type: string
      required:
      - success
      title: SuccessTextResponse
      type: object
    TemplateRequest:
      description: VPS OS Template Request
      example:
        template: ubuntu24
        localPassword: myUserPassword
      properties:
        template:
          description: OS Template Filename
          example: ubuntu24
          type: string
        password:
          description: Password for Root / Administrator Account.
          example: myUserPassword
          format: password
          minLength: 6
          type: string
        localPassword:
          description: Password for this account.
          format: password
          type: string
      required:
      - localPassword
      - template
      title: TemplateRequest
      type: object
    TextResponse:
      description: Text Response Object
      example:
        text: You were successfull.
      properties:
        text:
          description: Response text
          example: '"You were successfull."'
          type: string
        message:
          description: Response message
          type: string
      title: TextResponse
      type: object
    Tickets:
      description: A listing of support tickets.
      example:
        ima: client
        custid: "223513"
        view: Open
        currentPage: 1
        limit: 50
        sortcol: 6
        sortdir: 1
        rowsOffset: 0
        tickets: []
        pages: 7
        rowsTotal: 311
        inboxCount: 311
        countArray:
          Open: 3
          On Hold: 3
          Closed: 305
        viewText: Inbox
      properties:
        ima:
          type: string
        custid:
          type: string
        view:
          type: string
        currentPage:
          format: int32
          type: integer
        limit:
          format: int32
          type: integer
        sortcol:
          format: int32
          type: integer
        sortdir:
          format: int32
          type: integer
        rowsOffset:
          format: int32
          type: integer
        tickets:
          items:
            $ref: "#/components/schemas/TicketsRow"
          type: array
        pages:
          format: int32
          type: integer
        rowsTotal:
          format: int32
          type: integer
        inboxCount:
          format: int32
          type: integer
        countArray:
          $ref: "#/components/schemas/Tickets_countArray"
        viewText:
          type: string
      required:
      - countArray
      - currentPage
      - custid
      - ima
      - inboxCount
      - limit
      - pages
      - rowsOffset
      - rowsTotal
      - sortcol
      - sortdir
      - tickets
      - view
      - viewText
      title: Tickets
      type: object
    TicketsRow:
      description: Information about a single ticket.
      example:
        title: fix reprocessing of messages on (re)connect
        ticketmaskid: TLT-898-78119
        lastreplier: Joe
        status: <span class="badge badge-danger">Closed</span>
        priority: Standard - 1 to 3 hour resolution
        total_replies: 2
        lastactivity: 3 years ago
        departmenttitle: Bugs
        ticketid: 704185
        can_close: "no"
        attachments: []
        status_text: Closed
        checked: false
      properties:
        title:
          type: string
        ticketmaskid:
          type: string
        lastreplier:
          type: string
        status:
          type: string
        priority:
          type: string
        total_replies:
          format: int32
          type: integer
        lastactivity:
          type: string
        departmenttitle:
          type: string
        ticketid:
          format: int32
          type: integer
        can_close:
          type: string
        attachments: {}
        status_text:
          type: string
        checked:
          type: boolean
      required:
      - attachments
      - can_close
      - checked
      - departmenttitle
      - lastactivity
      - lastreplier
      - priority
      - status
      - status_text
      - ticketid
      - ticketmaskid
      - title
      - total_replies
      title: TicketsRow
      type: object
    TimezoneUpdate:
      description: The request to update the time zone of a vps.
      example:
        timezone: America/New_York
      properties:
        timezone:
          description: The time zone
          example: America/New_York
          type: string
      required:
      - timezone
      title: TimezoneUpdate
      type: object
    UrlRequest:
      description: URL
      example:
        url: https://templates.is.cc/systemrescuecd/systemrescue-7.01-amd64.iso
      properties:
        url:
          type: string
      title: UrlRequest
      type: object
    VPSTrafficDataDataSectionResponse:
      description: VPS Traffic Data section Data subsection Row Response
      example:
      - 2023-09-17T03:30:30Z
      - 3968
      items:
        $ref: "#/components/schemas/VPSTrafficDataDataSectionResponse_inner"
      title: VPSTrafficDataDataSectionResponse
      type: array
    Vps:
      example:
        serviceInfo:
          vps_id: "85872"
          vps_custid: "223513"
          vps_server: "2439"
          vps_ip: 1.2.3.4
          vps_ipv6: vps_ipv6
          vps_vzid: vps85872
          vps_currency: USD
          vps_type: "33"
          vps_order_date: 2022-12-26T20:14:59.000Z
          vps_status: active
          vps_invoice: "20130799"
          vps_coupon: "3646"
          vps_extra: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\",\"used\"\
            :36490445,\"date\":1692095220},{\"name\":\"second\",\"used\":40894464,\"\
            date\":1692181620},{\"name\":\"first\",\"used\":54735668,\"date\":1692268020}]}"
          vps_hostname: vps85872
          vps_server_status: running
          vps_comment: my-web-2
          vps_slices: "16"
          vps_vnc: 8.7.6.5
          vps_vnc_port: "5902"
          vps_rootpass: mypassword
          vps_mac: 00:16:3e:27:59:b2
          vps_os: ubuntu24
          vps_version: ubuntu
          vps_location: "1"
          vps_platform: kvm
          vps_diskused: "0"
          vps_diskmax: "0"
        client_links:
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
        billingDetails:
          service_last_invoice_date: "July 26, 2023"
          service_payment_status: Paid
          service_frequency: Monthly
          next_date: 2023-08-26T09:41:12.000Z
          service_next_invoice_date: "August 26, 2023"
          service_currency: USD
          service_currency_symbol: $
          service_coupon: TheCouponIUsed
          service_cost_info: "0.00"
          service_extra:
            spice: 5903
            snapshots:
            - name: name
              used: 0
              date: 6
            - name: name
              used: 0
              date: 6
          service_extra_json: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\"\
            ,\"used\":36490445,\"date\":1692095220},{\"name\":\"second\",\"used\"\
            :40894464,\"date\":1692181620},{\"name\":\"first\",\"used\":54735668,\"\
            date\":1692268020}]}"
        custCurrency: custCurrency
        custCurrencySymbol: custCurrencySymbol
        serviceMaster:
          vps_id: "2439"
          vps_name: Mystaging
          vps_ip: 10.11.12.13
          vps_type: "14"
          vps_hdsize: "1856"
          vps_hdfree: "1559"
          vps_bits: "64"
          vps_load: "0.17"
          vps_ram: "263998228"
          vps_cpu_model: Intel(R) Xeon(R) CPU E5-2678 v3 @ 2.50GHz
          vps_cpu_mhz: "1198.86"
          vps_location: "1"
          vps_last_update: 2023-08-17T22:19:04.000Z
          vps_raid_building: "0"
          vps_kernel: 5.4.0-121-generic
          vps_available: "0"
          vps_cores: "48"
          vps_iowait: "0.09"
          vps_raid_status: "OK: zfs:all pools are healthy"
          vps_mounts: "udev:125:0:125:/dev,/dev/md1:91:53:38:/,securityfs:0:0:0:/sys/kernel/security,cgroup2:0:0:0:/sys/fs/cgroup/unified,pstore:0:0:0:/sys/fs/pstore,efivarfs:0:0:0:/sys/firmware/efi/efivars,systemd-1:0:0:0:/proc/sys/fs/binfmt_misc,hugetlbfs:0:0:0:/dev/hugepages"
          vps_server_max: "50"
          vps_server_max_slices: "80"
          vps_drive_type: SSD
          vps_order: "36978"
        package: package
        os_template: os_template
        serviceExtra:
          spice: 5903
          snapshots:
          - name: name
            used: 0
            date: 6
          - name: name
            used: 0
            date: 6
        extraInfoTables:
          ip_info:
            title: IP Information
            rows:
            - desc: desc
              value: value
            - desc: desc
              value: value
        cpu_graph_data: ""
        module: module
        token: token
        da_link: 1
        sr_link: 5
        cp_data:
          name: name
          cost: 5
        da_data:
          free:
            name: name
            sub_name: sub_name
            cost: 2
            img_disabled: img_disabled
            img_active: img_active
        plesk12_data:
          admin:
            id: 7
            name: name
            sub_name: sub_name
            cost: 9
          pro:
            id: 7
            name: name
            sub_name: sub_name
            cost: 9
          host:
            id: 7
            name: name
            sub_name: sub_name
            cost: 9
        serviceAddons:
          has_cpanel: true
          has_directadmin: true
          has_fantastico: true
          has_softaculous: true
          has_hdspace: true
          dedicated_ip: true
          extra_ips:
          - extra_ips
          - extra_ips
          extra_ips6:
          - extra_ips6
          - extra_ips6
          unpaid_ips:
          - unpaid_ips
          - unpaid_ips
          ips:
          - ips
          - ips
          ips6:
          - ips6
          - ips6
          cpanel_id: 3
          cost: 2
          ids:
          - ids
          - ids
          rdata:
          - rdata
          - rdata
      properties:
        serviceInfo:
          $ref: "#/components/schemas/VpsServiceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/VpsClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/VpsBillingDetails"
        custCurrency:
          type: string
        custCurrencySymbol:
          type: string
        serviceMaster:
          $ref: "#/components/schemas/VpsServiceMaster"
        package:
          type: string
        os_template:
          type: string
        serviceExtra:
          $ref: "#/components/schemas/VpsServiceExtra"
        extraInfoTables:
          $ref: "#/components/schemas/VpsExtraInfoTables"
        cpu_graph_data: {}
        module:
          type: string
        token:
          type: string
        da_link:
          type: integer
        sr_link:
          type: integer
        cp_data:
          $ref: "#/components/schemas/VpsCPData"
        da_data:
          $ref: "#/components/schemas/VpsDAData"
        plesk12_data:
          $ref: "#/components/schemas/VpsPlesk12Data"
        serviceAddons:
          $ref: "#/components/schemas/VpsServiceAddons"
      required:
      - billingDetails
      - client_links
      - cp_data
      - custCurrency
      - custCurrencySymbol
      - da_data
      - da_link
      - extraInfoTables
      - module
      - package
      - plesk12_data
      - serviceAddons
      - serviceExtra
      - serviceInfo
      - serviceMaster
      - sr_link
      - token
      type: object
    VpsBackupRow:
      description: A single backed-up item and information about it.
      example:
        type: zfs
        service: 242828
        name: third
        size: 514850816
        date: 1693909500
      properties:
        type:
          description: Backup Type
          example: zfs
          type: string
        service:
          description: The service id such as vps  id.
          example: 12343
          type: integer
        name:
          description: The name of the backup.
          example: automated_backup
          type: string
        size:
          description: Size of the file in bytes
          example: 132412343124213
          type: integer
        date:
          description: The creation date of the backup in a unix timestamp.
          example: 1693996140
          format: int32
          type: integer
      required:
      - date
      - name
      - service
      - size
      - type
      title: VpsBackupRow
      type: object
    VpsBackupRows:
      description: The listing of the backups for your service.
      example:
      - type: zfs
        service: 2722890
        name: first
        size: 3229615
        date: 1694082660
      - type: swift
        service: 100
        name: reset_shot_vps100_20210928.jpg
        size: 40621
        date: 1632842251
      - type: swift
        service: 100
        name: reset_shot_vps100_20210929.jpg
        size: 36319
        date: 1632968243
      items:
        $ref: "#/components/schemas/VpsBackupRow"
      title: VpsBackupRows
      type: array
    VpsBillingDetails:
      description: "Billing information for a VPS service including payment status,\
        \ billing cycle, and cost."
      example:
        service_last_invoice_date: "July 26, 2023"
        service_payment_status: Paid
        service_frequency: Monthly
        next_date: 2023-08-26T09:41:12.000Z
        service_next_invoice_date: "August 26, 2023"
        service_currency: USD
        service_currency_symbol: $
        service_coupon: TheCouponIUsed
        service_cost_info: "0.00"
        service_extra:
          spice: 5903
          snapshots:
          - name: name
            used: 0
            date: 6
          - name: name
            used: 0
            date: 6
        service_extra_json: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\",\"\
          used\":36490445,\"date\":1692095220},{\"name\":\"second\",\"used\":40894464,\"\
          date\":1692181620},{\"name\":\"first\",\"used\":54735668,\"date\":1692268020}]}"
      properties:
        service_last_invoice_date:
          description: Last invoice date
          example: "July 26, 2023"
          type: string
        service_payment_status:
          description: Payment status
          example: Paid
          type: string
        service_frequency:
          description: Billing frequency
          example: Monthly
          type: string
        next_date:
          description: Next billing date
          example: 2023-08-26T09:41:12.000Z
          type: string
        service_next_invoice_date:
          description: Next invoice date
          example: "August 26, 2023"
          type: string
        service_currency:
          description: Currency used for billing
          example: USD
          type: string
        service_currency_symbol:
          description: Currency symbol
          example: $
          type: string
        service_coupon:
          description: Billing coupon code
          example: TheCouponIUsed
          type: string
        service_cost_info:
          description: Cost information
          example: "0.00"
          type: string
        service_extra:
          $ref: "#/components/schemas/VpsServiceExtra"
        service_extra_json:
          description: Additional information in JSON format
          example: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\",\"used\":36490445,\"\
            date\":1692095220},{\"name\":\"second\",\"used\":40894464,\"date\":1692181620},{\"\
            name\":\"first\",\"used\":54735668,\"date\":1692268020}]}"
          type: string
      type: object
    VpsCPData:
      description: Control panel license options available for a VPS.
      example:
        name: name
        cost: 5
      properties:
        name:
          description: "Control panel name (e.g., cPanel)."
          type: string
        cost:
          description: Monthly cost in cents for the control panel license.
          type: integer
      type: object
    VpsClientLink:
      description: A navigation link for VPS-related actions in the client portal.
      example:
        label: label
        link: link
        icon: icon
        icon_text: icon_text
        help_text: help_text
      properties:
        label:
          description: Display label for the link.
          type: string
        link:
          description: URL or route for the action.
          type: string
        icon:
          description: Icon class for the link.
          type: string
        icon_text:
          description: Icon text label.
          type: string
        help_text:
          description: Help tooltip text for the link.
          type: string
      type: object
    VpsDAData:
      description: DirectAdmin license options available for a VPS.
      example:
        free:
          name: name
          sub_name: sub_name
          cost: 2
          img_disabled: img_disabled
          img_active: img_active
      properties:
        free:
          $ref: "#/components/schemas/VpsDALicense"
      type: object
    VpsDALicense:
      description: A DirectAdmin license tier option.
      example:
        name: name
        sub_name: sub_name
        cost: 2
        img_disabled: img_disabled
        img_active: img_active
      properties:
        name:
          description: License tier name.
          type: string
        sub_name:
          description: License tier sub-name.
          type: string
        cost:
          description: Monthly cost in cents.
          type: integer
        img_disabled:
          description: Image path for the disabled state icon.
          type: string
        img_active:
          description: Image path for the active state icon.
          type: string
      type: object
    VpsExtraInfoTables:
      description: Additional informational tables displayed for a VPS service.
      example:
        ip_info:
          title: IP Information
          rows:
          - desc: desc
            value: value
          - desc: desc
            value: value
      properties:
        ip_info:
          $ref: "#/components/schemas/VpsIPInfo"
      type: object
    VpsIPInfo:
      description: IP address information table for a VPS service.
      example:
        title: IP Information
        rows:
        - desc: desc
          value: value
        - desc: desc
          value: value
      properties:
        title:
          description: Title of the table
          example: IP Information
          type: string
        rows:
          items:
            $ref: "#/components/schemas/VpsIPInfoRow"
          type: array
      type: object
    VpsIPInfoRow:
      example:
        desc: desc
        value: value
      properties:
        desc:
          description: Description
          type: string
        value:
          description: Value
          type: string
      type: object
    VpsOrder:
      description: "Pricing and configuration options for ordering a new VPS, including\
        \ slice costs by platform and available OS templates."
      example:
        vpsSliceSsdOvzCost: 9
        vpsSliceOvzCost: 6
        vpsSliceSsdVirtuozzoCost: 9
        vpsSliceVirtuozzoCost: 6
        vpsSliceHypervCost: 10
        vpsSliceVmwareCost: 10
        vpsSliceLxcCost: 6
        vpsSliceXenCost: 6
        vpsSliceKvmLCost: 6
        vpsSliceKvmStorageCost: 6
        vpsNyCost: 3
        vpsSliceKvmWCost: 10
        cpanelCost: 22
        daCost: 8
        ramSlice: "2048"
        hdSlice: "30"
        hdStorageSlice: "1000"
        bwSlice: "2000"
        bwType: "2"
        bwTotal: 2
        maxSlices: "16"
        platformPackages:
          kvm: 32
          kvmstorage: 57
          hyperv: 54
        platformNames:
          kvm: KVM
          kvmstorage: KVM Storage
          hyperv: HyperV
        packageCosts:
          "57": 6
        locationStock:
          "1":
            kvm: true
            kvmstorage: true
            hyperv: true
        locationNames:
          "3": Equinix NY4
        osNames:
          opensuse: OpenSUSE
          ubuntu: Ubuntu
        templates:
          hyperv:
            windows:
              Windows2019Standard: 2019 Standard
              Windows2022: "2022"
        serviceTypes:
          "32":
            services_id: "32"
            services_name: KVM Windows VPS Slice
            services_cost: "10.00"
            services_category: "2"
            services_buyable: "1"
            services_type: "1"
            services_field1: slice
            services_field2: ""
            services_module: vps
        currency: USD
        currencySymbol: $
      properties:
        vpsSliceSsdOvzCost:
          description: Cost of VPS Slice SSD OVZ
          example: 9
          type: number
        vpsSliceOvzCost:
          description: Cost of VPS Slice OVZ
          example: 6
          type: number
        vpsSliceSsdVirtuozzoCost:
          description: Cost of VPS Slice SSD Virtuozzo
          example: 9
          type: number
        vpsSliceVirtuozzoCost:
          description: Cost of VPS Slice Virtuozzo
          example: 6
          type: number
        vpsSliceHypervCost:
          description: Cost of VPS Slice HyperV
          example: 10
          type: number
        vpsSliceVmwareCost:
          description: Cost of VPS Slice VMware
          example: 10
          type: number
        vpsSliceLxcCost:
          description: Cost of VPS Slice LXC
          example: 6
          type: number
        vpsSliceXenCost:
          description: Cost of VPS Slice Xen
          example: 6
          type: number
        vpsSliceKvmLCost:
          description: Cost of VPS Slice KVM L
          example: 6
          type: number
        vpsSliceKvmStorageCost:
          description: Cost of VPS Slice KVM Storage
          example: 6
          type: number
        vpsNyCost:
          description: Cost of VPS in NY
          example: 3
          type: number
        vpsSliceKvmWCost:
          description: Cost of VPS Slice KVM Windows
          example: 10
          type: number
        cpanelCost:
          description: Cost of cPanel
          example: 22
          type: number
        daCost:
          description: Cost of DirectAdmin (DA)
          example: 8
          type: number
        ramSlice:
          description: RAM for VPS Slice
          example: "2048"
          type: string
        hdSlice:
          description: Hard Disk for VPS Slice
          example: "30"
          type: string
        hdStorageSlice:
          description: Storage Hard Disk for VPS Slice
          example: "1000"
          type: string
        bwSlice:
          description: Bandwidth for VPS Slice
          example: "2000"
          type: string
        bwType:
          description: Bandwidth Type
          example: "2"
          type: string
        bwTotal:
          description: Total Bandwidth
          example: 2
          type: number
        maxSlices:
          description: Maximum Slices
          example: "16"
          type: string
        platformPackages:
          $ref: "#/components/schemas/VpsOrder_platformPackages"
        platformNames:
          $ref: "#/components/schemas/VpsOrder_platformNames"
        packageCosts:
          $ref: "#/components/schemas/VpsOrder_packageCosts"
        locationStock:
          $ref: "#/components/schemas/VpsOrder_locationStock"
        locationNames:
          $ref: "#/components/schemas/VpsOrder_locationNames"
        osNames:
          $ref: "#/components/schemas/VpsOrder_osNames"
        templates:
          $ref: "#/components/schemas/VpsOrder_templates"
        serviceTypes:
          $ref: "#/components/schemas/VpsOrder_serviceTypes"
        currency:
          description: Currency
          example: USD
          type: string
        currencySymbol:
          description: Currency Symbol
          example: $
          type: string
      required:
      - bwSlice
      - bwTotal
      - bwType
      - cpanelCost
      - currency
      - currencySymbol
      - daCost
      - hdSlice
      - hdStorageSlice
      - locationNames
      - locationStock
      - maxSlices
      - osNames
      - packageCosts
      - platformNames
      - platformPackages
      - ramSlice
      - serviceTypes
      - templates
      - vpsNyCost
      - vpsSliceHypervCost
      - vpsSliceKvmLCost
      - vpsSliceKvmStorageCost
      - vpsSliceKvmWCost
      - vpsSliceLxcCost
      - vpsSliceOvzCost
      - vpsSliceSsdOvzCost
      - vpsSliceSsdVirtuozzoCost
      - vpsSliceVirtuozzoCost
      - vpsSliceVmwareCost
      - vpsSliceXenCost
      type: object
    VpsPlesk12Data:
      example:
        admin:
          id: 7
          name: name
          sub_name: sub_name
          cost: 9
        pro:
          id: 7
          name: name
          sub_name: sub_name
          cost: 9
        host:
          id: 7
          name: name
          sub_name: sub_name
          cost: 9
      properties:
        admin:
          $ref: "#/components/schemas/VpsPleskLicense"
        pro:
          $ref: "#/components/schemas/VpsPleskLicense"
        host:
          $ref: "#/components/schemas/VpsPleskLicense"
      type: object
    VpsPleskLicense:
      example:
        id: 7
        name: name
        sub_name: sub_name
        cost: 9
      properties:
        id:
          type: integer
        name:
          type: string
        sub_name:
          type: string
        cost:
          type: integer
      type: object
    VpsRow:
      description: A result row from the `Vps` `GET` request.
      example:
        vps_id: "100"
        vps_name: Builder
        repeat_invoices_cost: "10.00"
        vps_hostname: vps100
        vps_ip: 64.20.46.220
        vps_status: active
        services_name: KVM Windows VPS Slice
        vps_comment: ""
      properties:
        vps_id:
          description: The id of the vps.
          example: "100"
          type: string
        vps_name:
          description: The name of the vps.
          example: Builder
          type: string
        repeat_invoices_cost:
          description: The repeat invoices cost of the vps.
          example: "10.00"
          type: string
        vps_hostname:
          description: The hostname of the vps.
          example: vps100
          type: string
        vps_ip:
          description: The ip of the vps.
          example: 64.20.46.220
          type: string
        vps_status:
          description: The status of the vps.
          example: active
          type: string
        services_name:
          description: The services name of the vps.
          example: KVM Windows VPS Slice
          type: string
        vps_comment:
          description: The comment of the vps.
          example: ""
          type: string
      required:
      - repeat_invoices_cost
      - services_name
      - vps_comment
      - vps_hostname
      - vps_id
      - vps_ip
      - vps_name
      - vps_status
      title: VpsRow
      type: object
    VpsServiceAddons:
      description: "Add-on services and IP address information for a VPS, including\
        \ control panel licenses and extra IP assignments."
      example:
        has_cpanel: true
        has_directadmin: true
        has_fantastico: true
        has_softaculous: true
        has_hdspace: true
        dedicated_ip: true
        extra_ips:
        - extra_ips
        - extra_ips
        extra_ips6:
        - extra_ips6
        - extra_ips6
        unpaid_ips:
        - unpaid_ips
        - unpaid_ips
        ips:
        - ips
        - ips
        ips6:
        - ips6
        - ips6
        cpanel_id: 3
        cost: 2
        ids:
        - ids
        - ids
        rdata:
        - rdata
        - rdata
      properties:
        has_cpanel:
          description: Whether a cPanel license is active on this VPS.
          type: boolean
        has_directadmin:
          description: Whether a DirectAdmin license is active on this VPS.
          type: boolean
        has_fantastico:
          description: Whether a Fantastico license is active on this VPS.
          type: boolean
        has_softaculous:
          description: Whether a Softaculous license is active on this VPS.
          type: boolean
        has_hdspace:
          description: Whether extra disk space has been added to this VPS.
          type: boolean
        dedicated_ip:
          description: Whether a dedicated IP address is assigned to this VPS.
          type: boolean
        extra_ips:
          description: List of additional IPv4 addresses assigned to this VPS.
          items:
            type: string
          type: array
        extra_ips6:
          description: List of additional IPv6 addresses assigned to this VPS.
          items:
            type: string
          type: array
        unpaid_ips:
          description: List of IP addresses that have unpaid charges.
          items:
            type: string
          type: array
        ips:
          description: All IPv4 addresses assigned to this VPS.
          items:
            type: string
          type: array
        ips6:
          description: All IPv6 addresses assigned to this VPS.
          items:
            type: string
          type: array
        cpanel_id:
          description: The add-on service ID for the cPanel license.
          type: integer
        cost:
          description: Total monthly add-on cost in cents.
          type: integer
        ids:
          description: List of add-on service IDs active on this VPS.
          items:
            type: string
          type: array
        rdata:
          description: Raw add-on data entries.
          items:
            type: string
          type: array
      type: object
    VpsServiceExtra:
      example:
        spice: 5903
        snapshots:
        - name: name
          used: 0
          date: 6
        - name: name
          used: 0
          date: 6
      properties:
        spice:
          description: Spice
          example: 5903
          type: integer
        snapshots:
          items:
            $ref: "#/components/schemas/VpsSnapshot"
          type: array
      type: object
    VpsServiceInfo:
      example:
        vps_id: "85872"
        vps_custid: "223513"
        vps_server: "2439"
        vps_ip: 1.2.3.4
        vps_ipv6: vps_ipv6
        vps_vzid: vps85872
        vps_currency: USD
        vps_type: "33"
        vps_order_date: 2022-12-26T20:14:59.000Z
        vps_status: active
        vps_invoice: "20130799"
        vps_coupon: "3646"
        vps_extra: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\",\"used\":36490445,\"\
          date\":1692095220},{\"name\":\"second\",\"used\":40894464,\"date\":1692181620},{\"\
          name\":\"first\",\"used\":54735668,\"date\":1692268020}]}"
        vps_hostname: vps85872
        vps_server_status: running
        vps_comment: my-web-2
        vps_slices: "16"
        vps_vnc: 8.7.6.5
        vps_vnc_port: "5902"
        vps_rootpass: mypassword
        vps_mac: 00:16:3e:27:59:b2
        vps_os: ubuntu24
        vps_version: ubuntu
        vps_location: "1"
        vps_platform: kvm
        vps_diskused: "0"
        vps_diskmax: "0"
      properties:
        vps_id:
          description: VPS ID
          example: "85872"
          type: string
        vps_custid:
          description: Customer ID
          example: "223513"
          type: string
        vps_server:
          description: Server ID
          example: "2439"
          type: string
        vps_ip:
          description: IP address of the VPS
          example: 1.2.3.4
          type: string
        vps_ipv6:
          description: IPv6 address of the VPS
          type: string
        vps_vzid:
          description: VPS Virtuozzo ID
          example: vps85872
          type: string
        vps_currency:
          description: Currency used for billing
          example: USD
          type: string
        vps_type:
          description: VPS type
          example: "33"
          type: string
        vps_order_date:
          description: Date of VPS order
          example: 2022-12-26T20:14:59.000Z
          type: string
        vps_status:
          description: VPS status
          example: active
          type: string
        vps_invoice:
          description: VPS invoice number
          example: "20130799"
          type: string
        vps_coupon:
          description: VPS coupon code
          example: "3646"
          type: string
        vps_extra:
          description: Additional information about the VPS
          example: "{\"spice\":5903,\"snapshots\":[{\"name\":\"third\",\"used\":36490445,\"\
            date\":1692095220},{\"name\":\"second\",\"used\":40894464,\"date\":1692181620},{\"\
            name\":\"first\",\"used\":54735668,\"date\":1692268020}]}"
          type: string
        vps_hostname:
          description: VPS hostname
          example: vps85872
          type: string
        vps_server_status:
          description: Status of the VPS server
          example: running
          type: string
        vps_comment:
          description: Comment associated with the VPS
          example: my-web-2
          type: string
        vps_slices:
          description: Number of VPS slices
          example: "16"
          type: string
        vps_vnc:
          description: VNC address
          example: 8.7.6.5
          type: string
        vps_vnc_port:
          description: VNC port
          example: "5902"
          type: string
        vps_rootpass:
          description: Root password of the VPS
          example: mypassword
          type: string
        vps_mac:
          description: MAC address of the VPS
          example: 00:16:3e:27:59:b2
          type: string
        vps_os:
          description: Operating system of the VPS
          example: ubuntu24
          type: string
        vps_version:
          description: Version of the operating system
          example: ubuntu
          type: string
        vps_location:
          description: Location of the VPS
          example: "1"
          type: string
        vps_platform:
          description: Virtualization platform
          example: kvm
          type: string
        vps_diskused:
          description: Amount of disk space used
          example: "0"
          type: string
        vps_diskmax:
          description: Maximum disk space available
          example: "0"
          type: string
      type: object
    VpsServiceMaster:
      description: "Information about the host node (hypervisor) running this VPS,\
        \ including hardware specs and resource utilization."
      example:
        vps_id: "2439"
        vps_name: Mystaging
        vps_ip: 10.11.12.13
        vps_type: "14"
        vps_hdsize: "1856"
        vps_hdfree: "1559"
        vps_bits: "64"
        vps_load: "0.17"
        vps_ram: "263998228"
        vps_cpu_model: Intel(R) Xeon(R) CPU E5-2678 v3 @ 2.50GHz
        vps_cpu_mhz: "1198.86"
        vps_location: "1"
        vps_last_update: 2023-08-17T22:19:04.000Z
        vps_raid_building: "0"
        vps_kernel: 5.4.0-121-generic
        vps_available: "0"
        vps_cores: "48"
        vps_iowait: "0.09"
        vps_raid_status: "OK: zfs:all pools are healthy"
        vps_mounts: "udev:125:0:125:/dev,/dev/md1:91:53:38:/,securityfs:0:0:0:/sys/kernel/security,cgroup2:0:0:0:/sys/fs/cgroup/unified,pstore:0:0:0:/sys/fs/pstore,efivarfs:0:0:0:/sys/firmware/efi/efivars,systemd-1:0:0:0:/proc/sys/fs/binfmt_misc,hugetlbfs:0:0:0:/dev/hugepages"
        vps_server_max: "50"
        vps_server_max_slices: "80"
        vps_drive_type: SSD
        vps_order: "36978"
      properties:
        vps_id:
          description: VPS ID
          example: "2439"
          type: string
        vps_name:
          description: VPS name
          example: Mystaging
          type: string
        vps_ip:
          description: IP address of the VPS
          example: 10.11.12.13
          type: string
        vps_type:
          description: VPS type
          example: "14"
          type: string
        vps_hdsize:
          description: Hard drive size
          example: "1856"
          type: string
        vps_hdfree:
          description: Free hard drive space
          example: "1559"
          type: string
        vps_bits:
          description: Bits
          example: "64"
          type: string
        vps_load:
          description: CPU load
          example: "0.17"
          type: string
        vps_ram:
          description: RAM
          example: "263998228"
          type: string
        vps_cpu_model:
          description: CPU model
          example: Intel(R) Xeon(R) CPU E5-2678 v3 @ 2.50GHz
          type: string
        vps_cpu_mhz:
          description: CPU frequency in MHz
          example: "1198.86"
          type: string
        vps_location:
          description: Location of the VPS
          example: "1"
          type: string
        vps_last_update:
          description: Last update date
          example: 2023-08-17T22:19:04.000Z
          type: string
        vps_raid_building:
          description: RAID building status
          example: "0"
          type: string
        vps_kernel:
          description: Kernel version
          example: 5.4.0-121-generic
          type: string
        vps_available:
          description: Available
          example: "0"
          type: string
        vps_cores:
          description: Number of CPU cores
          example: "48"
          type: string
        vps_iowait:
          description: I/O wait
          example: "0.09"
          type: string
        vps_raid_status:
          description: RAID status
          example: "OK: zfs:all pools are healthy"
          type: string
        vps_mounts:
          description: Mounts
          example: "udev:125:0:125:/dev,/dev/md1:91:53:38:/,securityfs:0:0:0:/sys/kernel/security,cgroup2:0:0:0:/sys/fs/cgroup/unified,pstore:0:0:0:/sys/fs/pstore,efivarfs:0:0:0:/sys/firmware/efi/efivars,systemd-1:0:0:0:/proc/sys/fs/binfmt_misc,hugetlbfs:0:0:0:/dev/hugepages"
          type: string
        vps_server_max:
          description: Maximum number of servers
          example: "50"
          type: string
        vps_server_max_slices:
          description: Maximum number of server slices
          example: "80"
          type: string
        vps_drive_type:
          description: Drive type
          example: SSD
          type: string
        vps_order:
          description: Order number
          example: "36978"
          type: string
      type: object
    VpsSnapshot:
      description: A VPS snapshot (point-in-time backup) stored on the host node.
      example:
        name: name
        used: 0
        date: 6
      properties:
        name:
          description: Snapshot name.
          type: string
        used:
          description: Disk space used by this snapshot in bytes.
          type: integer
        date:
          description: Unix timestamp of when the snapshot was created.
          type: integer
      type: object
    VpsTemplateRow:
      description: A VPS OS Template.
      example:
        template_id: "1906"
        template_type: "14"
        template_os: ubuntu
        template_version: "22.04"
        template_bits: "64"
        template_file: ubuntu-22.04
        template_available: "1"
        template_name: Ubuntu
        template_dir: ""
      properties:
        template_id:
          type: string
        template_type:
          type: string
        template_os:
          type: string
        template_version:
          type: string
        template_bits:
          type: string
        template_file:
          type: string
        template_available:
          type: string
        template_name:
          type: string
        template_dir:
          type: string
      title: VpsTemplateRow
      type: object
    VpsTemplatesList:
      description: A listing of the OS Templates available for use.
      example:
        templates:
        - template_id: "1906"
          template_type: "14"
          template_os: ubuntu
          template_version: "22.04"
          template_bits: "64"
          template_file: ubuntu-22.04
          template_available: "1"
          template_name: Ubuntu
          template_dir: ""
        - template_id: "1906"
          template_type: "14"
          template_os: ubuntu
          template_version: "22.04"
          template_bits: "64"
          template_file: ubuntu-22.04
          template_available: "1"
          template_name: Ubuntu
          template_dir: ""
      properties:
        templates:
          description: A listing of the templates.
          items:
            $ref: "#/components/schemas/VpsTemplateRow"
          type: array
      required:
      - templates
      type: object
    VpsTrafficDataSectionResponse:
      description: VPS Traffic Data Section Response
      example:
        name: 74.50.91.46 In
        data:
        - - 2023-09-17T03:30:30Z
          - 3968
        - - 2023-09-17T03:31:30Z
          - 840
        - - 2023-09-17T03:32:31Z
          - 632
        - - 2023-09-17T03:33:31Z
          - 1232
        - - 2023-09-17T03:34:31Z
          - 2960
        - - 2023-09-17T07:50:12Z
          - 5504
      properties:
        name:
          type: string
        data:
          items:
            $ref: "#/components/schemas/VPSTrafficDataDataSectionResponse"
          type: array
      required:
      - data
      - name
      title: VpsTrafficDataSectionResponse
      type: object
    VpsTrafficHistoryResponse:
      description: VPS Traffic Data History Section
      example:
        hour:
          data:
          - - 2023-09-11T12:00:00Z
            - 834424
          - - 2023-09-11T23:00:00Z
            - 8247944
          - - 2023-09-14T05:00:00Z
            - 19763064
          - - 2023-09-14T06:00:00Z
            - 18892320
          - - 2023-09-17T07:00:00Z
            - 16546448
          times:
          - 2023-09-12T02:00:00Z
          - 2023-09-17T05:00:00Z
          - 2023-09-17T06:00:00Z
          - 2023-09-17T07:00:00Z
        day:
          data:
          - - 2023-09-12T00:00:00Z
            - 1455497448
          - - 2023-09-13T00:00:00Z
            - 182943992
          - - 2023-09-14T00:00:00Z
            - 290416056
          - - 2023-09-15T00:00:00Z
            - 440595840
          - - 2023-09-16T00:00:00Z
            - 411255120
          - - 2023-09-17T00:00:00Z
            - 346449520
          times:
          - 2023-09-12T00:00:00Z
          - 2023-09-13T00:00:00Z
          - 2023-09-14T00:00:00Z
          - 2023-09-15T00:00:00Z
          - 2023-09-16T00:00:00Z
          - 2023-09-17T00:00:00Z
      properties:
        hour:
          $ref: "#/components/schemas/VpsTrafficHistorySectionResponse"
        day:
          $ref: "#/components/schemas/VpsTrafficHistorySectionResponse"
      required:
      - day
      - hour
      title: VpsTrafficHistoryResponse
      type: object
    VpsTrafficHistorySectionDataResponse:
      description: VPS Traffic Hisotrty Data Row of Hour/Day Section
      items:
        $ref: "#/components/schemas/VPSTrafficDataDataSectionResponse_inner"
      type: array
    VpsTrafficHistorySectionResponse:
      description: VPS Traffic History Hour and Day Sections
      example:
        data:
        - - 2023-09-11T12:00:00Z
          - 834424
        - - 2023-09-11T23:00:00Z
          - 8247944
        - - 2023-09-14T05:00:00Z
          - 19763064
        - - 2023-09-14T06:00:00Z
          - 18892320
        - - 2023-09-17T07:00:00Z
          - 16546448
        times:
        - 2023-09-12T02:00:00Z
        - 2023-09-17T05:00:00Z
        - 2023-09-17T06:00:00Z
        - 2023-09-17T07:00:00Z
      properties:
        data:
          items:
            $ref: "#/components/schemas/VpsTrafficHistorySectionDataResponse"
          type: array
        times:
          items:
            format: date-time
            type: string
          type: array
      required:
      - data
      - times
      title: VpsTrafficHistorySectionResponse
      type: object
    VpsTrafficResponse:
      description: VPS Traffic Information
      example:
        name: vps2725349
        target: "2725349"
        interval: 30
        history:
          hour:
            data:
            - - 2023-09-11T12:00:00Z
              - 834424
            - - 2023-09-11T23:00:00Z
              - 8247944
            - - 2023-09-14T05:00:00Z
              - 19763064
            - - 2023-09-14T06:00:00Z
              - 18892320
            - - 2023-09-17T07:00:00Z
              - 16546448
            times:
            - 2023-09-12T02:00:00Z
            - 2023-09-17T05:00:00Z
            - 2023-09-17T06:00:00Z
            - 2023-09-17T07:00:00Z
          day:
            data:
            - - 2023-09-12T00:00:00Z
              - 1455497448
            - - 2023-09-13T00:00:00Z
              - 182943992
            - - 2023-09-14T00:00:00Z
              - 290416056
            - - 2023-09-15T00:00:00Z
              - 440595840
            - - 2023-09-16T00:00:00Z
              - 411255120
            - - 2023-09-17T00:00:00Z
              - 346449520
            times:
            - 2023-09-12T00:00:00Z
            - 2023-09-13T00:00:00Z
            - 2023-09-14T00:00:00Z
            - 2023-09-15T00:00:00Z
            - 2023-09-16T00:00:00Z
            - 2023-09-17T00:00:00Z
        last: 2023-09-17T07:50:12Z
        times:
        - 2023-09-17T03:30:30Z
        - 2023-09-17T03:31:30Z
        - 2023-09-17T03:32:31Z
        - 2023-09-17T03:33:31Z
        - 2023-09-17T04:12:30Z
        - 2023-09-17T04:13:31Z
        - 2023-09-17T04:14:30Z
        - 2023-09-17T04:15:30Z
        - 2023-09-17T04:16:31Z
        - 2023-09-17T04:17:30Z
        - 2023-09-17T04:18:31Z
        - 2023-09-17T04:19:30Z
        - 2023-09-17T04:20:30Z
        - 2023-09-17T04:21:30Z
        - 2023-09-17T04:22:31Z
        - 2023-09-17T07:50:12Z
        totals:
          day:
            in: 49719744
            out: 11316104
          month:
            in: 2880512936
            out: 380333384
          year:
            in: 2880512936
            out: 380333384
          all:
            in: 2880512936
            out: 380333384
        usage:
          current:
            in: 5504
            out: 1248
          peak:
            in: 9176
            out: 3600
          average:
            in:
              total: 949688
              count: 261
              value: 3639
            out:
              total: 200752
              count: 261
              value: 770
        data:
        - name: 74.50.91.46 In
          data:
          - - 2023-09-17T03:30:30Z
            - 3968
          - - 2023-09-17T03:31:30Z
            - 840
          - - 2023-09-17T03:32:31Z
            - 632
          - - 2023-09-17T03:33:31Z
            - 1232
          - - 2023-09-17T03:34:31Z
            - 2960
          - - 2023-09-17T07:50:12Z
            - 5504
        - name: 74.50.91.46 Out
          data:
          - - 2023-09-17T03:30:30Z
            - 456
          - - 2023-09-17T03:31:30Z
            - 472
          - - 2023-09-17T03:48:30Z
            - 592
      properties:
        name:
          type: string
        target:
          type: string
        interval:
          format: int32
          type: integer
        history:
          $ref: "#/components/schemas/VpsTrafficHistoryResponse"
        last:
          format: date-time
          type: string
        times:
          items:
            format: date-time
            type: string
          type: array
        totals:
          $ref: "#/components/schemas/VpsTrafficTotalsResposne"
        usage:
          $ref: "#/components/schemas/VpsTrafficUsageResponse"
        data:
          items:
            $ref: "#/components/schemas/VpsTrafficDataSectionResponse"
          type: array
      required:
      - data
      - history
      - interval
      - last
      - name
      - target
      - times
      - totals
      - usage
      title: VpsTrafficResponse
      type: object
    VpsTrafficTotalsResposne:
      description: VPS Traffic Totals Section
      example:
        day:
          in: 49719744
          out: 11316104
        month:
          in: 2880512936
          out: 380333384
        year:
          in: 2880512936
          out: 380333384
        all:
          in: 2880512936
          out: 380333384
      properties:
        day:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
        month:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
        year:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
        all:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
      required:
      - all
      - day
      - month
      - year
      title: VpsTrafficTotalsResposne
      type: object
    VpsTrafficTotalsSectionResponse:
      description: VPS Traffic Totals Secttions Data
      example:
        in: 49719744
        out: 11316104
      properties:
        in:
          format: int64
          type: integer
        out:
          format: int64
          type: integer
      required:
      - in
      - out
      title: VpsTrafficTotalsSectionResponse
      type: object
    VpsTrafficUsageAverageResponse:
      description: VPS Traffic Usage Average Section
      example:
        in:
          total: 949688
          count: 261
          value: 3639
        out:
          total: 200752
          count: 261
          value: 770
      properties:
        in:
          $ref: "#/components/schemas/VpsTrafficUsageAverageSectionResponse"
        out:
          $ref: "#/components/schemas/VpsTrafficUsageAverageSectionResponse"
      required:
      - in
      - out
      title: VpsTrafficUsageAverageResponse
      type: object
    VpsTrafficUsageAverageSectionResponse:
      description: VPS Traffic Usage Average Section Response
      example:
        total: 949688
        count: 261
        value: 3639
      properties:
        total:
          format: int32
          type: integer
        count:
          format: int32
          type: integer
        value:
          format: int32
          type: integer
      required:
      - count
      - total
      - value
      title: VpsTrafficUsageAverageSectionResponse
      type: object
    VpsTrafficUsageResponse:
      description: VPS Traffic Usage Section
      example:
        current:
          in: 5504
          out: 1248
        peak:
          in: 9176
          out: 3600
        average:
          in:
            total: 949688
            count: 261
            value: 3639
          out:
            total: 200752
            count: 261
            value: 770
      properties:
        current:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
        peak:
          $ref: "#/components/schemas/VpsTrafficTotalsSectionResponse"
        average:
          $ref: "#/components/schemas/VpsTrafficUsageAverageResponse"
      required:
      - average
      - current
      - peak
      title: VpsTrafficUsageResponse
      type: object
    Website:
      description: "Full detail view of a webhosting service including billing, service\
        \ configuration, and panel access links."
      example:
        serviceInfo:
          website_id: "1196829"
          website_server: "543"
          website_type: "11363"
          website_currency: USD
          website_order_date: 2023-03-16T22:51:54.000Z
          website_custid: "85872"
          website_ip: 74.50.80.15
          website_status: active
          website_invoice: "20261994"
          website_coupon: "1690"
          website_extra: "[]"
          website_hostname: vintagevultures.com
          website_comment: website_comment
          website_username: vintagev
          website_server_status: website_server_status
        client_links:
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
          other_attr: other_attr
        - label: label
          link: link
          icon: icon
          icon_text: icon_text
          help_text: help_text
          other_attr: other_attr
        billingDetails:
          service_last_invoice_date: service_last_invoice_date
          service_payment_status: service_payment_status
          service_frequency: service_frequency
          next_date: next_date
          service_next_invoice_date: service_next_invoice_date
          service_currency: USD
          service_currency_symbol: $
          service_coupon: service_coupon
          service_cost_info: service_cost_info
          service_extra: "{}"
          service_extra_json: service_extra_json
        custCurrency: USD
        custCurrencySymbol: $
        serviceMaster:
          website_id: "543"
          website_name: vda4200.is.cc
          website_ip: 74.50.80.15
          website_type: "206"
          website_available: "0"
          website_hdsize: "2062"
          website_hdfree: "196"
          website_load: "6.55"
          website_last_update: 2023-08-17T23:01:02.000Z
          website_max_sites: "300"
          website_order: "58984"
          website_partitions: website_partitions
          website_dns1: vda4200a.trouble-free.net
          website_dns2: vda4200b.trouble-free.net
        package: Web Hosting Direct Admin
        serviceExtra:
        - null
        - null
        extraInfoTables:
          links:
            title: title
            rows:
            - desc: desc
              value: value
            - desc: desc
              value: value
          preview:
            title: title
            rows:
            - desc: desc
              value: value
            - desc: desc
              value: value
          dns:
            title: title
            rows:
            - desc: desc
              value: value
            - desc: desc
              value: value
      properties:
        serviceInfo:
          $ref: "#/components/schemas/WebsiteServiceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/WebsiteClientLink"
          type: array
        billingDetails:
          $ref: "#/components/schemas/WebsiteBillingDetails"
        custCurrency:
          description: Customer's currency
          example: USD
          type: string
        custCurrencySymbol:
          description: Customer currency symbol
          example: $
          type: string
        serviceMaster:
          $ref: "#/components/schemas/WebsiteServiceMaster"
        package:
          description: Package information
          example: Web Hosting Direct Admin
          type: string
        serviceExtra:
          items:
            $ref: "#/components/schemas/WebsiteServiceExtra"
          type: array
        extraInfoTables:
          $ref: "#/components/schemas/WebsiteExtraInfoTables"
      type: object
    WebsiteBillingDetails:
      description: "Billing information for a webhosting service including payment\
        \ status, billing cycle, and cost."
      example:
        service_last_invoice_date: service_last_invoice_date
        service_payment_status: service_payment_status
        service_frequency: service_frequency
        next_date: next_date
        service_next_invoice_date: service_next_invoice_date
        service_currency: USD
        service_currency_symbol: $
        service_coupon: service_coupon
        service_cost_info: service_cost_info
        service_extra: "{}"
        service_extra_json: service_extra_json
      properties:
        service_last_invoice_date:
          description: Last invoice date for the service
          type: string
        service_payment_status:
          description: Payment status for the service
          type: string
        service_frequency:
          description: Frequency of the service
          type: string
        next_date:
          description: Next date for the service
          type: string
        service_next_invoice_date:
          description: Next invoice date for the service
          type: string
        service_currency:
          description: Currency for the service
          example: USD
          type: string
        service_currency_symbol:
          description: Currency symbol for the service
          example: $
          type: string
        service_coupon:
          description: Coupon for the service
          type: string
        service_cost_info:
          description: Cost information for the service
          type: string
        service_extra:
          description: Extra information for the service
          type: object
        service_extra_json:
          description: Extra information in JSON format for the service
          type: string
      type: object
    WebsiteClientLink:
      description: A navigation link for webhosting-related actions in the client
        portal.
      example:
        label: label
        link: link
        icon: icon
        icon_text: icon_text
        help_text: help_text
        other_attr: other_attr
      properties:
        label:
          description: Label for the link
          type: string
        link:
          description: Link URL
          type: string
        icon:
          description: Icon for the link
          type: string
        icon_text:
          description: Icon text for the link
          type: string
        help_text:
          description: Help text for the link
          type: string
        other_attr:
          description: Other attributes for the link
          type: string
      type: object
    WebsiteExtraInfoTables:
      description: "Supplementary information tables displayed for a webhosting service\
        \ (links, DNS, preview)."
      example:
        links:
          title: title
          rows:
          - desc: desc
            value: value
          - desc: desc
            value: value
        preview:
          title: title
          rows:
          - desc: desc
            value: value
          - desc: desc
            value: value
        dns:
          title: title
          rows:
          - desc: desc
            value: value
          - desc: desc
            value: value
      properties:
        links:
          $ref: "#/components/schemas/WebsiteTable"
        preview:
          $ref: "#/components/schemas/WebsiteTable"
        dns:
          $ref: "#/components/schemas/WebsiteTable"
      type: object
    WebsiteLoginResponse:
      description: Response from a website login request.
      example:
        type: location
        location: https://www.site.com/
      properties:
        type:
          type: string
        location:
          type: string
      title: WebsiteLoginResponse
      type: object
    WebsiteRow:
      description: A result row from the `Webhosting` `GET` request.
      example:
        website_id: "376359"
        website_hostname: hussfamily.com
        repeat_invoices_cost: "5.99"
        website_status: active
        services_name: Standard Web Hosting
        website_comment: ""
      properties:
        website_id:
          description: The id of the website.
          example: "376359"
          type: string
        website_hostname:
          description: The hostname of the website.
          example: hussfamily.com
          type: string
        repeat_invoices_cost:
          description: The repeat invoices cost of the website.
          example: "5.99"
          type: string
        website_status:
          description: The status of the website.
          example: active
          type: string
        services_name:
          description: The services name of the website.
          example: Standard Web Hosting
          type: string
        website_comment:
          description: The comment of the website.
          example: website for manhattan project.
          type: string
      required:
      - repeat_invoices_cost
      - services_name
      - website_comment
      - website_hostname
      - website_id
      - website_status
      title: WebsiteRow
      type: object
    WebsiteServiceExtra:
      description: Extra information for the service
      type: object
    WebsiteServiceInfo:
      example:
        website_id: "1196829"
        website_server: "543"
        website_type: "11363"
        website_currency: USD
        website_order_date: 2023-03-16T22:51:54.000Z
        website_custid: "85872"
        website_ip: 74.50.80.15
        website_status: active
        website_invoice: "20261994"
        website_coupon: "1690"
        website_extra: "[]"
        website_hostname: vintagevultures.com
        website_comment: website_comment
        website_username: vintagev
        website_server_status: website_server_status
      properties:
        website_id:
          description: Website ID
          example: "1196829"
          type: string
        website_server:
          description: Website server
          example: "543"
          type: string
        website_type:
          description: Website type
          example: "11363"
          type: string
        website_currency:
          description: Currency of the website
          example: USD
          type: string
        website_order_date:
          description: Order date of the website
          example: 2023-03-16T22:51:54.000Z
          type: string
        website_custid:
          description: Customer ID of the website
          example: "85872"
          type: string
        website_ip:
          description: IP address of the website
          example: 74.50.80.15
          type: string
        website_status:
          description: Status of the website
          example: active
          type: string
        website_invoice:
          description: Invoice of the website
          example: "20261994"
          type: string
        website_coupon:
          description: Coupon for the website
          example: "1690"
          type: string
        website_extra:
          description: Extra information in JSON format for the website
          example: "[]"
          type: string
        website_hostname:
          description: Hostname of the website
          example: vintagevultures.com
          type: string
        website_comment:
          description: Comment for the website
          type: string
        website_username:
          description: Username for the website
          example: vintagev
          type: string
        website_server_status:
          description: Server status of the website
          type: string
      type: object
    WebsiteServiceMaster:
      example:
        website_id: "543"
        website_name: vda4200.is.cc
        website_ip: 74.50.80.15
        website_type: "206"
        website_available: "0"
        website_hdsize: "2062"
        website_hdfree: "196"
        website_load: "6.55"
        website_last_update: 2023-08-17T23:01:02.000Z
        website_max_sites: "300"
        website_order: "58984"
        website_partitions: website_partitions
        website_dns1: vda4200a.trouble-free.net
        website_dns2: vda4200b.trouble-free.net
      properties:
        website_id:
          description: Website ID for the service master
          example: "543"
          type: string
        website_name:
          description: Website name for the service master
          example: vda4200.is.cc
          type: string
        website_ip:
          description: IP address for the service master
          example: 74.50.80.15
          type: string
        website_type:
          description: Website type for the service master
          example: "206"
          type: string
        website_available:
          description: Availability status for the service master
          example: "0"
          type: string
        website_hdsize:
          description: Hard drive size for the service master
          example: "2062"
          type: string
        website_hdfree:
          description: Free hard drive space for the service master
          example: "196"
          type: string
        website_load:
          description: Load for the service master
          example: "6.55"
          type: string
        website_last_update:
          description: Last update date for the service master
          example: 2023-08-17T23:01:02.000Z
          type: string
        website_max_sites:
          description: Maximum number of sites for the service master
          example: "300"
          type: string
        website_order:
          description: Order number for the service master
          example: "58984"
          type: string
        website_partitions:
          description: Partitions for the service master
          type: string
        website_dns1:
          description: DNS server 1 for the service master
          example: vda4200a.trouble-free.net
          type: string
        website_dns2:
          description: DNS server 2 for the service master
          example: vda4200b.trouble-free.net
          type: string
      type: object
    WebsiteTable:
      example:
        title: title
        rows:
        - desc: desc
          value: value
        - desc: desc
          value: value
      properties:
        title:
          description: Title of the table
          type: string
        rows:
          items:
            $ref: "#/components/schemas/WebsiteTableRow"
          type: array
      type: object
    WebsiteTableRow:
      example:
        desc: desc
        value: value
      properties:
        desc:
          description: Description for the row
          type: string
        value:
          description: Value for the row
          type: string
      type: object
    WebsitesOrder:
      description: Schema for the WebsitesOrder object
      example:
        step: order_form
        website: ""
        period: 1
        serviceOfferId: 0
        packages:
          "11447": Web Hosting Direct Admin (PriceLock)
        enableDomainRegistering: false
        jsonServices:
          "11447": "5.00"
        jsonServiceOffers:
          "1026":
          - service_offer_id: "136"
            service_id: "1026"
            intro_cost: 96
            renewal_cost: 96
            intro_frequency: "12"
            renewal_frequency: "12"
            allow_coupon: "1"
            service_module: webhosting
            created_at: 2023-01-25T14:50:55.000Z
            updated_at: updated_at
            deleted_at: deleted_at
            currencySymbol: $
          - service_offer_id: "136"
            service_id: "1026"
            intro_cost: 96
            renewal_cost: 96
            intro_frequency: "12"
            renewal_frequency: "12"
            allow_coupon: "1"
            service_module: webhosting
            created_at: 2023-01-25T14:50:55.000Z
            updated_at: updated_at
            deleted_at: deleted_at
            currencySymbol: $
        serviceTypes:
          "11447":
            services_id: "11447"
            services_name: Web Hosting Direct Admin (PriceLock)
            services_cost: "5.00"
            services_category: "204"
            services_buyable: "1"
            services_type: "206"
            services_field1: ""
            services_field2: Standard
            services_module: webhosting
            services_html: services_html
            services_description: services_description
            services_moreinfo_url: services_moreinfo_url
            services_hidden: "1"
        serviceOffers:
          "1026":
          - service_offer_id: "136"
            service_id: "1026"
            intro_cost: "96.00"
            renewal_cost: "96.00"
            intro_frequency: "12"
            renewal_frequency: "12"
            allow_coupon: "1"
            service_module: webhosting
            created_at: 2023-01-25T14:50:55.000Z
            updated_at: updated_at
            deleted_at: deleted_at
          - service_offer_id: "136"
            service_id: "1026"
            intro_cost: "96.00"
            renewal_cost: "96.00"
            intro_frequency: "12"
            renewal_frequency: "12"
            allow_coupon: "1"
            service_module: webhosting
            created_at: 2023-01-25T14:50:55.000Z
            updated_at: updated_at
            deleted_at: deleted_at
        packges:
          "11440":
            services_id: "11440"
            services_name: DA BOOST X
            services_cost: "69.95"
            services_category: "204"
            services_buyable: "1"
            services_type: "206"
            services_field1: ""
            services_field2: BoostX
            services_module: webhosting
            services_html: ""
            services_description: Direct Admin Web hosting package with 10x more resources
              over our standard web hosting package.
            services_moreinfo_url: ""
            services_hidden: "0"
      properties:
        step:
          description: Step description
          example: order_form
          type: string
        website:
          description: Website description
          example: ""
          type: string
        period:
          description: Period description
          example: 1
          type: integer
        serviceOfferId:
          description: Service offer ID description
          example: 0
          type: integer
        packages:
          $ref: "#/components/schemas/WebsitesOrderPackages"
        enableDomainRegistering:
          description: Enable domain registering description
          example: false
          type: boolean
        jsonServices:
          $ref: "#/components/schemas/WebsitesOrderJsonServices"
        jsonServiceOffers:
          $ref: "#/components/schemas/WebsitesOrderJsonServiceOffers"
        serviceTypes:
          $ref: "#/components/schemas/WebsitesOrder_serviceTypes"
        serviceOffers:
          $ref: "#/components/schemas/WebsitesOrder_serviceOffers"
        packges:
          $ref: "#/components/schemas/WebsitesOrder_packges"
      required:
      - enableDomainRegistering
      - jsonServiceOffers
      - jsonServices
      - packages
      - period
      - serviceOfferId
      - serviceOffers
      - serviceTypes
      - step
      - website
      type: object
    WebsitesOrderJsonServiceOffers:
      description: Schema for the jsonServiceOffers field in WebsitesOrder
      example:
        "1026":
        - service_offer_id: "136"
          service_id: "1026"
          intro_cost: 96
          renewal_cost: 96
          intro_frequency: "12"
          renewal_frequency: "12"
          allow_coupon: "1"
          service_module: webhosting
          created_at: 2023-01-25T14:50:55.000Z
          updated_at: updated_at
          deleted_at: deleted_at
          currencySymbol: $
        - service_offer_id: "136"
          service_id: "1026"
          intro_cost: 96
          renewal_cost: 96
          intro_frequency: "12"
          renewal_frequency: "12"
          allow_coupon: "1"
          service_module: webhosting
          created_at: 2023-01-25T14:50:55.000Z
          updated_at: updated_at
          deleted_at: deleted_at
          currencySymbol: $
      properties:
        "1026":
          description: Array of service offers
          items:
            $ref: "#/components/schemas/WebsitesOrderJsonServiceOffersItem"
          type: array
      required:
      - "1026"
      type: object
    WebsitesOrderJsonServiceOffersItem:
      example:
        service_offer_id: "136"
        service_id: "1026"
        intro_cost: 96
        renewal_cost: 96
        intro_frequency: "12"
        renewal_frequency: "12"
        allow_coupon: "1"
        service_module: webhosting
        created_at: 2023-01-25T14:50:55.000Z
        updated_at: updated_at
        deleted_at: deleted_at
        currencySymbol: $
      properties:
        service_offer_id:
          description: Service offer ID
          example: "136"
          type: string
        service_id:
          description: Service ID
          example: "1026"
          type: string
        intro_cost:
          description: Introductory cost
          example: 96
          type: integer
        renewal_cost:
          description: Renewal cost
          example: 96
          type: integer
        intro_frequency:
          description: Introductory frequency
          example: "12"
          type: string
        renewal_frequency:
          description: Renewal frequency
          example: "12"
          type: string
        allow_coupon:
          description: Allow coupon
          example: "1"
          type: string
        service_module:
          description: Service module
          example: webhosting
          type: string
        created_at:
          description: Creation date
          example: 2023-01-25T14:50:55.000Z
          type: string
        updated_at:
          description: Update date
          type: string
        deleted_at:
          description: Deletion date
          type: string
        currencySymbol:
          description: Currency symbol
          example: $
          type: string
      type: object
    WebsitesOrderJsonServices:
      description: Schema for the jsonServices field in WebsitesOrder
      example:
        "11447": "5.00"
      properties:
        "11447":
          description: JSON services description
          example: "5.00"
          type: string
      required:
      - "11447"
      type: object
    WebsitesOrderPackages:
      description: Schema for the packages field in WebsitesOrder
      example:
        "11447": Web Hosting Direct Admin (PriceLock)
      properties:
        "11447":
          description: Package description
          example: Web Hosting Direct Admin (PriceLock)
          type: string
      required:
      - "11447"
      type: object
    WebsitesOrderPackagesInfo:
      example:
        services_id: "11440"
        services_name: DA BOOST X
        services_cost: "69.95"
        services_category: "204"
        services_buyable: "1"
        services_type: "206"
        services_field1: ""
        services_field2: BoostX
        services_module: webhosting
        services_html: ""
        services_description: Direct Admin Web hosting package with 10x more resources
          over our standard web hosting package.
        services_moreinfo_url: ""
        services_hidden: "0"
      properties:
        services_id:
          description: The ID of the package.
          example: "11440"
          type: string
        services_name:
          description: The name of the package.
          example: DA BOOST X
          type: string
        services_cost:
          description: The cost of the package.
          example: "69.95"
          type: string
        services_category:
          description: The category of the package.
          example: "204"
          type: string
        services_buyable:
          description: "Indicates if the package is buyable (1 for yes, 0 for no)."
          example: "1"
          type: string
        services_type:
          description: The type of the package.
          example: "206"
          type: string
        services_field1:
          description: Additional field 1 for the package.
          example: ""
          type: string
        services_field2:
          description: Additional field 2 for the package.
          example: BoostX
          type: string
        services_module:
          description: The module of the package.
          example: webhosting
          type: string
        services_html:
          description: HTML content for the package.
          example: ""
          type: string
        services_description:
          description: Description of the package.
          example: Direct Admin Web hosting package with 10x more resources over our
            standard web hosting package.
          type: string
        services_moreinfo_url:
          description: URL for more information about the package.
          example: ""
          type: string
        services_hidden:
          description: "Indicates if the package is hidden (1 for yes, 0 for no)."
          example: "0"
          type: string
      required:
      - services_buyable
      - services_category
      - services_cost
      - services_description
      - services_id
      - services_module
      - services_name
      - services_type
      type: object
    WebsitesOrderServiceOffer:
      example:
        service_offer_id: "136"
        service_id: "1026"
        intro_cost: "96.00"
        renewal_cost: "96.00"
        intro_frequency: "12"
        renewal_frequency: "12"
        allow_coupon: "1"
        service_module: webhosting
        created_at: 2023-01-25T14:50:55.000Z
        updated_at: updated_at
        deleted_at: deleted_at
      properties:
        service_offer_id:
          description: The ID of the service offer.
          example: "136"
          type: string
        service_id:
          description: The ID of the associated service.
          example: "1026"
          type: string
        intro_cost:
          description: The introductory cost of the service offer.
          example: "96.00"
          type: string
        renewal_cost:
          description: The renewal cost of the service offer.
          example: "96.00"
          type: string
        intro_frequency:
          description: The introductory frequency of the service offer.
          example: "12"
          type: string
        renewal_frequency:
          description: The renewal frequency of the service offer.
          example: "12"
          type: string
        allow_coupon:
          description: "Indicates if coupons are allowed (1 for yes, 0 for no)."
          example: "1"
          type: string
        service_module:
          description: The module of the service offer.
          example: webhosting
          type: string
        created_at:
          description: The creation timestamp of the service offer.
          example: 2023-01-25T14:50:55.000Z
          type: string
        updated_at:
          description: The update timestamp of the service offer.
          type: string
        deleted_at:
          description: The deletion timestamp of the service offer.
          type: string
      required:
      - allow_coupon
      - created_at
      - intro_cost
      - intro_frequency
      - renewal_cost
      - renewal_frequency
      - service_id
      - service_module
      - service_offer_id
      type: object
    WebsitesOrderServiceTypes:
      example:
        services_id: "11447"
        services_name: Web Hosting Direct Admin (PriceLock)
        services_cost: "5.00"
        services_category: "204"
        services_buyable: "1"
        services_type: "206"
        services_field1: ""
        services_field2: Standard
        services_module: webhosting
        services_html: services_html
        services_description: services_description
        services_moreinfo_url: services_moreinfo_url
        services_hidden: "1"
      properties:
        services_id:
          description: The ID of the service.
          example: "11447"
          type: string
        services_name:
          description: The name of the service.
          example: Web Hosting Direct Admin (PriceLock)
          type: string
        services_cost:
          description: The cost of the service.
          example: "5.00"
          type: string
        services_category:
          description: The category of the service.
          example: "204"
          type: string
        services_buyable:
          description: "Indicates if the service is buyable (1 for yes, 0 for no)."
          example: "1"
          type: string
        services_type:
          description: The type of the service.
          example: "206"
          type: string
        services_field1:
          description: Additional field 1 for the service.
          example: ""
          type: string
        services_field2:
          description: Additional field 2 for the service.
          example: Standard
          type: string
        services_module:
          description: The module of the service.
          example: webhosting
          type: string
        services_html:
          description: HTML content for the service.
          type: string
        services_description:
          description: Description of the service.
          type: string
        services_moreinfo_url:
          description: URL for more information about the service.
          type: string
        services_hidden:
          description: "Indicates if the service is hidden (1 for yes, 0 for no)."
          example: "1"
          type: string
      required:
      - services_buyable
      - services_category
      - services_cost
      - services_hidden
      - services_id
      - services_module
      - services_name
      - services_type
      type: object
    IpObject:
      description: IP Address
      example:
        ip: 1.2.3.4
      properties:
        ip:
          description: IP Address
          type: string
      required:
      - ip
      title: IpObject
      type: object
    LoginSuccessResponse:
      description: The response from a successful login.
      example:
        sessionId: zzzzzzzzzzzzzz
        account_id: 123
        account_lid: my@user.com
        ima: client
        gravatar: https://gravatar.com/user/image.png
      properties:
        sessionId:
          type: string
        account_id:
          format: int32
          type: integer
        account_lid:
          type: string
        ima:
          type: string
        gravatar:
          type: string
      title: LoginSuccessResponse
      type: object
    EmailAddressName:
      description: An email contact.
      example:
        email: user@domain.com
        name: John Smith
      properties:
        email:
          description: The email address.
          example: user@domain.com
          type: string
        name:
          description: Name to use for the sending contact.
          example: John Smith
          type: string
      required:
      - email
      title: EmailAddressName
      type: object
    EmailAddress:
      description: an email address
      example:
        email: user@domain.com
      properties:
        email:
          description: an email address
          example: user@domain.com
          type: string
      title: EmailAddress
      type: object
    DenyRuleRecord:
      allOf:
      - properties:
          id:
            description: The deny rule Id number.
            example: 41124
            type: integer
          created:
            description: the date the rule was created.
            example: 2022-03-22 19:16:35
            type: string
        required:
        - created
        - id
        type: object
      - $ref: "#/components/schemas/DenyRuleNew"
      description: The data for a email deny rule record.
      example:
        id: 14
        user: mb20682
        type: email
        data: domeinwo@server.guesshost.net
        created: 2022-03-22 19:16:35
      title: DenyRuleRecord
      type: object
    DenyRuleNew:
      description: The data for a email deny rule record.
      example:
        user: mb20682
        type: email
        data: domeinwo@server.guesshost.net
      properties:
        user:
          description: Mail account username that will be tied to this rule.  If not
            specified the first active mail order will be used.
          example: mb20682
          type: string
        type:
          description: The type of deny rule.
          enum:
          - domain
          - email
          - startswith
          - destination
          example: email
          type: string
        data:
          description: The content of the rule.  If a domain type rule then an example
            would be google.com. For a begins with type an example would be msgid-.  For
            the email typer an example would be user@server.com.
          example: domeinwo@server.guesshost.net
          type: string
      required:
      - data
      - type
      title: DenyRuleNew
      type: object
    MailAttachment:
      description: (optional) File attachments to include in the email.  The file
        contents must be base64
      example:
        filename: file.txt
        data: base64_encoded_file_contents
      properties:
        filename:
          description: The filename of the attached file.
          example: message.txt
          type: string
        data:
          description: The file contents base64 encoded
          example: aGVsbG8gdGhlcmUK
          type: string
      required:
      - data
      - filename
      title: MailAttachment
      type: object
    MailBlockClickHouse:
      description: A block entry from the clickhouse mailblocks server.
      example:
        date: 2023-08-07
        from: user@domain.com
        messageId: pFaRqFUEWkucjhTuIzYuoAgWU@domain.com
        subject: Test Email
        to: "['client@site.com']"
      properties:
        date:
          format: date
          type: string
        from:
          type: string
        messageId:
          type: string
        subject:
          type: string
        to:
          type: string
      required:
      - date
      - from
      - messageId
      - subject
      - to
      title: MailBlockClickHouse
      type: object
    MailBlockRspamd:
      description: This is a block entry from the rspamd block list.
      example:
        from: user@domain.com
        subject: Test email
      properties:
        from:
          type: string
        subject:
          type: string
      required:
      - from
      - subject
      title: MailBlockRspamd
      type: object
    MailBlocks:
      description: The listing of blocked emails.
      example:
        local:
        - date: 2023-08-07
          from: user@domain.com
          messageId: pFaRqFUEWkucjhTuIzYuoAgWU@domain.com
          subject: Test Email
          to: "['client@site.com']"
        mbtrap:
        - date: 2023-08-07
          from: user@domain.com
          messageId: pFaRqFUEWkucjhTuIzYuoAgWU@domain.com
          subject: Test Email
          to: "['client@site.com']"
        subject:
        - from: user@domain.com
          subject: Test Email
      properties:
        local:
          items:
            $ref: "#/components/schemas/MailBlockClickHouse"
          type: array
        mbtrap:
          items:
            $ref: "#/components/schemas/MailBlockClickHouse"
          type: array
        subject:
          items:
            $ref: "#/components/schemas/MailBlockRspamd"
          type: array
      required:
      - local
      - mbtrap
      - subject
      title: MailBlocks
      type: object
    MailLog:
      description: "Paginated mail log response.  Contains the full matched count\
        \ (`total`) plus a page of `MailLogEntry` records.  The `total` reflects the\
        \ grouping mode: with `groupby=recipient` it counts delivery attempts, with\
        \ `groupby=message` it counts unique messages."
      example:
        total: 1
        skip: 0
        limit: 100
        emails:
        - _id: 103172
          id: 17c7eda538e0005d03
          from: person@mysite.com
          to: client@isp.com
          subject: sell 0.005 shares
          messageId: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
          created: 2021-10-14 08:50:10
          time: 1634215809
          user: mb5658
          transtype: ESMTPSA
          origin: 199.231.189.154
          interface: feeder
          sendingZone: interserver
          bodySize: 63
          seq: 1
          delivered: 1
          code: 250
          recipient: client@isp.com
          domain: interserver.net
          locked: 1
          lockTime: "1634215818533"
          assigned: relay1
          queued: 2021-10-14T12:50:15.487Z
          mxHostname: mx.j.is.cc
          response: 250 2.0.0 Ok queued as C91D83E128C
      properties:
        total:
          description: "Total number of log entries that match the supplied filters,\
            \ regardless of `skip` and `limit`.  Reflects the `groupby` mode."
          example: 10234
          type: integer
        skip:
          description: The `skip` value used for this page (echoed from the request).
          example: 0
          type: integer
        limit:
          description: The `limit` value used for this page (echoed from the request).
          example: 100
          type: integer
        emails:
          example:
          - _id: 103172
            id: 17c7eda538e0005d03
            from: person@mysite.com
            to: client@isp.com
            subject: sell 0.005 shares
            messageId: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
            created: 2021-10-14 08:50:10
            time: 1634215809
            user: mb5658
            transtype: ESMTPSA
            origin: 199.231.189.154
            interface: feeder
            sendingZone: interserver
            bodySize: 63
            seq: 1
            delivered: 1
            code: 250
            recipient: client@isp.com
            domain: interserver.net
            locked: 1
            lockTime: "1634215818533"
            assigned: relay1
            queued: 2021-10-14T12:50:15.487Z
            mxHostname: mx.j.is.cc
            response: 250 2.0.0 Ok queued as C91D83E128C
          items:
            $ref: "#/components/schemas/MailLogEntry"
          type: array
      required:
      - emails
      - limit
      - skip
      - total
      title: MailLog
      type: object
    MailLogEntry:
      description: "A single email record in the mail log.  Combines data from the\
        \ message store (envelope metadata), the queue release table (delivery status\
        \ and response), and the sender delivery table (MX routing details).  When\
        \ `groupby=recipient` each row represents one delivery attempt; when `groupby=message`\
        \ delivery fields reflect one arbitrary recipient."
      example:
        _id: 103172
        id: 17c7eda538e0005d03
        from: person@mysite.com
        to: client@isp.com
        subject: sell 0.005 shares
        messageId: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
        created: 2021-10-14 08:50:10
        time: 1634215809
        user: mb5658
        transtype: ESMTPSA
        origin: 199.231.189.154
        interface: feeder
        sendingZone: interserver
        bodySize: 63
        seq: 1
        delivered: 1
        code: 250
        recipient: client@isp.com
        domain: interserver.net
        locked: 1
        lockTime: "1634215818533"
        assigned: relay1
        queued: 2021-10-14T12:50:15.487Z
        mxHostname: mx.j.is.cc
        response: 250 2.0.0 Ok queued as C91D83E128C
      properties:
        _id:
          description: Internal auto-increment database row ID.
          example: 103172
          type: integer
          x-codegen-property-name: internalId
        id:
          description: The relay-assigned mail ID (18-19 hex characters).  Matches
            the `mailid` filter parameter and the `text` value returned by send endpoints.
          example: 17c7eda538e0005d03
          type: string
        from:
          description: SMTP envelope `MAIL FROM` address.
          example: person@mysite.com
          type: string
        to:
          description: SMTP envelope `RCPT TO` address.
          example: client@isp.com
          type: string
        subject:
          description: "The `Subject` header value.  MIME-encoded subjects (UTF-8,\
            \ ISO-8859, US-ASCII) are automatically decoded."
          example: sell 0.005 shares
          nullable: true
          type: string
        messageId:
          description: The `Message-ID` header value.  Can be used with the `messageId`
            filter for subsequent lookups.
          example: <vmiLEebsuCbSpUxD7oN3REpaN4VbN6BrdCAbNKIrdAo@relay0.mailbaby.net>
          nullable: true
          type: string
        created:
          description: Human-readable creation timestamp in `YYYY-MM-DD HH:MM:SS`
            format.
          example: 2021-10-14 08:50:10
          type: string
        time:
          description: Unix timestamp of message acceptance.  Corresponds to the `startDate`
            and `endDate` filter parameters.
          example: 1634215809
          type: integer
        user:
          description: The SMTP AUTH username used to submit the message (e.g. `mb5658`).
          example: mb5658
          type: string
        transtype:
          description: SMTP transaction type negotiated with the relay.
          example: ESMTPSA
          type: string
        origin:
          description: IP address of the client that submitted the message to the
            relay.
          example: 199.231.189.154
          type: string
        interface:
          description: Relay interface name that accepted the message.
          example: feeder
          type: string
        sendingZone:
          description: The sending zone assigned by the relay for outbound delivery.
          example: interserver
          nullable: true
          type: string
        bodySize:
          description: Size of the message body in bytes.
          example: 63
          nullable: true
          type: integer
        seq:
          description: Sequence index of this recipient in a multi-recipient message.
            Starts at 1.
          example: 1
          nullable: true
          type: integer
        delivered:
          description: "Delivery status flag.  `1` = successfully delivered to destination\
            \ MX. `0` = queued, deferred, or failed.  `null` = delivery not yet attempted."
          example: 1
          nullable: true
          type: integer
        code:
          description: The SMTP response code from the destination MX server (e.g.
            `250`).
          example: 250
          nullable: true
          type: integer
        recipient:
          description: The specific recipient address this delivery record is for.
          example: client@isp.com
          nullable: true
          type: string
        response:
          description: The full SMTP response string received from the destination
            MX server.
          example: 250 2.0.0 Ok queued as C91D83E128C
          nullable: true
          type: string
        domain:
          description: The destination domain for this delivery attempt.
          example: interserver.net
          nullable: true
          type: string
        locked:
          description: Whether the queue entry is currently locked for delivery processing.
          example: 1
          nullable: true
          type: integer
        lockTime:
          description: Millisecond-precision timestamp of the last queue lock acquisition.
          example: "1634215818533"
          nullable: true
          type: string
        assigned:
          description: The relay server node assigned to deliver this message.
          example: relay1
          nullable: true
          type: string
        queued:
          description: ISO 8601 timestamp when the message was placed into the delivery
            queue.
          example: 2021-10-14T12:50:15.487Z
          nullable: true
          type: string
        mxHostname:
          description: The MX hostname the relay connected to for delivery.  Corresponds
            to the `mx` filter parameter.
          example: mx.j.is.cc
          nullable: true
          type: string
      required:
      - _id
      - created
      - from
      - id
      - interface
      - origin
      - time
      - to
      - transtype
      - user
      title: MailLogEntry
      type: object
    SendMail:
      description: Details for an Email
      example:
        to: johndoe@company.com
        from: janedoe@company.com
        subject: Attention Client
        body: This is an email to inform you that something noteworthy happened.
      properties:
        to:
          description: The Contact whom is the primary recipient of this email.
          example: johndoe@company.com
          type: string
        from:
          description: The contact whom is the this email is from.
          example: janedoe@company.com
          type: string
        subject:
          description: The subject or title of the email
          example: Attention Client
          type: string
        body:
          description: The main email contents.
          example: This is an email to inform you that something noteworthy happened.
          type: string
      required:
      - body
      - from
      - subject
      - to
      title: SendMail
      type: object
    SendMailAdv:
      description: Details for an Email
      example:
        subject: Welcome
        body: Hello
        from:
          email: user@domain.com
        to:
        - email: someone@client.com
          name: Mr Client
        attachments:
        - filename: file.txt
          data: base64_encoded_contents
        id: 66
      properties:
        subject:
          description: The subject or title of the email
          example: Your Package has been Delivered!
          type: string
        body:
          description: The main email contents.
          example: "The package you ordered on 2021-01-23 has been delivered. If the\
            \ package is broken into many pieces, please blaim someone else."
          type: string
        from:
          $ref: "#/components/schemas/EmailAddressName"
        to:
          description: A list of destionation email addresses to send this to
          example:
          - email: user@domain.com
            name: John Smith
          items:
            $ref: "#/components/schemas/EmailAddressName"
          type: array
        replyto:
          description: (optional) A list of email addresses that specify where replies
            to the email should be sent instead of the _from_ address.
          example:
          - email: user@domain.com
            name: John Smith
          items:
            $ref: "#/components/schemas/EmailAddressName"
          type: array
        cc:
          description: (optional) A list of email addresses to carbon copy this message
            to.  They are listed on the email and anyone getting the email can see
            this full list of Contacts who received the email as well.
          example:
          - email: user@domain.com
            name: John Smith
          items:
            $ref: "#/components/schemas/EmailAddressName"
          type: array
        bcc:
          description: (optional) list of email addresses that should receive copies
            of the email.  They are hidden on the email and anyone gettitng the email
            would not see the other people getting the email in this list.
          example:
          - email: user@domain.com
            name: John Smith
          items:
            $ref: "#/components/schemas/EmailAddressName"
          type: array
        attachments:
          description: (optional) File attachments to include in the email.  The file
            contents must be base64 encoded!
          example:
          - filename: text.txt
            data: base64_encoded_contents
          items:
            $ref: "#/components/schemas/MailAttachment"
          type: array
        id:
          description: (optional)  ID of the Mail order within our system to use as
            the Mail Account.
          example: 5000
          format: int64
          type: integer
      required:
      - body
      - from
      - subject
      - to
      title: SendMailAdv
      type: object
    AffiliateDockSetup:
      description: Affiliate Landing Page information.
      example:
        affiliate_dock_title: Exclusive offer to viewers
        affiliate_dock_description: Use this coupon when placing an order to get the
          first month of hosting for only 1 penny.
        referrer_coupon: exclusiveoffer
      properties:
        affiliate_dock_title:
          type: string
        affiliate_dock_description:
          type: string
        referrer_coupon:
          type: string
      title: Root Type for AffiliateDockSetup
      type: object
    AffiliatePaymentSetup:
      description: Affiliate Payment Setup.  Here you can set if you want the payments
        to go to `prepay` or `paypal` and the PayPal email address.
      example:
        affiliate_paypal: paypal@email.com
        affiliate_payment_method: prepay
      properties:
        affiliate_paypal:
          type: string
        affiliate_payment_method:
          type: string
      title: Root Type for AffiliatePaymentSetup
      type: object
    ServerIpmiPowerRequest:
      description: IPMI Power command for servers
      example:
        asset: 5432
        action: reset
      properties:
        asset:
          description: The Asset ID
          example: 5432
          format: int32
          type: integer
        action:
          description: The power action to send to the ipmi controller.
          enum:
          - cycle
          - reset
          - "on"
          - "off"
          - soft
          example: "on"
          type: string
      required:
      - action
      title: Root Type for ServerIpmiPowerRequest
      type: object
    ServerBulkIpmiPowerResponse:
      description: Per-server IPMI power-status results for a bulk lookup.
      example:
        results:
        - id: 2313
          asset: 5432
          text: Chassis Power is on
        - id: 2314
          error: Service is not active
        - id: 2315
          error: Invalid Service Passed
      properties:
        results:
          items:
            $ref: "#/components/schemas/ServerBulkIpmiPowerResponse_results_inner"
          type: array
      required:
      - results
      title: Root Type for ServerBulkIpmiPowerResponse
      type: object
    ServerIpmiLiveRequest:
      description: Request body to setup an IPMI Live connection.
      example:
        asset: 5431
        ip: 1.2.3.4
      properties:
        asset:
          description: Asset ID
          example: 5431
          format: int32
          type: integer
        ip:
          description: Your IP Address you wish to connect to the IPMI system from.
          example: 1.2.3.4
          type: string
      required:
      - ip
      title: Root Type for ServerIpmiLiveRequest
      type: object
    BackupOrderPutRequest:
      description: Parameters to submit to validate your backup order
      example:
        validateOnly: true
        serviceType: 10831
        coupon: ""
      properties:
        validateOnly:
          type: boolean
        serviceType:
          format: int32
          type: integer
        coupon:
          type: string
      title: Root Type for BackupOrderPutRequest
      type: object
    BackupOrderPutResponse:
      description: Backup Order validation response
      example:
        continue: true
        errors: []
        serviceType: 10831
        serviceCost: "3.00"
        originalCost: "3.00"
        repeatServiceCost: "3.00"
        hostname: ""
        password: m13dxi6K
        coupon: ""
        couponCode: 0
      properties:
        continue:
          type: boolean
        errors:
          items:
            type: string
          type: array
        serviceType:
          format: int32
          type: integer
        serviceCost:
          type: string
        originalCost:
          type: string
        repeatServiceCost:
          type: string
        hostname:
          type: string
        password:
          type: string
        coupon:
          type: string
        couponCode:
          format: int32
          type: integer
      title: Root Type for BackupOrderPutResponse
      type: object
    BackupOrderPostResponse:
      description: Backup Order Placement Response
      example:
        continue: true
        errors: []
        total_cost: "3.00"
        iid: "25296597"
        iids:
        - SERVICEbackups40464
        real_iids:
        - "25296597"
        serviceId: 40464
        invoice_description: Storage ST 100
        cj_params:
          containerTagId: "1684"
          CID: "1525038"
          OID: backups40464
          TYPE: "355669"
          ITEM1: backups10831
          AMT1: "3.00"
          QTY1: 1
          CURRENCY: USD
      properties:
        continue:
          type: boolean
        errors:
          items:
            type: string
          type: array
        total_cost:
          type: string
        iid:
          type: string
        iids:
          items:
            type: string
          type: array
        real_iids:
          items:
            type: string
          type: array
        serviceId:
          format: int32
          type: integer
        invoice_description:
          type: string
        cj_params:
          $ref: "#/components/schemas/BackupOrderPostResponse_cj_params"
      title: Root Type for BackupOrderPostResponse
      type: object
    ServiceOrderPostResponse:
      description: Generic response returned after placing a service order. Contains
        invoice IDs for payment and the new service ID.
      example:
        continue: true
        errors: []
        total_cost: "5.00"
        iid: "25296600"
        iids:
        - SERVICE12345
        real_iids:
        - "25296600"
        serviceId: 12345
        invoice_description: New Service Order
      properties:
        continue:
          description: Whether the order was accepted and can proceed to payment.
          type: boolean
        errors:
          description: List of validation errors (empty on success).
          items:
            type: string
          type: array
        total_cost:
          description: Total cost of the order.
          type: string
        iid:
          description: Primary invoice ID for payment.
          type: string
        iids:
          description: All invoice identifiers associated with the order.
          items:
            type: string
          type: array
        real_iids:
          description: Numeric invoice IDs for use with billing endpoints.
          items:
            type: string
          type: array
        serviceId:
          description: The new service ID created by the order.
          format: int32
          type: integer
        invoice_description:
          description: Human-readable description of the invoice.
          type: string
      title: ServiceOrderPostResponse
      type: object
    ServerIpmiLiveInfo:
      description: Information about the IPMI connection.
      example:
        text: Setup Complete
        public_ip: 1.2.3.4
        allowed_ip: 1.2.3.4
        client_username: user
        client_password: pass
      properties:
        text:
          type: string
        public_ip:
          type: string
        allowed_ip:
          type: string
        client_username:
          type: string
        client_password:
          type: string
      title: Root Type for ServerIpmiLiveInfo
      type: object
    MailStatsType:
      description: "Statistics about the mail usage including volume by IP, To address,\
        \ and From address; as well as total sent / delivered counts and cost."
      example:
        time: all
        usage: 55
        currency: USD
        currencySymbol: $
        cost: 1.02
        received: 508
        sent: 495
        volume:
          to:
            client@domain.com: 395
            user@site.net: 57
            sales@company.com: 47
            client@anothersite.com: 9
          from:
            billing@somedomain.com: 369
            sales@somedomain.com: 139
          ip:
            "1.1.1.1": 142
            "2.2.2.2": 132
            "3.3.3.3": 129
            "4.4.4.4": 105
      properties:
        time:
          default: 1h
          enum:
          - all
          - billing
          - month
          - 7d
          - 24h
          - today
          - 1h
          type: string
        usage:
          format: int32
          type: integer
        currency:
          type: string
        currencySymbol:
          type: string
        cost:
          format: double
          type: number
        received:
          format: int32
          type: integer
        sent:
          format: int32
          type: integer
        volume:
          $ref: "#/components/schemas/MailStatsType_volume"
      title: Root Type for MailStatsType
      type: object
    VpsOrderPutRequest:
      description: request to validate a vps order
      example:
        osDistro: ubuntu
        slices: 1
        vpsPlatform: kvm
        controlpanel: none
        period: 1
        location: 1
        osVersion: ubuntu24
        hostname: ""
        coupon: ""
        rootpass: string
        comment: ""
      properties:
        osDistro:
          description: OS Distribution
          type: string
        slices:
          default: 1
          description: Number of slices
          format: int32
          maximum: 32
          minimum: 1
          type: integer
        vpsPlatform:
          description: VPS Platform
          enum:
          - kvm
          - hyperv
          - kvmstorage
          type: string
        controlpanel:
          description: Control Panel
          enum:
          - none
          - cpanel
          - da
          type: string
        period:
          default: 1
          description: Billing Period or Frequency
          format: int32
          maximum: 12
          minimum: 1
          type: integer
        location:
          default: 1
          description: Location
          format: int32
          maximum: 3
          minimum: 1
          type: integer
        osVersion:
          description: OS Version
          type: string
        hostname:
          default: ""
          description: The hostname to assign to the VPS
          type: string
        coupon:
          default: ""
          description: Coupon
          type: string
        rootpass:
          description: Root password to assign to the VVPS
          type: string
        comment:
          default: ""
          description: Order comments or notes
          type: string
      required:
      - hostname
      - location
      - osDistro
      - osVersion
      - period
      - rootpass
      - slices
      - vpsPlatform
      type: object
    VpsOrderPostRequest:
      description: request to validate a vps order
      example:
        osDistro: ubuntu
        slices: 1
        vpsPlatform: kvm
        controlpanel: none
        period: 1
        location: 1
        osVersion: ubuntu24
        hostname: ""
        coupon: ""
        rootpass: string
        comment: ""
      properties:
        osDistro:
          description: OS Distribution
          type: string
        slices:
          default: 1
          description: Number of slices
          format: int32
          maximum: 32
          minimum: 1
          type: integer
        vpsPlatform:
          description: VPS Platform
          enum:
          - kvm
          - hyperv
          - kvmstorage
          type: string
        controlpanel:
          description: Control Panel
          enum:
          - none
          - cpanel
          - da
          type: string
        period:
          default: 1
          description: Billing Period or Frequency
          format: int32
          maximum: 12
          minimum: 1
          type: integer
        location:
          default: 1
          description: Location
          format: int32
          maximum: 3
          minimum: 1
          type: integer
        osVersion:
          description: OS Version
          type: string
        hostname:
          default: ""
          description: The hostname to assign to the VPS
          type: string
        coupon:
          default: ""
          description: Coupon
          type: string
        rootpass:
          description: Root password to assign to the VVPS
          type: string
        comment:
          default: ""
          description: Order comments or notes
          type: string
      required:
      - hostname
      - location
      - osDistro
      - osVersion
      - period
      - rootpass
      - slices
      - vpsPlatform
      type: object
    WebsiteOrderPutRequest:
      description: request to validate a webhosting order
      example:
        hostname: mystore.example.com
        packageId: 1003
        rootpass: ""
        period: 1
        coupon: ""
        registerDomain: false
        script: 0
      properties:
        hostname:
          description: Primary FQDN for the website. Must pass valid_hostname() and
            the keyword blocklist.
          type: string
        packageId:
          description: "Plan id from getNewWebsite.serviceTypes[].services_id (must\
            \ be buyable)."
          type: integer
        rootpass:
          default: ""
          description: "Control-panel admin password. If blank, a random password\
            \ is generated server-side."
          type: string
        period:
          default: 1
          description: Billing cycle in months (1 / 6 / 12 / 24 / 36).
          format: int32
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        serviceOfferId:
          default: 0
          description: Promo bundle id from getNewWebsite.serviceOffers.
          format: int32
          type: integer
        script:
          default: 0
          description: Auto-installer id (0 = none).
          format: int32
          type: integer
        comment:
          default: ""
          description: Free-form note saved on the service row.
          type: string
        registerDomain:
          default: false
          description: "When true and enableDomainRegistering=true from the catalog,\
            \ also registers/transfers the domain through the order."
          type: boolean
      required:
      - hostname
      - packageId
      type: object
    WebsiteOrderPostRequest:
      description: request to place a webhosting order
      example:
        hostname: mystore.example.com
        packageId: 1003
        rootpass: ""
        period: 1
        coupon: ""
        registerDomain: false
        script: 0
      properties:
        hostname:
          description: Primary FQDN for the website. Must pass valid_hostname() and
            the keyword blocklist.
          type: string
        packageId:
          description: "Plan id from getNewWebsite.serviceTypes[].services_id (must\
            \ be buyable)."
          type: integer
        rootpass:
          default: ""
          description: "Control-panel admin password. If blank, a random password\
            \ is generated server-side."
          type: string
        period:
          default: 1
          description: Billing cycle in months (1 / 6 / 12 / 24 / 36).
          format: int32
          type: integer
        coupon:
          default: ""
          description: Coupon code.
          type: string
        serviceOfferId:
          default: 0
          description: Promo bundle id from getNewWebsite.serviceOffers.
          format: int32
          type: integer
        script:
          default: 0
          description: Auto-installer id (0 = none).
          format: int32
          type: integer
        comment:
          default: ""
          description: Free-form note saved on the service row.
          type: string
        registerDomain:
          default: false
          description: "When true and enableDomainRegistering=true from the catalog,\
            \ also registers/transfers the domain through the order."
          type: boolean
      required:
      - hostname
      - packageId
      type: object
    VpsOrderPutResponse:
      description: Response from VPS order validation request
      example:
        continue: true
        errors: []
        coupon_code: 0
        service_cost: 3
        slice_cost: 3
        service_type: 33
        repeat_slice_cost: 3
        original_slice_cost: 3
        original_cost: 3
        repeat_service_cost: 3
        monthly_service_cost: 3
        custid: "123456"
        os: ubuntu
        slices: "1"
        platform: kvm
        controlpanel: none
        period: 1
        location: 1
        version: ubuntu24
        hostname: server.blank.com
        coupon: ""
        rootpass: string
      properties:
        continue:
          type: boolean
        errors:
          items: {}
          type: array
        coupon_code:
          format: int32
          type: integer
        service_cost:
          format: int32
          type: integer
        slice_cost:
          format: int32
          type: integer
        service_type:
          format: int32
          type: integer
        repeat_slice_cost:
          format: int32
          type: integer
        original_slice_cost:
          format: int32
          type: integer
        original_cost:
          format: int32
          type: integer
        repeat_service_cost:
          format: int32
          type: integer
        monthly_service_cost:
          format: int32
          type: integer
        custid:
          type: string
        os:
          type: string
        slices:
          type: string
        platform:
          type: string
        controlpanel:
          type: string
        period:
          format: int32
          type: integer
        location:
          format: int32
          type: integer
        version:
          type: string
        hostname:
          type: string
        coupon:
          type: string
        rootpass:
          type: string
      title: Root Type for VpsOrderPutResponse
      type: object
    DnsNewRecord:
      description: Request data for a new DNS record.
      example:
        name: myfamily.com
        type: NS
        content: cdns1.interserver.net
        ttl: 86400
        prio: 0
      properties:
        name:
          description: Name part of record
          example: myfamily.com
          type: string
        type:
          $ref: "#/components/schemas/DnsRecordType"
        content:
          description: Content of record
          example: 127.0.0.1
          type: string
        ttl:
          default: 86400
          description: Time-to-live
          example: 86400
          format: int32
          type: integer
        prio:
          default: 0
          description: Priority
          example: 0
          format: int32
          type: integer
      required:
      - content
      - name
      - type
      title: Root Type for DnsNewRecord
      type: object
    DnsRecordType:
      description: Type of DNS Record
      enum:
      - A
      - A6
      - AAAA
      - AFSDB
      - ALIAS
      - CAA
      - CDNSKEY
      - CDS
      - CERT
      - CNAME
      - DHCID
      - DLV
      - DNSKEY
      - DNAME
      - DS
      - EUI48
      - EUI64
      - HINFO
      - IPSECKEY
      - KEY
      - KX
      - LOC
      - MAILA
      - MAILB
      - MINFO
      - MR
      - MX
      - NAPTR
      - NS
      - NSEC
      - NSEC3
      - NSEC3PARAM
      - OPENPGPKEY
      - OPT
      - PTR
      - RKEY
      - RP
      - RRSIG
      - SIG
      - SOA
      - SPF
      - SRV
      - SSHFP
      - TLSA
      - TKEY
      - TSIG
      - TXT
      - WKS
      - URI⏎
      example: NS
      type: string
    TicketNew:
      description: "New helpdesk ticket request.\r\n"
      example:
        subject: subject of ticket
        body: ticket contents
        service_id: 1231
        service_module: vps
      properties:
        subject:
          type: string
        body:
          type: string
        service_id:
          format: int32
          type: integer
        service_module:
          type: string
      required:
      - body
      - subject
      title: Root Type for TicketNew
      type: object
    TicketNewResponse:
      description: Response returned after creating a new support ticket.
      example:
        success: true
        text: Ticket is created!
        ticket_id: 1759653
      properties:
        ticket_id:
          type: integer
        text:
          type: string
        success:
          type: boolean
      required:
      - success
      - text
      title: Root Type for TicketNewSuccess
      type: object
    ViewTicketResponse:
      description: Ticket details
      example:
        success: true
        ticket:
          ticketid: 123456
          ticketmaskid: UAX-553-25735
          department: General
          status: Closed
          priority: Standard
          subject: Regarding ssl doubts
          created_on: "19 November, 2025 07:24 pm"
          updated_on: "20 November, 2025 05:41 pm"
        ticket_custom_fields:
          Customer Server Access: "y"
          Ip Address: 68.23.3.5
          Root Password: fsdnbfsd
          Sudo User: jjog
          Sudo Password: 123
          Port: 22
        ticket_posts:
        - post_id: 24244
          date: "20 November, 2025 05:41 pm"
          contents: Testing ticket reply from api
          creator: User
          creator_email: abc@email.com
          creator_name: ABC DEF
          hasattachments: 0
        - post_id: 24244
          date: "21 November, 2025 05:41 pm"
          contents: Test 234
          creator: User
          creator_email: john@email.com
          creator_name: John Wick
          hasattachments: 1
          attachment_download: https:my.interserver.net/download_link?id=xxx
      properties:
        success:
          type: boolean
        ticket:
          $ref: "#/components/schemas/TicketDetails"
        ticket_custom_fields:
          $ref: "#/components/schemas/TicketCustomFieldDetails"
        ticket_posts:
          $ref: "#/components/schemas/TicketPostDetails"
      required:
      - success
      - ticket
      title: Root type for ViewTicketResponse
      type: object
    TicketDetails:
      description: Detailed ticket information
      properties:
        ticketid:
          type: integer
        ticketmaskid:
          type: string
        department:
          type: string
        status:
          type: string
        priority:
          type: string
        subject:
          type: string
        created_on:
          type: string
        updated_on:
          type: string
      title: Ticket Information
      type: object
    TicketCustomFieldDetails:
      description: Optional fields providing additional info in ticket
      properties:
        Customer Server Access:
          enum:
          - "y"
          - "n"
          type: string
        Ip Address:
          type: string
        Root Password:
          type: string
        Sudo User:
          type: string
        Sudo Password:
          type: integer
        Port:
          type: integer
      title: Ticket custom field values
      type: object
    TicketPostDetails:
      description: Fetches every posts of ticket
      items:
        $ref: "#/components/schemas/TicketPostDetails_inner"
      title: Ticket Posts
      type: array
    UpdateTicket:
      description: Update custom fields of the ticket
      example:
        ip: 1.2.4.5
        customer_server_access: "y"
        sudo_username: mywebsp
        sudo_password: abc123
        port: 22
      properties:
        ip:
          type: string
        ip_address:
          type: string
        customer_server_access:
          enum:
          - "y"
          - "n"
          type: string
        root_password:
          type: string
        sudo_username:
          type: string
        sudo_password:
          type: string
        port:
          type: integer
      title: Update Ticket
      type: object
    UpdateTicketResponseSchema:
      example:
        success: true
        message: Ticket is updated!
      properties:
        success:
          type: boolean
        message:
          type: string
      title: Update ticket response
      type: object
    ReplyTicketRequest:
      description: Post reply to your ticket
      example:
        content: Requesting to migrate website 123
      properties:
        content:
          type: string
      title: Post reply
      type: object
    ReplyTicketResponseSchema:
      example:
        success: true
        post_id: 234452
      properties:
        success:
          type: boolean
        post_id:
          type: integer
      title: Post Reply to your ticket
      type: object
    CloseTicketResponseSchema:
      example:
        success: true
        text: Ticket is closed!
      properties:
        success:
          type: boolean
        text:
          type: string
      title: Close ticket
      type: object
    CreateFirewallRule:
      description: Create firewall rule for your ip
      example:
        destination_port: 22
        source_ip: 1.2.4.5
        source_port: 1302
        protocol_id: 1
        xdp_action: 1
      properties:
        destination_port:
          default: 80
          example: 22
          type: integer
        source_ip:
          default: 0.0.0.0
          description: Source IP address to match. Use '0.0.0.0' to match any source.
          example: 1.2.4.5
          type: string
        source_port:
          default: 0
          example: 1302
          type: integer
        protocol_id:
          description: "1 = TCP, 2 = UDP"
          enum:
          - 1
          - 2
          example: 1
          type: integer
        xdp_action:
          description: "1 = Block,  0 = Whitelist"
          enum:
          - 0
          - 1
          example: 1
          type: integer
      required:
      - protocol_id
      - xdp_action
      title: Create Firewall Rule
      type: object
    CreateGeoFirewallRule:
      description: Create firewall rule for your ip
      example:
        destination_port: 22
        country_code: 10
        asn: 1331
        xdp_action: 1
      properties:
        destination_port:
          default: 80
          example: 22
          type: integer
        country_code:
          description: To get country code refer our countries api - https://my.interserver.net/apiv2/account/countries?fetch_by=numcode
          example: 10
          type: integer
        asn:
          description: ASN number
          example: 1331
          type: integer
        xdp_action:
          description: "1 = Block,  0 = Whitelist"
          enum:
          - 0
          - 1
          example: 1
          type: integer
      required:
      - xdp_action
      title: Create Firewall Rule
      type: object
    CreateFilter:
      description: Create firewall rule for your ip
      example:
        filter_type: cs2_udp
        port: 8080
      properties:
        filter_type:
          example: cs2_udp
          type: string
        port:
          example: 8080
          type: integer
      required:
      - filter_type
      - port
      title: Create Filter
      type: object
    ScrubIpPlaceOrder:
      description: Place ScrubIP Order
      example:
        service_type: 102
        ip: 1.2.3.4
      properties:
        service_type:
          example: 102
          type: integer
        ip:
          example: 1.2.3.4
          type: string
      required:
      - ip
      - service_type
      title: Place ScrubIP Order
      type: object
    BuyItNowList:
      description: Marketplace buy it now servers list
      items:
        $ref: "#/components/schemas/BuyItNowRow"
      title: Marketpalce servers list
      type: array
    BuyItNowRow:
      description: "A buy-it-now dedicated server listing from the marketplace, with\
        \ hardware specs and pricing."
      example:
        server_id: "11432"
        cpu:
        - BuyItNowRow_cpu_inner
        - BuyItNowRow_cpu_inner
        memory: 64GB
        disk:
          key: disk
        bandwidth: 1Gbps Unmetered
        ips: 1 Vlan Ip (/30)
        location: NYC Region
        price: 64
      properties:
        server_id:
          description: Unique server ID. Use this as `server_id` in `POST /servers/order/buy_now_server`.
          example: "11432"
          type: string
        cpu:
          items:
            $ref: "#/components/schemas/BuyItNowRow_cpu_inner"
          type: array
        memory:
          example: 64GB
          type: string
        disk:
          additionalProperties:
            type: string
          type: object
        bandwidth:
          example: 1Gbps Unmetered
          type: string
        ips:
          example: 1 Vlan Ip (/30)
          type: string
        location:
          example: NYC Region
          type: string
        price:
          example: 64
          type: integer
      type: object
    StatusMonthlyBreakdown:
      description: "Monthly mail delivery status breakdown, showing counts per status\
        \ category."
      example:
        default: null
        failed: null
        rejected: null
        pending: null
        locked: null
        paid: null
      properties:
        default:
          $ref: "#/components/schemas/MonthlyCounts"
        failed:
          $ref: "#/components/schemas/MonthlyCounts"
        rejected:
          $ref: "#/components/schemas/MonthlyCounts"
        pending:
          $ref: "#/components/schemas/MonthlyCounts"
        locked:
          $ref: "#/components/schemas/MonthlyCounts"
        paid:
          $ref: "#/components/schemas/MonthlyCounts"
      required:
      - default
      - failed
      - locked
      - paid
      - pending
      - rejected
      type: object
    MonthlyCounts:
      additionalProperties:
        type: integer
      description: "A map of month labels (e.g., \"2024-01\") to counts for a specific\
        \ mail status."
      type: object
    DomainNameserverGetResponse:
      description: List of registered nameserver hosts with glue record metadata.
      items:
        $ref: "#/components/schemas/DomainNameserverGetResponse_inner"
      type: array
    DomainNameserverPostRequest:
      description: Payload for adding a registered nameserver (glue record).
      example:
        name: ns1.host.com
        ipAddress: 1.2.3.4Get
      properties:
        name:
          example: ns1.host.com
          format: hostname
          type: string
        ipAddress:
          example: 1.2.3.4Get
          format: ip
          type: string
      required:
      - ipAddress
      - name
      type: object
    DomainNameserverPutRequest:
      description: Payload for replacing the assigned nameserver list for a domain.
      example:
        nameserver:
        - ns1.example.com
        - ns1.example.com
      properties:
        nameserver:
          items:
            example: ns1.example.com
            type: string
          type: array
      required:
      - nameserver
      type: object
    RestoreRequest:
      description: Request data to trigger a restore from backup.
      example:
        backup: vps1234-backup
        password: mypaassword
      properties:
        backup:
          type: string
        password:
          type: string
      title: Root Type for RestoreRequest
      type: object
    WebsiteBackups:
      description: List of available backups for a webhosting service with their names
        and sizes.
      items:
        $ref: "#/components/schemas/WebsiteBackups_inner"
      type: array
    ServersBuyNowResponse:
      description: Success response after placing a buy-it-now dedicated server order.
      example:
        success: true
        text: Server order is placed.
        order_details:
          service_id: 1234
          invoice_id: 2342355
      properties:
        success:
          description: Whether the order was placed successfully.
          example: true
          type: boolean
        text:
          description: Human-readable status message.
          example: Server order is placed.
          type: string
        order_details:
          $ref: "#/components/schemas/ServersBuyNowResponse_order_details"
      type: object
    ServersBuyNowError:
      description: Error response when a buy-it-now server order fails validation.
      example:
        success: false
        text: Unable to place order.
        errors:
        - Server ID is missing.
        - Server Hostname is missing.;
        - "Password must contain atleast 8 characters, one lowercase letter, one uppercase\
          \ letter, one number & one special character"
      properties:
        success:
          description: Always false for error responses.
          example: false
          type: boolean
        text:
          description: Human-readable error summary.
          example: Unable to place order.
          type: string
        errors:
          description: List of specific validation error messages.
          example:
          - Server ID is missing.
          - Server Hostname is missing.;
          - "Password must contain atleast 8 characters, one lowercase letter, one\
            \ uppercase letter, one number & one special character"
          items:
            type: string
          type: array
      type: object
    getAccountInfo_401_response:
      example:
        code: 0
        message: message
      properties:
        code:
          type: integer
        message:
          type: string
      required:
      - code
      - message
      type: object
    getAccountTfaSetup_200_response:
      example:
        "2fa_google_key": 2fa_google_key
        "2fa_google_split": 2fa_google_split
      properties:
        "2fa_google_key":
          description: Base64-encoded secret key for TOTP setup.
          type: string
        "2fa_google_split":
          description: Human-readable formatted key (chunks of 4 characters).
          type: string
      type: object
    updateAccountTfa_request:
      properties:
        "2fa_google_code":
          description: The 6-digit verification code from your authenticator app.
          type: string
      required:
      - 2fa_google_code
      type: object
    getAffiliateSignups_200_response:
      example:
        data: "{}"
      properties:
        data:
          description: Affiliate signup statistics and client-side data.
          type: object
      type: object
    cancelBackup_200_response:
      example:
        success: true
        text: Backups is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    patchBillingCreditCardVerify_request:
      properties:
        cc_ccv2:
          description: The CVV/CVC code on the back of the credit card.
          type: string
      required:
      - cc_ccv2
      type: object
    CancelDomain_200_response:
      example:
        success: true
        text: Domains is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    floating_ipsCancel_200_response:
      example:
        success: true
        text: Floating IPs is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    licensesCancel_200_response:
      example:
        success: true
        text: License is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    getOauthRedirect_200_response:
      example:
        redirect_url: redirect_url
      properties:
        redirect_url:
          description: The URL to redirect the user to for OAuth authentication.
          type: string
      type: object
    postOauthCallback_request:
      properties:
        provider:
          description: The OAuth provider name (e.g. `Google`).
          type: string
      type: object
    postOauthCallback_200_response:
      example:
        login: true
        signup: true
        linked: true
        account_id: 0
        error_code: error_code
      properties:
        login:
          description: Whether the user was logged in to an existing account.
          type: boolean
        signup:
          description: Whether a new account was created.
          type: boolean
        linked:
          description: Whether the OAuth provider was linked to an existing account.
          type: boolean
        account_id:
          description: The account ID associated with the OAuth login.
          type: integer
        error_code:
          description: Error code if additional verification is needed (e.g. `2fa_required`).
          type: string
      type: object
    patchOauthTwoFactor_request:
      properties:
        account_id:
          description: The account ID returned from the POST callback.
          type: integer
        code:
          description: The 6-digit two-factor authentication code.
          type: string
      required:
      - account_id
      - code
      type: object
    patchOauthTwoFactor_200_response:
      example:
        login: true
      properties:
        login:
          description: Whether the 2FA verification succeeded and the user is now
            logged in.
          type: boolean
      type: object
    mailCancel_200_response:
      example:
        success: true
        text: Mail is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    deleteMailAlert_request:
      properties:
        alert_id:
          description: The ID of the alert to delete.
          type: integer
      required:
      - alert_id
      type: object
    initiatePayment_200_response:
      example:
        type: redirect
        redirect: redirect
        action: action
        method: method
        items: "{}"
        text: text
      properties:
        type:
          description: "The response type indicating how to handle the payment. Possible\
            \ values: `redirect` (redirect user to a URL), `submit` (submit a form\
            \ to a URL), `single` (immediate result)."
          enum:
          - redirect
          - submit
          - single
          type: string
        redirect:
          description: URL to redirect the user to for payment (when type is `redirect`).
          type: string
        action:
          description: Form action URL (when type is `submit`).
          type: string
        method:
          description: HTTP method for the form submission (when type is `submit`).
          type: string
        items:
          description: Form field name-value pairs to submit (when type is `submit`).
          type: object
        text:
          description: Status or result text.
          type: string
      type: object
    quickserversCancel_200_response:
      example:
        success: true
        text: Rapid Deploy Servers is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    downloadQsBackup_request:
      properties:
        file:
          description: The backup filename to download.
          type: string
      required:
      - file
      type: object
    downloadQsBackup_200_response:
      example:
        text: text
        url: url
      properties:
        text:
          type: string
        url:
          description: A pre-signed download URL valid for 24 hours.
          type: string
      type: object
    getScrubIpDetails_200_response_serviceInfo:
      properties:
        scrub_ip_id:
          type: string
        scrub_ip_type:
          type: string
        scrub_ip_custid:
          type: string
        scrub_ip_order_date:
          type: string
        scrub_ip_ip:
          type: string
        scrub_ip_service_id:
          type: string
        scrub_ip_service_module:
          type: string
        scrub_ip_status:
          enum:
          - active
          - pending
          - canceled
          - expired
          type: string
        scrub_ip_invoice:
          type: string
        scrub_ip_currency:
          type: string
        scrub_ip_coupon:
          type: string
        scrub_ip_comment:
          type: string
      type: object
    getScrubIpDetails_200_response_client_links_inner:
      properties:
        label:
          type: string
        link:
          type: string
        icon:
          type: string
        icon_text:
          type: string
        help_text:
          type: string
        other_attr:
          type: string
    getScrubIpDetails_200_response_billingDetails:
      properties:
        service_last_invoice_date:
          type: string
        service_payment_status:
          type: string
        service_frequency:
          type: string
        next_date:
          type: string
        service_next_invoice_date:
          type: string
        service_currency:
          type: string
        service_currency_symbol:
          type: string
        service_cost_info:
          type: string
      type: object
    getScrubIpDetails_200_response_extraInfoTables_scrub_ips_rows_inner:
      properties:
        desc:
          type: string
        value:
          type: string
    getScrubIpDetails_200_response_extraInfoTables_scrub_ips:
      properties:
        title:
          type: string
        rows:
          items:
            $ref: "#/components/schemas/getScrubIpDetails_200_response_extraInfoTables_scrub_ips_rows_inner"
          type: array
      type: object
    getScrubIpDetails_200_response_extraInfoTables:
      properties:
        scrub_ips:
          $ref: "#/components/schemas/getScrubIpDetails_200_response_extraInfoTables_scrub_ips"
      type: object
    getScrubIpDetails_200_response_filter_firewall_rules_inner:
      properties:
        id:
          type: string
        source_ip:
          type: string
        destination_ip:
          type: string
        protocol_id:
          type: string
        source_port:
          type: string
        destination_port:
          type: string
        xdp_action:
          type: string
        global_drop:
          type: string
    getScrubIpDetails_200_response_filter_firewall_filters_inner:
      properties:
        daddr:
          type: string
        dest:
          type: string
        filter_name:
          type: string
        destination_ip:
          type: string
        filter:
          type: string
    getScrubIpDetails_200_response_filter_firewall:
      properties:
        rules:
          items:
            $ref: "#/components/schemas/getScrubIpDetails_200_response_filter_firewall_rules_inner"
          type: array
        filters:
          items:
            $ref: "#/components/schemas/getScrubIpDetails_200_response_filter_firewall_filters_inner"
          type: array
        scrub_enabled:
          type: integer
      type: object
    getScrubIpDetails_200_response:
      example:
        serviceInfo:
          scrub_ip_id: "123"
          scrub_ip_type: "11552"
          scrub_ip_custid: "456"
          scrub_ip_order_date: 2025-12-26 08:24:02
          scrub_ip_ip: 11.24.11.23
          scrub_ip_service_id: "11111"
          scrub_ip_service_module: servers
          scrub_ip_status: active
          scrub_ip_invoice: "654321"
          scrub_ip_currency: USD
          scrub_ip_coupon: "0"
          scrub_ip_comment: ""
        client_links:
        - label: Invoices
          link: invoices
          icon: fas fa-file-invoice-dollar fa-w-12
          icon_text: ""
          help_text: Invoice History
        - label: Cancel Scrub IPs
          link: cancel
          icon: fas fa-times
          icon_text: ""
          help_text: Cancel Scrub IPs
        - label: Disable Scrub
          link: scrub_action
          icon: fa fa-shield text-lg
          icon_text: ""
          help_text: Enable/Disable Scrub
          other_attr: ""
        - label: Scrub Documentation
          link: https://www.interserver.net/tips/kb/scrub/
          icon: fa fa-file text-lg
          icon_text: ""
          help_text: Scrub Documentation
          other_attr: target= "_blank"
        billingDetails:
          service_last_invoice_date: "December 26, 2025"
          service_payment_status: Paid
          service_frequency: Monthly
          next_date: 2026-01-26 08:24:02
          service_next_invoice_date: "January 26, 2026"
          service_currency: USD
          service_currency_symbol: $
          service_cost_info: "5.00"
        custCurrency: USD
        custCurrencySymbol: $
        package: Current IP + Scrub
        extraInfoTables:
          scrub_ips:
            title: Connection Information
            rows:
            - desc: IP
              value: 11.12.12.12
            - desc: Scrub
              value: Enabled
        filter_firewall:
          rules: []
          filters:
          - daddr: "2331742347"
            dest: "80"
            filter_name: dns
            destination_ip: 11.12.12.12
            filter: Dns
          - daddr: "2331742347"
            dest: "443"
            filter_name: dns
            destination_ip: 11.12.12.12
            filter: Dns
          scrub_enabled: 21104
      properties:
        serviceInfo:
          $ref: "#/components/schemas/getScrubIpDetails_200_response_serviceInfo"
        client_links:
          items:
            $ref: "#/components/schemas/getScrubIpDetails_200_response_client_links_inner"
          type: array
        billingDetails:
          $ref: "#/components/schemas/getScrubIpDetails_200_response_billingDetails"
        custCurrency:
          type: string
        custCurrencySymbol:
          type: string
        package:
          type: string
        extraInfoTables:
          $ref: "#/components/schemas/getScrubIpDetails_200_response_extraInfoTables"
        filter_firewall:
          $ref: "#/components/schemas/getScrubIpDetails_200_response_filter_firewall"
      type: object
    cancelScrubIp_200_response:
      example:
        success: true
        text: Scrub Ips is canceled.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: Scrub Ips is canceled.
          type: string
      required:
      - success
      - text
      type: object
    enableScrub_200_response:
      example:
        success: true
        text: Scrub is enabled on your IP.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: Scrub is enabled on your IP.
          type: string
      required:
      - success
      - text
      type: object
    enableScrub_500_response:
      example:
        success: false
        text: Unable to enable Scrub on your IP.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to enable Scrub on your IP.
          type: string
      required:
      - success
      - text
      type: object
    disableScrub_200_response:
      example:
        success: true
        text: Scrub is disabled on your IP.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: Scrub is disabled on your IP.
          type: string
      required:
      - success
      - text
      type: object
    disableScrub_400_response:
      example:
        success: false
        text: Scrub is not enabled in this service.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Scrub is not enabled in this service.
          type: string
      required:
      - success
      - text
      type: object
    disableScrub_500_response:
      example:
        success: false
        text: Unable to disable scrub on your IP.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to disable scrub on your IP.
          type: string
      required:
      - success
      - text
      type: object
    createRule_201_response:
      example:
        success: true
        text: New firewall Rule has been created.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: New firewall Rule has been created.
          type: string
      type: object
    createRule_400_response:
      example:
        success: false
        text: Bad Request
        errors:
        - Source IP is not valid
        - Source IP is not valid
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Bad Request
          type: string
        errors:
          items:
            example: Source IP is not valid
            type: string
          type: array
      type: object
    createRule_500_response:
      example:
        success: false
        text: Unable to create new firewall Rule.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to create new firewall Rule.
          type: string
      type: object
    Delete_Firewall_Rule:
      description: Delete firewall rule for your ip
      properties:
        rule_id:
          example: 2045
          type: integer
      required:
      - rule_id
      title: Delete Firewall Rule
      type: object
    scrubIpsDeleteRule_200_response:
      example:
        success: true
        text: Firewall Rule has been deleted.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: Firewall Rule has been deleted.
          type: string
      type: object
    scrubIpsDeleteRule_400_response:
      example:
        success: false
        text: Invalid rule id
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Invalid rule id
          type: string
      type: object
    scrubIpsDeleteRule_500_response:
      example:
        success: false
        text: Unable to deleted firewall rule.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to deleted firewall rule.
          type: string
      type: object
    createGeoRule_400_response:
      example:
        success: false
        text: Bad Request
        errors:
        - Country or Asn is required.
        - Country or Asn is required.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Bad Request
          type: string
        errors:
          items:
            example: Country or Asn is required.
            type: string
          type: array
      type: object
    Delete_Geo_Firewall_Rule:
      description: Delete geo firewall rule for your scrub ip
      properties:
        rule_id:
          example: 2045
          type: integer
      required:
      - rule_id
      title: Delete Geo Firewall Rule
      type: object
    createFilter_201_response:
      example:
        success: true
        text: New filter has been created.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: New filter has been created.
          type: string
      type: object
    createFilter_400_response:
      example:
        success: false
        text: Bad Request
        errors:
        - Filter type is invalid
        - Filter type is invalid
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Bad Request
          type: string
        errors:
          items:
            example: Filter type is invalid
            type: string
          type: array
      type: object
    createFilter_500_response:
      example:
        success: false
        text: Unable to create new filter.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to create new filter.
          type: string
      type: object
    deleteFilter_200_response:
      example:
        success: true
        text: Filter is deleted.
      properties:
        success:
          example: true
          type: boolean
        text:
          example: Filter is deleted.
          type: string
      type: object
    deleteFilter_400_response:
      example:
        success: false
        text: Port is required.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Port is required.
          type: string
      type: object
    deleteFilter_500_response:
      example:
        success: false
        text: Unable to delete filter.
      properties:
        success:
          example: false
          type: boolean
        text:
          example: Unable to delete filter.
          type: string
      type: object
    getOrderDetail_200_response_packageCosts:
      example:
        package_id: 11552
        package_cost: 5
        currency: USD
        currencySymbol: $
      properties:
        package_id:
          example: 11552
          type: number
        package_cost:
          example: 5
          type: number
        currency:
          example: USD
          type: string
        currencySymbol:
          example: $
          type: string
      type: object
    getOrderDetail_200_response_serviceTypes_inner:
      example:
        services_id: 11552
        services_name: Current IP + Scrub
        services_cost: 5
        services_field1: ""
        services_field2: ""
        services_module: scrub_ips
      properties:
        services_id:
          example: 11552
          type: integer
        services_name:
          example: Current IP + Scrub
          type: string
        services_cost:
          example: 5
          type: integer
        services_field1:
          example: ""
          type: string
        services_field2:
          example: ""
          type: string
        services_module:
          example: scrub_ips
          type: string
    getOrderDetail_200_response_ips_inner:
      example:
        service_id: 12345
        service_module: vps
        service_hostname: server.gtest.com
      properties:
        service_id:
          example: 12345
          type: number
        service_module:
          example: vps
          type: string
        service_hostname:
          example: server.gtest.com
          type: string
    getOrderDetail_200_response:
      example:
        packageCosts:
          package_id: 11552
          package_cost: 5
          currency: USD
          currencySymbol: $
        serviceTypes:
        - services_id: 11552
          services_name: Current IP + Scrub
          services_cost: 5
          services_field1: ""
          services_field2: ""
          services_module: scrub_ips
        - services_id: 11552
          services_name: Current IP + Scrub
          services_cost: 5
          services_field1: ""
          services_field2: ""
          services_module: scrub_ips
        ips:
        - service_id: 12345
          service_module: vps
          service_hostname: server.gtest.com
        - service_id: 12345
          service_module: vps
          service_hostname: server.gtest.com
      properties:
        packageCosts:
          $ref: "#/components/schemas/getOrderDetail_200_response_packageCosts"
        serviceTypes:
          items:
            $ref: "#/components/schemas/getOrderDetail_200_response_serviceTypes_inner"
          type: array
        ips:
          items:
            $ref: "#/components/schemas/getOrderDetail_200_response_ips_inner"
          type: array
      type: object
    putScrubIps_200_response:
      example:
        continue: true
        errors:
        - errors
        - errors
        serviceType: 0
        serviceCost: 6.027456183070403
        originalCost: 1.4658129805029452
        repeatServiceCost: 5.962133916683182
      properties:
        continue:
          type: boolean
        errors:
          items:
            type: string
          type: array
        serviceType:
          type: integer
        serviceCost:
          type: number
        originalCost:
          type: number
        repeatServiceCost:
          type: number
      type: object
    placeScrubOrder_201_response_order_details_cj_params:
      example:
        containerTagId: 1684
        CID: 2314
        OID: scrub_ips12424
        TYPE: 2242343242
        ITEM1: scrub_ips904
        AMT1: 5
        QTY1: 1
        CURRENCY: USD
      properties:
        containerTagId:
          example: 1684
          type: integer
        CID:
          example: 2314
          type: integer
        OID:
          example: scrub_ips12424
          type: string
        TYPE:
          example: 2242343242
          type: integer
        ITEM1:
          example: scrub_ips904
          type: string
        AMT1:
          example: 5
          type: integer
        QTY1:
          example: 1
          type: integer
        CURRENCY:
          example: USD
          type: string
      type: object
    placeScrubOrder_201_response_order_details:
      example:
        total_cost: 5
        service_id: 12346
        invoice_id: 2746273
        invoice_description: Scrub + Current Ip
        cj_params:
          containerTagId: 1684
          CID: 2314
          OID: scrub_ips12424
          TYPE: 2242343242
          ITEM1: scrub_ips904
          AMT1: 5
          QTY1: 1
          CURRENCY: USD
      properties:
        total_cost:
          example: 5
          type: integer
        service_id:
          example: 12346
          type: integer
        invoice_id:
          example: 2746273
          type: integer
        invoice_description:
          example: Scrub + Current Ip
          type: string
        cj_params:
          $ref: "#/components/schemas/placeScrubOrder_201_response_order_details_cj_params"
      type: object
    placeScrubOrder_201_response:
      example:
        success: true
        text: ScrubIp order is placed.
        order_details:
          total_cost: 5
          service_id: 12346
          invoice_id: 2746273
          invoice_description: Scrub + Current Ip
          cj_params:
            containerTagId: 1684
            CID: 2314
            OID: scrub_ips12424
            TYPE: 2242343242
            ITEM1: scrub_ips904
            AMT1: 5
            QTY1: 1
            CURRENCY: USD
      properties:
        success:
          example: true
          type: boolean
        text:
          example: ScrubIp order is placed.
          type: string
        order_details:
          $ref: "#/components/schemas/placeScrubOrder_201_response_order_details"
      type: object
    addServer_200_response:
      example:
        text: Order Completed
        invoice: 0
        order: 6
      properties:
        text:
          description: Status message.
          example: Order Completed
          type: string
        invoice:
          description: Invoice ID for payment.
          type: integer
        order:
          description: Server order ID.
          type: integer
      type: object
    serversCancel_200_response:
      example:
        success: true
        text: Servers is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    sslCancel_200_response:
      example:
        success: true
        text: SSL is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    VPSCancel_200_response:
      example:
        success: true
        text: VPS is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    webhostingCancel_200_response:
      example:
        success: true
        text: Website is canceled.
      properties:
        success:
          type: boolean
        text:
          type: string
      required:
      - success
      - text
      type: object
    getWebsiteBuyIp_200_response:
      example:
        ips:
          key: ips
      properties:
        ips:
          additionalProperties:
            type: string
          description: A map of IP addresses to their current reverse DNS hostnames.
          type: object
      type: object
    postWebsiteBuyIp_request:
      properties:
        ips:
          additionalProperties:
            type: string
          description: A map of IP addresses to their desired reverse DNS hostnames.
          type: object
      type: object
    postWebsiteBuyIp_200_response:
      example:
        message: message
        success: true
      properties:
        message:
          type: string
        success:
          type: boolean
      type: object
    postWebsiteMigration_request:
      properties:
        custPortal:
          description: URL of the customer's current hosting portal.
          type: string
        regEmail:
          description: Registered email address at the current host.
          type: string
        password:
          description: Password for the current hosting account.
          type: string
        ctrlPanel:
          description: URL of the current control panel.
          type: string
        ftpUsername:
          description: FTP username at the current host.
          type: string
        ftpPassword:
          description: FTP password at the current host.
          type: string
        siteBusyMig:
          description: Information about site traffic during migration.
          type: string
        splReqMig:
          description: Special requirements for the migration.
          type: string
        domainReg:
          description: Whether domain registration assistance is needed.
          type: string
        dataMig:
          description: Data migration timing preference.
          type: string
        domainRegPortal:
          description: Domain registrar portal URL.
          type: string
        domainRegEmail:
          description: Email for the domain registrar account.
          type: string
        domainRegPassword:
          description: Password for the domain registrar account.
          type: string
      type: object
    postWebsiteMigration_request_1:
      properties:
        custPortal:
          type: string
        regEmail:
          type: string
        password:
          type: string
        ctrlPanel:
          type: string
        ftpUsername:
          type: string
        ftpPassword:
          type: string
        siteBusyMig:
          type: string
        splReqMig:
          type: string
        domainReg:
          type: string
        dataMig:
          type: string
        domainRegPortal:
          type: string
        domainRegEmail:
          type: string
        domainRegPassword:
          type: string
      type: object
    postWebsiteMigration_200_response:
      example:
        text: text
        ticket: 0
      properties:
        text:
          description: Confirmation message.
          type: string
        ticket:
          description: "The support ticket ID created for tracking the migration.\
            \ Use this with `/tickets/{id}` to check migration progress."
          type: integer
      type: object
    getAccountLocales_200_response_value:
      properties:
        name:
          description: English display name of the locale.
          type: string
        local_name:
          description: Display name of the locale in its own language.
          type: string
      type: object
    viewMailLog_startDate_parameter:
      oneOf:
      - format: int64
        maximum: 9999999999
        minimum: 0
        type: integer
      - type: string
    buyItNowServerOrder_200_response_bandwidth_inner:
      example:
        id: "10"
        short_desc: 1GBPS Unmetered
        long_desc: ""
        monthly_price: "0"
      properties:
        id:
          example: "10"
          type: string
        short_desc:
          example: 1GBPS Unmetered
          type: string
        long_desc:
          example: ""
          type: string
        monthly_price:
          example: "0"
          type: string
      type: object
    buyItNowServerOrder_200_response_ips_inner:
      example:
        id: "9"
        short_desc: 1 Vlan Ip (/30)
        long_desc: 1 IP In personal Vlan
        monthly_price: "0"
      properties:
        id:
          example: "9"
          type: string
        short_desc:
          example: 1 Vlan Ip (/30)
          type: string
        long_desc:
          example: 1 IP In personal Vlan
          type: string
        monthly_price:
          example: "0"
          type: string
      type: object
    buyItNowServerOrder_200_response_os_inner:
      example:
        id: "5"
        short_desc: FreeBSD
        long_desc: "Latest FreeBSD 6.x OS<br><pre>CP(s): cPanel/DirectAdmin</pre>"
        monthly_price: "0"
      properties:
        id:
          example: "5"
          type: string
        short_desc:
          example: FreeBSD
          type: string
        long_desc:
          example: "Latest FreeBSD 6.x OS<br><pre>CP(s): cPanel/DirectAdmin</pre>"
          type: string
        monthly_price:
          example: "0"
          type: string
      type: object
    buyItNowServerOrder_200_response_cp_inner:
      example:
        id: "1"
        short_desc: None
        long_desc: ""
        os_type: ""
        monthly_price: "0"
      properties:
        id:
          example: "1"
          type: string
        short_desc:
          example: None
          type: string
        long_desc:
          example: ""
          type: string
        os_type:
          example: ""
          type: string
        monthly_price:
          example: "0"
          type: string
      type: object
    buyItNowServerOrder_200_response_raid_inner:
      example:
        id: "0"
        short_desc: No Raid
        long_desc: No Raid
        monthly_price: "0"
      properties:
        id:
          example: "0"
          type: string
        short_desc:
          example: No Raid
          type: string
        long_desc:
          example: No Raid
          type: string
        monthly_price:
          example: "0"
          type: string
      type: object
    buyItNowServerOrder_200_response:
      example:
        bandwidth:
        - id: "10"
          short_desc: 1GBPS Unmetered
          long_desc: ""
          monthly_price: "0"
        - id: "10"
          short_desc: 1GBPS Unmetered
          long_desc: ""
          monthly_price: "0"
        ips:
        - id: "9"
          short_desc: 1 Vlan Ip (/30)
          long_desc: 1 IP In personal Vlan
          monthly_price: "0"
        - id: "9"
          short_desc: 1 Vlan Ip (/30)
          long_desc: 1 IP In personal Vlan
          monthly_price: "0"
        os:
        - id: "5"
          short_desc: FreeBSD
          long_desc: "Latest FreeBSD 6.x OS<br><pre>CP(s): cPanel/DirectAdmin</pre>"
          monthly_price: "0"
        - id: "5"
          short_desc: FreeBSD
          long_desc: "Latest FreeBSD 6.x OS<br><pre>CP(s): cPanel/DirectAdmin</pre>"
          monthly_price: "0"
        cp:
        - id: "1"
          short_desc: None
          long_desc: ""
          os_type: ""
          monthly_price: "0"
        - id: "1"
          short_desc: None
          long_desc: ""
          os_type: ""
          monthly_price: "0"
        raid:
        - id: "0"
          short_desc: No Raid
          long_desc: No Raid
          monthly_price: "0"
        - id: "0"
          short_desc: No Raid
          long_desc: No Raid
          monthly_price: "0"
      properties:
        bandwidth:
          items:
            $ref: "#/components/schemas/buyItNowServerOrder_200_response_bandwidth_inner"
          type: array
        ips:
          items:
            $ref: "#/components/schemas/buyItNowServerOrder_200_response_ips_inner"
          type: array
        os:
          items:
            $ref: "#/components/schemas/buyItNowServerOrder_200_response_os_inner"
          type: array
        cp:
          items:
            $ref: "#/components/schemas/buyItNowServerOrder_200_response_cp_inner"
          type: array
        raid:
          items:
            $ref: "#/components/schemas/buyItNowServerOrder_200_response_raid_inner"
          type: array
      type: object
    placeBuyNowServer_request:
      properties:
        server_id:
          description: The ID of the buy-it-now server configuration to order. Use
            the server listing from `GET /servers/order/buy_now_server` to find valid
            IDs.
          example: 2343
          type: number
        server_hostname:
          description: The fully-qualified hostname to assign to the server.
          example: server.int.com
          type: string
        server_root_password:
          description: The root or administrator password to set on the server.
          example: uD1c!@cgD
          type: string
      type: object
    AccountInfo_oauthproviders:
      oneOf:
      - items: {}
        type: array
      - $ref: "#/components/schemas/AccountInfoOauthConfigProviders"
    AccountInfoData_email_settings:
      properties:
        admin/cc_bad_response:
          type: string
        admin/mass_communications.tpl:
          type: string
      type: object
    AccountInfoData_extra:
      properties:
        private_whois:
          type: string
      type: object
    AccountInfoData_fraudrecord:
      properties:
        score:
          type: string
        count:
          type: string
        reliability:
          type: string
        code:
          type: string
      type: object
    AccountInfoLimits_inner:
      properties:
        start:
          type: string
        end:
          type: string
      type: object
    AccountInfoMaxMindResponse_riskScore:
      oneOf:
      - type: string
      - type: number
    AccountInfoOauthConfigProviders_value:
      properties:
        enabled:
          type: boolean
        linked:
          type: boolean
        account:
          type: string
        url:
          type: string
      type: object
    BackupsOrder_serviceTypes:
      example:
        "11006":
          services_id: "11006"
          services_name: Storage ST 700
          services_cost: "84.00"
          services_category: "702"
          services_buyable: "1"
          services_type: "703"
          services_field1: ""
          services_field2: "bandwidth=40000000,quota=40000000,inode=100,uinode=ON,vdomains=2,nsubdomains=5,nemails=0,nemailf=0,nemailml=0,nemailr=0,mysql=5,domainptr=5,ftp=100,aftp=OFF,cgi=ON,php=ON,spam=OFF,cron=OFF,catchall=OFF,ssl=ON,ssh=ON,sysinfo=ON,login_keys=ON,dnscontrol=OFF,suspend_at_limit=ON"
          services_module: backups
      properties:
        "11006":
          $ref: "#/components/schemas/BackupsOrderServiceTypes"
      type: object
    ChargeInvoiceRows_invoices_value_paid_invoices_value_refund_invoices_value:
      properties:
        invoices_id:
          type: number
        invoices_description:
          type: string
        invoices_amount:
          type: number
        invoices_date:
          type: string
        invoices_currency:
          type: string
        currency_symbol:
          type: string
        invoices_date_formatted:
          type: string
      type: object
    ChargeInvoiceRows_invoices_value_paid_invoices_value:
      properties:
        invoices_id:
          type: string
        invoices_description:
          type: string
        invoices_amount:
          type: number
        invoices_date:
          type: string
        invoices_currency:
          type: string
        currency_symbol:
          type: string
        invoices_date_formatted:
          type: string
        payment_type:
          type: string
        refund_invoices:
          additionalProperties:
            $ref: "#/components/schemas/ChargeInvoiceRows_invoices_value_paid_invoices_value_refund_invoices_value"
          description: This is optional when refund is present this will show
          type: object
      type: object
    ChargeInvoiceRows_invoices_value:
      properties:
        invoices_id:
          description: Unique invoice ID.
          type: number
        invoices_description:
          description: Description of the invoice charge.
          type: string
        invoices_amount:
          description: Invoice amount.
          type: number
        invoices_date:
          description: Invoice date.
          type: string
        invoices_paid:
          enum:
          - 0
          - 1
          type: number
        invoices_due_date:
          type: string
        invoices_currency:
          type: string
        currency_symbol:
          type: string
        invoices_date_formatted:
          type: string
        paid_invoices:
          additionalProperties:
            $ref: "#/components/schemas/ChargeInvoiceRows_invoices_value_paid_invoices_value"
          description: This is optional when invoices_paid = 1 this array will show
          type: object
      type: object
    DomainAllInfo_attributes_contact_set:
      example:
        owner:
          postal_code: postal_code
          org_name: org_name
          country: country
          status: status
          state: state
          first_name: first_name
          last_name: last_name
          address3: address3
          fax: fax
          address2: address2
          email: email
          city: city
          phone: phone
          address1: address1
        admin:
          status: status
          state: state
          org_name: org_name
          country: country
          postal_code: postal_code
          email: email
          fax: fax
          address2: address2
          address3: address3
          address1: address1
          city: city
          phone: phone
          first_name: first_name
          last_name: last_name
        tech:
          state: state
          status: status
          org_name: org_name
          country: country
          postal_code: postal_code
          fax: fax
          email: email
          address2: address2
          address3: address3
          address1: address1
          city: city
          phone: phone
          first_name: first_name
          last_name: last_name
      properties:
        owner:
          $ref: "#/components/schemas/DomainOwnerContact"
        admin:
          $ref: "#/components/schemas/DomainAdminContact"
        tech:
          $ref: "#/components/schemas/DomainTechContact"
      type: object
    DomainAllInfo_attributes:
      example:
        contact_set:
          owner:
            postal_code: postal_code
            org_name: org_name
            country: country
            status: status
            state: state
            first_name: first_name
            last_name: last_name
            address3: address3
            fax: fax
            address2: address2
            email: email
            city: city
            phone: phone
            address1: address1
          admin:
            status: status
            state: state
            org_name: org_name
            country: country
            postal_code: postal_code
            email: email
            fax: fax
            address2: address2
            address3: address3
            address1: address1
            city: city
            phone: phone
            first_name: first_name
            last_name: last_name
          tech:
            state: state
            status: status
            org_name: org_name
            country: country
            postal_code: postal_code
            fax: fax
            email: email
            address2: address2
            address3: address3
            address1: address1
            city: city
            phone: phone
            first_name: first_name
            last_name: last_name
        registry_createdate: registry_createdate
        registry_expiredate: registry_expiredate
        tld_data: tld_data
        let_expire: let_expire
        auto_renew: auto_renew
        sponsoring_rsp: sponsoring_rsp
        gdpr_consent_status: gdpr_consent_status
        nameserver_list:
        - sortorder: sortorder
          name: name
          ipaddress: ipaddress
        - sortorder: sortorder
          name: name
          ipaddress: ipaddress
        registry_updatedate: registry_updatedate
        affiliate_id: affiliate_id
        expiredate: expiredate
      properties:
        contact_set:
          $ref: "#/components/schemas/DomainAllInfo_attributes_contact_set"
        registry_createdate:
          type: string
        registry_expiredate:
          type: string
        tld_data:
          type: string
        let_expire:
          type: string
        auto_renew:
          type: string
        sponsoring_rsp:
          type: string
        gdpr_consent_status:
          type: string
        nameserver_list:
          items:
            $ref: "#/components/schemas/DomainNameServer"
          type: array
        registry_updatedate:
          type: string
        affiliate_id:
          type: string
        expiredate:
          type: string
      type: object
    DomainDnssecRecords_inner:
      example:
        algorithm: algorithm
        digest_type: digest_type
        digest: digest
        key_tag: key_tag
      properties:
        algorithm:
          type: string
        digest_type:
          type: string
        digest:
          type: string
        key_tag:
          type: string
      type: object
    DomainOrder_services:
      description: Available domain service catalog entries keyed by service ID.
      example:
        DomainOrderServices10001:
          services_id: "10001"
          services_name: .asia Domain Name Registration
          services_cost: "19.00"
          services_currency: USD
          services_category: "100"
          services_buyable: "1"
          services_type: "100"
          services_field1: .asia
          services_field2: ""
          services_module: domains
          services_hidden: "0"
      properties:
        DomainOrderServices10001:
          $ref: "#/components/schemas/DomainOrderServices10001"
      type: object
    DomainOrderResponse_attributes:
      example:
        id: id
        admin_email: admin_email
      properties:
        id:
          description: Registrar order ID.
          type: string
        admin_email:
          description: Administrative contact email provided for the order.
          type: string
      type: object
    DomainProvProcessPending_attributes:
      example:
        id: id
        order_id: order_id
        registration expiration date: registration expiration date
        f_auto_renew: f_auto_renew
      properties:
        id:
          type: string
        order_id:
          type: string
        registration expiration date:
          type: string
        f_auto_renew:
          type: string
      type: object
    Home_ticketStatus:
      description: Ticket statuses.
      example:
        Open: 4
        On Hold: 5
      properties:
        Open:
          description: Count of open tickets.
          example: 4
          type: integer
        On Hold:
          description: Count of tickets on hold.
          example: 5
          type: integer
      type: object
    Home_ticketStatusView:
      description: Ticket statuses with view numbers.
      example:
        "4": Open
        "5": On Hold
        "6": Closed
      properties:
        "4":
          description: Status corresponding to view number 4.
          example: Open
          type: string
        "5":
          description: Status corresponding to view number 5.
          example: On Hold
          type: string
        "6":
          description: Status corresponding to view number 6.
          example: Closed
          type: string
      type: object
    Home_details_modules:
      example:
        domains:
          icon: globe
          view_link: view_domain
          heading: Domains
          buy_link: domain_order
          list_link: view_domains_list
        webhosting:
          icon: window-maximize
          view_link: view_website
          heading: Web Hosting
          buy_link: order_website
          list_link: view_websites_list
        vps:
          icon: cloud-meatball
          view_link: view_vps
          heading: VPS
          buy_link: order_vps
          list_link: view_vps_list
        licenses:
          icon: id-card
          view_link: view_license
          heading: Licenses
          buy_link: order_license
          list_link: view_licenses_list
        backups:
          icon: warehouse
          view_link: view_backup
          heading: Storages
          buy_link: order_storage
          list_link: view_backups_list
        servers:
          icon: server
          view_link: view_server
          heading: Dedicated Servers
          buy_link: order_server
          list_link: view_servers_list
        quickservers:
          icon: database
          view_link: view_qs
          heading: Quick Servers
          buy_link: order_quickserver
          list_link: view_quickservers_list
      properties:
        domains:
          $ref: "#/components/schemas/HomeDetailsModulesDomains"
        webhosting:
          $ref: "#/components/schemas/HomeDetailsModulesWebhosting"
        vps:
          $ref: "#/components/schemas/HomeDetailsModulesVps"
        licenses:
          $ref: "#/components/schemas/HomeDetailsModulesLicenses"
        backups:
          $ref: "#/components/schemas/HomeDetailsModulesBackups"
        servers:
          $ref: "#/components/schemas/HomeDetailsModulesServers"
        quickservers:
          $ref: "#/components/schemas/HomeDetailsModulesQuickservers"
      type: object
    Home_details:
      description: Users details.
      example:
        modules:
          domains:
            icon: globe
            view_link: view_domain
            heading: Domains
            buy_link: domain_order
            list_link: view_domains_list
          webhosting:
            icon: window-maximize
            view_link: view_website
            heading: Web Hosting
            buy_link: order_website
            list_link: view_websites_list
          vps:
            icon: cloud-meatball
            view_link: view_vps
            heading: VPS
            buy_link: order_vps
            list_link: view_vps_list
          licenses:
            icon: id-card
            view_link: view_license
            heading: Licenses
            buy_link: order_license
            list_link: view_licenses_list
          backups:
            icon: warehouse
            view_link: view_backup
            heading: Storages
            buy_link: order_storage
            list_link: view_backups_list
          servers:
            icon: server
            view_link: view_server
            heading: Dedicated Servers
            buy_link: order_server
            list_link: view_servers_list
          quickservers:
            icon: database
            view_link: view_qs
            heading: Quick Servers
            buy_link: order_quickserver
            list_link: view_quickservers_list
      properties:
        modules:
          $ref: "#/components/schemas/Home_details_modules"
      type: object
    Home_services_domains:
      example:
        links:
          "376114": pimpmy.website
          "376503": hostingenuity.com
          "592337": detain.dev
        count: 5
      properties:
        links:
          $ref: "#/components/schemas/HomeServicesDomainsLinks"
        count:
          description: Number of domains.
          example: 5
          type: integer
      type: object
    Home_services_webhosting:
      example:
        links:
          "376359": Doefamily.com
          "376473": fancytush.com
          "386218": admincoded.net
        count: 8
      properties:
        links:
          $ref: "#/components/schemas/HomeServicesWebhostingLinks"
        count:
          description: Number of web hosting services.
          example: 8
          type: integer
      type: object
    Home_services_vps:
      example:
        links:
          "465503": vps465503
          "2500081": vps2500081
          "2578866": vps2578866
        count: 20
      properties:
        links:
          $ref: "#/components/schemas/HomeServicesVpsLinks"
        count:
          description: Number of VPS services.
          example: 20
          type: integer
      type: object
    Home_services_licenses:
      example:
        links:
          "386522": 1.2.3.4
        count: 1
      properties:
        links:
          $ref: "#/components/schemas/HomeServicesLicensesLinks"
        count:
          description: Number of licenses.
          example: 1
          type: integer
      type: object
    Home_services_servers:
      example:
        links:
          "16058": anotherserver.com
        count: 1
      properties:
        links:
          $ref: "#/components/schemas/HomeServicesServersLinks"
        count:
          description: Number of servers.
          example: 1
          type: integer
      type: object
    Home_services_backups:
      example:
        links:
        - links
        - links
        count: 0
      properties:
        links:
          description: List of backup links.
          items:
            type: string
          type: array
        count:
          description: Number of backups.
          example: 0
          type: integer
      type: object
    Home_services:
      description: Users services.
      example:
        domains:
          links:
            "376114": pimpmy.website
            "376503": hostingenuity.com
            "592337": detain.dev
          count: 5
        webhosting:
          links:
            "376359": Doefamily.com
            "376473": fancytush.com
            "386218": admincoded.net
          count: 8
        vps:
          links:
            "465503": vps465503
            "2500081": vps2500081
            "2578866": vps2578866
          count: 20
        licenses:
          links:
            "386522": 1.2.3.4
          count: 1
        servers:
          links:
            "16058": anotherserver.com
          count: 1
        backups:
          links:
          - links
          - links
          count: 0
      properties:
        domains:
          $ref: "#/components/schemas/Home_services_domains"
        webhosting:
          $ref: "#/components/schemas/Home_services_webhosting"
        vps:
          $ref: "#/components/schemas/Home_services_vps"
        licenses:
          $ref: "#/components/schemas/Home_services_licenses"
        servers:
          $ref: "#/components/schemas/Home_services_servers"
        backups:
          $ref: "#/components/schemas/Home_services_backups"
      type: object
    License_extraInfoTables:
      example:
        ip_info:
          title: IP Information
          rows:
          - desc: Netmask
            value: 255.255.255.248
          - desc: Netmask
            value: 255.255.255.248
      properties:
        ip_info:
          $ref: "#/components/schemas/LicenseIpInfo"
      type: object
    LicensesOrder_serviceCategories:
      description: License service categories
      example:
        LicensesOrderServiceCategories509:
          category_id: "509"
          category_name: Webuzo
          category_tag: webuzo
          category_module: licenses
      properties:
        LicensesOrderServiceCategories509:
          $ref: "#/components/schemas/LicensesOrderServiceCategories509"
      type: object
    LicensesOrder_packageCosts:
      description: Costs of license packages
      example:
        LicensesOrderPackageCosts11468: 3.75
      properties:
        LicensesOrderPackageCosts11468:
          description: Cost of package with ID 11468
          example: 3.75
          type: number
      type: object
    LicensesOrder_serviceTypes:
      description: Types of license services
      example:
        LicensesOrderServiceTypes11482:
          services_id: "11482"
          services_name: DirectAdmin Standard
          services_cost: "21.75"
          services_category: "506"
          services_buyable: "1"
          services_type: "506"
          services_field1: "2704,Standard"
          services_field2: Unlimited Accounts & Domains<br><br>The DirectAdmin standard
            license is best for medium to large shared-hosting and/or reseller-hosting
            companies. No limit on accounts or domains.
          services_module: licenses
      properties:
        LicensesOrderServiceTypes11482:
          $ref: "#/components/schemas/LicensesOrderServiceTypes11482"
      type: object
    LoginSubmissionExample_g_recaptcha_response_dep:
      properties:
        w:
          format: int32
          type: integer
        "n":
          format: int32
          type: integer
      type: object
    LoginSubmissionExample_g_recaptcha_response:
      properties:
        __v_isShallow:
          type: boolean
        dep:
          $ref: "#/components/schemas/LoginSubmissionExample_g_recaptcha_response_dep"
        __v_isRef:
          type: boolean
        _rawValue:
          type: string
        _value:
          type: string
      type: object
    MailSchema_extraInfoTables:
      description: Additional information tables for the mail service.
      example:
        mail:
          title: Connection Information
          rows:
          - desc: SMTP Server
            value: relay.mailbaby.net
          - desc: SMTP Server
            value: relay.mailbaby.net
        tutorials:
          title: Tutorials
          rows:
          - desc: cPanel Tutorial
            value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
              Here</a>
          - desc: cPanel Tutorial
            value: <a class="link" href="https://mail.baby/cpanel/" target="_blank">Click
              Here</a>
      properties:
        mail:
          $ref: "#/components/schemas/MailExtraInfoTable"
        tutorials:
          $ref: "#/components/schemas/MailTutorialsTable"
      type: object
    MailAlertsResponse_inner:
      example:
        alert_id: 0
        alert_type: alert_type
        alert_value: alert_value
        alert_to: alert_to
        alert_enabled: alert_enabled
      properties:
        alert_id:
          type: integer
        alert_type:
          type: string
        alert_value:
          type: string
        alert_to:
          type: string
        alert_enabled:
          type: string
      type: object
    Quickserver_serviceExtra:
      example:
        platform: kvm
      properties:
        platform:
          description: Platform information
          example: kvm
          type: string
      type: object
    Quickserver_extraInfoTables:
      example:
        ip_info:
          title: IP Information
          rows:
          - desc: Netmask
            value: ""
          - desc: Netmask
            value: ""
        addons:
          title: Addons
          rows:
          - desc: Additional IP Address
            value: ""
          - desc: Additional IP Address
            value: ""
      properties:
        ip_info:
          $ref: "#/components/schemas/QuickserverIpInfo"
        addons:
          $ref: "#/components/schemas/QuickserverAddons"
      type: object
    QuickserverOrder_server_details:
      description: Server details.
      example:
        "381":
          cpu: AMD Ryzen 9 5900X 12-Core Processor
          ram: 119GB
          hd: 1760GB
          cores: 24
          cost: $140.00
      properties:
        "381":
          $ref: "#/components/schemas/QuickserverOrderServerDetails381"
      type: object
    QuickserverOrder_templates:
      description: Templates details.
      example:
        Ubuntu:
        - - ubuntu24
          - ubuntu24
        - - ubuntu24
          - ubuntu24
      properties:
        Ubuntu:
          $ref: "#/components/schemas/QuickserverOrderTemplatesUbuntu64"
      type: object
    QuickserverOrder_version:
      description: Version details.
      example:
        centosstream-8:
          centosstream-8: 8 Stream (64 bits)
      properties:
        centosstream-8:
          $ref: "#/components/schemas/QuickserverOrderVersionCentosstream8"
      type: object
    QuickserverOrder_distro_sel:
      description: Distribution selection.
      example:
        Ubuntu:
          Ubuntu: Ubuntu
      properties:
        Ubuntu:
          $ref: "#/components/schemas/QuickserverOrderDistroSelUbuntu"
      type: object
    ScrubIpFilterTypes_filters_value:
      example:
        name: name
        desc: desc
      properties:
        name:
          type: string
        desc:
          type: string
      type: object
    Server_locations:
      example:
        "1":
          location_id: 0
          location_name: location_name
          location_description: location_description
          location_lat: location_lat
          location_long: location_long
          location_ipmi_group: 6
      properties:
        "1":
          $ref: "#/components/schemas/ServerLocation1"
      type: object
    ServerNetworkInfo_assets:
      description: Object containing asset information.
      example:
        "3497":
          id: 3497
          order_id: "16058"
          hostname: myserver.host.com
          status: active
          primary_ipv4: 1.2.3.250
          primary_ipv6: ""
          mac: mac
          datacenter: "2"
          type_id: "1"
          asset_tag: ""
          rack: "68"
          row: "017"
          col: "06"
          unit_start: "37"
          unit_end: "37"
          unit_sub: "0"
          ipmi_mac: 0c:c4:7a:af:35:00
          ipmi_ip: 10.8.69.7
          ipmi_admin_username: ipmi_admin_username
          ipmi_admin_password: ipmi_admin_password
          ipmi_client_username: ipmi_client_username
          ipmi_client_password: ipmi_client_password
          ipmi_updated: ipmi_updated
          ipmi_working: "0"
          company: int
          comments: ""
          make: Supermicro
          model: SYS-6018R-TDW
          description: ""
          customer_id: int5377
          external_id: ""
          billing_status: active
          overdue: "0"
          create_timestamp: create_timestamp
          update_timestamp: update_timestamp
          asset_id: "1"
          asset_name: server
          rack_id: "68"
          rack_name: "112.16"
          rack_location: "2"
          rack_size: "44"
          rack_x: "25"
          rack_y: "5"
          comment: comment
          switchports:
          - 10414
          vlans: []
          vlans6: []
          lease:
            mac: 0c:c4:7a:af:35:00
            authenticated: false
            group: "1"
      properties:
        "3497":
          $ref: "#/components/schemas/ServerAsset"
      required:
      - "3497"
      type: object
    ServerNetworkInfo_switchports:
      description: Object containing switchport information.
      example:
        "10414":
          switchport_id: 10414
          switch_id: "118"
          switch: edge1
          port: Ethernet1/33
          blade: Ethernet1
          justport: "33"
          graph_id: "12622"
          vlans: []
          vlans6: []
          asset_id: 3497
      properties:
        "10414":
          $ref: "#/components/schemas/ServerSwitchport"
      required:
      - "10414"
      type: object
    ServerOrder_cpu_li:
      description: CPU options for the server order.
      example:
        "254":
          id: "254"
          price: 0
          img: ryzen.png
          short_desc: AMD RYZEN 7900X
          long_desc: High core and thread count...
          location: New York
          fsb: fsb
          manu: manu
          type: AMD
          speed: "4.7"
          cache: cache
          active: "1"
          num_cores: "12"
          num_cpus: "1"
          benchmark: "121148"
          monthly_price: 75
          max_ram: "128"
          min_ram: "128"
          max_lff: "4"
          max_sff: "4"
          max_nve: "2"
          visible: "yes"
          hd_ids: hd_ids
          price_display: $0.00
          monthly_price_display: $75.00
      properties:
        "254":
          $ref: "#/components/schemas/ServerOrderCPU"
      type: object
    ServerOrder_memory_li_254:
      example:
        "65":
          id: "65"
          price: "0"
          img: hd.jpg
          short_desc: 20TB SATA
          long_desc: long_desc
          manu: manu
          size: "3000"
          type: type
          hidden: "0"
          monthly_price: 50
          drive_type: lff
          monthly_price_display: $50.00
      properties:
        "65":
          $ref: "#/components/schemas/ServerOrderMemory"
      type: object
    ServerOrder_memory_li:
      description: Memory options for the server order.
      example:
        "254":
          "65":
            id: "65"
            price: "0"
            img: hd.jpg
            short_desc: 20TB SATA
            long_desc: long_desc
            manu: manu
            size: "3000"
            type: type
            hidden: "0"
            monthly_price: 50
            drive_type: lff
            monthly_price_display: $50.00
      properties:
        "254":
          $ref: "#/components/schemas/ServerOrder_memory_li_254"
      type: object
    ServerOrder_bandwidth_li:
      description: Bandwidth options for the server order.
      example:
        "15":
          id: "15"
          price: 0
          img: bandwidth.jpg
          short_desc: 150TB (10Gb Port)
          long_desc: ' - 2000GB Bandwidth...'
          type: Standard
          qty: "4000"
          active: "1"
          monthly_price: 15
          price_display: $0.00
          monthly_price_display: $15.00
      properties:
        "15":
          $ref: "#/components/schemas/ServerOrderBandwidth"
      type: object
    ServerOrder_ips_li:
      description: IP options for the server order.
      example:
        "9":
          id: "9"
          price: 0
          img: ips.jpg
          short_desc: 1 Vlan Ip (/30)
          long_desc: 1 IP In personal Vlan
          qty: "1"
          monthly_price: 0
          price_display: $0.00
          monthly_price_display: $0.00
      properties:
        "9":
          $ref: "#/components/schemas/ServerOrderIP"
      type: object
    ServerOrder_os_li:
      description: Operating System options for the server order.
      example:
        "51":
          id: "51"
          price: 0
          img: img
          short_desc: AlmaLinux
          long_desc: long_desc
          monthly_price: 0
          active: "1"
          price_display: $0.00
          monthly_price_display: $0.00
      properties:
        "51":
          $ref: "#/components/schemas/ServerOrderOS"
      type: object
    ServerOrder_cp_li:
      description: Control Panel options for the server order.
      example:
        "9":
          id: "9"
          price: 80
          img: cpanel.gif
          short_desc: cPanel ($45+)
          long_desc: long_desc
          os_type: os_type
          monthly_price: 0
          types:
          - "1"
          - "2"
          - "4"
          - "5"
          - "8"
          - "17"
          - "23"
          - "30"
          - "51"
          price_display: $80.00
          monthly_price_display: $0.00
      properties:
        "9":
          $ref: "#/components/schemas/ServerOrderControlPanel"
      type: object
    ServerOrderPostRequest_hd:
      description: "Hard-drive configuration id(s) from config_li.hd_li[cpu]. Accepts\
        \ a single id or an array of ids."
      oneOf:
      - type: integer
      - items:
          type: integer
        type: array
    Tickets_countArray:
      properties:
        Open:
          format: int32
          type: integer
        On Hold:
          format: int32
          type: integer
        Closed:
          format: int32
          type: integer
    VPSTrafficDataDataSectionResponse_inner:
      oneOf:
      - format: date-time
        type: string
      - type: integer
    VpsOrder_platformPackages:
      description: Platform Packages
      example:
        kvm: 32
        kvmstorage: 57
        hyperv: 54
      properties:
        kvm:
          example: 32
          type: number
        kvmstorage:
          example: 57
          type: number
        hyperv:
          example: 54
          type: number
      type: object
    VpsOrder_platformNames:
      description: Platform Names
      example:
        kvm: KVM
        kvmstorage: KVM Storage
        hyperv: HyperV
      properties:
        kvm:
          example: KVM
          type: string
        kvmstorage:
          example: KVM Storage
          type: string
        hyperv:
          example: HyperV
          type: string
      type: object
    VpsOrder_packageCosts:
      description: Package Costs
      example:
        "57": 6
      properties:
        "57":
          example: 6
          type: number
      type: object
    VpsOrder_locationStock_1:
      example:
        kvm: true
        kvmstorage: true
        hyperv: true
      properties:
        kvm:
          example: true
          type: boolean
        kvmstorage:
          example: true
          type: boolean
        hyperv:
          example: true
          type: boolean
      type: object
    VpsOrder_locationStock:
      description: Location Stock
      example:
        "1":
          kvm: true
          kvmstorage: true
          hyperv: true
      properties:
        "1":
          $ref: "#/components/schemas/VpsOrder_locationStock_1"
      type: object
    VpsOrder_locationNames:
      description: Location Names
      example:
        "3": Equinix NY4
      properties:
        "3":
          example: Equinix NY4
          type: string
      type: object
    VpsOrder_osNames:
      description: OS Names
      example:
        opensuse: OpenSUSE
        ubuntu: Ubuntu
      properties:
        opensuse:
          example: OpenSUSE
          type: string
        ubuntu:
          example: Ubuntu
          type: string
      type: object
    VpsOrder_templates_hyperv_windows:
      example:
        Windows2019Standard: 2019 Standard
        Windows2022: "2022"
      properties:
        Windows2019Standard:
          example: 2019 Standard
          type: string
        Windows2022:
          example: "2022"
          type: string
      type: object
    VpsOrder_templates_hyperv:
      example:
        windows:
          Windows2019Standard: 2019 Standard
          Windows2022: "2022"
      properties:
        windows:
          $ref: "#/components/schemas/VpsOrder_templates_hyperv_windows"
      type: object
    VpsOrder_templates:
      description: Templates
      example:
        hyperv:
          windows:
            Windows2019Standard: 2019 Standard
            Windows2022: "2022"
      properties:
        hyperv:
          $ref: "#/components/schemas/VpsOrder_templates_hyperv"
      type: object
    VpsOrder_serviceTypes_32:
      example:
        services_id: "32"
        services_name: KVM Windows VPS Slice
        services_cost: "10.00"
        services_category: "2"
        services_buyable: "1"
        services_type: "1"
        services_field1: slice
        services_field2: ""
        services_module: vps
      properties:
        services_id:
          example: "32"
          type: string
        services_name:
          example: KVM Windows VPS Slice
          type: string
        services_cost:
          example: "10.00"
          type: string
        services_category:
          example: "2"
          type: string
        services_buyable:
          example: "1"
          type: string
        services_type:
          example: "1"
          type: string
        services_field1:
          example: slice
          type: string
        services_field2:
          example: ""
          type: string
        services_module:
          example: vps
          type: string
      type: object
    VpsOrder_serviceTypes:
      description: Service Types
      example:
        "32":
          services_id: "32"
          services_name: KVM Windows VPS Slice
          services_cost: "10.00"
          services_category: "2"
          services_buyable: "1"
          services_type: "1"
          services_field1: slice
          services_field2: ""
          services_module: vps
      properties:
        "32":
          $ref: "#/components/schemas/VpsOrder_serviceTypes_32"
      type: object
    WebsitesOrder_serviceTypes:
      description: The service types data.
      example:
        "11447":
          services_id: "11447"
          services_name: Web Hosting Direct Admin (PriceLock)
          services_cost: "5.00"
          services_category: "204"
          services_buyable: "1"
          services_type: "206"
          services_field1: ""
          services_field2: Standard
          services_module: webhosting
          services_html: services_html
          services_description: services_description
          services_moreinfo_url: services_moreinfo_url
          services_hidden: "1"
      properties:
        "11447":
          $ref: "#/components/schemas/WebsitesOrderServiceTypes"
      required:
      - "11447"
      type: object
    WebsitesOrder_serviceOffers:
      description: The service offers data.
      example:
        "1026":
        - service_offer_id: "136"
          service_id: "1026"
          intro_cost: "96.00"
          renewal_cost: "96.00"
          intro_frequency: "12"
          renewal_frequency: "12"
          allow_coupon: "1"
          service_module: webhosting
          created_at: 2023-01-25T14:50:55.000Z
          updated_at: updated_at
          deleted_at: deleted_at
        - service_offer_id: "136"
          service_id: "1026"
          intro_cost: "96.00"
          renewal_cost: "96.00"
          intro_frequency: "12"
          renewal_frequency: "12"
          allow_coupon: "1"
          service_module: webhosting
          created_at: 2023-01-25T14:50:55.000Z
          updated_at: updated_at
          deleted_at: deleted_at
      properties:
        "1026":
          items:
            $ref: "#/components/schemas/WebsitesOrderServiceOffer"
          type: array
      required:
      - "1026"
      type: object
    WebsitesOrder_packges:
      description: The packages data.
      example:
        "11440":
          services_id: "11440"
          services_name: DA BOOST X
          services_cost: "69.95"
          services_category: "204"
          services_buyable: "1"
          services_type: "206"
          services_field1: ""
          services_field2: BoostX
          services_module: webhosting
          services_html: ""
          services_description: Direct Admin Web hosting package with 10x more resources
            over our standard web hosting package.
          services_moreinfo_url: ""
          services_hidden: "0"
      properties:
        "11440":
          $ref: "#/components/schemas/WebsitesOrderPackagesInfo"
      required:
      - "11440"
      type: object
    ServerBulkIpmiPowerResponse_results_inner:
      properties:
        id:
          description: Server ID this result corresponds to.
          example: 2313
          type: integer
        asset:
          description: Asset ID that was queried for the server (omitted on errors
            before asset selection).
          example: 5432
          type: integer
        text:
          description: IPMI power-status output for this server.
          example: Chassis Power is on
          type: string
        error:
          description: "Error message for this server, if processing failed (mutually\
            \ exclusive with `text`)."
          example: Service is not active
          type: string
      type: object
    BackupOrderPostResponse_cj_params:
      properties:
        containerTagId:
          type: string
        CID:
          type: string
        OID:
          type: string
        TYPE:
          type: string
        ITEM1:
          type: string
        AMT1:
          type: string
        QTY1:
          format: int32
          type: integer
        CURRENCY:
          type: string
      type: object
    MailStatsType_volume_to:
      properties:
        client@domain.com:
          format: int32
          type: integer
        user@site.net:
          format: int32
          type: integer
        sales@company.com:
          format: int32
          type: integer
        client@anothersite.com:
          format: int32
          type: integer
      type: object
    MailStatsType_volume_from:
      properties:
        billing@somedomain.com:
          format: int32
          type: integer
        sales@somedomain.com:
          format: int32
          type: integer
      type: object
    MailStatsType_volume_ip:
      properties:
        "1.1.1.1":
          format: int32
          type: integer
        "2.2.2.2":
          format: int32
          type: integer
        "3.3.3.3":
          format: int32
          type: integer
        "4.4.4.4":
          format: int32
          type: integer
      type: object
    MailStatsType_volume:
      properties:
        to:
          $ref: "#/components/schemas/MailStatsType_volume_to"
        from:
          $ref: "#/components/schemas/MailStatsType_volume_from"
        ip:
          $ref: "#/components/schemas/MailStatsType_volume_ip"
      type: object
    TicketPostDetails_inner:
      properties:
        post_id:
          type: integer
        date:
          type: string
        contents:
          type: string
        creator:
          enum:
          - User
          - Staff
          type: string
        creator_email:
          type: string
        creator_name:
          type: string
        hasattachments:
          type: integer
        attachment_download:
          type: string
      type: object
    BuyItNowRow_cpu_inner_oneOf:
      properties:
        img:
          example: xeon-e3.png
          type: string
        type:
          example: XEON
          type: string
        speed:
          example: ""
          type: string
        num_cpus:
          example: "1"
          type: string
        num_cores:
          example: "4"
          type: string
      type: object
    BuyItNowRow_cpu_inner:
      oneOf:
      - type: string
      - $ref: "#/components/schemas/BuyItNowRow_cpu_inner_oneOf"
    DomainNameserverGetResponse_inner:
      example:
        name: ns1.domain.com
        ipaddress: 1.2.3.4
        can_delete: "1"
      properties:
        name:
          example: ns1.domain.com
          format: hostname
          type: string
        ipaddress:
          example: 1.2.3.4
          format: ip
          type: string
        can_delete:
          description: Whether the registrar allows deletion of this nameserver entry.
          enum:
          - "0"
          - "1"
          example: "1"
          type: string
      required:
      - can_delete
      - ipaddress
      - name
      type: object
    WebsiteBackups_inner:
      example:
        name: name
        size: 0
      properties:
        name:
          type: string
        size:
          type: integer
      required:
      - name
      type: object
    ServersBuyNowResponse_order_details:
      description: Details of the placed order.
      example:
        service_id: 1234
        invoice_id: 2342355
      properties:
        service_id:
          description: The newly created service ID for the ordered server.
          example: 1234
          type: number
        invoice_id:
          description: The invoice ID generated for the order.
          example: 2342355
          type: number
      type: object
  securitySchemes:
    apiKeyAuth:
      description: |
        In order to interact with the InterServer API you will need to first authenticate.  We support a number of different authentication methods.

        To authenticate with the API Key you must get a key from your [Account Security](https://my.interserver.net/account_settings) page.

        To use this key pass it in a header called `X-API-KEY`.

        ```
        curl -H "X-API-KEY: $YOURE_KEY" "https://my.interserver.net/apiv2/$CALL"
        ````
      in: header
      name: X-API-KEY
      type: apiKey
    sessionIdHeaderAuth:
      description: |
        In order to interact with the InterServer API you will need to first authenticate.  We support a number of different authentication methods.

        To authenticate with the Session ID in a HTTP header you need to first get the Session ID from a Login request.

        To use this key pass it in a header called `sessionid`.

        ```
        curl -H "seessionid: $YOUR_SESSION_ID" "https://my.interserver.net/apiv2/$CALL"
        ````
      in: header
      name: sessionid
      type: apiKey
    sessionIdCookieAuth:
      description: |
        In order to interact with the InterServer API you will need to first authenticate.  We support a number of different authentication methods.

        To authenticate with the Session ID in a Cookie you need to first get the Session ID from a Login request.

        To use this key pass it in a cookie called `sessionid`.

        ```
        curl -b "seessionid: $YOUR_SESSION_ID" "https://my.interserver.net/apiv2/$CALL"
        ````
      in: cookie
      name: sessionid
      type: apiKey
