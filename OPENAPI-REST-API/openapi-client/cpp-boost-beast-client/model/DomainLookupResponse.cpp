/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "DomainLookupResponse.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

DomainLookupResponse::DomainLookupResponse(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string DomainLookupResponse::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void DomainLookupResponse::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value DomainLookupResponse::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void DomainLookupResponse::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for DomainLookupResponse");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object DomainLookupResponse::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_AvailableIsSet) {
            object["available"] = JsonValueConverter<bool>::toJsonValue(isAvailable());
        }
        if (m_PremiumIsSet) {
            object["premium"] = JsonValueConverter<bool>::toJsonValue(isPremium());
        }
        if (m_WebsiteIsSet) {
            object["website"] = JsonValueConverter<bool>::toJsonValue(isWebsite());
        }
        if (m_Domain_serviceIsSet) {
            object["domain_service"] = JsonValueConverter<bool>::toJsonValue(isDomainService());
        }
        if (m_ServiceIsSet) {
            object["service"] = JsonValueConverter<boost::json::value>::toJsonValue(getService());
        }
        if (m_Whois_privacyIsSet) {
            object["whois_privacy"] = JsonValueConverter<bool>::toJsonValue(isWhoisPrivacy());
        }
        if (m_r_newIsSet) {
            object["new"] = JsonValueConverter<std::string>::toJsonValue(getRNew());
        }
        if (m_RenewalIsSet) {
            object["renewal"] = JsonValueConverter<std::string>::toJsonValue(getRenewal());
        }
        if (m_TransferIsSet) {
            object["transfer"] = JsonValueConverter<std::string>::toJsonValue(getTransfer());
        }
        if (m_FieldsIsSet) {
            object["fields"] = JsonValueConverter<boost::json::value>::toJsonValue(getFields());
        }
        if (m_CurrenciesIsSet) {
            object["currencies"] = JsonValueConverter<boost::json::value>::toJsonValue(getCurrencies());
        }
    return object;
}

void DomainLookupResponse::fromJsonObject_internal(boost::json::object const& object)
{
    m_AvailableIsSet = false;
    m_PremiumIsSet = false;
    m_WebsiteIsSet = false;
    m_Domain_serviceIsSet = false;
    m_ServiceIsSet = false;
    m_Whois_privacyIsSet = false;
    m_r_newIsSet = false;
    m_RenewalIsSet = false;
    m_TransferIsSet = false;
    m_FieldsIsSet = false;
    m_CurrenciesIsSet = false;
    {
        const auto AvailableIt = object.find("available");
        if (AvailableIt != object.end()) {
            setAvailable(JsonValueConverter<bool>::fromJsonValue(AvailableIt->value()));
        }
    }
    {
        const auto PremiumIt = object.find("premium");
        if (PremiumIt != object.end()) {
            setPremium(JsonValueConverter<bool>::fromJsonValue(PremiumIt->value()));
        }
    }
    {
        const auto WebsiteIt = object.find("website");
        if (WebsiteIt != object.end()) {
            setWebsite(JsonValueConverter<bool>::fromJsonValue(WebsiteIt->value()));
        }
    }
    {
        const auto Domain_serviceIt = object.find("domain_service");
        if (Domain_serviceIt != object.end()) {
            setDomainService(JsonValueConverter<bool>::fromJsonValue(Domain_serviceIt->value()));
        }
    }
    {
        const auto ServiceIt = object.find("service");
        if (ServiceIt != object.end()) {
            setService(JsonValueConverter<boost::json::value>::fromJsonValue(ServiceIt->value()));
        }
    }
    {
        const auto Whois_privacyIt = object.find("whois_privacy");
        if (Whois_privacyIt != object.end()) {
            setWhoisPrivacy(JsonValueConverter<bool>::fromJsonValue(Whois_privacyIt->value()));
        }
    }
    {
        const auto r_newIt = object.find("new");
        if (r_newIt != object.end()) {
            setRNew(JsonValueConverter<std::string>::fromJsonValue(r_newIt->value()));
        }
    }
    {
        const auto RenewalIt = object.find("renewal");
        if (RenewalIt != object.end()) {
            setRenewal(JsonValueConverter<std::string>::fromJsonValue(RenewalIt->value()));
        }
    }
    {
        const auto TransferIt = object.find("transfer");
        if (TransferIt != object.end()) {
            setTransfer(JsonValueConverter<std::string>::fromJsonValue(TransferIt->value()));
        }
    }
    {
        const auto FieldsIt = object.find("fields");
        if (FieldsIt != object.end()) {
            setFields(JsonValueConverter<boost::json::value>::fromJsonValue(FieldsIt->value()));
        }
    }
    {
        const auto CurrenciesIt = object.find("currencies");
        if (CurrenciesIt != object.end()) {
            setCurrencies(JsonValueConverter<boost::json::value>::fromJsonValue(CurrenciesIt->value()));
        }
    }
}

bool DomainLookupResponse::isAvailable() const
{
    return m_Available;
}

void DomainLookupResponse::setAvailable(bool value)
{
    
    m_Available = std::move(value);
    m_AvailableIsSet = true;
}
bool DomainLookupResponse::isPremium() const
{
    return m_Premium;
}

void DomainLookupResponse::setPremium(bool value)
{
    
    m_Premium = std::move(value);
    m_PremiumIsSet = true;
}
bool DomainLookupResponse::isWebsite() const
{
    return m_Website;
}

void DomainLookupResponse::setWebsite(bool value)
{
    
    m_Website = std::move(value);
    m_WebsiteIsSet = true;
}
bool DomainLookupResponse::isDomainService() const
{
    return m_Domain_service;
}

void DomainLookupResponse::setDomainService(bool value)
{
    
    m_Domain_service = std::move(value);
    m_Domain_serviceIsSet = true;
}
boost::json::value DomainLookupResponse::getService() const
{
    return m_Service;
}

void DomainLookupResponse::setService(boost::json::value value)
{
    
    m_Service = std::move(value);
    m_ServiceIsSet = true;
}
bool DomainLookupResponse::isWhoisPrivacy() const
{
    return m_Whois_privacy;
}

void DomainLookupResponse::setWhoisPrivacy(bool value)
{
    
    m_Whois_privacy = std::move(value);
    m_Whois_privacyIsSet = true;
}
std::string DomainLookupResponse::getRNew() const
{
    return m_r_new;
}

void DomainLookupResponse::setRNew(std::string value)
{
    
    m_r_new = std::move(value);
    m_r_newIsSet = true;
}
std::string DomainLookupResponse::getRenewal() const
{
    return m_Renewal;
}

void DomainLookupResponse::setRenewal(std::string value)
{
    
    m_Renewal = std::move(value);
    m_RenewalIsSet = true;
}
std::string DomainLookupResponse::getTransfer() const
{
    return m_Transfer;
}

void DomainLookupResponse::setTransfer(std::string value)
{
    
    m_Transfer = std::move(value);
    m_TransferIsSet = true;
}
boost::json::value DomainLookupResponse::getFields() const
{
    return m_Fields;
}

void DomainLookupResponse::setFields(boost::json::value value)
{
    
    m_Fields = std::move(value);
    m_FieldsIsSet = true;
}
boost::json::value DomainLookupResponse::getCurrencies() const
{
    return m_Currencies;
}

void DomainLookupResponse::setCurrencies(boost::json::value value)
{
    
    m_Currencies = std::move(value);
    m_CurrenciesIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<DomainLookupResponse>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<DomainLookupResponse>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<DomainLookupResponse>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<DomainLookupResponse>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

