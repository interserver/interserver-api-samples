/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Service.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Service::Service(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Service::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Service::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Service::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Service::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Service");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Service::toJsonObject_internal() const
{
    boost::json::object object;
        object["services_id"] = JsonValueConverter<int32_t>::toJsonValue(getServicesId());
        object["services_name"] = JsonValueConverter<std::string>::toJsonValue(getServicesName());
        object["services_cost"] = JsonValueConverter<double>::toJsonValue(getServicesCost());
        object["services_currency"] = JsonValueConverter<std::string>::toJsonValue(getServicesCurrency());
        object["services_category"] = JsonValueConverter<int32_t>::toJsonValue(getServicesCategory());
        object["services_buyable"] = JsonValueConverter<bool>::toJsonValue(isServicesBuyable());
        object["services_type"] = JsonValueConverter<int32_t>::toJsonValue(getServicesType());
        object["services_field1"] = JsonValueConverter<std::string>::toJsonValue(getServicesField1());
        object["services_field2"] = JsonValueConverter<std::string>::toJsonValue(getServicesField2());
        object["services_module"] = JsonValueConverter<std::string>::toJsonValue(getServicesModule());
    return object;
}

void Service::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto Services_idIt = object.find("services_id");
        if (Services_idIt != object.end()) {
            setServicesId(JsonValueConverter<int32_t>::fromJsonValue(Services_idIt->value()));
        }
    }
    {
        const auto Services_nameIt = object.find("services_name");
        if (Services_nameIt != object.end()) {
            setServicesName(JsonValueConverter<std::string>::fromJsonValue(Services_nameIt->value()));
        }
    }
    {
        const auto Services_costIt = object.find("services_cost");
        if (Services_costIt != object.end()) {
            setServicesCost(JsonValueConverter<double>::fromJsonValue(Services_costIt->value()));
        }
    }
    {
        const auto Services_currencyIt = object.find("services_currency");
        if (Services_currencyIt != object.end()) {
            setServicesCurrency(JsonValueConverter<std::string>::fromJsonValue(Services_currencyIt->value()));
        }
    }
    {
        const auto Services_categoryIt = object.find("services_category");
        if (Services_categoryIt != object.end()) {
            setServicesCategory(JsonValueConverter<int32_t>::fromJsonValue(Services_categoryIt->value()));
        }
    }
    {
        const auto Services_buyableIt = object.find("services_buyable");
        if (Services_buyableIt != object.end()) {
            setServicesBuyable(JsonValueConverter<bool>::fromJsonValue(Services_buyableIt->value()));
        }
    }
    {
        const auto Services_typeIt = object.find("services_type");
        if (Services_typeIt != object.end()) {
            setServicesType(JsonValueConverter<int32_t>::fromJsonValue(Services_typeIt->value()));
        }
    }
    {
        const auto Services_field1It = object.find("services_field1");
        if (Services_field1It != object.end()) {
            setServicesField1(JsonValueConverter<std::string>::fromJsonValue(Services_field1It->value()));
        }
    }
    {
        const auto Services_field2It = object.find("services_field2");
        if (Services_field2It != object.end()) {
            setServicesField2(JsonValueConverter<std::string>::fromJsonValue(Services_field2It->value()));
        }
    }
    {
        const auto Services_moduleIt = object.find("services_module");
        if (Services_moduleIt != object.end()) {
            setServicesModule(JsonValueConverter<std::string>::fromJsonValue(Services_moduleIt->value()));
        }
    }
}

int32_t Service::getServicesId() const
{
    return m_Services_id;
}

void Service::setServicesId(int32_t value)
{
    
    m_Services_id = std::move(value);
}
std::string Service::getServicesName() const
{
    return m_Services_name;
}

void Service::setServicesName(std::string value)
{
    
    m_Services_name = std::move(value);
}
double Service::getServicesCost() const
{
    return m_Services_cost;
}

void Service::setServicesCost(double value)
{
    
    m_Services_cost = std::move(value);
}
std::string Service::getServicesCurrency() const
{
    return m_Services_currency;
}

void Service::setServicesCurrency(std::string value)
{
    
    m_Services_currency = std::move(value);
}
int32_t Service::getServicesCategory() const
{
    return m_Services_category;
}

void Service::setServicesCategory(int32_t value)
{
    
    m_Services_category = std::move(value);
}
bool Service::isServicesBuyable() const
{
    return m_Services_buyable;
}

void Service::setServicesBuyable(bool value)
{
    
    m_Services_buyable = std::move(value);
}
int32_t Service::getServicesType() const
{
    return m_Services_type;
}

void Service::setServicesType(int32_t value)
{
    
    m_Services_type = std::move(value);
}
std::string Service::getServicesField1() const
{
    return m_Services_field1;
}

void Service::setServicesField1(std::string value)
{
    
    m_Services_field1 = std::move(value);
}
std::string Service::getServicesField2() const
{
    return m_Services_field2;
}

void Service::setServicesField2(std::string value)
{
    
    m_Services_field2 = std::move(value);
}
std::string Service::getServicesModule() const
{
    return m_Services_module;
}

void Service::setServicesModule(std::string value)
{
    
    m_Services_module = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Service>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<Service>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Service>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<Service>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

