/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Server.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Server::Server(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Server::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Server::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Server::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Server::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Server");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Server::toJsonObject_internal() const
{
    boost::json::object object;
        object["ipmiAuth"] = JsonValueConverter<bool>::toJsonValue(isIpmiAuth());
        object["client_links"] = JsonValueConverter<std::vector<std::shared_ptr<ServerClientLink>>>::toJsonValue(getClientLinks());
        object["billingDetails"] = JsonValueConverter<std::shared_ptr<ServerBillingDetails>>::toJsonValue(getBillingDetails());
        object["custCurrency"] = JsonValueConverter<std::string>::toJsonValue(getCustCurrency());
        object["custCurrencySymbol"] = JsonValueConverter<std::string>::toJsonValue(getCustCurrencySymbol());
        object["package"] = JsonValueConverter<std::string>::toJsonValue(getPackage());
        object["serviceExtra"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getServiceExtra());
        object["locations"] = JsonValueConverter<std::shared_ptr<Server_locations>>::toJsonValue(getLocations());
        object["networkInfo"] = JsonValueConverter<std::shared_ptr<ServerNetworkInfo>>::toJsonValue(getNetworkInfo());
        object["extraInfoTables"] = JsonValueConverter<std::shared_ptr<ServerExtraInfoTables>>::toJsonValue(getExtraInfoTables());
        object["serviceInfo"] = JsonValueConverter<std::shared_ptr<ServerServiceInfo>>::toJsonValue(getServiceInfo());
    return object;
}

void Server::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto IpmiAuthIt = object.find("ipmiAuth");
        if (IpmiAuthIt != object.end()) {
            setIpmiAuth(JsonValueConverter<bool>::fromJsonValue(IpmiAuthIt->value()));
        }
    }
    {
        const auto Client_linksIt = object.find("client_links");
        if (Client_linksIt != object.end()) {
            setClientLinks(JsonValueConverter<std::vector<std::shared_ptr<ServerClientLink>>>::fromJsonValue(Client_linksIt->value()));
        }
    }
    {
        const auto BillingDetailsIt = object.find("billingDetails");
        if (BillingDetailsIt != object.end()) {
            setBillingDetails(JsonValueConverter<std::shared_ptr<ServerBillingDetails>>::fromJsonValue(BillingDetailsIt->value()));
        }
    }
    {
        const auto CustCurrencyIt = object.find("custCurrency");
        if (CustCurrencyIt != object.end()) {
            setCustCurrency(JsonValueConverter<std::string>::fromJsonValue(CustCurrencyIt->value()));
        }
    }
    {
        const auto CustCurrencySymbolIt = object.find("custCurrencySymbol");
        if (CustCurrencySymbolIt != object.end()) {
            setCustCurrencySymbol(JsonValueConverter<std::string>::fromJsonValue(CustCurrencySymbolIt->value()));
        }
    }
    {
        const auto PackageIt = object.find("package");
        if (PackageIt != object.end()) {
            setPackage(JsonValueConverter<std::string>::fromJsonValue(PackageIt->value()));
        }
    }
    {
        const auto ServiceExtraIt = object.find("serviceExtra");
        if (ServiceExtraIt != object.end()) {
            setServiceExtra(JsonValueConverter<std::vector<std::string>>::fromJsonValue(ServiceExtraIt->value()));
        }
    }
    {
        const auto LocationsIt = object.find("locations");
        if (LocationsIt != object.end()) {
            setLocations(JsonValueConverter<std::shared_ptr<Server_locations>>::fromJsonValue(LocationsIt->value()));
        }
    }
    {
        const auto NetworkInfoIt = object.find("networkInfo");
        if (NetworkInfoIt != object.end()) {
            setNetworkInfo(JsonValueConverter<std::shared_ptr<ServerNetworkInfo>>::fromJsonValue(NetworkInfoIt->value()));
        }
    }
    {
        const auto ExtraInfoTablesIt = object.find("extraInfoTables");
        if (ExtraInfoTablesIt != object.end()) {
            setExtraInfoTables(JsonValueConverter<std::shared_ptr<ServerExtraInfoTables>>::fromJsonValue(ExtraInfoTablesIt->value()));
        }
    }
    {
        const auto ServiceInfoIt = object.find("serviceInfo");
        if (ServiceInfoIt != object.end()) {
            setServiceInfo(JsonValueConverter<std::shared_ptr<ServerServiceInfo>>::fromJsonValue(ServiceInfoIt->value()));
        }
    }
}

bool Server::isIpmiAuth() const
{
    return m_IpmiAuth;
}

void Server::setIpmiAuth(bool value)
{
    
    m_IpmiAuth = std::move(value);
}
std::vector<std::shared_ptr<ServerClientLink>> Server::getClientLinks() const
{
    return m_Client_links;
}

void Server::setClientLinks(std::vector<std::shared_ptr<ServerClientLink>> value)
{
    
    m_Client_links = std::move(value);
}
std::shared_ptr<ServerBillingDetails> Server::getBillingDetails() const
{
    return m_BillingDetails;
}

void Server::setBillingDetails(std::shared_ptr<ServerBillingDetails> value)
{
    
    m_BillingDetails = std::move(value);
}
std::string Server::getCustCurrency() const
{
    return m_CustCurrency;
}

void Server::setCustCurrency(std::string value)
{
    
    m_CustCurrency = std::move(value);
}
std::string Server::getCustCurrencySymbol() const
{
    return m_CustCurrencySymbol;
}

void Server::setCustCurrencySymbol(std::string value)
{
    
    m_CustCurrencySymbol = std::move(value);
}
std::string Server::getPackage() const
{
    return m_Package;
}

void Server::setPackage(std::string value)
{
    
    m_Package = std::move(value);
}
std::vector<std::string> Server::getServiceExtra() const
{
    return m_ServiceExtra;
}

void Server::setServiceExtra(std::vector<std::string> value)
{
    
    m_ServiceExtra = std::move(value);
}
std::shared_ptr<Server_locations> Server::getLocations() const
{
    return m_Locations;
}

void Server::setLocations(std::shared_ptr<Server_locations> value)
{
    
    m_Locations = std::move(value);
}
std::shared_ptr<ServerNetworkInfo> Server::getNetworkInfo() const
{
    return m_NetworkInfo;
}

void Server::setNetworkInfo(std::shared_ptr<ServerNetworkInfo> value)
{
    
    m_NetworkInfo = std::move(value);
}
std::shared_ptr<ServerExtraInfoTables> Server::getExtraInfoTables() const
{
    return m_ExtraInfoTables;
}

void Server::setExtraInfoTables(std::shared_ptr<ServerExtraInfoTables> value)
{
    
    m_ExtraInfoTables = std::move(value);
}
std::shared_ptr<ServerServiceInfo> Server::getServiceInfo() const
{
    return m_ServiceInfo;
}

void Server::setServiceInfo(std::shared_ptr<ServerServiceInfo> value)
{
    
    m_ServiceInfo = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Server>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<Server>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Server>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<Server>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

