/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "LicenseRow.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

LicenseRow::LicenseRow(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string LicenseRow::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void LicenseRow::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value LicenseRow::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void LicenseRow::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for LicenseRow");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object LicenseRow::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_License_idIsSet) {
            object["license_id"] = JsonValueConverter<std::string>::toJsonValue(getLicenseId());
        }
        if (m_License_hostnameIsSet) {
            object["license_hostname"] = JsonValueConverter<std::string>::toJsonValue(getLicenseHostname());
        }
        if (m_License_ipIsSet) {
            object["license_ip"] = JsonValueConverter<std::string>::toJsonValue(getLicenseIp());
        }
        if (m_Services_nameIsSet) {
            object["services_name"] = JsonValueConverter<std::string>::toJsonValue(getServicesName());
        }
        if (m_CostIsSet) {
            object["cost"] = JsonValueConverter<std::string>::toJsonValue(getCost());
        }
        if (m_License_statusIsSet) {
            object["license_status"] = JsonValueConverter<std::string>::toJsonValue(getLicenseStatus());
        }
        if (m_Invoices_paidIsSet) {
            object["invoices_paid"] = JsonValueConverter<std::string>::toJsonValue(getInvoicesPaid());
        }
        if (m_Invoices_dateIsSet) {
            object["invoices_date"] = JsonValueConverter<std::string>::toJsonValue(getInvoicesDate());
        }
    return object;
}

void LicenseRow::fromJsonObject_internal(boost::json::object const& object)
{
    m_License_idIsSet = false;
    m_License_hostnameIsSet = false;
    m_License_ipIsSet = false;
    m_Services_nameIsSet = false;
    m_CostIsSet = false;
    m_License_statusIsSet = false;
    m_Invoices_paidIsSet = false;
    m_Invoices_dateIsSet = false;
    {
        const auto License_idIt = object.find("license_id");
        if (License_idIt != object.end()) {
            setLicenseId(JsonValueConverter<std::string>::fromJsonValue(License_idIt->value()));
        }
    }
    {
        const auto License_hostnameIt = object.find("license_hostname");
        if (License_hostnameIt != object.end()) {
            setLicenseHostname(JsonValueConverter<std::string>::fromJsonValue(License_hostnameIt->value()));
        }
    }
    {
        const auto License_ipIt = object.find("license_ip");
        if (License_ipIt != object.end()) {
            setLicenseIp(JsonValueConverter<std::string>::fromJsonValue(License_ipIt->value()));
        }
    }
    {
        const auto Services_nameIt = object.find("services_name");
        if (Services_nameIt != object.end()) {
            setServicesName(JsonValueConverter<std::string>::fromJsonValue(Services_nameIt->value()));
        }
    }
    {
        const auto CostIt = object.find("cost");
        if (CostIt != object.end()) {
            setCost(JsonValueConverter<std::string>::fromJsonValue(CostIt->value()));
        }
    }
    {
        const auto License_statusIt = object.find("license_status");
        if (License_statusIt != object.end()) {
            setLicenseStatus(JsonValueConverter<std::string>::fromJsonValue(License_statusIt->value()));
        }
    }
    {
        const auto Invoices_paidIt = object.find("invoices_paid");
        if (Invoices_paidIt != object.end()) {
            setInvoicesPaid(JsonValueConverter<std::string>::fromJsonValue(Invoices_paidIt->value()));
        }
    }
    {
        const auto Invoices_dateIt = object.find("invoices_date");
        if (Invoices_dateIt != object.end()) {
            setInvoicesDate(JsonValueConverter<std::string>::fromJsonValue(Invoices_dateIt->value()));
        }
    }
}

std::string LicenseRow::getLicenseId() const
{
    return m_License_id;
}

void LicenseRow::setLicenseId(std::string value)
{
    
    m_License_id = std::move(value);
    m_License_idIsSet = true;
}
std::string LicenseRow::getLicenseHostname() const
{
    return m_License_hostname;
}

void LicenseRow::setLicenseHostname(std::string value)
{
    
    m_License_hostname = std::move(value);
    m_License_hostnameIsSet = true;
}
std::string LicenseRow::getLicenseIp() const
{
    return m_License_ip;
}

void LicenseRow::setLicenseIp(std::string value)
{
    
    m_License_ip = std::move(value);
    m_License_ipIsSet = true;
}
std::string LicenseRow::getServicesName() const
{
    return m_Services_name;
}

void LicenseRow::setServicesName(std::string value)
{
    
    m_Services_name = std::move(value);
    m_Services_nameIsSet = true;
}
std::string LicenseRow::getCost() const
{
    return m_Cost;
}

void LicenseRow::setCost(std::string value)
{
    
    m_Cost = std::move(value);
    m_CostIsSet = true;
}
std::string LicenseRow::getLicenseStatus() const
{
    return m_License_status;
}

void LicenseRow::setLicenseStatus(std::string value)
{
    
    m_License_status = std::move(value);
    m_License_statusIsSet = true;
}
std::string LicenseRow::getInvoicesPaid() const
{
    return m_Invoices_paid;
}

void LicenseRow::setInvoicesPaid(std::string value)
{
    
    m_Invoices_paid = std::move(value);
    m_Invoices_paidIsSet = true;
}
std::string LicenseRow::getInvoicesDate() const
{
    return m_Invoices_date;
}

void LicenseRow::setInvoicesDate(std::string value)
{
    
    m_Invoices_date = std::move(value);
    m_Invoices_dateIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<LicenseRow>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<LicenseRow>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<LicenseRow>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<LicenseRow>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

