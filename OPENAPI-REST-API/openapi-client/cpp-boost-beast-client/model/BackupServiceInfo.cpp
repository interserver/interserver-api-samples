/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BackupServiceInfo.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BackupServiceInfo::BackupServiceInfo(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BackupServiceInfo::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BackupServiceInfo::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BackupServiceInfo::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BackupServiceInfo::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BackupServiceInfo");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BackupServiceInfo::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Backup_idIsSet) {
            object["backup_id"] = JsonValueConverter<std::string>::toJsonValue(getBackupId());
        }
        if (m_Backup_serverIsSet) {
            object["backup_server"] = JsonValueConverter<std::string>::toJsonValue(getBackupServer());
        }
        if (m_Backup_usernameIsSet) {
            object["backup_username"] = JsonValueConverter<std::string>::toJsonValue(getBackupUsername());
        }
        if (m_Backup_typeIsSet) {
            object["backup_type"] = JsonValueConverter<std::string>::toJsonValue(getBackupType());
        }
        if (m_Backup_currencyIsSet) {
            object["backup_currency"] = JsonValueConverter<std::string>::toJsonValue(getBackupCurrency());
        }
        if (m_Backup_order_dateIsSet) {
            object["backup_order_date"] = JsonValueConverter<std::string>::toJsonValue(getBackupOrderDate());
        }
        if (m_Backup_custidIsSet) {
            object["backup_custid"] = JsonValueConverter<std::string>::toJsonValue(getBackupCustid());
        }
        if (m_Backup_quotaIsSet) {
            object["backup_quota"] = JsonValueConverter<std::string>::toJsonValue(getBackupQuota());
        }
        if (m_Backup_ipIsSet) {
            object["backup_ip"] = JsonValueConverter<std::string>::toJsonValue(getBackupIp());
        }
        if (m_Backup_statusIsSet) {
            object["backup_status"] = JsonValueConverter<std::string>::toJsonValue(getBackupStatus());
        }
        if (m_Backup_invoiceIsSet) {
            object["backup_invoice"] = JsonValueConverter<std::string>::toJsonValue(getBackupInvoice());
        }
        if (m_Backup_couponIsSet) {
            object["backup_coupon"] = JsonValueConverter<std::string>::toJsonValue(getBackupCoupon());
        }
        if (m_Backup_extraIsSet) {
            object["backup_extra"] = JsonValueConverter<std::string>::toJsonValue(getBackupExtra());
        }
        if (m_Backup_server_statusIsSet) {
            object["backup_server_status"] = JsonValueConverter<std::string>::toJsonValue(getBackupServerStatus());
        }
        if (m_Backup_commentIsSet) {
            object["backup_comment"] = JsonValueConverter<std::string>::toJsonValue(getBackupComment());
        }
    return object;
}

void BackupServiceInfo::fromJsonObject_internal(boost::json::object const& object)
{
    m_Backup_idIsSet = false;
    m_Backup_serverIsSet = false;
    m_Backup_usernameIsSet = false;
    m_Backup_typeIsSet = false;
    m_Backup_currencyIsSet = false;
    m_Backup_order_dateIsSet = false;
    m_Backup_custidIsSet = false;
    m_Backup_quotaIsSet = false;
    m_Backup_ipIsSet = false;
    m_Backup_statusIsSet = false;
    m_Backup_invoiceIsSet = false;
    m_Backup_couponIsSet = false;
    m_Backup_extraIsSet = false;
    m_Backup_server_statusIsSet = false;
    m_Backup_commentIsSet = false;
    {
        const auto Backup_idIt = object.find("backup_id");
        if (Backup_idIt != object.end()) {
            setBackupId(JsonValueConverter<std::string>::fromJsonValue(Backup_idIt->value()));
        }
    }
    {
        const auto Backup_serverIt = object.find("backup_server");
        if (Backup_serverIt != object.end()) {
            setBackupServer(JsonValueConverter<std::string>::fromJsonValue(Backup_serverIt->value()));
        }
    }
    {
        const auto Backup_usernameIt = object.find("backup_username");
        if (Backup_usernameIt != object.end()) {
            setBackupUsername(JsonValueConverter<std::string>::fromJsonValue(Backup_usernameIt->value()));
        }
    }
    {
        const auto Backup_typeIt = object.find("backup_type");
        if (Backup_typeIt != object.end()) {
            setBackupType(JsonValueConverter<std::string>::fromJsonValue(Backup_typeIt->value()));
        }
    }
    {
        const auto Backup_currencyIt = object.find("backup_currency");
        if (Backup_currencyIt != object.end()) {
            setBackupCurrency(JsonValueConverter<std::string>::fromJsonValue(Backup_currencyIt->value()));
        }
    }
    {
        const auto Backup_order_dateIt = object.find("backup_order_date");
        if (Backup_order_dateIt != object.end()) {
            setBackupOrderDate(JsonValueConverter<std::string>::fromJsonValue(Backup_order_dateIt->value()));
        }
    }
    {
        const auto Backup_custidIt = object.find("backup_custid");
        if (Backup_custidIt != object.end()) {
            setBackupCustid(JsonValueConverter<std::string>::fromJsonValue(Backup_custidIt->value()));
        }
    }
    {
        const auto Backup_quotaIt = object.find("backup_quota");
        if (Backup_quotaIt != object.end()) {
            setBackupQuota(JsonValueConverter<std::string>::fromJsonValue(Backup_quotaIt->value()));
        }
    }
    {
        const auto Backup_ipIt = object.find("backup_ip");
        if (Backup_ipIt != object.end()) {
            setBackupIp(JsonValueConverter<std::string>::fromJsonValue(Backup_ipIt->value()));
        }
    }
    {
        const auto Backup_statusIt = object.find("backup_status");
        if (Backup_statusIt != object.end()) {
            setBackupStatus(JsonValueConverter<std::string>::fromJsonValue(Backup_statusIt->value()));
        }
    }
    {
        const auto Backup_invoiceIt = object.find("backup_invoice");
        if (Backup_invoiceIt != object.end()) {
            setBackupInvoice(JsonValueConverter<std::string>::fromJsonValue(Backup_invoiceIt->value()));
        }
    }
    {
        const auto Backup_couponIt = object.find("backup_coupon");
        if (Backup_couponIt != object.end()) {
            setBackupCoupon(JsonValueConverter<std::string>::fromJsonValue(Backup_couponIt->value()));
        }
    }
    {
        const auto Backup_extraIt = object.find("backup_extra");
        if (Backup_extraIt != object.end()) {
            setBackupExtra(JsonValueConverter<std::string>::fromJsonValue(Backup_extraIt->value()));
        }
    }
    {
        const auto Backup_server_statusIt = object.find("backup_server_status");
        if (Backup_server_statusIt != object.end()) {
            setBackupServerStatus(JsonValueConverter<std::string>::fromJsonValue(Backup_server_statusIt->value()));
        }
    }
    {
        const auto Backup_commentIt = object.find("backup_comment");
        if (Backup_commentIt != object.end()) {
            setBackupComment(JsonValueConverter<std::string>::fromJsonValue(Backup_commentIt->value()));
        }
    }
}

std::string BackupServiceInfo::getBackupId() const
{
    return m_Backup_id;
}

void BackupServiceInfo::setBackupId(std::string value)
{
    
    m_Backup_id = std::move(value);
    m_Backup_idIsSet = true;
}
std::string BackupServiceInfo::getBackupServer() const
{
    return m_Backup_server;
}

void BackupServiceInfo::setBackupServer(std::string value)
{
    
    m_Backup_server = std::move(value);
    m_Backup_serverIsSet = true;
}
std::string BackupServiceInfo::getBackupUsername() const
{
    return m_Backup_username;
}

void BackupServiceInfo::setBackupUsername(std::string value)
{
    
    m_Backup_username = std::move(value);
    m_Backup_usernameIsSet = true;
}
std::string BackupServiceInfo::getBackupType() const
{
    return m_Backup_type;
}

void BackupServiceInfo::setBackupType(std::string value)
{
    
    m_Backup_type = std::move(value);
    m_Backup_typeIsSet = true;
}
std::string BackupServiceInfo::getBackupCurrency() const
{
    return m_Backup_currency;
}

void BackupServiceInfo::setBackupCurrency(std::string value)
{
    
    m_Backup_currency = std::move(value);
    m_Backup_currencyIsSet = true;
}
std::string BackupServiceInfo::getBackupOrderDate() const
{
    return m_Backup_order_date;
}

void BackupServiceInfo::setBackupOrderDate(std::string value)
{
    
    m_Backup_order_date = std::move(value);
    m_Backup_order_dateIsSet = true;
}
std::string BackupServiceInfo::getBackupCustid() const
{
    return m_Backup_custid;
}

void BackupServiceInfo::setBackupCustid(std::string value)
{
    
    m_Backup_custid = std::move(value);
    m_Backup_custidIsSet = true;
}
std::string BackupServiceInfo::getBackupQuota() const
{
    return m_Backup_quota;
}

void BackupServiceInfo::setBackupQuota(std::string value)
{
    
    m_Backup_quota = std::move(value);
    m_Backup_quotaIsSet = true;
}
std::string BackupServiceInfo::getBackupIp() const
{
    return m_Backup_ip;
}

void BackupServiceInfo::setBackupIp(std::string value)
{
    
    m_Backup_ip = std::move(value);
    m_Backup_ipIsSet = true;
}
std::string BackupServiceInfo::getBackupStatus() const
{
    return m_Backup_status;
}

void BackupServiceInfo::setBackupStatus(std::string value)
{
    
    m_Backup_status = std::move(value);
    m_Backup_statusIsSet = true;
}
std::string BackupServiceInfo::getBackupInvoice() const
{
    return m_Backup_invoice;
}

void BackupServiceInfo::setBackupInvoice(std::string value)
{
    
    m_Backup_invoice = std::move(value);
    m_Backup_invoiceIsSet = true;
}
std::string BackupServiceInfo::getBackupCoupon() const
{
    return m_Backup_coupon;
}

void BackupServiceInfo::setBackupCoupon(std::string value)
{
    
    m_Backup_coupon = std::move(value);
    m_Backup_couponIsSet = true;
}
std::string BackupServiceInfo::getBackupExtra() const
{
    return m_Backup_extra;
}

void BackupServiceInfo::setBackupExtra(std::string value)
{
    
    m_Backup_extra = std::move(value);
    m_Backup_extraIsSet = true;
}
std::string BackupServiceInfo::getBackupServerStatus() const
{
    return m_Backup_server_status;
}

void BackupServiceInfo::setBackupServerStatus(std::string value)
{
    
    m_Backup_server_status = std::move(value);
    m_Backup_server_statusIsSet = true;
}
std::string BackupServiceInfo::getBackupComment() const
{
    return m_Backup_comment;
}

void BackupServiceInfo::setBackupComment(std::string value)
{
    
    m_Backup_comment = std::move(value);
    m_Backup_commentIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BackupServiceInfo>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BackupServiceInfo>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BackupServiceInfo>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BackupServiceInfo>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

