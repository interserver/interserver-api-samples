/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "WebsitesOrderJsonServiceOffersItem.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

WebsitesOrderJsonServiceOffersItem::WebsitesOrderJsonServiceOffersItem(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string WebsitesOrderJsonServiceOffersItem::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void WebsitesOrderJsonServiceOffersItem::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value WebsitesOrderJsonServiceOffersItem::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void WebsitesOrderJsonServiceOffersItem::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for WebsitesOrderJsonServiceOffersItem");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object WebsitesOrderJsonServiceOffersItem::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Service_offer_idIsSet) {
            object["service_offer_id"] = JsonValueConverter<std::string>::toJsonValue(getServiceOfferId());
        }
        if (m_Service_idIsSet) {
            object["service_id"] = JsonValueConverter<std::string>::toJsonValue(getServiceId());
        }
        if (m_Intro_costIsSet) {
            object["intro_cost"] = JsonValueConverter<int32_t>::toJsonValue(getIntroCost());
        }
        if (m_Renewal_costIsSet) {
            object["renewal_cost"] = JsonValueConverter<int32_t>::toJsonValue(getRenewalCost());
        }
        if (m_Intro_frequencyIsSet) {
            object["intro_frequency"] = JsonValueConverter<std::string>::toJsonValue(getIntroFrequency());
        }
        if (m_Renewal_frequencyIsSet) {
            object["renewal_frequency"] = JsonValueConverter<std::string>::toJsonValue(getRenewalFrequency());
        }
        if (m_Allow_couponIsSet) {
            object["allow_coupon"] = JsonValueConverter<std::string>::toJsonValue(getAllowCoupon());
        }
        if (m_Service_moduleIsSet) {
            object["service_module"] = JsonValueConverter<std::string>::toJsonValue(getServiceModule());
        }
        if (m_Created_atIsSet) {
            object["created_at"] = JsonValueConverter<std::string>::toJsonValue(getCreatedAt());
        }
        if (m_Updated_atIsSet) {
            object["updated_at"] = JsonValueConverter<std::string>::toJsonValue(getUpdatedAt());
        }
        if (m_Deleted_atIsSet) {
            object["deleted_at"] = JsonValueConverter<std::string>::toJsonValue(getDeletedAt());
        }
        if (m_CurrencySymbolIsSet) {
            object["currencySymbol"] = JsonValueConverter<std::string>::toJsonValue(getCurrencySymbol());
        }
    return object;
}

void WebsitesOrderJsonServiceOffersItem::fromJsonObject_internal(boost::json::object const& object)
{
    m_Service_offer_idIsSet = false;
    m_Service_idIsSet = false;
    m_Intro_costIsSet = false;
    m_Renewal_costIsSet = false;
    m_Intro_frequencyIsSet = false;
    m_Renewal_frequencyIsSet = false;
    m_Allow_couponIsSet = false;
    m_Service_moduleIsSet = false;
    m_Created_atIsSet = false;
    m_Updated_atIsSet = false;
    m_Deleted_atIsSet = false;
    m_CurrencySymbolIsSet = false;
    {
        const auto Service_offer_idIt = object.find("service_offer_id");
        if (Service_offer_idIt != object.end()) {
            setServiceOfferId(JsonValueConverter<std::string>::fromJsonValue(Service_offer_idIt->value()));
        }
    }
    {
        const auto Service_idIt = object.find("service_id");
        if (Service_idIt != object.end()) {
            setServiceId(JsonValueConverter<std::string>::fromJsonValue(Service_idIt->value()));
        }
    }
    {
        const auto Intro_costIt = object.find("intro_cost");
        if (Intro_costIt != object.end()) {
            setIntroCost(JsonValueConverter<int32_t>::fromJsonValue(Intro_costIt->value()));
        }
    }
    {
        const auto Renewal_costIt = object.find("renewal_cost");
        if (Renewal_costIt != object.end()) {
            setRenewalCost(JsonValueConverter<int32_t>::fromJsonValue(Renewal_costIt->value()));
        }
    }
    {
        const auto Intro_frequencyIt = object.find("intro_frequency");
        if (Intro_frequencyIt != object.end()) {
            setIntroFrequency(JsonValueConverter<std::string>::fromJsonValue(Intro_frequencyIt->value()));
        }
    }
    {
        const auto Renewal_frequencyIt = object.find("renewal_frequency");
        if (Renewal_frequencyIt != object.end()) {
            setRenewalFrequency(JsonValueConverter<std::string>::fromJsonValue(Renewal_frequencyIt->value()));
        }
    }
    {
        const auto Allow_couponIt = object.find("allow_coupon");
        if (Allow_couponIt != object.end()) {
            setAllowCoupon(JsonValueConverter<std::string>::fromJsonValue(Allow_couponIt->value()));
        }
    }
    {
        const auto Service_moduleIt = object.find("service_module");
        if (Service_moduleIt != object.end()) {
            setServiceModule(JsonValueConverter<std::string>::fromJsonValue(Service_moduleIt->value()));
        }
    }
    {
        const auto Created_atIt = object.find("created_at");
        if (Created_atIt != object.end()) {
            setCreatedAt(JsonValueConverter<std::string>::fromJsonValue(Created_atIt->value()));
        }
    }
    {
        const auto Updated_atIt = object.find("updated_at");
        if (Updated_atIt != object.end()) {
            setUpdatedAt(JsonValueConverter<std::string>::fromJsonValue(Updated_atIt->value()));
        }
    }
    {
        const auto Deleted_atIt = object.find("deleted_at");
        if (Deleted_atIt != object.end()) {
            setDeletedAt(JsonValueConverter<std::string>::fromJsonValue(Deleted_atIt->value()));
        }
    }
    {
        const auto CurrencySymbolIt = object.find("currencySymbol");
        if (CurrencySymbolIt != object.end()) {
            setCurrencySymbol(JsonValueConverter<std::string>::fromJsonValue(CurrencySymbolIt->value()));
        }
    }
}

std::string WebsitesOrderJsonServiceOffersItem::getServiceOfferId() const
{
    return m_Service_offer_id;
}

void WebsitesOrderJsonServiceOffersItem::setServiceOfferId(std::string value)
{
    
    m_Service_offer_id = std::move(value);
    m_Service_offer_idIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getServiceId() const
{
    return m_Service_id;
}

void WebsitesOrderJsonServiceOffersItem::setServiceId(std::string value)
{
    
    m_Service_id = std::move(value);
    m_Service_idIsSet = true;
}
int32_t WebsitesOrderJsonServiceOffersItem::getIntroCost() const
{
    return m_Intro_cost;
}

void WebsitesOrderJsonServiceOffersItem::setIntroCost(int32_t value)
{
    
    m_Intro_cost = std::move(value);
    m_Intro_costIsSet = true;
}
int32_t WebsitesOrderJsonServiceOffersItem::getRenewalCost() const
{
    return m_Renewal_cost;
}

void WebsitesOrderJsonServiceOffersItem::setRenewalCost(int32_t value)
{
    
    m_Renewal_cost = std::move(value);
    m_Renewal_costIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getIntroFrequency() const
{
    return m_Intro_frequency;
}

void WebsitesOrderJsonServiceOffersItem::setIntroFrequency(std::string value)
{
    
    m_Intro_frequency = std::move(value);
    m_Intro_frequencyIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getRenewalFrequency() const
{
    return m_Renewal_frequency;
}

void WebsitesOrderJsonServiceOffersItem::setRenewalFrequency(std::string value)
{
    
    m_Renewal_frequency = std::move(value);
    m_Renewal_frequencyIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getAllowCoupon() const
{
    return m_Allow_coupon;
}

void WebsitesOrderJsonServiceOffersItem::setAllowCoupon(std::string value)
{
    
    m_Allow_coupon = std::move(value);
    m_Allow_couponIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getServiceModule() const
{
    return m_Service_module;
}

void WebsitesOrderJsonServiceOffersItem::setServiceModule(std::string value)
{
    
    m_Service_module = std::move(value);
    m_Service_moduleIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getCreatedAt() const
{
    return m_Created_at;
}

void WebsitesOrderJsonServiceOffersItem::setCreatedAt(std::string value)
{
    
    m_Created_at = std::move(value);
    m_Created_atIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getUpdatedAt() const
{
    return m_Updated_at;
}

void WebsitesOrderJsonServiceOffersItem::setUpdatedAt(std::string value)
{
    
    m_Updated_at = std::move(value);
    m_Updated_atIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getDeletedAt() const
{
    return m_Deleted_at;
}

void WebsitesOrderJsonServiceOffersItem::setDeletedAt(std::string value)
{
    
    m_Deleted_at = std::move(value);
    m_Deleted_atIsSet = true;
}
std::string WebsitesOrderJsonServiceOffersItem::getCurrencySymbol() const
{
    return m_CurrencySymbol;
}

void WebsitesOrderJsonServiceOffersItem::setCurrencySymbol(std::string value)
{
    
    m_CurrencySymbol = std::move(value);
    m_CurrencySymbolIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<WebsitesOrderJsonServiceOffersItem>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<WebsitesOrderJsonServiceOffersItem>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<WebsitesOrderJsonServiceOffersItem>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<WebsitesOrderJsonServiceOffersItem>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

