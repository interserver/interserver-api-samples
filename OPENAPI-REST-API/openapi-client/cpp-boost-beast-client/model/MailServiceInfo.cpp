/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "MailServiceInfo.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

MailServiceInfo::MailServiceInfo(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string MailServiceInfo::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void MailServiceInfo::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value MailServiceInfo::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void MailServiceInfo::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for MailServiceInfo");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object MailServiceInfo::toJsonObject_internal() const
{
    boost::json::object object;
        object["mail_id"] = JsonValueConverter<std::string>::toJsonValue(getMailId());
        object["mail_type"] = JsonValueConverter<std::string>::toJsonValue(getMailType());
        object["mail_currency"] = JsonValueConverter<std::string>::toJsonValue(getMailCurrency());
        object["mail_order_date"] = JsonValueConverter<std::string>::toJsonValue(getMailOrderDate());
        object["mail_custid"] = JsonValueConverter<std::string>::toJsonValue(getMailCustid());
        object["mail_quota"] = JsonValueConverter<std::string>::toJsonValue(getMailQuota());
        object["mail_status"] = JsonValueConverter<std::string>::toJsonValue(getMailStatus());
        object["mail_invoice"] = JsonValueConverter<std::string>::toJsonValue(getMailInvoice());
        if (m_Mail_usernameIsSet) {
            object["mail_username"] = JsonValueConverter<std::string>::toJsonValue(getMailUsername());
        }
        if (m_Mail_ipIsSet) {
            object["mail_ip"] = JsonValueConverter<std::string>::toJsonValue(getMailIp());
        }
        if (m_Mail_couponIsSet) {
            object["mail_coupon"] = JsonValueConverter<std::string>::toJsonValue(getMailCoupon());
        }
        if (m_Mail_extraIsSet) {
            object["mail_extra"] = JsonValueConverter<std::string>::toJsonValue(getMailExtra());
        }
        if (m_Mail_server_statusIsSet) {
            object["mail_server_status"] = JsonValueConverter<std::string>::toJsonValue(getMailServerStatus());
        }
        if (m_Mail_commentIsSet) {
            object["mail_comment"] = JsonValueConverter<std::string>::toJsonValue(getMailComment());
        }
    return object;
}

void MailServiceInfo::fromJsonObject_internal(boost::json::object const& object)
{
    m_Mail_usernameIsSet = false;
    m_Mail_ipIsSet = false;
    m_Mail_couponIsSet = false;
    m_Mail_extraIsSet = false;
    m_Mail_server_statusIsSet = false;
    m_Mail_commentIsSet = false;
    {
        const auto Mail_idIt = object.find("mail_id");
        if (Mail_idIt != object.end()) {
            setMailId(JsonValueConverter<std::string>::fromJsonValue(Mail_idIt->value()));
        }
    }
    {
        const auto Mail_typeIt = object.find("mail_type");
        if (Mail_typeIt != object.end()) {
            setMailType(JsonValueConverter<std::string>::fromJsonValue(Mail_typeIt->value()));
        }
    }
    {
        const auto Mail_currencyIt = object.find("mail_currency");
        if (Mail_currencyIt != object.end()) {
            setMailCurrency(JsonValueConverter<std::string>::fromJsonValue(Mail_currencyIt->value()));
        }
    }
    {
        const auto Mail_order_dateIt = object.find("mail_order_date");
        if (Mail_order_dateIt != object.end()) {
            setMailOrderDate(JsonValueConverter<std::string>::fromJsonValue(Mail_order_dateIt->value()));
        }
    }
    {
        const auto Mail_custidIt = object.find("mail_custid");
        if (Mail_custidIt != object.end()) {
            setMailCustid(JsonValueConverter<std::string>::fromJsonValue(Mail_custidIt->value()));
        }
    }
    {
        const auto Mail_quotaIt = object.find("mail_quota");
        if (Mail_quotaIt != object.end()) {
            setMailQuota(JsonValueConverter<std::string>::fromJsonValue(Mail_quotaIt->value()));
        }
    }
    {
        const auto Mail_statusIt = object.find("mail_status");
        if (Mail_statusIt != object.end()) {
            setMailStatus(JsonValueConverter<std::string>::fromJsonValue(Mail_statusIt->value()));
        }
    }
    {
        const auto Mail_invoiceIt = object.find("mail_invoice");
        if (Mail_invoiceIt != object.end()) {
            setMailInvoice(JsonValueConverter<std::string>::fromJsonValue(Mail_invoiceIt->value()));
        }
    }
    {
        const auto Mail_usernameIt = object.find("mail_username");
        if (Mail_usernameIt != object.end()) {
            setMailUsername(JsonValueConverter<std::string>::fromJsonValue(Mail_usernameIt->value()));
        }
    }
    {
        const auto Mail_ipIt = object.find("mail_ip");
        if (Mail_ipIt != object.end()) {
            setMailIp(JsonValueConverter<std::string>::fromJsonValue(Mail_ipIt->value()));
        }
    }
    {
        const auto Mail_couponIt = object.find("mail_coupon");
        if (Mail_couponIt != object.end()) {
            setMailCoupon(JsonValueConverter<std::string>::fromJsonValue(Mail_couponIt->value()));
        }
    }
    {
        const auto Mail_extraIt = object.find("mail_extra");
        if (Mail_extraIt != object.end()) {
            setMailExtra(JsonValueConverter<std::string>::fromJsonValue(Mail_extraIt->value()));
        }
    }
    {
        const auto Mail_server_statusIt = object.find("mail_server_status");
        if (Mail_server_statusIt != object.end()) {
            setMailServerStatus(JsonValueConverter<std::string>::fromJsonValue(Mail_server_statusIt->value()));
        }
    }
    {
        const auto Mail_commentIt = object.find("mail_comment");
        if (Mail_commentIt != object.end()) {
            setMailComment(JsonValueConverter<std::string>::fromJsonValue(Mail_commentIt->value()));
        }
    }
}

std::string MailServiceInfo::getMailId() const
{
    return m_Mail_id;
}

void MailServiceInfo::setMailId(std::string value)
{
    
    m_Mail_id = std::move(value);
}
std::string MailServiceInfo::getMailType() const
{
    return m_Mail_type;
}

void MailServiceInfo::setMailType(std::string value)
{
    
    m_Mail_type = std::move(value);
}
std::string MailServiceInfo::getMailCurrency() const
{
    return m_Mail_currency;
}

void MailServiceInfo::setMailCurrency(std::string value)
{
    
    m_Mail_currency = std::move(value);
}
std::string MailServiceInfo::getMailOrderDate() const
{
    return m_Mail_order_date;
}

void MailServiceInfo::setMailOrderDate(std::string value)
{
    
    m_Mail_order_date = std::move(value);
}
std::string MailServiceInfo::getMailCustid() const
{
    return m_Mail_custid;
}

void MailServiceInfo::setMailCustid(std::string value)
{
    
    m_Mail_custid = std::move(value);
}
std::string MailServiceInfo::getMailQuota() const
{
    return m_Mail_quota;
}

void MailServiceInfo::setMailQuota(std::string value)
{
    
    m_Mail_quota = std::move(value);
}
std::string MailServiceInfo::getMailStatus() const
{
    return m_Mail_status;
}

void MailServiceInfo::setMailStatus(std::string value)
{
    
    m_Mail_status = std::move(value);
}
std::string MailServiceInfo::getMailInvoice() const
{
    return m_Mail_invoice;
}

void MailServiceInfo::setMailInvoice(std::string value)
{
    
    m_Mail_invoice = std::move(value);
}
std::string MailServiceInfo::getMailUsername() const
{
    return m_Mail_username;
}

void MailServiceInfo::setMailUsername(std::string value)
{
    
    m_Mail_username = std::move(value);
    m_Mail_usernameIsSet = true;
}
std::string MailServiceInfo::getMailIp() const
{
    return m_Mail_ip;
}

void MailServiceInfo::setMailIp(std::string value)
{
    
    m_Mail_ip = std::move(value);
    m_Mail_ipIsSet = true;
}
std::string MailServiceInfo::getMailCoupon() const
{
    return m_Mail_coupon;
}

void MailServiceInfo::setMailCoupon(std::string value)
{
    
    m_Mail_coupon = std::move(value);
    m_Mail_couponIsSet = true;
}
std::string MailServiceInfo::getMailExtra() const
{
    return m_Mail_extra;
}

void MailServiceInfo::setMailExtra(std::string value)
{
    
    m_Mail_extra = std::move(value);
    m_Mail_extraIsSet = true;
}
std::string MailServiceInfo::getMailServerStatus() const
{
    return m_Mail_server_status;
}

void MailServiceInfo::setMailServerStatus(std::string value)
{
    
    m_Mail_server_status = std::move(value);
    m_Mail_server_statusIsSet = true;
}
std::string MailServiceInfo::getMailComment() const
{
    return m_Mail_comment;
}

void MailServiceInfo::setMailComment(std::string value)
{
    
    m_Mail_comment = std::move(value);
    m_Mail_commentIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<MailServiceInfo>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<MailServiceInfo>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<MailServiceInfo>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<MailServiceInfo>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

