/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Home_services.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Home_services::Home_services(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Home_services::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Home_services::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Home_services::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Home_services::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Home_services");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Home_services::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_DomainsIsSet) {
            object["domains"] = JsonValueConverter<std::shared_ptr<Home_services_domains>>::toJsonValue(getDomains());
        }
        if (m_WebhostingIsSet) {
            object["webhosting"] = JsonValueConverter<std::shared_ptr<Home_services_webhosting>>::toJsonValue(getWebhosting());
        }
        if (m_VpsIsSet) {
            object["vps"] = JsonValueConverter<std::shared_ptr<Home_services_vps>>::toJsonValue(getVps());
        }
        if (m_LicensesIsSet) {
            object["licenses"] = JsonValueConverter<std::shared_ptr<Home_services_licenses>>::toJsonValue(getLicenses());
        }
        if (m_ServersIsSet) {
            object["servers"] = JsonValueConverter<std::shared_ptr<Home_services_servers>>::toJsonValue(getServers());
        }
        if (m_BackupsIsSet) {
            object["backups"] = JsonValueConverter<std::shared_ptr<Home_services_backups>>::toJsonValue(getBackups());
        }
    return object;
}

void Home_services::fromJsonObject_internal(boost::json::object const& object)
{
    m_DomainsIsSet = false;
    m_WebhostingIsSet = false;
    m_VpsIsSet = false;
    m_LicensesIsSet = false;
    m_ServersIsSet = false;
    m_BackupsIsSet = false;
    {
        const auto DomainsIt = object.find("domains");
        if (DomainsIt != object.end()) {
            setDomains(JsonValueConverter<std::shared_ptr<Home_services_domains>>::fromJsonValue(DomainsIt->value()));
        }
    }
    {
        const auto WebhostingIt = object.find("webhosting");
        if (WebhostingIt != object.end()) {
            setWebhosting(JsonValueConverter<std::shared_ptr<Home_services_webhosting>>::fromJsonValue(WebhostingIt->value()));
        }
    }
    {
        const auto VpsIt = object.find("vps");
        if (VpsIt != object.end()) {
            setVps(JsonValueConverter<std::shared_ptr<Home_services_vps>>::fromJsonValue(VpsIt->value()));
        }
    }
    {
        const auto LicensesIt = object.find("licenses");
        if (LicensesIt != object.end()) {
            setLicenses(JsonValueConverter<std::shared_ptr<Home_services_licenses>>::fromJsonValue(LicensesIt->value()));
        }
    }
    {
        const auto ServersIt = object.find("servers");
        if (ServersIt != object.end()) {
            setServers(JsonValueConverter<std::shared_ptr<Home_services_servers>>::fromJsonValue(ServersIt->value()));
        }
    }
    {
        const auto BackupsIt = object.find("backups");
        if (BackupsIt != object.end()) {
            setBackups(JsonValueConverter<std::shared_ptr<Home_services_backups>>::fromJsonValue(BackupsIt->value()));
        }
    }
}

std::shared_ptr<Home_services_domains> Home_services::getDomains() const
{
    return m_Domains;
}

void Home_services::setDomains(std::shared_ptr<Home_services_domains> value)
{
    
    m_Domains = std::move(value);
    m_DomainsIsSet = true;
}
std::shared_ptr<Home_services_webhosting> Home_services::getWebhosting() const
{
    return m_Webhosting;
}

void Home_services::setWebhosting(std::shared_ptr<Home_services_webhosting> value)
{
    
    m_Webhosting = std::move(value);
    m_WebhostingIsSet = true;
}
std::shared_ptr<Home_services_vps> Home_services::getVps() const
{
    return m_Vps;
}

void Home_services::setVps(std::shared_ptr<Home_services_vps> value)
{
    
    m_Vps = std::move(value);
    m_VpsIsSet = true;
}
std::shared_ptr<Home_services_licenses> Home_services::getLicenses() const
{
    return m_Licenses;
}

void Home_services::setLicenses(std::shared_ptr<Home_services_licenses> value)
{
    
    m_Licenses = std::move(value);
    m_LicensesIsSet = true;
}
std::shared_ptr<Home_services_servers> Home_services::getServers() const
{
    return m_Servers;
}

void Home_services::setServers(std::shared_ptr<Home_services_servers> value)
{
    
    m_Servers = std::move(value);
    m_ServersIsSet = true;
}
std::shared_ptr<Home_services_backups> Home_services::getBackups() const
{
    return m_Backups;
}

void Home_services::setBackups(std::shared_ptr<Home_services_backups> value)
{
    
    m_Backups = std::move(value);
    m_BackupsIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Home_services>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<Home_services>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Home_services>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<Home_services>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

