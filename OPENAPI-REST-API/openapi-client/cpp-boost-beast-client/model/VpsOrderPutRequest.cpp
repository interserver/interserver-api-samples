/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "VpsOrderPutRequest.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

VpsOrderPutRequest::VpsOrderPutRequest(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string VpsOrderPutRequest::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void VpsOrderPutRequest::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value VpsOrderPutRequest::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void VpsOrderPutRequest::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for VpsOrderPutRequest");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object VpsOrderPutRequest::toJsonObject_internal() const
{
    boost::json::object object;
        object["osDistro"] = JsonValueConverter<std::string>::toJsonValue(getOsDistro());
        object["slices"] = JsonValueConverter<int32_t>::toJsonValue(getSlices());
        object["vpsPlatform"] = JsonValueConverter<std::string>::toJsonValue(getVpsPlatform());
        object["period"] = JsonValueConverter<int32_t>::toJsonValue(getPeriod());
        object["location"] = JsonValueConverter<int32_t>::toJsonValue(getLocation());
        object["osVersion"] = JsonValueConverter<std::string>::toJsonValue(getOsVersion());
        object["hostname"] = JsonValueConverter<std::string>::toJsonValue(getHostname());
        object["rootpass"] = JsonValueConverter<std::string>::toJsonValue(getRootpass());
        if (m_ControlpanelIsSet) {
            object["controlpanel"] = JsonValueConverter<std::string>::toJsonValue(getControlpanel());
        }
        if (m_CouponIsSet) {
            object["coupon"] = JsonValueConverter<std::string>::toJsonValue(getCoupon());
        }
        if (m_CommentIsSet) {
            object["comment"] = JsonValueConverter<std::string>::toJsonValue(getComment());
        }
    return object;
}

void VpsOrderPutRequest::fromJsonObject_internal(boost::json::object const& object)
{
    m_ControlpanelIsSet = false;
    m_CouponIsSet = false;
    m_CommentIsSet = false;
    {
        const auto OsDistroIt = object.find("osDistro");
        if (OsDistroIt != object.end()) {
            setOsDistro(JsonValueConverter<std::string>::fromJsonValue(OsDistroIt->value()));
        }
    }
    {
        const auto SlicesIt = object.find("slices");
        if (SlicesIt != object.end()) {
            setSlices(JsonValueConverter<int32_t>::fromJsonValue(SlicesIt->value()));
        }
    }
    {
        const auto VpsPlatformIt = object.find("vpsPlatform");
        if (VpsPlatformIt != object.end()) {
            setVpsPlatform(JsonValueConverter<std::string>::fromJsonValue(VpsPlatformIt->value()));
        }
    }
    {
        const auto PeriodIt = object.find("period");
        if (PeriodIt != object.end()) {
            setPeriod(JsonValueConverter<int32_t>::fromJsonValue(PeriodIt->value()));
        }
    }
    {
        const auto LocationIt = object.find("location");
        if (LocationIt != object.end()) {
            setLocation(JsonValueConverter<int32_t>::fromJsonValue(LocationIt->value()));
        }
    }
    {
        const auto OsVersionIt = object.find("osVersion");
        if (OsVersionIt != object.end()) {
            setOsVersion(JsonValueConverter<std::string>::fromJsonValue(OsVersionIt->value()));
        }
    }
    {
        const auto HostnameIt = object.find("hostname");
        if (HostnameIt != object.end()) {
            setHostname(JsonValueConverter<std::string>::fromJsonValue(HostnameIt->value()));
        }
    }
    {
        const auto RootpassIt = object.find("rootpass");
        if (RootpassIt != object.end()) {
            setRootpass(JsonValueConverter<std::string>::fromJsonValue(RootpassIt->value()));
        }
    }
    {
        const auto ControlpanelIt = object.find("controlpanel");
        if (ControlpanelIt != object.end()) {
            setControlpanel(JsonValueConverter<std::string>::fromJsonValue(ControlpanelIt->value()));
        }
    }
    {
        const auto CouponIt = object.find("coupon");
        if (CouponIt != object.end()) {
            setCoupon(JsonValueConverter<std::string>::fromJsonValue(CouponIt->value()));
        }
    }
    {
        const auto CommentIt = object.find("comment");
        if (CommentIt != object.end()) {
            setComment(JsonValueConverter<std::string>::fromJsonValue(CommentIt->value()));
        }
    }
}

std::string VpsOrderPutRequest::getOsDistro() const
{
    return m_OsDistro;
}

void VpsOrderPutRequest::setOsDistro(std::string value)
{
    
    m_OsDistro = std::move(value);
}
int32_t VpsOrderPutRequest::getSlices() const
{
    return m_Slices;
}

void VpsOrderPutRequest::setSlices(int32_t value)
{
    
    m_Slices = std::move(value);
}
std::string VpsOrderPutRequest::getVpsPlatform() const
{
    return m_VpsPlatform;
}

void VpsOrderPutRequest::setVpsPlatform(std::string value)
{
    static const std::array<std::string, 4> allowedValues = {
        "kvm","hyperv","kvmstorage","11184809"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_VpsPlatform = std::move(value);
}
int32_t VpsOrderPutRequest::getPeriod() const
{
    return m_Period;
}

void VpsOrderPutRequest::setPeriod(int32_t value)
{
    
    m_Period = std::move(value);
}
int32_t VpsOrderPutRequest::getLocation() const
{
    return m_Location;
}

void VpsOrderPutRequest::setLocation(int32_t value)
{
    
    m_Location = std::move(value);
}
std::string VpsOrderPutRequest::getOsVersion() const
{
    return m_OsVersion;
}

void VpsOrderPutRequest::setOsVersion(std::string value)
{
    
    m_OsVersion = std::move(value);
}
std::string VpsOrderPutRequest::getHostname() const
{
    return m_Hostname;
}

void VpsOrderPutRequest::setHostname(std::string value)
{
    
    m_Hostname = std::move(value);
}
std::string VpsOrderPutRequest::getRootpass() const
{
    return m_Rootpass;
}

void VpsOrderPutRequest::setRootpass(std::string value)
{
    
    m_Rootpass = std::move(value);
}
std::string VpsOrderPutRequest::getControlpanel() const
{
    return m_Controlpanel;
}

void VpsOrderPutRequest::setControlpanel(std::string value)
{
    static const std::array<std::string, 4> allowedValues = {
        "none","cpanel","da","11184809"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Controlpanel = std::move(value);
    m_ControlpanelIsSet = true;
}
std::string VpsOrderPutRequest::getCoupon() const
{
    return m_Coupon;
}

void VpsOrderPutRequest::setCoupon(std::string value)
{
    
    m_Coupon = std::move(value);
    m_CouponIsSet = true;
}
std::string VpsOrderPutRequest::getComment() const
{
    return m_Comment;
}

void VpsOrderPutRequest::setComment(std::string value)
{
    
    m_Comment = std::move(value);
    m_CommentIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<VpsOrderPutRequest>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<VpsOrderPutRequest>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<VpsOrderPutRequest>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<VpsOrderPutRequest>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

