/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "SendMailAdv.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

SendMailAdv::SendMailAdv(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string SendMailAdv::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void SendMailAdv::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value SendMailAdv::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void SendMailAdv::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for SendMailAdv");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object SendMailAdv::toJsonObject_internal() const
{
    boost::json::object object;
        object["subject"] = JsonValueConverter<std::string>::toJsonValue(getSubject());
        object["body"] = JsonValueConverter<std::string>::toJsonValue(getBody());
        object["from"] = JsonValueConverter<std::shared_ptr<EmailAddressName>>::toJsonValue(getFrom());
        object["to"] = JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::toJsonValue(getTo());
        if (m_ReplytoIsSet) {
            object["replyto"] = JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::toJsonValue(getReplyto());
        }
        if (m_CcIsSet) {
            object["cc"] = JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::toJsonValue(getCc());
        }
        if (m_BccIsSet) {
            object["bcc"] = JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::toJsonValue(getBcc());
        }
        if (m_AttachmentsIsSet) {
            object["attachments"] = JsonValueConverter<std::vector<std::shared_ptr<MailAttachment>>>::toJsonValue(getAttachments());
        }
        if (m_IdIsSet) {
            object["id"] = JsonValueConverter<int64_t>::toJsonValue(getId());
        }
    return object;
}

void SendMailAdv::fromJsonObject_internal(boost::json::object const& object)
{
    m_ReplytoIsSet = false;
    m_CcIsSet = false;
    m_BccIsSet = false;
    m_AttachmentsIsSet = false;
    m_IdIsSet = false;
    {
        const auto SubjectIt = object.find("subject");
        if (SubjectIt != object.end()) {
            setSubject(JsonValueConverter<std::string>::fromJsonValue(SubjectIt->value()));
        }
    }
    {
        const auto BodyIt = object.find("body");
        if (BodyIt != object.end()) {
            setBody(JsonValueConverter<std::string>::fromJsonValue(BodyIt->value()));
        }
    }
    {
        const auto FromIt = object.find("from");
        if (FromIt != object.end()) {
            setFrom(JsonValueConverter<std::shared_ptr<EmailAddressName>>::fromJsonValue(FromIt->value()));
        }
    }
    {
        const auto ToIt = object.find("to");
        if (ToIt != object.end()) {
            setTo(JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::fromJsonValue(ToIt->value()));
        }
    }
    {
        const auto ReplytoIt = object.find("replyto");
        if (ReplytoIt != object.end()) {
            setReplyto(JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::fromJsonValue(ReplytoIt->value()));
        }
    }
    {
        const auto CcIt = object.find("cc");
        if (CcIt != object.end()) {
            setCc(JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::fromJsonValue(CcIt->value()));
        }
    }
    {
        const auto BccIt = object.find("bcc");
        if (BccIt != object.end()) {
            setBcc(JsonValueConverter<std::vector<std::shared_ptr<EmailAddressName>>>::fromJsonValue(BccIt->value()));
        }
    }
    {
        const auto AttachmentsIt = object.find("attachments");
        if (AttachmentsIt != object.end()) {
            setAttachments(JsonValueConverter<std::vector<std::shared_ptr<MailAttachment>>>::fromJsonValue(AttachmentsIt->value()));
        }
    }
    {
        const auto IdIt = object.find("id");
        if (IdIt != object.end()) {
            setId(JsonValueConverter<int64_t>::fromJsonValue(IdIt->value()));
        }
    }
}

std::string SendMailAdv::getSubject() const
{
    return m_Subject;
}

void SendMailAdv::setSubject(std::string value)
{
    
    m_Subject = std::move(value);
}
std::string SendMailAdv::getBody() const
{
    return m_Body;
}

void SendMailAdv::setBody(std::string value)
{
    
    m_Body = std::move(value);
}
std::shared_ptr<EmailAddressName> SendMailAdv::getFrom() const
{
    return m_From;
}

void SendMailAdv::setFrom(std::shared_ptr<EmailAddressName> value)
{
    
    m_From = std::move(value);
}
std::vector<std::shared_ptr<EmailAddressName>> SendMailAdv::getTo() const
{
    return m_To;
}

void SendMailAdv::setTo(std::vector<std::shared_ptr<EmailAddressName>> value)
{
    
    m_To = std::move(value);
}
std::vector<std::shared_ptr<EmailAddressName>> SendMailAdv::getReplyto() const
{
    return m_Replyto;
}

void SendMailAdv::setReplyto(std::vector<std::shared_ptr<EmailAddressName>> value)
{
    
    m_Replyto = std::move(value);
    m_ReplytoIsSet = true;
}
std::vector<std::shared_ptr<EmailAddressName>> SendMailAdv::getCc() const
{
    return m_Cc;
}

void SendMailAdv::setCc(std::vector<std::shared_ptr<EmailAddressName>> value)
{
    
    m_Cc = std::move(value);
    m_CcIsSet = true;
}
std::vector<std::shared_ptr<EmailAddressName>> SendMailAdv::getBcc() const
{
    return m_Bcc;
}

void SendMailAdv::setBcc(std::vector<std::shared_ptr<EmailAddressName>> value)
{
    
    m_Bcc = std::move(value);
    m_BccIsSet = true;
}
std::vector<std::shared_ptr<MailAttachment>> SendMailAdv::getAttachments() const
{
    return m_Attachments;
}

void SendMailAdv::setAttachments(std::vector<std::shared_ptr<MailAttachment>> value)
{
    
    m_Attachments = std::move(value);
    m_AttachmentsIsSet = true;
}
int64_t SendMailAdv::getId() const
{
    return m_Id;
}

void SendMailAdv::setId(int64_t value)
{
    
    m_Id = std::move(value);
    m_IdIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<SendMailAdv>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<SendMailAdv>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<SendMailAdv>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<SendMailAdv>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

