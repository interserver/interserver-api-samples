/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "DomainAllInfo_attributes.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

DomainAllInfo_attributes::DomainAllInfo_attributes(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string DomainAllInfo_attributes::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void DomainAllInfo_attributes::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value DomainAllInfo_attributes::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void DomainAllInfo_attributes::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for DomainAllInfo_attributes");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object DomainAllInfo_attributes::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Contact_setIsSet) {
            object["contact_set"] = JsonValueConverter<std::shared_ptr<DomainAllInfo_attributes_contact_set>>::toJsonValue(getContactSet());
        }
        if (m_Registry_createdateIsSet) {
            object["registry_createdate"] = JsonValueConverter<std::string>::toJsonValue(getRegistryCreatedate());
        }
        if (m_Registry_expiredateIsSet) {
            object["registry_expiredate"] = JsonValueConverter<std::string>::toJsonValue(getRegistryExpiredate());
        }
        if (m_Tld_dataIsSet) {
            object["tld_data"] = JsonValueConverter<std::string>::toJsonValue(getTldData());
        }
        if (m_Let_expireIsSet) {
            object["let_expire"] = JsonValueConverter<std::string>::toJsonValue(getLetExpire());
        }
        if (m_Auto_renewIsSet) {
            object["auto_renew"] = JsonValueConverter<std::string>::toJsonValue(getAutoRenew());
        }
        if (m_Sponsoring_rspIsSet) {
            object["sponsoring_rsp"] = JsonValueConverter<std::string>::toJsonValue(getSponsoringRsp());
        }
        if (m_Gdpr_consent_statusIsSet) {
            object["gdpr_consent_status"] = JsonValueConverter<std::string>::toJsonValue(getGdprConsentStatus());
        }
        if (m_Nameserver_listIsSet) {
            object["nameserver_list"] = JsonValueConverter<std::vector<std::shared_ptr<DomainNameServer>>>::toJsonValue(getNameserverList());
        }
        if (m_Registry_updatedateIsSet) {
            object["registry_updatedate"] = JsonValueConverter<std::string>::toJsonValue(getRegistryUpdatedate());
        }
        if (m_Affiliate_idIsSet) {
            object["affiliate_id"] = JsonValueConverter<std::string>::toJsonValue(getAffiliateId());
        }
        if (m_ExpiredateIsSet) {
            object["expiredate"] = JsonValueConverter<std::string>::toJsonValue(getExpiredate());
        }
    return object;
}

void DomainAllInfo_attributes::fromJsonObject_internal(boost::json::object const& object)
{
    m_Contact_setIsSet = false;
    m_Registry_createdateIsSet = false;
    m_Registry_expiredateIsSet = false;
    m_Tld_dataIsSet = false;
    m_Let_expireIsSet = false;
    m_Auto_renewIsSet = false;
    m_Sponsoring_rspIsSet = false;
    m_Gdpr_consent_statusIsSet = false;
    m_Nameserver_listIsSet = false;
    m_Registry_updatedateIsSet = false;
    m_Affiliate_idIsSet = false;
    m_ExpiredateIsSet = false;
    {
        const auto Contact_setIt = object.find("contact_set");
        if (Contact_setIt != object.end()) {
            setContactSet(JsonValueConverter<std::shared_ptr<DomainAllInfo_attributes_contact_set>>::fromJsonValue(Contact_setIt->value()));
        }
    }
    {
        const auto Registry_createdateIt = object.find("registry_createdate");
        if (Registry_createdateIt != object.end()) {
            setRegistryCreatedate(JsonValueConverter<std::string>::fromJsonValue(Registry_createdateIt->value()));
        }
    }
    {
        const auto Registry_expiredateIt = object.find("registry_expiredate");
        if (Registry_expiredateIt != object.end()) {
            setRegistryExpiredate(JsonValueConverter<std::string>::fromJsonValue(Registry_expiredateIt->value()));
        }
    }
    {
        const auto Tld_dataIt = object.find("tld_data");
        if (Tld_dataIt != object.end()) {
            setTldData(JsonValueConverter<std::string>::fromJsonValue(Tld_dataIt->value()));
        }
    }
    {
        const auto Let_expireIt = object.find("let_expire");
        if (Let_expireIt != object.end()) {
            setLetExpire(JsonValueConverter<std::string>::fromJsonValue(Let_expireIt->value()));
        }
    }
    {
        const auto Auto_renewIt = object.find("auto_renew");
        if (Auto_renewIt != object.end()) {
            setAutoRenew(JsonValueConverter<std::string>::fromJsonValue(Auto_renewIt->value()));
        }
    }
    {
        const auto Sponsoring_rspIt = object.find("sponsoring_rsp");
        if (Sponsoring_rspIt != object.end()) {
            setSponsoringRsp(JsonValueConverter<std::string>::fromJsonValue(Sponsoring_rspIt->value()));
        }
    }
    {
        const auto Gdpr_consent_statusIt = object.find("gdpr_consent_status");
        if (Gdpr_consent_statusIt != object.end()) {
            setGdprConsentStatus(JsonValueConverter<std::string>::fromJsonValue(Gdpr_consent_statusIt->value()));
        }
    }
    {
        const auto Nameserver_listIt = object.find("nameserver_list");
        if (Nameserver_listIt != object.end()) {
            setNameserverList(JsonValueConverter<std::vector<std::shared_ptr<DomainNameServer>>>::fromJsonValue(Nameserver_listIt->value()));
        }
    }
    {
        const auto Registry_updatedateIt = object.find("registry_updatedate");
        if (Registry_updatedateIt != object.end()) {
            setRegistryUpdatedate(JsonValueConverter<std::string>::fromJsonValue(Registry_updatedateIt->value()));
        }
    }
    {
        const auto Affiliate_idIt = object.find("affiliate_id");
        if (Affiliate_idIt != object.end()) {
            setAffiliateId(JsonValueConverter<std::string>::fromJsonValue(Affiliate_idIt->value()));
        }
    }
    {
        const auto ExpiredateIt = object.find("expiredate");
        if (ExpiredateIt != object.end()) {
            setExpiredate(JsonValueConverter<std::string>::fromJsonValue(ExpiredateIt->value()));
        }
    }
}

std::shared_ptr<DomainAllInfo_attributes_contact_set> DomainAllInfo_attributes::getContactSet() const
{
    return m_Contact_set;
}

void DomainAllInfo_attributes::setContactSet(std::shared_ptr<DomainAllInfo_attributes_contact_set> value)
{
    
    m_Contact_set = std::move(value);
    m_Contact_setIsSet = true;
}
std::string DomainAllInfo_attributes::getRegistryCreatedate() const
{
    return m_Registry_createdate;
}

void DomainAllInfo_attributes::setRegistryCreatedate(std::string value)
{
    
    m_Registry_createdate = std::move(value);
    m_Registry_createdateIsSet = true;
}
std::string DomainAllInfo_attributes::getRegistryExpiredate() const
{
    return m_Registry_expiredate;
}

void DomainAllInfo_attributes::setRegistryExpiredate(std::string value)
{
    
    m_Registry_expiredate = std::move(value);
    m_Registry_expiredateIsSet = true;
}
std::string DomainAllInfo_attributes::getTldData() const
{
    return m_Tld_data;
}

void DomainAllInfo_attributes::setTldData(std::string value)
{
    
    m_Tld_data = std::move(value);
    m_Tld_dataIsSet = true;
}
std::string DomainAllInfo_attributes::getLetExpire() const
{
    return m_Let_expire;
}

void DomainAllInfo_attributes::setLetExpire(std::string value)
{
    
    m_Let_expire = std::move(value);
    m_Let_expireIsSet = true;
}
std::string DomainAllInfo_attributes::getAutoRenew() const
{
    return m_Auto_renew;
}

void DomainAllInfo_attributes::setAutoRenew(std::string value)
{
    
    m_Auto_renew = std::move(value);
    m_Auto_renewIsSet = true;
}
std::string DomainAllInfo_attributes::getSponsoringRsp() const
{
    return m_Sponsoring_rsp;
}

void DomainAllInfo_attributes::setSponsoringRsp(std::string value)
{
    
    m_Sponsoring_rsp = std::move(value);
    m_Sponsoring_rspIsSet = true;
}
std::string DomainAllInfo_attributes::getGdprConsentStatus() const
{
    return m_Gdpr_consent_status;
}

void DomainAllInfo_attributes::setGdprConsentStatus(std::string value)
{
    
    m_Gdpr_consent_status = std::move(value);
    m_Gdpr_consent_statusIsSet = true;
}
std::vector<std::shared_ptr<DomainNameServer>> DomainAllInfo_attributes::getNameserverList() const
{
    return m_Nameserver_list;
}

void DomainAllInfo_attributes::setNameserverList(std::vector<std::shared_ptr<DomainNameServer>> value)
{
    
    m_Nameserver_list = std::move(value);
    m_Nameserver_listIsSet = true;
}
std::string DomainAllInfo_attributes::getRegistryUpdatedate() const
{
    return m_Registry_updatedate;
}

void DomainAllInfo_attributes::setRegistryUpdatedate(std::string value)
{
    
    m_Registry_updatedate = std::move(value);
    m_Registry_updatedateIsSet = true;
}
std::string DomainAllInfo_attributes::getAffiliateId() const
{
    return m_Affiliate_id;
}

void DomainAllInfo_attributes::setAffiliateId(std::string value)
{
    
    m_Affiliate_id = std::move(value);
    m_Affiliate_idIsSet = true;
}
std::string DomainAllInfo_attributes::getExpiredate() const
{
    return m_Expiredate;
}

void DomainAllInfo_attributes::setExpiredate(std::string value)
{
    
    m_Expiredate = std::move(value);
    m_ExpiredateIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<DomainAllInfo_attributes>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<DomainAllInfo_attributes>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<DomainAllInfo_attributes>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<DomainAllInfo_attributes>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

