/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "DomainBillingDetails.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

DomainBillingDetails::DomainBillingDetails(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string DomainBillingDetails::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void DomainBillingDetails::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value DomainBillingDetails::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void DomainBillingDetails::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for DomainBillingDetails");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object DomainBillingDetails::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Service_last_invoice_dateIsSet) {
            object["service_last_invoice_date"] = JsonValueConverter<std::string>::toJsonValue(getServiceLastInvoiceDate());
        }
        if (m_Service_payment_statusIsSet) {
            object["service_payment_status"] = JsonValueConverter<std::string>::toJsonValue(getServicePaymentStatus());
        }
        if (m_Service_frequencyIsSet) {
            object["service_frequency"] = JsonValueConverter<std::string>::toJsonValue(getServiceFrequency());
        }
        if (m_Next_dateIsSet) {
            object["next_date"] = JsonValueConverter<std::string>::toJsonValue(getNextDate());
        }
        if (m_Service_next_invoice_dateIsSet) {
            object["service_next_invoice_date"] = JsonValueConverter<std::string>::toJsonValue(getServiceNextInvoiceDate());
        }
        if (m_Service_currencyIsSet) {
            object["service_currency"] = JsonValueConverter<std::string>::toJsonValue(getServiceCurrency());
        }
        if (m_Service_currency_symbolIsSet) {
            object["service_currency_symbol"] = JsonValueConverter<std::string>::toJsonValue(getServiceCurrencySymbol());
        }
        if (m_Service_cost_infoIsSet) {
            object["service_cost_info"] = JsonValueConverter<std::string>::toJsonValue(getServiceCostInfo());
        }
        if (m_Service_extraIsSet) {
            object["service_extra"] = JsonValueConverter<std::shared_ptr<DomainBillingExtra>>::toJsonValue(getServiceExtra());
        }
        if (m_Service_extra_jsonIsSet) {
            object["service_extra_json"] = JsonValueConverter<std::string>::toJsonValue(getServiceExtraJson());
        }
    return object;
}

void DomainBillingDetails::fromJsonObject_internal(boost::json::object const& object)
{
    m_Service_last_invoice_dateIsSet = false;
    m_Service_payment_statusIsSet = false;
    m_Service_frequencyIsSet = false;
    m_Next_dateIsSet = false;
    m_Service_next_invoice_dateIsSet = false;
    m_Service_currencyIsSet = false;
    m_Service_currency_symbolIsSet = false;
    m_Service_cost_infoIsSet = false;
    m_Service_extraIsSet = false;
    m_Service_extra_jsonIsSet = false;
    {
        const auto Service_last_invoice_dateIt = object.find("service_last_invoice_date");
        if (Service_last_invoice_dateIt != object.end()) {
            setServiceLastInvoiceDate(JsonValueConverter<std::string>::fromJsonValue(Service_last_invoice_dateIt->value()));
        }
    }
    {
        const auto Service_payment_statusIt = object.find("service_payment_status");
        if (Service_payment_statusIt != object.end()) {
            setServicePaymentStatus(JsonValueConverter<std::string>::fromJsonValue(Service_payment_statusIt->value()));
        }
    }
    {
        const auto Service_frequencyIt = object.find("service_frequency");
        if (Service_frequencyIt != object.end()) {
            setServiceFrequency(JsonValueConverter<std::string>::fromJsonValue(Service_frequencyIt->value()));
        }
    }
    {
        const auto Next_dateIt = object.find("next_date");
        if (Next_dateIt != object.end()) {
            setNextDate(JsonValueConverter<std::string>::fromJsonValue(Next_dateIt->value()));
        }
    }
    {
        const auto Service_next_invoice_dateIt = object.find("service_next_invoice_date");
        if (Service_next_invoice_dateIt != object.end()) {
            setServiceNextInvoiceDate(JsonValueConverter<std::string>::fromJsonValue(Service_next_invoice_dateIt->value()));
        }
    }
    {
        const auto Service_currencyIt = object.find("service_currency");
        if (Service_currencyIt != object.end()) {
            setServiceCurrency(JsonValueConverter<std::string>::fromJsonValue(Service_currencyIt->value()));
        }
    }
    {
        const auto Service_currency_symbolIt = object.find("service_currency_symbol");
        if (Service_currency_symbolIt != object.end()) {
            setServiceCurrencySymbol(JsonValueConverter<std::string>::fromJsonValue(Service_currency_symbolIt->value()));
        }
    }
    {
        const auto Service_cost_infoIt = object.find("service_cost_info");
        if (Service_cost_infoIt != object.end()) {
            setServiceCostInfo(JsonValueConverter<std::string>::fromJsonValue(Service_cost_infoIt->value()));
        }
    }
    {
        const auto Service_extraIt = object.find("service_extra");
        if (Service_extraIt != object.end()) {
            setServiceExtra(JsonValueConverter<std::shared_ptr<DomainBillingExtra>>::fromJsonValue(Service_extraIt->value()));
        }
    }
    {
        const auto Service_extra_jsonIt = object.find("service_extra_json");
        if (Service_extra_jsonIt != object.end()) {
            setServiceExtraJson(JsonValueConverter<std::string>::fromJsonValue(Service_extra_jsonIt->value()));
        }
    }
}

std::string DomainBillingDetails::getServiceLastInvoiceDate() const
{
    return m_Service_last_invoice_date;
}

void DomainBillingDetails::setServiceLastInvoiceDate(std::string value)
{
    
    m_Service_last_invoice_date = std::move(value);
    m_Service_last_invoice_dateIsSet = true;
}
std::string DomainBillingDetails::getServicePaymentStatus() const
{
    return m_Service_payment_status;
}

void DomainBillingDetails::setServicePaymentStatus(std::string value)
{
    
    m_Service_payment_status = std::move(value);
    m_Service_payment_statusIsSet = true;
}
std::string DomainBillingDetails::getServiceFrequency() const
{
    return m_Service_frequency;
}

void DomainBillingDetails::setServiceFrequency(std::string value)
{
    
    m_Service_frequency = std::move(value);
    m_Service_frequencyIsSet = true;
}
std::string DomainBillingDetails::getNextDate() const
{
    return m_Next_date;
}

void DomainBillingDetails::setNextDate(std::string value)
{
    
    m_Next_date = std::move(value);
    m_Next_dateIsSet = true;
}
std::string DomainBillingDetails::getServiceNextInvoiceDate() const
{
    return m_Service_next_invoice_date;
}

void DomainBillingDetails::setServiceNextInvoiceDate(std::string value)
{
    
    m_Service_next_invoice_date = std::move(value);
    m_Service_next_invoice_dateIsSet = true;
}
std::string DomainBillingDetails::getServiceCurrency() const
{
    return m_Service_currency;
}

void DomainBillingDetails::setServiceCurrency(std::string value)
{
    
    m_Service_currency = std::move(value);
    m_Service_currencyIsSet = true;
}
std::string DomainBillingDetails::getServiceCurrencySymbol() const
{
    return m_Service_currency_symbol;
}

void DomainBillingDetails::setServiceCurrencySymbol(std::string value)
{
    
    m_Service_currency_symbol = std::move(value);
    m_Service_currency_symbolIsSet = true;
}
std::string DomainBillingDetails::getServiceCostInfo() const
{
    return m_Service_cost_info;
}

void DomainBillingDetails::setServiceCostInfo(std::string value)
{
    
    m_Service_cost_info = std::move(value);
    m_Service_cost_infoIsSet = true;
}
std::shared_ptr<DomainBillingExtra> DomainBillingDetails::getServiceExtra() const
{
    return m_Service_extra;
}

void DomainBillingDetails::setServiceExtra(std::shared_ptr<DomainBillingExtra> value)
{
    
    m_Service_extra = std::move(value);
    m_Service_extraIsSet = true;
}
std::string DomainBillingDetails::getServiceExtraJson() const
{
    return m_Service_extra_json;
}

void DomainBillingDetails::setServiceExtraJson(std::string value)
{
    
    m_Service_extra_json = std::move(value);
    m_Service_extra_jsonIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<DomainBillingDetails>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<DomainBillingDetails>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<DomainBillingDetails>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<DomainBillingDetails>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

