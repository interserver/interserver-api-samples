/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ServerSwitchport.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

ServerSwitchport::ServerSwitchport(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string ServerSwitchport::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void ServerSwitchport::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value ServerSwitchport::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void ServerSwitchport::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for ServerSwitchport");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object ServerSwitchport::toJsonObject_internal() const
{
    boost::json::object object;
        object["switchport_id"] = JsonValueConverter<int32_t>::toJsonValue(getSwitchportId());
        object["switch_id"] = JsonValueConverter<std::string>::toJsonValue(getSwitchId());
        object["switch"] = JsonValueConverter<std::string>::toJsonValue(getRSwitch());
        object["port"] = JsonValueConverter<std::string>::toJsonValue(getPort());
        object["blade"] = JsonValueConverter<std::string>::toJsonValue(getBlade());
        object["justport"] = JsonValueConverter<std::string>::toJsonValue(getJustport());
        object["graph_id"] = JsonValueConverter<std::string>::toJsonValue(getGraphId());
        if (m_VlansIsSet) {
            object["vlans"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getVlans());
        }
        if (m_Vlans6IsSet) {
            object["vlans6"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getVlans6());
        }
        object["asset_id"] = JsonValueConverter<int32_t>::toJsonValue(getAssetId());
    return object;
}

void ServerSwitchport::fromJsonObject_internal(boost::json::object const& object)
{
    m_VlansIsSet = false;
    m_Vlans6IsSet = false;
    {
        const auto Switchport_idIt = object.find("switchport_id");
        if (Switchport_idIt != object.end()) {
            setSwitchportId(JsonValueConverter<int32_t>::fromJsonValue(Switchport_idIt->value()));
        }
    }
    {
        const auto Switch_idIt = object.find("switch_id");
        if (Switch_idIt != object.end()) {
            setSwitchId(JsonValueConverter<std::string>::fromJsonValue(Switch_idIt->value()));
        }
    }
    {
        const auto r_switchIt = object.find("switch");
        if (r_switchIt != object.end()) {
            setRSwitch(JsonValueConverter<std::string>::fromJsonValue(r_switchIt->value()));
        }
    }
    {
        const auto PortIt = object.find("port");
        if (PortIt != object.end()) {
            setPort(JsonValueConverter<std::string>::fromJsonValue(PortIt->value()));
        }
    }
    {
        const auto BladeIt = object.find("blade");
        if (BladeIt != object.end()) {
            setBlade(JsonValueConverter<std::string>::fromJsonValue(BladeIt->value()));
        }
    }
    {
        const auto JustportIt = object.find("justport");
        if (JustportIt != object.end()) {
            setJustport(JsonValueConverter<std::string>::fromJsonValue(JustportIt->value()));
        }
    }
    {
        const auto Graph_idIt = object.find("graph_id");
        if (Graph_idIt != object.end()) {
            setGraphId(JsonValueConverter<std::string>::fromJsonValue(Graph_idIt->value()));
        }
    }
    {
        const auto VlansIt = object.find("vlans");
        if (VlansIt != object.end()) {
            setVlans(JsonValueConverter<std::vector<std::string>>::fromJsonValue(VlansIt->value()));
        }
    }
    {
        const auto Vlans6It = object.find("vlans6");
        if (Vlans6It != object.end()) {
            setVlans6(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Vlans6It->value()));
        }
    }
    {
        const auto Asset_idIt = object.find("asset_id");
        if (Asset_idIt != object.end()) {
            setAssetId(JsonValueConverter<int32_t>::fromJsonValue(Asset_idIt->value()));
        }
    }
}

int32_t ServerSwitchport::getSwitchportId() const
{
    return m_Switchport_id;
}

void ServerSwitchport::setSwitchportId(int32_t value)
{
    
    m_Switchport_id = std::move(value);
}
std::string ServerSwitchport::getSwitchId() const
{
    return m_Switch_id;
}

void ServerSwitchport::setSwitchId(std::string value)
{
    
    m_Switch_id = std::move(value);
}
std::string ServerSwitchport::getRSwitch() const
{
    return m_r_switch;
}

void ServerSwitchport::setRSwitch(std::string value)
{
    
    m_r_switch = std::move(value);
}
std::string ServerSwitchport::getPort() const
{
    return m_Port;
}

void ServerSwitchport::setPort(std::string value)
{
    
    m_Port = std::move(value);
}
std::string ServerSwitchport::getBlade() const
{
    return m_Blade;
}

void ServerSwitchport::setBlade(std::string value)
{
    
    m_Blade = std::move(value);
}
std::string ServerSwitchport::getJustport() const
{
    return m_Justport;
}

void ServerSwitchport::setJustport(std::string value)
{
    
    m_Justport = std::move(value);
}
std::string ServerSwitchport::getGraphId() const
{
    return m_Graph_id;
}

void ServerSwitchport::setGraphId(std::string value)
{
    
    m_Graph_id = std::move(value);
}
std::vector<std::string> ServerSwitchport::getVlans() const
{
    return m_Vlans;
}

void ServerSwitchport::setVlans(std::vector<std::string> value)
{
    
    m_Vlans = std::move(value);
    m_VlansIsSet = true;
}
std::vector<std::string> ServerSwitchport::getVlans6() const
{
    return m_Vlans6;
}

void ServerSwitchport::setVlans6(std::vector<std::string> value)
{
    
    m_Vlans6 = std::move(value);
    m_Vlans6IsSet = true;
}
int32_t ServerSwitchport::getAssetId() const
{
    return m_Asset_id;
}

void ServerSwitchport::setAssetId(int32_t value)
{
    
    m_Asset_id = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<ServerSwitchport>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<ServerSwitchport>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<ServerSwitchport>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<ServerSwitchport>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

