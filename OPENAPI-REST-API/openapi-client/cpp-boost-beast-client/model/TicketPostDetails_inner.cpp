/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "TicketPostDetails_inner.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <algorithm>
#include <array>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {
template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues);

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues);

template<typename EnumValue, std::size_t Size>
void validateEnumValues(
    const EnumValue& value,
    const std::array<EnumValue, Size>& allowedValues)
{
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
}

template<typename Element, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::vector<Element>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value, allowedValues);
    }
}

template<typename MappedValue, typename EnumValue, std::size_t Size>
void validateEnumValues(
    const std::map<std::string, MappedValue>& values,
    const std::array<EnumValue, Size>& allowedValues)
{
    for (const auto& value : values) {
        validateEnumValues(value.second, allowedValues);
    }
}

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

TicketPostDetails_inner::TicketPostDetails_inner(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string TicketPostDetails_inner::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void TicketPostDetails_inner::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value TicketPostDetails_inner::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void TicketPostDetails_inner::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for TicketPostDetails_inner");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object TicketPostDetails_inner::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Post_idIsSet) {
            object["post_id"] = JsonValueConverter<int32_t>::toJsonValue(getPostId());
        }
        if (m_dateIsSet) {
            object["date"] = JsonValueConverter<std::string>::toJsonValue(getDate());
        }
        if (m_ContentsIsSet) {
            object["contents"] = JsonValueConverter<std::string>::toJsonValue(getContents());
        }
        if (m_CreatorIsSet) {
            object["creator"] = JsonValueConverter<std::string>::toJsonValue(getCreator());
        }
        if (m_Creator_emailIsSet) {
            object["creator_email"] = JsonValueConverter<std::string>::toJsonValue(getCreatorEmail());
        }
        if (m_Creator_nameIsSet) {
            object["creator_name"] = JsonValueConverter<std::string>::toJsonValue(getCreatorName());
        }
        if (m_HasattachmentsIsSet) {
            object["hasattachments"] = JsonValueConverter<int32_t>::toJsonValue(getHasattachments());
        }
        if (m_Attachment_downloadIsSet) {
            object["attachment_download"] = JsonValueConverter<std::string>::toJsonValue(getAttachmentDownload());
        }
    return object;
}

void TicketPostDetails_inner::fromJsonObject_internal(boost::json::object const& object)
{
    m_Post_idIsSet = false;
    m_dateIsSet = false;
    m_ContentsIsSet = false;
    m_CreatorIsSet = false;
    m_Creator_emailIsSet = false;
    m_Creator_nameIsSet = false;
    m_HasattachmentsIsSet = false;
    m_Attachment_downloadIsSet = false;
    {
        const auto Post_idIt = object.find("post_id");
        if (Post_idIt != object.end()) {
            setPostId(JsonValueConverter<int32_t>::fromJsonValue(Post_idIt->value()));
        }
    }
    {
        const auto dateIt = object.find("date");
        if (dateIt != object.end()) {
            setDate(JsonValueConverter<std::string>::fromJsonValue(dateIt->value()));
        }
    }
    {
        const auto ContentsIt = object.find("contents");
        if (ContentsIt != object.end()) {
            setContents(JsonValueConverter<std::string>::fromJsonValue(ContentsIt->value()));
        }
    }
    {
        const auto CreatorIt = object.find("creator");
        if (CreatorIt != object.end()) {
            setCreator(JsonValueConverter<std::string>::fromJsonValue(CreatorIt->value()));
        }
    }
    {
        const auto Creator_emailIt = object.find("creator_email");
        if (Creator_emailIt != object.end()) {
            setCreatorEmail(JsonValueConverter<std::string>::fromJsonValue(Creator_emailIt->value()));
        }
    }
    {
        const auto Creator_nameIt = object.find("creator_name");
        if (Creator_nameIt != object.end()) {
            setCreatorName(JsonValueConverter<std::string>::fromJsonValue(Creator_nameIt->value()));
        }
    }
    {
        const auto HasattachmentsIt = object.find("hasattachments");
        if (HasattachmentsIt != object.end()) {
            setHasattachments(JsonValueConverter<int32_t>::fromJsonValue(HasattachmentsIt->value()));
        }
    }
    {
        const auto Attachment_downloadIt = object.find("attachment_download");
        if (Attachment_downloadIt != object.end()) {
            setAttachmentDownload(JsonValueConverter<std::string>::fromJsonValue(Attachment_downloadIt->value()));
        }
    }
}

int32_t TicketPostDetails_inner::getPostId() const
{
    return m_Post_id;
}

void TicketPostDetails_inner::setPostId(int32_t value)
{
    
    m_Post_id = std::move(value);
    m_Post_idIsSet = true;
}
std::string TicketPostDetails_inner::getDate() const
{
    return m_date;
}

void TicketPostDetails_inner::setDate(std::string value)
{
    
    m_date = std::move(value);
    m_dateIsSet = true;
}
std::string TicketPostDetails_inner::getContents() const
{
    return m_Contents;
}

void TicketPostDetails_inner::setContents(std::string value)
{
    
    m_Contents = std::move(value);
    m_ContentsIsSet = true;
}
std::string TicketPostDetails_inner::getCreator() const
{
    return m_Creator;
}

void TicketPostDetails_inner::setCreator(std::string value)
{
    static const std::array<std::string, 3> allowedValues = {
        "User","Staff","11184809"
    };
    if (std::find(allowedValues.begin(), allowedValues.end(), value) == allowedValues.end()) {
        std::ostringstream errorMessage;
        errorMessage << "Value " << value << " not allowed";
        throw std::runtime_error(errorMessage.str());
    }
    
    m_Creator = std::move(value);
    m_CreatorIsSet = true;
}
std::string TicketPostDetails_inner::getCreatorEmail() const
{
    return m_Creator_email;
}

void TicketPostDetails_inner::setCreatorEmail(std::string value)
{
    
    m_Creator_email = std::move(value);
    m_Creator_emailIsSet = true;
}
std::string TicketPostDetails_inner::getCreatorName() const
{
    return m_Creator_name;
}

void TicketPostDetails_inner::setCreatorName(std::string value)
{
    
    m_Creator_name = std::move(value);
    m_Creator_nameIsSet = true;
}
int32_t TicketPostDetails_inner::getHasattachments() const
{
    return m_Hasattachments;
}

void TicketPostDetails_inner::setHasattachments(int32_t value)
{
    
    m_Hasattachments = std::move(value);
    m_HasattachmentsIsSet = true;
}
std::string TicketPostDetails_inner::getAttachmentDownload() const
{
    return m_Attachment_download;
}

void TicketPostDetails_inner::setAttachmentDownload(std::string value)
{
    
    m_Attachment_download = std::move(value);
    m_Attachment_downloadIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<TicketPostDetails_inner>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<TicketPostDetails_inner>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<TicketPostDetails_inner>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<TicketPostDetails_inner>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

