/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "VpsServiceAddons.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

VpsServiceAddons::VpsServiceAddons(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string VpsServiceAddons::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void VpsServiceAddons::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value VpsServiceAddons::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void VpsServiceAddons::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for VpsServiceAddons");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object VpsServiceAddons::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Has_cpanelIsSet) {
            object["has_cpanel"] = JsonValueConverter<bool>::toJsonValue(isHasCpanel());
        }
        if (m_Has_directadminIsSet) {
            object["has_directadmin"] = JsonValueConverter<bool>::toJsonValue(isHasDirectadmin());
        }
        if (m_Has_fantasticoIsSet) {
            object["has_fantastico"] = JsonValueConverter<bool>::toJsonValue(isHasFantastico());
        }
        if (m_Has_softaculousIsSet) {
            object["has_softaculous"] = JsonValueConverter<bool>::toJsonValue(isHasSoftaculous());
        }
        if (m_Has_hdspaceIsSet) {
            object["has_hdspace"] = JsonValueConverter<bool>::toJsonValue(isHasHdspace());
        }
        if (m_Dedicated_ipIsSet) {
            object["dedicated_ip"] = JsonValueConverter<bool>::toJsonValue(isDedicatedIp());
        }
        if (m_Extra_ipsIsSet) {
            object["extra_ips"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getExtraIps());
        }
        if (m_Extra_ips6IsSet) {
            object["extra_ips6"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getExtraIps6());
        }
        if (m_Unpaid_ipsIsSet) {
            object["unpaid_ips"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getUnpaidIps());
        }
        if (m_IpsIsSet) {
            object["ips"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getIps());
        }
        if (m_Ips6IsSet) {
            object["ips6"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getIps6());
        }
        if (m_Cpanel_idIsSet) {
            object["cpanel_id"] = JsonValueConverter<int32_t>::toJsonValue(getCpanelId());
        }
        if (m_CostIsSet) {
            object["cost"] = JsonValueConverter<int32_t>::toJsonValue(getCost());
        }
        if (m_IdsIsSet) {
            object["ids"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getIds());
        }
        if (m_RdataIsSet) {
            object["rdata"] = JsonValueConverter<std::vector<std::string>>::toJsonValue(getRdata());
        }
    return object;
}

void VpsServiceAddons::fromJsonObject_internal(boost::json::object const& object)
{
    m_Has_cpanelIsSet = false;
    m_Has_directadminIsSet = false;
    m_Has_fantasticoIsSet = false;
    m_Has_softaculousIsSet = false;
    m_Has_hdspaceIsSet = false;
    m_Dedicated_ipIsSet = false;
    m_Extra_ipsIsSet = false;
    m_Extra_ips6IsSet = false;
    m_Unpaid_ipsIsSet = false;
    m_IpsIsSet = false;
    m_Ips6IsSet = false;
    m_Cpanel_idIsSet = false;
    m_CostIsSet = false;
    m_IdsIsSet = false;
    m_RdataIsSet = false;
    {
        const auto Has_cpanelIt = object.find("has_cpanel");
        if (Has_cpanelIt != object.end()) {
            setHasCpanel(JsonValueConverter<bool>::fromJsonValue(Has_cpanelIt->value()));
        }
    }
    {
        const auto Has_directadminIt = object.find("has_directadmin");
        if (Has_directadminIt != object.end()) {
            setHasDirectadmin(JsonValueConverter<bool>::fromJsonValue(Has_directadminIt->value()));
        }
    }
    {
        const auto Has_fantasticoIt = object.find("has_fantastico");
        if (Has_fantasticoIt != object.end()) {
            setHasFantastico(JsonValueConverter<bool>::fromJsonValue(Has_fantasticoIt->value()));
        }
    }
    {
        const auto Has_softaculousIt = object.find("has_softaculous");
        if (Has_softaculousIt != object.end()) {
            setHasSoftaculous(JsonValueConverter<bool>::fromJsonValue(Has_softaculousIt->value()));
        }
    }
    {
        const auto Has_hdspaceIt = object.find("has_hdspace");
        if (Has_hdspaceIt != object.end()) {
            setHasHdspace(JsonValueConverter<bool>::fromJsonValue(Has_hdspaceIt->value()));
        }
    }
    {
        const auto Dedicated_ipIt = object.find("dedicated_ip");
        if (Dedicated_ipIt != object.end()) {
            setDedicatedIp(JsonValueConverter<bool>::fromJsonValue(Dedicated_ipIt->value()));
        }
    }
    {
        const auto Extra_ipsIt = object.find("extra_ips");
        if (Extra_ipsIt != object.end()) {
            setExtraIps(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Extra_ipsIt->value()));
        }
    }
    {
        const auto Extra_ips6It = object.find("extra_ips6");
        if (Extra_ips6It != object.end()) {
            setExtraIps6(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Extra_ips6It->value()));
        }
    }
    {
        const auto Unpaid_ipsIt = object.find("unpaid_ips");
        if (Unpaid_ipsIt != object.end()) {
            setUnpaidIps(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Unpaid_ipsIt->value()));
        }
    }
    {
        const auto IpsIt = object.find("ips");
        if (IpsIt != object.end()) {
            setIps(JsonValueConverter<std::vector<std::string>>::fromJsonValue(IpsIt->value()));
        }
    }
    {
        const auto Ips6It = object.find("ips6");
        if (Ips6It != object.end()) {
            setIps6(JsonValueConverter<std::vector<std::string>>::fromJsonValue(Ips6It->value()));
        }
    }
    {
        const auto Cpanel_idIt = object.find("cpanel_id");
        if (Cpanel_idIt != object.end()) {
            setCpanelId(JsonValueConverter<int32_t>::fromJsonValue(Cpanel_idIt->value()));
        }
    }
    {
        const auto CostIt = object.find("cost");
        if (CostIt != object.end()) {
            setCost(JsonValueConverter<int32_t>::fromJsonValue(CostIt->value()));
        }
    }
    {
        const auto IdsIt = object.find("ids");
        if (IdsIt != object.end()) {
            setIds(JsonValueConverter<std::vector<std::string>>::fromJsonValue(IdsIt->value()));
        }
    }
    {
        const auto RdataIt = object.find("rdata");
        if (RdataIt != object.end()) {
            setRdata(JsonValueConverter<std::vector<std::string>>::fromJsonValue(RdataIt->value()));
        }
    }
}

bool VpsServiceAddons::isHasCpanel() const
{
    return m_Has_cpanel;
}

void VpsServiceAddons::setHasCpanel(bool value)
{
    
    m_Has_cpanel = std::move(value);
    m_Has_cpanelIsSet = true;
}
bool VpsServiceAddons::isHasDirectadmin() const
{
    return m_Has_directadmin;
}

void VpsServiceAddons::setHasDirectadmin(bool value)
{
    
    m_Has_directadmin = std::move(value);
    m_Has_directadminIsSet = true;
}
bool VpsServiceAddons::isHasFantastico() const
{
    return m_Has_fantastico;
}

void VpsServiceAddons::setHasFantastico(bool value)
{
    
    m_Has_fantastico = std::move(value);
    m_Has_fantasticoIsSet = true;
}
bool VpsServiceAddons::isHasSoftaculous() const
{
    return m_Has_softaculous;
}

void VpsServiceAddons::setHasSoftaculous(bool value)
{
    
    m_Has_softaculous = std::move(value);
    m_Has_softaculousIsSet = true;
}
bool VpsServiceAddons::isHasHdspace() const
{
    return m_Has_hdspace;
}

void VpsServiceAddons::setHasHdspace(bool value)
{
    
    m_Has_hdspace = std::move(value);
    m_Has_hdspaceIsSet = true;
}
bool VpsServiceAddons::isDedicatedIp() const
{
    return m_Dedicated_ip;
}

void VpsServiceAddons::setDedicatedIp(bool value)
{
    
    m_Dedicated_ip = std::move(value);
    m_Dedicated_ipIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getExtraIps() const
{
    return m_Extra_ips;
}

void VpsServiceAddons::setExtraIps(std::vector<std::string> value)
{
    
    m_Extra_ips = std::move(value);
    m_Extra_ipsIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getExtraIps6() const
{
    return m_Extra_ips6;
}

void VpsServiceAddons::setExtraIps6(std::vector<std::string> value)
{
    
    m_Extra_ips6 = std::move(value);
    m_Extra_ips6IsSet = true;
}
std::vector<std::string> VpsServiceAddons::getUnpaidIps() const
{
    return m_Unpaid_ips;
}

void VpsServiceAddons::setUnpaidIps(std::vector<std::string> value)
{
    
    m_Unpaid_ips = std::move(value);
    m_Unpaid_ipsIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getIps() const
{
    return m_Ips;
}

void VpsServiceAddons::setIps(std::vector<std::string> value)
{
    
    m_Ips = std::move(value);
    m_IpsIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getIps6() const
{
    return m_Ips6;
}

void VpsServiceAddons::setIps6(std::vector<std::string> value)
{
    
    m_Ips6 = std::move(value);
    m_Ips6IsSet = true;
}
int32_t VpsServiceAddons::getCpanelId() const
{
    return m_Cpanel_id;
}

void VpsServiceAddons::setCpanelId(int32_t value)
{
    
    m_Cpanel_id = std::move(value);
    m_Cpanel_idIsSet = true;
}
int32_t VpsServiceAddons::getCost() const
{
    return m_Cost;
}

void VpsServiceAddons::setCost(int32_t value)
{
    
    m_Cost = std::move(value);
    m_CostIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getIds() const
{
    return m_Ids;
}

void VpsServiceAddons::setIds(std::vector<std::string> value)
{
    
    m_Ids = std::move(value);
    m_IdsIsSet = true;
}
std::vector<std::string> VpsServiceAddons::getRdata() const
{
    return m_Rdata;
}

void VpsServiceAddons::setRdata(std::vector<std::string> value)
{
    
    m_Rdata = std::move(value);
    m_RdataIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<VpsServiceAddons>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<VpsServiceAddons>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<VpsServiceAddons>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<VpsServiceAddons>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

