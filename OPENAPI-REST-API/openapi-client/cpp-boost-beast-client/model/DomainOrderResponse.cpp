/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "DomainOrderResponse.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

DomainOrderResponse::DomainOrderResponse(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string DomainOrderResponse::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void DomainOrderResponse::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value DomainOrderResponse::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void DomainOrderResponse::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for DomainOrderResponse");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object DomainOrderResponse::toJsonObject_internal() const
{
    boost::json::object object;
        if (m__OPS_versionIsSet) {
            object["_OPS_version"] = JsonValueConverter<std::string>::toJsonValue(get_OPSVersion());
        }
        if (m_ProtocolIsSet) {
            object["protocol"] = JsonValueConverter<std::string>::toJsonValue(getProtocol());
        }
        if (m_Is_successIsSet) {
            object["is_success"] = JsonValueConverter<std::string>::toJsonValue(getIsSuccess());
        }
        if (m_ActionIsSet) {
            object["action"] = JsonValueConverter<std::string>::toJsonValue(getAction());
        }
        if (m_AttributesIsSet) {
            object["attributes"] = JsonValueConverter<std::shared_ptr<DomainOrderResponse_attributes>>::toJsonValue(getAttributes());
        }
        if (m_Response_textIsSet) {
            object["response_text"] = JsonValueConverter<std::string>::toJsonValue(getResponseText());
        }
        if (m_objectIsSet) {
            object["object"] = JsonValueConverter<std::string>::toJsonValue(getObject());
        }
        if (m_Response_codeIsSet) {
            object["response_code"] = JsonValueConverter<std::string>::toJsonValue(getResponseCode());
        }
    return object;
}

void DomainOrderResponse::fromJsonObject_internal(boost::json::object const& object)
{
    m__OPS_versionIsSet = false;
    m_ProtocolIsSet = false;
    m_Is_successIsSet = false;
    m_ActionIsSet = false;
    m_AttributesIsSet = false;
    m_Response_textIsSet = false;
    m_objectIsSet = false;
    m_Response_codeIsSet = false;
    {
        const auto _OPS_versionIt = object.find("_OPS_version");
        if (_OPS_versionIt != object.end()) {
            set_OPSVersion(JsonValueConverter<std::string>::fromJsonValue(_OPS_versionIt->value()));
        }
    }
    {
        const auto ProtocolIt = object.find("protocol");
        if (ProtocolIt != object.end()) {
            setProtocol(JsonValueConverter<std::string>::fromJsonValue(ProtocolIt->value()));
        }
    }
    {
        const auto Is_successIt = object.find("is_success");
        if (Is_successIt != object.end()) {
            setIsSuccess(JsonValueConverter<std::string>::fromJsonValue(Is_successIt->value()));
        }
    }
    {
        const auto ActionIt = object.find("action");
        if (ActionIt != object.end()) {
            setAction(JsonValueConverter<std::string>::fromJsonValue(ActionIt->value()));
        }
    }
    {
        const auto AttributesIt = object.find("attributes");
        if (AttributesIt != object.end()) {
            setAttributes(JsonValueConverter<std::shared_ptr<DomainOrderResponse_attributes>>::fromJsonValue(AttributesIt->value()));
        }
    }
    {
        const auto Response_textIt = object.find("response_text");
        if (Response_textIt != object.end()) {
            setResponseText(JsonValueConverter<std::string>::fromJsonValue(Response_textIt->value()));
        }
    }
    {
        const auto objectIt = object.find("object");
        if (objectIt != object.end()) {
            setObject(JsonValueConverter<std::string>::fromJsonValue(objectIt->value()));
        }
    }
    {
        const auto Response_codeIt = object.find("response_code");
        if (Response_codeIt != object.end()) {
            setResponseCode(JsonValueConverter<std::string>::fromJsonValue(Response_codeIt->value()));
        }
    }
}

std::string DomainOrderResponse::get_OPSVersion() const
{
    return m__OPS_version;
}

void DomainOrderResponse::set_OPSVersion(std::string value)
{
    
    m__OPS_version = std::move(value);
    m__OPS_versionIsSet = true;
}
std::string DomainOrderResponse::getProtocol() const
{
    return m_Protocol;
}

void DomainOrderResponse::setProtocol(std::string value)
{
    
    m_Protocol = std::move(value);
    m_ProtocolIsSet = true;
}
std::string DomainOrderResponse::getIsSuccess() const
{
    return m_Is_success;
}

void DomainOrderResponse::setIsSuccess(std::string value)
{
    
    m_Is_success = std::move(value);
    m_Is_successIsSet = true;
}
std::string DomainOrderResponse::getAction() const
{
    return m_Action;
}

void DomainOrderResponse::setAction(std::string value)
{
    
    m_Action = std::move(value);
    m_ActionIsSet = true;
}
std::shared_ptr<DomainOrderResponse_attributes> DomainOrderResponse::getAttributes() const
{
    return m_Attributes;
}

void DomainOrderResponse::setAttributes(std::shared_ptr<DomainOrderResponse_attributes> value)
{
    
    m_Attributes = std::move(value);
    m_AttributesIsSet = true;
}
std::string DomainOrderResponse::getResponseText() const
{
    return m_Response_text;
}

void DomainOrderResponse::setResponseText(std::string value)
{
    
    m_Response_text = std::move(value);
    m_Response_textIsSet = true;
}
std::string DomainOrderResponse::getObject() const
{
    return m_object;
}

void DomainOrderResponse::setObject(std::string value)
{
    
    m_object = std::move(value);
    m_objectIsSet = true;
}
std::string DomainOrderResponse::getResponseCode() const
{
    return m_Response_code;
}

void DomainOrderResponse::setResponseCode(std::string value)
{
    
    m_Response_code = std::move(value);
    m_Response_codeIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<DomainOrderResponse>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<DomainOrderResponse>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<DomainOrderResponse>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<DomainOrderResponse>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

