/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "WebsitesOrder.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

WebsitesOrder::WebsitesOrder(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string WebsitesOrder::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void WebsitesOrder::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value WebsitesOrder::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void WebsitesOrder::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for WebsitesOrder");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object WebsitesOrder::toJsonObject_internal() const
{
    boost::json::object object;
        object["step"] = JsonValueConverter<std::string>::toJsonValue(getStep());
        object["website"] = JsonValueConverter<std::string>::toJsonValue(getWebsite());
        object["period"] = JsonValueConverter<int32_t>::toJsonValue(getPeriod());
        object["serviceOfferId"] = JsonValueConverter<int32_t>::toJsonValue(getServiceOfferId());
        object["packages"] = JsonValueConverter<std::shared_ptr<WebsitesOrderPackages>>::toJsonValue(getPackages());
        object["enableDomainRegistering"] = JsonValueConverter<bool>::toJsonValue(isEnableDomainRegistering());
        object["jsonServices"] = JsonValueConverter<std::shared_ptr<WebsitesOrderJsonServices>>::toJsonValue(getJsonServices());
        object["jsonServiceOffers"] = JsonValueConverter<std::shared_ptr<WebsitesOrderJsonServiceOffers>>::toJsonValue(getJsonServiceOffers());
        object["serviceTypes"] = JsonValueConverter<std::shared_ptr<WebsitesOrder_serviceTypes>>::toJsonValue(getServiceTypes());
        object["serviceOffers"] = JsonValueConverter<std::shared_ptr<WebsitesOrder_serviceOffers>>::toJsonValue(getServiceOffers());
        if (m_PackgesIsSet) {
            object["packges"] = JsonValueConverter<std::shared_ptr<WebsitesOrder_packges>>::toJsonValue(getPackges());
        }
    return object;
}

void WebsitesOrder::fromJsonObject_internal(boost::json::object const& object)
{
    m_PackgesIsSet = false;
    {
        const auto StepIt = object.find("step");
        if (StepIt != object.end()) {
            setStep(JsonValueConverter<std::string>::fromJsonValue(StepIt->value()));
        }
    }
    {
        const auto WebsiteIt = object.find("website");
        if (WebsiteIt != object.end()) {
            setWebsite(JsonValueConverter<std::string>::fromJsonValue(WebsiteIt->value()));
        }
    }
    {
        const auto PeriodIt = object.find("period");
        if (PeriodIt != object.end()) {
            setPeriod(JsonValueConverter<int32_t>::fromJsonValue(PeriodIt->value()));
        }
    }
    {
        const auto ServiceOfferIdIt = object.find("serviceOfferId");
        if (ServiceOfferIdIt != object.end()) {
            setServiceOfferId(JsonValueConverter<int32_t>::fromJsonValue(ServiceOfferIdIt->value()));
        }
    }
    {
        const auto PackagesIt = object.find("packages");
        if (PackagesIt != object.end()) {
            setPackages(JsonValueConverter<std::shared_ptr<WebsitesOrderPackages>>::fromJsonValue(PackagesIt->value()));
        }
    }
    {
        const auto EnableDomainRegisteringIt = object.find("enableDomainRegistering");
        if (EnableDomainRegisteringIt != object.end()) {
            setEnableDomainRegistering(JsonValueConverter<bool>::fromJsonValue(EnableDomainRegisteringIt->value()));
        }
    }
    {
        const auto JsonServicesIt = object.find("jsonServices");
        if (JsonServicesIt != object.end()) {
            setJsonServices(JsonValueConverter<std::shared_ptr<WebsitesOrderJsonServices>>::fromJsonValue(JsonServicesIt->value()));
        }
    }
    {
        const auto JsonServiceOffersIt = object.find("jsonServiceOffers");
        if (JsonServiceOffersIt != object.end()) {
            setJsonServiceOffers(JsonValueConverter<std::shared_ptr<WebsitesOrderJsonServiceOffers>>::fromJsonValue(JsonServiceOffersIt->value()));
        }
    }
    {
        const auto ServiceTypesIt = object.find("serviceTypes");
        if (ServiceTypesIt != object.end()) {
            setServiceTypes(JsonValueConverter<std::shared_ptr<WebsitesOrder_serviceTypes>>::fromJsonValue(ServiceTypesIt->value()));
        }
    }
    {
        const auto ServiceOffersIt = object.find("serviceOffers");
        if (ServiceOffersIt != object.end()) {
            setServiceOffers(JsonValueConverter<std::shared_ptr<WebsitesOrder_serviceOffers>>::fromJsonValue(ServiceOffersIt->value()));
        }
    }
    {
        const auto PackgesIt = object.find("packges");
        if (PackgesIt != object.end()) {
            setPackges(JsonValueConverter<std::shared_ptr<WebsitesOrder_packges>>::fromJsonValue(PackgesIt->value()));
        }
    }
}

std::string WebsitesOrder::getStep() const
{
    return m_Step;
}

void WebsitesOrder::setStep(std::string value)
{
    
    m_Step = std::move(value);
}
std::string WebsitesOrder::getWebsite() const
{
    return m_Website;
}

void WebsitesOrder::setWebsite(std::string value)
{
    
    m_Website = std::move(value);
}
int32_t WebsitesOrder::getPeriod() const
{
    return m_Period;
}

void WebsitesOrder::setPeriod(int32_t value)
{
    
    m_Period = std::move(value);
}
int32_t WebsitesOrder::getServiceOfferId() const
{
    return m_ServiceOfferId;
}

void WebsitesOrder::setServiceOfferId(int32_t value)
{
    
    m_ServiceOfferId = std::move(value);
}
std::shared_ptr<WebsitesOrderPackages> WebsitesOrder::getPackages() const
{
    return m_Packages;
}

void WebsitesOrder::setPackages(std::shared_ptr<WebsitesOrderPackages> value)
{
    
    m_Packages = std::move(value);
}
bool WebsitesOrder::isEnableDomainRegistering() const
{
    return m_EnableDomainRegistering;
}

void WebsitesOrder::setEnableDomainRegistering(bool value)
{
    
    m_EnableDomainRegistering = std::move(value);
}
std::shared_ptr<WebsitesOrderJsonServices> WebsitesOrder::getJsonServices() const
{
    return m_JsonServices;
}

void WebsitesOrder::setJsonServices(std::shared_ptr<WebsitesOrderJsonServices> value)
{
    
    m_JsonServices = std::move(value);
}
std::shared_ptr<WebsitesOrderJsonServiceOffers> WebsitesOrder::getJsonServiceOffers() const
{
    return m_JsonServiceOffers;
}

void WebsitesOrder::setJsonServiceOffers(std::shared_ptr<WebsitesOrderJsonServiceOffers> value)
{
    
    m_JsonServiceOffers = std::move(value);
}
std::shared_ptr<WebsitesOrder_serviceTypes> WebsitesOrder::getServiceTypes() const
{
    return m_ServiceTypes;
}

void WebsitesOrder::setServiceTypes(std::shared_ptr<WebsitesOrder_serviceTypes> value)
{
    
    m_ServiceTypes = std::move(value);
}
std::shared_ptr<WebsitesOrder_serviceOffers> WebsitesOrder::getServiceOffers() const
{
    return m_ServiceOffers;
}

void WebsitesOrder::setServiceOffers(std::shared_ptr<WebsitesOrder_serviceOffers> value)
{
    
    m_ServiceOffers = std::move(value);
}
std::shared_ptr<WebsitesOrder_packges> WebsitesOrder::getPackges() const
{
    return m_Packges;
}

void WebsitesOrder::setPackges(std::shared_ptr<WebsitesOrder_packges> value)
{
    
    m_Packges = std::move(value);
    m_PackgesIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<WebsitesOrder>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<WebsitesOrder>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<WebsitesOrder>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<WebsitesOrder>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

