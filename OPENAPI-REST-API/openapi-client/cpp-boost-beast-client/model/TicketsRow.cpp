/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "TicketsRow.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

TicketsRow::TicketsRow(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string TicketsRow::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void TicketsRow::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value TicketsRow::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void TicketsRow::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for TicketsRow");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object TicketsRow::toJsonObject_internal() const
{
    boost::json::object object;
        object["title"] = JsonValueConverter<std::string>::toJsonValue(getTitle());
        object["ticketmaskid"] = JsonValueConverter<std::string>::toJsonValue(getTicketmaskid());
        object["lastreplier"] = JsonValueConverter<std::string>::toJsonValue(getLastreplier());
        object["status"] = JsonValueConverter<std::string>::toJsonValue(getStatus());
        object["priority"] = JsonValueConverter<std::string>::toJsonValue(getPriority());
        object["total_replies"] = JsonValueConverter<int32_t>::toJsonValue(getTotalReplies());
        object["lastactivity"] = JsonValueConverter<std::string>::toJsonValue(getLastactivity());
        object["departmenttitle"] = JsonValueConverter<std::string>::toJsonValue(getDepartmenttitle());
        object["ticketid"] = JsonValueConverter<int32_t>::toJsonValue(getTicketid());
        object["can_close"] = JsonValueConverter<std::string>::toJsonValue(getCanClose());
        object["attachments"] = JsonValueConverter<boost::json::value>::toJsonValue(getAttachments());
        object["status_text"] = JsonValueConverter<std::string>::toJsonValue(getStatusText());
        object["checked"] = JsonValueConverter<bool>::toJsonValue(isChecked());
    return object;
}

void TicketsRow::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto TitleIt = object.find("title");
        if (TitleIt != object.end()) {
            setTitle(JsonValueConverter<std::string>::fromJsonValue(TitleIt->value()));
        }
    }
    {
        const auto TicketmaskidIt = object.find("ticketmaskid");
        if (TicketmaskidIt != object.end()) {
            setTicketmaskid(JsonValueConverter<std::string>::fromJsonValue(TicketmaskidIt->value()));
        }
    }
    {
        const auto LastreplierIt = object.find("lastreplier");
        if (LastreplierIt != object.end()) {
            setLastreplier(JsonValueConverter<std::string>::fromJsonValue(LastreplierIt->value()));
        }
    }
    {
        const auto StatusIt = object.find("status");
        if (StatusIt != object.end()) {
            setStatus(JsonValueConverter<std::string>::fromJsonValue(StatusIt->value()));
        }
    }
    {
        const auto PriorityIt = object.find("priority");
        if (PriorityIt != object.end()) {
            setPriority(JsonValueConverter<std::string>::fromJsonValue(PriorityIt->value()));
        }
    }
    {
        const auto Total_repliesIt = object.find("total_replies");
        if (Total_repliesIt != object.end()) {
            setTotalReplies(JsonValueConverter<int32_t>::fromJsonValue(Total_repliesIt->value()));
        }
    }
    {
        const auto LastactivityIt = object.find("lastactivity");
        if (LastactivityIt != object.end()) {
            setLastactivity(JsonValueConverter<std::string>::fromJsonValue(LastactivityIt->value()));
        }
    }
    {
        const auto DepartmenttitleIt = object.find("departmenttitle");
        if (DepartmenttitleIt != object.end()) {
            setDepartmenttitle(JsonValueConverter<std::string>::fromJsonValue(DepartmenttitleIt->value()));
        }
    }
    {
        const auto TicketidIt = object.find("ticketid");
        if (TicketidIt != object.end()) {
            setTicketid(JsonValueConverter<int32_t>::fromJsonValue(TicketidIt->value()));
        }
    }
    {
        const auto Can_closeIt = object.find("can_close");
        if (Can_closeIt != object.end()) {
            setCanClose(JsonValueConverter<std::string>::fromJsonValue(Can_closeIt->value()));
        }
    }
    {
        const auto AttachmentsIt = object.find("attachments");
        if (AttachmentsIt != object.end()) {
            setAttachments(JsonValueConverter<boost::json::value>::fromJsonValue(AttachmentsIt->value()));
        }
    }
    {
        const auto Status_textIt = object.find("status_text");
        if (Status_textIt != object.end()) {
            setStatusText(JsonValueConverter<std::string>::fromJsonValue(Status_textIt->value()));
        }
    }
    {
        const auto CheckedIt = object.find("checked");
        if (CheckedIt != object.end()) {
            setChecked(JsonValueConverter<bool>::fromJsonValue(CheckedIt->value()));
        }
    }
}

std::string TicketsRow::getTitle() const
{
    return m_Title;
}

void TicketsRow::setTitle(std::string value)
{
    
    m_Title = std::move(value);
}
std::string TicketsRow::getTicketmaskid() const
{
    return m_Ticketmaskid;
}

void TicketsRow::setTicketmaskid(std::string value)
{
    
    m_Ticketmaskid = std::move(value);
}
std::string TicketsRow::getLastreplier() const
{
    return m_Lastreplier;
}

void TicketsRow::setLastreplier(std::string value)
{
    
    m_Lastreplier = std::move(value);
}
std::string TicketsRow::getStatus() const
{
    return m_Status;
}

void TicketsRow::setStatus(std::string value)
{
    
    m_Status = std::move(value);
}
std::string TicketsRow::getPriority() const
{
    return m_Priority;
}

void TicketsRow::setPriority(std::string value)
{
    
    m_Priority = std::move(value);
}
int32_t TicketsRow::getTotalReplies() const
{
    return m_Total_replies;
}

void TicketsRow::setTotalReplies(int32_t value)
{
    
    m_Total_replies = std::move(value);
}
std::string TicketsRow::getLastactivity() const
{
    return m_Lastactivity;
}

void TicketsRow::setLastactivity(std::string value)
{
    
    m_Lastactivity = std::move(value);
}
std::string TicketsRow::getDepartmenttitle() const
{
    return m_Departmenttitle;
}

void TicketsRow::setDepartmenttitle(std::string value)
{
    
    m_Departmenttitle = std::move(value);
}
int32_t TicketsRow::getTicketid() const
{
    return m_Ticketid;
}

void TicketsRow::setTicketid(int32_t value)
{
    
    m_Ticketid = std::move(value);
}
std::string TicketsRow::getCanClose() const
{
    return m_Can_close;
}

void TicketsRow::setCanClose(std::string value)
{
    
    m_Can_close = std::move(value);
}
boost::json::value TicketsRow::getAttachments() const
{
    return m_Attachments;
}

void TicketsRow::setAttachments(boost::json::value value)
{
    
    m_Attachments = std::move(value);
}
std::string TicketsRow::getStatusText() const
{
    return m_Status_text;
}

void TicketsRow::setStatusText(std::string value)
{
    
    m_Status_text = std::move(value);
}
bool TicketsRow::isChecked() const
{
    return m_Checked;
}

void TicketsRow::setChecked(bool value)
{
    
    m_Checked = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<TicketsRow>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<TicketsRow>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<TicketsRow>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<TicketsRow>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

