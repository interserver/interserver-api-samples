/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BackupServiceMaster.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BackupServiceMaster::BackupServiceMaster(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BackupServiceMaster::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BackupServiceMaster::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BackupServiceMaster::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BackupServiceMaster::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BackupServiceMaster");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BackupServiceMaster::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Backup_idIsSet) {
            object["backup_id"] = JsonValueConverter<int32_t>::toJsonValue(getBackupId());
        }
        if (m_Backup_nameIsSet) {
            object["backup_name"] = JsonValueConverter<std::string>::toJsonValue(getBackupName());
        }
        if (m_Backup_ipIsSet) {
            object["backup_ip"] = JsonValueConverter<std::string>::toJsonValue(getBackupIp());
        }
        if (m_Backup_typeIsSet) {
            object["backup_type"] = JsonValueConverter<int32_t>::toJsonValue(getBackupType());
        }
        if (m_Backup_hdsizeIsSet) {
            object["backup_hdsize"] = JsonValueConverter<int32_t>::toJsonValue(getBackupHdsize());
        }
        if (m_Backup_hdfreeIsSet) {
            object["backup_hdfree"] = JsonValueConverter<int32_t>::toJsonValue(getBackupHdfree());
        }
        if (m_Backup_last_updateIsSet) {
            object["backup_last_update"] = JsonValueConverter<std::string>::toJsonValue(getBackupLastUpdate());
        }
        if (m_Backup_availableIsSet) {
            object["backup_available"] = JsonValueConverter<int32_t>::toJsonValue(getBackupAvailable());
        }
        if (m_Backup_iowaitIsSet) {
            object["backup_iowait"] = JsonValueConverter<int32_t>::toJsonValue(getBackupIowait());
        }
        if (m_Backup_orderIsSet) {
            object["backup_order"] = JsonValueConverter<int32_t>::toJsonValue(getBackupOrder());
        }
    return object;
}

void BackupServiceMaster::fromJsonObject_internal(boost::json::object const& object)
{
    m_Backup_idIsSet = false;
    m_Backup_nameIsSet = false;
    m_Backup_ipIsSet = false;
    m_Backup_typeIsSet = false;
    m_Backup_hdsizeIsSet = false;
    m_Backup_hdfreeIsSet = false;
    m_Backup_last_updateIsSet = false;
    m_Backup_availableIsSet = false;
    m_Backup_iowaitIsSet = false;
    m_Backup_orderIsSet = false;
    {
        const auto Backup_idIt = object.find("backup_id");
        if (Backup_idIt != object.end()) {
            setBackupId(JsonValueConverter<int32_t>::fromJsonValue(Backup_idIt->value()));
        }
    }
    {
        const auto Backup_nameIt = object.find("backup_name");
        if (Backup_nameIt != object.end()) {
            setBackupName(JsonValueConverter<std::string>::fromJsonValue(Backup_nameIt->value()));
        }
    }
    {
        const auto Backup_ipIt = object.find("backup_ip");
        if (Backup_ipIt != object.end()) {
            setBackupIp(JsonValueConverter<std::string>::fromJsonValue(Backup_ipIt->value()));
        }
    }
    {
        const auto Backup_typeIt = object.find("backup_type");
        if (Backup_typeIt != object.end()) {
            setBackupType(JsonValueConverter<int32_t>::fromJsonValue(Backup_typeIt->value()));
        }
    }
    {
        const auto Backup_hdsizeIt = object.find("backup_hdsize");
        if (Backup_hdsizeIt != object.end()) {
            setBackupHdsize(JsonValueConverter<int32_t>::fromJsonValue(Backup_hdsizeIt->value()));
        }
    }
    {
        const auto Backup_hdfreeIt = object.find("backup_hdfree");
        if (Backup_hdfreeIt != object.end()) {
            setBackupHdfree(JsonValueConverter<int32_t>::fromJsonValue(Backup_hdfreeIt->value()));
        }
    }
    {
        const auto Backup_last_updateIt = object.find("backup_last_update");
        if (Backup_last_updateIt != object.end()) {
            setBackupLastUpdate(JsonValueConverter<std::string>::fromJsonValue(Backup_last_updateIt->value()));
        }
    }
    {
        const auto Backup_availableIt = object.find("backup_available");
        if (Backup_availableIt != object.end()) {
            setBackupAvailable(JsonValueConverter<int32_t>::fromJsonValue(Backup_availableIt->value()));
        }
    }
    {
        const auto Backup_iowaitIt = object.find("backup_iowait");
        if (Backup_iowaitIt != object.end()) {
            setBackupIowait(JsonValueConverter<int32_t>::fromJsonValue(Backup_iowaitIt->value()));
        }
    }
    {
        const auto Backup_orderIt = object.find("backup_order");
        if (Backup_orderIt != object.end()) {
            setBackupOrder(JsonValueConverter<int32_t>::fromJsonValue(Backup_orderIt->value()));
        }
    }
}

int32_t BackupServiceMaster::getBackupId() const
{
    return m_Backup_id;
}

void BackupServiceMaster::setBackupId(int32_t value)
{
    
    m_Backup_id = std::move(value);
    m_Backup_idIsSet = true;
}
std::string BackupServiceMaster::getBackupName() const
{
    return m_Backup_name;
}

void BackupServiceMaster::setBackupName(std::string value)
{
    
    m_Backup_name = std::move(value);
    m_Backup_nameIsSet = true;
}
std::string BackupServiceMaster::getBackupIp() const
{
    return m_Backup_ip;
}

void BackupServiceMaster::setBackupIp(std::string value)
{
    
    m_Backup_ip = std::move(value);
    m_Backup_ipIsSet = true;
}
int32_t BackupServiceMaster::getBackupType() const
{
    return m_Backup_type;
}

void BackupServiceMaster::setBackupType(int32_t value)
{
    
    m_Backup_type = std::move(value);
    m_Backup_typeIsSet = true;
}
int32_t BackupServiceMaster::getBackupHdsize() const
{
    return m_Backup_hdsize;
}

void BackupServiceMaster::setBackupHdsize(int32_t value)
{
    
    m_Backup_hdsize = std::move(value);
    m_Backup_hdsizeIsSet = true;
}
int32_t BackupServiceMaster::getBackupHdfree() const
{
    return m_Backup_hdfree;
}

void BackupServiceMaster::setBackupHdfree(int32_t value)
{
    
    m_Backup_hdfree = std::move(value);
    m_Backup_hdfreeIsSet = true;
}
std::string BackupServiceMaster::getBackupLastUpdate() const
{
    return m_Backup_last_update;
}

void BackupServiceMaster::setBackupLastUpdate(std::string value)
{
    
    m_Backup_last_update = std::move(value);
    m_Backup_last_updateIsSet = true;
}
int32_t BackupServiceMaster::getBackupAvailable() const
{
    return m_Backup_available;
}

void BackupServiceMaster::setBackupAvailable(int32_t value)
{
    
    m_Backup_available = std::move(value);
    m_Backup_availableIsSet = true;
}
int32_t BackupServiceMaster::getBackupIowait() const
{
    return m_Backup_iowait;
}

void BackupServiceMaster::setBackupIowait(int32_t value)
{
    
    m_Backup_iowait = std::move(value);
    m_Backup_iowaitIsSet = true;
}
int32_t BackupServiceMaster::getBackupOrder() const
{
    return m_Backup_order;
}

void BackupServiceMaster::setBackupOrder(int32_t value)
{
    
    m_Backup_order = std::move(value);
    m_Backup_orderIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BackupServiceMaster>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BackupServiceMaster>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BackupServiceMaster>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BackupServiceMaster>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

