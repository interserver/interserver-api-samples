/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "Tickets.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

Tickets::Tickets(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string Tickets::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void Tickets::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value Tickets::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void Tickets::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for Tickets");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object Tickets::toJsonObject_internal() const
{
    boost::json::object object;
        object["ima"] = JsonValueConverter<std::string>::toJsonValue(getIma());
        object["custid"] = JsonValueConverter<std::string>::toJsonValue(getCustid());
        object["view"] = JsonValueConverter<std::string>::toJsonValue(getView());
        object["currentPage"] = JsonValueConverter<int32_t>::toJsonValue(getCurrentPage());
        object["limit"] = JsonValueConverter<int32_t>::toJsonValue(getLimit());
        object["sortcol"] = JsonValueConverter<int32_t>::toJsonValue(getSortcol());
        object["sortdir"] = JsonValueConverter<int32_t>::toJsonValue(getSortdir());
        object["rowsOffset"] = JsonValueConverter<int32_t>::toJsonValue(getRowsOffset());
        object["tickets"] = JsonValueConverter<std::vector<std::shared_ptr<TicketsRow>>>::toJsonValue(getTickets());
        object["pages"] = JsonValueConverter<int32_t>::toJsonValue(getPages());
        object["rowsTotal"] = JsonValueConverter<int32_t>::toJsonValue(getRowsTotal());
        object["inboxCount"] = JsonValueConverter<int32_t>::toJsonValue(getInboxCount());
        object["countArray"] = JsonValueConverter<std::shared_ptr<Tickets_countArray>>::toJsonValue(getCountArray());
        object["viewText"] = JsonValueConverter<std::string>::toJsonValue(getViewText());
    return object;
}

void Tickets::fromJsonObject_internal(boost::json::object const& object)
{
    {
        const auto ImaIt = object.find("ima");
        if (ImaIt != object.end()) {
            setIma(JsonValueConverter<std::string>::fromJsonValue(ImaIt->value()));
        }
    }
    {
        const auto CustidIt = object.find("custid");
        if (CustidIt != object.end()) {
            setCustid(JsonValueConverter<std::string>::fromJsonValue(CustidIt->value()));
        }
    }
    {
        const auto ViewIt = object.find("view");
        if (ViewIt != object.end()) {
            setView(JsonValueConverter<std::string>::fromJsonValue(ViewIt->value()));
        }
    }
    {
        const auto CurrentPageIt = object.find("currentPage");
        if (CurrentPageIt != object.end()) {
            setCurrentPage(JsonValueConverter<int32_t>::fromJsonValue(CurrentPageIt->value()));
        }
    }
    {
        const auto LimitIt = object.find("limit");
        if (LimitIt != object.end()) {
            setLimit(JsonValueConverter<int32_t>::fromJsonValue(LimitIt->value()));
        }
    }
    {
        const auto SortcolIt = object.find("sortcol");
        if (SortcolIt != object.end()) {
            setSortcol(JsonValueConverter<int32_t>::fromJsonValue(SortcolIt->value()));
        }
    }
    {
        const auto SortdirIt = object.find("sortdir");
        if (SortdirIt != object.end()) {
            setSortdir(JsonValueConverter<int32_t>::fromJsonValue(SortdirIt->value()));
        }
    }
    {
        const auto RowsOffsetIt = object.find("rowsOffset");
        if (RowsOffsetIt != object.end()) {
            setRowsOffset(JsonValueConverter<int32_t>::fromJsonValue(RowsOffsetIt->value()));
        }
    }
    {
        const auto TicketsIt = object.find("tickets");
        if (TicketsIt != object.end()) {
            setTickets(JsonValueConverter<std::vector<std::shared_ptr<TicketsRow>>>::fromJsonValue(TicketsIt->value()));
        }
    }
    {
        const auto PagesIt = object.find("pages");
        if (PagesIt != object.end()) {
            setPages(JsonValueConverter<int32_t>::fromJsonValue(PagesIt->value()));
        }
    }
    {
        const auto RowsTotalIt = object.find("rowsTotal");
        if (RowsTotalIt != object.end()) {
            setRowsTotal(JsonValueConverter<int32_t>::fromJsonValue(RowsTotalIt->value()));
        }
    }
    {
        const auto InboxCountIt = object.find("inboxCount");
        if (InboxCountIt != object.end()) {
            setInboxCount(JsonValueConverter<int32_t>::fromJsonValue(InboxCountIt->value()));
        }
    }
    {
        const auto CountArrayIt = object.find("countArray");
        if (CountArrayIt != object.end()) {
            setCountArray(JsonValueConverter<std::shared_ptr<Tickets_countArray>>::fromJsonValue(CountArrayIt->value()));
        }
    }
    {
        const auto ViewTextIt = object.find("viewText");
        if (ViewTextIt != object.end()) {
            setViewText(JsonValueConverter<std::string>::fromJsonValue(ViewTextIt->value()));
        }
    }
}

std::string Tickets::getIma() const
{
    return m_Ima;
}

void Tickets::setIma(std::string value)
{
    
    m_Ima = std::move(value);
}
std::string Tickets::getCustid() const
{
    return m_Custid;
}

void Tickets::setCustid(std::string value)
{
    
    m_Custid = std::move(value);
}
std::string Tickets::getView() const
{
    return m_View;
}

void Tickets::setView(std::string value)
{
    
    m_View = std::move(value);
}
int32_t Tickets::getCurrentPage() const
{
    return m_CurrentPage;
}

void Tickets::setCurrentPage(int32_t value)
{
    
    m_CurrentPage = std::move(value);
}
int32_t Tickets::getLimit() const
{
    return m_Limit;
}

void Tickets::setLimit(int32_t value)
{
    
    m_Limit = std::move(value);
}
int32_t Tickets::getSortcol() const
{
    return m_Sortcol;
}

void Tickets::setSortcol(int32_t value)
{
    
    m_Sortcol = std::move(value);
}
int32_t Tickets::getSortdir() const
{
    return m_Sortdir;
}

void Tickets::setSortdir(int32_t value)
{
    
    m_Sortdir = std::move(value);
}
int32_t Tickets::getRowsOffset() const
{
    return m_RowsOffset;
}

void Tickets::setRowsOffset(int32_t value)
{
    
    m_RowsOffset = std::move(value);
}
std::vector<std::shared_ptr<TicketsRow>> Tickets::getTickets() const
{
    return m_Tickets;
}

void Tickets::setTickets(std::vector<std::shared_ptr<TicketsRow>> value)
{
    
    m_Tickets = std::move(value);
}
int32_t Tickets::getPages() const
{
    return m_Pages;
}

void Tickets::setPages(int32_t value)
{
    
    m_Pages = std::move(value);
}
int32_t Tickets::getRowsTotal() const
{
    return m_RowsTotal;
}

void Tickets::setRowsTotal(int32_t value)
{
    
    m_RowsTotal = std::move(value);
}
int32_t Tickets::getInboxCount() const
{
    return m_InboxCount;
}

void Tickets::setInboxCount(int32_t value)
{
    
    m_InboxCount = std::move(value);
}
std::shared_ptr<Tickets_countArray> Tickets::getCountArray() const
{
    return m_CountArray;
}

void Tickets::setCountArray(std::shared_ptr<Tickets_countArray> value)
{
    
    m_CountArray = std::move(value);
}
std::string Tickets::getViewText() const
{
    return m_ViewText;
}

void Tickets::setViewText(std::string value)
{
    
    m_ViewText = std::move(value);
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<Tickets>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<Tickets>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<Tickets>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<Tickets>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

