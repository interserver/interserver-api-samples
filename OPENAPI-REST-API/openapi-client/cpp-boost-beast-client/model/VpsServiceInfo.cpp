/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "VpsServiceInfo.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

VpsServiceInfo::VpsServiceInfo(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string VpsServiceInfo::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void VpsServiceInfo::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value VpsServiceInfo::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void VpsServiceInfo::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for VpsServiceInfo");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object VpsServiceInfo::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Vps_idIsSet) {
            object["vps_id"] = JsonValueConverter<std::string>::toJsonValue(getVpsId());
        }
        if (m_Vps_custidIsSet) {
            object["vps_custid"] = JsonValueConverter<std::string>::toJsonValue(getVpsCustid());
        }
        if (m_Vps_serverIsSet) {
            object["vps_server"] = JsonValueConverter<std::string>::toJsonValue(getVpsServer());
        }
        if (m_Vps_ipIsSet) {
            object["vps_ip"] = JsonValueConverter<std::string>::toJsonValue(getVpsIp());
        }
        if (m_Vps_ipv6IsSet) {
            object["vps_ipv6"] = JsonValueConverter<std::string>::toJsonValue(getVpsIpv6());
        }
        if (m_Vps_vzidIsSet) {
            object["vps_vzid"] = JsonValueConverter<std::string>::toJsonValue(getVpsVzid());
        }
        if (m_Vps_currencyIsSet) {
            object["vps_currency"] = JsonValueConverter<std::string>::toJsonValue(getVpsCurrency());
        }
        if (m_Vps_typeIsSet) {
            object["vps_type"] = JsonValueConverter<std::string>::toJsonValue(getVpsType());
        }
        if (m_Vps_order_dateIsSet) {
            object["vps_order_date"] = JsonValueConverter<std::string>::toJsonValue(getVpsOrderDate());
        }
        if (m_Vps_statusIsSet) {
            object["vps_status"] = JsonValueConverter<std::string>::toJsonValue(getVpsStatus());
        }
        if (m_Vps_invoiceIsSet) {
            object["vps_invoice"] = JsonValueConverter<std::string>::toJsonValue(getVpsInvoice());
        }
        if (m_Vps_couponIsSet) {
            object["vps_coupon"] = JsonValueConverter<std::string>::toJsonValue(getVpsCoupon());
        }
        if (m_Vps_extraIsSet) {
            object["vps_extra"] = JsonValueConverter<std::string>::toJsonValue(getVpsExtra());
        }
        if (m_Vps_hostnameIsSet) {
            object["vps_hostname"] = JsonValueConverter<std::string>::toJsonValue(getVpsHostname());
        }
        if (m_Vps_server_statusIsSet) {
            object["vps_server_status"] = JsonValueConverter<std::string>::toJsonValue(getVpsServerStatus());
        }
        if (m_Vps_commentIsSet) {
            object["vps_comment"] = JsonValueConverter<std::string>::toJsonValue(getVpsComment());
        }
        if (m_Vps_slicesIsSet) {
            object["vps_slices"] = JsonValueConverter<std::string>::toJsonValue(getVpsSlices());
        }
        if (m_Vps_vncIsSet) {
            object["vps_vnc"] = JsonValueConverter<std::string>::toJsonValue(getVpsVnc());
        }
        if (m_Vps_vnc_portIsSet) {
            object["vps_vnc_port"] = JsonValueConverter<std::string>::toJsonValue(getVpsVncPort());
        }
        if (m_Vps_rootpassIsSet) {
            object["vps_rootpass"] = JsonValueConverter<std::string>::toJsonValue(getVpsRootpass());
        }
        if (m_Vps_macIsSet) {
            object["vps_mac"] = JsonValueConverter<std::string>::toJsonValue(getVpsMac());
        }
        if (m_Vps_osIsSet) {
            object["vps_os"] = JsonValueConverter<std::string>::toJsonValue(getVpsOs());
        }
        if (m_Vps_versionIsSet) {
            object["vps_version"] = JsonValueConverter<std::string>::toJsonValue(getVpsVersion());
        }
        if (m_Vps_locationIsSet) {
            object["vps_location"] = JsonValueConverter<std::string>::toJsonValue(getVpsLocation());
        }
        if (m_Vps_platformIsSet) {
            object["vps_platform"] = JsonValueConverter<std::string>::toJsonValue(getVpsPlatform());
        }
        if (m_Vps_diskusedIsSet) {
            object["vps_diskused"] = JsonValueConverter<std::string>::toJsonValue(getVpsDiskused());
        }
        if (m_Vps_diskmaxIsSet) {
            object["vps_diskmax"] = JsonValueConverter<std::string>::toJsonValue(getVpsDiskmax());
        }
    return object;
}

void VpsServiceInfo::fromJsonObject_internal(boost::json::object const& object)
{
    m_Vps_idIsSet = false;
    m_Vps_custidIsSet = false;
    m_Vps_serverIsSet = false;
    m_Vps_ipIsSet = false;
    m_Vps_ipv6IsSet = false;
    m_Vps_vzidIsSet = false;
    m_Vps_currencyIsSet = false;
    m_Vps_typeIsSet = false;
    m_Vps_order_dateIsSet = false;
    m_Vps_statusIsSet = false;
    m_Vps_invoiceIsSet = false;
    m_Vps_couponIsSet = false;
    m_Vps_extraIsSet = false;
    m_Vps_hostnameIsSet = false;
    m_Vps_server_statusIsSet = false;
    m_Vps_commentIsSet = false;
    m_Vps_slicesIsSet = false;
    m_Vps_vncIsSet = false;
    m_Vps_vnc_portIsSet = false;
    m_Vps_rootpassIsSet = false;
    m_Vps_macIsSet = false;
    m_Vps_osIsSet = false;
    m_Vps_versionIsSet = false;
    m_Vps_locationIsSet = false;
    m_Vps_platformIsSet = false;
    m_Vps_diskusedIsSet = false;
    m_Vps_diskmaxIsSet = false;
    {
        const auto Vps_idIt = object.find("vps_id");
        if (Vps_idIt != object.end()) {
            setVpsId(JsonValueConverter<std::string>::fromJsonValue(Vps_idIt->value()));
        }
    }
    {
        const auto Vps_custidIt = object.find("vps_custid");
        if (Vps_custidIt != object.end()) {
            setVpsCustid(JsonValueConverter<std::string>::fromJsonValue(Vps_custidIt->value()));
        }
    }
    {
        const auto Vps_serverIt = object.find("vps_server");
        if (Vps_serverIt != object.end()) {
            setVpsServer(JsonValueConverter<std::string>::fromJsonValue(Vps_serverIt->value()));
        }
    }
    {
        const auto Vps_ipIt = object.find("vps_ip");
        if (Vps_ipIt != object.end()) {
            setVpsIp(JsonValueConverter<std::string>::fromJsonValue(Vps_ipIt->value()));
        }
    }
    {
        const auto Vps_ipv6It = object.find("vps_ipv6");
        if (Vps_ipv6It != object.end()) {
            setVpsIpv6(JsonValueConverter<std::string>::fromJsonValue(Vps_ipv6It->value()));
        }
    }
    {
        const auto Vps_vzidIt = object.find("vps_vzid");
        if (Vps_vzidIt != object.end()) {
            setVpsVzid(JsonValueConverter<std::string>::fromJsonValue(Vps_vzidIt->value()));
        }
    }
    {
        const auto Vps_currencyIt = object.find("vps_currency");
        if (Vps_currencyIt != object.end()) {
            setVpsCurrency(JsonValueConverter<std::string>::fromJsonValue(Vps_currencyIt->value()));
        }
    }
    {
        const auto Vps_typeIt = object.find("vps_type");
        if (Vps_typeIt != object.end()) {
            setVpsType(JsonValueConverter<std::string>::fromJsonValue(Vps_typeIt->value()));
        }
    }
    {
        const auto Vps_order_dateIt = object.find("vps_order_date");
        if (Vps_order_dateIt != object.end()) {
            setVpsOrderDate(JsonValueConverter<std::string>::fromJsonValue(Vps_order_dateIt->value()));
        }
    }
    {
        const auto Vps_statusIt = object.find("vps_status");
        if (Vps_statusIt != object.end()) {
            setVpsStatus(JsonValueConverter<std::string>::fromJsonValue(Vps_statusIt->value()));
        }
    }
    {
        const auto Vps_invoiceIt = object.find("vps_invoice");
        if (Vps_invoiceIt != object.end()) {
            setVpsInvoice(JsonValueConverter<std::string>::fromJsonValue(Vps_invoiceIt->value()));
        }
    }
    {
        const auto Vps_couponIt = object.find("vps_coupon");
        if (Vps_couponIt != object.end()) {
            setVpsCoupon(JsonValueConverter<std::string>::fromJsonValue(Vps_couponIt->value()));
        }
    }
    {
        const auto Vps_extraIt = object.find("vps_extra");
        if (Vps_extraIt != object.end()) {
            setVpsExtra(JsonValueConverter<std::string>::fromJsonValue(Vps_extraIt->value()));
        }
    }
    {
        const auto Vps_hostnameIt = object.find("vps_hostname");
        if (Vps_hostnameIt != object.end()) {
            setVpsHostname(JsonValueConverter<std::string>::fromJsonValue(Vps_hostnameIt->value()));
        }
    }
    {
        const auto Vps_server_statusIt = object.find("vps_server_status");
        if (Vps_server_statusIt != object.end()) {
            setVpsServerStatus(JsonValueConverter<std::string>::fromJsonValue(Vps_server_statusIt->value()));
        }
    }
    {
        const auto Vps_commentIt = object.find("vps_comment");
        if (Vps_commentIt != object.end()) {
            setVpsComment(JsonValueConverter<std::string>::fromJsonValue(Vps_commentIt->value()));
        }
    }
    {
        const auto Vps_slicesIt = object.find("vps_slices");
        if (Vps_slicesIt != object.end()) {
            setVpsSlices(JsonValueConverter<std::string>::fromJsonValue(Vps_slicesIt->value()));
        }
    }
    {
        const auto Vps_vncIt = object.find("vps_vnc");
        if (Vps_vncIt != object.end()) {
            setVpsVnc(JsonValueConverter<std::string>::fromJsonValue(Vps_vncIt->value()));
        }
    }
    {
        const auto Vps_vnc_portIt = object.find("vps_vnc_port");
        if (Vps_vnc_portIt != object.end()) {
            setVpsVncPort(JsonValueConverter<std::string>::fromJsonValue(Vps_vnc_portIt->value()));
        }
    }
    {
        const auto Vps_rootpassIt = object.find("vps_rootpass");
        if (Vps_rootpassIt != object.end()) {
            setVpsRootpass(JsonValueConverter<std::string>::fromJsonValue(Vps_rootpassIt->value()));
        }
    }
    {
        const auto Vps_macIt = object.find("vps_mac");
        if (Vps_macIt != object.end()) {
            setVpsMac(JsonValueConverter<std::string>::fromJsonValue(Vps_macIt->value()));
        }
    }
    {
        const auto Vps_osIt = object.find("vps_os");
        if (Vps_osIt != object.end()) {
            setVpsOs(JsonValueConverter<std::string>::fromJsonValue(Vps_osIt->value()));
        }
    }
    {
        const auto Vps_versionIt = object.find("vps_version");
        if (Vps_versionIt != object.end()) {
            setVpsVersion(JsonValueConverter<std::string>::fromJsonValue(Vps_versionIt->value()));
        }
    }
    {
        const auto Vps_locationIt = object.find("vps_location");
        if (Vps_locationIt != object.end()) {
            setVpsLocation(JsonValueConverter<std::string>::fromJsonValue(Vps_locationIt->value()));
        }
    }
    {
        const auto Vps_platformIt = object.find("vps_platform");
        if (Vps_platformIt != object.end()) {
            setVpsPlatform(JsonValueConverter<std::string>::fromJsonValue(Vps_platformIt->value()));
        }
    }
    {
        const auto Vps_diskusedIt = object.find("vps_diskused");
        if (Vps_diskusedIt != object.end()) {
            setVpsDiskused(JsonValueConverter<std::string>::fromJsonValue(Vps_diskusedIt->value()));
        }
    }
    {
        const auto Vps_diskmaxIt = object.find("vps_diskmax");
        if (Vps_diskmaxIt != object.end()) {
            setVpsDiskmax(JsonValueConverter<std::string>::fromJsonValue(Vps_diskmaxIt->value()));
        }
    }
}

std::string VpsServiceInfo::getVpsId() const
{
    return m_Vps_id;
}

void VpsServiceInfo::setVpsId(std::string value)
{
    
    m_Vps_id = std::move(value);
    m_Vps_idIsSet = true;
}
std::string VpsServiceInfo::getVpsCustid() const
{
    return m_Vps_custid;
}

void VpsServiceInfo::setVpsCustid(std::string value)
{
    
    m_Vps_custid = std::move(value);
    m_Vps_custidIsSet = true;
}
std::string VpsServiceInfo::getVpsServer() const
{
    return m_Vps_server;
}

void VpsServiceInfo::setVpsServer(std::string value)
{
    
    m_Vps_server = std::move(value);
    m_Vps_serverIsSet = true;
}
std::string VpsServiceInfo::getVpsIp() const
{
    return m_Vps_ip;
}

void VpsServiceInfo::setVpsIp(std::string value)
{
    
    m_Vps_ip = std::move(value);
    m_Vps_ipIsSet = true;
}
std::string VpsServiceInfo::getVpsIpv6() const
{
    return m_Vps_ipv6;
}

void VpsServiceInfo::setVpsIpv6(std::string value)
{
    
    m_Vps_ipv6 = std::move(value);
    m_Vps_ipv6IsSet = true;
}
std::string VpsServiceInfo::getVpsVzid() const
{
    return m_Vps_vzid;
}

void VpsServiceInfo::setVpsVzid(std::string value)
{
    
    m_Vps_vzid = std::move(value);
    m_Vps_vzidIsSet = true;
}
std::string VpsServiceInfo::getVpsCurrency() const
{
    return m_Vps_currency;
}

void VpsServiceInfo::setVpsCurrency(std::string value)
{
    
    m_Vps_currency = std::move(value);
    m_Vps_currencyIsSet = true;
}
std::string VpsServiceInfo::getVpsType() const
{
    return m_Vps_type;
}

void VpsServiceInfo::setVpsType(std::string value)
{
    
    m_Vps_type = std::move(value);
    m_Vps_typeIsSet = true;
}
std::string VpsServiceInfo::getVpsOrderDate() const
{
    return m_Vps_order_date;
}

void VpsServiceInfo::setVpsOrderDate(std::string value)
{
    
    m_Vps_order_date = std::move(value);
    m_Vps_order_dateIsSet = true;
}
std::string VpsServiceInfo::getVpsStatus() const
{
    return m_Vps_status;
}

void VpsServiceInfo::setVpsStatus(std::string value)
{
    
    m_Vps_status = std::move(value);
    m_Vps_statusIsSet = true;
}
std::string VpsServiceInfo::getVpsInvoice() const
{
    return m_Vps_invoice;
}

void VpsServiceInfo::setVpsInvoice(std::string value)
{
    
    m_Vps_invoice = std::move(value);
    m_Vps_invoiceIsSet = true;
}
std::string VpsServiceInfo::getVpsCoupon() const
{
    return m_Vps_coupon;
}

void VpsServiceInfo::setVpsCoupon(std::string value)
{
    
    m_Vps_coupon = std::move(value);
    m_Vps_couponIsSet = true;
}
std::string VpsServiceInfo::getVpsExtra() const
{
    return m_Vps_extra;
}

void VpsServiceInfo::setVpsExtra(std::string value)
{
    
    m_Vps_extra = std::move(value);
    m_Vps_extraIsSet = true;
}
std::string VpsServiceInfo::getVpsHostname() const
{
    return m_Vps_hostname;
}

void VpsServiceInfo::setVpsHostname(std::string value)
{
    
    m_Vps_hostname = std::move(value);
    m_Vps_hostnameIsSet = true;
}
std::string VpsServiceInfo::getVpsServerStatus() const
{
    return m_Vps_server_status;
}

void VpsServiceInfo::setVpsServerStatus(std::string value)
{
    
    m_Vps_server_status = std::move(value);
    m_Vps_server_statusIsSet = true;
}
std::string VpsServiceInfo::getVpsComment() const
{
    return m_Vps_comment;
}

void VpsServiceInfo::setVpsComment(std::string value)
{
    
    m_Vps_comment = std::move(value);
    m_Vps_commentIsSet = true;
}
std::string VpsServiceInfo::getVpsSlices() const
{
    return m_Vps_slices;
}

void VpsServiceInfo::setVpsSlices(std::string value)
{
    
    m_Vps_slices = std::move(value);
    m_Vps_slicesIsSet = true;
}
std::string VpsServiceInfo::getVpsVnc() const
{
    return m_Vps_vnc;
}

void VpsServiceInfo::setVpsVnc(std::string value)
{
    
    m_Vps_vnc = std::move(value);
    m_Vps_vncIsSet = true;
}
std::string VpsServiceInfo::getVpsVncPort() const
{
    return m_Vps_vnc_port;
}

void VpsServiceInfo::setVpsVncPort(std::string value)
{
    
    m_Vps_vnc_port = std::move(value);
    m_Vps_vnc_portIsSet = true;
}
std::string VpsServiceInfo::getVpsRootpass() const
{
    return m_Vps_rootpass;
}

void VpsServiceInfo::setVpsRootpass(std::string value)
{
    
    m_Vps_rootpass = std::move(value);
    m_Vps_rootpassIsSet = true;
}
std::string VpsServiceInfo::getVpsMac() const
{
    return m_Vps_mac;
}

void VpsServiceInfo::setVpsMac(std::string value)
{
    
    m_Vps_mac = std::move(value);
    m_Vps_macIsSet = true;
}
std::string VpsServiceInfo::getVpsOs() const
{
    return m_Vps_os;
}

void VpsServiceInfo::setVpsOs(std::string value)
{
    
    m_Vps_os = std::move(value);
    m_Vps_osIsSet = true;
}
std::string VpsServiceInfo::getVpsVersion() const
{
    return m_Vps_version;
}

void VpsServiceInfo::setVpsVersion(std::string value)
{
    
    m_Vps_version = std::move(value);
    m_Vps_versionIsSet = true;
}
std::string VpsServiceInfo::getVpsLocation() const
{
    return m_Vps_location;
}

void VpsServiceInfo::setVpsLocation(std::string value)
{
    
    m_Vps_location = std::move(value);
    m_Vps_locationIsSet = true;
}
std::string VpsServiceInfo::getVpsPlatform() const
{
    return m_Vps_platform;
}

void VpsServiceInfo::setVpsPlatform(std::string value)
{
    
    m_Vps_platform = std::move(value);
    m_Vps_platformIsSet = true;
}
std::string VpsServiceInfo::getVpsDiskused() const
{
    return m_Vps_diskused;
}

void VpsServiceInfo::setVpsDiskused(std::string value)
{
    
    m_Vps_diskused = std::move(value);
    m_Vps_diskusedIsSet = true;
}
std::string VpsServiceInfo::getVpsDiskmax() const
{
    return m_Vps_diskmax;
}

void VpsServiceInfo::setVpsDiskmax(std::string value)
{
    
    m_Vps_diskmax = std::move(value);
    m_Vps_diskmaxIsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<VpsServiceInfo>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<VpsServiceInfo>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<VpsServiceInfo>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<VpsServiceInfo>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

