/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "BillingAddCcRequest.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

BillingAddCcRequest::BillingAddCcRequest(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string BillingAddCcRequest::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void BillingAddCcRequest::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value BillingAddCcRequest::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void BillingAddCcRequest::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for BillingAddCcRequest");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object BillingAddCcRequest::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_NameIsSet) {
            object["name"] = JsonValueConverter<std::string>::toJsonValue(getName());
        }
        if (m_AddressIsSet) {
            object["address"] = JsonValueConverter<std::string>::toJsonValue(getAddress());
        }
        if (m_CityIsSet) {
            object["city"] = JsonValueConverter<std::string>::toJsonValue(getCity());
        }
        if (m_StateIsSet) {
            object["state"] = JsonValueConverter<std::string>::toJsonValue(getState());
        }
        if (m_CountryIsSet) {
            object["country"] = JsonValueConverter<std::string>::toJsonValue(getCountry());
        }
        if (m_ZipIsSet) {
            object["zip"] = JsonValueConverter<std::string>::toJsonValue(getZip());
        }
        if (m_CcIsSet) {
            object["cc"] = JsonValueConverter<std::string>::toJsonValue(getCc());
        }
        if (m_Cc_expIsSet) {
            object["cc_exp"] = JsonValueConverter<std::string>::toJsonValue(getCcExp());
        }
        if (m_Cc_ccv2IsSet) {
            object["cc_ccv2"] = JsonValueConverter<std::string>::toJsonValue(getCcCcv2());
        }
    return object;
}

void BillingAddCcRequest::fromJsonObject_internal(boost::json::object const& object)
{
    m_NameIsSet = false;
    m_AddressIsSet = false;
    m_CityIsSet = false;
    m_StateIsSet = false;
    m_CountryIsSet = false;
    m_ZipIsSet = false;
    m_CcIsSet = false;
    m_Cc_expIsSet = false;
    m_Cc_ccv2IsSet = false;
    {
        const auto NameIt = object.find("name");
        if (NameIt != object.end()) {
            setName(JsonValueConverter<std::string>::fromJsonValue(NameIt->value()));
        }
    }
    {
        const auto AddressIt = object.find("address");
        if (AddressIt != object.end()) {
            setAddress(JsonValueConverter<std::string>::fromJsonValue(AddressIt->value()));
        }
    }
    {
        const auto CityIt = object.find("city");
        if (CityIt != object.end()) {
            setCity(JsonValueConverter<std::string>::fromJsonValue(CityIt->value()));
        }
    }
    {
        const auto StateIt = object.find("state");
        if (StateIt != object.end()) {
            setState(JsonValueConverter<std::string>::fromJsonValue(StateIt->value()));
        }
    }
    {
        const auto CountryIt = object.find("country");
        if (CountryIt != object.end()) {
            setCountry(JsonValueConverter<std::string>::fromJsonValue(CountryIt->value()));
        }
    }
    {
        const auto ZipIt = object.find("zip");
        if (ZipIt != object.end()) {
            setZip(JsonValueConverter<std::string>::fromJsonValue(ZipIt->value()));
        }
    }
    {
        const auto CcIt = object.find("cc");
        if (CcIt != object.end()) {
            setCc(JsonValueConverter<std::string>::fromJsonValue(CcIt->value()));
        }
    }
    {
        const auto Cc_expIt = object.find("cc_exp");
        if (Cc_expIt != object.end()) {
            setCcExp(JsonValueConverter<std::string>::fromJsonValue(Cc_expIt->value()));
        }
    }
    {
        const auto Cc_ccv2It = object.find("cc_ccv2");
        if (Cc_ccv2It != object.end()) {
            setCcCcv2(JsonValueConverter<std::string>::fromJsonValue(Cc_ccv2It->value()));
        }
    }
}

std::string BillingAddCcRequest::getName() const
{
    return m_Name;
}

void BillingAddCcRequest::setName(std::string value)
{
    
    m_Name = std::move(value);
    m_NameIsSet = true;
}
std::string BillingAddCcRequest::getAddress() const
{
    return m_Address;
}

void BillingAddCcRequest::setAddress(std::string value)
{
    
    m_Address = std::move(value);
    m_AddressIsSet = true;
}
std::string BillingAddCcRequest::getCity() const
{
    return m_City;
}

void BillingAddCcRequest::setCity(std::string value)
{
    
    m_City = std::move(value);
    m_CityIsSet = true;
}
std::string BillingAddCcRequest::getState() const
{
    return m_State;
}

void BillingAddCcRequest::setState(std::string value)
{
    
    m_State = std::move(value);
    m_StateIsSet = true;
}
std::string BillingAddCcRequest::getCountry() const
{
    return m_Country;
}

void BillingAddCcRequest::setCountry(std::string value)
{
    
    m_Country = std::move(value);
    m_CountryIsSet = true;
}
std::string BillingAddCcRequest::getZip() const
{
    return m_Zip;
}

void BillingAddCcRequest::setZip(std::string value)
{
    
    m_Zip = std::move(value);
    m_ZipIsSet = true;
}
std::string BillingAddCcRequest::getCc() const
{
    return m_Cc;
}

void BillingAddCcRequest::setCc(std::string value)
{
    
    m_Cc = std::move(value);
    m_CcIsSet = true;
}
std::string BillingAddCcRequest::getCcExp() const
{
    return m_Cc_exp;
}

void BillingAddCcRequest::setCcExp(std::string value)
{
    
    m_Cc_exp = std::move(value);
    m_Cc_expIsSet = true;
}
std::string BillingAddCcRequest::getCcCcv2() const
{
    return m_Cc_ccv2;
}

void BillingAddCcRequest::setCcCcv2(std::string value)
{
    
    m_Cc_ccv2 = std::move(value);
    m_Cc_ccv2IsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<BillingAddCcRequest>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<BillingAddCcRequest>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<BillingAddCcRequest>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<BillingAddCcRequest>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

