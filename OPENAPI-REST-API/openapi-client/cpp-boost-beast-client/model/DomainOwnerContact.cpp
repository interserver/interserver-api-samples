/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.24.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "DomainOwnerContact.h"

#include <cstddef>
#include <map>
#include <memory>
#include <stdexcept>
#include <sstream>
#include <string>
#include <iterator>
#include <utility>
#include <vector>
#include <boost/json.hpp>

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace {

template <typename Target>
struct JsonValueConverter
{
    static boost::json::value toJsonValue(const Target& sourceValue)
    {
        return boost::json::value_from(sourceValue);
    }

    static Target fromJsonValue(const boost::json::value& jsonValue)
    {
        return boost::json::value_to<Target>(jsonValue);
    }
};

template <>
struct JsonValueConverter<std::nullptr_t>
{
    static boost::json::value toJsonValue(std::nullptr_t)
    {
        return nullptr;
    }

    static std::nullptr_t fromJsonValue(const boost::json::value& jsonValue)
    {
        if (!jsonValue.is_null()) {
            throw std::invalid_argument("Expected a null JSON value");
        }
        return nullptr;
    }
};

template <typename ModelType>
struct JsonValueConverter<std::shared_ptr<ModelType>>
{
    static boost::json::value toJsonValue(const std::shared_ptr<ModelType>& model)
    {
        return model == nullptr ? boost::json::value(nullptr) : model->toJsonValue();
    }

    static std::shared_ptr<ModelType> fromJsonValue(const boost::json::value& jsonValue)
    {
        if (jsonValue.is_null()) {
            return nullptr;
        }
        return std::make_shared<ModelType>(jsonValue);
    }
};

template <typename Element>
struct JsonValueConverter<std::vector<Element>>
{
    static boost::json::value toJsonValue(const std::vector<Element>& sourceValues)
    {
        boost::json::array jsonValues;
        for (const auto& sourceValue : sourceValues) {
            jsonValues.emplace_back(JsonValueConverter<Element>::toJsonValue(sourceValue));
        }
        return jsonValues;
    }

    static std::vector<Element> fromJsonValue(const boost::json::value& jsonValue)
    {
        const auto& jsonValues = jsonValue.as_array();
        std::vector<Element> convertedValues;
        convertedValues.reserve(jsonValues.size());
        for (const auto& jsonElement : jsonValues) {
            convertedValues.emplace_back(JsonValueConverter<Element>::fromJsonValue(jsonElement));
        }
        return convertedValues;
    }
};

template <typename MappedValue>
struct JsonValueConverter<std::map<std::string, MappedValue>>
{
    static boost::json::value toJsonValue(const std::map<std::string, MappedValue>& sourceValues)
    {
        boost::json::object jsonValues;
        for (const auto& sourceEntry : sourceValues) {
            jsonValues[sourceEntry.first] = JsonValueConverter<MappedValue>::toJsonValue(sourceEntry.second);
        }
        return jsonValues;
    }

    static std::map<std::string, MappedValue> fromJsonValue(const boost::json::value& jsonValue)
    {
        std::map<std::string, MappedValue> convertedValues;
        for (const auto& jsonEntry : jsonValue.as_object()) {
            const std::string entryKey(jsonEntry.key().data(), jsonEntry.key().size());
            convertedValues.emplace(entryKey, JsonValueConverter<MappedValue>::fromJsonValue(jsonEntry.value()));
        }
        return convertedValues;
    }
};

void writePrettyJson(std::ostream& output, boost::json::value const& value, std::string& indent)
{
    if (value.is_object()) {
        const auto& object = value.as_object();
        if (object.empty()) {
            output << "{}";
            return;
        }

        output << "{\n";
        indent.append(4, ' ');
        for (auto iterator = object.begin(); iterator != object.end(); ++iterator) {
            output << indent << boost::json::serialize(iterator->key()) << ": ";
            writePrettyJson(output, iterator->value(), indent);
            if (std::next(iterator) != object.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << '}';
        return;
    }

    if (value.is_array()) {
        const auto& array = value.as_array();
        if (array.empty()) {
            output << "[]";
            return;
        }

        output << "[\n";
        indent.append(4, ' ');
        for (auto iterator = array.begin(); iterator != array.end(); ++iterator) {
            output << indent;
            writePrettyJson(output, *iterator, indent);
            if (std::next(iterator) != array.end()) {
                output << ',';
            }
            output << '\n';
        }
        indent.resize(indent.size() - 4);
        output << indent << ']';
        return;
    }

    output << boost::json::serialize(value);
}
}

DomainOwnerContact::DomainOwnerContact(boost::json::value const& value)
{
    fromJsonValue(value);
}

std::string DomainOwnerContact::toJsonString(bool prettyJson /* = false */) const
{
    const auto value = toJsonValue();
    if (!prettyJson) {
        return boost::json::serialize(value);
    }

    std::ostringstream output;
    std::string indent;
    writePrettyJson(output, value, indent);
    return output.str();
}

void DomainOwnerContact::fromJsonString(std::string const& jsonString)
{
    fromJsonValue(boost::json::parse(jsonString));
}

boost::json::value DomainOwnerContact::toJsonValue() const
{
    return boost::json::value(toJsonObject_internal());
}

void DomainOwnerContact::fromJsonValue(boost::json::value const& value)
{
    if (!value.is_object()) {
        throw std::invalid_argument("Expected a JSON object for DomainOwnerContact");
    }
    fromJsonObject_internal(value.as_object());
}

boost::json::object DomainOwnerContact::toJsonObject_internal() const
{
    boost::json::object object;
        if (m_Postal_codeIsSet) {
            object["postal_code"] = JsonValueConverter<std::string>::toJsonValue(getPostalCode());
        }
        if (m_Org_nameIsSet) {
            object["org_name"] = JsonValueConverter<std::string>::toJsonValue(getOrgName());
        }
        if (m_CountryIsSet) {
            object["country"] = JsonValueConverter<std::string>::toJsonValue(getCountry());
        }
        if (m_StatusIsSet) {
            object["status"] = JsonValueConverter<std::string>::toJsonValue(getStatus());
        }
        if (m_StateIsSet) {
            object["state"] = JsonValueConverter<std::string>::toJsonValue(getState());
        }
        if (m_First_nameIsSet) {
            object["first_name"] = JsonValueConverter<std::string>::toJsonValue(getFirstName());
        }
        if (m_Last_nameIsSet) {
            object["last_name"] = JsonValueConverter<std::string>::toJsonValue(getLastName());
        }
        if (m_Address3IsSet) {
            object["address3"] = JsonValueConverter<std::string>::toJsonValue(getAddress3());
        }
        if (m_FaxIsSet) {
            object["fax"] = JsonValueConverter<std::string>::toJsonValue(getFax());
        }
        if (m_Address2IsSet) {
            object["address2"] = JsonValueConverter<std::string>::toJsonValue(getAddress2());
        }
        if (m_EmailIsSet) {
            object["email"] = JsonValueConverter<std::string>::toJsonValue(getEmail());
        }
        if (m_CityIsSet) {
            object["city"] = JsonValueConverter<std::string>::toJsonValue(getCity());
        }
        if (m_PhoneIsSet) {
            object["phone"] = JsonValueConverter<std::string>::toJsonValue(getPhone());
        }
        if (m_Address1IsSet) {
            object["address1"] = JsonValueConverter<std::string>::toJsonValue(getAddress1());
        }
    return object;
}

void DomainOwnerContact::fromJsonObject_internal(boost::json::object const& object)
{
    m_Postal_codeIsSet = false;
    m_Org_nameIsSet = false;
    m_CountryIsSet = false;
    m_StatusIsSet = false;
    m_StateIsSet = false;
    m_First_nameIsSet = false;
    m_Last_nameIsSet = false;
    m_Address3IsSet = false;
    m_FaxIsSet = false;
    m_Address2IsSet = false;
    m_EmailIsSet = false;
    m_CityIsSet = false;
    m_PhoneIsSet = false;
    m_Address1IsSet = false;
    {
        const auto Postal_codeIt = object.find("postal_code");
        if (Postal_codeIt != object.end()) {
            setPostalCode(JsonValueConverter<std::string>::fromJsonValue(Postal_codeIt->value()));
        }
    }
    {
        const auto Org_nameIt = object.find("org_name");
        if (Org_nameIt != object.end()) {
            setOrgName(JsonValueConverter<std::string>::fromJsonValue(Org_nameIt->value()));
        }
    }
    {
        const auto CountryIt = object.find("country");
        if (CountryIt != object.end()) {
            setCountry(JsonValueConverter<std::string>::fromJsonValue(CountryIt->value()));
        }
    }
    {
        const auto StatusIt = object.find("status");
        if (StatusIt != object.end()) {
            setStatus(JsonValueConverter<std::string>::fromJsonValue(StatusIt->value()));
        }
    }
    {
        const auto StateIt = object.find("state");
        if (StateIt != object.end()) {
            setState(JsonValueConverter<std::string>::fromJsonValue(StateIt->value()));
        }
    }
    {
        const auto First_nameIt = object.find("first_name");
        if (First_nameIt != object.end()) {
            setFirstName(JsonValueConverter<std::string>::fromJsonValue(First_nameIt->value()));
        }
    }
    {
        const auto Last_nameIt = object.find("last_name");
        if (Last_nameIt != object.end()) {
            setLastName(JsonValueConverter<std::string>::fromJsonValue(Last_nameIt->value()));
        }
    }
    {
        const auto Address3It = object.find("address3");
        if (Address3It != object.end()) {
            setAddress3(JsonValueConverter<std::string>::fromJsonValue(Address3It->value()));
        }
    }
    {
        const auto FaxIt = object.find("fax");
        if (FaxIt != object.end()) {
            setFax(JsonValueConverter<std::string>::fromJsonValue(FaxIt->value()));
        }
    }
    {
        const auto Address2It = object.find("address2");
        if (Address2It != object.end()) {
            setAddress2(JsonValueConverter<std::string>::fromJsonValue(Address2It->value()));
        }
    }
    {
        const auto EmailIt = object.find("email");
        if (EmailIt != object.end()) {
            setEmail(JsonValueConverter<std::string>::fromJsonValue(EmailIt->value()));
        }
    }
    {
        const auto CityIt = object.find("city");
        if (CityIt != object.end()) {
            setCity(JsonValueConverter<std::string>::fromJsonValue(CityIt->value()));
        }
    }
    {
        const auto PhoneIt = object.find("phone");
        if (PhoneIt != object.end()) {
            setPhone(JsonValueConverter<std::string>::fromJsonValue(PhoneIt->value()));
        }
    }
    {
        const auto Address1It = object.find("address1");
        if (Address1It != object.end()) {
            setAddress1(JsonValueConverter<std::string>::fromJsonValue(Address1It->value()));
        }
    }
}

std::string DomainOwnerContact::getPostalCode() const
{
    return m_Postal_code;
}

void DomainOwnerContact::setPostalCode(std::string value)
{
    
    m_Postal_code = std::move(value);
    m_Postal_codeIsSet = true;
}
std::string DomainOwnerContact::getOrgName() const
{
    return m_Org_name;
}

void DomainOwnerContact::setOrgName(std::string value)
{
    
    m_Org_name = std::move(value);
    m_Org_nameIsSet = true;
}
std::string DomainOwnerContact::getCountry() const
{
    return m_Country;
}

void DomainOwnerContact::setCountry(std::string value)
{
    
    m_Country = std::move(value);
    m_CountryIsSet = true;
}
std::string DomainOwnerContact::getStatus() const
{
    return m_Status;
}

void DomainOwnerContact::setStatus(std::string value)
{
    
    m_Status = std::move(value);
    m_StatusIsSet = true;
}
std::string DomainOwnerContact::getState() const
{
    return m_State;
}

void DomainOwnerContact::setState(std::string value)
{
    
    m_State = std::move(value);
    m_StateIsSet = true;
}
std::string DomainOwnerContact::getFirstName() const
{
    return m_First_name;
}

void DomainOwnerContact::setFirstName(std::string value)
{
    
    m_First_name = std::move(value);
    m_First_nameIsSet = true;
}
std::string DomainOwnerContact::getLastName() const
{
    return m_Last_name;
}

void DomainOwnerContact::setLastName(std::string value)
{
    
    m_Last_name = std::move(value);
    m_Last_nameIsSet = true;
}
std::string DomainOwnerContact::getAddress3() const
{
    return m_Address3;
}

void DomainOwnerContact::setAddress3(std::string value)
{
    
    m_Address3 = std::move(value);
    m_Address3IsSet = true;
}
std::string DomainOwnerContact::getFax() const
{
    return m_Fax;
}

void DomainOwnerContact::setFax(std::string value)
{
    
    m_Fax = std::move(value);
    m_FaxIsSet = true;
}
std::string DomainOwnerContact::getAddress2() const
{
    return m_Address2;
}

void DomainOwnerContact::setAddress2(std::string value)
{
    
    m_Address2 = std::move(value);
    m_Address2IsSet = true;
}
std::string DomainOwnerContact::getEmail() const
{
    return m_Email;
}

void DomainOwnerContact::setEmail(std::string value)
{
    
    m_Email = std::move(value);
    m_EmailIsSet = true;
}
std::string DomainOwnerContact::getCity() const
{
    return m_City;
}

void DomainOwnerContact::setCity(std::string value)
{
    
    m_City = std::move(value);
    m_CityIsSet = true;
}
std::string DomainOwnerContact::getPhone() const
{
    return m_Phone;
}

void DomainOwnerContact::setPhone(std::string value)
{
    
    m_Phone = std::move(value);
    m_PhoneIsSet = true;
}
std::string DomainOwnerContact::getAddress1() const
{
    return m_Address1;
}

void DomainOwnerContact::setAddress1(std::string value)
{
    
    m_Address1 = std::move(value);
    m_Address1IsSet = true;
}

std::string createJsonStringFromModelVector(const std::vector<std::shared_ptr<DomainOwnerContact>>& data)
{
    return boost::json::serialize(JsonValueConverter<std::vector<std::shared_ptr<DomainOwnerContact>>>::toJsonValue(data));
}

void createModelVectorFromJsonString(std::vector<std::shared_ptr<DomainOwnerContact>>& vec, const std::string& json)
{
    vec = JsonValueConverter<std::vector<std::shared_ptr<DomainOwnerContact>>>::fromJsonValue(boost::json::parse(json));
}

}
}
}
}

