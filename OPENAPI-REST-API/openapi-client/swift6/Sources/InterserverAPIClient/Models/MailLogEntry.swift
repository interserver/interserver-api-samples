//
// MailLogEntry.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** A single email record in the mail log.  Combines data from the message store (envelope metadata), the queue release table (delivery status and response), and the sender delivery table (MX routing details).  When &#x60;groupby&#x3D;recipient&#x60; each row represents one delivery attempt; when &#x60;groupby&#x3D;message&#x60; delivery fields reflect one arbitrary recipient. */
@objcMembers public final class MailLogEntry: NSObject, Codable, @unchecked Sendable {

    /** Internal auto-increment database row ID. */
    public private(set) var _id: Int
    /** The relay-assigned mail ID (18-19 hex characters).  Matches the `mailid` filter parameter and the `text` value returned by send endpoints. */
    public private(set) var _id: String
    /** SMTP envelope `MAIL FROM` address. */
    public private(set) var from: String
    /** SMTP envelope `RCPT TO` address. */
    public private(set) var to: String
    /** Human-readable creation timestamp in `YYYY-MM-DD HH:MM:SS` format. */
    public private(set) var created: String
    /** Unix timestamp of message acceptance.  Corresponds to the `startDate` and `endDate` filter parameters. */
    public private(set) var time: Int
    /** The SMTP AUTH username used to submit the message (e.g. `mb5658`). */
    public private(set) var user: String
    /** SMTP transaction type negotiated with the relay. */
    public private(set) var transtype: String
    /** IP address of the client that submitted the message to the relay. */
    public private(set) var origin: String
    /** Relay interface name that accepted the message. */
    public private(set) var interface: String
    /** The `Subject` header value.  MIME-encoded subjects (UTF-8, ISO-8859, US-ASCII) are automatically decoded. */
    public private(set) var subject: String?
    /** The `Message-ID` header value.  Can be used with the `messageId` filter for subsequent lookups. */
    public private(set) var messageId: String?
    /** The sending zone assigned by the relay for outbound delivery. */
    public private(set) var sendingZone: String?
    /** Size of the message body in bytes. */
    public private(set) var bodySize: Int?
    public var bodySizeNum: NSNumber? {
        get {
            return bodySize as NSNumber?
        }
    }
    /** Sequence index of this recipient in a multi-recipient message. Starts at 1. */
    public private(set) var seq: Int?
    public var seqNum: NSNumber? {
        get {
            return seq as NSNumber?
        }
    }
    /** Delivery status flag.  `1` = successfully delivered to destination MX. `0` = queued, deferred, or failed.  `null` = delivery not yet attempted. */
    public private(set) var delivered: Int?
    public var deliveredNum: NSNumber? {
        get {
            return delivered as NSNumber?
        }
    }
    /** The SMTP response code from the destination MX server (e.g. `250`). */
    public private(set) var code: Int?
    public var codeNum: NSNumber? {
        get {
            return code as NSNumber?
        }
    }
    /** The specific recipient address this delivery record is for. */
    public private(set) var recipient: String?
    /** The full SMTP response string received from the destination MX server. */
    public private(set) var response: String?
    /** The destination domain for this delivery attempt. */
    public private(set) var domain: String?
    /** Whether the queue entry is currently locked for delivery processing. */
    public private(set) var locked: Int?
    public var lockedNum: NSNumber? {
        get {
            return locked as NSNumber?
        }
    }
    /** Millisecond-precision timestamp of the last queue lock acquisition. */
    public private(set) var lockTime: String?
    /** The relay server node assigned to deliver this message. */
    public private(set) var assigned: String?
    /** ISO 8601 timestamp when the message was placed into the delivery queue. */
    public private(set) var queued: String?
    /** The MX hostname the relay connected to for delivery.  Corresponds to the `mx` filter parameter. */
    public private(set) var mxHostname: String?

    public init(_id: Int, _id: String, from: String, to: String, created: String, time: Int, user: String, transtype: String, origin: String, interface: String, subject: String? = nil, messageId: String? = nil, sendingZone: String? = nil, bodySize: Int? = nil, seq: Int? = nil, delivered: Int? = nil, code: Int? = nil, recipient: String? = nil, response: String? = nil, domain: String? = nil, locked: Int? = nil, lockTime: String? = nil, assigned: String? = nil, queued: String? = nil, mxHostname: String? = nil) {
        self._id = _id
        self._id = _id
        self.from = from
        self.to = to
        self.created = created
        self.time = time
        self.user = user
        self.transtype = transtype
        self.origin = origin
        self.interface = interface
        self.subject = subject
        self.messageId = messageId
        self.sendingZone = sendingZone
        self.bodySize = bodySize
        self.seq = seq
        self.delivered = delivered
        self.code = code
        self.recipient = recipient
        self.response = response
        self.domain = domain
        self.locked = locked
        self.lockTime = lockTime
        self.assigned = assigned
        self.queued = queued
        self.mxHostname = mxHostname
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case _id
        case _id = "id"
        case from
        case to
        case created
        case time
        case user
        case transtype
        case origin
        case interface
        case subject
        case messageId
        case sendingZone
        case bodySize
        case seq
        case delivered
        case code
        case recipient
        case response
        case domain
        case locked
        case lockTime
        case assigned
        case queued
        case mxHostname
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(_id, forKey: ._id)
        try container.encode(_id, forKey: ._id)
        try container.encode(from, forKey: .from)
        try container.encode(to, forKey: .to)
        try container.encode(created, forKey: .created)
        try container.encode(time, forKey: .time)
        try container.encode(user, forKey: .user)
        try container.encode(transtype, forKey: .transtype)
        try container.encode(origin, forKey: .origin)
        try container.encode(interface, forKey: .interface)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(messageId, forKey: .messageId)
        try container.encodeIfPresent(sendingZone, forKey: .sendingZone)
        try container.encodeIfPresent(bodySize, forKey: .bodySize)
        try container.encodeIfPresent(seq, forKey: .seq)
        try container.encodeIfPresent(delivered, forKey: .delivered)
        try container.encodeIfPresent(code, forKey: .code)
        try container.encodeIfPresent(recipient, forKey: .recipient)
        try container.encodeIfPresent(response, forKey: .response)
        try container.encodeIfPresent(domain, forKey: .domain)
        try container.encodeIfPresent(locked, forKey: .locked)
        try container.encodeIfPresent(lockTime, forKey: .lockTime)
        try container.encodeIfPresent(assigned, forKey: .assigned)
        try container.encodeIfPresent(queued, forKey: .queued)
        try container.encodeIfPresent(mxHostname, forKey: .mxHostname)
    }
}


extension MailLogEntry: Identifiable {}
