//
// DomainOrderRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request body for the domain order flow: putDomains (returns required fields), patchDomains (validates fields), addDomain (places the order). The TLD is resolved from hostname. Per-TLD registrant/contact fields returned by putDomains may be supplied as additional properties on patchDomains/addDomain. */
@objcMembers public final class DomainOrderRequest: NSObject, Codable, @unchecked Sendable {

    public enum ModelType: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case register = "register"
        case transfer = "transfer"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    public enum WhoisPrivacy: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case enable = "enable"
        case disable = "disable"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** Fully-qualified domain to register or transfer (e.g. example.com). */
    public private(set) var hostname: String
    /** Order type. */
    public private(set) var type: ModelType? = .register
    /** Coupon code (addDomain only). */
    public private(set) var coupon: String? = ""
    /** Set to \"enable\" to add Whois privacy (addDomain only). */
    public private(set) var whoisPrivacy: WhoisPrivacy?

    public init(hostname: String, type: ModelType? = .register, coupon: String? = "", whoisPrivacy: WhoisPrivacy? = nil) {
        self.hostname = hostname
        self.type = type
        self.coupon = coupon
        self.whoisPrivacy = whoisPrivacy
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case hostname
        case type
        case coupon
        case whoisPrivacy = "whois_privacy"
    }

    public private(set) var additionalProperties: [String: JSONValue] = [:]

    public subscript(key: String) -> JSONValue? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(hostname, forKey: .hostname)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(coupon, forKey: .coupon)
        try container.encodeIfPresent(whoisPrivacy, forKey: .whoisPrivacy)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        hostname = try container.decode(String.self, forKey: .hostname)
        type = try container.decodeIfPresent(ModelType.self, forKey: .type)
        coupon = try container.decodeIfPresent(String.self, forKey: .coupon)
        whoisPrivacy = try container.decodeIfPresent(WhoisPrivacy.self, forKey: .whoisPrivacy)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("hostname")
        nonAdditionalPropertyKeys.insert("type")
        nonAdditionalPropertyKeys.insert("coupon")
        nonAdditionalPropertyKeys.insert("whois_privacy")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(JSONValue.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}


extension DomainOrderRequest: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if type == .unknownDefaultOpenApi { return true }
        if whoisPrivacy == .unknownDefaultOpenApi { return true }
        return false
    }
}
