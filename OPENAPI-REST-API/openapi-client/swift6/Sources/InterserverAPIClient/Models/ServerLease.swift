//
// ServerLease.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

@objcMembers public final class ServerLease: NSObject, Codable, @unchecked Sendable {

    /** MAC address associated with the lease. */
    public private(set) var mac: String
    /** Indicates if the lease is authenticated. */
    public private(set) var authenticated: Bool
    /** Group identifier for the lease. */
    public private(set) var group: String

    public init(mac: String, authenticated: Bool, group: String) {
        self.mac = mac
        self.authenticated = authenticated
        self.group = group
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case mac
        case authenticated
        case group
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(mac, forKey: .mac)
        try container.encode(authenticated, forKey: .authenticated)
        try container.encode(group, forKey: .group)
    }
}

