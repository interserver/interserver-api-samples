//
// VpsBackupRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** A single backed-up item and information about it. */
@objcMembers public final class VpsBackupRow: NSObject, Codable, @unchecked Sendable {

    /** Backup Type */
    public private(set) var type: String
    /** The service id such as vps  id. */
    public private(set) var service: Int
    /** The name of the backup. */
    public private(set) var name: String
    /** Size of the file in bytes */
    public private(set) var size: Int
    /** The creation date of the backup in a unix timestamp. */
    public private(set) var date: Int

    public init(type: String, service: Int, name: String, size: Int, date: Int) {
        self.type = type
        self.service = service
        self.name = name
        self.size = size
        self.date = date
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case service
        case name
        case size
        case date
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encode(service, forKey: .service)
        try container.encode(name, forKey: .name)
        try container.encode(size, forKey: .size)
        try container.encode(date, forKey: .date)
    }
}

