//
// LoginInfo.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Basic information useful for rendering a login page. */
@objcMembers public final class LoginInfo: NSObject, Codable, @unchecked Sendable {

    /** A base64 encoded image to use for rendering the alternateive captcha. */
    public private(set) var captcha: String
    public private(set) var counts: LoginServiceCounts
    /** A logo image url. */
    public private(set) var logo: String?
    /** The desired langauge to render the site with. */
    public private(set) var language: String?

    public init(captcha: String, counts: LoginServiceCounts, logo: String? = nil, language: String? = nil) {
        self.captcha = captcha
        self.counts = counts
        self.logo = logo
        self.language = language
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case captcha
        case counts
        case logo
        case language
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(captcha, forKey: .captcha)
        try container.encode(counts, forKey: .counts)
        try container.encodeIfPresent(logo, forKey: .logo)
        try container.encodeIfPresent(language, forKey: .language)
    }
}

