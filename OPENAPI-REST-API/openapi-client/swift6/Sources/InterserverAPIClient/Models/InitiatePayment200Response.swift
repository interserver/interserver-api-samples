//
// InitiatePayment200Response.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

@objcMembers public final class InitiatePayment200Response: NSObject, Codable, @unchecked Sendable {

    public enum ModelType: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case redirect = "redirect"
        case submit = "submit"
        case single = "single"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** The response type indicating how to handle the payment. Possible values: `redirect` (redirect user to a URL), `submit` (submit a form to a URL), `single` (immediate result). */
    public private(set) var type: ModelType?
    /** URL to redirect the user to for payment (when type is `redirect`). */
    public private(set) var redirect: String?
    /** Form action URL (when type is `submit`). */
    public private(set) var action: String?
    /** HTTP method for the form submission (when type is `submit`). */
    public private(set) var method: String?
    /** Form field name-value pairs to submit (when type is `submit`). */
    public private(set) var items: JSONValue?
    /** Status or result text. */
    public private(set) var text: String?

    public init(type: ModelType? = nil, redirect: String? = nil, action: String? = nil, method: String? = nil, items: JSONValue? = nil, text: String? = nil) {
        self.type = type
        self.redirect = redirect
        self.action = action
        self.method = method
        self.items = items
        self.text = text
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case redirect
        case action
        case method
        case items
        case text
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(redirect, forKey: .redirect)
        try container.encodeIfPresent(action, forKey: .action)
        try container.encodeIfPresent(method, forKey: .method)
        try container.encodeIfPresent(items, forKey: .items)
        try container.encodeIfPresent(text, forKey: .text)
    }
}


extension InitiatePayment200Response: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if type == .unknownDefaultOpenApi { return true }
        return false
    }
}
