#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CsrType* {.pure.} = enum
  Generated
  Provided
  UnknownDefaultOpenApi

type SslOrderRequest* = object
  ## Request body to validate (putSsl) or place (addSsl) an SSL certificate order. Contact fields default from the account if omitted. Package ids come from getNewSsl.serviceTypes.
  ssl*: int ## SSL package service type id from getNewSsl.serviceTypes.
  hostname*: string ## Domain the certificate is issued for. Wildcard certs require *.domain.com format.
  approverEmail*: string ## Domain-control approver email (required for all SSL orders).
  frequency*: Option[int] ## Billing frequency in months (12, 24, or 36).
  coupon*: Option[string] ## Coupon code.
  csrType*: Option[CsrType] ## Whether the CSR is server-generated or customer-provided.
  csr*: Option[string] ## PEM-encoded CSR (>= 2048-bit) when csr_type=provided.
  firstname*: Option[string] ## Contact first name (defaults from account).
  lastname*: Option[string] ## Contact last name (defaults from account).
  email*: Option[string] ## Contact email (defaults from account).
  address*: Option[string] ## Contact address (defaults from account).
  city*: Option[string] ## Contact city (defaults from account).
  state*: Option[string] ## Contact state/region (defaults from account).
  zip*: Option[string] ## Contact postal code (defaults from account).
  country*: Option[string] ## Contact country (defaults from account).
  phone*: Option[string] ## Contact phone (defaults from account).
  company*: Option[string] ## Contact company/organization (defaults from account).
  department*: Option[string] ## Contact department (defaults to Administration).
  agency*: Option[string] ## EV certificate incorporating agency (only for EV packages).
  businessCategory*: Option[string] ## EV certificate business category (only for EV packages).

func `%`*(v: CsrType): JsonNode =
  result = case v:
    of CsrType.Generated: %"generated"
    of CsrType.Provided: %"provided"
    of CsrType.UnknownDefaultOpenApi: %"11184809"
func `$`*(v: CsrType): string =
  result = case v:
    of CsrType.Generated: $("generated")
    of CsrType.Provided: $("provided")
    of CsrType.UnknownDefaultOpenApi: $("11184809")

proc to*(node: JsonNode, T: typedesc[CsrType]): CsrType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CsrType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("generated"):
    return CsrType.Generated
  of $("provided"):
    return CsrType.Provided
  of $("11184809"):
    return CsrType.UnknownDefaultOpenApi
  else:
    raise newException(ValueError, "Invalid enum value for CsrType: " & strVal)


# Custom JSON deserialization for SslOrderRequest with custom field names
proc to*(node: JsonNode, T: typedesc[SslOrderRequest]): SslOrderRequest =
  result = SslOrderRequest()
  if node.kind == JObject:
    if node.hasKey("ssl"):
      result.ssl = to(node["ssl"], int)
    if node.hasKey("hostname"):
      result.hostname = to(node["hostname"], string)
    if node.hasKey("approver_email"):
      result.approverEmail = to(node["approver_email"], string)
    if node.hasKey("frequency") and node["frequency"].kind != JNull:
      result.frequency = some(to(node["frequency"], typeof(result.frequency.get())))
    if node.hasKey("coupon") and node["coupon"].kind != JNull:
      result.coupon = some(to(node["coupon"], typeof(result.coupon.get())))
    if node.hasKey("csr_type") and node["csr_type"].kind != JNull:
      result.csrType = some(to(node["csr_type"], CsrType))
    if node.hasKey("csr") and node["csr"].kind != JNull:
      result.csr = some(to(node["csr"], typeof(result.csr.get())))
    if node.hasKey("firstname") and node["firstname"].kind != JNull:
      result.firstname = some(to(node["firstname"], typeof(result.firstname.get())))
    if node.hasKey("lastname") and node["lastname"].kind != JNull:
      result.lastname = some(to(node["lastname"], typeof(result.lastname.get())))
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("address") and node["address"].kind != JNull:
      result.address = some(to(node["address"], typeof(result.address.get())))
    if node.hasKey("city") and node["city"].kind != JNull:
      result.city = some(to(node["city"], typeof(result.city.get())))
    if node.hasKey("state") and node["state"].kind != JNull:
      result.state = some(to(node["state"], typeof(result.state.get())))
    if node.hasKey("zip") and node["zip"].kind != JNull:
      result.zip = some(to(node["zip"], typeof(result.zip.get())))
    if node.hasKey("country") and node["country"].kind != JNull:
      result.country = some(to(node["country"], typeof(result.country.get())))
    if node.hasKey("phone") and node["phone"].kind != JNull:
      result.phone = some(to(node["phone"], typeof(result.phone.get())))
    if node.hasKey("company") and node["company"].kind != JNull:
      result.company = some(to(node["company"], typeof(result.company.get())))
    if node.hasKey("department") and node["department"].kind != JNull:
      result.department = some(to(node["department"], typeof(result.department.get())))
    if node.hasKey("agency") and node["agency"].kind != JNull:
      result.agency = some(to(node["agency"], typeof(result.agency.get())))
    if node.hasKey("business_category") and node["business_category"].kind != JNull:
      result.businessCategory = some(to(node["business_category"], typeof(result.businessCategory.get())))

# Custom JSON serialization for SslOrderRequest with custom field names
proc `%`*(obj: SslOrderRequest): JsonNode =
  result = newJObject()
  result["ssl"] = %obj.ssl
  result["hostname"] = %obj.hostname
  result["approver_email"] = %obj.approverEmail
  if obj.frequency.isSome():
    result["frequency"] = %obj.frequency.get()
  if obj.coupon.isSome():
    result["coupon"] = %obj.coupon.get()
  if obj.csrType.isSome():
    result["csr_type"] = %obj.csrType.get()
  if obj.csr.isSome():
    result["csr"] = %obj.csr.get()
  if obj.firstname.isSome():
    result["firstname"] = %obj.firstname.get()
  if obj.lastname.isSome():
    result["lastname"] = %obj.lastname.get()
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  if obj.address.isSome():
    result["address"] = %obj.address.get()
  if obj.city.isSome():
    result["city"] = %obj.city.get()
  if obj.state.isSome():
    result["state"] = %obj.state.get()
  if obj.zip.isSome():
    result["zip"] = %obj.zip.get()
  if obj.country.isSome():
    result["country"] = %obj.country.get()
  if obj.phone.isSome():
    result["phone"] = %obj.phone.get()
  if obj.company.isSome():
    result["company"] = %obj.company.get()
  if obj.department.isSome():
    result["department"] = %obj.department.get()
  if obj.agency.isSome():
    result["agency"] = %obj.agency.get()
  if obj.businessCategory.isSome():
    result["business_category"] = %obj.businessCategory.get()

