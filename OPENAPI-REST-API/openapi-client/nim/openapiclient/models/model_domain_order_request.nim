#
# InterServer Management API
# 
# # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
# The version of the OpenAPI document: 0.9.0
# Contact: support@interserver.net
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type `Type`* {.pure.} = enum
  Register
  Transfer
  UnknownDefaultOpenApi

type WhoisPrivacy* {.pure.} = enum
  Enable
  Disable
  UnknownDefaultOpenApi

type DomainOrderRequest* = object
  ## Request body for the domain order flow: putDomains (returns required fields), patchDomains (validates fields), addDomain (places the order). The TLD is resolved from hostname. Per-TLD registrant/contact fields returned by putDomains may be supplied as additional properties on patchDomains/addDomain.
  hostname*: string ## Fully-qualified domain to register or transfer (e.g. example.com).
  `type`*: Option[`Type`] ## Order type.
  coupon*: Option[string] ## Coupon code (addDomain only).
  whoisPrivacy*: Option[WhoisPrivacy] ## Set to \"enable\" to add Whois privacy (addDomain only).

func `%`*(v: `Type`): JsonNode =
  result = case v:
    of `Type`.Register: %"register"
    of `Type`.Transfer: %"transfer"
    of `Type`.UnknownDefaultOpenApi: %"11184809"
func `$`*(v: `Type`): string =
  result = case v:
    of `Type`.Register: $("register")
    of `Type`.Transfer: $("transfer")
    of `Type`.UnknownDefaultOpenApi: $("11184809")

proc to*(node: JsonNode, T: typedesc[`Type`]): `Type` =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum `Type`, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("register"):
    return `Type`.Register
  of $("transfer"):
    return `Type`.Transfer
  of $("11184809"):
    return `Type`.UnknownDefaultOpenApi
  else:
    raise newException(ValueError, "Invalid enum value for `Type`: " & strVal)

func `%`*(v: WhoisPrivacy): JsonNode =
  result = case v:
    of WhoisPrivacy.Enable: %"enable"
    of WhoisPrivacy.Disable: %"disable"
    of WhoisPrivacy.UnknownDefaultOpenApi: %"11184809"
func `$`*(v: WhoisPrivacy): string =
  result = case v:
    of WhoisPrivacy.Enable: $("enable")
    of WhoisPrivacy.Disable: $("disable")
    of WhoisPrivacy.UnknownDefaultOpenApi: $("11184809")

proc to*(node: JsonNode, T: typedesc[WhoisPrivacy]): WhoisPrivacy =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum WhoisPrivacy, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("enable"):
    return WhoisPrivacy.Enable
  of $("disable"):
    return WhoisPrivacy.Disable
  of $("11184809"):
    return WhoisPrivacy.UnknownDefaultOpenApi
  else:
    raise newException(ValueError, "Invalid enum value for WhoisPrivacy: " & strVal)


# Custom JSON deserialization for DomainOrderRequest with custom field names
proc to*(node: JsonNode, T: typedesc[DomainOrderRequest]): DomainOrderRequest =
  result = DomainOrderRequest()
  if node.kind == JObject:
    if node.hasKey("hostname"):
      result.hostname = to(node["hostname"], string)
    if node.hasKey("type") and node["type"].kind != JNull:
      result.`type` = some(to(node["type"], `Type`))
    if node.hasKey("coupon") and node["coupon"].kind != JNull:
      result.coupon = some(to(node["coupon"], typeof(result.coupon.get())))
    if node.hasKey("whois_privacy") and node["whois_privacy"].kind != JNull:
      result.whoisPrivacy = some(to(node["whois_privacy"], WhoisPrivacy))

# Custom JSON serialization for DomainOrderRequest with custom field names
proc `%`*(obj: DomainOrderRequest): JsonNode =
  result = newJObject()
  result["hostname"] = %obj.hostname
  if obj.`type`.isSome():
    result["type"] = %obj.`type`.get()
  if obj.coupon.isSome():
    result["coupon"] = %obj.coupon.get()
  if obj.whoisPrivacy.isSome():
    result["whois_privacy"] = %obj.whoisPrivacy.get()

