-module(interserver_api_monthly_counts).

-export([encode/1]).

-export_type([interserver_api_monthly_counts/0]).

-type interserver_api_monthly_counts() ::
    #{ 
     }.

encode(#{ 
        }) ->
    #{ 
     }.
