-module(interserver_api_dns_record_type).

-export([encode/1]).

-export_type([interserver_api_dns_record_type/0]).

-type interserver_api_dns_record_type() ::
    #{ 
     }.

encode(#{ 
        }) ->
    #{ 
     }.
