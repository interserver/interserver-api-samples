/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.openapitools.client.model

import org.json4s.JValue

  /**
   * Request body for the domain order flow: putDomains (returns required fields), patchDomains (validates fields), addDomain (places the order). The TLD is resolved from hostname. Per-TLD registrant/contact fields returned by putDomains may be supplied as additional properties on patchDomains/addDomain.
   */
case class DomainOrderRequest(
  /* Fully-qualified domain to register or transfer (e.g. example.com). */
  hostname: String,
  /* Order type. */
  `type`: Option[DomainOrderRequestEnums.`Type`] = None,
  /* Coupon code (addDomain only). */
  coupon: Option[String] = None,
  /* Set to \"enable\" to add Whois privacy (addDomain only). */
  whois_privacy: Option[DomainOrderRequestEnums.WhoisPrivacy] = None
) extends Map[String, org.json4s.JValue]
object DomainOrderRequestEnums {

  sealed trait `Type`
  object `Type` {
    case object Register extends `Type`
    case object Transfer extends `Type`

    import org.json4s._

    implicit object `Type`Serializer extends Serializer[`Type`] {
      def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), `Type`] = {
        case (TypeInfo(clazz, _), json) if classOf[`Type`].isAssignableFrom(clazz) =>
          json match {
            case JString("register") => Register
            case JString("transfer") => Transfer
            case other => throw new MappingException(s"Invalid `Type`: $other")
          }
      }

      def serialize(implicit format: Formats): PartialFunction[Any, JValue] = {
        case Register => JString("register")
        case Transfer => JString("transfer")
      }
    }
  }

  sealed trait WhoisPrivacy
  object WhoisPrivacy {
    case object Enable extends WhoisPrivacy
    case object Disable extends WhoisPrivacy

    import org.json4s._

    implicit object WhoisPrivacySerializer extends Serializer[WhoisPrivacy] {
      def deserialize(implicit format: Formats): PartialFunction[(TypeInfo, JValue), WhoisPrivacy] = {
        case (TypeInfo(clazz, _), json) if classOf[WhoisPrivacy].isAssignableFrom(clazz) =>
          json match {
            case JString("enable") => Enable
            case JString("disable") => Disable
            case other => throw new MappingException(s"Invalid WhoisPrivacy: $other")
          }
      }

      def serialize(implicit format: Formats): PartialFunction[Any, JValue] = {
        case Enable => JString("enable")
        case Disable => JString("disable")
      }
    }
  }
}
