/* tslint:disable */
/* eslint-disable */
/**
 * InterServer Management API
 * # Overview  The InterServer Management API provides programmatic access to manage your InterServer services. Use this REST API to automate provisioning, configuration, and billing operations across your account.  The API covers the following service categories: - [Domains](https://www.interserver.net/domains/) — registration, transfers, and DNS management - [Web Hosting](https://www.interserver.net/hosting/) — shared and reseller hosting - [VPS Hosting](https://www.interserver.net/vps/) — virtual private servers - [Dedicated Servers](https://www.interserver.net/dedicated/) — bare metal and [Rapid Deploy Servers](https://www.interserver.net/dedicated/rapid-deploy.html) - [Backups](https://www.interserver.net/storage/) — storage and backup services - Licenses — control panel and software licenses - [Mail](https://www.mail.baby/) — mail delivery services - SSL — certificate provisioning - Billing — invoices, payment methods, and account management  For interactive testing, see the [API documentation](/api-docs/).  # Authentication  Most endpoints require authentication. Two methods are supported:  ## API Key (Preferred)  Generate an API key from the [Account Security](https://my.interserver.net/account_security) page on [my.interserver.net](https://my.interserver.net/). Pass it in the `X-API-KEY` request header:  ``` X-API-KEY: your-api-key-here ```  ## Session-Based Authentication  Alternatively, authenticate by creating a session:  1. **Log in** — Send a `POST` request to `/login` with your account credentials. The response includes a session identifier. 2. **Pass the session ID** — Include the session identifier in the `sessionid` header on subsequent requests:  ``` sessionid: your-session-id-here ```  API key authentication is recommended for most integrations as it does not expire and avoids the overhead of session management.  
 *
 * The version of the OpenAPI document: 0.9.0
 * Contact: support@interserver.net
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type AccountInfo,
    AccountInfoFromJSON,
    AccountInfoToJSON,
} from '../models/AccountInfo';
import {
    type GenericResponse,
    GenericResponseFromJSON,
    GenericResponseToJSON,
} from '../models/GenericResponse';
import {
    type GetAccountInfo401Response,
    GetAccountInfo401ResponseFromJSON,
    GetAccountInfo401ResponseToJSON,
} from '../models/GetAccountInfo401Response';
import {
    type GetAccountTfaSetup200Response,
    GetAccountTfaSetup200ResponseFromJSON,
    GetAccountTfaSetup200ResponseToJSON,
} from '../models/GetAccountTfaSetup200Response';
import {
    type Home,
    HomeFromJSON,
    HomeToJSON,
} from '../models/Home';
import {
    type IpLimitRange,
    IpLimitRangeFromJSON,
    IpLimitRangeToJSON,
} from '../models/IpLimitRange';
import {
    type SearchAutocompleteResponse,
    SearchAutocompleteResponseFromJSON,
    SearchAutocompleteResponseToJSON,
} from '../models/SearchAutocompleteResponse';
import {
    type SuccessTextResponse,
    SuccessTextResponseFromJSON,
    SuccessTextResponseToJSON,
} from '../models/SuccessTextResponse';
import {
    type TextResponse,
    TextResponseFromJSON,
    TextResponseToJSON,
} from '../models/TextResponse';

export interface DeleteAccountOauthNameRequest {
    name: string;
}

export interface DeleteIpLimitRequest {
    ipLimitRange?: IpLimitRange;
}

export interface LogoutAccountOauthRequest {
    name: string;
}

export interface UpdateAccountFeaturesRequest {
    disableReset?: number;
    disableReinstall?: number;
}

export interface UpdateAccountInfoRequest {
    name: string;
    address: string;
    city: string;
    state: string;
    zip: string;
    country: string;
    phone: string;
    company?: string;
    address2?: string;
    locale?: string;
    emailInvoices?: string;
    emailAbuse?: string;
    disableReset?: boolean;
    disableReinstall?: boolean;
    disableServerNotifications?: boolean;
    disableEmailNotifications?: boolean;
    gstin?: string;
}

export interface UpdateAccountIpLimitsRequest {
    start: string;
    end: string;
}

export interface UpdateAccountPasswordRequest {
    password: string;
}

export interface UpdateAccountSshKeyRequest {
    sshKey?: string;
}

export interface UpdateAccountTfaRequest {
    _2faGoogleCode: string;
}

/**
 * 
 */
export class AccountApi extends runtime.BaseAPI {

    /**
     * Creates request options for deleteAccountOauthName without sending the request
     */
    async deleteAccountOauthNameRequestOpts(requestParameters: DeleteAccountOauthNameRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling deleteAccountOauthName().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/oauth/{name}`;
        urlPath = urlPath.replace('{name}', encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * DESTRUCTIVE: removes the linked provider\'s tokens from `accounts_ext` (rows where `account_key` IN (`{name}_id`,`{name}_url`)). After unlinking, that provider can no longer be used to log in or pre-fill profile data — the user must log in via password (and 2FA if enabled). Path param: `name` (case-insensitive provider key, e.g. `google`, `github`, `facebook`) — must be present in `getOauthConfig().providers`. No request body. Use when the customer wants to revoke a previously authorized social-login. Returns `{success:true, text:\'OAuth Provider Unlinked.\'}`. Errors: 400 `Invalid Provider Name.` if `name` not configured; 401 unauthenticated. Sibling ops: `logoutAccountOauth`, `getAccountInfo`, `updateAccountPassword`.
     * Unlink a third-party OAuth/social provider (Google, GitHub, etc.) from the account
     */
    async deleteAccountOauthNameRaw(requestParameters: DeleteAccountOauthNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.deleteAccountOauthNameRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: removes the linked provider\'s tokens from `accounts_ext` (rows where `account_key` IN (`{name}_id`,`{name}_url`)). After unlinking, that provider can no longer be used to log in or pre-fill profile data — the user must log in via password (and 2FA if enabled). Path param: `name` (case-insensitive provider key, e.g. `google`, `github`, `facebook`) — must be present in `getOauthConfig().providers`. No request body. Use when the customer wants to revoke a previously authorized social-login. Returns `{success:true, text:\'OAuth Provider Unlinked.\'}`. Errors: 400 `Invalid Provider Name.` if `name` not configured; 401 unauthenticated. Sibling ops: `logoutAccountOauth`, `getAccountInfo`, `updateAccountPassword`.
     * Unlink a third-party OAuth/social provider (Google, GitHub, etc.) from the account
     */
    async deleteAccountOauthName(requestParameters: DeleteAccountOauthNameRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.deleteAccountOauthNameRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteAccountTfa without sending the request
     */
    async deleteAccountTfaRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/2fa`;

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * DESTRUCTIVE: removes the 2FA secret from `account_security` and clears the in-session secret cache. After success, only password authentication is required for future logins — security posture drops materially. No body, no path params. Use when the customer has lost their authenticator device or wants to re-enroll from scratch (call this, then `getAccountTfaSetup` -> `updateAccountTfa`). Returns `{success:true, text:\'Google Two Factor Authentication is disabled successfully!\'}`. Errors: 401 unauthenticated. Caveat: existing sessions remain valid; rotate `updateAccountPassword` if you suspect credential compromise. Sibling ops: `getAccountTfaSetup`, `updateAccountTfa`, `updateAccountPassword`.
     * Disable two-factor authentication and remove the TOTP secret
     */
    async deleteAccountTfaRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.deleteAccountTfaRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: removes the 2FA secret from `account_security` and clears the in-session secret cache. After success, only password authentication is required for future logins — security posture drops materially. No body, no path params. Use when the customer has lost their authenticator device or wants to re-enroll from scratch (call this, then `getAccountTfaSetup` -> `updateAccountTfa`). Returns `{success:true, text:\'Google Two Factor Authentication is disabled successfully!\'}`. Errors: 401 unauthenticated. Caveat: existing sessions remain valid; rotate `updateAccountPassword` if you suspect credential compromise. Sibling ops: `getAccountTfaSetup`, `updateAccountTfa`, `updateAccountPassword`.
     * Disable two-factor authentication and remove the TOTP secret
     */
    async deleteAccountTfa(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.deleteAccountTfaRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteIpLimit without sending the request
     */
    async deleteIpLimitRequestOpts(requestParameters: DeleteIpLimitRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/iplimits`;

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: IpLimitRangeToJSON(requestParameters['ipLimitRange']),
        };
    }

    /**
     * DESTRUCTIVE: deletes the matching `{start, end}` entry from `accounts.session_limit`. Method is PATCH (not DELETE) because the path collides with `updateAccountIpLimits`. Body: `{start, end}` — must exactly match an existing range (trim-equal on both bounds). Behaviour: if removing this range would leave an empty list, IP limiting is disabled and the account becomes accessible from any IP. If ranges remain but none cover the caller\'s source IP, the server auto-injects a /32 for the caller to prevent self-lockout (response text warns). MCP callers bypass via `X-API-APP: 1` header. Returns `{success:true, text:\'IP Range deleted.\'}`. Errors: 400/422 `Invalid IP Address` if `start`/`end` aren\'t valid IPs; 401 unauthenticated. Sibling ops: `updateAccountIpLimits`, `getAccountInfo`.
     * Remove one IP range from the account allow-list (PATCH on /account/iplimits)
     */
    async deleteIpLimitRaw(requestParameters: DeleteIpLimitRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GenericResponse>> {
        const requestOptions = await this.deleteIpLimitRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GenericResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: deletes the matching `{start, end}` entry from `accounts.session_limit`. Method is PATCH (not DELETE) because the path collides with `updateAccountIpLimits`. Body: `{start, end}` — must exactly match an existing range (trim-equal on both bounds). Behaviour: if removing this range would leave an empty list, IP limiting is disabled and the account becomes accessible from any IP. If ranges remain but none cover the caller\'s source IP, the server auto-injects a /32 for the caller to prevent self-lockout (response text warns). MCP callers bypass via `X-API-APP: 1` header. Returns `{success:true, text:\'IP Range deleted.\'}`. Errors: 400/422 `Invalid IP Address` if `start`/`end` aren\'t valid IPs; 401 unauthenticated. Sibling ops: `updateAccountIpLimits`, `getAccountInfo`.
     * Remove one IP range from the account allow-list (PATCH on /account/iplimits)
     */
    async deleteIpLimit(requestParameters: DeleteIpLimitRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GenericResponse> {
        const response = await this.deleteIpLimitRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAccountInfo without sending the request
     */
    async getAccountInfoRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Use to render the account-settings page or to verify current state before mutating with `updateAccountInfo`. No body, no path params. Returns: full profile (name, company, address1/2, city, state, zip, country, phone, email_invoices, email_abuse, gstin, locale, timezone), masked credit-card list (last-4 digits only — full PAN never returned), OAuth provider config (with secret keys stripped), feature toggles (`disable_reset`, `disable_reinstall`, `disable_*_notifications`), gravatar URL, language, country->currency map, and `enableLocales`/`enableCurrencies` UI flags. Timezone defaults to IP-derived value if unset, falling back to America/New_York. Errors: 401 if session invalid or expired. Sibling ops: `updateAccountInfo`, `getAccountTfaSetup`, `updateAccountFeatures`, `updateAccountIpLimits`.
     * Read full account profile, billing address, and security settings
     */
    async getAccountInfoRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AccountInfo>> {
        const requestOptions = await this.getAccountInfoRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AccountInfoFromJSON(jsonValue));
    }

    /**
     * Use to render the account-settings page or to verify current state before mutating with `updateAccountInfo`. No body, no path params. Returns: full profile (name, company, address1/2, city, state, zip, country, phone, email_invoices, email_abuse, gstin, locale, timezone), masked credit-card list (last-4 digits only — full PAN never returned), OAuth provider config (with secret keys stripped), feature toggles (`disable_reset`, `disable_reinstall`, `disable_*_notifications`), gravatar URL, language, country->currency map, and `enableLocales`/`enableCurrencies` UI flags. Timezone defaults to IP-derived value if unset, falling back to America/New_York. Errors: 401 if session invalid or expired. Sibling ops: `updateAccountInfo`, `getAccountTfaSetup`, `updateAccountFeatures`, `updateAccountIpLimits`.
     * Read full account profile, billing address, and security settings
     */
    async getAccountInfo(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AccountInfo> {
        const response = await this.getAccountInfoRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAccountTfaSetup without sending the request
     */
    async getAccountTfaSetupRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/2fa`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Use as step 1 of 2FA enrollment. The 160-bit secret is generated on first call and cached in the session until the user completes (or abandons) setup. No body, no path params. Returns `{2fa_google_key, 2fa_google_split}` — render `2fa_google_key` as a QR code (otpauth://totp/My.InterServer:LID?secret=KEY) and display `2fa_google_split` (key chunked into 4-char groups, space-separated) for manual entry. After the user types the 6-digit code from their app, finalize enrollment with `updateAccountTfa`. Calling this multiple times before enrolling reuses the same in-session secret. Errors: 401 if session invalid. Sibling ops: `updateAccountTfa` (verify & enable), `deleteAccountTfa` (disable).
     * Fetch TOTP secret to enroll a 2FA authenticator app (Google Authenticator etc.)
     */
    async getAccountTfaSetupRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetAccountTfaSetup200Response>> {
        const requestOptions = await this.getAccountTfaSetupRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetAccountTfaSetup200ResponseFromJSON(jsonValue));
    }

    /**
     * Use as step 1 of 2FA enrollment. The 160-bit secret is generated on first call and cached in the session until the user completes (or abandons) setup. No body, no path params. Returns `{2fa_google_key, 2fa_google_split}` — render `2fa_google_key` as a QR code (otpauth://totp/My.InterServer:LID?secret=KEY) and display `2fa_google_split` (key chunked into 4-char groups, space-separated) for manual entry. After the user types the 6-digit code from their app, finalize enrollment with `updateAccountTfa`. Calling this multiple times before enrolling reuses the same in-session secret. Errors: 401 if session invalid. Sibling ops: `updateAccountTfa` (verify & enable), `deleteAccountTfa` (disable).
     * Fetch TOTP secret to enroll a 2FA authenticator app (Google Authenticator etc.)
     */
    async getAccountTfaSetup(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetAccountTfaSetup200Response> {
        const response = await this.getAccountTfaSetupRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getHome without sending the request
     */
    async getHomeRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/home`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Use to render the post-login client portal home/dashboard. No body, no params. Returns the structure produced by `getClientHomeData()` — counts of active services per module (vps, webhosting, domains, mail, ssl, licenses, backups, floating_ips, scrub_ips, quickservers, servers), recent invoices, payment due alerts, ticket activity summaries, abuse/maintenance announcements, and account-level banners. Designed for one-shot dashboard hydration so individual modules don\'t each issue list calls. Cached implementation lives in `function_requirements(\'client_home\')` -> `getClientHomeData()`. Errors: 401 if session is invalid or expired (unauthenticated). Sibling ops: `getSearch` (autocomplete), `getAccountInfo`, plus per-module list ops like `getVpsList`, `getDomainsList`, `getBillingInvoices`.
     * Aggregate dashboard payload — service counts, recent activity, alerts
     */
    async getHomeRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Home>> {
        const requestOptions = await this.getHomeRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => HomeFromJSON(jsonValue));
    }

    /**
     * Use to render the post-login client portal home/dashboard. No body, no params. Returns the structure produced by `getClientHomeData()` — counts of active services per module (vps, webhosting, domains, mail, ssl, licenses, backups, floating_ips, scrub_ips, quickservers, servers), recent invoices, payment due alerts, ticket activity summaries, abuse/maintenance announcements, and account-level banners. Designed for one-shot dashboard hydration so individual modules don\'t each issue list calls. Cached implementation lives in `function_requirements(\'client_home\')` -> `getClientHomeData()`. Errors: 401 if session is invalid or expired (unauthenticated). Sibling ops: `getSearch` (autocomplete), `getAccountInfo`, plus per-module list ops like `getVpsList`, `getDomainsList`, `getBillingInvoices`.
     * Aggregate dashboard payload — service counts, recent activity, alerts
     */
    async getHome(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Home> {
        const response = await this.getHomeRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getSearch without sending the request
     */
    async getSearchRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/search`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Use to power the global search box in the client portal — typeahead across services, domains, hostnames, IPs, and ticket subjects scoped to the current account (cross-account leakage is impossible). No body, no path params. Query string is conventionally `q=` though the underlying `getSearchAutoComplete($custid)` may match against multiple fields. Returns a `SearchAutocompleteResponse` object grouping hits by category (vps, domains, websites, mail, tickets, invoices, etc.) so the UI can render section headers. Optimized for low latency — does NOT replace per-module list ops for paginated browsing. Errors: 401 unauthenticated. Sibling ops: `getHome`, `getAccountInfo`, plus per-module list ops (`getVpsList`, `getDomainsList`, `getMailList`, `getTicketsList`).
     * Global autocomplete across the caller\'s services, domains, and records
     */
    async getSearchRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchAutocompleteResponse>> {
        const requestOptions = await this.getSearchRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchAutocompleteResponseFromJSON(jsonValue));
    }

    /**
     * Use to power the global search box in the client portal — typeahead across services, domains, hostnames, IPs, and ticket subjects scoped to the current account (cross-account leakage is impossible). No body, no path params. Query string is conventionally `q=` though the underlying `getSearchAutoComplete($custid)` may match against multiple fields. Returns a `SearchAutocompleteResponse` object grouping hits by category (vps, domains, websites, mail, tickets, invoices, etc.) so the UI can render section headers. Optimized for low latency — does NOT replace per-module list ops for paginated browsing. Errors: 401 unauthenticated. Sibling ops: `getHome`, `getAccountInfo`, plus per-module list ops (`getVpsList`, `getDomainsList`, `getMailList`, `getTicketsList`).
     * Global autocomplete across the caller\'s services, domains, and records
     */
    async getSearch(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchAutocompleteResponse> {
        const response = await this.getSearchRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for logout without sending the request
     */
    async logoutRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/logout`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * DESTRUCTIVE: invalidates the caller\'s session record and appsession bag. After this returns the session id can no longer authenticate requests; the client must discard it and prompt the user to log in again. Idempotent — calling with an already-invalid session returns `200` (no-op when `App::accounts()->data` is empty). API keys (`updateAccountApiKey`) and persistent OAuth links are NOT affected — only this session token. Sibling ops: `updateAccountPassword`, `updateAccountApiKey`, `logoutAccountOauth`, `deleteAccountOauthName`.  **Path/Query/Body:** None.  **Returns:** `{ success: true, text: \'Logged Out\' }`.  **Side effects:** calls `App::session()->destroy()` only when `api_check_auth_limits()` passes for the current account, so a locked account is short-circuited gracefully without further error.  **Auth:** Session/API key.  **Errors:** - `401` — only on a completely malformed auth header.  **Related calls:** - **Re-login:** `submitLogin` or `getOauthRedirect`. - **Per-provider OAuth sign-out (does NOT invalidate the session):** `logoutAccountOauth`. 
     * Destroy the current API/web session — token becomes unusable
     */
    async logoutRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.logoutRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: invalidates the caller\'s session record and appsession bag. After this returns the session id can no longer authenticate requests; the client must discard it and prompt the user to log in again. Idempotent — calling with an already-invalid session returns `200` (no-op when `App::accounts()->data` is empty). API keys (`updateAccountApiKey`) and persistent OAuth links are NOT affected — only this session token. Sibling ops: `updateAccountPassword`, `updateAccountApiKey`, `logoutAccountOauth`, `deleteAccountOauthName`.  **Path/Query/Body:** None.  **Returns:** `{ success: true, text: \'Logged Out\' }`.  **Side effects:** calls `App::session()->destroy()` only when `api_check_auth_limits()` passes for the current account, so a locked account is short-circuited gracefully without further error.  **Auth:** Session/API key.  **Errors:** - `401` — only on a completely malformed auth header.  **Related calls:** - **Re-login:** `submitLogin` or `getOauthRedirect`. - **Per-provider OAuth sign-out (does NOT invalidate the session):** `logoutAccountOauth`. 
     * Destroy the current API/web session — token becomes unusable
     */
    async logout(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.logoutRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for logoutAccountOauth without sending the request
     */
    async logoutAccountOauthRequestOpts(requestParameters: LogoutAccountOauthRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling logoutAccountOauth().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/oauth/{name}/logout`;
        urlPath = urlPath.replace('{name}', encodeURIComponent(String(requestParameters['name'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft de-authorization for a linked OAuth provider — terminates only the upstream provider session/cookie state. The account-level link in `accounts_ext` is preserved, so the user can log back in with that provider without re-linking. Path param: `name` (provider key, e.g. `google`, `github`). No request body. Use when forcing a fresh consent screen on next OAuth login, or after the user reports a stuck/stale provider session. NOT a substitute for `Logout` (which kills the MyAdmin session) and NOT a substitute for `deleteAccountOauthName` (which permanently severs the link). Returns `{success:true, text:\'OAuth Provider Logged Out.\'}`. Errors: 401 unauthenticated. Sibling ops: `deleteAccountOauthName`, `Logout`, `getAccountInfo`.
     * Sign out of the upstream OAuth provider session (does not unlink the account)
     */
    async logoutAccountOauthRaw(requestParameters: LogoutAccountOauthRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.logoutAccountOauthRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * Soft de-authorization for a linked OAuth provider — terminates only the upstream provider session/cookie state. The account-level link in `accounts_ext` is preserved, so the user can log back in with that provider without re-linking. Path param: `name` (provider key, e.g. `google`, `github`). No request body. Use when forcing a fresh consent screen on next OAuth login, or after the user reports a stuck/stale provider session. NOT a substitute for `Logout` (which kills the MyAdmin session) and NOT a substitute for `deleteAccountOauthName` (which permanently severs the link). Returns `{success:true, text:\'OAuth Provider Logged Out.\'}`. Errors: 401 unauthenticated. Sibling ops: `deleteAccountOauthName`, `Logout`, `getAccountInfo`.
     * Sign out of the upstream OAuth provider session (does not unlink the account)
     */
    async logoutAccountOauth(requestParameters: LogoutAccountOauthRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.logoutAccountOauthRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountApiKey without sending the request
     */
    async updateAccountApiKeyRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }


        let urlPath = `/account/apikey`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * DESTRUCTIVE: generates a new 128-character random API key and overwrites the existing entry in `account_security` (type `api_key`, label `default`). The OLD key stops working the moment this returns — any scripts, MCP clients, or CI jobs using the previous key will start receiving 401 until updated. No body, no path params. Returns `{success:true, text:NEW_KEY}` — the plaintext key is returned ONCE in this response and is not retrievable later (only stored hashed-equivalent server-side for verification). Store immediately in a secret manager. Use after suspected credential leak, employee offboarding, or routine rotation. Errors: 401 unauthenticated. Sibling ops: `updateAccountPassword`, `updateAccountIpLimits`, `Logout`.
     * Rotate the account\'s REST/MCP API key — old key is invalidated immediately
     */
    async updateAccountApiKeyRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountApiKeyRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: generates a new 128-character random API key and overwrites the existing entry in `account_security` (type `api_key`, label `default`). The OLD key stops working the moment this returns — any scripts, MCP clients, or CI jobs using the previous key will start receiving 401 until updated. No body, no path params. Returns `{success:true, text:NEW_KEY}` — the plaintext key is returned ONCE in this response and is not retrievable later (only stored hashed-equivalent server-side for verification). Store immediately in a secret manager. Use after suspected credential leak, employee offboarding, or routine rotation. Errors: 401 unauthenticated. Sibling ops: `updateAccountPassword`, `updateAccountIpLimits`, `Logout`.
     * Rotate the account\'s REST/MCP API key — old key is invalidated immediately
     */
    async updateAccountApiKey(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountApiKeyRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountFeatures without sending the request
     */
    async updateAccountFeaturesRequestOpts(requestParameters: UpdateAccountFeaturesRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['disableReset'] != null) {
            formParams.append('disable_reset', requestParameters['disableReset'] as any);
        }

        if (requestParameters['disableReinstall'] != null) {
            formParams.append('disable_reinstall', requestParameters['disableReinstall'] as any);
        }


        let urlPath = `/account/features`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Updates account-level feature flags that gate destructive service operations across every VPS / dedicated / QuickServer the customer owns. Useful for production accounts that want belt-and-suspenders protection against accidental reinstalls or root-password resets via the panel/API. Changes take effect immediately for all subsequent service operations. Sibling ops: `getAccountInfo`, `updateAccountInfo`, `updateAccountIpLimits`.  **Body fields:** - `disable_reset` (bool, optional) — when `true`, blocks server / VPS root-password resets account-wide. - `disable_reinstall` (bool, optional) — when `true`, blocks OS reinstalls account-wide.  Submit either or both. Flags absent from the request default to `0` for the comparison and only persist if their value differs from the current stored value.  **Returns:** `{ success: true, text }`.  **Errors:** - `401` — unauthenticated. - `400` / `422` — `Nothing to update` when neither flag\'s value differs from current. 
     * Toggle account-wide safety locks for password reset and OS reinstall
     */
    async updateAccountFeaturesRaw(requestParameters: UpdateAccountFeaturesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountFeaturesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * Updates account-level feature flags that gate destructive service operations across every VPS / dedicated / QuickServer the customer owns. Useful for production accounts that want belt-and-suspenders protection against accidental reinstalls or root-password resets via the panel/API. Changes take effect immediately for all subsequent service operations. Sibling ops: `getAccountInfo`, `updateAccountInfo`, `updateAccountIpLimits`.  **Body fields:** - `disable_reset` (bool, optional) — when `true`, blocks server / VPS root-password resets account-wide. - `disable_reinstall` (bool, optional) — when `true`, blocks OS reinstalls account-wide.  Submit either or both. Flags absent from the request default to `0` for the comparison and only persist if their value differs from the current stored value.  **Returns:** `{ success: true, text }`.  **Errors:** - `401` — unauthenticated. - `400` / `422` — `Nothing to update` when neither flag\'s value differs from current. 
     * Toggle account-wide safety locks for password reset and OS reinstall
     */
    async updateAccountFeatures(requestParameters: UpdateAccountFeaturesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountFeaturesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountInfo without sending the request
     */
    async updateAccountInfoRequestOpts(requestParameters: UpdateAccountInfoRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['name'] == null) {
            throw new runtime.RequiredError(
                'name',
                'Required parameter "name" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['address'] == null) {
            throw new runtime.RequiredError(
                'address',
                'Required parameter "address" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['city'] == null) {
            throw new runtime.RequiredError(
                'city',
                'Required parameter "city" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['state'] == null) {
            throw new runtime.RequiredError(
                'state',
                'Required parameter "state" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['zip'] == null) {
            throw new runtime.RequiredError(
                'zip',
                'Required parameter "zip" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['country'] == null) {
            throw new runtime.RequiredError(
                'country',
                'Required parameter "country" was null or undefined when calling updateAccountInfo().'
            );
        }

        if (requestParameters['phone'] == null) {
            throw new runtime.RequiredError(
                'phone',
                'Required parameter "phone" was null or undefined when calling updateAccountInfo().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['name'] != null) {
            formParams.append('name', requestParameters['name'] as any);
        }

        if (requestParameters['company'] != null) {
            formParams.append('company', requestParameters['company'] as any);
        }

        if (requestParameters['address'] != null) {
            formParams.append('address', requestParameters['address'] as any);
        }

        if (requestParameters['address2'] != null) {
            formParams.append('address2', requestParameters['address2'] as any);
        }

        if (requestParameters['city'] != null) {
            formParams.append('city', requestParameters['city'] as any);
        }

        if (requestParameters['state'] != null) {
            formParams.append('state', requestParameters['state'] as any);
        }

        if (requestParameters['zip'] != null) {
            formParams.append('zip', requestParameters['zip'] as any);
        }

        if (requestParameters['country'] != null) {
            formParams.append('country', requestParameters['country'] as any);
        }

        if (requestParameters['phone'] != null) {
            formParams.append('phone', requestParameters['phone'] as any);
        }

        if (requestParameters['locale'] != null) {
            formParams.append('locale', requestParameters['locale'] as any);
        }

        if (requestParameters['emailInvoices'] != null) {
            formParams.append('email_invoices', requestParameters['emailInvoices'] as any);
        }

        if (requestParameters['emailAbuse'] != null) {
            formParams.append('email_abuse', requestParameters['emailAbuse'] as any);
        }

        if (requestParameters['disableReset'] != null) {
            formParams.append('disable_reset', requestParameters['disableReset'] as any);
        }

        if (requestParameters['disableReinstall'] != null) {
            formParams.append('disable_reinstall', requestParameters['disableReinstall'] as any);
        }

        if (requestParameters['disableServerNotifications'] != null) {
            formParams.append('disable_server_notifications', requestParameters['disableServerNotifications'] as any);
        }

        if (requestParameters['disableEmailNotifications'] != null) {
            formParams.append('disable_email_notifications', requestParameters['disableEmailNotifications'] as any);
        }

        if (requestParameters['gstin'] != null) {
            formParams.append('gstin', requestParameters['gstin'] as any);
        }


        let urlPath = `/account`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Use to change the customer\'s name, company, mailing address, phone, GSTIN, locale, timezone, or notification-email overrides (`email_invoices`, `email_abuse`). Submit only fields you want to change — partial updates supported. Required (must be non-empty if sent): `name`, `country`, `address`, `city`, `state`, `zip`, `phone`. Phone is normalized: parens, dashes, underscores stripped. Timezone must be a valid IANA identifier (e.g. `America/New_York`). Side effects: triggers FraudRecord + MaxMind risk re-scoring on first save, updates Kayako helpdesk username when `name` changes. Returns `{success:true}`. Errors: 401 missing-required field; 422 invalid timezone or empty payload. Sibling ops: `getAccountInfo`, `updateAccountFeatures`, `updateAccountPassword`.
     * Update contact and billing-address fields on the customer profile
     */
    async updateAccountInfoRaw(requestParameters: UpdateAccountInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountInfoRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * Use to change the customer\'s name, company, mailing address, phone, GSTIN, locale, timezone, or notification-email overrides (`email_invoices`, `email_abuse`). Submit only fields you want to change — partial updates supported. Required (must be non-empty if sent): `name`, `country`, `address`, `city`, `state`, `zip`, `phone`. Phone is normalized: parens, dashes, underscores stripped. Timezone must be a valid IANA identifier (e.g. `America/New_York`). Side effects: triggers FraudRecord + MaxMind risk re-scoring on first save, updates Kayako helpdesk username when `name` changes. Returns `{success:true}`. Errors: 401 missing-required field; 422 invalid timezone or empty payload. Sibling ops: `getAccountInfo`, `updateAccountFeatures`, `updateAccountPassword`.
     * Update contact and billing-address fields on the customer profile
     */
    async updateAccountInfo(requestParameters: UpdateAccountInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountInfoRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountIpLimits without sending the request
     */
    async updateAccountIpLimitsRequestOpts(requestParameters: UpdateAccountIpLimitsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['start'] == null) {
            throw new runtime.RequiredError(
                'start',
                'Required parameter "start" was null or undefined when calling updateAccountIpLimits().'
            );
        }

        if (requestParameters['end'] == null) {
            throw new runtime.RequiredError(
                'end',
                'Required parameter "end" was null or undefined when calling updateAccountIpLimits().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['start'] != null) {
            formParams.append('start', requestParameters['start'] as any);
        }

        if (requestParameters['end'] != null) {
            formParams.append('end', requestParameters['end'] as any);
        }


        let urlPath = `/account/iplimits`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * DESTRUCTIVE / LOCKOUT-RISK: appends an IP range to `accounts.session_limit`. Once ANY range exists, all `/apiv2` and panel access is restricted to matching source IPs. Body: `{start, end, restrict?}` — both IPv4 dotted-quad; `restrict` is `Web & API` (default) or `Only API`. Safety net: server checks the caller\'s IP against the resulting list and auto-appends a /32 for the caller if not already covered (response text warns about this). The MCP server sets header `X-API-APP: 1` which short-circuits the IP check entirely (see `api_check_auth_limits()`), so MCP tools keep working. Caveats: `192.168.1.0`-`192.168.1.255` is rejected as a placeholder. Returns `{success:true, text}`. Errors: 400/422 `Invalid IP Address`; 401 unauthenticated. Sibling ops: `deleteIpLimit`, `getAccountInfo`.
     * Add an IP CIDR/range to the account\'s API+web allow-list (lockout-safe)
     */
    async updateAccountIpLimitsRaw(requestParameters: UpdateAccountIpLimitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountIpLimitsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE / LOCKOUT-RISK: appends an IP range to `accounts.session_limit`. Once ANY range exists, all `/apiv2` and panel access is restricted to matching source IPs. Body: `{start, end, restrict?}` — both IPv4 dotted-quad; `restrict` is `Web & API` (default) or `Only API`. Safety net: server checks the caller\'s IP against the resulting list and auto-appends a /32 for the caller if not already covered (response text warns about this). The MCP server sets header `X-API-APP: 1` which short-circuits the IP check entirely (see `api_check_auth_limits()`), so MCP tools keep working. Caveats: `192.168.1.0`-`192.168.1.255` is rejected as a placeholder. Returns `{success:true, text}`. Errors: 400/422 `Invalid IP Address`; 401 unauthenticated. Sibling ops: `deleteIpLimit`, `getAccountInfo`.
     * Add an IP CIDR/range to the account\'s API+web allow-list (lockout-safe)
     */
    async updateAccountIpLimits(requestParameters: UpdateAccountIpLimitsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountIpLimitsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountPassword without sending the request
     */
    async updateAccountPasswordRequestOpts(requestParameters: UpdateAccountPasswordRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['password'] == null) {
            throw new runtime.RequiredError(
                'password',
                'Required parameter "password" was null or undefined when calling updateAccountPassword().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['password'] != null) {
            formParams.append('password', requestParameters['password'] as any);
        }


        let urlPath = `/account/password`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * DESTRUCTIVE: changes the account login password and invalidates all OTHER active sessions for this account. The current caller\'s session is preserved; API keys generated via `updateAccountApiKey` remain valid. Sibling ops: `updateAccountApiKey`, `Logout`, `updateAccountTfa`.  **Body fields:** - `currentpassword` (string, required) — verified via `auth::authenticate`. - `password` (string, required) — must pass `valid_password()` — 8–50 chars, at least one uppercase, one lowercase, one digit, and one of `_~-!@#$%^&*`. - `password2` (string, required) — must equal `password`.  **Returns:** `{ success: bool }` — flash messages on the response capture per-field errors.  **Side effects:** - Persists `md5(password)` to `accounts.account_passwd`. - Sends `password_change_notify.tpl` email to the account login id. - Destroys all other sessions for this account row-by-row.  **Errors:** - `401` — unauthenticated. - Flash `Current login password is mismatching` — bad `currentpassword`. - Flash `Confirm Password is mismatching` — `password` ≠ `password2`. - Flash password-policy violation message. 
     * Change the account login password (verifies current, kills other sessions)
     */
    async updateAccountPasswordRaw(requestParameters: UpdateAccountPasswordRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TextResponse>> {
        const requestOptions = await this.updateAccountPasswordRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TextResponseFromJSON(jsonValue));
    }

    /**
     * DESTRUCTIVE: changes the account login password and invalidates all OTHER active sessions for this account. The current caller\'s session is preserved; API keys generated via `updateAccountApiKey` remain valid. Sibling ops: `updateAccountApiKey`, `Logout`, `updateAccountTfa`.  **Body fields:** - `currentpassword` (string, required) — verified via `auth::authenticate`. - `password` (string, required) — must pass `valid_password()` — 8–50 chars, at least one uppercase, one lowercase, one digit, and one of `_~-!@#$%^&*`. - `password2` (string, required) — must equal `password`.  **Returns:** `{ success: bool }` — flash messages on the response capture per-field errors.  **Side effects:** - Persists `md5(password)` to `accounts.account_passwd`. - Sends `password_change_notify.tpl` email to the account login id. - Destroys all other sessions for this account row-by-row.  **Errors:** - `401` — unauthenticated. - Flash `Current login password is mismatching` — bad `currentpassword`. - Flash `Confirm Password is mismatching` — `password` ≠ `password2`. - Flash password-policy violation message. 
     * Change the account login password (verifies current, kills other sessions)
     */
    async updateAccountPassword(requestParameters: UpdateAccountPasswordRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TextResponse> {
        const response = await this.updateAccountPasswordRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountSshKey without sending the request
     */
    async updateAccountSshKeyRequestOpts(requestParameters: UpdateAccountSshKeyRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['sshKey'] != null) {
            formParams.append('ssh_key', requestParameters['sshKey'] as any);
        }


        let urlPath = `/account/sshkey`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Stores or replaces the SSH public key on `account_security` (type `ssh_key`, label `default`). On future VPS, dedicated server, or quickserver orders the activation flow can install this key into `~/.ssh/authorized_keys` for the root/sudo user, eliminating password-based SSH for the initial provisioning. Body: `{sshKey:string}` — full single-line OpenSSH public key (ssh-rsa/ssh-ed25519/ecdsa-sha2-* + base64 + optional comment). Newlines are stripped on save. Existing servers are NOT retroactively updated — only new orders pick this up. Use to set up key-based access ahead of order activation, or to rotate the canonical key. Returns `{success:true, text:\'SSH Keys Updated.\'}`. Errors: 401 unauthenticated. Sibling ops: `getAccountInfo`, `updateAccountPassword`, `updateAccountApiKey`.
     * Set the account-level SSH public key auto-installed on new VPS/dedicated orders
     */
    async updateAccountSshKeyRaw(requestParameters: UpdateAccountSshKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountSshKeyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * Stores or replaces the SSH public key on `account_security` (type `ssh_key`, label `default`). On future VPS, dedicated server, or quickserver orders the activation flow can install this key into `~/.ssh/authorized_keys` for the root/sudo user, eliminating password-based SSH for the initial provisioning. Body: `{sshKey:string}` — full single-line OpenSSH public key (ssh-rsa/ssh-ed25519/ecdsa-sha2-* + base64 + optional comment). Newlines are stripped on save. Existing servers are NOT retroactively updated — only new orders pick this up. Use to set up key-based access ahead of order activation, or to rotate the canonical key. Returns `{success:true, text:\'SSH Keys Updated.\'}`. Errors: 401 unauthenticated. Sibling ops: `getAccountInfo`, `updateAccountPassword`, `updateAccountApiKey`.
     * Set the account-level SSH public key auto-installed on new VPS/dedicated orders
     */
    async updateAccountSshKey(requestParameters: UpdateAccountSshKeyRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountSshKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAccountTfa without sending the request
     */
    async updateAccountTfaRequestOpts(requestParameters: UpdateAccountTfaRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['_2faGoogleCode'] == null) {
            throw new runtime.RequiredError(
                '_2faGoogleCode',
                'Required parameter "_2faGoogleCode" was null or undefined when calling updateAccountTfa().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-KEY"] = await this.configuration.apiKey("X-API-KEY"); // apiKeyAuth authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["sessionid"] = await this.configuration.apiKey("sessionid"); // sessionIdHeaderAuth authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
            { contentType: 'application/json' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['_2faGoogleCode'] != null) {
            formParams.append('2fa_google_code', requestParameters['_2faGoogleCode'] as any);
        }


        let urlPath = `/account/2fa`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Use as step 2 of 2FA enrollment, after `getAccountTfaSetup`. Body: `{2fa_google_code:string}` — the 6-digit code currently displayed by the user\'s authenticator app for the secret returned from `getAccountTfaSetup`. On verify success, the secret is persisted to `account_security` (type `2fa_google_key`, label `default`) and ALL OTHER active sessions for this account are invalidated (server destroys appsessions and sessions rows where session_id != current). The current session remains. Subsequent logins will require both password and a fresh TOTP code. Returns `{success:true, text}`. Errors: 401 unauthenticated; 422 `Invalid Code` if the TOTP doesn\'t match (clock skew, wrong app entry, or expired). Sibling ops: `getAccountTfaSetup`, `deleteAccountTfa`.
     * Verify TOTP code and enable two-factor authentication on the account
     */
    async updateAccountTfaRaw(requestParameters: UpdateAccountTfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SuccessTextResponse>> {
        const requestOptions = await this.updateAccountTfaRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SuccessTextResponseFromJSON(jsonValue));
    }

    /**
     * Use as step 2 of 2FA enrollment, after `getAccountTfaSetup`. Body: `{2fa_google_code:string}` — the 6-digit code currently displayed by the user\'s authenticator app for the secret returned from `getAccountTfaSetup`. On verify success, the secret is persisted to `account_security` (type `2fa_google_key`, label `default`) and ALL OTHER active sessions for this account are invalidated (server destroys appsessions and sessions rows where session_id != current). The current session remains. Subsequent logins will require both password and a fresh TOTP code. Returns `{success:true, text}`. Errors: 401 unauthenticated; 422 `Invalid Code` if the TOTP doesn\'t match (clock skew, wrong app entry, or expired). Sibling ops: `getAccountTfaSetup`, `deleteAccountTfa`.
     * Verify TOTP code and enable two-factor authentication on the account
     */
    async updateAccountTfa(requestParameters: UpdateAccountTfaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SuccessTextResponse> {
        const response = await this.updateAccountTfaRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
