//
// DomainOrderRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/// Request body for the domain order flow: putDomains (returns required fields), patchDomains (validates fields), addDomain (places the order). The TLD is resolved from hostname. Per-TLD registrant/contact fields returned by putDomains may be supplied as additional properties on patchDomains/addDomain. 
public struct DomainOrderRequest: Codable {
    public enum ModelType: String, Codable, CaseIterable {
        case register = "register"
        case transfer = "transfer"
    }
    public enum WhoisPrivacy: String, Codable, CaseIterable {
        case enable = "enable"
        case disable = "disable"
    }
    /// Fully-qualified domain to register or transfer (e.g. example.com).
    public var hostname: String
    /// Order type.
    public var type: ModelType? = .register
    /// Coupon code (addDomain only).
    public var coupon: String? = ""
    /// Set to \"enable\" to add Whois privacy (addDomain only).
    public var whoisPrivacy: WhoisPrivacy?

    public init(hostname: String, type: ModelType? = .register, coupon: String? = "", whoisPrivacy: WhoisPrivacy? = nil) {
        self.hostname = hostname
        self.type = type
        self.coupon = coupon
        self.whoisPrivacy = whoisPrivacy
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case hostname
        case type
        case coupon
        case whoisPrivacy = "whois_privacy"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        hostname = try container.decode(String.self, forKey: .hostname)
        type = try container.decodeIfPresent(ModelType.self, forKey: .type)
        coupon = try container.decodeIfPresent(String.self, forKey: .coupon)
        whoisPrivacy = try container.decodeIfPresent(WhoisPrivacy.self, forKey: .whoisPrivacy)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(hostname, forKey: .hostname)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(coupon, forKey: .coupon)
        try container.encodeIfPresent(whoisPrivacy, forKey: .whoisPrivacy)
    }
}
